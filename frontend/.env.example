# Frontend local environment example

# Backend origin used by the Vite /api proxy during local development.
VITE_BACKEND_ORIGIN=http://127.0.0.1:3000

# Optional direct API base if the browser should bypass same-origin /api.
# VITE_API_BASE_URL=http://127.0.0.1:3000/api

