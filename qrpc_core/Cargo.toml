[package]
name = "qrpc-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = { workspace = true }
qrpc-core-ir = { path = "../qrpc_core_ir" }
