# v0.5.0 扩展审计清单

> 基于 2026-05-09 General_Policy 全量合规完成后的补充审计方向 | 6 角度 30 项

---

## A1 — i18n 国际化完整性审计

| # | 检查点 | 方法 | 估时 |
|---|--------|------|:--:|
| A1-1 | 全量 JSX 硬编码中文扫描 | `tools/check-i18n.ps1` 全量运行 | 0.25d |
| A1-2 | `locales/zh-CN.js` / `en-US.js` 填充 | 提取所有 `t()` 调用，生成 key 列表 | 0.5d |
| A1-3 | 日期/数字格式化统一 | 检查 `toLocaleString()` 调用点 → i18n 统一 | 0.25d |
| A1-4 | `<option>` 文本国际化 | 当前 `<option>全部</option>` 等硬编码 | 0.25d |

---

## A2 — 依赖健康度审计

| # | 检查点 | 方法 | 估时 |
|---|--------|------|:--:|
| A2-1 | `npm audit` 全量修复 | `npm audit --audit-level=moderate` → 公开发布阻塞项 | 0.5d |
| A2-2 | Rust 依赖过期检查 | `cargo outdated` 或 `cargo update` 审计 | 0.25d |
| A2-3 | 依赖许可证兼容 | `cargo deny check license` | 0.25d |
| A2-4 | 间接依赖安全 | `cargo tree` 深层次审查 | 0.25d |

---

## A3 — 性能基准审计

| # | 检查点 | 方法 | 估时 |
|---|--------|------|:--:|
| A3-1 | 首屏加载时间重测 | Playwright `perf-first-screen-review.spec.js` 复跑 | 0.25d |
| A3-2 | ReactFlow 画布帧率 | `perf-react-flow-mount-review.spec.js` 复跑 + 节点 50+ 压测 | 0.25d |
| A3-3 | 编译/回测 API 延迟 | `cargo test` benchmark 模式，记录 p50/p95/p99 | 0.25d |
| A3-4 | 前端 bundle 体积基线 | `npx vite build --debug` 分析 + 设置上限 (当前 161KB gzip) | 0.15d |
| A3-5 | 长时间运行内存 | 前端 1h 运行后 heap snapshot 对比 | 0.25d |

---

## A4 — 错误恢复能力审计

| # | 检查点 | 方法 | 估时 |
|---|--------|------|:--:|
| A4-1 | 后端崩溃前端行为 | kill 后端 → 观察前端错误状态 + 重连 | 0.25d |
| A4-2 | API 超时全局策略 | 添加 fetch timeout wrapper (30s 默认) | 0.25d |
| A4-3 | 凭证文件损坏引导 | 前端捕获 503 → 引导用户运行 `credential set` CLI | 0.25d |
| A4-4 | localStorage 满处理 | quota 检测 + 降级提示 | 0.15d |
| A4-5 | 网络断开/恢复 | offline/online 事件监听 + UI 提示 | 0.25d |

---

## A5 — 可访问性 (A11Y) 审计

| # | 检查点 | 方法 | 估时 |
|---|--------|------|:--:|
| A5-1 | 键盘导航完整性 | Tab 遍历所有交互元素，画布节点聚焦 | 0.25d |
| A5-2 | 屏幕阅读器兼容 | 添加 `role` / `aria-label` 到关键区域 | 0.25d |
| A5-3 | 色彩对比度 (WCAG AA) | 低饱和色板 vs AA 对比度标准 | 0.25d |
| A5-4 | 焦点管理 | 页面切换时 `focus` 重置到内容区 | 0.15d |

---

## A6 — CI/CD 管道审计

| # | 检查点 | 方法 | 估时 |
|---|--------|------|:--:|
| A6-1 | Pre-commit hook | `.git/hooks/pre-commit` → `cargo check + vite build` | 0.15d |
| A6-2 | CI 全门禁通过 | `.github/workflows/` 覆盖 cargo test + vitest + build | 0.25d |
| A6-3 | 构建产物签名 | Signature snapshots 集成到 release 流程 | 0.25d |
| A6-4 | 版本号自动化 | `Cargo.toml` / `package.json` 版本同步 | 0.1d |
| A6-5 | 分支保护策略 | GitHub branch protection rules | 0.1d |

---

## 验收总表

| 角度 | 项数 | 估时 | 优先级 | 备注 |
|------|:--:|:--:|:--:|------|
| A1 i18n | 4 | 1.25d | 🔴 P0 | check-i18n.ps1 已有，一键启动 |
| A2 依赖 | 4 | 1.25d | 🔴 P0 | npm audit 是公开发布阻塞项 |
| A3 性能 | 5 | 1.15d | 🟠 P1 | UI 重构后必须复测 |
| A4 错误恢复 | 5 | 1.15d | 🟠 P1 | 直接用户感受 |
| A5 A11Y | 4 | 0.9d | 🟢 P2 | 合规加分，非阻塞 |
| A6 CI/CD | 5 | 0.85d | 🟢 P2 | 团队协作必需 |

**合计：6 角度 30 项，估 6.55 天**

---

## 执行顺序

```
Phase 1 (P0, 2.5d):
  A1-1 → A1-2 (i18n 扫描 + 语言文件)
  A2-1 → A2-2 (npm audit + cargo outdated)

Phase 2 (P1, 2.3d):
  A3-1 → A3-2 (首屏 + 画布性能)
  A4-1 → A4-2 → A4-3 (崩溃/超时/凭证恢复)

Phase 3 (P2, 1.75d):
  A5-1 → A5-2 (键盘 + 屏幕阅读器)
  A6-1 → A6-2 (pre-commit + CI)
```
