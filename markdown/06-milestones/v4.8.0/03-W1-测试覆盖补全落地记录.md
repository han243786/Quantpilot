# v4.8.0 W1 测试覆盖补全落地记录

> 日期: 2026-05-25
> 范围: W1-1 至 W1-5

## 落地结果

| 项 | 已落地内容 | 回归命令 |
|---|---|---|
| W1-1 | `tests/api_run.rs` 新增 7 个 `start_v4_runtime_run_*` API 用例, 覆盖 source 成功、默认 catalog event、graph 直传、initial_event override、缺 source/graph、非 `PaperSimulated` handoff 拒绝、缺 event catalog | `cargo test --test api_run start_v4_runtime_run` |
| W1-2 | `quantscript/src/v4_static_audit.rs` 新增 QSV 诊断码矩阵测试, 覆盖 30 个 QSV 码 | `cargo test -p quantscript v4_static` |
| W1-3 | `frontend/tests/e2e/v4-runtime-contracts.spec.js` 新增 auth capability failure、v4 strategy runtime contract、v4 backtest contract 3 个 browser contract 用例 | `npm.cmd run test:e2e -- tests/e2e/v4-runtime-contracts.spec.js` |
| W1-4 | `frontend/src/components/StrategyCanvas.interaction.test.jsx` 新增画布点击选中、pane 清选、拖拽落点、连线创建、选中删除关联边 5 个交互用例; `StrategyCanvas.focus.test.jsx` 补足 issue focus lane 汇总用例 | `npm.cmd test -- StrategyCanvas.interaction.test.jsx StrategyCanvas.focus.test.jsx` |
| W1-5 | `quantscript/src/v4_static_audit.rs` 生产路径和测试 helper 裸 `.unwrap()` 已替换为带定位语义的 `.expect()` | `rg -n "unwrap\\(" quantscript/src/v4_static_audit.rs` |

## 边界说明

- W1 只补测试和 panic 定位语义, 不扩大 v4 执行端能力范围。
- v4 仍只确保 OKX 单一 provider 切面; 多 provider 适配延后至 v5。
- `PaperSimulated` 继续保持 `provider_order_submission_attached=false`; `PaperActual` 仅用于 OKX demo submit/query/cancel 回执路径。
