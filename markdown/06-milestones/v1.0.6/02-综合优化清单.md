# v1.0.6 综合优化清单

> 基于四维度全量用户困惑点审计 | 79 项发现 | 2026-05-13 | **状态: ✅ 已收口 (79/79, 100%)**

---

## 审计覆盖矩阵

| Agent | 维度 | 审计文件数 | 发现数 |
|:-----:|------|:--:|:--:|
| 1 | 后端错误消息 | 9 | 22 |
| 2 | 前端 UX 困惑点 | 10 | 24 |
| 3 | API 响应设计 | 10 | 18 |
| 4 | QS 编译器诊断 | 17 | 15 |
| **合计** | | **46** | **79** |

---

## 一、S0 阻断级 — 13 项

### S0-1 — graphStore.js 中 capability 状态消息硬编码英文

**严重度**: 🔴 S0 | **违反**: GP §2.1, GP §2.5 | **来源**: Agent 2
**文件**: `frontend/src/store/graphStore.js:130-133, 151-153`

**问题**:

```js
capabilityMessage:
  "Capability fetch failed. Using the latest cached capability snapshot. ...",
// 和
capabilityMessage:
  "Capability fetch failed. Entering safe fallback mode. ...",
```

这两处面向用户的消息是硬编码英文，不经 `t()` 包裹。中文用户在 degraded/error 状态下看到整段英文，完全无法理解。

**方案**:

1. 将英文消息替换为 `t()` 调用
2. 中文键值：能力状态消息，中文默认值保留原中文翻译

**验收**:
- `graphStore.js` 中不再包含硬编码英文 `capabilityMessage` 赋值
- 触发能力加载失败时，中文界面显示中文消息
- `npx vitest run` 全量通过

**估时**: 15min

---

### S0-2 — 跨标签删除同步消息硬编码中文

**严重度**: 🔴 S0 | **违反**: GP §2.5 | **来源**: Agent 2
**文件**: `frontend/src/store/graphStore.js:174`

**问题**:

```js
backendError: "此策略已被另一标签页删除，请返回策略中心。"
```

英文语言环境用户会看到中文错误。

**方案**:

使用 `t()` 包裹。

**验收**:
- `grep "此策略已被另一标签页删除" frontend/src/store/graphStore.js` 仅出现在 `t()` 调用内
- `npx vitest run` 全量通过

**估时**: 5min

---

### S0-3 — QS0609 诊断码冲突

**严重度**: 🔴 S0 | **违反**: GP §2.3 | **来源**: Agent 4
**文件**: `quantscript/src/resolve.rs:1095`、`quantscript/src/analysis.rs:191`

**问题**:

同一个代码 **QS0609** 在两个文件中用于两个完全不同的诊断：

1. `resolve.rs:1095` — **WARNING**: "重复的变量定义 '{}'"
2. `analysis.rs:191` — **ERROR**: "形式化 QuantScript 不支持可执行主干中使用 `.push(...)` 构建可变列表"

用户收到 QS0609 时无法判断实际是哪个错误，诊断查找和 i18n 映射同时失效。

**方案**:

将 `resolve.rs` 中的 WARNING 改用新代码 **QS0613**："重复的变量定义"。`analysis.rs` 中的 ERROR 保留 QS0609。

**验收**:
- `grep -rn "QS0609" quantscript/src/` 只返回 `analysis.rs` 一处
- `grep -rn "QS0613" quantscript/src/` 返回 `resolve.rs` 一处
- `cargo test --workspace` 全量通过
- `tools/check-i18n.ps1` 中更新诊断码列表

**估时**: 15min

---

### S0-4 — QS0502 绕过诊断系统直接用 eprintln!

**严重度**: 🔴 S0 | **违反**: GP §2.1 | **来源**: Agent 4
**文件**: `quantscript/src/lowering/bindings.rs:457-461`

**问题**:

```rust
eprintln!(
    "[QS0502] 指标周期 {} 被截断为整数 {}",
    n,
    *n as usize
);
```

使用 `eprintln!` 直接输出到 stderr，不走 `Diagnostic::warning()` 结构化诊断管道。前端永远收不到此警告，用户可能在 stderr 日志中看到但 miss 掉。

**方案**:

将 `eprintln!` 调用改为 `Diagnostic::warning("QS0502", format!(...), span)`。

**验收**:
- `grep "QS0502" quantscript/src/lowering/bindings.rs` 不再返回 `eprintln`
- `cargo test --workspace` 全量通过

**估时**: 15min

---

### S0-5 — `api_errors.rs` 的 `internal_error()` 丢弃根本原因

**严重度**: 🔴 S0 | **来源**: Agent 1
**文件**: `src/api_errors.rs:58`

**问题**:

```rust
pub(super) fn internal_error(_error: anyhow::Error) -> (StatusCode, Json<Value>) {
    // 接收了详细错误但完全丢弃
    (StatusCode::INTERNAL_SERVER_ERROR, Json(json!({
        "error": "internal_error",
        "message": "内部服务器错误"
    })))
}
```

当后端真正出错时，用户只看到 "内部服务器错误"，完全无法知道哪个操作失败、什么原因、怎么处理。

**方案**:

1. 保留 `message` 为用户友好的通用消息
2. 追加 `detail` 字段记录 sanitized 错误信息（经 `safe_log::sanitize_secrets` 处理）
3. 开发模式下 (QUANTPILOT_DEV=true) 返回完整错误链

**验收**:
- `internal_error(e)` 的 JSON 响应中包含 `detail` 字段
- 开发模式下包含完整错误上下文
- `cargo test --workspace` 全量通过

**估时**: 20min

---

### S0-6 — `compile_api.rs` 裸字符串错误响应

**严重度**: 🔴 S0 | **来源**: Agent 3
**文件**: `src/compile_api.rs:38`

**问题**:

```rust
(StatusCode::SERVICE_UNAVAILABLE, "编译服务已关闭".to_string())
```

直接返回裸字符串，不是 JSON。前端 `client.js` 的 `response.json()` 会在解析时崩溃，抛出非预期的异常。

**方案**:

改为返回标准 JSON 错误格式：

```rust
(StatusCode::SERVICE_UNAVAILABLE, Json(json!({
    "error": "service_unavailable",
    "message": "编译服务已关闭"
})))
```

**验收**:
- `compile_api.rs` 中不再有 `(StatusCode, String)` 裸字符串错误返回
- `cargo test --workspace` 全量通过
- curl `/api/runtime/compile` 在服务关闭时返回 JSON 而非纯文本

**估时**: 15min

---

### S0-7 — `auth_middleware.rs` 声称 RFC 9457 但不遵守格式

**严重度**: 🔴 S0 | **来源**: Agent 1
**文件**: `src/auth_middleware.rs:73`

**问题**:

```rust
.header(axum::http::header::CONTENT_TYPE, "application/problem+json")
.body(...)
```

设置了 `Content-Type: application/problem+json`，但响应体是 `{"error": "unauthorized", "message": "..."}` — 缺少 RFC 9457 要求的 `type`、`title`、`status`、`detail`、`instance` 字段。这会让解析 RFC 9457 格式的客户端出错。

**方案**:

去掉 `application/problem+json` Content-Type，改用标准 `application/json`。或补充 RFC 9457 字段。

决策（见规划方案决策 4）：改用标准 JSON 格式。

**验收**:
- `auth_middleware.rs` 不再使用 `application/problem+json`
- 认证失败响应使用标准 JSON 格式
- `cargo test --workspace` 全量通过

**估时**: 10min

---

### S0-8 — `compile_diagnostics.rs` 中 English/Chinese 诊断消息混用

**严重度**: 🔴 S0 | **违反**: GP §2.1 | **来源**: Agent 1
**文件**: `src/compile_diagnostics.rs:117-145, 298, 352, 360`

**问题**:

`CUSTOM*` / `QPSTRATSPREAD*` 的诊断提示全是英文，`QPQSLOW*` 的诊断提示全是中文。同一个编译请求可能返回混合语言的诊断列表。更严重的是：

- 第 298 行: `"QPQSLOW007 strategy lowering requires at least one fetch/get_data call"` — 英文
- 第 352 行: `"QPQSLOW005 emit Intent requires action"` — 英文
- 第 360 行: `"QPQSLOW004 unsupported Intent action for runtime lowering: {normalized}"` — 英文

但这些代码在 lowering 路径中的**对应提示**（第 170-266 行）全是中文。用户会在同一批编译结果中看到消息语言来回切换。

**方案**:

将第 117-145 行（CUSTOM* / QPSTRATSPREAD*）和第 298/352/360 行（QPQSLOW* 英文版）的提示文本全部改为中文，与第 170-266 行保持一致。

**验收**:
- `grep -n "strategy lowering\|emit Intent\|unsupported Intent\|Custom indicators are restricted\|Use \`spread_output_code\`\|Set \`max_time_diff_ms\`\|Use a one-sided buy threshold\|Provide exactly two spread" src/compile_diagnostics.rs` 返回 0
- `cargo test --workspace` 全量通过（更新 golden view 断言）

**估时**: 30min

---

### S0-9 — `runtime_validation.rs` 错误消息中英混用

**严重度**: 🔴 S0 | **违反**: GP §2.1 | **来源**: Agent 1
**文件**: `src/runtime_validation.rs:47-350`

**问题**:

第 47-350 行的大多数验证错误是英文，但第 355-359 行（backtest 参数验证）和第 447-470 行（graph_id 验证）是中文。同一文件内严重不一致。

**英文消息示例**:
- `"runtime mode \`{}\` is not supported in the current beta"` (line 47)
- `"module \`{}\` is not enabled for compile in the current beta"` (line 60)
- `"capability schema_hash must be formatted as sha256:<64 lowercase hex chars>"` (line 284)

**方案**:

将第 47-350 行全部英文错误消息汉化。

**验收**:
- `grep -n "is not supported in the current beta\|is not enabled for compile\|must be formatted as\|does not match the current\|field must be" src/runtime_validation.rs` 返回 0
- `cargo test --workspace` 全量通过（更新所有 `.contains("...") ` 断言）

**估时**: 45min

---

### S0-10 — `sandbox_verification.rs` 错误中文/警告英文混用

**严重度**: 🔴 S0 | **违反**: GP §2.1 | **来源**: Agent 1
**文件**: `src/sandbox_verification.rs:177-188`

**问题**:

```rust
// 警告是英文
"replay fidelity partial: 候选与基线使用同一数据集，对比参考价值有限"
"turnover increase {:.0}%, additional fee impact expected"
"max drawdown increased, verify risk tolerance"

// 但错误是中文
"提案 '{}' 的沙箱报告不存在"
"沙箱验证要求 AI 提案已通过静态检查"
```

同一函数产出的 `SandboxVerificationReport` 中，warnings 是英文，errors 是中文。

**方案**:

将 warnings 改为中文。

**验收**:
- `grep "replay fidelity partial:\|turnover increase\|max drawdown increased\|additional fee impact" src/sandbox_verification.rs` 返回 0
- `cargo test --workspace` 全量通过

**估时**: 15min

---

### S0-11 — `runtime_diagnostics.rs` 中文标签中的英文缩写

**严重度**: 🔴 S0 | **来源**: Agent 1
**文件**: `src/runtime_diagnostics.rs:28, 45, 59, 62, 137-139`

**问题**:

- `"新鲜度 (ms)"` — `ms` 是英文缩写，非技术用户可能不理解
- `"Ping 延迟 (ms)"` — `Ping` + `ms` 双英文
- `"源延迟 (ms)"` — 同上
- 布尔值显示 `"Yes"` / `"No"` 而非 `"是"` / `"否"`
- `"RuntimeWarning"` 翻译为 `"数据告警"` 但 `"RuntimeNotice"` 翻译为 `"运行提示"` —— 同是 Runtime* 前缀，一个映射到"数据"，一个映射到"运行"

**方案**:

1. `(ms)` → `（毫秒）`
2. `Ping 延迟` → `探测延迟`
3. `Yes`/`No` → `是`/`否`
4. `数据告警` → `运行告警`（与 `运行提示` 保持一致）
5. `数据错误` → `运行错误`

**验收**:
- `grep "ms)\|Ping 延迟\|Yes.*No" src/runtime_diagnostics.rs` 返回 0
- `grep "数据告警\|数据错误" src/runtime_diagnostics.rs` 返回 0
- `cargo test --workspace` 全量通过

**估时**: 15min

---

### S0-12 — App.jsx TDZ 错误导致 React 崩溃黑屏

**严重度**: 🔴 S0 | **来源**: 实际 Bug（devtools 捕获）
**文件**: `frontend/src/App.jsx:108`

**问题**:

```js
const routeRef = useRef(route);  // line 108 — route 在此行尚未声明
// ...
const [route, setRoute] = useState(...);  // line 133 — route 在此行才声明
```

JavaScript Temporal Dead Zone 错误：`Cannot access 'route' before initialization`。整个 React 组件树崩溃，页面完全黑屏。这是 v1.0.5 最严重的用户体验破坏性 bug。

**方案**:

将 `const [route, setRoute] = useState(...)` 移动到 `const routeRef = useRef(route)` 之前。

**验收**:
- 浏览器 Console 不再出现 `Cannot access 'route' before initialization`
- 页面正常渲染（不再黑屏）
- `npx vitest run` 全量通过

**状态**: ✅ 已修复

---

### S0-13 — start.bat 含有无法编译的 `bledelayedexpansion` 和中文乱码

**严重度**: 🔴 S0 | **来源**: 实际 Bug
**文件**: `start.bat`

**问题**:

批处理文件中 `::` 行内注释后的中文字符导致 CMD 解析器跳过后续关键行。`enabledelayedexpansion` 被截断为 `bledelayedexpansion`。`QUANTPILOT_DEV` 变量从未真正设置。后端退出条件 `!ERRORLEVEL!` 无法展开。

**方案**:

1. 将 `::` 注释改为 `REM`
2. 避免在 `start.bat` 中使用中文字符
3. 使用 `setlocal enabledelayedexpansion`

**验收**:
- `start.bat` 输出 `QUANTPILOT_DEV = true`（非空）
- 后端成功编译并启动
- Vite 成功启动不报端口占用

**状态**: ✅ 已修复

---

## 二、P1 高优先级 — 22 项

### P1-1 — "not supported in the current beta" 不说支持什么

**严重度**: 🟠 P1 | **来源**: Agent 1
**文件**: `src/runtime_validation.rs` (9 处)

**问题**:

消息如 `"exchange \`{}\` is not supported in the current beta"` 出现 9+ 次。用户被告知某个值不支持，但**从不被告知支持哪些值**。用户必须猜测或读源码。

**方案**:

在每处错误消息后追加 `"支持的值: {list}"`。例如 `"不支持的交易所 '{}'。当前支持的交易所: binance, okx"`

**验收**:
- 9 处 "not supported" 消息全部包含有效值列表
- `cargo test --workspace` 全量通过

**估时**: 30min

---

### P1-2 — auth 错误不告诉怎么获取 token

**严重度**: 🟠 P1 | **来源**: Agent 1
**文件**: `src/auth_middleware.rs:62`

**问题**:

```rust
"认证失败: 请在 Authorization 头中提供有效的 Bearer token"
```

用户不知道 token 从哪获取、如何设置。没有提及 `QUANTPILOT_API_KEY` 环境变量或 `QUANTPILOT_DEV=true` 开发模式。

**方案**:

```rust
"认证失败: 请设置 QUANTPILOT_API_KEY 环境变量，或设置 QUANTPILOT_DEV=true 跳过认证（仅开发环境）"
```

**验收**:
- 认证失败消息包含环境变量名和开发模式开关
- `cargo test --workspace` 全量通过

**估时**: 10min

---

### P1-3 — 报警规则行动描述含糊不清

**严重度**: 🟠 P1 | **来源**: Agent 1
**文件**: `src/alert_engine.rs:31-98`

**问题**:

10 条告警规则的 `action` 字段使用模糊的行动描述：
- `"检查参数是否异常"` — 哪些参数？基准是什么？
- `"检查 AI 模型输出质量"` — 如何检查？
- `"考虑冻结提案"` — "考虑"不是可执行行动
- `"通知值班"` — 谁？怎么通知？

**方案**:

将行动描述改为可执行步骤：
- `"检查当前策略参数的 risk_reject_rate，对比近 24h 均值。如持续 > 90% 且样本数充足，建议回滚最近一次参数变更"`
- `"AI 提案 24h 拒绝率超过阈值。检查最近提案的 static_check 报告，如模型输出不收敛建议暂停提案 24h"`
- `"通知值班：data_freshness P95 超过 3 倍轮询间隔"`

**验收**:
- 10 条规则中不再出现 "检查"、"考虑"、"建议" 等模糊措辞，改为具体操作步骤
- `cargo test --workspace` 全量通过

**估时**: 30min

---

### P1-4 — runbook 混用中文和技术标识语

**严重度**: 🟠 P1 | **来源**: Agent 1
**文件**: `src/runbook.rs`

**问题**:

每个故障场景中混用了大量英文技术标识：`data_freshness_p95_ms`、`poll_interval`、`risk_reject_rate_5m`、`EventGapDetected`、`deployment_revision`、`ring buffer`、`shadow_replay`、`REDUCE_ONLY`。运维人员（非开发者）收到 runbook 时无法理解这些内部指标名。

**方案**:

在首次出现英文标识时，用括号注明中文含义。例如 `"data_freshness_p95_ms（P95 数据新鲜度）持续上升"`。

**验收**:
- 6 个故障场景中所有首次出现的英文指标名都有中文注释
- `cargo test --workspace` 全量通过

**估时**: 25min

---

### P1-5 — QS 诊断中 "下层转换" / "lowering" 术语暴露

**严重度**: 🟠 P1 | **来源**: Agent 4
**文件**: `quantscript/src/lowering/intents.rs`、`quantscript/src/lowering/orchestrator.rs`

**问题**:

QPQSLOW001/002/004/007 的诊断消息中出现 "下层转换" 一词。这是编译器内部术语（lowering），用户不需要知道编译过程有 "lowering" 这一阶段。

**方案**:

`"下层转换"` → `"编译"`。例如 `"不支持的条件下发 Intent 下层转换"` → `"不支持的条件下发 Intent：条件无法编译为可用指标"`

**验收**:
- `grep "下层转换" quantscript/src/` 返回 0
- `cargo test --workspace` 全量通过（更新诊断断言）

**估时**: 15min

---

### P1-6 — QS 诊断中 "形式化 QuantScript" 啰嗦

**严重度**: 🟠 P1 | **来源**: Agent 4
**文件**: `quantscript/src/analysis.rs`、`src/compile_diagnostics.rs`

**问题**:

几乎所有 QS 诊断都以 "形式化 QuantScript" 开头（例如 "形式化 QuantScript 不支持可执行主干中的 while 循环"）。用户只写 "QuantScript"，"形式化" 是编译器内部用来区分新旧路径的术语。

**方案**:

去掉所有 "形式化" 前缀，统一用 "QuantScript"。

**验收**:
- `grep "形式化 QuantScript" quantscript/src/ quantscript/src/lowering/` 返回 0
- `grep "形式化 QuantScript" src/compile_diagnostics.rs` 返回 0
- `cargo test --workspace` 全量通过

**估时**: 20min

---

### P1-7 — "可执行主干" 术语无定义

**严重度**: 🟠 P1 | **来源**: Agent 4
**文件**: `quantscript/src/analysis.rs`

**问题**:

QS0601-QS0610 全部使用 "形式化 QuantScript 不支持可执行主干中的..."。用户不知道 "可执行主干" 是什么——是 strategy() 函数体内的代码？是顶层语句？

**方案**:

`"可执行主干"` → `"strategy() 函数体"`。例如 `"QuantScript 不支持 strategy() 函数体中的 while 循环"`。

**验收**:
- `grep "可执行主干" quantscript/src/` 返回 0
- `cargo test --workspace` 全量通过

**估时**: 15min

---

### P1-8 — 9 个诊断缺少 span 信息

**严重度**: 🟠 P1 | **来源**: Agent 4
**文件**: `quantscript/src/analysis.rs`

**问题**:

以下诊断的 `span = None`，用户不知道脚本哪一行出了问题：

| 代码 | 消息 |
|------|------|
| QS0401 | 前视风险: 负数序列索引 |
| QS0402 | 前视: center=true |
| QS0403 | trailing 窗口需要正数跨度 |
| QS0404 | 索引可能超出回看窗口 |
| QS0501 | 预热不足 |
| QS0503 | fetch lookback 小于 1 |
| QS0505 | 未知交易对 |
| QS0611 | 策略必须包含 emit Intent() |
| QS0612 | dead code emit |

**方案**:

为这 9 个诊断补充 source span（从对应的 AST 节点获取 `stmt.span()` 或 `expr.span()`）。

**验收**:
- `grep "span.*None" quantscript/src/analysis.rs | grep QS04\|QS05\|QS06` 返回 0
- `cargo test --workspace` 全量通过

**估时**: 45min

---

### P1-9 — 错误消息建议 "使用辅助函数" 但不说是哪些

**严重度**: 🟠 P1 | **来源**: Agent 4
**文件**: `quantscript/src/lowering/orchestrator.rs`、`src/compile_diagnostics.rs`

**问题**:

QPQSLOW013："rebalance(...) 当前需要一个分配辅助函数调用" — 不说是哪些辅助函数。对比 `compile_diagnostics.rs` 中的 hint 字段有列出，但原始 lowering 错误没有。

**方案**:

在 lowering 错误消息中追加 "有效的分配函数: equal_weight(...), fixed_weights(...), rank_weight(...), score_weight(...)"。

**验收**:
- QPQSLOW013/014/025 的错误消息包含有效函数/键列表
- `cargo test --workspace` 全量通过

**估时**: 15min

---

### P1-10 — QPQSLOW999 catch-all 被过滤出 API 响应

**严重度**: 🟠 P1 | **来源**: Agent 4
**文件**: `src/compile_api.rs:246`

**问题**:

```rust
if diagnostic.code != "QPQSLOW999" {
    // only then include in response
}
```

任何落到 QPQSLOW999 的 lowering 错误被**静默过滤掉**。用户看不到任何错误，但编译失败了。

**方案**:

保留 QPQSLOW999 在响应中，但附加通用提示 "编译过程遇到未预期的内部错误，请检查最新策略语法或联系支持"。同时在日志中记录完整错误链。

**验收**:
- QPQSLOW999 诊断不再被过滤
- `cargo test --workspace` 全量通过
- 新增测试：构造 QPQSLOW999 场景，验证 API 返回中包含该诊断

**估时**: 15min

---

### P1-11 — 前端 `fetchJson`/`postJson`/`deleteJson` 分页自动解包静默丢弃 total

**严重度**: 🟠 P1 | **来源**: Agent 3
**文件**: `frontend/src/store/graphStorePersistenceHelpers.js:233-237, 268-271, 300-304`

**问题**:

三个函数中复制了相同的解包逻辑：

```javascript
if (json && typeof json === "object" && Array.isArray(json.data) && typeof json.total === "number") {
    return json.data;  // total 被丢弃
}
```

任何列表组件都无法判断数据是否被截断分页。

**方案**:

见规划方案决策 5。统一改为不解包，由调用方显式取 `response.data`。

**验收**:
- `fetchJson`/`postJson`/`deleteJson` 不再有自动解包逻辑
- 所有调用方正确访问 `.data`
- `npx vitest run` 全量通过
- `npx vite build` 通过

**估时**: 1h

---

### P1-12 — 前端 `client.js` 截断错误消息到 200 字符

**严重度**: 🟠 P1 | **来源**: Agent 3
**文件**: `frontend/src/api/client.js:38`

**问题**:

```javascript
const error = new Error(text.slice(0, 200) || `HTTP ${response.status}`);
```

如果后端返回详细诊断（CompileDiagnostic 多错误列表），前端截断 200 字符，最关键的错误细节可能被丢弃。

**方案**:

将截断限制提升到 2000 字符，或移除截断（依赖 UI 层的展示截断）。

**验收**:
- `client.js` 不再有 `text.slice(0, 200)` 截断
- `npx vitest run` 全量通过

**估时**: 10min

---

### P1-13 — App.jsx 离线横幅硬编码中文

**严重度**: 🟠 P1 | **违反**: GP §2.5 | **来源**: Agent 2
**文件**: `frontend/src/App.jsx:214-217`

**问题**:

```jsx
<div className="ad-offline-banner" role="alert">
  网络连接已断开，部分功能不可用。
</div>
```

英文用户看到中文横幅。

**方案**:

使用 `t()` 包裹。

**验收**:
- 离线横幅文本通过 `t()` 获取
- `npx vitest run` 全量通过

**估时**: 5min

---

### P1-14 — App.jsx 存储配额警告可点击关闭但未解决问题

**严重度**: 🟠 P1 | **来源**: Agent 2
**文件**: `frontend/src/App.jsx:219-222`

**问题**:

```jsx
onClick={() => setStorageQuotaExceeded(false)}
```

用户点击关闭后警告消失，但存储问题**未解决**。下次保存时再次出现。用户被反复打断但没有解决路径。

**方案**:

1. 提示中增加 "清理旧版本" 的操作指引
2. 添加跳转到清理页面的按钮（如适用）或触发 localStorage 清理的代码

**验收**:
- 警告包含具体操作指引
- 用户有可执行的清理路径

**估时**: 15min

---

### P1-15 — loading 状态无进度上下文

**严重度**: 🟠 P1 | **来源**: Agent 2
**文件**: `frontend/src/App.jsx:25-46`

**问题**:

```jsx
<div className="app-loading-shell__title">正在加载界面</div>
```

用户等待时不知道在加载什么——是连接后端？加载能力？恢复策略图？没有阶段指示。

**方案**:

基于 `capabilityStatus` 和 `graphIndexStatus` 的 Zustand 状态，显示动态加载阶段文本：
- `capabilityStatus === "loading"` → "正在连接后端..."
- `graphIndexStatus === "loading"` → "正在加载策略列表..."
- 初始化完成 → "正在准备编辑器..."

**验收**:
- loading 文本随加载阶段动态变化
- `npx vitest run` 全量通过

**估时**: 20min

---

### P1-16 — 8 秒硬编码超时对慢网络用户不友好

**严重度**: 🟠 P1 | **来源**: Agent 2
**文件**: `frontend/src/App.jsx:30`

**问题**:

```jsx
const t = setTimeout(() => setWaited(true), 8000);
```

慢网络用户（>8 秒首次加载）看不到跳过按钮的出现过程。没有倒数计时或进度指示。

**方案**:

改为显示简单的加载阶段文本（见 P1-15），`waited` 触发点改为 5 秒（配合 P1-15 的阶段文本变化）。

**验收**:
- 跳过按钮在 5 秒后出现
- 加载阶段文本随 `capabilityStatus` 变化

**估时**: 10min

---

### P1-17 — Tutorial 步骤在首次使用时不检查元素存在性

**严重度**: 🟠 P1 | **来源**: Agent 2
**文件**: `frontend/src/data/tutorialSteps.js`

**问题**:

步骤 1 的 `target: '[data-testid="module-sidebar"]'` — 如果侧边栏未渲染（新用户首次加载，loading 状态中），教程箭头指向空白。

**方案**:

在 `TutorialOverlay.jsx` 中渲染前检查 `document.querySelector(step.target)` 是否存在。不存在时灰显该步骤并提示 "请等待界面加载完成"。

**验收**:
- 侧边栏未渲染时教程不会指向空元素
- `npx vitest run` 全量通过

**估时**: 20min

---

### P1-18 — 教程无进度指示器

**严重度**: 🟠 P1 | **来源**: Agent 2
**文件**: `frontend/src/components/TutorialOverlay.jsx`

**问题**:

教程没有 "第 X 步 / 共 Y 步" 的进度指示。用户在 5 步教程中失去方向。

**方案**:

在 `TutorialOverlay` 中添加步骤计数显示。

**验收**:
- 教程面板显示 "第 1/5 步" 格式的进度
- `npx vitest run` 全量通过

**估时**: 15min

---

### P1-19 — `runtime_diagnostics.rs` 中 `RuntimeWarning`/`RuntimeError` 翻译不一致

**严重度**: 🟠 P1 | **来源**: Agent 1
**文件**: `src/runtime_diagnostics.rs:14-15`

**问题**:

| 原文 | 译文 | 应译文 |
|------|------|--------|
| `RuntimeNotice` | `运行提示` | ✅ |
| `RuntimeWarning` | `数据告警` | `运行告警` |
| `RuntimeError` | `数据错误` | `运行错误` |

`"Runtime"` 在 Notice 中译为 `"运行"`，但在 Warning/Error 中译为 `"数据"`。不一致会导致用户困惑：到底是数据问题还是运行问题？

**方案**:

统一为 `"运行"` 前缀。

**验收**:
- `grep "数据告警\|数据错误" src/runtime_diagnostics.rs` 返回 0
- `cargo test --workspace` 全量通过

**估时**: 5min

---

### P1-20 — `strategyHubRosterProjection.test.js` 和 `strategyHubInspectorProjection.test.js` 中 `sortBy` 的 `.sort()` 回调缺少边界情况

**严重度**: 🟠 P1 | **来源**: Agent 2
**文件**: `frontend/src/utils/strategyHubRosterProjection.js`

**问题**:

排序函数未处理相同键值时的稳定排序。非确定性排序在不同浏览器中产生不同的列表顺序。

**方案**:

在排序函数中添加 tie-breaking：相同值时按 `graph_id` 字母序排列。

**验收**:
- 相同 score 的条目在不同浏览器中顺序一致
- `npx vitest run` 全量通过

**估时**: 10min

---

### P1-21 — QS 编译器 "索引 {} 可能超出 {} 的回看窗口" 没有说明哪个变量

**严重度**: 🟠 P1 | **来源**: Agent 4
**文件**: `quantscript/src/analysis.rs:1519`

**问题**:

QS0404 的消息格式 `"索引 {} 可能超出 {} 的回看窗口 {}"` — 其中 `{}` 是内部 `BindingId` 数字，对用户毫无意义。

**方案**:

将 `BindingId` 解析为用户可见的变量名（通过 `semantic.var_name()` 或类似 API）。

**验收**:
- QS0404 消息中包含用户变量名而非内部 ID
- `cargo test --workspace` 全量通过

**估时**: 20min

---

### P1-22 — QS 编译器 QS0603 建议 "不支持 while" 但不说明替代方案

**严重度**: 🟠 P1 | **来源**: Agent 4
**文件**: `quantscript/src/analysis.rs:131`

**问题**:

"QuantScript 不支持 strategy() 函数体中的 while 循环" — 不告诉用户用什么代替。

**方案**:

追加 "请使用 for ... in ... 替代，或改用 series 窗口聚合"。

**验收**:
- QS0603 消息包含替代方案
- `cargo test --workspace` 全量通过

**估时**: 10min

---

## 三、P2 中优先级 — 24 项

### P2-1 — `rate_limiter.rs` 的文档链接可能不存在

**严重度**: 🟡 P2 | **来源**: Agent 1
**文件**: `src/rate_limiter.rs:94`

**问题**:

```rust
"type": "https://quantpilot.dev/problems/rate-limited"
```

如果 `quantpilot.dev` 域名不存在或未部署文档，用户看到的是死链接。

**方案**:

1. 验证域名是否可解析
2. 如果不能，改为本地文档路径或移除 `type` 字段

**验收**:
- 速率限制响应中的 type 字段指向可访问的资源

**估时**: 10min

---

### P2-2 — 速率限制不告之阈值

**严重度**: 🟡 P2 | **来源**: Agent 1
**文件**: `src/rate_limiter.rs:95-97`

**问题**:

```
"请求过于频繁"
"速率限制已触发, 请在 {} 秒后重试"
```

不告诉用户限制阈值是多少（每秒多少请求），用户不知道如何调整行为以避免再次触发。

**方案**:

追加 "当前限制: 每秒 N 个请求"。

**验收**:
- 速率限制消息包含具体限制数值
- `cargo test --workspace` 全量通过

**估时**: 10min

---

### P2-3 — 后端错误消息中内部 JSON 路径暴露

**严重度**: 🟡 P2 | **来源**: Agent 1
**文件**: `src/runtime_validation.rs:355-358`

**问题**:

```rust
"backtest_options.execution_assumptions.fee_bps 必须大于等于 0"
```

`"backtest_options.execution_assumptions.fee_bps"` 是内部 JSON 字段路径，不是用户概念。用户不知道这是指哪个界面的哪个设置。

**方案**:

`"回测费用基点（fee_bps）必须大于等于 0"`。

**验收**:
- `grep "backtest_options.execution_assumptions\|agent.config.rebalance_symbols\|capability_context.permission_boundary" src/runtime_validation.rs` 返回 0
- `cargo test --workspace` 全量通过

**估时**: 15min

---

### P2-4 — `sandbox_verification.rs` 的 fidelity 警告隐藏了其他警告

**严重度**: 🟡 P2 | **来源**: Agent 1
**文件**: `src/sandbox_verification.rs:178-181`

**问题**:

当 `fidelity == "partial"` 时，函数提前返回，turnover 和 drawdown 警告被**抑制**。即使用户的候选策略 turnover 很大、drawdown 很深，只要 replay fidelity 是 partial，用户就看不到这些重要警告。

**方案**:

移除提前返回。即使 fidelity 是 partial，仍然检查 turnover 和 drawdown 并产出警告。

**验收**:
- partial fidelity 沙箱验证仍产出 turnover/drawdown 警告
- `cargo test --workspace` 全量通过

**估时**: 10min

---

### P2-5 — 前端离线状态显示对桌面应用无意义

**严重度**: 🟡 P2 | **来源**: Agent 2
**文件**: `frontend/src/App.jsx:58-59`

**问题**:

```jsx
const [isOffline, setIsOffline] = useState(!navigator.onLine);
```

Tauri 桌面应用中，后端在同一台机器上。`navigator.onLine` 反映的是系统网络状态，但后端在 localhost 总是可达的。离线横幅对桌面应用用户来说令人困惑。

**方案**:

在 Tauri 环境中隐藏离线横幅，或将其改为 "后端连接断开"（检测后端可达性）。

**验收**:
- Tauri 桌面应用中不会因为系统断网而显示离线横幅
- 浏览器环境中离线横幅行为不变

**估时**: 15min

---

### P2-6 — `beforeunload` 使用浏览器默认对话框

**严重度**: 🟡 P2 | **来源**: Agent 2
**文件**: `frontend/src/App.jsx:111-117`

**问题**:

```jsx
e.returnValue = "";
```

触发浏览器默认的 "离开此网站？" 对话框，没有自定义消息。用户不知道离开会丢失什么——未保存的策略图？正在运行的模拟？

**方案**:

在 `e.returnValue` 中填入有意义的提示。

**验收**:
- beforeunload 对话框包含策略相关提示
- `npx vitest run` 全量通过

**估时**: 5min

---

### P2-7 — router 静默拒绝包含空字节的 URL

**严重度**: 🟡 P2 | **来源**: Agent 2
**文件**: `frontend/src/router.js:96`

**问题**:

```js
if (decoded.includes("\x00") || decoded.length > 128) {
    return { name: "strategies" };  // 静默回退
}
```

用户输入无效 graph ID 时被静默重定向到首页，不知道为什么。

**方案**:

返回 `{ name: "strategies", error: "无效的策略 ID" }`，由 App.jsx 渲染一条 toast 提示。

**验收**:
- 无效 graph ID 触发可见错误提示
- `npx vitest run` 全量通过

**估时**: 15min

---

### P2-8 — router 100ms 反抖对双击静默

**严重度**: 🟡 P2 | **来源**: Agent 2
**文件**: `frontend/src/router.js:142`

**问题**:

```js
// 防 100ms 内重复导航
```

快速双击会被静默丢弃。没有任何视觉反馈。

**方案**:

添加控制台 warning 或 UI 指示器。

**验收**:
- 被防抖丢弃的导航有 trace 日志

**估时**: 10min

---

### P2-9 — `errorText.js` 英文消息匹配依赖精确标点

**严重度**: 🟡 P2 | **来源**: Agent 2
**文件**: `frontend/src/utils/errorText.js:3-4`

**问题**:

匹配键 `"strategy graph source must start with strategy_graph"`（无句点），但后端可能发送 `"strategy graph source must start with strategy_graph."`（有句点）——不匹配，回退到通用错误。

**方案**:

在匹配前标准化标点（去掉尾随句点）或将键改为正则。

**验收**:
- 带句点和不带句点的后端错误都能正确匹配
- `npx vitest run` 全量通过

**估时**: 10min

---

### P2-10 — `actionFailure.js` 暴露内部架构路径

**严重度**: 🟡 P2 | **来源**: Agent 2
**文件**: `frontend/src/utils/actionFailure.js:21-22`

**问题**:

```js
`${COMPILE_CONTRACT.runtimeSourceOfTruthLabel}`
```

暴露了 `COMPILE_CONTRACT.runtimeSourceOfTruthLabel`（可能是原始 API 路径）。用户看到 `/api/runtime/compile` 这样的路径。

**方案**:

用人类可读的文本替代，如 "运行时编译端点"。

**验收**:
- `actionFailure.js` 的 nextStep 文本中不包含 API 路径
- `npx vitest run` 全量通过

**估时**: 10min

---

### P2-11 — `en-US.js` 约 250 个翻译键为 null

**严重度**: 🟡 P2 | **来源**: Agent 2
**文件**: `frontend/src/i18n/locales/en-US.js`

**问题**:

约 250 个键值为 `null`，回退到中文。英文用户看到大面积的中文 UI 文本。

**方案**:

本里程碑只标记此问题。补齐翻译留给 v1.0.7。

**验收**:
- 本里程碑 CHANGELOG 中记录 "en-US 翻译覆盖率约 30%，计划 v1.0.7 补齐"

**估时**: N/A（不在此里程碑）

---

### P2-12 — i18n 缺失翻译静默回退无日志

**严重度**: 🟡 P2 | **来源**: Agent 2
**文件**: `frontend/src/i18n/index.js:27-29`

**问题**:

```js
function interpolate(message, variables = {}) {
    return String(message).replace(/\{(\w+)\}/g, (_, key) =>
        variables[key] === undefined || variables[key] === null ? "" : String(variables[key])
    );
}
```

缺失翻译键被静默替换为空字符串，没有 console.warn。UI 中出现空白文本。

**方案**:

在开发模式下添加 `console.warn` 提示缺失的翻译键。

**验收**:
- 开发模式下缺失翻译键打印 warning
- `npx vitest run` 全量通过

**估时**: 10min

---

### P2-13 — 治理面板标签是内部术语

**严重度**: 🟡 P2 | **来源**: Agent 2
**文件**: `frontend/src/utils/runtimeGovernance.js:87-108`

**问题**:

面板标签 `"部署修订"`、`"权限模型"`、`"AI 写入"`、`"治理来源"` 对交易策略开发者来说晦涩难懂。

**方案**:

增加 tooltip 解释或将标签改为更日常的用语。

**验收**:
- 每个标签有 tooltip 说明含义
- `npx vitest run` 全量通过

**估时**: 20min

---

### P2-14 — API 路由 `/api/v1/` vs `/api/` 版本号前缀不一致

**严重度**: 🟡 P2 | **来源**: Agent 3
**文件**: `src/app_router.rs`

**问题**:

alerts、snapshots、runbook 使用 `/api/v1/` 前缀，其他所有路由使用 `/api/`。API 消费者需要知道哪些端点带 v1。

**方案**:

在 `frontend_api_types.rs` 或 `docs/` 中记录 API 版本约定。如果 v1 是全局的，考虑统一前缀或统一去掉。

**验收**:
- 文档中记录了版本前缀约定

**估时**: 15min

---

### P2-15 — CLI usage 文本对 credential 命令未列出完整帮助

**严重度**: 🟡 P2 | **来源**: Agent 3
**文件**: `src/cli_support.rs:11`

**问题**:

```rust
"Usage:\n  quantpilot                      Start the QuantPilot API server\n  quantpilot strategy-ir validate <path>\n ..."
```

遗漏 `quantpilot credential` 子命令的帮助。

**方案**:

在 cli_usage() 中追加 `quantpilot credential` 的用法。

**验收**:
- `cli_usage()` 包含 credential 子命令
- `cargo test --workspace` 全量通过

**估时**: 10min

---

### P2-16 — `frontend_api_types.rs` 无 `ApiErrorResponse` 统一结构体

**严重度**: 🟡 P2 | **来源**: Agent 3
**文件**: `src/frontend_api_types.rs`

**问题**:

2019 行的类型文件中没有任何统一的 API 错误响应类型。每个 handler 自由返回不同格式的错误。

**方案**:

定义一个 `ApiErrorResponse` struct：

```rust
pub(super) struct ApiErrorResponse {
    pub(super) error: String,
    pub(super) message: String,
    pub(super) details: Option<Vec<String>>,
}
```

本里程碑只定义结构体，不在所有 handler 中强制使用（属 P1 范围，留给 v1.0.7）。

**验收**:
- `frontend_api_types.rs` 中有 `ApiErrorResponse` 结构体
- `cargo check` 通过

**估时**: 10min

---

### P2-17 — `capability_api.rs` 响应中哈希值无人类可读说明

**严重度**: 🟡 P2 | **来源**: Agent 3
**文件**: `src/capability_api.rs:155-224`

**问题**:

`CapabilityContract` 返回的 `schema_hash`、`capability_hash` 是 `sha256:...` 格式的 hex 字符串。用户看到这些值但不知道它们是什么、用来做什么、是否需要验证。

**方案**:

在 capability API 响应文档中增加注释说明，或在响应中增加 `_doc` 字段解释各哈希值的用途。

**验收**:
- capability 响应中有哈希值的用途说明（至少文档层面）

**估时**: 15min

---

### P2-18 — `graphStoreHelpers.js` 中三个函数的重复解包逻辑

**严重度**: 🟡 P2 | **来源**: Agent 3
**文件**: `frontend/src/store/graphStorePersistenceHelpers.js`

**问题**:

`fetchJson`、`postJson`、`deleteJson` 三个独立函数中复制了完全相同的分页解包逻辑（3 处重复）。任何修改都需要三份同步。

**方案**:

提取共享的 `unwrapPagination(responseJson)` 函数，三个函数调用它。

**验收**:
- 解包逻辑仅存在于一个函数中
- `npx vitest run` 全量通过

**估时**: 15min

---

### P2-19 — QS0609 error 消息建议 "改用列表字面量" 但不给示例

**严重度**: 🟡 P2 | **来源**: Agent 4
**文件**: `quantscript/src/analysis.rs:191`

**问题**:

"QuantScript 不支持 strategy() 函数体中使用 `.push(...)` 构建可变列表" — 不告诉用户用什么代替。

**方案**:

追加 "请直接使用列表字面量 [a, b, c] 代替"。

**验收**:
- QS0609 消息包含替代语法示例
- `cargo test --workspace` 全量通过

**估时**: 5min

---

### P2-20 — QS 编译器 QS0505 的 "可能" 措辞模糊

**严重度**: 🟡 P2 | **来源**: Agent 4
**文件**: `quantscript/src/analysis.rs:1737`

**问题**:

"未知交易对 '{}', 运行时可能失败" — "可能"是什么意思？一定会失败吗？还是取决于环境？

**方案**:

改为 "交易对 '{}' 不在当前支持列表中。支持: BTCUSDT, ETHUSDT, SOLUSDT。运行时沙箱将无法获取该交易对数据"。

**验收**:
- QS0505 消息包含支持列表且移除 "可能"
- `cargo test --workspace` 全量通过

**估时**: 10min

---

### P2-21 — start.bat 中的 `taskkill` 杀 node.exe 过于激进

**严重度**: 🟡 P2 | **来源**: 实际观察
**文件**: `start.bat`

**问题**:

`taskkill /f /im node.exe` 会杀死机器上所有 Node.js 进程，包括用户的其他开发服务器。

**方案**:

改为只杀端口 5173 上的进程：`for /f "tokens=5" %a in ('netstat -ano ^| findstr :5173 ^| findstr LISTENING') do taskkill /f /pid %a`。

**验收**:
- `start.bat` 不再 kill 全局 node.exe
- 只清理端口 5173 上的进程

**估时**: 10min

---

### P2-22 — `main.rs` 窗口 label "main" 可能不存在

**严重度**: 🟡 P2 | **来源**: 编译器错误
**文件**: `src-tauri/src/main.rs:40`

**问题**:

```rust
app.get_webview_window("main")
```

如果 `tauri.conf.json` 中窗口 label 不是 "main"（例如用户修改了配置），devtools 不会打开，无错误提示。

**方案**:

改用 `app.webview_windows().values().next()` 获取第一个窗口，或从 config 读取 label。

**验收**:
- 无论窗口 label 如何配置，devtools 都能打开
- `cargo build` 通过

**估时**: 10min

---

### P2-23 — `tauri.conf.json` CSP 开发模式过于宽松

**严重度**: 🟡 P2 | **来源**: 调试过程
**文件**: `src-tauri/tauri.conf.json:27`

**问题**:

当前 CSP 设置了 `'unsafe-inline'` 和 `'unsafe-eval'`，以及 `http://localhost:5173` 等开发 URL。这在开发模式下可以接受，但生产构建会使用 `frontendDist` 目录，应恢复为更严格的生产 CSP。

**方案**:

记录此问题为技术债。生产构建前需要恢复严格 CSP。

**验收**:
- CHANGELOG 记录此项

**估时**: N/A（技术债标记）

---

### P2-24 — `enabledelayedexpansion` 在非英语 Windows 上的兼容性

**严重度**: 🟡 P2 | **来源**: 实际 Bug
**文件**: `start.bat`

**问题**:

中文 Windows CMD 对 `!var!` 语法支持有限。`setlocal enabledelayedexpansion` 在某些 Windows 配置下不生效。

**方案**:

将 `start.bat` 改为 PowerShell 脚本 `start.ps1`，享受原生 UTF-8 支持和更好的错误处理。

**验收**:
- `start.ps1` 可在 PowerShell 中双击运行
- 或：`start.bat` 使用 `call` + 临时文件替代 `!var!` 语法

**估时**: 30min

---

## 四、P3 低优先级 — 20 项

### P3-1 — `errorText.js` 默认回退 "操作失败" 不区分上下文

**严重度**: 🟢 P3 | **来源**: Agent 2
**文件**: `frontend/src/utils/errorText.js:66`

**问题**:

任何未被匹配的错误都回退到 `"操作失败。"`，没有操作名、没有上下文。

**方案**:

在默认回退中拼接 action 名称（如存在）。

**验收**:
- 默认回退消息包含可用上下文
- `npx vitest run` 全量通过

**估时**: 10min

---

### P3-2 — `actionFailure.js` 将原因和下一步压缩在一行文本中

**严重度**: 🟢 P3 | **来源**: Agent 2
**文件**: `frontend/src/utils/actionFailure.js:47`

**问题**:

```js
`原因：{reason} 后续：{nextStep}`
```

原因和下一步被压成一行文本。UI 没有区分这两部分的结构。

**方案**:

返回结构化对象而非扁平字符串，由 UI 分别渲染。

**验收**:
- `actionFailure.js` 返回结构化数据
- UI 可以将 "原因" 和 "后续" 分开渲染

**估时**: 15min

---

### P3-3 — 部分 `en-US.js` 翻译键含 Unicode 转义序列而非英文

**严重度**: 🟢 P3 | **来源**: Agent 2
**文件**: `frontend/src/i18n/locales/en-US.js:7-23`

**问题**:

```js
'\\u4ee3\\u7406\\u6a21\\u5757': null  // "代理模块"
```

使用 Unicode 转义序列作为键名，维护困难。

**方案**:

将 Unicode 转义序列替换为实际中文字符。

**验收**:
- `en-US.js` 不再包含 `\\uXXXX` 格式的键
- `npx vitest run` 全量通过

**估时**: 10min

---

### P3-4 — CLI help 文本是英文

**严重度**: 🟢 P3 | **违反**: GP §2.1 | **来源**: Agent 3
**文件**: `src/cli_support.rs:11`

**问题**:

```rust
"Usage:\n  quantpilot                      Start the QuantPilot API server\n..."
```

CLI 帮助文本全部英文。

**方案**:

汉化 CLI 帮助文本。

**验收**:
- `cli_usage()` 返回中文文本
- `cargo test --workspace` 全量通过

**估时**: 10min

---

### P3-5 — `frontend_api_types.rs` 多个 enum 无 `description()` 方法

**严重度**: 🟢 P3 | **来源**: Agent 3
**文件**: `src/frontend_api_types.rs`

**问题**:

`RuntimeTimelineCompactability`、`SandboxVerdict`、`OpsDataHealth` 等枚举变体名（如 `DeclaredOnly`、`SourceChanged`、`ReplayFidelityPartial`）无人可读解释。

**方案**:

为每个枚举添加 `impl` 块和 `fn description(&self) -> &str` 方法。

**验收**:
- 至少 3 个关键枚举有 `description()` 方法
- `cargo check` 通过

**估时**: 30min

---

### P3-6 — `graphStore.js` 的 `recoverLatestRunnableGraph` 重试过程无用户反馈

**严重度**: 🟢 P3 | **来源**: Agent 2
**文件**: `frontend/src/store/graphStore.js:268-336`

**问题**:

10 次重试 × 1.5s = 最长 15s 的静默等待。用户不知道系统在做什么。

**方案**:

在 `runtime.backendError` 中设置临时消息 "正在重试连接后端..."。

**验收**:
- 重试期间显示 loading 消息
- `npx vitest run` 全量通过

**估时**: 10min

---

### P3-7 — `App.jsx` Tauri 窗口 API 失败被静默捕获

**严重度**: 🟢 P3 | **来源**: Agent 2
**文件**: `frontend/src/App.jsx:64`

**问题**:

```jsx
try { appWindow = getCurrentWindow(); } catch (_) { /* 非 Tauri 环境 */ }
```

错误完全被吞噬。如果 Tauri 可用但因临时问题失败，用户看不到错误且没有自定义标题栏。

**方案**:

在开发模式下 console.warn 记录失败原因。

**验收**:
- Tauri API 失败时有开发模式日志
- `npx vitest run` 全量通过

**估时**: 5min

---

### P3-8 — QS0004 在 i18n 检查中但从未在代码中使用

**严重度**: 🟢 P3 | **来源**: Agent 4
**文件**: `tools/check-i18n.ps1:20`

**问题**:

`QS0004` 出现在检查脚本的诊断码列表中，但在 `quantscript/src/` 中没有任何 `Diagnostic` 调用使用它。僵尸代码。

**方案**:

从检查脚本中删除 QS0004，或在代码中恢复使用。

**验收**:
- `grep "QS0004" quantscript/src/` 和 `grep "QS0004" tools/check-i18n.ps1` 结果一致（要么都有，要么都无）

**估时**: 5min

---

### P3-9 — QS 编译器多个位置共享同一个 `ERR_MISSING_STRATEGY_FN` 常量

**严重度**: 🟢 P3 | **来源**: Agent 4
**文件**: `quantscript/src/lowering/orchestrator.rs:26`

**问题**:

```rust
const ERR_MISSING_STRATEGY_FN: &str = "...";
```

多个 lowering 阶段使用同一个常量作为错误消息。如果不同阶段需要不同的错误说明（例如给出不同的修复建议），当前设计限制了可定制性。

**方案**:

保留常量但增加参数化。不同调用点传入不同的 `suggestion` 部分。

**验收**:
- `ERR_MISSING_STRATEGY_FN` 仍被使用，但错误上下文更具体
- `cargo test --workspace` 全量通过

**估时**: 15min

---

### P3-10 至 P3-20 — CSS/JSX 小修复

以下 11 项为低优先级样式微调，不影响用户理解，列在此处以备后续清理：

| # | 项 | 文件 | 估时 |
|---|-----|------|:--:|
| P3-10 | `backtest-analysis.css` 中 `font-size: 11px/12px` 令牌化 | backtest-analysis.css | 10min |
| P3-11 | `styles.css` 剩余 `border-radius: 8px` 令牌化 | styles.css | 5min |
| P3-12 | 交互元素 transition 补充 (sidebar-search 等) | styles.css | 10min |
| P3-13 | `--ad-border-rgb` 应用于所有 RGB 颜色引用 | 多个 CSS | 15min |
| P3-14 | `AlertsPage.jsx` 中 `style={{}}` → CSS 类 | AlertsPage.jsx | 15min |
| P3-15 | `SnapshotsPage.jsx` 中 `style={{}}` → CSS 类 | SnapshotsPage.jsx | 10min |
| P3-16 | `RunbookPage.jsx` 中 `style={{}}` → CSS 类 | RunbookPage.jsx | 10min |
| P3-17 | `ChaosPage.jsx` 中 `style={{}}` → CSS 类 | ChaosPage.jsx | 10min |
| P3-18 | 空 CSS 变量声明清理 | 多个 CSS | 10min |
| P3-19 | 跨文件重复 `@keyframes` 合并 | 多个 CSS | 10min |
| P3-20 | `npx vite build` CSS 警告清零 | 多个 CSS | 20min |

---

## 五、验收总表

| 严重度 | 项数 | 估时 | 验收条件 |
|:--:|:--:|:--:|------|
| S0 | 13 | ~5h | GP §2.1 全中文错误、诊断码不冲突、start.bat 可正常启动、App.jsx 不崩 |
| P1 | 22 | ~8h | 错误消息含可执行指引、span 补齐、分页 total 保留、loading 有阶段指示 |
| P2 | 24 | ~5h | 模糊措辞消除、API 错误格式改善、i18n 缺失记录、技术债标记 |
| P3 | 20 | ~3h | CSS 令牌化收尾、内联样式清理、enum 文档补齐 |
| **合计** | **79** | **~21h** | |

---

## 六、总体验收

```bash
# 门禁全量通过
cargo check --workspace
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
npx vite build
npx vitest run
npm audit --audit-level=moderate

# 质量检查
grep -rn "bail!\|anyhow!" src/ --include="*.rs" | grep -v target | grep -v "^[^:]*://"
# → 不能包含英文自然语言文本

grep -rn "下层转换\|可执行主干\|形式化 QuantScript" quantscript/src/ src/compile_diagnostics.rs
# → 返回 0

grep -rn "is not supported in the current beta" src/
# → 返回 0

grep -rn "QS0609" quantscript/src/ --include="*.rs"
# → 仅 analysis.rs 一处

grep -rn "eprintln.*QS" quantscript/src/ --include="*.rs"
# → 返回 0

grep -rn "capabilityMessage.*'" frontend/src/store/graphStore.js
# → 以下不应包含硬编码英文

# 前端加载验证
npx vite build  # 无新增 CSS warning
npx vitest run  # 269 测试全量通过
```
