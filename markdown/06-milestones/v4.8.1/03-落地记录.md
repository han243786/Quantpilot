# v4.8.1 落地记录 — API 契约与部署治理

> 日期: 2026-05-26
> 范围: OpenAPI 结构、route diff 基线、全量树同步
> 裁剪: 账户相关内容已按用户要求移出本轮执行范围

---

## 已落地

| 项 | 结果 | 证据 |
|----|------|------|
| OpenAPI 凭证路径结构 | `/api/credentials` 与 `/api/credentials/{service}` 已回到顶层 `paths` | `contracts/openapi/root.yaml` |
| Route diff 基线 | 新增 `tools/check-openapi-route-diff.ps1`, 支持 `-FailOnDiff` | `powershell ... check-openapi-route-diff.ps1 -FailOnDiff` 通过 |
| Executor API 规范补齐 | 补齐 health、strategy detail、start、stop、klines、params 路径 | OpenAPI route diff clean |
| 文档裁剪 | v4.8.1 规划和清单改为非账户治理范围 | `v4.8.1/01-规划方案.md`, `02-综合优化清单.md` |

## 验证

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-openapi-route-diff.ps1 -FailOnDiff
powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-user-facing-text.ps1
git diff --check
```

## 边界

- 未新增登录、注销、密码找回、会话撤销或 refresh token 行为。
- route diff 现在为阻断可用基线, 后续新增公开 API 必须同步 OpenAPI 与全量树。
