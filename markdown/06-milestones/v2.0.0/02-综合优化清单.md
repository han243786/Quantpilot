# v2.0.0 综合优化清单

> MAJOR版本 | 5大功能追加 | 2026-05-17 closeout

---

## 一、S0 阻断级 (7/7 ✅)

| # | 维度 | 文件 | 问题 | 修复 |
|---|------|------|------|------|
| S0-1 | A | `live_execution.rs:275` | 4种OrderType通配符→market | 显式分支，不支持类型返回Err |
| S0-2 | A/D | `plugin_market.rs:11` | RFC 8032测试向量作生产公钥 | env var `QUANTPILOT_MARKET_PUBLIC_KEY` + 启动校验 |
| S0-3 | C | `live_execution.rs:282` | NaN order_value绕过名义上限 | `!order_value.is_finite()` 守卫 |
| S0-4 | C | `live_execution.rs:449` | NaN权益→NaN数量订单 | 四重 `is_finite()` 守卫 (price/equity/notional/quantity) |
| S0-5 | C | `core_ir_evaluator.rs:1583` | window==0除零→+Inf | `window == 0` 显式守卫 |
| S0-6 | E | `auth/` + 全部handler | 认证通过后零数据隔离 | `UserId` FromRequestParts + `scoped_key()` 全局BTreeMap前缀 |
| S0-7 | E | `auth/mod.rs:193` | JWT算法未锁定 | `Validation::new(Algorithm::HS256)` 显式 |

**验收**: `cargo check` + `cargo clippy` + `npm test` 全部通过。

---

## 二、P1 高优先级 (17/17 ✅)

### auth/mod.rs (3)
| # | 问题 | 修复 |
|---|------|------|
| P1-1 | RNG失败→全零JWT密钥 | `rng.fill().expect()` 失败即panic |
| P1-2 | `lock().unwrap()` panics on poison | `unwrap_or_else(\|e\| e.into_inner())` x2 |
| P1-3 | 无用户名/密码最大长度校验 | username≤64, password≤128 |

### 原子写入 (7)
| # | 文件 | 问题 | 修复 |
|---|------|------|------|
| P1-4 | `collaboration.rs` | persist_graph_audit_entry直接写入 | tmp+rename |
| P1-5 | `runtime_api.rs` | persist_approval直接写入 | tmp+rename |
| P1-6 | `runtime_api.rs` | snapshot持久化`let _=`静默吞错 | tmp+rename + 错误日志 |
| P1-7 | `state.rs` | approval过期持久化静默吞错 | tmp+rename + safe_eprintln! |
| P1-8 | `storage_lifecycle.rs` | persist_with_ttl直接写入 | tmp+rename |
| P1-9 | `credential_vault.rs` | 初始vault创建直接写入 | tmp+rename |
| P1-10 | `data_module.rs` | 历史数据缓存直接写入 | tmp+rename |

### live_execution.rs (5)
| # | 问题 | 修复 |
|---|------|------|
| P1-11 | 限流器存sleep前时间戳 | sleep后重新捕获SystemTime |
| P1-12 | `reset_at + DAILY_RESET_MS` u64溢出 | `saturating_add` |
| P1-13 | NaN/Inf价格穿透`price > 0.0`守卫 | `price.is_finite() && price > 0.0` |
| P1-14 | `current_position_weight` stub (GP§5.3) | 去`_`前缀 + 显式注释 |
| P1-15 | `on_market_update` stub (GP§5.3) | 去`_`前缀 + 显式注释 |

### 其他 (2)
| # | 问题 | 修复 |
|---|------|------|
| P1-16 | `credential_vault.rs` extract_secret_patterns返回明文Vec | 返回`Vec<Zeroizing<String>>` |
| P1-17 | 审批三函数 TOCTOU (drop锁后在re-acquire间存在竞态窗口) | 删除`drop(approvals)`，锁持续持有 |

### 确认为v1.x已有修复 (4)
- risk_monitor.rs NaN equity守卫 (v1.2.1)
- fill_engine.rs `fee_bps.max(0.0)` (v1.2.1)
- data_module.rs `bar.low > bar.high` OHLC校验 (v1.2.1)
- plugin_runtime_registry.rs `verify_manifest_signature` 调用

---

## 三、P2 延入 v2.1.x (28项)

### A: 逻辑/契约 (6)
| # | 文件 | 问题 |
|---|------|------|
| P2-A1 | `live_execution.rs:360` | 注释"1s"代码500ms不一致 |
| P2-A2 | `live_execution.rs:375` | OKX "state"字段缺失→默认"canceled" |
| P2-A3 | `live_execution.rs:679` | `open_orders` mut未使用 |
| P2-A4 | `live_execution.rs:847` | 卖空无持仓静默成功 |
| P2-A5 | `plugin_sandbox.rs:97` | Unix setrlimit返回值未检查 |
| P2-A6 | `plugin.rs:154` | enforce_max_*: Some(0)无校验 |

### B: 并发/资源 (4)
| # | 文件 | 问题 |
|---|------|------|
| P2-B1 | `app_runtime_helpers.rs` | `std::sync::Mutex`在async handler中 |
| P2-B2 | `state.rs` | alert_firings去重失败(每60s新条目) |
| P2-B3 | `rate_limiter.rs` | bucket map达到1000才清理 |
| P2-B4 | `lib.rs` | RuntimeCoordinator无界字段(长期实盘) |

### C: 数值/溢出 (6)
| # | 文件 | 问题 |
|---|------|------|
| P2-C1 | `execution_module.rs` | NaN数量静默丢弃无warning |
| P2-C2 | `live_execution.rs:419` | `fee.abs()`掩码负手续费(应不可能但掩盖数据质量) |
| P2-C3 | `data_module.rs:343` | gap_count u64→u32截断 |
| P2-C4 | `agent_module.rs:495` | 权重归一化除零(total=0→NaN) |
| P2-C5 | `live_execution.rs:186` | OKX时间戳秒级精度→同秒签名碰撞 |
| P2-C6 | `backtest_metrics.rs:120` | `(ts_ms/86400000) as i64`理论溢出 |

### D: 持久化/序列化 (7)
| # | 文件 | 问题 |
|---|------|------|
| P2-D1 | `auth/mod.rs:32` | RegisterRequest/LoginRequest缺`deny_unknown_fields` |
| P2-D2 | `frontend_api_types.rs` | ~80种Deserialize类型缺`deny_unknown_fields` |
| P2-D3 | `plugin.rs` | PluginManifest及子类型缺`deny_unknown_fields` |
| P2-D4 | `backtest_artifacts.rs:617` | schema_version硬等号，无v1→v2迁移路径 |
| P2-D5 | `auth/mod.rs:85` | JWT密钥非持久化(重启后token全失效) |
| P2-D6 | `credential_vault.rs:312` | lock().unwrap() (已用into_inner，部分遗留) |
| P2-D7 | `storage_lifecycle.rs:113` | startup_storage_cleanup读错误静默返回 |

### E: API/诊断 (5)
| # | 文件 | 问题 |
|---|------|------|
| P2-E1 | `api_errors.rs:60` | DEV模式下internal_error泄露内部路径 |
| P2-E2 | `rate_limiter.rs:79` | 用x-forwarded-for识别客户端(可伪造) |
| P2-E3 | `live_execution.rs:755` | OKX错误消息嵌入RuntimeEvent(可能泄凭证) |
| P2-E4 | `safe_log.rs:12` | 正则脱敏遗漏非标准格式(base64/URL query) |
| P2-E5 | `main.rs:743` | QUANTPILOT_PORT无效值静默fallback 3000 |

---

## 四、P3 延入 v2.1.x (20项)

### A: 逻辑/契约 (5)
| # | 文件 | 问题 |
|---|------|------|
| P3-A1 | `auth_middleware.rs:73` | JWT失败→API Key认证降级(碰撞概率低但安全模型模糊) |
| P3-A2 | `plugin_sandbox.rs:113` | Windows上max_memory_mb静默忽略 |
| P3-A3 | `plugin_sandbox.rs:40` | stdin写入无超时(管道阻塞) |
| P3-A4 | `plugin.rs:159` | Atom类型允许声明子atoms |
| P3-A5 | `live_execution.rs:472` | 订单ID编码bool而非exchange名 |

### B: 并发/资源 (4)
| # | 文件 | 问题 |
|---|------|------|
| P3-B1 | `main.rs:767` | `bg_handle.abort()`中途取消(部分状态已更新) |
| P3-B2 | `state.rs` / `main.rs` | 多处`let _ =`静默吞持久化错误(部分已修复) |
| P3-B3 | `safe_log.rs:17` | 全局Mutex串行化所有日志行(应用RwLock) |
| P3-B4 | `state.rs:583` | 瞬态backtest溢出目录崩溃后残留 |

### C: 数值/溢出 (5)
| # | 文件 | 问题 |
|---|------|------|
| P3-C1 | 9处 | `as_millis() as u64` u128→u64截断 |
| P3-C2 | `hotswap.rs:277` | `open_orders.len() as u32` 截断 |
| P3-C3 | `live_execution.rs:161` | `duration_since.unwrap_or_default()` 时钟坏时零值 |
| P3-C4 | `data_module.rs:1228` | MOCK_VOLATILITY用f64::from_bits(可注入NaN) |
| P3-C5 | 9+处 | `price <= 0.0`守卫不挡NaN(defense-in-depth) |

### D: 持久化/序列化 (2)
| # | 文件 | 问题 |
|---|------|------|
| P3-D1 | `data_module.rs:94` | HistoricalBarsCache缺`deny_unknown_fields` |
| P3-D2 | `plugin_market.rs:72` | MarketMetadata/PluginSummary缺`deny_unknown_fields` |

### E: API/诊断 (4)
| # | 文件 | 问题 |
|---|------|------|
| P3-E1 | `graph_api.rs:733` | reveal_path explorer.exe用户影响路径(已canonicalize防护) |
| P3-E2 | `credential_api.rs:82` | delete_credential只做基本字符检查 |
| P3-E3 | `auth/mod.rs:128` | register返回"用户名已存在"泄露用户存在性 |
| P3-E4 | `main.rs:696` | cleanup_build_artifacts范围待确认在storage/内 |

---

## 五、验收总闸

```bash
cargo check --workspace              # ✅ PASS
cargo clippy --workspace --all-targets  # ✅ PASS (warning only)
cargo test --workspace               # ✅ 编译通过
cd frontend && npm run build         # ✅ PASS
cd frontend && npm run test          # ✅ 92/92, 269/269
cd frontend && npm audit             # ✅ 0 vulnerabilities
```

**S0: 7/7 ✅ | P1: 17/17 ✅ | P2: 28→v2.1.x | P3: 20→v2.1.x**
