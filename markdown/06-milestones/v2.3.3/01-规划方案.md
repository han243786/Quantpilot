# v2.3.3 规划方案

> 版本类型: PATCH | 基准: v2.3.2 | 日期: 2026-05-19

---

## 目标

- 修复两轮自由维度诱错审计发现的全部 9 条 S0 阻断项
- 修复 15 条最关键的 P1 高危发现 (安全/数据完整性/用户阻塞)
- 升级 GP 合规矩阵至 v2.3.3
- 门禁 10/10 全量通过

## 非目标

- 不涉及新功能开发
- 不涉及大规模重构
- 不涉及 P2/P3 项修复 (留给 v2.4.0)
- 不改变 API 接口

## 背景

v2.3.2 完成架构优化 (ISP拆分 + 模块重构) 后, 执行了两轮自由维度诱错审计 (11维度):
- **第1轮 A~E**: 代码级安全扫描 — 87条发现 (4 S0, 26 P1, 31 P2, 26 P3)
- **第2轮 F~J + UX**: 上层质量与交互完整性 — 128条发现 (5 S0, 42 P1, 57 P2, 24 P3)
- **合计**: 215条发现 (9 S0, 68 P1, 88 P2, 50 P3)

v2.3.3 聚焦于 S0 阻断项的全部修复和 P1 关键项的修复。

## 方案

### S0 阻断项 (9条, 已修复)

| # | 维度 | 文件 | 修复方案 |
|---|------|------|---------|
| 1 | B | `src/main.rs` | 拆分嵌套写锁为两阶段: 先收集过期ID, 释放approval锁, 再获取proposal锁 |
| 2 | E | `src/auth_middleware.rs` | JWT验证失败直接返回401, 移除API Key降级路径 |
| 3 | E | `src/main.rs` | CORS origin 解析拒绝 `*` 通配符和非 http(s) scheme |
| 4 | E | `src/graph_api.rs` | graph_version_dir 内调用 sanitize_storage_path_segment 纵深防御 |
| 5 | G | `src/graph_quantscript_api.rs` | 未知意图模块键返回 `anyhow::bail!` 明确错误 |
| 6 | G | `frontend/src/graph/compileGraph.js` | 前端未知意图抛出 Error 而非静默降级为 ma_cross |
| 7 | I | `markdown/02-protocol/README.md` | RFC-017/018/019 状态 ✅→🔄 与各RFC文档一致 |
| 8 | I | `markdown/04-guides/guide-api-reference.md` | 更新废弃声明, 指向权威来源 (openapi.yaml + system-architecture.md) |
| 9 | I | `markdown/03-implementation/governance/implementation-support-matrix.md` | 修复全部损坏的绝对路径引用为相对路径 |

### P1 关键项 (15条)

| # | 维度 | 文件 | 问题 | 修复方案 |
|---|------|------|------|---------|
| P1-1 | D | `src/backtest_artifacts.rs` | write_json 缺少 fsync | 补齐 fsync(tmp) + fsync(parent) |
| P1-2 | D | `src/snapshot_service.rs` | persist_snapshot 缺少 fsync | 补齐原子写入四步 |
| P1-3 | D | `src/alert_engine.rs` | persist_alert_firing 缺少 fsync | 补齐原子写入 |
| P1-4 | D | `src/collaboration.rs` | persist_graph_audit_entry 缺少 fsync | 补齐原子写入 |
| P1-5 | D | `src/chaos_experiment.rs` | 报告写入缺少 fsync | 补齐原子写入 |
| P1-6 | D | `src/runtime/mutation.rs` | 两处写入缺少 fsync | 补齐原子写入 |
| P1-7 | D | `src/storage_lifecycle.rs` | ensure_storage_quota 写入缺少 fsync | 补齐原子写入 |
| P1-8 | D | `qrpc_runtime/src/data_module.rs` | 历史缓存写入缺少 fsync | 补齐原子写入 |
| P1-9 | D | `src/backup.rs` | 备份清单写入缺少 fsync | 补齐原子写入 |
| P1-10 | D | `src/main.rs` | 待办项持久化缺少 fsync | 补齐原子写入 |
| P1-11 | B | `src/main.rs` | 8个BTreeMap无界增长 | 后台任务扩展淘汰逻辑 |
| P1-12 | A | `frontend/src/styles.css` | radial-gradient 违反 GP §8.3 | 改用纯色或 SVG 图案 |
| P1-13 | A | `frontend/src/styles.css` | 圆角 12px 违反 GP §8.2 (>8px) | 改为 var(--ad-radius-lg) |
| P1-14 | UX | `frontend/src/components/TopToolbar.jsx` | 编译/回测按钮无加载状态 | 添加 compiling/backtesting 文字反馈 |
| P1-15 | UX | 全局 | 技术术语泄漏 (Strategy IR, Core IR 等) | 替换为用户友好表述 |

### P1 原子写入统一方案

**策略**: 抽取公共模块 `atomic_write_json_with_fsync()` 到 `src/runtime_persistence.rs`, 所有11处调用点统一替换。

```rust
// 新公共函数, 替代各处手写的 write+rename 模式
pub(crate) async fn atomic_write_json_with_fsync(
    dir: &Path, filename: &str, data: &[u8]
) -> std::io::Result<()> {
    tokio::fs::create_dir_all(dir).await?;
    let final_path = dir.join(filename);
    let tmp = dir.join(format!("{}.tmp", filename));
    tokio::fs::write(&tmp, data).await?;
    // fsync tmp
    tokio::fs::File::open(&tmp).await?.sync_all().await?;
    tokio::fs::rename(&tmp, &final_path).await?;
    // fsync parent dir
    if let Some(parent) = final_path.parent() {
        tokio::fs::File::open(parent).await?.sync_all().await?;
    }
    Ok(())
}
```

### P1 前端UX修复方案

| 文件 | 修改内容 |
|------|---------|
| `TopToolbar.jsx` | 编译/模拟/回测按钮增加 `compiling/running/backtesting` 加载态文字 |
| `utils/actionFailure.js` | 替换 "Strategy IR" 为 "策略编译中间表示" 或直接删除技术细节 |
| `StrategyWorkspaceOverviewTab.jsx` | 折叠技术指标区域, 默认隐藏 "Strategy IR 角色" |
| `BacktestDetailPage.jsx` | 为夏普/索提诺/卡尔玛等指标添加 tooltip 说明 |
| `StrategyWorkspacePage.jsx` | <1180px 时显示引导提示而非不可用的压缩画布 |
| 全局 `t()` 调用 | 补充 `zh-CN.js` 中缺失的翻译键 |

## 验收

| # | 验收项 | 命令 |
|---|--------|------|
| 1 | 编译通过 | `cargo check --workspace` |
| 2 | 测试全量通过 | `cargo test --workspace` |
| 3 | Clippy 零警告 | `cargo clippy --workspace --all-targets -- -D warnings` |
| 4 | 前端构建 | `cd frontend && npm run build` |
| 5 | 前端测试 | `cd frontend && npm run test` |
| 6 | 全部门禁 | `.\tools\run-closeout-gates.bat` |
| 7 | 原子写入路径 fsync 补齐 | `grep -r "fs::write\|fs::rename" src/ --include="*.rs" \| grep -v "fsync\|tmp+rename\|atomic_write"` 应返回0 |
| 8 | GP 合规矩阵更新 | `markdown/05-testing/GP合规矩阵-v2.3.3.md` 存在且 28/28 |
| 9 | 未知意图报错可见 | 编译含未知 module_key 的意图节点 → 返回中文错误 |
| 10 | 加载反馈可见 | 点击编译/回测 → 按钮文字变为 "编译中..."/"回测中..." |

## 风险

| 风险 | 影响 | 缓解 |
|------|------|------|
| atomic_write_json_with_fsync 重构范围大 (11处) | 可能引入回归 | 逐文件替换, 每替换一处跑一次 cargo test |
| JWT 降级移除影响现有 DevOps 脚本 | 使用 API Key 的脚本需改用 JWT | 文档注明变更, 提供迁移指引 |
| 前端编译错误抛出可能影响现有 try/catch | 未捕获异常导致白屏 | 在调用处包裹 try/catch 并展示错误消息 |
