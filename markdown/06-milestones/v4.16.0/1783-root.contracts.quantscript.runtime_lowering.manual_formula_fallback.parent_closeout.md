# v4.16.0 root.contracts.quantscript.runtime_lowering.manual_formula_fallback parent closeout

> Batch: BE-002JN-01
> Node: `root.contracts.quantscript.runtime_lowering.manual_formula_fallback`
> Stage: `parent_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`parent_closeout: true`.

`manual_formula_fallback` is closed as a parent after RSI, momentum, MACD, and
ZScore recognizer leaves were extracted and closed. The remaining moving-average
window fallback, boundary lookback helper, local sum-window compatibility path,
and fallback tests are intentionally retained in the parent module.

This follows the current terminal leaf control guidance: the parent now sits in
the target terminal range and the remaining children are too small or too
tightly coupled to justify separate recursive cycles.

## Residual Judgment

| Residual | Decision | Reason |
| --- | --- | --- |
| `manual_moving_average_window` | Keep in parent | Direct moving-average handling shares sum-window compatibility and parent fallback order |
| `boundary_lookback_fallback` | Keep in parent | Single public helper is better preserved as a local compatibility surface |
| `fallback_test_harness` | Keep in parent | Tests exercise several fallback recognizers together and remain useful as parent-level evidence |

## Terminal Leaf Control

`tools/evaluate-leaf-granularity.ps1 -Path quantscript/src/lowering/fallback.rs` result:

| Metric | Result |
| --- | --- |
| `split_decision` | `STOP` |
| `governance_packaging` | `stop_split` |
| `final_decision` | `STOP` |
| `normalized_split_score` | 58 |
| `loc` | 513 |
| `functions` | 35 |
| `public_surface` | 13 |
| `branches` | 21 |
| `risk_tags` | `cross_crate`, `public_api` |
| `governance_mode` | `stop_split` |

## Closed Children

| Child | Status |
| --- | --- |
| `manual_rsi_formula` | closed |
| `manual_momentum_formula` | closed |
| `manual_macd_formula` | closed |
| `manual_zscore_formula` | closed |

## Verification

| Gate | Result |
| --- | --- |
| Parent residual judgment | PASS |
| Terminal leaf control | PASS, `STOP` |
| Rust movement | none |

Governance gates are run before commit for diff whitespace, UTF-8, full feature tree, and matrix governance.

## Next Step

BE-002JO-01 return to `root.contracts.quantscript.runtime_lowering` parent residual judgment.
