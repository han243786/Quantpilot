# v4.16.0 root.contracts.runtime_support.v4_runtime_support.event_replay_orchestration extract closeout

> Batch: BE-002DT-01
> Node: `root.contracts.runtime_support.v4_runtime_support.event_replay_orchestration`
> Parent: `root.contracts.runtime_support.v4_runtime_support`
> Stage: `actual_extraction`
> Movement: Rust code movement.

---

## Extracted Child

`qrpc_runtime/src/v4_runtime/event_replay_orchestration.rs` now owns the v4 runtime replay/input orchestration surface:

- `V4PaperSimulatedRuntime::run_backtest_bars`
- `V4PaperSimulatedRuntime::run_backtest_ticks`
- `V4PaperSimulatedRuntime::submit_event`
- `V4PaperSimulatedRuntime::submit_market_price_tick`
- `V4PaperSimulatedRuntime::submit_market_bar_closed`
- `V4PaperSimulatedRuntime::advance_time`
- `V4PaperSimulatedRuntime::output_since`
- `V4PaperSimulatedRuntime::events_since_and_trim`
- `V4PaperSimulatedRuntime::trim_event_log`
- `V4PaperSimulatedRuntime::run_until_idle`

The v4 runtime parent now declares `mod event_replay_orchestration;`.

## Boundary Preserved

The extraction deliberately keeps these parent-owned:

- shared event construction helpers such as `enqueue_graph_event` and `record_control_event`;
- public snapshot surfaces;
- `process_event`;
- machine transition matching/application;
- risk-plane and execution-capability decision evaluation;
- simulated execution order/fill internals;
- v4 tests.

`events_since_and_trim` and `trim_event_log` are `pub(super)` because parent-owned recovery and simulated-market update paths still call them.

## Equivalence Notes

- Bar and tick replay ordering remains deterministic.
- Market input validation still fails closed before enqueue.
- Replay artifacts still project trajectory, risk-plane records, execution capability sources, microstructure metrics, and final snapshot.
- Event queue drain still enforces `V4_RUNTIME_MAX_EVENT_STEPS`.
- Public inherent methods remain available through `V4PaperSimulatedRuntime` / `V4Runtime`.

## Verified Gates

- `cargo fmt --check`: PASS
- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime v4 -- --nocapture`: PASS, 32 passed
- `cargo check -p quantpilot`: PASS

Pending governance gates are expected to run before commit:

- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002DT-02 `root.contracts.runtime_support.v4_runtime_support.event_replay_orchestration` single_leaf_closeout.
