# v4.16.0 root.contracts.runtime_support.core_ir_evaluator parent residual judgment selects shared_lookup_math_helpers

> Batch: BE-002AP-01
> Node: `root.contracts.runtime_support.core_ir_evaluator`
> Parent: `root.contracts.runtime_support`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> Selected child: `root.contracts.runtime_support.core_ir_evaluator.shared_lookup_math_helpers`

---

## Decision

`root.contracts.runtime_support.core_ir_evaluator.shared_lookup_math_helpers` is selected as the next residual candidate.

This is a judgment step only. It does not authorize code movement yet.

## Evidence

The already-closed children are:

- `classic_indicator_wave`
- `advanced_indicator_wave`
- `spread_custom_expression_wave`

The remaining parent file still owns:

- public evaluator DTOs, error enum, trait, registry, and `evaluate_indicator_signal`;
- shared lookup helpers: `param_or_default`, `find_kline_snapshot`, `find_quote_snapshot`, and `find_quote_snapshots`;
- shared math helpers: moving average, threshold/ratio scaling, EMA/SMA, RSI, MACD, momentum, z-score, true range, ATR, Bollinger, OBV, CMF, ADX, stochastic, CCI, SAR, Keltner, and Donchian helpers;
- local evaluator tests.

## Candidate Shape

The candidate is not yet proven as a physical child.

It must be evaluated as either:

- parent-owned helper pocket: no Rust movement, close as parent-mediated shared utility ownership; or
- extracted helper child: only if the baseline proves cleaner parent-mediated access without sibling shortcuts or wrapper churn.

## Boundary Rules

| Boundary | Requirement |
| --- | --- |
| Parent mediation | Closed evaluator children may keep using parent-owned helpers through `super::*`; they must not import a sibling helper module directly. |
| Public API | `indicator_registry` and `evaluate_indicator_signal` stay parent-owned. |
| Test boundary | Local tests remain queued separately as `test_harness`. |
| Release transition | No horizontal shortcut or performance bypass is proposed. |

## Next Step

BE-002AQ-01 `root.contracts.runtime_support.core_ir_evaluator.shared_lookup_math_helpers` baseline_plan.
