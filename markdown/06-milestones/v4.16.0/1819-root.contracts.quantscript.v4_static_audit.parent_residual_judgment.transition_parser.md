# v4.16.0 root.contracts.quantscript.v4_static_audit parent residual judgment selects transition_parser

> Batch: BE-002KH-01
> Node: `root.contracts.quantscript.v4_static_audit`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select next child:

`root.contracts.quantscript.v4_static_audit.transition_parser`

## Parent Residual Scan

Closed children:

- `runtime_handoff_builder`;
- `audit_entrypoint`;
- `capability_type_parser`;
- `event_catalog_derivation`;
- `parser_utilities_diagnostics`;
- `static_document_parser`;
- `machine_block_parser`;
- `state_block_parser`.

Remaining parent-owned clusters:

- state-group parsing;
- memory field parsing;
- transition parsing;
- edge and risk-plane parsing;
- machine template wrapper and thin capability/type wrappers;
- public DTOs and local static-audit tests.

## Why This Child

`transition_parser` is the next cohesive state-machine grammar leaf. It owns the
`on <event> from <state> to <state>` transition line, emit/write modifier
scanning, and `MachineTransition` assembly.

This is larger and more behavior-bearing than the remaining state-group or
memory single-line helpers, while still small enough to extract without
changing parent parser control flow.

## Proposed Ownership

| Field | Value |
| --- | --- |
| Candidate child | `transition_parser` |
| Planned file | `quantscript/src/v4_static_audit/transition_parser.rs` |
| Likely owned functions | `parse_transition` |
| Parent wrapper | `parse_transition` |
| Parent bridge dependencies | parser utilities: `diag`, `split_words`, `split_csv_words` |

## Exclusions For Next Baseline

- Do not move `parse_state_group`.
- Do not move `parse_memory`.
- Do not move `parse_edge`.
- Do not move `parse_risk_plane`.
- Do not move `parse_machine_template`.
- Do not move public report DTOs or local tests.
- Do not introduce release-transition links.

## Next Step

BE-002KI-01 `root.contracts.quantscript.v4_static_audit.transition_parser`
baseline_plan.
