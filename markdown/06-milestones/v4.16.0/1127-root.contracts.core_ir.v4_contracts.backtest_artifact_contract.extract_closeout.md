# v4.16.0 root.contracts.core_ir.v4_contracts.backtest_artifact_contract actual extraction complete

> Batch: BE-001TX-02
> Node: `root.contracts.core_ir.v4_contracts.backtest_artifact_contract`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `extract_closeout`
> Movement: Code movement complete.

---

## Summary

`root.contracts.core_ir.v4_contracts.backtest_artifact_contract` is now extracted as a private v4 child module.

The child owns exactly the five frozen DTOs:

- `V4BacktestArtifact`
- `V4BacktestMicrostructureMetrics`
- `V4BacktestMachineTrajectoryPoint`
- `V4BacktestRiskPlaneDecisionRecord`
- `V4BacktestExecutionCapabilitySourceRecord`

The v4 parent now declares and re-exports the child:

- `mod backtest_artifact_contract;`
- `pub use backtest_artifact_contract::*;`

## Boundary

**Real files**:

- `qrpc_core_ir/src/v4.rs`
- `qrpc_core_ir/src/v4/backtest_artifact_contract.rs`

**Markers**:

- `BE-001TX-02`

## White-Box Result

| Surface | Owner after extraction | Parent-mediated dependencies |
| --- | --- | --- |
| v4 backtest artifact root DTO | `backtest_artifact_contract` | `default_v4_backtest_artifact_version` |
| microstructure metrics DTO | `backtest_artifact_contract` | none |
| trajectory point DTO | `backtest_artifact_contract` | `MachineTemplateKind` |
| risk decision record DTO | `backtest_artifact_contract` | none |
| execution capability source record DTO | `backtest_artifact_contract` | `RuntimeTradingMode`, `ExecutionCapabilityKind`, `CapabilitySupportSource` |

## Non-Moved Surfaces

The extraction did not move:

- `MachineTemplateKind`
- `RuntimeTradingMode`
- `ExecutionCapabilityKind`
- `CapabilitySupportSource`
- `default_v4_backtest_artifact_version`
- `V4_BACKTEST_ARTIFACT_VERSION`
- validators, serde tests, runtime handlers, persistence, backend code, or frontend code

## Equivalence Evidence

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir` passed: 54 tests.
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`

## Next Step

BE-001TX-03 `root.contracts.core_ir.v4_contracts.backtest_artifact_contract` single_leaf_closeout.
