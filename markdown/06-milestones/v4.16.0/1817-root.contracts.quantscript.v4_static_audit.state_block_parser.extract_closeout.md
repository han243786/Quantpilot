# v4.16.0 root.contracts.quantscript.v4_static_audit.state_block_parser extract closeout

> Batch: BE-002KG-02
> Node: `root.contracts.quantscript.v4_static_audit.state_block_parser`
> Stage: `actual_extraction`
> Movement: Rust code movement complete.
> governance_next_authority: active

---

## Movement Summary

`quantscript/src/v4_static_audit/state_block_parser.rs` now owns the state line
and state block parser boundary.

Moved implementation:

- `parse_state`;
- `parse_state_block`;
- private `skip_block`;
- private `empty_child_machine`.

The parent `quantscript/src/v4_static_audit.rs` now declares
`mod state_block_parser;` and keeps private wrappers named `parse_state` and
`parse_state_block`.

## Preserved Boundaries

- Public API unchanged.
- `machine_block_parser` continues through parent `parse_state` and
  `parse_state_block` wrappers.
- Nested child-machine parsing still goes through the parent
  `parse_machine_block` wrapper.
- `parse_state_group`, `parse_memory`, `parse_transition`, `parse_edge`, and
  `parse_risk_plane` remain parent-owned.
- Public report DTOs and local static-audit tests remain parent-owned.
- No release-transition link was introduced.

## Gates

| Gate | Result |
| --- | --- |
| `cargo fmt --all --check` | PASS |
| `cargo test -p quantscript` | PASS, 88 tests |
| `cargo check -p quantpilot` | PASS, no warnings |
| `git diff --check` | PASS |
| `check-utf8.ps1` | PASS |
| `check-full-feature-tree.ps1` | PASS |
| `check-matrix-governance.ps1` | PASS |

## Next Step

BE-002KG-03 `root.contracts.quantscript.v4_static_audit.state_block_parser`
single_leaf_closeout.
