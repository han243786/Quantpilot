# v4.16.0 root.contracts.quantscript.typed_resolution parent residual judgment selects callable_classification_surface

> Batch: BE-002GE-01
> Node: `root.contracts.quantscript.typed_resolution`
> Selected child: `root.contracts.quantscript.typed_resolution.callable_classification_surface`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Parent State

Closed typed-resolution children:

- `contracts.quantscript.typed_resolution.public_type_surface`

Remaining internal families:

- callable classification helper functions;
- resolver orchestration and AST-to-HIR lowering;
- type inference and binding insertion;
- expression semantic inference;
- manual indicator formula inference;
- callable registry and helper classification tables;
- resolver-local test harness.

## Decision

Select `root.contracts.quantscript.typed_resolution.callable_classification_surface` as the next child.

This child should isolate callable/helper classification and registry seeding before deeper resolver behavior moves. It is a useful next split because semantic inference, type inference, and lowering all depend on stable callable kinds and helper taxonomy.

## Candidate Physical Surface

Current candidate functions in `quantscript/src/resolve/mod.rs`:

- `classify_series_capability_name`
- `classify_builtin_math_name`
- `classify_member_mutation_name`
- `build_resolved_callables`
- `seed_callable`
- `classify_imported_helper`
- `classify_change_smoothing_kind`
- `classify_fetch_source_kind`
- `is_known_helper_function`

The next baseline may adjust the exact ownership line if a helper is proven to belong with resolver orchestration, but it must keep public classifier behavior stable.

## Boundary

The next baseline must not include:

- public DTO/enums already closed under `public_type_surface`;
- `lower_script_to_typed_hir`;
- `expr_semantic_key`;
- `Resolver` lowering and type inference methods;
- expression semantic inference;
- manual indicator formula inference;
- tests, except as protected evidence.

## Why This Child Next

| Criterion | Judgment |
| --- | --- |
| Shared dependency value | High. Callable taxonomy is used by resolver and downstream lowering. |
| Cohesion | Strong. The selected surface maps names to typed callable/helper kinds. |
| Risk | Medium. Public classifiers must remain stable, but the implementation is table-like and test-covered. |
| Split value | High. It reduces the resolver parent before moving heavier AST-to-HIR and semantic inference behavior. |

## Next Step

BE-002GF-01 `root.contracts.quantscript.typed_resolution.callable_classification_surface` baseline_plan.
