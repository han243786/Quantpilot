# v4.16.0 root.contracts.runtime_support.v4_runtime_support.event_replay_orchestration single leaf closeout

> Batch: BE-002DT-02
> Node: `root.contracts.runtime_support.v4_runtime_support.event_replay_orchestration`
> Parent: `root.contracts.runtime_support.v4_runtime_support`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Decision

`stop_split: true`

The v4 runtime event replay orchestration child is terminal for the current recursive pass.

## Terminal Leaf Control

`tools/evaluate-leaf-granularity.ps1 -Path qrpc_runtime/src/v4_runtime/event_replay_orchestration.rs` result:

- Decision: `STOP`
- Score: `44`
- LOC: `383`
- Functions: `10`
- Public surface: `8`
- Branches: `26`
- Domains: `6`
- Risk tags: `cross_crate`, `persistence`, `public_api`, `route_schema`, `state_machine`, `trading_semantics`
- Governance mode: `stop_split`

## Evidence

The extracted child owns one coherent orchestration responsibility:

- deterministic bar/tick replay;
- public external input submission;
- market input validation and replay event submission;
- event queue drain until idle;
- event-log window projection;
- replay artifact projection for trajectory, risk-plane decisions, execution capability sources, microstructure metrics, and final snapshot.

Further splitting bar replay, tick replay, input submission, and local output helpers would duplicate artifact projection and create micro leaves around shared event/snapshot behavior that remains parent-owned.

## Verified Gates

Inherited from BE-002DT-01:

- `cargo fmt --check`: PASS
- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime v4 -- --nocapture`: PASS, 32 passed
- `cargo check -p quantpilot`: PASS
- `git diff --check`: PASS
- UTF-8 check: PASS
- full feature tree check: PASS
- matrix governance check: PASS

## Next Step

BE-002DU-01 `root.contracts.runtime_support.v4_runtime_support` parent_residual_judgment.
