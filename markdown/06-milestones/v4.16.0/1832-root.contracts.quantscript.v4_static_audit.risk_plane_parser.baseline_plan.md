# v4.16.0 root.contracts.quantscript.v4_static_audit.risk_plane_parser baseline plan

> Batch: BE-002KO-01
> Node: `root.contracts.quantscript.v4_static_audit.risk_plane_parser`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Goal

Freeze the risk-plane parser boundary before Rust movement.

This child owns one graph-level risk-plane declaration rule and
`MachineGraphRiskPlane` construction.

## Frozen Boundary

| Field | Value |
| --- | --- |
| Approved function | `parse_risk_plane` |
| Planned file | `quantscript/src/v4_static_audit/risk_plane_parser.rs` |
| Inputs | risk-plane line text and source line number |
| Outputs | `MachineGraphRiskPlane` or `Diagnostic` |
| Parent callers | `static_document_parser` through parent wrapper |
| Visibility | child function may be `pub(super)` only for the parent wrapper |

## Equivalent Behavior

The extracted child must preserve:

- missing machine diagnostic `QSV4121`;
- missing priority value diagnostic `QSV4122`;
- invalid priority diagnostic `QSV4123`;
- CSV machine id expansion;
- default `min_priority` value;
- `required: true` behavior.

## Exclusions

- Do not move `parse_state_group`.
- Do not move `parse_machine_template`.
- Do not move public report DTOs or local tests.
- Do not introduce release-transition cross-child links.

## Allowed Rust Movement For Next Step

BE-002KO-02 may:

1. add `mod risk_plane_parser;`;
2. create `quantscript/src/v4_static_audit/risk_plane_parser.rs`;
3. move the `parse_risk_plane` implementation body;
4. keep the parent private wrapper named `parse_risk_plane`;
5. adjust imports and `pub(super)` visibility only where required by this
   extraction.

## Gates For Actual Extraction

| Gate | Command |
| --- | --- |
| format | `cargo fmt --all --check` |
| crate tests | `cargo test -p quantscript` |
| workspace compile | `cargo check -p quantpilot` |
| diff hygiene | `git diff --check` |

## Next Step

BE-002KO-02 `root.contracts.quantscript.v4_static_audit.risk_plane_parser`
actual_extraction.
