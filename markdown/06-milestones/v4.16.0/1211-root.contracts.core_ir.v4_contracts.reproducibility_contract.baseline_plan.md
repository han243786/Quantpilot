# v4.16.0 root.contracts.core_ir.v4_contracts.reproducibility_contract equivalence baseline and extraction plan

> Batch: BE-001VQ-01
> Node: `root.contracts.core_ir.v4_contracts.reproducibility_contract`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `baseline_plan`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.reproducibility_contract` freezes the equivalence baseline for extracting the v4
reproducibility evidence contract from `qrpc_core_ir/src/v4.rs`.

Frozen public surface:

- `ReproducibilityContract`
- `RunEvidenceKind`
- `EventEnvelopeField`
- `impl Default for ReproducibilityContract`
- `ReproducibilityContract::validate_static_contract`

Frozen internal child helpers:

- `default_reproducibility_contract_version`
- `default_reproducibility_evidence`
- `default_event_envelope_fields`

Frozen behavior:

- default required evidence list and order remain unchanged
- default event envelope field list and order remain unchanged
- key decision path replay remains required by default
- full tick replay remains false by default and still rejected as reserved for a later phase
- validation keeps current schema version, required evidence, required event envelope field, key decision path replay,
  and full tick replay error strings

Allowed extraction:

- add `mod reproducibility_contract;` to `qrpc_core_ir/src/v4.rs`
- re-export the child through `pub use reproducibility_contract::*;`
- create `qrpc_core_ir/src/v4/reproducibility_contract.rs`
- move reproducibility DTOs/enums/defaults and static validation
- import `default_true` and schema identity constants through the v4 surface

Forbidden movement:

- plugin governance, complexity budget, developer learning pipeline, static bundles, compile-time capability reports,
  compatibility bridge, bridge builders, tests, machine contract, machine graph contract, QS contracts, runtime mode,
  venue capability matrix, and version manifest

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs`

**Markers**:
- `BE-001VQ-01`

**Next step**:
BE-001VQ-02 root.contracts.core_ir.v4_contracts.reproducibility_contract extract_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
