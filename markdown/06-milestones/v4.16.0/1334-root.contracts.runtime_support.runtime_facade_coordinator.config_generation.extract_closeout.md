# v4.16.0 runtime_facade_coordinator.config_generation extract closeout

> Batch: BE-001YP-02
> Node: `root.contracts.runtime_support.runtime_facade_coordinator.config_generation`
> Parent: `root.contracts.runtime_support.runtime_facade_coordinator`
> Stage: `actual_extraction`
> Movement: Code movement complete.

## Extracted Files

- `qrpc_runtime/src/runtime_facade_coordinator/config_generation.rs`

## Code Movement

The config generation methods moved out of `qrpc_runtime/src/runtime_facade_coordinator.rs` into a child module:

- `RuntimeCoordinator::swap_module_config`
- `RuntimeCoordinator::apply_pending_module_configs`
- `RuntimeCoordinator::current_generation`
- `RuntimeCoordinator::generation_history`

`qrpc_runtime/src/runtime_facade_coordinator.rs` now declares `mod config_generation;` and keeps provider helpers, portfolio projection, and tests.

## Public API Equivalence

Public calls remain available as `RuntimeCoordinator::*`.

The crate-root facade remains unchanged through `qrpc_runtime/src/lib.rs`.

## Boundary Result

The child owns module config swap/apply, deterministic deployment revision generation, generation counter reads, and generation history projection.

The parent still owns:

- provider delegation helper implementations
- `portfolio_update_event`
- `refresh_portfolio_state`
- coordinator tests

## Equivalence Gates

Required gates for this batch:

- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-001YP-03 `root.contracts.runtime_support.runtime_facade_coordinator.config_generation` single_leaf_closeout.
