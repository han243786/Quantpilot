# v4.16.0 root.contracts.runtime_support.risk_support baseline plan

> Batch: BE-002BP-01
> Node: `root.contracts.runtime_support.risk_support`
> Parent: `root.contracts.runtime_support`
> Stage: `baseline_plan`
> Movement: No Rust code movement.

---

## Scope

Freeze the baseline for `root.contracts.runtime_support.risk_support`.

Current primary file:

- `qrpc_runtime/src/risk_checker.rs`

This parent owns runtime risk-checking contracts, the default risk checker, risk decision evaluation, direction/cross-symbol constraints, action clamp helpers, portfolio target clamp helpers, exposure math, risk event payload projection, reason-code helpers, and local tests.

## Leaf Tool Result

The leaf granularity tool returned conservative `STOP`, score 61, because no same-parent wave candidate was supplied to the tool.

However, the file is oversized for a terminal leaf:

- LOC: 1930
- Functions: 44
- Branches: 119
- Public surface: 14
- Size bucket: `oversized_over_800`

So the baseline opens a limited cost-controlled same-parent wave queue instead of treating this entire file as terminal.

## Child Queue

Planned child queue:

1. `contracts.runtime_support.risk_support.event_payload_projection`
2. `contracts.runtime_support.risk_support.direction_cross_constraints`
3. `contracts.runtime_support.risk_support.action_clamp_helpers`
4. `contracts.runtime_support.risk_support.portfolio_target_clamp_helpers`
5. `contracts.runtime_support.risk_support.exposure_math_helpers`
6. `contracts.runtime_support.risk_support.test_harness`

Parent-owned for now:

- `RiskCheckRequest`
- `RiskCheckOutput`
- `RiskCheckerProvider`
- `RiskChecker`
- `RiskCheckerProvider::evaluate`
- `evaluate_risk_decision`
- `enforce_risk_mode`
- child imports and parent-mediated composition

## Boundary Rules

- runtime facade and provider injection remain outside this child;
- agent decision production remains outside this child;
- execution/fill handling remains outside this child;
- risk monitor internals remain outside this child unless a later parent residual judgment proves shared ownership;
- no child may import a sibling directly;
- release-transition optimization remains out of scope.

## Baseline Watchpoints

Historical mojibake comments in `qrpc_runtime/src/risk_checker.rs` sometimes sit on the same physical line as code. During actual extraction, comment normalization is allowed only when needed to preserve formatter/compiler clarity or to prevent code from being hidden by a comment. Such normalization must be local to the moved block and covered by gates.

## Gate Baseline

Required gates for actual movement:

- `cargo fmt`
- `cargo check -p qrpc-runtime`
- risk-support focused tests where available
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 / full feature tree / matrix governance

## Next Step

BE-002BQ-01 `root.contracts.runtime_support.risk_support` parent_residual_judgment selects the first child.
