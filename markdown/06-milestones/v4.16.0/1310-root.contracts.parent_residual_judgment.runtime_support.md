# v4.16.0 root.contracts parent residual judgment selects runtime_support

> Batch: BE-001YC-01
> Node: `root.contracts`
> Stage: `parent_residual_judgment`
> Selected child: `root.contracts.runtime_support`
> Movement: No code movement.

## Parent State

`root.contracts.compiler_bridge` is closed. The closed contracts children are:

- `contracts.api_surface`
- `contracts.qrpc_core`
- `contracts.core_ir`
- `contracts.compiler_bridge`

The remaining contracts queue is:

- `contracts.runtime_support`
- `contracts.quantscript`
- `contracts.plugin_metadata`

The global Rust residual `executor` remains outside `root.contracts`.

## Selected Child Boundary

`root.contracts.runtime_support` owns the QRPC runtime support crate boundary:

- physical root: `qrpc_runtime/src`
- crate package: `qrpc-runtime`
- facade: `qrpc_runtime/src/lib.rs`
- runtime support families: coordinator, data, intent, agent, Core IR evaluation, execution planning, fill simulation, risk checking, risk monitor, slippage, reconciliation, sandbox replay/timeline, live execution support, plugin runtime support, and v4 runtime support

The selected child excludes:

- `contracts.quantscript`
- `contracts.plugin_metadata`
- `executor`
- backend HTTP route surfaces
- frontend callers
- release-transition optimization

## Leaf Split Decision Gate

The child must continue to a baseline step because it satisfies the required split triggers:

- `white_box_boundary_named`: `qrpc-runtime` already exposes a clear runtime support facade.
- `public_surface_owned`: `qrpc_runtime/src/lib.rs` re-exports runtime support DTOs, traits, providers, and coordinator APIs.
- `size_or_behavior_pressure`: the runtime crate contains multiple large behavior families and about 25k Rust lines.
- `independent_failure_modes`: data collection, agent evaluation, Core IR evaluation, execution, risk, sandbox, live execution, and v4 runtime support fail for different reasons.
- `equivalence_baseline_freezable`: no code movement is needed in this parent selection step.

No stop rule applies at the parent selection level.

## Next Step

BE-001YD-01 `root.contracts.runtime_support` baseline_plan.

## Verification Plan

- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`
