# v4.16.0 root.contracts.quantscript.v4_static_audit.static_document_parser single leaf closeout

> Batch: BE-002KC-03
> Node: `root.contracts.quantscript.v4_static_audit.static_document_parser`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`stop_split: true`

The extracted child is a terminal-sized cohesive document-parser leaf. It owns a
single top-level parser function that maps one document envelope into graph and
capability-request outputs while delegating specialized parser work through the
parent boundary.

## Leaf Granularity Evidence

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -LeafId root.contracts.quantscript.v4_static_audit.static_document_parser -ParentId root.contracts.quantscript.v4_static_audit -Path quantscript\src\v4_static_audit\static_document_parser.rs -Depth 4
```

Observed result:

| Field | Value |
| --- | --- |
| Split decision | STOP |
| Governance packaging | stop_split |
| Decision | STOP |
| Score | 30 |
| LOC | 185 |
| Functions | 1 |
| Public surface | 4 |
| Branches | 22 |
| Domains | 6 |
| Risk tags | cross_crate, lock, public_api, route_schema, trading_semantics |
| Size bucket | terminal_target_150_600 |

## Why No Further Split

- The leaf is inside the target terminal size range.
- The single parser function is cohesive around the `v4_strategy` document
  envelope.
- Lower-level parser responsibilities are already separated or parent-owned.
- Further splitting would create thin fragments for venue/mode/capability/header
  parsing with higher governance cost than ownership value.
- Parent-child communication remains explicit through the parent wrapper and
  parent bridge.

## Preserved Boundaries

- Public API unchanged.
- `audit_entrypoint` continues through the parent parser wrapper.
- Machine, state, memory, transition, edge, and risk-plane parser bodies remain
  parent-owned.
- Capability/type parser helpers, parser utilities, and event catalog derivation
  remain in their closed child modules.
- Public report DTOs and local tests remain parent-owned.
- No release-transition link was introduced.

## Gates

| Gate | Result |
| --- | --- |
| leaf granularity evaluator | PASS, STOP score 30 |
| `git diff --check` | PASS |
| `check-utf8.ps1` | PASS |
| `check-full-feature-tree.ps1` | PASS |
| `check-matrix-governance.ps1` | PASS |

## Next Step

BE-002KD-01 `root.contracts.quantscript.v4_static_audit`
parent_residual_judgment.
