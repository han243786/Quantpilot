# v4.16.0 root.contracts.runtime_support.core_ir_evaluator parent residual judgment selects advanced_indicator_wave

> Batch: BE-002AL-01
> Node: `root.contracts.runtime_support.core_ir_evaluator`
> Parent: `root.contracts.runtime_support`
> Stage: `parent_residual_judgment`
> Selected child: `root.contracts.runtime_support.core_ir_evaluator.advanced_indicator_wave`
> Movement: No Rust code movement.

---

## Parent State

Closed children:

- `contracts.runtime_support.core_ir_evaluator.classic_indicator_wave`

The next child in the frozen queue is `advanced_indicator_wave`.

## Selected Child Boundary

`root.contracts.runtime_support.core_ir_evaluator.advanced_indicator_wave` owns advanced builtin indicator evaluator entrypoints currently embedded in `qrpc_runtime/src/core_ir_evaluator.rs`:

- `evaluate_atr`
- `evaluate_bollinger_bands`
- `evaluate_obv`
- `evaluate_cmf`
- `evaluate_adx`
- `evaluate_stochastic`
- `evaluate_cci`
- `evaluate_parabolic_sar`
- `evaluate_keltner_channel`
- `evaluate_donchian_channel`

## Exclusions

The selected child must not absorb:

- already-closed classic indicator wave functions;
- Spread or Custom evaluator entrypoints;
- typed series expression machinery;
- shared lookup/math helpers such as ATR, Bollinger, OBV, CMF, ADX, Stochastic, CCI, PSAR, Keltner, and Donchian numeric helper formulas;
- local tests.

## Split Decision

`continue_split: true`.

Reason:

- these ten functions are direct builtin registry targets and form a cohesive advanced-indicator same-parent wave;
- extracting them reduces parent evaluator weight without turning every indicator into a standalone micro leaf;
- shared math helper ownership should remain parent-owned until helper residual judgment, because those formulas are used by tests and can be reused by multiple waves.

No release-transition optimization is proposed.

## Next Step

BE-002AM-01 `root.contracts.runtime_support.core_ir_evaluator.advanced_indicator_wave` baseline_plan.

## Verification

No code moved in this step. Required gates:

- `git diff --check`
- `tools/check-utf8.ps1`
- `tools/check-full-feature-tree.ps1`
- `tools/check-matrix-governance.ps1`
