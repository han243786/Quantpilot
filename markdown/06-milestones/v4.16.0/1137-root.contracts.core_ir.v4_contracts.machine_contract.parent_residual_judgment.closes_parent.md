# v4.16.0 root.contracts.core_ir.v4_contracts.machine_contract parent residual judgment closes parent

> Batch: BE-001UC-01
> Node: `root.contracts.core_ir.v4_contracts.machine_contract`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_contract` closes as a parent facade.

Closed child:

- `root.contracts.core_ir.v4_contracts.machine_contract.static_validation`

The parent now owns machine schema/taxonomy and delegates validation behavior to its private validation child:

- `qrpc_core_ir/src/v4/machine_contract.rs`
- `qrpc_core_ir/src/v4/machine_contract/static_validation.rs`

No additional child is selected inside `machine_contract`.

## Closeout Rationale

The remaining parent surface is schema/taxonomy only:

- machine policy/template enums;
- `V4MachineContract`;
- `MachineState`;
- `StateGroup`;
- `MachineTransition`;
- `MachineEventSelector`;
- `MachineActionSpec`;
- `MachineMemoryField`;
- one private `mod static_validation;` declaration.

Further splitting schema/taxonomy would create tiny DTO-only leaves and raise communication cost across recursive `V4MachineContract` and nested DTO references.

## Residual Queue Result

| Child | Status |
| --- | --- |
| `machine_contract.static_validation` | Closed with `stop_split: true`. |
| `machine_contract.schema_and_taxonomy` | Closed as parent facade residual. |
| `machine_contract.default_helper_surface` | Not moved; remains v4 parent-mediated. |

## Next Step

BE-001UD-01 `root.contracts.core_ir.v4_contracts` parent_residual_judgment selects `machine_graph_contract`.

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
