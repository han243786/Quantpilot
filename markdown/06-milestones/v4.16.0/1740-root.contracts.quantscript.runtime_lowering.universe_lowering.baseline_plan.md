# v4.16.0 root.contracts.quantscript.runtime_lowering.universe_lowering baseline plan

> Batch: BE-002IO-01
> Node: `root.contracts.quantscript.runtime_lowering.universe_lowering`
> Parent: `root.contracts.quantscript.runtime_lowering`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Baseline Decision

Freeze `universe_lowering` as a precision single-leaf parent before any Rust movement.

`quantscript/src/lowering/universe.rs` is already a physical module, so this baseline does not approve moving the whole file. It freezes the current public entry surface and opens an internal child queue for recursive extraction.

## Public / Parent-Visible Surface To Preserve

| Symbol | Owner After Baseline | Rule |
| --- | --- | --- |
| `detect_portfolio_rebalance_directive` | `universe_lowering` parent facade until a child is approved | Signature and `PortfolioRebalanceDirective` semantics must remain stable |
| `extract_instrument_pool_spec` | `universe_lowering` parent facade until a child is approved | Public(crate) re-export from `lowering/mod.rs` must remain stable |
| `expand_universe_constructs` | `universe_lowering` parent facade until a child is approved | Expanded `ScriptModule` behavior must remain equivalent |

## Internal Candidate Queue

| Candidate | Scope | Initial action |
| --- | --- | --- |
| `rebalance_directive_detection` | strategy traversal, `rebalance(...)` detection, schedule/allocation parsing, directive assembly | first parent residual judgment |
| `universe_construct_expansion` | `expand_universe_constructs`, statement expansion, symbol substitution, local binding renaming | queued |
| `universe_expression_evaluation` | `symbols`, `universe`, `filter`, `sort_by`, `top`, and universe arg evaluation | queued |
| `universe_metadata_selection` | snapshot asset lookup, metric projection, listed-as-of filtering, metric sorting | queued |
| `universe_test_harness` | module-local tests and sample universe snapshot fixture | queued |

## Leaf Granularity Evidence

Command:

- `tools/evaluate-leaf-granularity.ps1 -LeafId root.contracts.quantscript.runtime_lowering.universe_lowering -ParentId root.contracts.quantscript.runtime_lowering -Path quantscript/src/lowering/universe.rs -Depth 4`

Result:

- `Decision: PRECISION`
- `Split decision: CONTINUE`
- `Governance packaging: precision_single_leaf`
- `Score: 56`
- `loc=1560`
- `functions=32`
- `public_surface=3`
- `branches=141`
- `domains=6`
- `risk_tags=cross_crate,persistence,public_api,state_machine,trading_semantics`
- `size_bucket=oversized_over_800`
- `governance_mode=precision_single_leaf`

## Movement Rules

- Do not batch `universe_lowering` with sibling runtime lowering children.
- Do not change public re-exports in `quantscript/src/lowering/mod.rs`.
- Parent-child communication must remain parent-mediated through `universe.rs`.
- First movement must be preceded by a child-specific baseline.
- Any child extraction must preserve `cargo test -p quantscript` and `cargo check -p quantpilot`.

## Next Step

BE-002IP-01 `root.contracts.quantscript.runtime_lowering.universe_lowering` parent_residual_judgment.
