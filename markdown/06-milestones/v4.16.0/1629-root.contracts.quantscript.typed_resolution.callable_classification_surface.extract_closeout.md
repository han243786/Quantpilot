# v4.16.0 root.contracts.quantscript.typed_resolution.callable_classification_surface extract closeout

> Batch: BE-002GF-02
> Node: `root.contracts.quantscript.typed_resolution.callable_classification_surface`
> Parent: `root.contracts.quantscript.typed_resolution`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> governance_next_authority: active

---

## Movement

Created:

- `quantscript/src/resolve/callable_classification_surface.rs`

Updated:

- `quantscript/src/resolve/mod.rs`

Moved callable/helper classifier functions and registry seeding helpers into the child module. The parent now declares `mod callable_classification_surface;`, imports resolver-internal helpers through `use callable_classification_surface::{...};`, and preserves the public classifier surface with `pub use callable_classification_surface::{...};`.

## Preserved Boundary

Preserved:

- crate-root `pub use resolve::{...}` compatibility in `quantscript/src/lib.rs`;
- downstream `crate::resolve::*` imports;
- typed-resolution public DTO and enum ownership in `quantscript/src/resolve/public_type_surface.rs`;
- resolver orchestration, type inference, expression semantic inference, manual indicator formula inference, lowering, analysis, evaluator, and local tests.

Moved only:

- `classify_series_capability_name`
- `classify_builtin_math_name`
- `classify_member_mutation_name`
- `build_resolved_callables`
- `seed_callable`
- `classify_imported_helper`
- `classify_change_smoothing_kind`
- `classify_fetch_source_kind`
- `is_known_helper_function`

## Evidence

- `cargo fmt --check` PASS
- `cargo test -p quantscript` PASS, 88 tests
- `cargo check -p quantpilot` PASS

## Next Step

BE-002GF-03 `root.contracts.quantscript.typed_resolution.callable_classification_surface` single_leaf_closeout.
