# v4.16.0 root.contracts.runtime_support.agent_module.portfolio_rebalance extract closeout

> Batch: BE-001ZZ-02
> Node: `root.contracts.runtime_support.agent_module.portfolio_rebalance`
> Parent: `root.contracts.runtime_support.agent_module`
> Stage: `actual_extraction`
> Movement: Rust code movement.

---

## Extracted Child

Created:

- `qrpc_runtime/src/agent_module/portfolio_rebalance.rs`

Updated:

- `qrpc_runtime/src/agent_module.rs`

## Movement Summary

Moved from parent into the child:

- `portfolio_rebalance_due`
- `RebalanceSymbolPlan`
- `build_portfolio_rebalance_decision`
- `assign_target_weights`

Parent retained:

- public request/output/provider/builtin types;
- `BuiltinAgentModule::evaluate_agents` dispatch and event assembly;
- `evaluated_rebalance_agent_ids` insertion point;
- shared helpers:
  - `build_signal_kind_index`
  - `signal_score`
  - `current_position_ratio`
  - `MIN_QUANTITY_RATIO`
  - `DEFAULT_DECISION_THRESHOLD`
- weighted signal child, cross-venue arbitrage, shared helper queue, and tests.

## Parent Mediation

The parent now declares:

- `mod portfolio_rebalance;`

The parent dispatches `AgentPolicyKind::PortfolioRebalance` on `slow` cycle through:

- `portfolio_rebalance::portfolio_rebalance_due(...)`
- `portfolio_rebalance::build_portfolio_rebalance_decision(...)`

The child uses parent-owned helpers through `super::...`; no sibling dependency or release-transition shortcut is introduced.

## Equivalence Preserved

The extracted child keeps:

- rebalance cadence gates;
- non-observe signal filtering;
- explicit and signal-derived universe behavior;
- selected-symbol threshold behavior;
- exchange/reference-price fallback from signals and positions;
- current-weight projection through parent helper;
- equal, fixed, rank, inverse-rank, and score-weight allocation;
- zero-delta suppression;
- portfolio target DTO assembly and reason strings.

## Size Impact

- `qrpc_runtime/src/agent_module.rs`: reduced to 1268 lines after extraction.
- `qrpc_runtime/src/agent_module/portfolio_rebalance.rs`: 315 lines.

## Gates

- `cargo fmt --check`: PASS
- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime`: PASS, 227 passed, 2 ignored
- `cargo check -p quantpilot`: PASS
- `git diff --check`: PASS
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`: PASS
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`: PASS
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`: PASS

## Next Step

BE-001ZZ-03 `root.contracts.runtime_support.agent_module.portfolio_rebalance` single_leaf_closeout.
