# v4.16.0 root.executor.runner.runner_support_files.okx_rest baseline plan

> Batch: BE-002LS-01
> Node: `root.executor.runner.runner_support_files.okx_rest`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Goal

Freeze the OKX REST adapter boundary before precision recursive treatment.

`src-executor/okx_rest.rs` is already a separate support file, but it is an
oversized high-risk leaf that owns signing, request construction, transport,
order actions, lookup validation, balance fetch, and OKX demo smoke tests.

## Current Surface

| Field | Value |
| --- | --- |
| Current file | `src-executor/okx_rest.rs` |
| LOC | 846 |
| Functions | 39 |
| Public surface | 45 |
| Risk tags | cross_crate, live_execution, persistence, public_api, route_schema, security, state_machine, trading_semantics |
| Granularity decision | CONTINUE / PRECISION |

## Frozen Public API

The next movements must preserve the public symbols currently exposed through
`okx_rest`:

- constants: provider key, audit environment, REST paths;
- types: `OkxTradingProfile`, `OkxCredentials`, `OkxSignedRequest`,
  `OkxOrderRequest`, `OkxCancelOrderRequest`;
- signing entry: `build_signed_request`;
- order actions: `place_order`, `place_order_with_profile`, `query_order`,
  `query_order_with_profile`, `cancel_order`, `cancel_order_with_profile`;
- lookup helper: `okx_order_lookup_path`;
- balance actions: `fetch_balance`, `fetch_balance_with_profile`.

## Child Queue

| Order | Candidate child | Initial scope |
| --- | --- | --- |
| 1 | `signing_request_surface` | credential validation, timestamp/signing, signed request construction |
| 2 | `transport_response_surface` | base URL/proxy/agent setup, raw send, OKX success checks |
| 3 | `order_action_surface` | place/query/cancel/balance public action wrappers |
| 4 | `lookup_validation_surface` | lookup path construction and inst/order id validation |
| 5 | `test_harness` | local serialization/profile/signing/path tests plus optional OKX demo smoke probes |

## Equivalent Behavior

Further recursion must preserve:

- OKX demo simulated-trading header and audit markers;
- HMAC signing input shape and base64 output behavior;
- REST base URL and proxy environment handling;
- provider error/success semantics for response code and per-order `sCode`;
- query/cancel path validation;
- network smoke tests remaining opt-in through existing environment variables.

## Gates For First Movement

| Gate | Command |
| --- | --- |
| format | `cargo fmt --all --check` |
| executor compile | `cargo check -p quantpilot --bin executor` |
| focused tests | `cargo test -p quantpilot --bin executor okx_` |
| diff hygiene | `git diff --check` |

## Next Step

BE-002LS-02 `root.executor.runner.runner_support_files.okx_rest`
parent_residual_judgment.
