# v4.16.0 root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface parent residual judgment: integration_test_harness

> Batch: BE-002HZ-01
> Node: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface`
> Selected child: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `integration_test_harness` as the next child under `lowering_orchestrator_surface`.

The previous child `profile_detection_surface` is closed with `stop_split: true`. The parent `orchestrator.rs` now has two remaining responsibilities:

- parent-owned public lowering entrypoints and runtime config assembly;
- colocated integration regression tests.

The integration tests are cohesive, test-only, and owner-stable enough to extract as the next child without changing public lowering behavior.

## White-Box Boundary

| Field | Boundary |
| --- | --- |
| Input | QuantScript fixture source strings, parsed `ScriptModule`, public lowering calls, runtime compiler/coordinator assertions |
| Output | Regression coverage for runtime lowering data sources, intents, risk defaults, profile detection, rebalance directives, and runtime protocol compatibility |
| Current owner | `#[cfg(test)] mod tests` in `quantscript/src/lowering/orchestrator.rs` |
| Planned owner | `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness` |
| Parent caller | `lowering_orchestrator_surface` test module only |
| Exclusions | Public lowering entrypoints, runtime config assembly, profile detection child, universe lowering, intent inference, binding/source decoding, fallback, semantic bridge, diagnostics formatting |

## Split Decision Gate

| Gate | Result | Evidence |
| --- | --- | --- |
| `micro_leaf_without_owner` | PASS | The current test block owns many integration scenarios and fixture constants, not a single helper. |
| `communication_cost_rises` | PASS | Test-only child keeps calls through the parent module and does not create sibling horizontal production links. |
| `local_proof_missing` | PASS | `cargo test -p quantscript` is the direct equivalence surface. |
| `line_count_only` | PASS | Selection is based on test-harness ownership and parent simplification, not raw LOC alone. |

## Risk Classification

| Risk | Result |
| --- | --- |
| Public API / route / schema | No change planned |
| Persistence / lock owner | No change planned |
| State machine semantics | No change planned |
| Runtime lowering output shape | Must remain unchanged |
| Test coverage | Must remain equivalent; target gate is `cargo test -p quantscript` |

## Residual Judgment

`integration_test_harness` is the next safest child because it removes the largest non-production responsibility from the parent while keeping the production entrypoint assembly intact.

The parent-owned `entrypoint_runtime_config_assembly` remains after this child and should not be split until the test harness is closed. That remaining production assembly must be judged separately because it owns public behavior and runtime protocol output shape.

## Next Step

BE-002IA-01 `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness` baseline_plan.
