# v4.16.0 root.contracts.runtime_support.intent_module single leaf closeout

> Batch: BE-001ZT-02
> Node: `root.contracts.runtime_support.intent_module`
> Parent: `root.contracts.runtime_support`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Decision

`stop_split: true`

`root.contracts.runtime_support.intent_module` is closed as a terminal runtime-support child.

## Evidence

The module is already physically separated:

- `qrpc_runtime/src/intent_module.rs`

Crate-root public surface remains parent-mediated:

- `qrpc_runtime/src/lib.rs`
- `pub use intent_module::{BuiltinIntentModule, IntentEvaluationOutput, IntentEvaluationRequest, IntentModuleProvider};`

Terminal leaf control evidence:

```text
Leaf: root.contracts.runtime_support.intent_module
Decision: STOP
Score: 39
Metrics: loc=416, functions=14, public_surface=11, branches=10, domains=8
Terminal leaf control: size_bucket=terminal_target_150_600, governance_mode=stop_split
```

## White-Box Closure

The terminal leaf owns:

- `IntentEvaluationRequest`;
- `IntentEvaluationOutput`;
- `IntentModuleProvider`;
- `BuiltinIntentModule`;
- resolved intent configuration;
- Core IR indicator backed intent evaluation;
- runtime intent signal construction;
- `IntentEvaluated` and `IntentTriggered` event emission;
- Core IR indicator kind to `IntentKind` mapping;
- local intent-module tests.

## Why No Deeper Split

No deeper child is opened because:

- the module is already in the terminal target size range;
- public API and trading semantics increase movement risk;
- private helper extraction would not improve parent-child communication;
- test fixture extraction would create a micro-leaf with higher governance cost than value;
- `core_ir_evaluator`, agent, risk, execution, fill, sandbox, v4 runtime, live execution, plugin runtime, ops compatibility, and metrics are separate queued children.

## Equivalence Gates

- `cargo fmt --check`: PASS
- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime`: PASS, 227 passed, 2 ignored
- `cargo check -p quantpilot`: PASS
- `git diff --check`: PASS
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`: PASS
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`: PASS
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`: PASS

## Next Step

BE-001ZU-01 `root.contracts.runtime_support` parent_residual_judgment selects `contracts.runtime_support.agent_module`.
