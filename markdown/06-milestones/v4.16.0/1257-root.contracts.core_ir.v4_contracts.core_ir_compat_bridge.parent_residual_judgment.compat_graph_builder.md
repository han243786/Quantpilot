# v4.16.0 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge parent residual judgment selects compat_graph_builder

> Batch: BE-001WO-01
> Node: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `parent_residual_judgment`
> Movement: No code movement. This document selects the next bridge child for extraction.

---

## Summary

root.contracts.core_ir.v4_contracts.core_ir_compat_bridge parent residual judgment selects compat_graph_builder

The next child is `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder`.

It owns v4 compatibility graph construction:

- `build_core_ir_compat_machine_graph`
- `build_core_ir_observation_machine`
- `build_core_ir_decision_machine`
- `build_core_ir_execution_machine`
- `compat_machine`
- `build_core_ir_compat_event_catalog`
- `compat_machine_metadata`
- `compat_edge_metadata`
- `count_memory_field`
- `core_ir_edge_labels`
- `string_value_array`
- `sanitize_core_ir_compat_id`

It does not own bridge DTOs, report phase4 validation, the public bridge entrypoint, Core IR validation, `default_core_ir_compat_bridge_version`, or `push_core_ir_v4_bridge_diagnostic`.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | PASS | `compat_graph_builder` has clear graph, machine, event-catalog, metadata, and id-sanitization ownership. |
| parent_child_communication_kept | PASS | The bridge parent can call the builder child after validation without child-to-child horizontal calls. |
| equivalence_baseline_freezable | PASS | Existing bridge tests cover accepted mapping and static graph contract validation through the public bridge entrypoint. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | PARTIAL | The child is private, but it builds the graph returned by the public bridge report. |
| state_machine_phase | PASS | The child constructs Observation, Decision, and Execution compatibility machines plus graph edges and event catalog. |
| strategy_branch | PASS | It maps data bindings, indicators, signals, agents, risks, execution config, and Core IR edge labels into v4 metadata. |
| independent_failure_mode | PASS | Builder/static graph failures (`V4BRIDGE900` after parent validation) are distinct from Core IR validation failures. |
| reuse_pressure | PARTIAL | Future bridge variants may reuse graph construction separately from validation and report DTOs. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | PASS | The selected child is a full graph construction phase, not a helper-only fragment. |
| communication_cost_rises | PASS | The child can accept `CoreStrategyIr` and return `V4MachineGraphContract`; parent-child data flow stays simple. |
| local_proof_missing | PASS | `core_ir_v4_bridge_maps_legacy_core_ir_to_default_machines` covers the construction path. |
| line_count_only | PASS | Selection is based on state-machine construction ownership and failure mode, not line count alone. |

leaf_split_decision_result

`selected_child: compat_graph_builder`

The bridge parent remains open. Remaining parent surfaces after this child include bridge DTOs, phase4 report validation, bridge entrypoint, bridge default, and bridge diagnostic push helper.

next_recursive_step

BE-001WP-01 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder baseline_plan
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/core_ir_compat_bridge.rs`

**Markers**:
- `BE-001WO-01`

**Next step**:
BE-001WP-01 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
