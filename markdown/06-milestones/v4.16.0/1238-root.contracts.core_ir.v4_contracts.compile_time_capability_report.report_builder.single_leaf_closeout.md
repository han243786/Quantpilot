# v4.16.0 root.contracts.core_ir.v4_contracts.compile_time_capability_report.report_builder single leaf closeout stops split

> Batch: BE-001WD-03
> Node: `root.contracts.core_ir.v4_contracts.compile_time_capability_report.report_builder`
> Parent: `root.contracts.core_ir.v4_contracts.compile_time_capability_report`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`report_builder` is terminal for the current recursive pass.

Closed ownership:

- `V4StaticContractBundle::build_compile_time_capability_report`
- type capability entry assembly
- execution capability entry assembly
- plugin capability entry assembly
- V4CAP000-301 diagnostic construction
- capability report verdict construction

Further splitting is rejected because the child is one coherent diagnostic/report assembly phase. Smaller splits would scatter shared diagnostics, required-set handling, and status mapping across micro leaves.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `report_builder` is a named builder behavior leaf. |
| parent_child_communication_kept | pass | The leaf lives under the capability report parent and implements builder methods on `V4StaticContractBundle`. |
| equivalence_baseline_freezable | pass | Diagnostic codes, entry assembly, and verdict construction were frozen and verified. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | satisfied | The leaf owns the public `build_compile_time_capability_report` behavior. |
| state_machine_phase | no | This is report construction, not state-machine topology. |
| strategy_branch | no | This is not strategy branch behavior. |
| independent_failure_mode | satisfied | Builder diagnostics are distinct from report DTO schema and final report validation. |
| reuse_pressure | low | Tests call the public builder through `V4StaticContractBundle`. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | yes | Splitting type/execution/plugin helpers further would create small helper-only owners around one diagnostic accumulator. |
| communication_cost_rises | yes | More children would add routing around shared diagnostics and required sets. |
| local_proof_missing | no | `cargo test -p qrpc-core-ir` and `cargo check -p quantpilot` passed after extraction. |
| line_count_only | no | Stop decision is based on diagnostic assembly cohesion. |

leaf_split_decision_result

`stop_split: true`

next_recursive_step

BE-001WE-01 root.contracts.core_ir.v4_contracts.compile_time_capability_report parent_residual_judgment selects report_validation
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/compile_time_capability_report/report_builder.rs`

**Markers**:
- `BE-001WD-03`

**Next step**:
BE-001WE-01 root.contracts.core_ir.v4_contracts.compile_time_capability_report parent_residual_judgment selects report_validation

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
