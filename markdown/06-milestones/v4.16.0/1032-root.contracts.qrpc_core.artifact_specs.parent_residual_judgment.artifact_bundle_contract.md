# v4.16.0 root.contracts.qrpc_core.artifact_specs parent residual judgment selects artifact_bundle_contract

> Batch: BE-001RX-01
> Node: `root.contracts.qrpc_core.artifact_specs`
> Selected child: `root.contracts.qrpc_core.artifact_specs.artifact_bundle_contract`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core.artifact_specs` returns to its child queue after `run_backtest_specs` closeout.

Decision:

`next_child: root.contracts.qrpc_core.artifact_specs.artifact_bundle_contract`

## Closed Artifact Specs Children

| Child | Result |
| --- | --- |
| `contracts.qrpc_core.artifact_specs.canonical_digest` | Closed with `stop_split: true`; owns digest algorithm, digest DTO, and canonical JSON SHA-256 helper. |
| `contracts.qrpc_core.artifact_specs.run_backtest_specs` | Closed with `stop_split: true`; owns run/backtest modes, dataset/execution projections, market data snapshot specs, `RunSpec`, and `BacktestSpec`. |

## Open Artifact Specs Children

| Child | Status |
| --- | --- |
| `contracts.qrpc_core.artifact_specs.artifact_bundle_contract` | Selected next. Owns strategy/core-IR/compile artifact DTOs and `CompileArtifactBundle`. |

## Selection Rationale

`artifact_bundle_contract` is selected because it is the final artifact specs schema family:

- it owns `StrategyArtifactSourceKind`;
- it owns `StrategyArtifact`;
- it owns `CoreIrArtifact`;
- it owns `CompileArtifact`;
- it owns `CompileArtifactBundle`;
- it can move after canonical digest and run/backtest specs because those dependencies are now parent-mediated through artifact specs re-exports.

## Same-Parent Parallel Judgment

`same_parent_parallel_children` is not needed.

Only one artifact specs residual remains, and it is compact enough for serial baseline and extraction.

## Hard Boundaries

The next `artifact_bundle_contract` baseline must not:

- edit Rust source code;
- change enum variants, serde attributes, field names, bundle shape, public exports, or tests;
- move runtime IO DTOs, RFC execution contracts, or crate-root tests;
- change closed canonical digest, run/backtest specs, other qrpc-core children, or release transition.

## Next Step

BE-001RY-01 `root.contracts.qrpc_core.artifact_specs.artifact_bundle_contract` baseline_plan.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
