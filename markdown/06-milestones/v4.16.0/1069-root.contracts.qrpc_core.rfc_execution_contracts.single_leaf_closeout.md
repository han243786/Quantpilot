# v4.16.0 root.contracts.qrpc_core.rfc_execution_contracts single leaf closeout

> Batch: BE-001SQ-03
> Node: `root.contracts.qrpc_core.rfc_execution_contracts`
> Parent: `root.contracts.qrpc_core`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`rfc_execution_contracts` is now physically extracted, but it should continue recursively as a parent module rather than stop as one final leaf.

The file currently owns multiple independent public contract families:

- data request DTOs and taxonomies;
- allocation DTO plus `Allocation::apply_to_targets`;
- order DTO plus `OrderStatus::can_transition_to`;
- execution feedback DTOs;
- handoff snapshot DTO plus `HandoffSnapshot::validate_completeness`.

## Closeout Evidence

| Check | Result | Evidence |
| --- | --- | --- |
| Physical owner exists | pass | `qrpc_core/src/rfc_execution_contracts.rs` now owns the moved contracts. |
| Public facade preserved | pass | `qrpc_core/src/lib.rs` re-exports `rfc_execution_contracts::*`. |
| Rust equivalence verified | pass | `cargo check -p qrpc-core`, `cargo test -p qrpc-core`, and smart pre-commit passed in BE-001SQ-02. |
| Mixed responsibility detected | pass | The file has distinct data request, allocation, order, feedback, and handoff owners. |

## Candidate Child Queue

| Candidate | Proposed scope | Reason |
| --- | --- | --- |
| `data_request` | `MarketScope` through `DataRequest` | RFC-001 data request schema and taxonomy are a self-contained public contract family. |
| `allocation` | `AllocationMethod`, `Allocation`, and `Allocation::apply_to_targets` | Allocation has its own DTO plus pure projection helper. |
| `order_contract` | `OrderStatus`, `OrderStatus::can_transition_to`, and `Order` | Order schema and transition matrix form a separate compatibility contract. |
| `execution_feedback` | `FeedbackKind` and `ExecutionFeedback` | Feedback events are a compact schema owner separate from order status transitions. |
| `handoff_snapshot` | `HandoffSnapshot` and `HandoffSnapshot::validate_completeness` | Runtime handoff snapshot and validation are consumed by sandbox/runtime paths. |

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | Five candidate child families are named with clear owned inputs/outputs. |
| parent_child_communication_kept | pass | Future children can be mediated through the `rfc_execution_contracts` parent facade. |
| equivalence_baseline_freezable | pass | Each child has a small, explicit public surface and local behavior to freeze. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | pass | All candidates are public qrpc-core contract surfaces. |
| state_machine_phase | guarded | `order_contract` contains a pure transition matrix; it is worth isolating but must preserve exact behavior. |
| strategy_branch | no | No strategy branch is involved. |
| independent_failure_mode | pass | Data request schema drift, allocation math drift, order transition drift, feedback schema drift, and handoff validation drift fail independently. |
| reuse_pressure | pass | Runtime/backend/sandbox callers can consume each child through the qrpc-core facade. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | Candidate children are owner-based, not per-field fragments. |
| communication_cost_rises | no | Parent facade can preserve current public names and mediate child dependencies. |
| local_proof_missing | no | qrpc-core compile/test gates cover each planned child. |
| line_count_only | no | The split is based on public contract families and helper behavior, not raw size. |

leaf_split_decision_result

`continue_split: true`

`rfc_execution_contracts` becomes a local parent facade candidate. The next child is `root.contracts.qrpc_core.rfc_execution_contracts.data_request`.

## Same-Parent Parallel Judgment

`same_parent_parallel_children` remains inactive for the next selection.

Reason: this is the first child under the new parent. The safest high-speed path is to establish one child/facade pattern first, then decide whether later sibling DTO leaves can use a parallel wave.

## Hard Boundaries

The next `data_request` parent residual judgment and baseline must not:

- move allocation, order, feedback, or handoff contracts;
- change enum variants, field names, serde derives, helper behavior, validation messages, public exports, or tests;
- reopen closed qrpc-core siblings;
- introduce sibling horizontal shortcuts or release-transition optimization.

## Next Step

BE-001SR-01 `root.contracts.qrpc_core.rfc_execution_contracts` parent_residual_judgment selects `data_request`.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
