# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline.sandbox_mode_surface extract closeout

> Batch: BE-002CV-01
> Node: `root.contracts.runtime_support.sandbox_replay_timeline.sandbox_mode_surface`
> Parent: `root.contracts.runtime_support.sandbox_replay_timeline`
> Stage: `actual_extraction`
> Movement: Rust code movement.

---

## Scope

Extract the public sandbox mode/control surface from `qrpc_runtime/src/sandbox/mod.rs`.

Moved file:

- `qrpc_runtime/src/sandbox/mode_surface.rs`

Moved surface:

- supported runtime mode and execution-module keys;
- `RuntimeSupportBoundary` and `runtime_support_boundary`;
- deterministic test mode enums and DTO;
- `SandboxMode`;
- `SandboxSnapshot`;
- `Sandbox` trait.

## Parent Boundary

`qrpc_runtime/src/sandbox/mod.rs` now declares and re-exports the mode surface child while keeping concrete sandbox implementations parent-owned.

The parent still owns:

- `RealTimeSandbox`;
- `FastBacktestSandbox`;
- benchmark/snapshot/trace helper functions;
- local sandbox tests.

## Equivalence

The extraction preserves:

- crate-root exports from `qrpc_runtime/src/lib.rs`;
- `Sandbox` trait method signatures;
- `SandboxSnapshot` serialization shape;
- deterministic test mode defaults and constructors;
- `runtime_support_boundary` key values;
- concrete sandbox implementations of the trait.

## Gates

Verified for this batch:

- `cargo fmt --check`: pass
- `cargo check -p qrpc-runtime`: pass
- `cargo test -p qrpc-runtime sandbox -- --nocapture`: pass, 21 passed / 2 ignored

Remaining full gates are required before commit:

- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 / full feature tree / matrix governance

## Next Step

BE-002CV-02 `root.contracts.runtime_support.sandbox_replay_timeline.sandbox_mode_surface` single_leaf_closeout.
