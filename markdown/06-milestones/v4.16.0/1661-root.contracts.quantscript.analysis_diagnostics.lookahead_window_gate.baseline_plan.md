# v4.16.0 root.contracts.quantscript.analysis_diagnostics.lookahead_window_gate baseline plan

> Batch: BE-002GW-01
> Node: `root.contracts.quantscript.analysis_diagnostics.lookahead_window_gate`
> Parent: `root.contracts.quantscript.analysis_diagnostics`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Scope

Freeze the lookahead-window diagnostic gate before movement.

Current owner:

- `quantscript/src/analysis_diagnostics/analysis.rs`

Planned child:

- `quantscript/src/analysis_diagnostics/lookahead_window_gate.rs`

## Items Frozen For Movement

The next extraction may move only:

- `collect_series_index_diagnostics`
- `collect_series_index_from_stmts`
- `collect_series_index_from_expr`
- `collect_centered_window_diagnostics`
- `collect_centered_window_from_stmts`
- `collect_centered_window_from_expr`
- `centered_window_uses_future_bars`
- `arg_bool_named`
- `expr_integer`

## Diagnostic Boundary

The child owns only:

- `QS0401` negative series index and invalid trailing-window diagnostics;
- `QS0402` centered rolling-window future-bar diagnostic;
- `QS0403` zero trailing-window diagnostic.

## Parent Wiring To Preserve

The analysis parent must keep:

- `ScriptAnalysis` and `analyze_script_module` public surface unchanged;
- diagnostic family invocation order unchanged:
  - `collect_unsupported_construct_diagnostics`
  - `collect_series_index_diagnostics`
  - `collect_centered_window_diagnostics`
  - remaining diagnostic families;
- crate-root public exports and `crate::analysis` / `crate::diagnostics` compatibility unchanged.

The planned child may expose only parent-visible `pub(super)` collection functions required by `analysis.rs`.

## Forbidden Movement

The next extraction must not move or change:

- `infer_required_warmup_bars` or `required_warmup_for_semantic`;
- `collect_warmup_diagnostics`, `collect_fetch_lookbacks_from_*`, `fetch_lookback`, or `arg_number_named`;
- strategy fetch/emit presence checks;
- fetch lookback warnings;
- indirect recursion checks;
- data-source map and index bounds checks;
- dead-code emit checks;
- symbol whitelist checks;
- `Diagnostic`, `DiagnosticSeverity`, `Span`, or `SpanContext` DTOs;
- evaluator normalization, runtime lowering, v4 static audit, tests, authoring samples, backend, executor, frontend, plugin metadata, or release transition.

## Expected Validation

The extraction must pass:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002GW-02 `root.contracts.quantscript.analysis_diagnostics.lookahead_window_gate` actual_extraction.
