# v4.16.0 root.contracts.core_ir.v4_contracts.qs_type_system_contract actual extraction complete

> Batch: BE-001VK-02
> Node: `root.contracts.core_ir.v4_contracts.qs_type_system_contract`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `extract_closeout`
> Movement: Code movement completed.

---

## Summary

`root.contracts.core_ir.v4_contracts.qs_type_system_contract` actual extraction is complete.

Implemented movement:

- `qrpc_core_ir/src/v4.rs` now declares `mod qs_type_system_contract;`
- `qrpc_core_ir/src/v4.rs` re-exports the child through `pub use qs_type_system_contract::*;`
- `qrpc_core_ir/src/v4/qs_type_system_contract.rs` owns QS type DTOs, scalar/composite enums, type refs,
  first-wave QS type constants, public constructors, static validation, and type-ref validation helpers
- `default_qs_type_system_version` moved into the child and remains `pub(in crate::v4)` for parent
  `V4VersionManifest` serde defaults
- private QS serde defaults for scalar types, composite specs, and max nesting depth moved with the child

Equivalence notes:

- public QS type system surface remains unchanged through the parent re-export
- first-wave scalar/composite type order, default composite specs, max nesting depth, and validation error strings
  remain frozen from BE-001VK-01
- schema identity constants, version manifest orchestration, plugin governance, reproducibility, complexity budget,
  developer learning pipeline, static bundles, compile-time capability reports, compatibility bridge, bridge builders,
  tests, machine contract, machine graph contract, QS profile, runtime mode contract, and venue capability matrix remain
  outside this child

Verification:

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir` (54 passed)
- `cargo check -p quantpilot`

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs,qrpc_core_ir/src/v4/qs_type_system_contract.rs`

**Markers**:
- `BE-001VK-02`

**Next step**:
BE-001VK-03 root.contracts.core_ir.v4_contracts.qs_type_system_contract single_leaf_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
