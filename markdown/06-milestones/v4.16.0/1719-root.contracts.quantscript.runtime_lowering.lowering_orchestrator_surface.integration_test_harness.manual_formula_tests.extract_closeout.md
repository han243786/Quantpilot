# v4.16.0 root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.manual_formula_tests extract closeout

> Batch: BE-002IC-02
> Node: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.manual_formula_tests`
> Parent: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness`
> Stage: `actual_extraction`
> Movement: Rust code moved.
> governance_next_authority: active

---

## Extraction Result

`manual_formula_tests` is extracted.

Created:

- `quantscript/src/lowering/orchestrator/integration_test_harness/manual_formula_tests.rs`

Updated:

- `quantscript/src/lowering/orchestrator/integration_test_harness.rs`

## Moved Owner

The new child owns:

- manual moving average formula lowering test;
- manual momentum, zscore, MACD histogram, momentum ratio, and MA gap ratio tests;
- manual RSI variants covering RS ratio, avg gain/loss aliases, EMA RSI, and Cutler RSI;
- unsupported `for` / `while` loop-built gain/loss list rejection tests.

## Parent Residual

`integration_test_harness.rs` remains the parent test harness for:

- basic MA / RSI / alias smoke;
- runtime coordinator smoke;
- user-defined helper call smoke;
- semantic missing-helper rejection;
- exchange argument decoding;
- spread lowering admission and rejection;
- rebalance lowering and rejection;
- child test module declarations.

No production code moved. No sibling production link was introduced.

## Validation

Passed:

- `cargo fmt`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`

Remaining closeout gates:

- `git diff --check`
- UTF-8
- full-feature-tree
- matrix governance

## Next Step

BE-002IC-03 single_leaf_closeout.
