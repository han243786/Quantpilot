# v4.16.0 root.contracts.runtime_support.core_ir_evaluator.spread_custom_expression_wave extract closeout

> Batch: BE-002AO-02
> Node: `root.contracts.runtime_support.core_ir_evaluator.spread_custom_expression_wave`
> Parent: `root.contracts.runtime_support.core_ir_evaluator`
> Stage: `actual_extraction`
> Movement: Rust code movement.

---

## Result

`root.contracts.runtime_support.core_ir_evaluator.spread_custom_expression_wave` has been physically extracted.

New owner file:

- `qrpc_runtime/src/core_ir_evaluator/spread_custom_expression_wave.rs`

Parent file:

- `qrpc_runtime/src/core_ir_evaluator.rs`

## Moved Surface

Moved into the child:

- `evaluate_custom`
- `evaluate_custom_predicate`
- `custom_strength`
- `custom_signal_side`
- `comparison_name`
- `evaluate_spread`
- `evaluate_legacy_spread`
- `SeriesPoint`
- `evaluate_typed_spread`
- `evaluate_series_expr`
- `materialize_data_field`
- `quote_field_value`
- `kline_field_value`
- `resample_series_points`
- `aggregate_bucket`
- `window_aggregate_series_points`
- `aggregate_values`
- `align_series_point`
- `evaluate_custom_value_expr`
- `latest_field_value`
- `aggregate_window_field`
- `aggregate_numeric_values`
- `collect_custom_scope`
- `collect_custom_data_ids_from_expr`
- `custom_reference_price`

Parent changes:

- added `mod spread_custom_expression_wave;`
- imported `evaluate_custom` and `evaluate_spread` from the child for builtin registry dispatch;
- preserved public `indicator_registry` and `evaluate_indicator_signal` behavior.

## Preserved Parent Ownership

The parent still owns:

- public evaluator DTOs, error enum, trait, registry, and facade dispatch;
- closed `classic_indicator_wave` and `advanced_indicator_wave`;
- shared lookup helpers such as `find_kline_snapshot`, `find_quote_snapshot`, and `find_quote_snapshots`;
- generic numeric indicator helpers and formula helpers;
- local tests until the dedicated test-harness step.

The child exposes only `pub(super)` evaluator entrypoints required by the parent registry. Shared helper access remains parent-mediated through `super::*`; no public API was widened and no sibling shortcut was introduced.

## Compatibility Note

During extraction, corrupted non-ASCII assertion/reason strings that had been merged with code comments were normalized to ASCII-only messages so the Rust parser and UTF-8 gate see the same semantics. This did not change evaluator numeric behavior, registry keys, DTO shape, or dispatch ownership.

## Equivalence Evidence

Passed:

- `cargo fmt`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime core_ir_evaluator`: 22 passed
- `cargo check -p quantpilot`

Remaining for commit gate:

- `git diff --check`
- `tools/check-utf8.ps1`
- `tools/check-full-feature-tree.ps1`
- `tools/check-matrix-governance.ps1`

## Next Step

BE-002AO-03 `root.contracts.runtime_support.core_ir_evaluator.spread_custom_expression_wave` single_leaf_closeout.
