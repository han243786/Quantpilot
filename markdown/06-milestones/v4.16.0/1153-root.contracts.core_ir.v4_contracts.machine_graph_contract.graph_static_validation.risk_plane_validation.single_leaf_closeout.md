# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.risk_plane_validation single leaf closeout stops split

> Batch: BE-001UK-03
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.risk_plane_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.risk_plane_validation`
stops splitting.

The leaf now owns one coherent execution-safety validation phase:
`V4MachineGraphContract::validate_risk_plane`. Further splitting would turn branch checks
into helper-only fragments without creating a stronger module boundary.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | The file is a named white-box child for risk-plane graph invariants. |
| parent_child_communication_kept | pass | It communicates only through parent-owned graph state and the `machines_by_id` input map. |
| equivalence_baseline_freezable | pass | `cargo test -p qrpc-core-ir` passed after extraction with 54 tests passed and 0 failed. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | no | The public graph validator remains in the parent orchestration file. |
| state_machine_phase | pass | The leaf is a complete execution-safety phase of graph validation. |
| strategy_branch | guarded | It protects execution/risk graph semantics consumed by downstream validation flows. |
| independent_failure_mode | pass | Missing risk plane, required flag, risk machine validity, and inbound edge origin are independent from DAG/event checks. |
| reuse_pressure | no | There is no current second caller that justifies splitting inner helper fragments. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | yes | Smaller pieces would be private branch helpers rather than stable white-box children. |
| communication_cost_rises | yes | Sub-splitting would pass execution/risk ID sets and error vectors between helpers for little boundary gain. |
| local_proof_missing | no | Existing graph/risk-plane tests prove the leaf through the public validator. |
| line_count_only | no | The stop decision is based on ownership and communication cost, not line count. |

leaf_split_decision_result

`stop_split: true`

Next parent residual candidate:

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation`

next_recursive_step

BE-001UL-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation parent_residual_judgment selects event_usage_validation
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/risk_plane_validation.rs`

**Markers**:
- `BE-001UK-03`

**Next step**:
BE-001UL-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation parent_residual_judgment selects event_usage_validation

---

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
