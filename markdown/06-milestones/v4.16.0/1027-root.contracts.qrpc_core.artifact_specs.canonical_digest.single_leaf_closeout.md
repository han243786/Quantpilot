# v4.16.0 root.contracts.qrpc_core.artifact_specs.canonical_digest single leaf closeout

> Batch: BE-001RU-03
> Node: `root.contracts.qrpc_core.artifact_specs.canonical_digest`
> Parent: `root.contracts.qrpc_core.artifact_specs`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core.artifact_specs.canonical_digest` has been evaluated after BE-001RU-02 extraction.

Decision:

`stop_split: true`

The node is now a compact digest child module with one responsibility: artifact digest algorithm, digest DTO, and canonical JSON SHA-256 helper.

## Split Rule Evaluation

| Rule | Result |
| --- | --- |
| Physical owner | Stop. The selected surface is isolated in `qrpc_core/src/artifact_specs/canonical_digest.rs`. |
| Public method count | Stop. The child exposes one pure helper and two small public schema surfaces. |
| Mixed responsibility | Stop. Algorithm enum, digest DTO, and helper are one digest contract family. |
| State ownership | Stop. The child owns no runtime mutable state. |
| Side effects | Stop. No IO, persistence, locks, async lifecycle, external calls, or public API mutation are involved. |
| Parent-mediated dependency | Covered. The artifact specs parent re-exports the child and keeps sibling schemas parent-mediated. |
| Future reopen rule | Allowed only when a concrete digest algorithm, digest DTO, helper behavior, serde shape, or public facade proposal is made. |

## White-Box Boundary

| Input | Processing owner | Output |
| --- | --- | --- |
| Serializable artifact payload | `contracts.qrpc_core.artifact_specs.canonical_digest` | Stable `ArtifactDigest` using canonical JSON SHA-256 |

The leaf may describe and guard:

- digest algorithm variants;
- digest DTO fields;
- canonical JSON serialization source;
- SHA-256 hashing and lowercase hex formatting.

## Rust Local Fields

| Field | Result |
| --- | --- |
| Crate | `qrpc-core` |
| Child visibility | Private module; public items are re-exported by the artifact specs parent. |
| Public exports | Preserved through `pub use canonical_digest::*;` and crate-root `pub use artifact_specs::*;`. |
| Parent facade | `qrpc_core/src/artifact_specs.rs` remains the local coordinator. |
| Production dependency check | Passed. Run/backtest specs and tests consume digest names through the parent facade. |

## Non-Claims

This closeout does not claim:

- run/backtest specs or artifact bundle DTOs have been extracted;
- runtime IO contracts, RFC execution contracts, digest behavior, or crate-root tests changed;
- qrpc_core parent residual judgment is complete;
- closed qrpc-core children or release transition changed.

## Proof

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-001RV-01 `root.contracts.qrpc_core.artifact_specs` parent_residual_judgment selects `run_backtest_specs`.
