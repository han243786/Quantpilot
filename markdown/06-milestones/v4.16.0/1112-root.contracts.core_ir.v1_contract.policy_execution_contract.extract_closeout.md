# v4.16.0 root.contracts.core_ir.v1_contract.policy_execution_contract extract closeout

> Batch: BE-001TO-02
> Node: `root.contracts.core_ir.v1_contract.policy_execution_contract`
> Parent: `root.contracts.core_ir.v1_contract`
> Stage: `extract_closeout`
> Movement: Rust code moved under the same v1 parent facade.

---

## Summary

`policy_execution_contract` now owns the v1 production signal, agent policy, risk policy, execution DTOs, and serde default helpers that were previously embedded in `qrpc_core_ir/src/v1.rs`.

Created:

- `qrpc_core_ir/src/v1/policy_execution_contract.rs`

Updated:

- `qrpc_core_ir/src/v1.rs`

## Moved Surface

| Surface | Result |
| --- | --- |
| `SignalRule` | Moved into `policy_execution_contract.rs` unchanged. |
| `AgentPolicy`, `AgentPolicyKind`, `RebalanceSchedule` | Moved into `policy_execution_contract.rs` unchanged. |
| `RiskPolicy` | Moved into `policy_execution_contract.rs` unchanged, including optional `max_cross_symbol_leverage` serde behavior. |
| `ExecutionSizingKind`, `CoreTimeInForce`, `ExecutionRule` | Moved into `policy_execution_contract.rs` unchanged. |
| `default_true` | Moved with policy/risk DTOs so serde defaults remain local to the child. |
| `default_execution_sizing_kind` | Moved with `ExecutionRule` unchanged. |
| `default_time_in_force` | Moved with `ExecutionRule` unchanged. |
| v1 parent facade | Added `mod policy_execution_contract;` and `pub use policy_execution_contract::*;`. |

## Preserved Parent Residuals

These declarations intentionally remain in `qrpc_core_ir/src/v1.rs`:

- child module declarations and public re-exports;
- cfg-test `BTreeMap` import for local test construction;
- local v1 round-trip regression test.

## Equivalence Proof

| Check | Result | Evidence |
| --- | --- | --- |
| Public names preserved | pass | `qrpc_core_ir/src/v1.rs` re-exports `policy_execution_contract::*`, and crate root still re-exports `v1::*`. |
| Serde shape preserved | pass | Derives, field names, defaults, skip rules, and enum rename rules were mechanically moved. |
| Default helper behavior preserved | pass | Helper bodies are unchanged. |
| Parent-mediated dependency preserved | pass | The child imports `ScalarExpr` and `SignalKind` through `super::{ScalarExpr, SignalKind}` rather than direct sibling shortcuts. |
| Test residual preserved | pass | Local test still lives under `qrpc_core_ir/src/v1.rs` and keeps `use super::*`. |
| Release transition avoided | pass | No cross-child direct wiring, performance shortcut, or release-transition proposal was introduced. |

## Parent-Child Communication

The v1 parent remains the local gateway:

- external consumers continue through `qrpc_core_ir::*`;
- v1-local consumers continue through `v1` parent re-exports;
- policy execution child depends on expression names through the parent boundary only;
- local tests continue proving the full v1 facade.

## Hard Boundary Result

No struct field, enum variant, serde shape, helper behavior, test assertion, v4 contract, qrpc-core behavior, compiler behavior, runtime behavior, backend behavior, executor behavior, frontend behavior, physical `plugins/*`, or release-transition pathway was intentionally changed.

## Next Step

BE-001TO-03 `root.contracts.core_ir.v1_contract.policy_execution_contract` single_leaf_closeout.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
