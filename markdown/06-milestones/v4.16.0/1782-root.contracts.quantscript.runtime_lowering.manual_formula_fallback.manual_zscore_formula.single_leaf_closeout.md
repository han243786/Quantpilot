# v4.16.0 root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_zscore_formula single leaf closeout

> Batch: BE-002JM-03
> Node: `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_zscore_formula`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`stop_split: true`.

`manual_zscore_formula` is now a terminal leaf. It owns one cohesive ZScore
fallback recognition decision: direct semantic ZScore handling, numerator
subtract validation, resolve-first operand matching, and legacy
current/mean/stddev recovery. Further splitting would separate tightly coupled
operand evidence and raise governance cost without improving module clarity.

## Terminal Leaf Control

`tools/evaluate-leaf-granularity.ps1 -Path quantscript/src/lowering/fallback/manual_zscore_formula.rs` result:

| Metric | Result |
| --- | --- |
| `split_decision` | `STOP` |
| `governance_packaging` | `stop_split` |
| `final_decision` | `STOP` |
| `normalized_split_score` | 22 |
| `loc` | 114 |
| `functions` | 3 |
| `public_surface` | 2 |
| `branches` | 6 |
| `risk_tags` | `cross_crate`, `public_api`, `trading_semantics` |
| `governance_mode` | `stop_split` |

## Closeout Rationale

| Gate | Result | Evidence |
| --- | --- | --- |
| Terminal size fit | PASS | Current child is a small cohesive leaf |
| Ownership cohesion | PASS | All functions participate in one ZScore recognition family |
| Public surface control | PASS | Parent wrappers preserve ZScore helper surfaces |
| Split benefit | LOW | Script scored split benefit at 8 |
| Governance cost | STOP | Script recommends `stop_split` |

## Next Step

BE-002JN-01 return to `root.contracts.quantscript.runtime_lowering.manual_formula_fallback` parent residual judgment.
