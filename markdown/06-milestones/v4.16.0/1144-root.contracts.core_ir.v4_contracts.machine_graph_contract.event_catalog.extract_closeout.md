# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.event_catalog actual extraction complete

> Batch: BE-001UG-02
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.event_catalog`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract`
> Stage: `extract_closeout`
> Movement: Code movement complete.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract.event_catalog` is now extracted as a private graph child module.

The child owns exactly:

- `MachineEventCatalog`
- `MachineEventTypeSpec`
- `MachineEventPayloadField`
- `MachineEventSourceKind`
- `MachineEventScope`
- `MachineEventCatalog::validate_static_contract`

The graph parent now declares and re-exports the child:

- `mod event_catalog;`
- `pub use event_catalog::*;`

Graph-level event presence, emitter, consumer, party, and traversal checks remain in the graph parent residual.

## Boundary

**Real files**:

- `qrpc_core_ir/src/v4/machine_graph_contract.rs`
- `qrpc_core_ir/src/v4/machine_graph_contract/event_catalog.rs`

**Markers**:

- `BE-001UG-02`

## White-Box Result

| Surface | Owner after extraction | Parent-mediated dependencies |
| --- | --- | --- |
| event catalog schema | `event_catalog` | event catalog default helper and version constant |
| event catalog local validation | `event_catalog` | `V4_MACHINE_EVENT_CATALOG_VERSION` |
| graph-level event validation | `machine_graph_contract` | consumes catalog schema through graph parent re-export |

## Equivalence Evidence

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir` passed: 54 tests.
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`

## Next Step

BE-001UG-03 `root.contracts.core_ir.v4_contracts.machine_graph_contract.event_catalog` single_leaf_closeout.
