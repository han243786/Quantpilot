# v4.16.0 root.contracts.runtime_support.v4_runtime_support.risk_execution_gate extract closeout

> Batch: BE-002DZ-01
> Node: `root.contracts.runtime_support.v4_runtime_support.risk_execution_gate`
> Parent: `root.contracts.runtime_support.v4_runtime_support`
> Stage: `actual_extraction`
> Movement: Rust code movement.

---

## Extracted Child

`qrpc_runtime/src/v4_runtime/risk_execution_gate.rs` now owns the v4 risk/execution gate surface:

- `V4PaperSimulatedRuntime::evaluate_risk_plane_for_execution`
- `V4PaperSimulatedRuntime::record_risk_plane_decision`
- `V4PaperSimulatedRuntime::evaluate_execution_capabilities_for_execution`
- `V4PaperSimulatedRuntime::record_execution_decision`
- `V4PaperSimulatedRuntime::validate_single_execution_capability`

The v4 runtime parent now declares `mod risk_execution_gate;`.

## Boundary Preserved

The extraction deliberately keeps these parent-owned:

- `apply_runtime_simulated_execution_for_transition`;
- simulated order request construction;
- simulated order capability enumeration;
- simulated execution order/fill internals;
- snapshots and v4 tests.

All moved gate methods are `pub(super)` because the closed machine transition child and parent-owned simulated execution hooks call them.

## Equivalence Notes

- Risk Plane required/source/origin/event-source/payload/priority/status checks remain equivalent.
- Risk approved/rejected counters and last-decision projection remain equivalent.
- Execution capability policy missing/empty/not-declared/unsupported/mode-rejected checks remain equivalent.
- Execution accepted/rejected counters and last-decision projection remain equivalent.
- Single capability validation for amend/cancel-replace paths remains equivalent.

## Verified Gates

- `cargo fmt --check`: PASS
- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime v4 -- --nocapture`: PASS, 32 passed
- `cargo check -p quantpilot`: PASS

Pending governance gates are expected to run before commit:

- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002DZ-02 `root.contracts.runtime_support.v4_runtime_support.risk_execution_gate` single_leaf_closeout.
