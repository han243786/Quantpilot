# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline.fast_backtest_sandbox single leaf closeout

> Batch: BE-002DB-02
> Node: `root.contracts.runtime_support.sandbox_replay_timeline.fast_backtest_sandbox`
> Parent: `root.contracts.runtime_support.sandbox_replay_timeline`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Decision

`stop_split: true`

`fast_backtest_sandbox` is a terminal leaf. It owns one cohesive fast backtest implementation surface and should not be split into constructor, replay setup, run loop, output assembly, benchmark projection, and `Sandbox` trait micro-leaves.

## Leaf Granularity Evidence

`tools/evaluate-leaf-granularity.ps1` result:

- Decision: STOP
- Score: 47
- LOC: 454 reported by tool
- Functions: 30
- Public surface: 21
- Governance mode: `stop_split`
- Reason: medium score without same-parent wave candidate; avoid standalone governance

## Stop Rationale

- The leaf is inside the target terminal size band.
- The run loop, output assembly, and benchmark projection share the same `RuntimeCoordinator`, timeline, and portfolio state owner.
- Splitting would create high communication overhead between micro-leaves without producing a stronger owner boundary.
- The next useful parent residual is test harness extraction, not further production implementation split.

## Preserved Boundary

`qrpc_runtime/src/sandbox/fast_backtest_sandbox.rs` remains the owner for:

- `FastBacktestSandbox`
- fast replay/backtest constructors
- execution assumption and risk monitor helpers
- `run_backtest`
- `impl Sandbox for FastBacktestSandbox`
- `compute_benchmark_equity`

`qrpc_runtime/src/sandbox/mod.rs` remains the parent owner for:

- `snapshot_from`
- `ensure_running`
- `trace_id`
- sandbox tests
- parent re-exports

## Next Step

BE-002DC-01 `root.contracts.runtime_support.sandbox_replay_timeline` parent_residual_judgment, expected next candidate: `test_harness`.
