# v4.16.0 root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_rsi_formula single leaf closeout

> Batch: BE-002JG-03
> Node: `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_rsi_formula`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`stop_split: true`.

`manual_rsi_formula` is now a terminal leaf. It owns one cohesive RSI fallback
recognition decision: formula shell matching, RS pair extraction, balanced
gain/loss smoothing validation, source recovery, and `RsiMethod` mapping.
Further splitting would separate tightly coupled parts of the same recognizer
and raise governance cost without improving module clarity.

## Terminal Leaf Control

`tools/evaluate-leaf-granularity.ps1 -Path quantscript/src/lowering/fallback/manual_rsi_formula.rs` result:

| Metric | Result |
| --- | --- |
| `split_decision` | `STOP` |
| `governance_packaging` | `stop_split` |
| `final_decision` | `STOP` |
| `normalized_split_score` | 44 |
| `loc` | 184 |
| `functions` | 7 |
| `public_surface` | 2 |
| `branches` | 14 |
| `risk_tags` | `cross_crate`, `public_api` |
| `governance_mode` | `stop_split` |

## Closeout Rationale

| Gate | Result | Evidence |
| --- | --- | --- |
| Terminal size fit | PASS | Current child is inside the target terminal range |
| Ownership cohesion | PASS | All functions participate in one RSI manual fallback recognizer |
| Public surface control | PASS | Only parent-facing delegation functions are exposed to `fallback.rs` |
| Split benefit | LOW | No independent sub-owner exists without fragmenting RS formula semantics |
| Governance cost | STOP | Script recommends `stop_split` |

## Next Step

BE-002JH-01 return to `root.contracts.quantscript.runtime_lowering.manual_formula_fallback` parent residual judgment.
