# v4.16.0 root.contracts.runtime_support.core_ir_evaluator parent residual judgment selects classic_indicator_wave

> Batch: BE-002AJ-01
> Node: `root.contracts.runtime_support.core_ir_evaluator`
> Parent: `root.contracts.runtime_support`
> Stage: `parent_residual_judgment`
> Selected child: `root.contracts.runtime_support.core_ir_evaluator.classic_indicator_wave`
> Movement: No Rust code movement.

---

## Parent State

BE-002AI-01 froze the `core_ir_evaluator` baseline and opened same-parent waves rather than single-indicator micro leaves.

The first child is `classic_indicator_wave`.

## Selected Child Boundary

`root.contracts.runtime_support.core_ir_evaluator.classic_indicator_wave` owns the stable classic evaluator entrypoints currently embedded in `qrpc_runtime/src/core_ir_evaluator.rs`:

- `evaluate_ma_family`
- `evaluate_rsi`
- `evaluate_macd`
- `evaluate_momentum`
- `evaluate_zscore`
- `evaluate_quote_observe`

These functions are selected as one wave because they are all direct builtin evaluator entrypoints registered from the parent registry, and they depend on parent-mediated lookup/math helpers.

## Exclusions

The selected child must not absorb:

- advanced indicator evaluators: ATR, Bollinger Bands, OBV, CMF, ADX, Stochastic, CCI, Parabolic SAR, Keltner Channel, Donchian Channel;
- Spread or Custom evaluator entrypoints;
- typed series expression, resampling, alignment, or aggregation machinery;
- shared lookup/math helpers such as `find_kline_snapshot`, `param_or_default`, `moving_average`, `relative_strength_index`, `macd_histogram`, and `z_score`;
- local tests.

## Split Decision

`continue_split: true`.

Reason:

- the selected functions are cohesive public-registry targets and can be extracted behind parent-mediated registry dispatch;
- the wave is large enough to reduce parent file weight but avoids one-indicator governance churn;
- helper ownership remains parent-owned until later residual judgment proves a cleaner owner.

No release-transition optimization is proposed.

## Next Step

BE-002AK-01 `root.contracts.runtime_support.core_ir_evaluator.classic_indicator_wave` baseline_plan.

## Verification

No code moved in this step. Required gates:

- `git diff --check`
- `tools/check-utf8.ps1`
- `tools/check-full-feature-tree.ps1`
- `tools/check-matrix-governance.ps1`
