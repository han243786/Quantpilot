# v4.16.0 root.contracts.core_ir.v1_contract.root_graph_contract extract closeout

> Batch: BE-001TK-02
> Node: `root.contracts.core_ir.v1_contract.root_graph_contract`
> Parent: `root.contracts.core_ir.v1_contract`
> Stage: `extract_closeout`
> Movement: Rust code moved under the same v1 parent facade.

---

## Summary

`root_graph_contract` now owns the v1 root graph schema, constructor, metadata/source DTOs, and DAG validation that were previously embedded in `qrpc_core_ir/src/v1.rs`.

Created:

- `qrpc_core_ir/src/v1/root_graph_contract.rs`

Updated:

- `qrpc_core_ir/src/v1.rs`

## Moved Surface

| Surface | Result |
| --- | --- |
| `CORE_IR_V1_VERSION` | Moved into `root_graph_contract.rs` unchanged. |
| `CUSTOM_EXPR_V1_VERSION` | Moved into `root_graph_contract.rs` unchanged. |
| `CoreStrategyIr` | Moved into `root_graph_contract.rs` unchanged. |
| `CoreIREdge` | Moved into `root_graph_contract.rs` unchanged. |
| `CoreStrategyIr::new` | Moved into `root_graph_contract.rs` unchanged. |
| `CoreStrategyIr::validate_dag` | Moved into `root_graph_contract.rs` unchanged. |
| `CoreMetadata` and `CoreSourceKind` | Moved into `root_graph_contract.rs` unchanged. |
| v1 parent facade | Added `mod root_graph_contract;` and `pub use root_graph_contract::*;`. |

## Equivalence Proof

| Check | Result | Evidence |
| --- | --- | --- |
| Public names preserved | pass | `qrpc_core_ir/src/v1.rs` re-exports `root_graph_contract::*`, and crate root still re-exports `v1::*`. |
| Root graph fields preserved | pass | The selected lines were mechanically moved. |
| Constructor preserved | pass | `CoreStrategyIr::new` body was mechanically moved. |
| DAG validation preserved | pass | DFS implementation and diagnostic string were mechanically moved. |
| Parent-mediated dependencies preserved | pass | Child imports sibling DTO references through `super::{AgentPolicy, DataBinding, ExecutionRule, IndicatorNode, RiskPolicy, SignalRule}`. |
| Test coverage preserved | pending validation | BE-001TK-02 must pass `cargo test -p qrpc-core-ir`. |

## Parent-Child Communication

The v1 parent remains the local gateway:

- external consumers continue through `qrpc_core_ir::*`;
- v1-local consumers continue through `v1` parent re-exports;
- child references to sibling DTOs remain parent-mediated through `super::...`.

## Hard Boundary Result

No constant value, struct field, enum variant, serde shape, constructor behavior, DAG validation behavior, diagnostic, test assertion, v4 contract, qrpc-core behavior, compiler behavior, runtime behavior, backend behavior, executor behavior, frontend behavior, physical `plugins/*`, or release-transition pathway was intentionally changed.

## Next Step

BE-001TK-03 `root.contracts.core_ir.v1_contract.root_graph_contract` single_leaf_closeout.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
