# v4.16.0 root.contracts.runtime_support.data_module.mock_data_generation extract closeout

> Batch: BE-001ZK-02
> Node: `root.contracts.runtime_support.data_module.mock_data_generation`
> Parent: `root.contracts.runtime_support.data_module`
> Stage: `actual_extraction`
> Movement: Rust code movement.

---

## Extraction Summary

Created:

- `qrpc_runtime/src/data_module/mock_data_generation.rs`

Updated:

- `qrpc_runtime/src/data_module.rs`

Moved implementation surface:

- `MOCK_VOLATILITY`
- `DEFAULT_MOCK_VOLATILITY`
- `get_mock_volatility`
- `mock_raw_klines`
- `pseudo_random`
- `mock_raw_quote`
- `mock_kline_bars_for_backtest`

## Parent Facade

`qrpc_runtime/src/data_module.rs` now owns only:

- `mod mock_data_generation;`
- `pub use mock_data_generation::MOCK_VOLATILITY;`
- `pub(crate) use mock_data_generation::mock_kline_bars_for_backtest;`
- imports for `mock_raw_klines` and `mock_raw_quote` used by `BuiltinDataModule::mock_data`.

## Boundary Preservation

| Boundary | Result |
| --- | --- |
| Public volatility surface | Preserved through parent re-export of `MOCK_VOLATILITY`. |
| Crate-visible backtest helper | Preserved through parent crate-visible re-export of `mock_kline_bars_for_backtest`. |
| Provider fallback | `BuiltinDataModule::mock_data` still calls the same mock raw quote/kline generation behavior. |
| Determinism | `pseudo_random` and seed logic moved unchanged in behavior. |
| Safety clamp | NaN/Inf rejection and volatility clamp stay in the mock child. |
| Parent-child communication | Mock child calls normalization through the parent path; no sibling horizontal link is introduced. |
| Parent test compatibility | Parent tests keep access to `pseudo_random` through a `cfg(test)` parent import; production surface is not widened. |

## Exclusions Preserved

No movement was made to:

- historical cache helpers;
- HTTP fetch and PowerShell fallback;
- exchange endpoint and payload parsing;
- provider orchestration;
- quality diagnostics;
- data-module tests.

## Next Step

BE-001ZK-03 `root.contracts.runtime_support.data_module.mock_data_generation` single_leaf_closeout. The closeout must run `terminal_leaf_control_v2` against `qrpc_runtime/src/data_module/mock_data_generation.rs` before deciding whether to split further.
