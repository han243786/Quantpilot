# v4.16.0 root.contracts.runtime_support.agent_module parent residual judgment selects test_harness

> Batch: BE-002AE-01
> Node: `root.contracts.runtime_support.agent_module`
> Stage: `parent_residual_judgment`
> Selected child: `root.contracts.runtime_support.agent_module.test_harness`
> Movement: No Rust code movement.

---

## Parent State

Closed agent-module children:

- `contracts.runtime_support.agent_module.weighted_signal_decisions`
- `contracts.runtime_support.agent_module.portfolio_rebalance`
- `contracts.runtime_support.agent_module.cross_venue_arbitrage`
- `contracts.runtime_support.agent_module.shared_scoring_portfolio_helpers`

Remaining agent-module queue from the frozen baseline:

- `contracts.runtime_support.agent_module.test_harness`

## Selected Child Boundary

`test_harness` owns the local `agent_module` unit-test fixtures and assertions currently embedded in the parent file.

Candidate current owner:

- `qrpc_runtime/src/agent_module.rs`

Candidate test surface:

- `#[cfg(test)] mod tests`
- sample execution-rule fixture;
- sample Core IR and portfolio fixtures;
- weighted signal decision tests;
- cross-venue arbitrage tests;
- portfolio rebalance tests;
- long-term symbol/exchange inheritance tests.

## Why This Child Next

| Criterion | Judgment |
| --- | --- |
| Queue order | It is the final remaining child in the frozen agent-module queue. |
| Cohesion | Strong: all candidate code is local test harness and fixtures. |
| Production risk | Low: no production behavior should move. |
| Extraction risk | Medium: test fixtures depend on private parent helpers and child-visible public surfaces. |
| Exclusions | Production dispatch, policy children, helper ownership, provider trait, and public DTOs stay outside this child. |

## Equivalence Baseline For Next Step

Next baseline must decide whether the local test module should:

- move into a `#[cfg(test)]` child module; or
- remain parent-embedded if fixture access would require noisy test-only plumbing.

Any movement must preserve:

- all current agent-module tests and assertions;
- fixture construction shape;
- access to parent-owned helper constants only through the parent module;
- no production code movement;
- no changes to `AgentModuleProvider` behavior.

Required gates for the next movement:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-002AF-01 `root.contracts.runtime_support.agent_module.test_harness` baseline_plan.
