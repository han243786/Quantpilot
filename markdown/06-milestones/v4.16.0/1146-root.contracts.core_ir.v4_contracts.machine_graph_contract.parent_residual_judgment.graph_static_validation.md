# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract parent residual judgment selects graph_static_validation

> Batch: BE-001UH-01
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract` returns to its residual queue after `event_catalog` closeout.

Selected next child:

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation`

This child owns graph-level validation behavior only.

## Residual Queue

| Child | Status |
| --- | --- |
| `machine_graph_contract.event_catalog` | Closed with `stop_split: true`. |
| `machine_graph_contract.graph_static_validation` | Selected next. |
| `machine_graph_contract.schema_and_helpers` | Parent residual after behavior children close. |

## Selection Rationale

`graph_static_validation` is selected because:

- it owns `V4MachineGraphContract::validate_static_contract`;
- it owns graph DAG, event catalog usage, event party, and risk-plane validation behavior;
- it has independent failure modes covered by graph tests;
- moving it leaves the graph parent as schema/helper facade after behavior closeout.

## Explicit Non-Owners

The selected child does not own:

- `V4MachineGraphContract` field definitions;
- graph edge/risk-plane DTO definitions;
- event catalog DTOs or local catalog validation, already closed under `event_catalog`;
- `collect_machine_family` and `machine_nested_depth`, which remain graph parent helpers because the v4 parent complexity metrics reuse them;
- default helper bodies;
- tests.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | The child owns graph static validation behavior. |
| parent_child_communication_kept | pass | Validation can import graph schema and traversal helpers through the graph parent. |
| equivalence_baseline_freezable | pass | Validation branches and error strings can be frozen before movement. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | pass | `validate_static_contract` is public graph API. |
| state_machine_phase | pass | It validates graph-level orchestration. |
| strategy_branch | guarded | Compat bridge/static bundle consume graph validation, but bridge behavior remains outside. |
| independent_failure_mode | pass | Graph identity, duplicate machine, edge, DAG, event usage, and risk-plane failures are independent. |
| reuse_pressure | pass | Static bundle, bridge, profile, and tests depend on graph validation. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | Validation is a real graph behavior owner. |
| communication_cost_rises | no | Moving the impl as one child keeps schema dependency simple. |
| local_proof_missing | no | Existing graph validation tests pass. |
| line_count_only | no | Split is by behavior ownership. |

leaf_split_decision_result

`continue_baseline: true`; `graph_static_validation` is selected as the next graph child.

## Next Step

BE-001UI-01 `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation` baseline_plan.

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
