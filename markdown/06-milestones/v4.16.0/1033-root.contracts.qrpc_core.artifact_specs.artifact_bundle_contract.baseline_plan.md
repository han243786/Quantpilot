# v4.16.0 root.contracts.qrpc_core.artifact_specs.artifact_bundle_contract baseline plan

> Batch: BE-001RY-01
> Node: `root.contracts.qrpc_core.artifact_specs.artifact_bundle_contract`
> Parent: `root.contracts.qrpc_core.artifact_specs`
> Stage: `baseline_plan`
> Movement: No code movement.

---

## Summary

This baseline freezes the qrpc-core artifact bundle contract surface before extraction.

The next implementation step may move only strategy/core-IR/compile artifact DTOs and `CompileArtifactBundle` from `qrpc_core/src/artifact_specs.rs` into a private child module under the artifact specs parent.

## Selected Surface

The selected child owns:

- `StrategyArtifactSourceKind`;
- `StrategyArtifact`;
- `CoreIrArtifact`;
- `CompileArtifact`;
- `CompileArtifactBundle`.

## Equivalence Baseline

| Surface | Frozen behavior |
| --- | --- |
| Source taxonomy | `StrategyArtifactSourceKind` keeps all variants and snake_case serde. |
| Strategy artifact | `StrategyArtifact` keeps schema/id/source metadata fields and default metadata map. |
| Core IR artifact | `CoreIrArtifact` keeps schema/id/digest/core IR fields. |
| Compile artifact | `CompileArtifact` keeps schema/id/protocol/config/artifact id/digest/runtime config fields. |
| Bundle contract | `CompileArtifactBundle` keeps `strategy`, `compile`, and `core_ir` fields. |
| Public facade | Existing crate-root public names remain available through `qrpc_core::*`. |

## Planned Extraction

BE-001RY-02 may:

- create `qrpc_core/src/artifact_specs/artifact_bundle_contract.rs`;
- add `mod artifact_bundle_contract;` to `qrpc_core/src/artifact_specs.rs`;
- add `pub use artifact_bundle_contract::*;` in the artifact specs parent;
- move only the selected artifact bundle contract region described above;
- leave the artifact specs parent as a local facade over canonical digest, run/backtest specs, and artifact bundle contract children.

## Rust Local Fields

| Field | Required result |
| --- | --- |
| Crate | `qrpc-core` |
| Parent facade | `qrpc_core/src/artifact_specs.rs` remains the local parent facade and re-export owner. |
| Crate facade | `qrpc_core/src/lib.rs` remains unchanged and re-exports artifact specs through `pub use artifact_specs::*;`. |
| Child visibility | Private child module with public items re-exported by the artifact specs parent. |
| Parent-owned residuals | No artifact specs residuals remain after this child extraction. |

## Hard Boundaries

BE-001RY-02 must not:

- change enum variants, serde attributes, field names, bundle shape, public exports, or tests;
- move runtime IO DTOs, RFC execution contracts, or crate-root tests;
- change closed canonical digest, run/backtest specs, other qrpc-core children, or release transition.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-pre-commit-hook.ps1`

## Next Step

BE-001RY-02 `root.contracts.qrpc_core.artifact_specs.artifact_bundle_contract` extract_closeout.
