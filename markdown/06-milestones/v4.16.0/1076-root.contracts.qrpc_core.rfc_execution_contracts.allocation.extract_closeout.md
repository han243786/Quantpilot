# v4.16.0 root.contracts.qrpc_core.rfc_execution_contracts.allocation extract closeout

> Batch: BE-001SU-02
> Node: `root.contracts.qrpc_core.rfc_execution_contracts.allocation`
> Parent: `root.contracts.qrpc_core.rfc_execution_contracts`
> Stage: `extract_closeout`
> Movement: Rust physical extraction complete.

---

## Summary

`allocation` now owns the allocation DTO, taxonomy, and pure projection helper that were previously embedded in `qrpc_core/src/rfc_execution_contracts.rs`.

Moved file:

- `qrpc_core/src/rfc_execution_contracts/allocation.rs`

Updated parent facade:

- `qrpc_core/src/rfc_execution_contracts.rs`

## Actual Extraction

| Baseline item | Result |
| --- | --- |
| `AllocationMethod` | Moved into `allocation.rs`. |
| `Allocation` | Moved into `allocation.rs`. |
| `Allocation::apply_to_targets` | Moved into `allocation.rs`. |
| Parent facade | Added `mod allocation;` and `pub use allocation::*;`. |

## Equivalence Evidence

| Check | Result | Evidence |
| --- | --- | --- |
| Public facade preserved | pass | `qrpc_core/src/rfc_execution_contracts.rs` re-exports `allocation::*`, and `qrpc_core/src/lib.rs` still re-exports RFC execution contracts. |
| Closed sibling boundaries preserved | pass | `data_request` remains closed; order, feedback, and handoff code stayed in the RFC execution parent. |
| Allocation surface preserved | pass | The moved file contains `AllocationMethod`, `Allocation`, and `Allocation::apply_to_targets`. |
| Parent-child communication | pass | The child imports only serde, `BTreeMap`, and public `PortfolioTarget` / `Symbol` through the crate facade. |

## Validation Results

- `cargo fmt -p qrpc-core` passed.
- `cargo check -p qrpc-core` passed.
- `cargo test -p qrpc-core` passed: 12 unit tests passed; doc tests passed with 0 tests.

## Hard Boundaries

This extraction did not:

- change enum variants, struct fields, derives, serde shape, allocation math, public exports, or tests;
- move order, feedback, or handoff contracts;
- reopen closed qrpc-core child modules or closed `data_request`;
- change `qrpc_core_ir`, `qrpc_compiler`, `qrpc_runtime`, `quantscript`, backend, executor, frontend, physical `plugins/*`, or release transition;
- introduce direct sibling shortcuts.

## Next Step

BE-001SU-03 `root.contracts.qrpc_core.rfc_execution_contracts.allocation` single_leaf_closeout.

The closeout must decide whether allocation DTO and helper behavior should stay together or split into smaller subleaves.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
