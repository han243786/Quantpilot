# v4.16.0 root.contracts.runtime_support.data_module parent residual judgment selects historical_cache

> Batch: BE-001ZL-01
> Node: `root.contracts.runtime_support.data_module`
> Stage: `parent_residual_judgment`
> Selected child: `root.contracts.runtime_support.data_module.historical_cache`
> Movement: No Rust code movement.

---

## Parent State

Closed data-module children:

- `contracts.runtime_support.data_module.source_mapping`
- `contracts.runtime_support.data_module.quality_diagnostics`
- `contracts.runtime_support.data_module.collection_orchestration`
- `contracts.runtime_support.data_module.exchange_surface_wave`
- `contracts.runtime_support.data_module.exchange_endpoints`
- `contracts.runtime_support.data_module.exchange_payload_parsing`
- `contracts.runtime_support.data_module.normalization`
- `contracts.runtime_support.data_module.mock_data_generation`

Remaining data-module residuals:

- `contracts.runtime_support.data_module.historical_cache`
- `contracts.runtime_support.data_module.http_transport`
- `contracts.runtime_support.data_module.test_harness`
- parent facade closeout

## Selected Child Boundary

`root.contracts.runtime_support.data_module.historical_cache` owns historical replay kline cache loading, stale fallback, persistence, and historical raw kline fetch orchestration.

Candidate implementation surface in `qrpc_runtime/src/data_module.rs`:

- `HistoricalBarsCache`
- `historical_cache_path`
- `sanitize_filename_component`
- `load_historical_cache`
- `load_stale_historical_cache`
- `read_historical_cache_body`
- `persist_historical_cache`
- `shared_http_client`
- `fetch_historical_raw_klines`
- `historical_kline_bars_for_backtest`

## Why This Child Next

| Criterion | Judgment |
| --- | --- |
| Cohesion | Strong: path safety, cache read/write, stale fallback, historical fetch, and backtest replay bars form one historical cache owner. |
| Inputs | `DataSourceConfig`, `now_ms`, exchange endpoint helpers, parser helpers, normalization helper |
| Outputs | cached or fetched normalized historical kline bars |
| Risk | Medium: persistence, filesystem safety, quota checks, fsync, HTTP fallback, and stale cache fallback must be preserved. |
| Exclusions | request-interval in-memory cache, live provider fetch, mock generation, exchange parser ownership, normalization implementation, quality diagnostics, and tests remain outside this child. |
| Parent communication | Child must be reached through the `data_module` parent; no sibling horizontal link is allowed. |

## Leaf Split Decision Gate

`historical_cache` should continue to baseline/extraction because:

- it has a distinct persistence and replay-cache owner;
- it is separate from live HTTP transport and request interval cache;
- it has independent failure modes: path safety, stale cache, storage quota, atomic rename/fsync, historical fetch;
- extracting it reduces `data_module.rs` residual complexity while preserving parent facade compatibility.

Further split inside historical cache is not approved yet. After extraction, the child must run terminal leaf control before deciding whether path safety, cache persistence, fetch orchestration, and stale fallback should remain together.

## Equivalence Baseline For Next Step

Next baseline must preserve:

- cache path sanitization and bounded filename behavior;
- fresh cache load and stale cache fallback behavior;
- historical cache max-body and directory quota checks;
- temporary file write, rename, and fsync behavior;
- OKX/Binance historical raw kline fetch and parser selection;
- `historical_kline_bars_for_backtest` crate-visible compatibility;
- parent-owned normalization and exchange parser import boundaries.

Required gates for the next movement:

- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-001ZM-01 `root.contracts.runtime_support.data_module.historical_cache` baseline_plan.
