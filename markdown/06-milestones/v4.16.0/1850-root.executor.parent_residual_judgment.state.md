# v4.16.0 root.executor parent residual judgment selects state

> Batch: BE-002LC-01
> Node: `root.executor`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select next child:

`root.executor.state`

## Parent Residual Scan

Executor child queue from baseline:

- `executor.state`
- `executor.runner`

## Why This Child

`executor.state` owns executor state persistence, execution mode state, audit
append support, API guard, credential storage support, and the executor API
handlers that expose health/mode/params state surfaces. It should be handled
before runner movement so runner behavior continues to depend on a stable state
boundary.

## Proposed Ownership

| Field | Value |
| --- | --- |
| Candidate child | `executor.state` |
| Existing files | `src-executor/executor_state.rs`, `src-executor/audit_log.rs`, `src-executor/api_guard.rs`, `src-executor/credential_vault_v2.rs` |
| Planned movement | extract state route handlers from `src-executor/main.rs` into `src-executor/state_routes.rs` |
| Parent wrapper | route registration in `main.rs` |

## Exclusions

- Do not move strategy deployment/start/stop handlers.
- Do not move OKX demo order handlers.
- Do not move runner pool, migration API, websocket, kline buffer, or OKX REST adapters.
- Do not migrate backend/runtime state ownership.
- Do not introduce release-transition links.

## Next Step

BE-002LD-01 `root.executor.state` baseline_plan.
