# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation parent residual judgment selects event_usage_validation

> Batch: BE-001UL-01
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation` selects
`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation`
as the next child.

The selected child owns only graph event usage validation:
`V4MachineGraphContract::validate_event_catalog`. It does not own event catalog DTOs,
`MachineEventCatalog::validate_static_contract`, graph root validation orchestration,
DAG validation, risk-plane validation, graph schema, traversal helpers, or bridge code.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `event_usage_validation` is a named white-box child for graph-to-event-catalog usage rules. |
| parent_child_communication_kept | pass | The child can receive the all-machine map and use parent graph fields without lateral child calls. |
| equivalence_baseline_freezable | pass | The helper has finite event reference, catalog presence, emitter, consumer, and edge-party branches. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | The public graph validator remains in the parent, while event usage is a distinct private validation phase. |
| state_machine_phase | pass | Event usage validation is the event-routing phase of graph static validation. |
| strategy_branch | guarded | It protects emitted/consumed transition events and graph edge events used by downstream contract consumers. |
| independent_failure_mode | pass | Catalog missing, undeclared events, emitter/consumer mismatch, and edge-party mismatch fail independently from DAG/risk-plane checks. |
| reuse_pressure | guarded | Reuse remains through root validation, but the phase is large enough to isolate for review and later residual judgment. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | Event usage validation owns a full graph event contract phase. |
| communication_cost_rises | no | Moving the helper preserves parent-mediated input and does not require sibling-to-sibling calls. |
| local_proof_missing | no | Existing qrpc-core-ir tests cover missing catalog, undeclared event, and emitter/consumer failures. |
| line_count_only | no | Selection is based on behavior and failure modes, not size alone. |

leaf_split_decision_result

`continue_split: true`

Selected next child:

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation`

next_recursive_step

BE-001UM-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation baseline_plan
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation.rs`

**Markers**:
- `BE-001UL-01`

**Next step**:
BE-001UM-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
