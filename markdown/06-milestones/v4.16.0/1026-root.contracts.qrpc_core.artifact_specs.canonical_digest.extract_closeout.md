# v4.16.0 root.contracts.qrpc_core.artifact_specs.canonical_digest extract closeout

> Batch: BE-001RU-02
> Node: `root.contracts.qrpc_core.artifact_specs.canonical_digest`
> Parent: `root.contracts.qrpc_core.artifact_specs`
> Stage: `extract_closeout`
> Movement: Rust code extraction.

---

## Summary

`root.contracts.qrpc_core.artifact_specs.canonical_digest` has been extracted from `qrpc_core/src/artifact_specs.rs` into a private child module.

The artifact specs parent preserves the local public facade:

- `mod canonical_digest;`
- `pub use canonical_digest::*;`

The qrpc-core crate root remains unchanged and continues to preserve public exports through `pub use artifact_specs::*;`.

## Files Changed

- `qrpc_core/src/artifact_specs.rs`
- `qrpc_core/src/artifact_specs/canonical_digest.rs`

## Extraction Result

`qrpc_core/src/artifact_specs/canonical_digest.rs` now owns:

- `ArtifactDigestAlgorithm`;
- `ArtifactDigest`;
- `canonical_json_sha256_digest`.

`qrpc_core/src/artifact_specs.rs` keeps later artifact specs residuals in place:

- run/backtest modes;
- dataset and execution assumption projections;
- market data snapshot specs;
- `RunSpec` and `BacktestSpec`;
- strategy/core-IR/compile artifact DTOs;
- `CompileArtifactBundle`.

## Rust Local Fields

| Field | Result |
| --- | --- |
| Crate | `qrpc-core` |
| Parent facade | Preserved. `artifact_specs.rs` remains the local parent facade and re-export owner. |
| Child visibility | The module is private; public digest surfaces are preserved through `pub use canonical_digest::*;`. |
| Crate public exports | Preserved through the existing crate-root `pub use artifact_specs::*;`. |
| Parent-owned residuals | Run/backtest specs and artifact bundle DTOs remain parent-owned for later recursion. |
| Sibling dependency check | Passed. No run/backtest or artifact bundle child depends on digest internals directly. |

## Equivalence Preservation

| Surface | Preserved evidence |
| --- | --- |
| Algorithm enum | Variant and snake_case serde remain unchanged. |
| Digest DTO | Field names and derives remain unchanged. |
| Helper signature | Generic bound, parameter, and result type remain unchanged. |
| Serialization source | `serde_json::to_vec(value)?` remains unchanged. |
| Hash algorithm | `Sha256` hashing remains unchanged. |
| Output format | Lowercase hex formatting remains unchanged. |
| Public facade | Artifact specs and crate-root re-exports preserve public access. |

## Hard Boundaries Held

BE-001RU-02 did not:

- change digest algorithm variant names, serde rename behavior, digest DTO field names, helper signature, JSON serialization source, SHA-256 hashing, hex formatting, public exports, or tests;
- move run/backtest specs, artifact bundle DTOs, runtime IO DTOs, RFC execution contracts, or crate-root tests;
- change closed qrpc-core children or release transition.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-pre-commit-hook.ps1`

## Next Step

BE-001RU-03 `root.contracts.qrpc_core.artifact_specs.canonical_digest` single_leaf_closeout.
