# v4.16.0 root.contracts.qrpc_core.runtime_io_contract parent residual judgment selects portfolio_state

> Batch: BE-001SI-01
> Node: `root.contracts.qrpc_core.runtime_io_contract`
> Selected child: `root.contracts.qrpc_core.runtime_io_contract.portfolio_state`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core.runtime_io_contract` returns to its child queue after `execution_io` closeout.

Decision:

`next_child: root.contracts.qrpc_core.runtime_io_contract.portfolio_state`

## Closed Runtime IO Children

| Child | Result |
| --- | --- |
| `contracts.qrpc_core.runtime_io_contract.market_data_io` | Closed with `stop_split: true`. |
| `contracts.qrpc_core.runtime_io_contract.decision_flow` | Closed with `stop_split: true`. |
| `contracts.qrpc_core.runtime_io_contract.execution_io` | Closed with `stop_split: true`. |

## Open Runtime IO Residuals

| Residual | Status |
| --- | --- |
| `contracts.qrpc_core.runtime_io_contract.portfolio_state` | Selected next. Owns position, exchange exposure, portfolio state DTOs, and portfolio helper methods. |
| `contracts.qrpc_core.runtime_io_contract.runtime_output` | Queued. Owns runtime event, cycle output, and session output DTOs. |
| `contracts.qrpc_core.runtime_io_contract.backtest_output` | Queued. Owns backtest equity, metrics, summary, period return, and final output DTOs. |

## Selection Rationale

`portfolio_state` is selected because it is the next contiguous runtime IO family in `qrpc_core/src/runtime_io_contract.rs`:

- it owns `Position`;
- it owns `ExchangeExposure`;
- it owns `PortfolioState`;
- it owns `PortfolioState::new`;
- it owns `PortfolioState::debug_assert_invariants`;
- it is a compact portfolio/account schema boundary consumed by runtime output and downstream response mapping;
- it depends on `OpenOrder` through the parent facade after `execution_io` extraction.

## Same-Parent Parallel Judgment

`same_parent_parallel_children` remains inactive for this selection.

Reason: `PortfolioState` consumes the newly closed `OpenOrder` execution DTO and is also consumed by later `runtime_output` / `backtest_output` residuals. Serial extraction keeps the dependency chain parent-mediated and avoids mixing portfolio helper methods with output DTO ownership.

## Hard Boundaries

The next `portfolio_state` baseline must not:

- edit Rust source code;
- change portfolio field names, types, serde attributes, public methods, debug assertion behavior, or public exports;
- move `RuntimeEvent`, `RuntimeCycleOutput`, `SessionOutput`, backtest output DTOs, RFC execution contracts, or crate-root tests;
- reopen closed market data IO, decision flow, or execution IO children;
- change downstream runtime/backend/compiler behavior or release transition.

## Next Step

BE-001SJ-01 `root.contracts.qrpc_core.runtime_io_contract.portfolio_state` baseline_plan.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
