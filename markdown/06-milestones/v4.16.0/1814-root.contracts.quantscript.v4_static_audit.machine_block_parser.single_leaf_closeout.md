# v4.16.0 root.contracts.quantscript.v4_static_audit.machine_block_parser single leaf closeout

> Batch: BE-002KE-03
> Node: `root.contracts.quantscript.v4_static_audit.machine_block_parser`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`stop_split: true`

The extracted child is a terminal-sized cohesive machine-envelope parser. It
owns one parser function that validates a `machine` block, delegates nested
parsing through parent helpers, and assembles the final `V4MachineContract`.

## Leaf Granularity Evidence

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -LeafId root.contracts.quantscript.v4_static_audit.machine_block_parser -ParentId root.contracts.quantscript.v4_static_audit -Path quantscript\src\v4_static_audit\machine_block_parser.rs -Depth 4
```

Observed result:

| Field | Value |
| --- | --- |
| Split decision | STOP |
| Governance packaging | stop_split |
| Decision | STOP |
| Score | 34 |
| LOC | 177 |
| Functions | 1 |
| Public surface | 3 |
| Branches | 22 |
| Domains | 6 |
| Risk tags | cross_crate, lock, persistence, public_api, route_schema, state_machine |
| Size bucket | terminal_target_150_600 |

## Why No Further Split

- The leaf is inside the target terminal size range.
- The parser owns one cohesive machine envelope operation.
- State, memory, transition, edge, and risk-plane responsibilities remain
  separate parent-controlled candidates.
- Splitting header parsing, priority parsing, and contract assembly would create
  thin helper fragments with higher governance cost than ownership value.
- Parent-child communication remains explicit through the parent wrapper.

## Preserved Boundaries

- Public API unchanged.
- `static_document_parser` and nested state parsing continue through the parent
  `parse_machine_block` wrapper.
- `parse_state`, `parse_state_block`, `parse_memory`, `parse_transition`,
  `parse_edge`, and `parse_risk_plane` remain parent-owned.
- Public report DTOs and local static-audit tests remain parent-owned.
- No release-transition link was introduced.

## Gates

| Gate | Result |
| --- | --- |
| leaf granularity evaluator | PASS, STOP score 34 |
| `git diff --check` | PASS |
| `check-utf8.ps1` | PASS |
| `check-full-feature-tree.ps1` | PASS |
| `check-matrix-governance.ps1` | PASS |

## Next Step

BE-002KF-01 `root.contracts.quantscript.v4_static_audit`
parent_residual_judgment.
