# v4.16.0 root.contracts.runtime_support.v4_runtime_support.graph_symbol_expansion single leaf closeout

> Batch: BE-002DN-02
> Node: `root.contracts.runtime_support.v4_runtime_support.graph_symbol_expansion`
> Parent: `root.contracts.runtime_support.v4_runtime_support`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Decision

`stop_split: true`

The v4 graph symbol expansion child is terminal for the current recursive pass.

## Evidence

The extracted child `qrpc_runtime/src/v4_runtime/graph_symbol_expansion.rs` owns one coherent preprocessing responsibility:

- normalize v4 backtest symbols;
- expand machine ids for multi-symbol graphs;
- clone/prefix machine families;
- expand graph edges;
- expand event catalog parties;
- expand risk-plane machine ids;
- provide `symbol_for_machine_id` to the v4 parent for runtime projection.

Further splitting normalization, id formatting, event-party expansion, or risk-plane id expansion would create micro leaves with extra import churn and no stronger owner.

## Verified Gates

Inherited from BE-002DN-01:

- `cargo fmt --check`: PASS
- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime v4 -- --nocapture`: PASS, 32 passed
- `cargo check -p quantpilot`: PASS
- `git diff --check`: PASS
- UTF-8 check: PASS
- full feature tree check: PASS
- matrix governance check: PASS

## Next Step

BE-002DO-01 `root.contracts.runtime_support.v4_runtime_support` parent_residual_judgment.
