# v4.16.0 root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness extract closeout

> Batch: BE-002IA-02
> Node: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness`
> Parent: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface`
> Stage: `actual_extraction`
> Movement: Rust code moved.
> governance_next_authority: active

---

## Extraction Result

`integration_test_harness` is extracted.

Created:

- `quantscript/src/lowering/orchestrator/integration_test_harness.rs`

Updated:

- `quantscript/src/lowering/orchestrator.rs`

## Moved Owner

The new child owns:

- `MA_SCRIPT` and `RSI_SCRIPT` fixture constants;
- all 32 existing lowering integration tests previously colocated in the parent;
- test-only imports for parsing, runtime protocol compilation, runtime coordinator smoke, and qrpc-core assertion types.

## Parent Residual

`orchestrator.rs` now remains the parent owner for:

- public lowering entrypoints;
- normalization, universe expansion, typed HIR, semantic analysis, and diagnostics sequencing;
- profile detection child calls;
- data-source dedupe;
- binding collection;
- intent inference;
- agent/risk/runtime config assembly;
- the test module declaration only.

No production sibling horizontal link was introduced. The child test module reaches parent behavior through the parent module boundary.

## Validation

Passed:

- `cargo fmt`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`

Remaining closeout gates:

- `git diff --check`
- UTF-8
- full-feature-tree
- matrix governance

## Next Step

BE-002IA-03 single_leaf_closeout.
