# v4.16.0 root.contracts.runtime_support.v4_runtime_support.test_harness.payload_validation_tests baseline plan

> Batch: BE-002FD-01
> Node: `root.contracts.runtime_support.v4_runtime_support.test_harness.payload_validation_tests`
> Parent: `root.contracts.runtime_support.v4_runtime_support.test_harness`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Freeze `payload_validation_tests` as a test-only movement.

The next movement may move only malformed runtime input and simulated config rejection tests from `qrpc_runtime/src/v4_runtime/test_harness.rs` into a dedicated child file.

## Allowed Movement

Move only these tests:

| Test | Responsibility |
| --- | --- |
| `v4_runtime_rejects_event_payload_missing_required_catalog_field` | Required catalog payload field rejection. |
| `v4_runtime_rejects_event_payload_with_wrong_catalog_type` | Catalog payload type mismatch rejection. |
| `v4_runtime_rejects_unknown_external_input_payload_field` | Unknown external input payload field rejection. |
| `v4_runtime_input_event_denies_unknown_top_level_fields` | Serde deny-unknown-fields contract for runtime input event DTO. |
| `v4_runtime_rejects_non_finite_default_execution_costs` | Non-finite simulated execution fee/slippage config rejection. |

Parent module changes allowed:

- add `mod payload_validation_tests;`;
- leave all non-selected tests in `qrpc_runtime/src/v4_runtime/test_harness.rs`.

Child module requirements:

- use `use super::*;` to preserve parent-mediated test access;
- do not move fixture builders or production code;
- preserve test names and assertion bodies.

## Required Gates After Extraction

The extraction closeout must run:

1. `cargo fmt --check`
2. `cargo check -p qrpc-runtime`
3. `cargo test -p qrpc-runtime v4 -- --nocapture`
4. `cargo check -p quantpilot`
5. `git diff --check`
6. UTF-8, full feature tree, and matrix governance checks

## Stop Conditions

Stop before code movement if moving these tests requires:

1. editing assertions or payload literals;
2. moving replay, simulated execution, capability, risk, or live tests in the same step;
3. changing fixture builder visibility beyond the test harness parent;
4. changing production runtime code.

## Next Step

BE-002FE-01 `root.contracts.runtime_support.v4_runtime_support.test_harness.payload_validation_tests` actual_extraction.
