# v4.16.0 root.contracts.quantscript.v4_static_audit parent residual judgment selects edge_parser

> Batch: BE-002KL-01
> Node: `root.contracts.quantscript.v4_static_audit`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select next child:

`root.contracts.quantscript.v4_static_audit.edge_parser`

## Parent Residual Scan

Closed children:

- `runtime_handoff_builder`;
- `audit_entrypoint`;
- `capability_type_parser`;
- `event_catalog_derivation`;
- `parser_utilities_diagnostics`;
- `static_document_parser`;
- `machine_block_parser`;
- `state_block_parser`;
- `transition_parser`;
- `memory_parser`.

Remaining parent-owned clusters:

- state-group parsing;
- edge parsing;
- risk-plane parsing;
- machine template wrapper and thin capability/type wrappers;
- public DTOs and local static-audit tests.

## Why This Child

`edge_parser` owns the graph edge grammar:
`edge <source> -> <target> on <event>`.

This is a complete graph topology declaration rule and can move without
changing risk-plane parsing, machine-template parsing, DTO ownership, or test
ownership.

## Proposed Ownership

| Field | Value |
| --- | --- |
| Candidate child | `edge_parser` |
| Planned file | `quantscript/src/v4_static_audit/edge_parser.rs` |
| Likely owned functions | `parse_edge` |
| Parent wrapper | `parse_edge` |
| Parent bridge dependencies | `diag`, `split_words` |

## Exclusions For Next Baseline

- Do not move `parse_state_group`.
- Do not move `parse_risk_plane`.
- Do not move `parse_machine_template`.
- Do not move public report DTOs or local tests.
- Do not introduce release-transition links.

## Next Step

BE-002KM-01 `root.contracts.quantscript.v4_static_audit.edge_parser`
baseline_plan.
