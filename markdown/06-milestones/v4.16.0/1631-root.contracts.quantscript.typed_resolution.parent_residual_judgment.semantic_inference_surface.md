# v4.16.0 root.contracts.quantscript.typed_resolution parent residual judgment: semantic_inference_surface

> Batch: BE-002GG-01
> Node: `root.contracts.quantscript.typed_resolution`
> Selected child: `root.contracts.quantscript.typed_resolution.semantic_inference_surface`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `root.contracts.quantscript.typed_resolution.semantic_inference_surface` for the next recursive child.

## Current Parent State

Closed typed-resolution children:

- `root.contracts.quantscript.typed_resolution.public_type_surface`
- `root.contracts.quantscript.typed_resolution.callable_classification_surface`

Current parent file:

- `quantscript/src/resolve/mod.rs`

The parent still owns resolver orchestration, AST-to-HIR lowering, type inference, binding insertion, expression semantic inference, manual indicator formula inference, misc scalar/series helpers, and resolver-local tests.

## Residual Candidate Review

| Candidate | Evidence | Decision |
|-----------|----------|----------|
| `semantic_inference_surface` | Cohesive block from `infer_expr_semantic` through `is_ema_like_name`; owns expression semantics, manual MACD/MA/ZScore recognition, window aggregate views, boundary lookback pairs, smoothed change pairs, alias resolution, and series view shape helpers. | Select |
| `type_inference_binding_surface` | Clear owner around call/member/unary/binary/list/index/sequence inference and `insert_binding`, but it depends heavily on lowering methods and should follow semantic extraction. | Queue |
| `resolver_orchestration_surface` | Contains module/function/stmt/expr lowering and should remain parent until semantic and type helper pockets shrink. | Queue |
| `resolver_test_harness` | Test block is useful, but moving tests before behavior pockets would not reduce production parent complexity. | Queue |

## Selected Child Scope

The next baseline may freeze only the semantic inference pocket:

- `infer_expr_semantic`
- `infer_manual_indicator_formula`
- `infer_manual_macd_histogram_formula`
- `infer_manual_macd_line_formula`
- `infer_manual_macd_signal_formula`
- `infer_manual_macd_line_shape`
- `infer_signal_line_shape`
- `infer_ema_call`
- `infer_manual_moving_average_formula`
- `infer_manual_zscore_formula`
- `infer_window_aggregate_view`
- `infer_boundary_lookback_pair`
- `infer_balanced_smoothed_change_pair`
- `infer_smoothed_change_binding`
- `infer_change_source_call`
- `resolve_alias_expr`
- `infer_series_view_shape`
- `series_capability_target_expr`
- `window_aggregate_target_expr`
- `is_ema_like_name`

Likely child file:

- `quantscript/src/resolve/semantic_inference_surface.rs`

## Forbidden Movement

The next baseline and extraction must not move:

- public resolver DTOs already closed in `public_type_surface`;
- callable/helper classifier and registry seeding functions already closed in `callable_classification_surface`;
- `lower_script_to_typed_hir`;
- `resolve_module`, `resolve_function`, `lower_block`, `lower_stmt`, `lower_expr`, and `lower_callee_expr`;
- call/member/unary/binary/list/index/sequence type inference helpers;
- `insert_binding`, `validate_condition_type`, `resolve_type`;
- resolver ID allocation and expression label helpers;
- resolver-local tests, analysis, lowering, evaluator, runtime, backend, executor, frontend, or plugin metadata.

## Next Step

BE-002GH-01 `root.contracts.quantscript.typed_resolution.semantic_inference_surface` baseline_plan.
