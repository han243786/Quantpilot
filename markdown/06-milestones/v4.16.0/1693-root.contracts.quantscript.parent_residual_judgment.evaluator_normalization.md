# v4.16.0 root.contracts.quantscript parent residual judgment: evaluator_normalization

> Batch: BE-002HM-01
> Node: `root.contracts.quantscript`
> Selected child: `root.contracts.quantscript.evaluator_normalization`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `root.contracts.quantscript.evaluator_normalization` for the next recursive child.

## Parent Residual Scan

Current parent:

- `quantscript/src/lib.rs`
- `quantscript/src/evaluator.rs`
- `quantscript/src/legacy_config_compat.rs`
- `quantscript/src/test_plan.rs`
- `quantscript/src/v4_static_audit.rs`
- already modularized directories under `quantscript/src/analysis_diagnostics`, `quantscript/src/lowering`, `quantscript/src/resolve`, and `quantscript/src/syntax_ast_surface`

Closed children:

- `root.contracts.quantscript.syntax_ast_surface`
- `root.contracts.quantscript.legacy_config_compat`
- `root.contracts.quantscript.typed_resolution`
- `root.contracts.quantscript.analysis_diagnostics`

Queued residuals:

| Candidate | Boundary | Decision |
| --- | --- | --- |
| `evaluator_normalization` | `normalize_script_module`, expression normalization, helper function execution, constant/builtin folding, list/slice helpers, and evaluator tests in `quantscript/src/evaluator.rs`. | Select |
| `runtime_lowering` | Lowering pipeline and runtime config projection under `quantscript/src/lowering/`. | Queue |
| `v4_static_audit` | v4 static audit report, runtime handoff, capability/depth/memory checks. | Queue |
| `test_plan_surface` | Test plan extraction and test item split. | Queue |
| `authoring_docs_samples` | Authoring docs and sample surface, if still present in code/doc owner. | Queue |

## Selected Boundary

The next baseline should freeze `quantscript/src/evaluator.rs` as the current owner of evaluator normalization.

The selected child currently owns:

- public `normalize_script_module` entrypoint;
- module/function normalization;
- user-defined helper inlining;
- helper function execution for `if` / `for` / `while` / `match`;
- constant folding for unary, binary, index, slice, member builtin, identifier builtin, list and primitive expression helpers;
- evaluator-local tests.

## Why This Child

`evaluator_normalization` is the next best child because:

- it is the largest remaining single-file QuantScript residual after `analysis_diagnostics`;
- it has a stable public surface of one exported function: `normalize_script_module`;
- it is already isolated from parser, analysis diagnostics, typed resolution, and lowering except through public AST/result types;
- its internal structure has obvious second-level child candidates.

## Extraction Guard

The next movement must not change:

- `pub use evaluator::normalize_script_module` in `quantscript/src/lib.rs`;
- the public signature of `normalize_script_module`;
- `lowering::orchestrator` call order;
- existing `quantscript` tests or diagnostic semantics;
- already closed `analysis_diagnostics`, `typed_resolution`, `legacy_config_compat`, or `syntax_ast_surface` children.

If the next step converts `evaluator.rs` into an `evaluator/` module directory, it must preserve the crate-level module name `evaluator` and keep the public re-export unchanged.

## Next Step

BE-002HN-01 `root.contracts.quantscript.evaluator_normalization` baseline_plan.
