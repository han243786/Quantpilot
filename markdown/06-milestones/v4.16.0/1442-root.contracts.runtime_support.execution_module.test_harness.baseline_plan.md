# v4.16.0 root.contracts.runtime_support.execution_module.test_harness baseline plan

> Batch: BE-002AX-01
> Node: `root.contracts.runtime_support.execution_module.test_harness`
> Parent: `root.contracts.runtime_support.execution_module`
> Stage: `baseline_plan`
> Movement: No Rust code movement.

---

## Scope

This baseline freezes the test-only extraction for execution-module local tests.

Current owner file:

- `qrpc_runtime/src/execution_module.rs`

Planned child file:

- `qrpc_runtime/src/execution_module/test_harness.rs`

## Allowed Movement

The next movement may:

- add `#[cfg(test)] mod test_harness;` to `qrpc_runtime/src/execution_module.rs`;
- move only the current `#[cfg(test)] mod tests` body into `qrpc_runtime/src/execution_module/test_harness.rs`;
- keep test fixtures and assertions in the child;
- preserve `use super::*` so tests access parent-owned production helpers without public API widening.

The next movement must not move:

- public request/output DTOs or provider traits;
- `BuiltinExecutionModule`;
- order construction helpers;
- execution semantics and payload helpers;
- market/portfolio helper functions;
- fill-engine, risk, data, or runtime-facade behavior.

## Equivalence Requirements

The extraction must preserve:

- action-order planning tests;
- portfolio-target order planning tests;
- fill-engine submission/on-market-update delegation tests;
- execution payload semantics tests.

## Gates

Required for the next movement:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime execution_module`
- `cargo check -p quantpilot`
- `git diff --check`
- `tools/check-utf8.ps1`
- `tools/check-full-feature-tree.ps1`
- `tools/check-matrix-governance.ps1`

## Next Step

BE-002AX-02 `root.contracts.runtime_support.execution_module.test_harness` actual_extraction.
