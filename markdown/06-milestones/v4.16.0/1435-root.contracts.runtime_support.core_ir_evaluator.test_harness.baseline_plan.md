# v4.16.0 root.contracts.runtime_support.core_ir_evaluator.test_harness baseline plan

> Batch: BE-002AS-01
> Node: `root.contracts.runtime_support.core_ir_evaluator.test_harness`
> Parent: `root.contracts.runtime_support.core_ir_evaluator`
> Stage: `baseline_plan`
> Movement: No Rust code movement.

---

## Scope

This baseline freezes the test-only extraction for Core IR evaluator local tests.

Current owner file:

- `qrpc_runtime/src/core_ir_evaluator.rs`

Planned child file:

- `qrpc_runtime/src/core_ir_evaluator/test_harness.rs`

## Allowed Movement

The next movement may:

- add `#[cfg(test)] mod test_harness;` to `qrpc_runtime/src/core_ir_evaluator.rs`;
- move only the current `#[cfg(test)] mod tests` body into `qrpc_runtime/src/core_ir_evaluator/test_harness.rs`;
- keep test-only imports and fixtures inside the child;
- preserve `use super::*` so tests access parent-owned helpers without public API widening.

The next movement must not move:

- public evaluator DTOs or error enum;
- `IndicatorEvaluator`, `indicator_registry`, or `evaluate_indicator_signal`;
- closed evaluator children;
- parent-owned lookup/math helpers;
- production behavior.

## Preserved Boundaries

| Boundary | Requirement |
| --- | --- |
| Production boundary | No production evaluator behavior moves. |
| Parent mediation | Tests access helpers through the parent module only. |
| Public API | No helper or evaluator surface is promoted. |
| Release transition | No horizontal shortcut or performance bypass is proposed. |

## Equivalence Requirements

The extraction must preserve:

- Spread and Custom evaluator tests;
- advanced/classic formula helper smoke tests;
- registry integration coverage through `evaluate_indicator_signal`;
- `cargo test -p qrpc-runtime core_ir_evaluator` result count and pass status.

## Gates

Required for the next movement:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime core_ir_evaluator`
- `cargo check -p quantpilot`
- `git diff --check`
- `tools/check-utf8.ps1`
- `tools/check-full-feature-tree.ps1`
- `tools/check-matrix-governance.ps1`

## Next Step

BE-002AS-02 `root.contracts.runtime_support.core_ir_evaluator.test_harness` actual_extraction.
