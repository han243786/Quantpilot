# v4.16.0 root.contracts.quantscript.analysis_diagnostics.unsupported_construct_gate extract closeout

> Batch: BE-002GU-02
> Node: `root.contracts.quantscript.analysis_diagnostics.unsupported_construct_gate`
> Parent: `root.contracts.quantscript.analysis_diagnostics`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> governance_next_authority: active

---

## Result

`root.contracts.quantscript.analysis_diagnostics.unsupported_construct_gate` actual extraction is complete.

New child:

- `quantscript/src/analysis_diagnostics/unsupported_construct_gate.rs`

Parent wiring:

- `quantscript/src/analysis_diagnostics/mod.rs` declares `mod unsupported_construct_gate;`.
- `quantscript/src/analysis_diagnostics/analysis.rs` imports `collect_unsupported_construct_diagnostics` from the child.
- `analyze_script_module` keeps the same diagnostic invocation order by extending diagnostics from the child at the original call site.

## Moved Surface

Moved into the child:

- `collect_unsupported_construct_diagnostics`
- `collect_unsupported_constructs_from_stmts`
- `collect_unsupported_constructs_from_expr`
- `is_supported_formal_try_target`
- `function_contains_direct_recursion`
- `stmts_contain_direct_recursion`
- `expr_contains_direct_recursion`
- `collect_non_universe_for_loop_diagnostics`
- `collect_non_universe_for_loop_diagnostics_from_hir_stmts`

## Preserved Boundary

Preserved:

- `ScriptAnalysis` and `analyze_script_module` public behavior;
- diagnostic family ordering in `analyze_script_module`;
- diagnostic codes `QS0601` through `QS0610`, message text, hints, spans, and severity;
- `ResolveResult` and `TypedHirModule` based unsupported-loop behavior;
- crate-root public exports and `crate::analysis` / `crate::diagnostics` compatibility;
- series index, centered window, warmup, fetch lookback, strategy fetch/emit, data-source bounds, dead-code emit, and fetch symbol whitelist helpers outside this child.

Forbidden movement remained frozen: evaluator normalization, runtime lowering, v4 static audit, test plans, samples, backend, executor, frontend, plugin metadata, and release transition were not touched.

## Validation

Executed for this extraction:

- `cargo fmt --check` PASS
- `cargo test -p quantscript` PASS, 88 tests
- `cargo check -p quantpilot` PASS
- `git diff --check` PASS
- UTF-8 check PASS
- full feature tree check PASS
- matrix governance check PASS

## Next Step

BE-002GU-03 `root.contracts.quantscript.analysis_diagnostics.unsupported_construct_gate` single_leaf_closeout.
