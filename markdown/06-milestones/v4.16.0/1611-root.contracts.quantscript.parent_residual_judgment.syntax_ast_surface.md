# v4.16.0 root.contracts.quantscript parent residual judgment selects syntax_ast_surface

> Batch: BE-002FW-01
> Node: `root.contracts.quantscript`
> Selected child: `root.contracts.quantscript.syntax_ast_surface`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Parent State

The BE-002FV-01 baseline froze the QuantScript child queue:

- `syntax_ast_surface`
- `legacy_config_compat`
- `typed_resolution`
- `analysis_diagnostics`
- `evaluator_normalization`
- `runtime_lowering`
- `v4_static_audit`
- `test_plan_surface`
- `authoring_docs_samples`

## Decision

Select `root.contracts.quantscript.syntax_ast_surface` as the first QuantScript child.

This child should own the formal QS syntax boundary and the public AST/type/HIR DTO surface required by downstream resolution, analysis, lowering, test-plan extraction, backend compile routes, and test runner behavior.

## Candidate Physical Surface

- `quantscript/src/script.rs`
- `quantscript/src/types.rs`
- `quantscript/src/hir.rs`
- parser/type/HIR public re-exports in `quantscript/src/lib.rs`

## Public Boundary To Freeze Next

The next baseline must preserve:

- `parse_quant_script_module`
- `parse_expr`
- `ScriptModule`, `Item`, `FunctionDecl`, `ImportDecl`, `ImportName`, `Param`
- `Stmt`, `Expr`, `BinaryOp`, `UnaryOp`, `CallArg`
- `TestBlock`, `StepBlock`, `TestAction`, `TestParamValue`
- `Type`, `TypeArena`, `TypeId`, `parse_type_annotation`
- `TypedHirModule`, `Hir*`, `DefId`, and `ExprId`

## Why This Child First

| Criterion | Judgment |
| --- | --- |
| Queue order | It is the first child in the frozen QuantScript baseline. |
| Dependency direction | Parser/AST/HIR/type surface feeds resolve, analysis, lowering, test plans, diagnostics, and backend callers. |
| Risk control | Freezing syntax before moving typed resolution or lowering reduces semantic drift. |
| Granularity | Medium. It avoids token/expr/stmt micro-splitting while isolating the language entry surface. |

## Exclusions

Do not move or change:

- legacy config-style parser and compatibility compile helpers in `lib.rs`;
- typed resolution behavior in `resolve.rs`;
- analysis, evaluator, lowering, v4 static audit, test-plan logic, docs/samples, backend, runtime, executor, frontend, or release-transition behavior.

## Next Step

BE-002FX-01 `root.contracts.quantscript.syntax_ast_surface` baseline_plan.
