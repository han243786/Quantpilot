# v4.16.0 root.contracts.runtime_support.core_ir_evaluator.spread_custom_expression_wave single leaf closeout

> Batch: BE-002AO-03
> Node: `root.contracts.runtime_support.core_ir_evaluator.spread_custom_expression_wave`
> Parent: `root.contracts.runtime_support.core_ir_evaluator`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Decision

`stop_split: true`

`root.contracts.runtime_support.core_ir_evaluator.spread_custom_expression_wave` is closed as a terminal expression-evaluation wave.

## Leaf Granularity Evidence

The read-only leaf tool returned:

```text
Leaf: root.contracts.runtime_support.core_ir_evaluator.spread_custom_expression_wave
Decision: STOP
Score: 59
Metrics: loc=628, functions=24, public_surface=2, branches=54, domains=4
Terminal leaf control: size_bucket=split_pressure_601_800, governance_mode=stop_split
Recommended action: Set stop_split: true unless developer supplies stronger ownership evidence.
```

## Closeout Rationale

The child is slightly above the target terminal range but still cohesive:

- Custom and Spread share `SeriesExpr` materialization, field projection, alignment, aggregation, scope discovery, and reference-price helpers;
- splitting Custom and typed Spread now would create sibling-helper pressure or force parent wrappers around tightly shared expression machinery;
- only `evaluate_custom` and `evaluate_spread` are parent-visible, so the public surface is already narrow;
- the parent keeps registry dispatch and shared lookup/math helpers, preserving parent-mediated communication.

No stronger same-parent wave evidence exists for a smaller internal split in this pass.

## Preserved Boundaries

| Boundary | Preserved Rule |
| --- | --- |
| Registry boundary | Builtin registry dispatch remains parent-owned in `core_ir_evaluator.rs`. |
| Helper boundary | Shared lookup/math helpers remain parent-owned; child does not call sibling modules. |
| QuantScript boundary | Parser/lowering/static audit semantics remain outside this runtime evaluator leaf. |
| Release transition | No horizontal shortcut or performance bypass is proposed. |

## Evidence

The preceding extraction commit passed:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime core_ir_evaluator`: 22 passed
- `cargo check -p quantpilot`
- `git diff --check`
- `tools/check-utf8.ps1`
- `tools/check-full-feature-tree.ps1`
- `tools/check-matrix-governance.ps1`
- `tools/run-smart-pre-commit.ps1`

## Next Step

BE-002AP-01 `root.contracts.runtime_support.core_ir_evaluator` parent_residual_judgment selects the next queued child, expected `shared_lookup_math_helpers`.
