# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation.event_party_validation equivalence baseline and extraction plan

> Batch: BE-001UO-01
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation.event_party_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation`
> Stage: `baseline_plan`
> Movement: No code movement.

---

## Summary

This baseline freezes event party permission validation before extraction.

The selected child may own only the transition/action/edge emitter and consumer
permission checks currently inside `V4MachineGraphContract::validate_event_catalog`.

It must not own referenced event discovery, event catalog presence checks, local catalog
validation, undeclared event checks, event catalog DTOs, graph validation orchestration,
or risk-plane/DAG validation.

## Frozen Validation Surface

The next extraction must preserve:

- transition consumer allowed-party validation;
- transition source allowed-emitter validation;
- action emitted-event allowed-emitter validation;
- edge source allowed-emitter validation;
- edge target allowed-consumer validation;
- skip behavior when a transition/action/edge event spec is missing;
- skip behavior when edge source/target endpoints are unknown;
- `machine_event_party_allowed` semantics;
- all error strings and branch order.

## Allowed BE-001UO-02 Movement

The next extraction step may:

- create `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/event_usage_validation/event_party_validation.rs`;
- add `mod event_party_validation;` to `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/event_usage_validation.rs`;
- move the transition/action/edge party-check loops into a child helper;
- keep `validate_event_catalog` as the parent orchestration entry;
- call the child helper after event specs are built and undeclared-event checks have run.

The child may depend on:

- `std::collections::BTreeMap`;
- `machine_event_party_allowed`;
- `MachineEventTypeSpec`;
- `V4MachineContract`;
- `V4MachineGraphContract`.

## Forbidden BE-001UO-02 Movement

The next extraction step must not move or edit behavior for:

- referenced event collection from machine transitions/actions/edges;
- event catalog presence requirement;
- `MachineEventCatalog::validate_static_contract` delegation;
- event spec map construction;
- undeclared event rejection;
- event catalog DTOs;
- graph validation parent orchestration;
- DAG or risk-plane validation;
- tests.

It must not alter validation branch order, error strings, event lookup behavior,
or unknown-edge endpoint skip behavior.

## Equivalence Proof Plan

BE-001UO-02 must pass:

- `cargo fmt --check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/event_usage_validation.rs`

**Planned child file**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/event_usage_validation/event_party_validation.rs`

**Markers**:
- `BE-001UO-01`

**Next step**:
BE-001UO-02 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation.event_party_validation extract_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
