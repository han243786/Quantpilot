# v4.16.0 root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_macd_formula extract closeout

> Batch: BE-002JK-02
> Node: `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_macd_formula`
> Stage: `actual_extraction`
> Movement: Rust code movement complete.
> governance_next_authority: active

---

## Extraction Result

`quantscript/src/lowering/fallback/manual_macd_formula.rs` now owns manual MACD fallback recognition:

- direct `ResolvedManualIndicatorFormula::MacdLine` handling;
- direct `ResolvedManualIndicatorFormula::MacdHistogram` handling;
- EMA spread fallback recognition for MACD line;
- MACD line/signal pair fallback recognition for histogram;
- source and fast/slow/signal period validation.

The parent `quantscript/src/lowering/fallback.rs` keeps the stable public(crate) surface:

- `manual_macd_line_from_expr` delegates to the child implementation;
- `manual_macd_histogram_from_expr` delegates to the child implementation;
- subtract-branch recognizer order remains MACD histogram, MACD line, momentum, momentum-ratio.

## Boundary Preservation

| Contract | Result |
| --- | --- |
| Recognizer order | Preserved in `resolve_manual_formula_binding` |
| Public(crate) wrappers | Preserved in the parent module |
| Non-MACD fallback owners | Not moved |
| Trading semantics | Preserved by existing MACD fallback tests and full `quantscript` test suite |
| Parent/child communication | Parent delegates through `mod manual_macd_formula`; child helpers remain private |

## Verification

| Gate | Result |
| --- | --- |
| `cargo fmt --all --check` | PASS |
| `cargo test -p quantscript` | PASS, 88 tests |
| `cargo check -p quantpilot` | PASS, no warnings |

Additional governance gates are run before commit for diff whitespace, UTF-8, full feature tree, and matrix governance.

## Next Step

BE-002JK-03 `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_macd_formula` single_leaf_closeout.
