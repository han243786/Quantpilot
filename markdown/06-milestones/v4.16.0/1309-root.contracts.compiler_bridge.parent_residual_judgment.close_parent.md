# v4.16.0 root.contracts.compiler_bridge parent residual judgment closes parent

> Batch: BE-001YB-01
> Node: `root.contracts.compiler_bridge`
> Stage: `parent_residual_judgment`
> Movement: No code movement.
> Decision: `parent_closeout: true` — all three children are closed; remaining parent-owned surface is a cohesive public facade.

---

## Summary

`root.contracts.compiler_bridge` is closed as a compact parent facade. All three children have completed closeout:

| Child | Status |
| --- | --- |
| `contracts.compiler_bridge.runtime_protocol_validation` | Closed with `stop_split: true`. |
| `contracts.compiler_bridge.runtime_protocol_lowering` | Closed as parent facade over `intent_signal_lowering`, `agent_policy_lowering`, `risk_policy_lowering`. |
| `contracts.compiler_bridge.strategy_ir_lowering` | Closed with `stop_split: true` (BE-001YA-02). |

## Parent-Owned Surface

| Surface | Detail | Reason to keep in parent |
| --- | --- | --- |
| Public API facades (6 functions) | `validate_runtime_protocol_config`, `compile_runtime_protocol_config`, `compile_runtime_protocol_config_with_metadata`, `lower_runtime_protocol_to_core_ir`, `lower_runtime_protocol_to_core_ir_with_metadata`, `lower_strategy_ir_to_core_ir` | External API boundary. All are thin delegations (1–10 lines). Extracting would create micro-leaves without independent owner. |
| Shared helpers (~12 functions) | `parse_strategy_signal_compare`, `strategy_indicator_usize_param`, `lower_rebalance_schedule`, `runtime_intent_is_spread`, `lower_runtime_spread_spec`, `build_spread_spec`, `build_spread_series_expr`, `param_with_fallback`, `decode_series_field`, `decode_series_aggregation`, `decode_align_direction`, `decode_spread_output` | Used by both `runtime_protocol_lowering` and `strategy_ir_lowering`. Moving to either child would force sibling horizontal access. Kept in parent where both children can access via `use super::`. |
| Tests module | `#[cfg(test)] mod tests` (28 tests) | Tests both runtime protocol and strategy IR lowering from parent scope. |

## Parent-Child Communication

```text
qrpc_compiler::lib (parent facade, now closed)
  ├── pub fn validate_runtime_protocol_config(...) → child validation
  ├── pub fn compile_runtime_protocol_config(...) → child lowering
  ├── pub fn lower_strategy_ir_to_core_ir(...) → child lowering
  ├── fn parse_strategy_signal_compare(...)   ← shared, used by both children
  ├── fn build_spread_spec(...)                ← shared
  ├── fn decode_* helpers                      ← shared
  ├── mod runtime_protocol_validation (child, closed)
  ├── mod runtime_protocol_lowering (child, closed)
  │     ├── mod intent_signal_lowering (closed)
  │     ├── mod agent_policy_lowering (closed)
  │     └── mod risk_policy_lowering (closed)
  └── mod strategy_ir_lowering (child, closed, stop_split: true)
```

No sibling horizontal links. All children access shared helpers via `use super::`.

## Leaf Split Decision Gate (Parent Level)

### leaf_split_base_gate

| Rule | Result | Detail |
| --- | --- | --- |
| `white_box_boundary_named` | fail | No residual child can be clearly named. Public facades are thin delegations; shared helpers lack independent owner. |
| `parent_child_communication_kept` | n/a | No candidate child exists. |
| `equivalence_baseline_freezable` | n/a | No candidate code movement. |

### leaf_split_positive_trigger

None triggered. All three original children are closed. No new behavior pockets have emerged in the parent facade.

### leaf_split_stop_condition

| Condition | Result |
| --- | --- |
| `micro_leaf_without_owner` | **Triggered** — extracting public facades or shared helpers would create micro-leaves (1–10 line delegations; helper-only fragments). |
| `communication_cost_rises` | **Triggered** — extracting shared helpers would force sibling horizontal access or require a new shared module that both children import. |
| `local_proof_missing` | Not triggered — 28 tests prove equivalence from parent scope. |
| `line_count_only` | Not triggered — parent is ~260 lines (compact). |

### leaf_split_decision_result

**`parent_closeout: true`**

### next_recursive_step

Return to `root.contracts` parent residual judgment. Next candidate: `contracts.runtime_support`, `contracts.quantscript`, `contracts.plugin_metadata`, or `executor` (from `open_residuals`).

## Hard Boundaries

1. Do not extract public API facades — they are the external boundary of the compiler bridge.
2. Do not extract shared helpers — they are used by multiple children and belong in the parent.
3. Do not introduce sibling horizontal links between any children.
4. Do not propose release transition.
5. Do not move tests into child modules.

## Next Step

BE-001YC-01 `root.contracts` parent_residual_judgment.

## Gates

- `cargo test -p qrpc-compiler`: 28 passed, 0 failed
- `cargo check -p quantpilot`: pass
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
