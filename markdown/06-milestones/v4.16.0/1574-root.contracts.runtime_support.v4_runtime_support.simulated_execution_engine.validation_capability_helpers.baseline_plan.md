# v4.16.0 simulated_execution_engine.validation_capability_helpers baseline plan

> Batch: BE-002ET-01
> Node: `root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine.validation_capability_helpers`
> Parent: `root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Freeze `validation_capability_helpers` as a same-parent child movement candidate.

The next movement may extract validation, admission, capability derivation, conditional-order helper classification, fill-price, and marketability helpers while preserving the existing sibling call shape through parent-level re-exports.

## Allowed Movement

The actual extraction may add `qrpc_runtime/src/v4_runtime/simulated_execution_engine/validation_capability_helpers.rs` and move only these helpers from `qrpc_runtime/src/v4_simulated_execution.rs`:

| Method/helper | Visibility after move | Reason |
| --- | --- | --- |
| `validate_position_action` | `pub(super)` method | Parent direct-submit and fill/accounting child both validate position actions. |
| `pre_execution_rejection_reason` | `pub(super)` method | Parent direct-submit and fill/accounting child both use pre-execution rejection checks. |
| `non_executable_resting_reason` | `pub(super)` method | Parent direct-submit orchestration admits resting orders through this helper. |
| `V4SimulatedPositionAction::side` | `pub(super)` method | Parent and children derive order side from action. |
| `V4SimulatedPositionAction::is_reducing` | `pub(super)` method | Market trigger request conversion keeps reduce-only semantics. |
| `is_conditional_order_type` | parent re-exported free helper | Market trigger flow classifies conditional orders through the parent boundary. |
| `conditional_order_execution_type` | parent re-exported free helper | Market trigger flow converts triggered order types through the parent boundary. |
| `conditional_order_is_triggered` | parent re-exported free helper | Market trigger flow evaluates trigger direction through the parent boundary. |
| `validate_simulated_execution_config` | parent re-exported free helper | Runtime adapter validates simulated config while staying under simulated execution parent ownership. |
| `validate_simulated_order_request` | parent re-exported free helper | Parent direct-submit and fill/accounting child share request validation. |
| `simulated_order_required_capabilities` | parent re-exported free helper | Runtime adapter capability gate derives required capabilities. |
| `compute_simulated_fill_price` | parent re-exported free helper | Parent direct-submit and fill/accounting child share fill price math. |
| `limit_order_is_marketable` | parent re-exported free helper | Parent direct-submit, validation helper, and market trigger flow share marketability checks. |

Parent module re-export requirement:

```rust
pub(super) use validation_capability_helpers::{
    conditional_order_execution_type,
    conditional_order_is_triggered,
    compute_simulated_fill_price,
    is_conditional_order_type,
    limit_order_is_marketable,
    simulated_order_required_capabilities,
    validate_simulated_execution_config,
    validate_simulated_order_request,
};
```

The exact re-export list may include only the helpers required by compile, but sibling modules must continue to import through `use super::*`; no sibling may call the child module path directly.

## Frozen Parent-Owned Surface

The next movement must leave the following outside `validation_capability_helpers`:

- `submit_order` and direct-submit orchestration;
- payload parsing helpers: `payload_string`, `payload_f64`, `payload_u64`, `payload_bool`, `metadata_string`, parse helpers, and `normalize_token`;
- snapshot and metrics projection already closed in `snapshot_metrics_projection`;
- fill execution and ledger mutation already closed in `fill_ledger_accounting`;
- market trigger flow already closed in `market_trigger_flow`;
- order lifecycle internals already closed in `order_lifecycle_flow`;
- runtime adapter behavior already closed in `runtime_adapter`;
- v4 tests.

## Local Invariants

| Invariant | Requirement |
| --- | --- |
| API freeze | No public API, route, event schema, persistence owner, or runtime mode change |
| Validation semantics | Error text, validation order, and capability set derivation remain unchanged |
| Parent mediation | Free helpers are re-exported through the simulated execution parent boundary |
| No horizontal bypass | Sibling children must not import `validation_capability_helpers` directly |
| Release guard | No release-transition optimization, caching shortcut, or hot-path horizontal connection |

## Evidence Baseline

Current parent residual evaluation with same-parent wave candidate:

- `split_decision`: `STOP`
- `governance_packaging`: `same_parent_wave`
- `final_decision`: `WAVE`
- Score: `50`
- LOC: `536`
- Functions: `24`
- Public surface: `2`
- Branches: `75`
- Risk tags: `persistence`, `public_api`, `route_schema`, `security`, `state_machine`, `trading_semantics`

## Required Gates After Extraction

The extraction closeout must run:

1. `cargo fmt --check`
2. `cargo check -p qrpc-runtime`
3. `cargo test -p qrpc-runtime v4 -- --nocapture`
4. `cargo check -p quantpilot`
5. `git diff --check`
6. UTF-8, full feature tree, and matrix governance checks

## Stop Conditions

Stop before code movement if moving the allowed helpers requires:

1. moving `submit_order` wholesale;
2. moving payload parsing into the same child;
3. changing validation error text, capability sets, conditional trigger semantics, marketability, or fill price math;
4. adding direct child-to-child imports;
5. moving tests instead of preserving behavior;
6. changing runtime mode, persistence owner, route/schema, or public API.

## Next Step

BE-002EU-01 `root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine.validation_capability_helpers` actual_extraction.
