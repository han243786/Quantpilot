# v4.16.0 root.executor.runner.provider_order_routes extract closeout

> Batch: BE-002LH-02
> Node: `root.executor.runner.provider_order_routes`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> governance_next_authority: active

---

## Movement

OKX demo provider order route handlers have been extracted from
`src-executor/main.rs` into:

`src-executor/provider_order_routes.rs`

The parent keeps route registration and the provider helper surface for this
first movement.

## Ownership Now

| Field | Value |
| --- | --- |
| Child file | `src-executor/provider_order_routes.rs` |
| Owned handlers | `submit_okx_demo_order`, `query_okx_demo_order`, `cancel_okx_demo_order` |
| Parent retained | `OkxDemo*` request DTOs, provider mode gate, credential loading, request cleaning, audit payload helpers, `append_audit` |
| Parent route registration | unchanged except handler path qualification |

## Preserved Behavior

- OKX demo order submit route behavior is unchanged.
- OKX demo order query route behavior is unchanged.
- OKX demo order cancel route behavior is unchanged.
- Paper-actual mode gating remains parent helper mediated.
- Credential lookup and OKX REST call behavior are unchanged.
- Audit redaction and provider response shape are unchanged.

## Gates

| Gate | Result |
| --- | --- |
| `cargo fmt --all --check` | PASS |
| `cargo check -p quantpilot --bin executor` | PASS |
| `git diff --check` | PASS |

## Next Step

BE-002LH-03 `root.executor.runner.provider_order_routes`
single_leaf_closeout.
