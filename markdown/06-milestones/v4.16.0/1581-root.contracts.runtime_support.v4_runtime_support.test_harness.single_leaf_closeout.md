# v4.16.0 root.contracts.runtime_support.v4_runtime_support.test_harness single leaf closeout

> Batch: BE-002EY-02
> Node: `root.contracts.runtime_support.v4_runtime_support.test_harness`
> Parent: `root.contracts.runtime_support.v4_runtime_support`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`continue_split: true`

The extracted test harness is behaviorally equivalent, but it remains an oversized precision leaf: `1294` LOC and `40` functions. Unlike smaller test-only harnesses, this one contains multiple independently named fixture and scenario families, so the next recursive step should inspect its internal ownership before declaring it terminal.

## Leaf Granularity Evidence

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -Path qrpc_runtime\src\v4_runtime\test_harness.rs
```

Result:

| Field | Value |
| --- | --- |
| `split_decision` | `CONTINUE` |
| `governance_packaging` | `precision_single_leaf` |
| `final_decision` | `PRECISION` |
| Score | `52` |
| LOC | `1294` |
| Functions | `40` |
| Public/internal-visible surface | `0` |
| Branches | `9` |
| Domains | `11` |
| Risk tags | `compiler_contract`, `cross_crate`, `lock`, `persistence`, `route_schema`, `security`, `state_machine`, `trading_semantics` |
| Terminal size bucket | `oversized_over_800` |

## Why Not Stop Here

Stopping here would preserve equivalence but leave the v4 runtime support module tree with a very large terminal test leaf. The test file has natural internal seams:

- sample Core IR and compatibility graph fixtures;
- capability matrix fixtures;
- replay and multi-symbol behavior tests;
- payload rejection tests;
- simulated execution scenario tests;
- live-actual and capability/risk guard tests.

The next step should decide whether any of these deserve a small number of same-parent test-only children. This must remain test-only; no production movement is authorized by this closeout.

## Preserved Boundary

The BE-002EY-01 extraction remains valid:

- all 32 v4 tests pass under `v4_runtime::test_harness::*`;
- production v4 runtime behavior is unchanged;
- parent-mediated access through `use super::*` is preserved;
- no release-transition optimization, route/schema change, persistence owner change, runtime mode change, or production code movement was introduced.

## Evidence

Inherited from BE-002EY-01 extraction:

- `cargo fmt --check`: PASS
- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime v4 -- --nocapture`: PASS, 32 tests
- `cargo check -p quantpilot`: PASS

This closeout is docs-only and updates the recursive cursor plus governance indexes.

## Next Step

BE-002EZ-01 `root.contracts.runtime_support.v4_runtime_support.test_harness` parent_residual_judgment.
