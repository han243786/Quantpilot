# v4.16.0 root.contracts.core_ir.v4_contracts.qs_state_machine_profile single leaf closeout stops split

> Batch: BE-001VE-03
> Node: `root.contracts.core_ir.v4_contracts.qs_state_machine_profile`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.qs_state_machine_profile` stops splitting after extracting the complete QS
state-machine profile contract family.

The child owns:

- seven QS profile/policy structs
- their default implementations
- public `default_v4_qs_state_machine_profile`
- private serde default helpers
- `QsStateMachineProfile::validate_static_contract`

The policy structs are cohesive because all validation branches are evaluated through one profile entrypoint and
share the same schema/version/default contract.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `qs_state_machine_profile` names the complete QS profile contract family. |
| parent_child_communication_kept | pass | The v4 parent re-exports the child while the child imports only shared constants/types. |
| equivalence_baseline_freezable | pass | BE-001VE-01 froze defaults and validation branch behavior. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | Public profile types remain available through `qrpc_core_ir::v4`. |
| state_machine_phase | pass | The child owns QS state-machine profile policy. |
| strategy_branch | guarded | Policy families are separate fields but one profile contract. |
| independent_failure_mode | guarded | Validation branches are independent but share one public profile entrypoint. |
| reuse_pressure | guarded | Tests and bundle defaults use the profile through the v4 surface. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | yes | Splitting each policy struct now would create small leaves without separate public owners. |
| communication_cost_rises | yes | Further split would route one profile validation across many policy files for little gain. |
| local_proof_missing | no | `cargo test -p qrpc-core-ir` and commit-time `cargo check -p quantpilot` passed after extraction. |
| line_count_only | no | The stop decision is based on cohesive contract-family ownership. |

leaf_split_decision_result

`stop_split: true`

This leaf is complete. Return to `root.contracts.core_ir.v4_contracts` for parent residual judgment.

next_recursive_step

BE-001VF-01 root.contracts.core_ir.v4_contracts parent_residual_judgment
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/qs_state_machine_profile.rs`

**Markers**:
- `BE-001VE-03`

**Next step**:
BE-001VF-01 root.contracts.core_ir.v4_contracts parent_residual_judgment

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
