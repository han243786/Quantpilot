# v4.16.0 root.contracts.runtime_support.risk_support.direction_cross_constraints baseline plan

> Batch: BE-002BU-01
> Node: `root.contracts.runtime_support.risk_support.direction_cross_constraints`
> Parent: `root.contracts.runtime_support.risk_support`
> Stage: `baseline_plan`
> Movement: No Rust code movement.

---

## Scope

Freeze the baseline for extracting direction and cross-symbol risk constraint passes.

Allowed movement:

- add `qrpc_runtime/src/risk_checker/direction_cross_constraints.rs`;
- add a parent child declaration in `qrpc_runtime/src/risk_checker.rs`;
- move only the selected post-processing helpers;
- keep helper visibility parent-mediated with `pub(super)` where needed.

## In-Scope Helpers

- `apply_direction_conflict_check`
- `apply_cross_symbol_constraints`

## Out of Scope

- provider trait and public evaluate surface;
- `evaluate_risk_decision`;
- `enforce_risk_mode`;
- event payload projection child;
- action clamp helpers;
- portfolio target clamp helpers;
- exposure math helpers;
- tests.

## Dependency Rule

The child may use `super::*` to access parent-owned types and exposure math helpers. It must not import sibling children directly and must not introduce a release-transition shortcut.

## Encoding Watchpoint

The source region contains historical mojibake comments that sit on the same physical line as executable code. During actual extraction, local comment normalization is allowed only to keep code visible to the formatter/compiler and to preserve existing behavior. No risk decision status, reason code, event payload key, scaling formula, or rejection/clamping condition may change.

## Equivalence Baseline

The next actual extraction must preserve:

- direction-conflict rejection semantics;
- cross-symbol leverage scaling semantics;
- emitted event payload keys;
- risk decision ordering and count;
- provider/evaluate public surface.

## Gates

Required gates for actual extraction:

- `cargo fmt`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime risk -- --nocapture`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 / full feature tree / matrix governance

## Next Step

BE-002BV-01 `root.contracts.runtime_support.risk_support.direction_cross_constraints` actual_extraction.
