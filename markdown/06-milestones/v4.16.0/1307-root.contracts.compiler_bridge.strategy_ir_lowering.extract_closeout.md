# v4.16.0 root.contracts.compiler_bridge.strategy_ir_lowering extract closeout

> Batch: BE-001YA-01
> Node: `root.contracts.compiler_bridge.strategy_ir_lowering`
> Stage: `extract_closeout` (lightweight: extraction record + single leaf closeout merged)
> Movement: Code movement — Strategy IR lowering functions extracted from `qrpc_compiler/src/lib.rs` into `qrpc_compiler/src/strategy_ir_lowering.rs`.

---

## Extraction Record

### What was moved

Created `qrpc_compiler/src/strategy_ir_lowering.rs` containing 16 cohesive functions:

| Function | Visibility | Description |
| --- | --- | --- |
| `lower_strategy_ir_to_core_ir` | `pub(super)` | Strategy IR → Core IR entry point |
| `lower_strategy_indicator_kind` | private | Indicator kind enum mapping (18 variants) |
| `lower_logic_action` | private | Logic action → signal kind |
| `lower_strategy_logic_rule` | private | Logic rule → signal rule |
| `lower_strategy_logic_condition` | private | Condition dispatch (MaCross/RSI/Momentum/ZScore/Spread) |
| `lower_strategy_spread_threshold_condition` | private | Spread threshold with BPS gate |
| `lower_strategy_ma_cross_condition` | private | MA cross → structured series compare |
| `lower_strategy_indicator_threshold_condition` | private | Indicator threshold → structured compare |
| `lower_strategy_spread_spec` | private | Strategy spread spec builder |
| `lower_strategy_custom_expr` | private | Custom expression lowering |
| `validate_custom_expr_spec` | private | Custom expr schema/version validation |
| `validate_custom_value_expr` | private | Value expression recursive validation |
| `validate_custom_data_id` | private | Data ID reference validation |
| `validate_window_agg_field` | private | Window aggregation field validation |
| `parse_time_in_force` | private | Time-in-force string parsing |
| `known_or_unknown_f64` | private | KnownOrUnknown<f64> helper |

### What changed in lib.rs

1. Added private `mod strategy_ir_lowering;`
2. `lower_strategy_ir_to_core_ir` → thin delegation to `strategy_ir_lowering::lower_strategy_ir_to_core_ir`
3. Removed all 16 extracted functions
4. Kept shared helpers in place: `parse_strategy_signal_compare`, `strategy_indicator_usize_param`, `lower_rebalance_schedule`, `runtime_intent_is_spread`, `lower_runtime_spread_spec`, `build_spread_spec`, `build_spread_series_expr`, `param_with_fallback`, decode functions
5. Tests module unchanged (accesses moved functions through `use super::*` imports)

### Parent-child communication

```text
qrpc_compiler::lib (parent)
  ├── pub fn lower_strategy_ir_to_core_ir() → delegates → strategy_ir_lowering
  ├── parent-private shared helpers (used by both children via use super::)
  ├── mod runtime_protocol_validation (child, closed)
  ├── mod runtime_protocol_lowering (child, closed)
  └── mod strategy_ir_lowering (new child)
        └── uses super::{build_spread_spec, lower_rebalance_schedule,
              parse_strategy_signal_compare, strategy_indicator_usize_param}
```

No sibling horizontal link.

### Equivalence proof

- `cargo test -p qrpc-compiler`: **28 passed, 0 failed**
- `cargo check -p quantpilot`: **pass** (downstream crates compile)
- Public API unchanged: `lower_strategy_ir_to_core_ir(&StrategyIr) -> Result<CoreStrategyIr>`
- External callers (`src/graph_api.rs`, `src/compile_api.rs`) unaffected
- Error codes preserved: `CUSTOM001`–`CUSTOM011`, `QPSTRATSPREAD001`–`QPSTRATSPREAD004`

## Leaf Split Decision Gate

### leaf_split_base_gate

| Rule | Result | Detail |
| --- | --- | --- |
| `white_box_boundary_named` | pass | Child owns Strategy IR→Core IR lowering; shared helpers remain in parent. |
| `parent_child_communication_kept` | pass | Parent delegates; child accesses shared helpers via `use super::`. |
| `equivalence_baseline_freezable` | pass | 28 tests pass; public API unchanged. |

### leaf_split_positive_trigger

| Trigger | Detail |
| --- | --- |
| `public_or_handler_boundary` | `lower_strategy_ir_to_core_ir` is the public entry point. |
| `strategy_branch` | Contains independent indicator kind, logic rule, spread, and custom expression lowering branches. |

### leaf_split_stop_condition

| Condition | Result |
| --- | --- |
| `micro_leaf_without_owner` | Not triggered — the child has a single owner and 16 cohesive functions. |
| `communication_cost_rises` | Not triggered — imports from super are limited to 4 shared helpers. |
| `local_proof_missing` | Not triggered — 28 tests prove equivalence. |
| `line_count_only` | Not triggered. |

### leaf_split_decision_result

`stop_split: true` — the extracted child is cohesive; further subdivision into individual indicator/condition lowering helpers would create micro-leaves without independent owners and increase communication cost.

### next_recursive_step

Return to `root.contracts.compiler_bridge` parent residual judgment.

## Next Step

BE-001YB-01 `root.contracts.compiler_bridge` parent residual judgment.

## Gates

- `cargo test -p qrpc-compiler`: 28 passed, 0 failed
- `cargo check -p quantpilot`: pass
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
