# v4.16.0 root.contracts.runtime_support.agent_module parent residual judgment selects portfolio_rebalance

> Batch: BE-001ZY-01
> Node: `root.contracts.runtime_support.agent_module`
> Stage: `parent_residual_judgment`
> Selected child: `root.contracts.runtime_support.agent_module.portfolio_rebalance`
> Movement: No Rust code movement.

---

## Parent State

Closed agent-module children:

- `contracts.runtime_support.agent_module.weighted_signal_decisions`

Remaining agent-module queue from the frozen baseline:

- `contracts.runtime_support.agent_module.portfolio_rebalance`
- `contracts.runtime_support.agent_module.cross_venue_arbitrage`
- `contracts.runtime_support.agent_module.shared_scoring_portfolio_helpers`
- `contracts.runtime_support.agent_module.test_harness`

## Selected Child Boundary

`portfolio_rebalance` owns slow-cycle portfolio rebalance policy behavior that converts selected signals, explicit rebalance universe, current portfolio state, and allocation configuration into a portfolio target decision.

Candidate physical surface:

- current owner: `qrpc_runtime/src/agent_module.rs`
- planned child module: `qrpc_runtime/src/agent_module/portfolio_rebalance.rs`

Candidate functions/types:

- `portfolio_rebalance_due`
- `RebalanceSymbolPlan`
- `build_portfolio_rebalance_decision`
- `assign_target_weights`

Candidate parent/shared helper dependencies:

- `build_signal_kind_index`
- `signal_score`
- `current_position_ratio`
- constants `MIN_QUANTITY_RATIO` and `DEFAULT_DECISION_THRESHOLD`

## Why This Child Next

| Criterion | Judgment |
| --- | --- |
| Queue order | It follows the closed `weighted_signal_decisions` child in the frozen agent-module queue. |
| Cohesion | Strong: one policy family focused on portfolio target generation. |
| Size pressure | High enough: this branch contains cadence, universe selection, current-weight projection, allocation methods, and target DTO assembly. |
| Inputs | Agent policy, selected intent signals, Core IR signal rules, portfolio state, timestamp, trace id, and last rebalance timestamps |
| Outputs | One portfolio-target `AgentDecision` plus evaluated rebalance agent id handled by the parent dispatch |
| Risk | High: allocation weights, explicit universe exits, cadence gates, and current-weight calculation must remain equivalent. |
| Exclusions | Weighted signal direct actions, cross-venue arbitrage, event emission, provider dispatch, and tests stay outside this child. |

## Equivalence Baseline For Next Step

Next baseline must preserve:

- `EverySlow`, `Every1d`, `Weekly`, and missing schedule cadence behavior;
- non-observe signal filtering;
- explicit universe versus signal-derived universe selection;
- selected-symbol threshold behavior;
- exchange and reference price fallback from signals and current positions;
- current weight projection;
- equal weight, fixed weights, rank weight, and score weight allocation behavior;
- zero-delta suppression;
- target DTO shape, reason string, net side, net strength, timestamp, and trace id;
- existing tests for equal/fixed/rank/score weighting, universe-member sell target, and daily/weekly cadence.

Required gates for the next movement:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-001ZZ-01 `root.contracts.runtime_support.agent_module.portfolio_rebalance` baseline_plan.
