# v4.16.0 root.contracts.quantscript.v4_static_audit.state_group_parser single leaf closeout

> Batch: BE-002KQ-03
> Node: `root.contracts.quantscript.v4_static_audit.state_group_parser`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`stop_split: true`

The extracted child is a micro state-group parser leaf. It owns one flat
state-group grammar rule, member expansion, default conflict policy, and
`StateGroup` assembly. Further split would fragment one cohesive state-machine
declaration.

## Leaf Granularity Evidence

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -LeafId root.contracts.quantscript.v4_static_audit.state_group_parser -ParentId root.contracts.quantscript.v4_static_audit -Path quantscript\src\v4_static_audit\state_group_parser.rs -Depth 4
```

Observed result:

| Field | Value |
| --- | --- |
| Split decision | STOP |
| Governance packaging | stop_split |
| Decision | STOP |
| Score | 25 |
| LOC | 29 |
| Functions | 1 |
| Public surface | 1 |
| Branches | 2 |
| Domains | 3 |
| Risk tags | cross_crate, public_api, state_machine |
| Size bucket | micro_under_100 |

## Why No Further Split

- The child is under 100 LOC.
- There is only one parent-facing function.
- State-group syntax, state member expansion, default conflict policy, and
  output assembly are one state-machine rule.
- Machine-template parsing remains the only parser residual in the parent.
- Parent-child communication remains explicit through the parent wrapper.

## Preserved Boundaries

- Public API unchanged.
- `machine_block_parser` continues through the parent wrapper.
- `parse_machine_template` remains parent-owned.
- Public report DTOs and local static-audit tests remain parent-owned.
- No release-transition link was introduced.

## Gates

| Gate | Result |
| --- | --- |
| leaf granularity evaluator | PASS, STOP score 25 |
| `cargo fmt --all --check` | PASS |
| `cargo test -p quantscript` | PASS, 88 tests |
| `cargo check -p quantpilot` | PASS |
| `git diff --check` | PASS |

## Next Step

BE-002KR-01 `root.contracts.quantscript.v4_static_audit`
parent_residual_judgment.
