# v4.16.0 root.contracts.qrpc_core.test_fixture baseline plan

> Batch: BE-001TD-01
> Node: `root.contracts.qrpc_core.test_fixture`
> Parent: `root.contracts.qrpc_core`
> Stage: `baseline_plan`
> Movement: No code movement.

---

## Summary

This baseline freezes the qrpc-core crate-root test fixture before extraction from `qrpc_core/src/lib.rs`.

The next implementation step may move only the cfg-test helper and tests into `qrpc_core/src/tests.rs`. The production crate facade must remain unchanged except for replacing the inline `#[cfg(test)] mod tests { ... }` block with `#[cfg(test)] mod tests;`.

## Frozen Surface

The selected child owns:

- `sample_runtime_protocol`;
- `canonical_digest_is_stable_for_equivalent_payloads`;
- `run_and_backtest_specs_capture_protocol_boundary`;
- the cfg-test `BTreeMap` import required by the helper.

## Equivalence Baseline

| Surface | Frozen behavior |
| --- | --- |
| Helper shape | `sample_runtime_protocol` continues returning the same `RuntimeProtocolCoreConfig`. |
| Data source fixture | Exchange, symbol, market type, data kind, days, interval, ping flags, request interval, and enabled fields remain unchanged. |
| Intent fixture | Intent ID, name, kind, input data IDs, params, and enabled flag remain unchanged. |
| Agent fixture | Agent ID, name, input intents, rebalance defaults, params, and enabled flag remain unchanged. |
| Risk fixture | Risk IDs, observed agents, ratio/leverage/cost fields, action interval, and enabled flag remain unchanged. |
| Initial cash and fees | `initial_cash_balance`, `taker_fee_bps`, `default_slippage_bps`, and `total_cost_buffer_bps` remain unchanged. |
| Digest regression | Equivalent JSON payloads still produce equal canonical SHA256 digests and the expected algorithm. |
| Run/backtest regression | Run spec, snapshot spec, and backtest spec assertions remain unchanged. |
| Production facade | Public module declarations and `pub use` exports remain unchanged. |

## Planned Extraction Shape

BE-001TD-02 may:

- create `qrpc_core/src/tests.rs`;
- move the full cfg-test module body into that file;
- move `#[cfg(test)] use std::collections::BTreeMap;` into the test file;
- replace the inline block in `qrpc_core/src/lib.rs` with `#[cfg(test)] mod tests;`;
- keep `use super::*;` in the test file to preserve the same crate-root test view.

## Rust Local Fields

| Field | Baseline |
| --- | --- |
| Crate | `qrpc-core` |
| Parent facade | `qrpc_core/src/lib.rs` should become production facade plus cfg-test module declaration. |
| Child visibility | `tests.rs` remains cfg-test only and private to crate-root tests. |
| Production behavior | No production behavior or public export may change. |
| Test behavior | `cargo test -p qrpc-core` must keep the same 12 passing tests. |

## Same-Parent Parallel Wave Judgment

`same_parent_parallel_children` remains inactive for BE-001TD-02.

Reason: this is the only remaining qrpc-core residual before parent closeout.

## Hard Boundaries

BE-001TD-02 must not:

- change helper data, assertions, public exports, production module declarations, serde shapes, digest behavior, spec behavior, or runtime behavior;
- reopen closed qrpc-core child modules;
- change `qrpc_core_ir`, `qrpc_compiler`, `qrpc_runtime`, `quantscript`, backend, executor, frontend, physical `plugins/*`, or release transition.

## Split Rule Pre-Check

| Rule | Baseline result |
| --- | --- |
| Physical owner | Worth extracting. Test fixture is the last non-facade parent residual. |
| Public surface | Not public API, but blocks parent facade closeout. |
| Mixed responsibility | Likely stop after extraction. Helper and two tests are one local regression fixture. |
| State ownership | No mutable lifecycle state. |
| Side effects | None beyond test assertions. |
| Parent-mediated dependency | Required. Tests keep using `super::*` from the crate root. |
| Future reopen rule | Required after extraction to decide whether helper and assertions should stay together. |

## Next Step

BE-001TD-02 `root.contracts.qrpc_core.test_fixture` extract_closeout.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
