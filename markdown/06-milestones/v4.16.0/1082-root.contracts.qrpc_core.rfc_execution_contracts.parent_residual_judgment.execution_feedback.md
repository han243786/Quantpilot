# v4.16.0 root.contracts.qrpc_core.rfc_execution_contracts parent residual judgment selects execution_feedback

> Batch: BE-001SX-01
> Node: `root.contracts.qrpc_core.rfc_execution_contracts`
> Parent: `root.contracts.qrpc_core`
> Selected child: `root.contracts.qrpc_core.rfc_execution_contracts.execution_feedback`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core.rfc_execution_contracts` returns to its child queue after `order_contract` closeout.

Decision:

`next_child: root.contracts.qrpc_core.rfc_execution_contracts.execution_feedback`

## Closed RFC Execution Children

| Child | Result |
| --- | --- |
| `contracts.qrpc_core.rfc_execution_contracts.data_request` | Closed with `stop_split: true`. |
| `contracts.qrpc_core.rfc_execution_contracts.allocation` | Closed with `stop_split: true`. |
| `contracts.qrpc_core.rfc_execution_contracts.order_contract` | Closed with `stop_split: true`. |

## Open RFC Execution Residuals

| Residual | Status |
| --- | --- |
| `contracts.qrpc_core.rfc_execution_contracts.execution_feedback` | Selected next. Owns execution feedback event DTOs and feedback kind taxonomy. |
| `contracts.qrpc_core.rfc_execution_contracts.handoff_snapshot` | Queued. Owns handoff snapshot DTO and completeness validation. |

## Selection Rationale

`execution_feedback` is selected because it is the next contiguous RFC execution family in `qrpc_core/src/rfc_execution_contracts.rs` after closed `order_contract`:

- `FeedbackKind`;
- `ExecutionFeedback`.

This child is a compact event contract. It depends on order identity by value only, and does not own order transition behavior, handoff state, persistence, locks, or runtime lifecycle.

## White Box Boundary

| Item | Owner | Input | Output | Side effects |
| --- | --- | --- | --- | --- |
| Feedback kind taxonomy | `execution_feedback` | venue/runtime execution outcome class | feedback enum schema | none |
| Execution feedback DTO | `execution_feedback` | order ID, feedback kind, optional fill/reject/venue fields, timestamps | execution feedback event schema | none |

## Same-Parent Parallel Judgment

`same_parent_parallel_children` remains inactive for this selection.

Reason: `execution_feedback` is tiny and adjacent to `handoff_snapshot`, but it should be isolated first because handoff snapshots embed open orders while feedback events only reference order IDs.

## Hard Boundaries

The next `execution_feedback` baseline must not:

- edit Rust source code;
- move handoff snapshot contracts;
- change enum variants, struct fields, derives, serde shapes, public exports, or tests;
- reopen closed qrpc-core children, `data_request`, `allocation`, or `order_contract`;
- change `qrpc_core_ir`, `qrpc_compiler`, `qrpc_runtime`, `quantscript`, backend, executor, frontend, physical `plugins/*`, or release transition;
- introduce direct sibling shortcuts.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `execution_feedback` owns `FeedbackKind` and `ExecutionFeedback`. |
| parent_child_communication_kept | pass | Future child exports remain mediated through the RFC execution parent facade and crate root. |
| equivalence_baseline_freezable | pass | Enum variants, optional fields, timestamps, derives, and serde shape can be frozen. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | pass | Execution feedback is public qrpc-core contract API. |
| state_machine_phase | no | It is an immutable event DTO family, not a mutable runtime state machine. |
| strategy_branch | no | No strategy branch is selected. |
| independent_failure_mode | pass | Feedback schema drift is independent from order transition drift and handoff validation. |
| reuse_pressure | pass | Runtime execution paths can consume these names through `qrpc_core::*`. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The selected child has taxonomy plus event schema ownership. |
| communication_cost_rises | no | Parent facade re-export preserves current consumers. |
| local_proof_missing | no | qrpc-core compile/test gates cover the move. |
| line_count_only | no | Split is by feedback event ownership, not raw line count. |

leaf_split_decision_result

`continue_baseline: true`; `execution_feedback` is worth selecting as the next RFC execution child.

## Next Step

BE-001SY-01 `root.contracts.qrpc_core.rfc_execution_contracts.execution_feedback` baseline_plan.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
