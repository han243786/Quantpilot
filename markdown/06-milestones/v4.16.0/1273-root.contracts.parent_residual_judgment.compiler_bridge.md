# v4.16.0 root.contracts parent residual judgment selects contracts.compiler_bridge

> Batch: BE-001WY-01
> Node: `root.contracts`
> Parent: `root`
> Stage: `parent_residual_judgment`
> Movement: no Rust code movement; parent queue selection only.

---

## Summary

`root.contracts` selects `contracts.compiler_bridge` as the next Rust-facing contracts child.

The previously queued contract children `contracts.api_surface`, `contracts.qrpc_core`, and `contracts.core_ir` are closed. The remaining contracts residual queue is `contracts.compiler_bridge`, `contracts.runtime_support`, `contracts.quantscript`, and `contracts.plugin_metadata`; `executor` remains queued at the root Rust level.

`contracts.compiler_bridge` currently maps to `qrpc_compiler/src/lib.rs`. This file owns runtime protocol validation, runtime-protocol-to-Core-IR lowering, Strategy-IR-to-Core-IR lowering, canonical config hashing, and compiler helper validation. No code movement is allowed in this selection step; the next step must freeze its baseline and split plan.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | Selected child is `root.contracts.compiler_bridge`. |
| parent_child_communication_kept | pass | Selection keeps compiler bridge under `root.contracts`; no sibling shortcut or release-transition connection is introduced. |
| equivalence_baseline_freezable | pass | `qrpc_compiler/src/lib.rs` is unchanged and can be frozen in BE-001WZ-01. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | true | Public compiler functions include validation, compile, and lowering entrypoints. |
| state_machine_phase | true | The bridge lowers runtime/strategy contract inputs into Core IR execution/data/agent/risk phases. |
| strategy_branch | true | Runtime protocol and Strategy IR lowering are distinct semantic branches inside one file. |
| independent_failure_mode | true | Validation, lowering, hashing, and helper conversion can fail independently. |
| reuse_pressure | true | Compiler bridge helpers are called by downstream runtime/build flows and should gain white-box ownership. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | false | The selected child is a substantial contract bridge, not a helper-only micro leaf. |
| communication_cost_rises | false | Baseline first will preserve parent ownership before any split. |
| local_proof_missing | false | Existing Rust checks can validate no movement in this selection step. |
| line_count_only | false | Selection is based on public APIs and semantic ownership, not only the file's line count. |

leaf_split_decision_result

`selected: contracts.compiler_bridge`.

Next recursive step: BE-001WZ-01 freezes the `root.contracts.compiler_bridge` baseline and proposes its first child split. No Rust code movement is allowed before that baseline.

next_recursive_step

BE-001WZ-01 root.contracts.compiler_bridge baseline_plan
## Boundary

**Real files**:
- `qrpc_compiler/src/lib.rs`

**Markers**:
- `BE-001WY-01`
- `root.contracts selected contracts.compiler_bridge`
- `compiler bridge source remains frozen`
- `next compiler bridge baseline`

**Next step**:
BE-001WZ-01 root.contracts.compiler_bridge baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
