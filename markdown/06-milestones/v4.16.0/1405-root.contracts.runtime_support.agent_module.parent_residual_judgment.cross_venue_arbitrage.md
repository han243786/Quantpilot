# v4.16.0 root.contracts.runtime_support.agent_module parent residual judgment selects cross_venue_arbitrage

> Batch: BE-002AA-01
> Node: `root.contracts.runtime_support.agent_module`
> Stage: `parent_residual_judgment`
> Selected child: `root.contracts.runtime_support.agent_module.cross_venue_arbitrage`
> Movement: No Rust code movement.

---

## Parent State

Closed agent-module children:

- `contracts.runtime_support.agent_module.weighted_signal_decisions`
- `contracts.runtime_support.agent_module.portfolio_rebalance`

Remaining agent-module queue from the frozen baseline:

- `contracts.runtime_support.agent_module.cross_venue_arbitrage`
- `contracts.runtime_support.agent_module.shared_scoring_portfolio_helpers`
- `contracts.runtime_support.agent_module.test_harness`

## Selected Child Boundary

`cross_venue_arbitrage` owns fast-cycle cross-venue spread decision behavior that converts quote observe signals or explicit spread observe signals into paired buy/sell proposed actions.

Candidate physical surface:

- current owner: `qrpc_runtime/src/agent_module.rs`
- planned child module: `qrpc_runtime/src/agent_module/cross_venue_arbitrage.rs`

Candidate functions:

- `build_arb_agent_decision`
- `build_arb_decision_from_spread_signal`
- `total_cost_buffer_ratio`

Candidate parent/shared helper dependencies:

- `available_position_ratio`
- constant `SPREAD_MULTIPLIER`

## Why This Child Next

| Criterion | Judgment |
| --- | --- |
| Queue order | It follows the closed `portfolio_rebalance` child in the frozen agent-module queue. |
| Cohesion | Strong: one policy family focused on cross-venue arbitrage decisions. |
| Cycle boundary | It is the only fast-cycle agent policy path remaining in the parent. |
| Inputs | Agent policy, related intent signals, Core IR execution cost settings, portfolio state, timestamp, and trace id |
| Outputs | One paired-leg `AgentDecision` plus parent-owned event emission |
| Risk | Medium: spread trigger, cost buffer, exchange leg selection, and sell-side availability clamp must remain equivalent. |
| Exclusions | Weighted signal decisions, portfolio rebalance, shared scoring/portfolio helper ownership, provider dispatch, event emission, and tests stay outside this child. |

## Equivalence Baseline For Next Step

Next baseline must preserve:

- quote-observe path using Binance and OKX reference prices;
- explicit spread-signal path using `spread_ratio`, `buy_mid_price`, and `sell_mid_price`;
- default and policy-provided spread trigger behavior;
- total cost buffer calculation from taker fee, slippage, and configured cost buffer;
- buy/sell exchange selection by lower and higher mid price;
- target symbol fallback to `Symbol::BtcUsdt`;
- max quantity clamp, spread multiplier, and available sell-position clamp;
- zero/invalid quantity suppression;
- proposed action tags `arb_buy_leg` and `arb_sell_leg`;
- reason string, net side, net strength, timestamp, and trace id shape;
- existing tests for fast-cycle quote arbitrage and explicit spread-signal arbitrage.

Required gates for the next movement:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-002AB-01 `root.contracts.runtime_support.agent_module.cross_venue_arbitrage` baseline_plan.
