# v4.16.0 root.executor.runner.runner_support_files.ws_client baseline plan

> Batch: BE-002MS-01
> Node: `root.executor.runner.runner_support_files.ws_client`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Frozen Boundary

`ws_client` is already physically isolated as:

`src-executor/ws_client.rs`

The leaf owns websocket event types, test-only websocket pool helpers, OKX
subscribe message construction, OKX ticker/kline parsing, public feed symbol
configuration, proxy-aware websocket connection setup, market dispatch, public
feed orchestration, Binance test-only parsers, and local tests.

## Leaf Granularity Gate

The terminal leaf evaluator returned `STOP` with score 49 for the whole
`src-executor/ws_client.rs` file.

| Field | Value |
| --- | --- |
| LOC | 588 |
| Functions | 27 |
| Governance packaging | `stop_split` |
| Size bucket | `terminal_target_150_600` |
| Risk tags | `compiler_contract`, `cross_crate`, `public_api`, `security`, `state_machine` |

## Decision

No internal extraction is approved in this step. `ws_client` remains a terminal
support-file leaf unless future evidence identifies a stronger ownership split.

## Required Gates

| Gate | Required |
| --- | --- |
| `cargo check -p quantpilot --bin executor` | Yes |
| `git diff --check` | Yes |
| Full feature tree | Yes |
| Matrix governance | Yes |

## Next Step

BE-002MS-02
`root.executor.runner.runner_support_files.ws_client`
single_leaf_closeout.
