# v4.16.0 runtime_facade_coordinator.config_generation baseline plan

> Batch: BE-001YP-01
> Node: `root.contracts.runtime_support.runtime_facade_coordinator.config_generation`
> Parent: `root.contracts.runtime_support.runtime_facade_coordinator`
> Stage: `baseline_plan`
> Movement: No code movement.

## Boundary

This child owns the public config generation surface on `RuntimeCoordinator`.

Planned extracted file:

- `qrpc_runtime/src/runtime_facade_coordinator/config_generation.rs`

Planned moved methods:

- `RuntimeCoordinator::swap_module_config`
- `RuntimeCoordinator::apply_pending_module_configs`
- `RuntimeCoordinator::current_generation`
- `RuntimeCoordinator::generation_history`

## White-Box Node

Inputs:

- `module_key`
- module config `serde_json::Value`
- pending config map
- applied deployment revision list
- config generation counter
- config generation history mutex

Outputs:

- deterministic deployment revision string
- activated deployment revision list
- current config generation
- cloned generation history entries

Internal behavior to preserve:

- deterministic revision generation through `qrpc_core::canonical_json_sha256_digest`
- bounded applied deployment revision retention
- generation counter increment only when pending configs exist
- bounded config generation history retention
- `rev-unknown` fallback when no deployment revision exists

## Exclusions

Do not move in this batch:

- constructor/provider wiring
- session/cycle orchestration
- execution/market entrypoints
- state/provider/risk accessors
- provider delegation helper implementations
- portfolio projection helpers
- coordinator tests

## Equivalence Plan

The public API remains `RuntimeCoordinator::*`.

The parent module will declare `mod config_generation;` and the child will implement `impl RuntimeCoordinator`.

Required gates:

- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-001YP-02 `root.contracts.runtime_support.runtime_facade_coordinator.config_generation` actual_extraction.
