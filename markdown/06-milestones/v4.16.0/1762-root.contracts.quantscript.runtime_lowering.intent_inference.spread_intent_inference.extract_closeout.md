# v4.16.0 root.contracts.quantscript.runtime_lowering.intent_inference.spread_intent_inference extract closeout

> Batch: BE-002JB-02
> Node: `root.contracts.quantscript.runtime_lowering.intent_inference.spread_intent_inference`
> Parent: `root.contracts.quantscript.runtime_lowering.intent_inference`
> Stage: `actual_extraction`
> Movement: Production Rust movement.
> governance_next_authority: active

---

## Extraction Summary

`spread_intent_inference` has been extracted into:

- `quantscript/src/lowering/intents/spread_intent_inference.rs`

The parent `quantscript/src/lowering/intents.rs` keeps the `spread_intent_from_condition` wrapper and delegates formal spread matching, operand decoding, spread params, and resample/align parsing into the child module.

## Symbols Moved

Moved into the child:

- `SpreadSeriesOperand`
- `SpreadOutputKind`
- `SpreadMatch`
- `spread_intent_from_condition` implementation body
- `match_formal_admitted_spread_expr`
- `decode_spread_operand`
- `decode_spread_operand_or_source`
- `decode_explicit_spread_operand`
- `decode_formal_admitted_spread_operand`
- `default_spread_operand`
- `parse_spread_output_kind`
- `parse_field_code`
- `parse_aggregation_code`
- `parse_align_direction_code`
- `parse_resample_period_ms`
- `merge_optional_f64`
- `spread_params`
- `inferred_resample_period_ms`
- `interval_to_ms`

The child entry remains `pub(super)` and returns the same optional `IntentConfig` / `anyhow::Error` surface as the parent-owned wrapper.

## Parent Boundary Kept

The parent remains owner for:

- `canonicalize_data_sources`
- `canonicalize_data_source_for_bindings`
- `inferred_agent_params`
- `infer_intents` public(crate) facade
- `intents_from_condition`
- `single_indicator_intent`
- `moving_average_ratio_intent`
- `moving_average_ratio_binding`
- `legacy_intent_from_emit`
- `merge_intent`
- relation/id helpers
- structured comparison keys and comparison helpers

## Equivalence Notes

The extraction preserves:

- greater-than / greater-equal gating for spread conditions;
- unsupported or incomplete `spread(...)` expressions returning no intent;
- same-data-id and different-symbol rejection behavior;
- `QuoteObserve` intent id, name, input ids, params, and enabled flag;
- field, resample, align, aggregation, tolerance, and inferred interval numeric encodings;
- parent-mediated communication without sibling horizontal links.

## Validation

Passed after extraction:

- `cargo fmt --check`
- `cargo test -p quantscript` (88 tests)
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8, full feature tree, and matrix governance checks

## Next Step

BE-002JB-03 single_leaf_closeout.
