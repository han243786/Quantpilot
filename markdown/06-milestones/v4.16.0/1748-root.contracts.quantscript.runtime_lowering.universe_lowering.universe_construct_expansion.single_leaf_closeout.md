# v4.16.0 root.contracts.quantscript.runtime_lowering.universe_lowering.universe_construct_expansion single leaf closeout

> Batch: BE-002IS-03
> Node: `root.contracts.quantscript.runtime_lowering.universe_lowering.universe_construct_expansion`
> Parent: `root.contracts.quantscript.runtime_lowering.universe_lowering`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Closeout Decision

`universe_construct_expansion` is closed as a terminal child and sets `stop_split: true`.

The extracted child owns a cohesive AST expansion transaction:

- strategy item traversal for universe expansion;
- statement expansion for `let`, `for`, `if`, `while`, `match`, `return`, `emit`, and expression statements;
- local binding uniquification by symbol suffix;
- universe binding substitution across nested expression shapes;
- per-symbol substitution for expanded loop bodies.

Splitting these helpers again would create micro owners around one expansion transaction and would increase parent-child forwarding cost without producing a stable independent public owner.

## Leaf Split Decision Result

Command:

- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -LeafId root.contracts.quantscript.runtime_lowering.universe_lowering.universe_construct_expansion -ParentId root.contracts.quantscript.runtime_lowering.universe_lowering -Path quantscript/src/lowering/universe/universe_construct_expansion.rs -Depth 5`

Result:

| Field | Value |
| --- | --- |
| `split_decision` | `STOP` |
| `governance_packaging` | `stop_split` |
| `final_decision` | `STOP` |
| `normalized_split_score` | 48 |
| `loc` | 612 |
| `function_count` | 11 |
| `public_surface` | 1 |
| `branches` | 55 |
| `risk_tags` | `cross_crate`, `public_api`, `state_machine` |
| `reason` | medium score without same-parent wave candidate; avoid standalone governance |

## Boundary Kept

The parent `quantscript/src/lowering/universe.rs` remains the mediator for:

- public(crate) universe lowering facades;
- universe expression evaluation;
- metadata selection and sorting;
- instrument pool selection key mapping;
- module-local tests.

The child does not introduce sibling horizontal links and only calls parent-owned `evaluate_universe_expr` and `UniverseValue` through the parent module boundary.

## Validation

No Rust movement in this closeout step. The preceding extraction was validated by:

- `cargo fmt --check`
- `cargo test -p quantscript` (88 tests)
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8, full feature tree, and matrix governance checks

## Next Step

Return to `root.contracts.quantscript.runtime_lowering.universe_lowering` parent residual judgment.
