# v4.16.0 root.contracts.compiler_bridge.runtime_protocol_lowering parent residual judgment selects risk_policy_lowering

> Batch: BE-001XV-01
> Node: `root.contracts.compiler_bridge.runtime_protocol_lowering`
> Selected child: `root.contracts.compiler_bridge.runtime_protocol_lowering.risk_policy_lowering`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.compiler_bridge.runtime_protocol_lowering` returns to its child queue after `agent_policy_lowering` closeout.

Decision:

`next_child: root.contracts.compiler_bridge.runtime_protocol_lowering.risk_policy_lowering`

## Closed Runtime Lowering Children

| Child | Result |
| --- | --- |
| `contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering` | Closed as parent facade over structured condition and fallback description children. |
| `contracts.compiler_bridge.runtime_protocol_lowering.agent_policy_lowering` | Closed with `stop_split: true`; owns runtime agent policy conversion and rebalance schedule mapping. |

## Open Runtime Lowering Residuals

| Residual | Status |
| --- | --- |
| `contracts.compiler_bridge.runtime_protocol_lowering.risk_policy_lowering` | Selected next. Owns runtime risk config to Core IR risk policy conversion. |
| `contracts.compiler_bridge.runtime_protocol_lowering.runtime_orchestration` | Conditional. Owns top-level runtime protocol lowering, validation delegation, data binding construction, and DAG validation; only split or close after risk policy judgment. |

## Selection Rationale

`risk_policy_lowering` is selected next because it is the last distinct policy conversion pocket in `qrpc_compiler/src/runtime_protocol_lowering.rs`:

- it owns `lower_runtime_risk_to_policy`;
- it converts one `RiskConfig` into one `RiskPolicy`;
- it projects risk ids, observed agents, concentration limits, turnover, leverage, trade interval, enabled state, and the current `max_cross_symbol_leverage: None` default;
- it is separate from top-level runtime orchestration and already-closed agent/intent children;
- extracting it can leave the parent focused on runtime orchestration and data binding.

## Same-Parent Parallel Judgment

`same_parent_parallel_children` is inactive because `agent_policy_lowering` is already closed and the remaining candidate is a single risk child.

## Hard Boundaries

The next `risk_policy_lowering` baseline must not:

- edit Rust source code before the baseline is frozen;
- change `lower_runtime_protocol_to_core_ir_with_metadata` signature or public compiler facades;
- change validation delegation, data binding construction, indicator/signal lowering, agent policy lowering, DAG validation, Strategy IR lowering, shared spread/custom helpers, canonical hash, tests, or release transition;
- introduce direct sibling-to-sibling calls; risk policy lowering must remain parent-mediated.

## Next Step

BE-001XW-01 `root.contracts.compiler_bridge.runtime_protocol_lowering.risk_policy_lowering` baseline_plan.

## Gates

- `cargo test -p qrpc-compiler`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
