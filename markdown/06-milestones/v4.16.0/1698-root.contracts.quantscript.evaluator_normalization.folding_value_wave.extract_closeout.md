# v4.16.0 root.contracts.quantscript.evaluator_normalization.folding_value_wave extract closeout

> Batch: BE-002HP-02
> Node: `root.contracts.quantscript.evaluator_normalization.folding_value_wave`
> Parent: `root.contracts.quantscript.evaluator_normalization`
> Stage: `extract_closeout`
> Movement: Rust code movement complete.
> governance_next_authority: active

---

## Result

`root.contracts.quantscript.evaluator_normalization.folding_value_wave` is extracted.

New child:

- `quantscript/src/evaluator/folding_value_wave.rs`

Parent updated:

- `quantscript/src/evaluator/mod.rs`

The parent now declares `mod folding_value_wave;` and imports only the parent-needed bridge functions.

## Moved Ownership

The child now owns:

- expression iterable projection;
- unary, binary, index, and slice folding;
- builtin member and identifier folding;
- fetch/get_data health helper recognition;
- numeric, bool, string, list, and slice helper projection.

Parent-retained behavior:

- public `normalize_script_module`;
- AST normalization orchestration;
- helper inline execution;
- control-flow helper execution for `if`, `for`, `while`, and `match`;
- evaluator tests.

## Public Surface

No public API changed.

Preserved:

- `normalize_script_module(module: &ScriptModule) -> Result<ScriptModule>`

The child exposes only `pub(super)` bridge functions to the evaluator parent.

## Validation

Local validation passed:

- `cargo fmt --check`
- `cargo test -p quantscript` (88 tests)
- `cargo check -p quantpilot`

Governance validation must pass before commit:

- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002HP-03 `root.contracts.quantscript.evaluator_normalization.folding_value_wave` single_leaf_closeout.
