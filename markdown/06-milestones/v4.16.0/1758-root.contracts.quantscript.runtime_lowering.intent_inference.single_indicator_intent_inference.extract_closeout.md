# v4.16.0 root.contracts.quantscript.runtime_lowering.intent_inference.single_indicator_intent_inference extract closeout

> Batch: BE-002IZ-02
> Node: `root.contracts.quantscript.runtime_lowering.intent_inference.single_indicator_intent_inference`
> Parent: `root.contracts.quantscript.runtime_lowering.intent_inference`
> Stage: `actual_extraction`
> Movement: Production Rust movement.
> governance_next_authority: active

---

## Extraction Summary

`single_indicator_intent_inference` has been extracted into:

- `quantscript/src/lowering/intents/single_indicator_intent_inference.rs`

The parent `quantscript/src/lowering/intents.rs` keeps the `single_indicator_intent` wrapper and delegates the RSI, MACD, Momentum, and ZScore single-indicator construction into the child module.

## Symbols Moved

Moved into the child:

- `single_indicator_intent` implementation body

The child remains `pub(super)` and returns the same `Vec<IntentConfig>` / `anyhow::Error` surface as the parent-owned wrapper.

## Parent Boundary Kept

The parent remains owner for:

- `canonicalize_data_sources`
- `inferred_agent_params`
- `infer_intents` public(crate) facade
- `intents_from_condition`
- `spread_intent_from_condition`
- `moving_average_ratio_intent`
- `legacy_intent_from_emit`
- `merge_intent`
- relation/id helpers
- structured comparison keys and comparison helpers
- data-source canonicalization and spread operand helpers

## Equivalence Notes

The extraction preserves:

- RSI BUY/SELL threshold handling and structured comparison params;
- MACD BUY/SELL histogram threshold handling;
- Momentum BUY/SELL threshold and structured comparison params;
- ZScore BUY/SELL entry threshold handling;
- runtime id hint behavior and sanitized-instrument fallback ids;
- emitted `IntentKind`, `input_data_ids`, params, and `enabled` flags.

## Validation

Passed after extraction:

- `cargo fmt --check`
- `cargo test -p quantscript` (88 tests)
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8, full feature tree, and matrix governance checks

## Next Step

BE-002IZ-03 single_leaf_closeout.
