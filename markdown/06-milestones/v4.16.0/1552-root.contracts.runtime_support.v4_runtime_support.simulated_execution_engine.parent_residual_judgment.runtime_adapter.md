# v4.16.0 simulated_execution_engine parent residual judgment - runtime_adapter

> Batch: BE-002ED-01
> Node: `root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine`
> Selected child: `root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine.runtime_adapter`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_trial: true

---

## Decision

Select `runtime_adapter` as the next child under `simulated_execution_engine`.

## Boundary

`runtime_adapter` owns the v4 runtime-to-simulator adapter layer currently held in `qrpc_runtime/src/v4_runtime/simulated_execution_engine.rs`:

- simulated execution config attachment;
- simulated market price public entrypoint;
- simulated snapshot public projection;
- transition-to-simulator handoff;
- runtime event payload to `V4SimulatedOrderRequest` construction;
- execution capability validation for simulated order requests;
- simulated execution event recording.

## Exclusions

The selected child must not own:

- order submit/fill/amend/expire implementation;
- OCO sibling cancellation;
- trailing stop trigger mechanics;
- ledger/accounting math;
- asset curve and microstructure metric internals;
- validation and parsing helper families that are not adapter-specific;
- v4 tests.

## Terminal Leaf Control

`tools/evaluate-leaf-granularity.ps1 -Path qrpc_runtime/src/v4_runtime/simulated_execution_engine.rs -SameParentWaveCandidate` result:

- `split_decision`: `STOP`
- `governance_packaging`: `stop_split`
- `final_decision`: `STOP`
- Score: `36`
- LOC: `198`
- Functions: `8`
- Public surface: `6`
- Risk tags: `persistence`, `public_api`, `route_schema`, `security`, `state_machine`, `trading_semantics`

The child should become a terminal white-box node after extraction.

## Next Step

BE-002EE-01 `root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine.runtime_adapter` baseline_plan.
