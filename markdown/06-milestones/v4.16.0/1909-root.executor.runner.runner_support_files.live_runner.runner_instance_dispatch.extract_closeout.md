# v4.16.0 root.executor.runner.runner_support_files.live_runner.runner_instance_dispatch extract closeout

> Batch: BE-002MH-02
> Node: `root.executor.runner.runner_support_files.live_runner.runner_instance_dispatch`
> Stage: `actual_extraction`
> governance_next_authority: active

---

## Result

`root.executor.runner.runner_support_files.live_runner.runner_instance_dispatch`
has been extracted into:

`src-executor/live_runner/runner_instance_dispatch.rs`

The child owns `RunnerInstance` and its V3/V4 variant dispatch impl. The parent
re-exports `RunnerInstance` and retains `RunnerPool`, `V4Runner`, V4 helper
functions, and local tests.

## Preserved Surface

- `RunnerInstance::V3`
- `RunnerInstance::V4`
- `kline_pool`
- `v4_memory_snapshot`

Parent-scoped dispatch helpers remain `pub(super)` for runner pool access.

## Gates

| Gate | Result |
| --- | --- |
| `cargo fmt --all --check` | PASS |
| `cargo check -p quantpilot --bin executor` | PASS |
| `cargo test -p quantpilot --bin executor live_runner` | PASS |
| `git diff --check` | PASS |

## Next Step

BE-002MH-03
`root.executor.runner.runner_support_files.live_runner.runner_instance_dispatch`
single_leaf_closeout.
