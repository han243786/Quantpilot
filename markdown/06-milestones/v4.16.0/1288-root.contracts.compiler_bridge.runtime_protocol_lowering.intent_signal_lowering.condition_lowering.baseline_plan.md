# v4.16.0 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.condition_lowering equivalence baseline and extraction plan

> Batch: BE-001XN-01
> Node: `root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.condition_lowering`
> Parent: `root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering`
> Stage: `baseline_plan`
> Movement: no Rust code movement.

---

## Summary

`root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.condition_lowering` is frozen before physical Rust movement.

This child owns runtime structured condition construction for MA/SMA, LongTermSell, RSI, Momentum, ZScore, and QuoteObserve spread-threshold paths. Because these branches depend directly on runtime comparison metadata decoding, the comparison decode helpers are included in this child baseline.

Decision:

`baseline_frozen: true`

Next movement:

`BE-001XO-01` may create `qrpc_compiler/src/runtime_protocol_lowering/intent_signal_lowering/condition_lowering.rs` or equivalent private child module and move only the condition/conversion helpers listed below. Parent `intent_signal_lowering` must preserve parent-facing indicator/signal functions, fallback description, intent kind mapping, and all calls from the runtime lowering parent.

## Allowed Movement Set

The extraction step may move these symbols:

| Symbol | Responsibility |
| --- | --- |
| `lower_runtime_intent_condition` | Build structured MA/SMA, LongTermSell, RSI, Momentum, ZScore, and QuoteObserve condition expressions. |
| `lower_runtime_spread_threshold_condition` | Build structured QuoteObserve spread threshold conditions. |
| `decode_runtime_comparison_op` | Decode numeric runtime comparison op metadata. |
| `comparison_op_code` | Provide default comparison op encoding for metadata fallback. |
| `RuntimeComparisonShape` | Runtime buy/sell shape enum. |
| `decode_runtime_comparison_shape` | Decode runtime buy/sell comparison shape metadata. |

## Parent Calls Frozen

After extraction:

- `lower_runtime_intent_to_signal_rule` may call the child for structured condition construction.
- Fallback to `describe_runtime_intent_condition` must remain unchanged when the child returns `None`.
- `lower_runtime_intent_to_indicator` and `lower_runtime_intent_kind` must remain outside this child.
- `lower_runtime_spread_spec` remains owned by the parent/shared spread boundary and may be accessed only through parent-mediated private calls.

## Equivalence Baseline

BE-001XO-01 must preserve:

- MA/SMA moving-average compare output and supported op checks;
- LongTermSell moving-average deviation compare output and supported op checks;
- RSI one-sided threshold behavior and default threshold logic;
- Momentum one-sided threshold behavior;
- ZScore one-sided threshold behavior;
- QuoteObserve spread-threshold structured compare output;
- unsupported comparison shape/op fallback to raw-text behavior through parent;
- public runtime compile/lower output shape.

## Hard Exclusions

This baseline must not:

- move `lower_runtime_intent_to_indicator`;
- move `lower_runtime_intent_to_signal_rule`;
- move `describe_runtime_intent_condition`;
- move `lower_runtime_intent_kind`;
- move runtime agent/risk/data orchestration;
- move shared spread/custom helpers beyond parent-mediated calls;
- alter Strategy IR lowering, validation, tests, canonical hash, or release-transition rules.

## Boundary

**Real files**:
- `qrpc_compiler/src/runtime_protocol_lowering/intent_signal_lowering.rs`

**Markers**:
- `BE-001XN-01`
- `condition_lowering baseline_frozen: true`
- `fallback description excluded`
- `intent kind mapping excluded`

**Next step**:
BE-001XO-01 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.condition_lowering extract_closeout

---

## Gates

- `cargo test -p qrpc-compiler`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
