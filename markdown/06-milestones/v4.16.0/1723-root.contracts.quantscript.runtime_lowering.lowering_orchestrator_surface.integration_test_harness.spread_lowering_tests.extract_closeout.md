# v4.16.0 root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.spread_lowering_tests extract closeout

> Batch: BE-002IE-02
> Node: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.spread_lowering_tests`
> Parent: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness`
> Stage: `actual_extraction`
> Movement: Rust code moved.
> governance_next_authority: active

---

## Extraction Result

`spread_lowering_tests` is extracted.

Created:

- `quantscript/src/lowering/orchestrator/integration_test_harness/spread_lowering_tests.rs`

Updated:

- `quantscript/src/lowering/orchestrator/integration_test_harness.rs`

## Moved Owner

The new child owns:

- non-admitted cross-source spread formula rejection;
- non-admitted asymmetric-window spread formula rejection;
- admitted explicit spread helper lowering into `IntentKind::QuoteObserve`;
- Core IR spread spec, align, resample, field, and condition assertions;
- non-admitted helper-annotated spread rejection.

## Parent Residual

`integration_test_harness.rs` remains the parent test harness for:

- basic runtime smoke and data binding tests;
- runtime coordinator smoke;
- semantic missing-helper rejection;
- exchange-only data-source decoding;
- rebalance lowering and rejection;
- `manual_formula_tests` and `spread_lowering_tests` child declarations.

No production code moved. No sibling production link was introduced.

## Validation

Passed:

- `cargo fmt`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`

Remaining closeout gates:

- `git diff --check`
- UTF-8
- full-feature-tree
- matrix governance

## Next Step

BE-002IE-03 single_leaf_closeout.
