# v4.16.0 root.contracts.core_ir.v4_contracts.version_manifest single leaf closeout stops split

> Batch: BE-001VM-03
> Node: `root.contracts.core_ir.v4_contracts.version_manifest`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.version_manifest` stops splitting after extracting the full v4 version manifest
compatibility contract.

The child owns:

- `V4VersionManifest`
- version manifest default construction
- schema/version compatibility validation
- child-local version manifest and QS language default helpers

These pieces form one compatibility proof: version defaults, compatibility flags, and version validation failures must
stay together to keep v4 schema governance readable.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `version_manifest` names the complete v4 version compatibility contract boundary. |
| parent_child_communication_kept | pass | The v4 parent re-exports the public version manifest and sibling helpers remain accessed through parent mediation. |
| equivalence_baseline_freezable | pass | BE-001VM-01 froze version defaults, compatibility flags, and validation behavior. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | `V4VersionManifest` remains public through `qrpc_core_ir::v4`. |
| state_machine_phase | no | Version compatibility is schema governance, not graph state topology. |
| strategy_branch | no | The child is not a strategy branch. |
| independent_failure_mode | guarded | Version mismatch and compatibility policy failures belong to one version manifest contract. |
| reuse_pressure | guarded | Static bundles and tests depend on the version manifest as part of the complete v4 contract bundle. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | yes | Splitting the two default helpers from the DTO/validator would create small leaves without independent owners. |
| communication_cost_rises | yes | Further split would route one version compatibility proof across unnecessary private modules. |
| local_proof_missing | no | `cargo test -p qrpc-core-ir`, `cargo check -p quantpilot`, and commit-time gates passed after extraction. |
| line_count_only | no | The stop decision is based on cohesive version manifest ownership. |

leaf_split_decision_result

`stop_split: true`

This leaf is complete. Return to `root.contracts.core_ir.v4_contracts` for parent residual judgment.

next_recursive_step

BE-001VN-01 root.contracts.core_ir.v4_contracts parent_residual_judgment
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/version_manifest.rs`

**Markers**:
- `BE-001VM-03`

**Next step**:
BE-001VN-01 root.contracts.core_ir.v4_contracts parent_residual_judgment

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
