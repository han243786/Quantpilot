# v4.16.0 root.contracts.quantscript.typed_resolution.resolver_support_surface baseline plan

> Batch: BE-002GN-01
> Node: `root.contracts.quantscript.typed_resolution.resolver_support_surface`
> Parent: `root.contracts.quantscript.typed_resolution`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Scope

Freeze resolver support helpers before movement.

Current owner:

- `quantscript/src/resolve/mod.rs`

Planned child:

- `quantscript/src/resolve/resolver_support_surface.rs`

## Items Frozen For Movement

The next extraction may move only:

- `make_expr`
- `expr_label`
- `alloc_def_id`
- `alloc_expr_id`
- `callable_target`
- `call_target_return_type`
- `expr_number`
- `expr_integer`
- `series_index_view_kind`
- `series_window_span`
- `build_resolved_functions`
- `first_return_expr`
- `returned_list_target`

## Parent Wiring To Preserve

The parent resolver module must keep:

- `lower_script_to_typed_hir` public entrypoint unchanged;
- public DTO and enum re-exports unchanged;
- `FunctionSignature`, `CallableTarget`, `BindingInfo`, `Resolver`, and `MemberCapabilityUse` ownership unchanged unless a moved method requires parent-visible access;
- closed child calls from orchestration, semantic inference, type inference, and callable classification unchanged;
- resolved function metadata shape unchanged;
- expression semantic keys and resolver-local tests unchanged.

## Forbidden Movement

The next extraction must not move or change:

- public resolver DTOs/enums already closed in `public_type_surface`;
- callable/helper classifiers and registry seeding already closed in `callable_classification_surface`;
- semantic/manual indicator inference already closed in `semantic_inference_surface`;
- type inference, binding insertion, condition validation, and type annotation resolution already closed in `type_inference_binding_surface`;
- module/function/block/statement/expression lowering already closed in `resolver_orchestration_surface`;
- `lower_script_to_typed_hir`, `expr_semantic_key`, `FunctionSignature`, `CallableTarget`, `BindingInfo`, `Resolver`, `MemberCapabilityUse`, and resolver-local tests;
- analysis, lowering, evaluator, runtime, backend, executor, frontend, plugin metadata, or release transition.

## Expected Validation

The extraction must pass:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002GN-02 `root.contracts.quantscript.typed_resolution.resolver_support_surface` actual_extraction.
