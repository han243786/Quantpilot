# v4.16.0 root.contracts.qrpc_core.runtime_io_contract.decision_flow extract closeout

> Batch: BE-001SF-02
> Node: `root.contracts.qrpc_core.runtime_io_contract.decision_flow`
> Parent: `root.contracts.qrpc_core.runtime_io_contract`
> Stage: `extract_closeout`
> Movement: Rust code extraction.

---

## Summary

`root.contracts.qrpc_core.runtime_io_contract.decision_flow` has been extracted from `qrpc_core/src/runtime_io_contract.rs` into a private runtime IO child module.

The runtime IO parent preserves the local public facade:

- `mod decision_flow;`
- `pub use decision_flow::*;`

The qrpc-core crate root remains unchanged and continues to preserve public exports through `pub use runtime_io_contract::*;`.

## Files Changed

- `qrpc_core/src/runtime_io_contract.rs`
- `qrpc_core/src/runtime_io_contract/decision_flow.rs`

## Extraction Result

`qrpc_core/src/runtime_io_contract/decision_flow.rs` now owns:

- `IntentSignal`;
- `ProposedAction`;
- `TargetWeight`;
- `PortfolioTarget`;
- `PortfolioTargetDecision`;
- `AgentDecision`;
- `RiskDecision`.

`qrpc_core/src/runtime_io_contract.rs` now keeps the runtime IO parent facade plus queued execution IO, portfolio state, runtime output, and backtest output DTO families.

## Rust Local Fields

| Field | Result |
| --- | --- |
| Crate | `qrpc-core` |
| Parent facade | Preserved. `runtime_io_contract.rs` declares and re-exports the decision flow child. |
| Child visibility | The module is private; public decision flow surfaces are preserved through `pub use decision_flow::*;`. |
| Crate public exports | Preserved through qrpc-core crate-root `pub use runtime_io_contract::*;`. |
| Parent-owned residuals | Execution IO, portfolio state, runtime output, backtest output, RFC execution contracts, and tests remain parent-owned. |
| Sibling dependency check | Passed. The child imports closed qrpc-core public names through the crate root and does not import sibling internals directly. |

## Equivalence Preservation

| Surface | Preserved evidence |
| --- | --- |
| Intent signal | Signal identity, scope, side/strength/confidence, metrics, timing, TTL, and trace fields remain unchanged. |
| Proposed action | Exchange, side, quantity ratio, reference price, and strategy tag fields remain unchanged. |
| Target DTOs | Target weight, portfolio target, portfolio target decision fields, defaulted optional score/weights, and `PartialEq` derives remain unchanged. |
| Agent decision | Agent identity, symbol, exchange targets, net side/strength, defaulted optional target decision, actions, reason, timestamp, and trace fields remain unchanged. |
| Risk decision | Risk identity, status, defaulted mode, optional adjusted target, adjusted actions, reason codes/text, timestamp, and trace fields remain unchanged. |
| Public facade | Parent and crate-root re-exports preserve `qrpc_core::*` access. |

## Hard Boundaries Held

BE-001SF-02 did not:

- change decision/action/target fields, serde derives, optional/default attributes, `PartialEq` derives, public exports, or tests;
- move execution IO, portfolio state, runtime output, backtest output, RFC execution contracts, or crate-root tests;
- change closed market data IO, other qrpc-core children, downstream runtime/backend/compiler behavior, or release transition.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-pre-commit-hook.ps1`

## Next Step

BE-001SF-03 `root.contracts.qrpc_core.runtime_io_contract.decision_flow` single_leaf_closeout.
