# v4.16.0 root.contracts.quantscript.typed_resolution.callable_classification_surface baseline plan

> Batch: BE-002GF-01
> Node: `root.contracts.quantscript.typed_resolution.callable_classification_surface`
> Parent: `root.contracts.quantscript.typed_resolution`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Scope

Freeze callable/helper classification and registry seeding before movement.

Current owner:

- `quantscript/src/resolve/mod.rs`

Planned child:

- `quantscript/src/resolve/callable_classification_surface.rs`

## Items Frozen For Movement

The next extraction may move only:

- `classify_series_capability_name`
- `classify_builtin_math_name`
- `classify_member_mutation_name`
- `build_resolved_callables`
- `seed_callable`
- `classify_imported_helper`
- `classify_change_smoothing_kind`
- `classify_fetch_source_kind`
- `is_known_helper_function`

## Parent Wiring To Preserve

The parent resolver module must keep:

- public access to `classify_series_capability_name`;
- public access to `classify_builtin_math_name`;
- public access to `classify_member_mutation_name`;
- resolver access to `build_resolved_callables`, `classify_imported_helper`, and `is_known_helper_function`;
- existing downstream imports through `crate::resolve::*`.

## Forbidden Movement

The next extraction must not move or change:

- public DTO/enums already closed in `public_type_surface`;
- `lower_script_to_typed_hir`;
- `expr_semantic_key`;
- `FunctionSignature`, `CallableTarget`, `BindingInfo`, `Resolver`;
- resolver lowering/type inference methods;
- expression semantic inference;
- manual indicator formula inference;
- resolver-local tests, except as protected evidence.

## Expected Validation

The extraction must pass:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002GF-02 `root.contracts.quantscript.typed_resolution.callable_classification_surface` actual_extraction.
