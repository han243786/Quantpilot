# v4.16.0 root.contracts.runtime_support.data_module.http_transport single leaf closeout

> Batch: BE-001ZO-03
> Node: `root.contracts.runtime_support.data_module.http_transport`
> Parent: `root.contracts.runtime_support.data_module`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Leaf Status

`root.contracts.runtime_support.data_module.http_transport` is closed as a terminal leaf.

The child owns:

- ping probing;
- OKX live kline/quote fetch;
- JSON request retry and fallback;
- Tokio blocking bridge;
- fallback runtime singleton;
- Windows PowerShell HTTP fallback.

## Leaf Granularity Judge Evidence

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -LeafId root.contracts.runtime_support.data_module.http_transport -ParentId root.contracts.runtime_support.data_module -Path qrpc_runtime/src/data_module/http_transport.rs -Depth 4
```

Result:

| Field | Value |
| --- | --- |
| `decision` | `STOP` |
| `normalized_split_score` | 41 |
| `governance_mode` | `stop_split` |
| `size_bucket` | `terminal_target_150_600` |
| `loc` | 188 |
| `function_count` | 6 |
| `public_surface` | 10 |
| `high_cost_leaf_needs_wave_or_stop` | `true` |

Reason: the child is within the terminal target size and splitting ping probing, OKX live fetch, JSON retry/fallback, runtime bridge, and Windows fallback would add parent-forwarding and governance cost without a stronger independent owner.

## White-Box Boundary

| Boundary | Decision |
| --- | --- |
| Input | parent-owned `reqwest::Client`, `DataSourceConfig`, `now_ms`, exchange surface helpers, normalization helpers |
| Output | `PingProbe`, live normalized market data with `FetchDiagnostics`, or JSON payloads for transport callers |
| Owner | `qrpc_runtime/src/data_module/http_transport.rs` |
| Parent facade | `qrpc_runtime/src/data_module.rs` delegates live transport calls and re-exports helpers for historical cache mediation |
| Excluded | request cache, breaker decision logic, mock fallback, historical cache persistence, exchange parser ownership, quality diagnostics, and tests |

## Equivalence Evidence

The preceding extraction step passed:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Closeout Decision

`stop_split: true`

No further child queue is opened for HTTP transport.

Next step: BE-001ZP-01 `root.contracts.runtime_support.data_module` parent_residual_judgment selects the next remaining data-module child, expected `contracts.runtime_support.data_module.test_harness` or parent facade closeout if tests are judged too costly to split.
