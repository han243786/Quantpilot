# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation parent residual judgment selects event_party_validation

> Batch: BE-001UN-01
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation`
selects
`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation.event_party_validation`
as the next child.

The selected child owns transition/action/edge emitter and consumer permission checks after
event references and event specs have been resolved. It does not own referenced event discovery,
catalog presence checks, local catalog validation, undeclared event checks, event catalog DTOs,
or graph validation orchestration.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `event_party_validation` is a named white-box child for emitter/consumer permission checks. |
| parent_child_communication_kept | pass | It can receive resolved `all_machines`, `event_specs`, and `machines_by_id` from the event usage parent. |
| equivalence_baseline_freezable | pass | The party-check section has finite transition/source/action/edge failure branches. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | The public graph validator remains higher up, while party validation is a stable private event usage phase. |
| state_machine_phase | pass | Party validation is the event-routing permission phase after catalog resolution. |
| strategy_branch | guarded | It protects transition, action, and graph edge event routing semantics. |
| independent_failure_mode | pass | Consumer mismatch, source emitter mismatch, emitted-event emitter mismatch, and edge party mismatch are independent. |
| reuse_pressure | guarded | The parent remains the only caller, but isolating the section makes the event usage leaf reviewable. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The child owns concrete permission checks, not incidental helper lines. |
| communication_cost_rises | no | Passing resolved maps keeps communication parent-mediated and avoids sibling calls. |
| local_proof_missing | no | Existing qrpc-core-ir tests cover event emitter/consumer validation through the public validator. |
| line_count_only | no | Selection is driven by failure-mode ownership. |

leaf_split_decision_result

`continue_split: true`

Selected next child:

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation.event_party_validation`

next_recursive_step

BE-001UO-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation.event_party_validation baseline_plan
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/event_usage_validation.rs`

**Markers**:
- `BE-001UN-01`

**Next step**:
BE-001UO-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation.event_party_validation baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
