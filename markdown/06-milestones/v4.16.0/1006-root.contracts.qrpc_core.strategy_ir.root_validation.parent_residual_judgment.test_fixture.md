# v4.16.0 root.contracts.qrpc_core.strategy_ir.root_validation parent residual judgment selects test_fixture

> Batch: BE-001RJ-01
> Node: `root.contracts.qrpc_core.strategy_ir.root_validation`
> Selected child: `root.contracts.qrpc_core.strategy_ir.root_validation.test_fixture`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core.strategy_ir.root_validation` returns to its child queue after `unknown_marker_validation` closeout.

Decision:

`next_child: root.contracts.qrpc_core.strategy_ir.root_validation.test_fixture`

## Closed Root Validation Children

| Child | Result |
| --- | --- |
| `contracts.qrpc_core.strategy_ir.root_validation.identity_required_validation` | Closed with `stop_split: true`; owns identity, required field, required collection, and duplicate id validation. |
| `contracts.qrpc_core.strategy_ir.root_validation.signal_logic_validation` | Closed with `stop_split: true`; owns signal detail, indicator support, logic rule, and logic position unknown-marker validation. |
| `contracts.qrpc_core.strategy_ir.root_validation.risk_validation` | Closed with `stop_split: true`; owns risk unknownable checks and risk profile id/numeric validation. |
| `contracts.qrpc_core.strategy_ir.root_validation.data_execution_validation` | Closed with `stop_split: true`; owns data requirement checks plus execution and execution profile validation. |
| `contracts.qrpc_core.strategy_ir.root_validation.unknown_marker_validation` | Closed with `stop_split: true`; owns unknowns path/reason validation and unknownable helper implementation. |

## Open Root Validation Residuals

| Residual | Status |
| --- | --- |
| `contracts.qrpc_core.strategy_ir.root_validation.test_fixture` | Selected next. Owns the local `SAMPLE_JSON` fixture and root validation unit tests. |
| `contracts.qrpc_core.strategy_ir.root_validation.parent_facade` | Queued for parent closeout after the test fixture decision closes. |

## Selection Rationale

`test_fixture` is selected because all non-test validation families have closed, but the parent file still carries a large local test module:

- it owns `SAMPLE_JSON`;
- it owns the root validation success parse test;
- it owns invalid unknown marker regression coverage;
- it owns duplicate signal id regression coverage;
- it owns execution profile numeric regression coverage;
- it owns custom indicator support regression coverage;
- moving it can make `root_validation.rs` a clearer production facade without changing test behavior.

## Same-Parent Parallel Judgment

`same_parent_parallel_children` remains inactive for this selection.

The remaining candidate touches the parent test module declaration and local fixture ownership only. It should be handled as a serial parent facade edit.

## Hard Boundaries

The next `test_fixture` baseline must not:

- edit Rust source code;
- change test assertions, fixture JSON, validation ordering, diagnostics, public methods, root DTO fields, or serde shape;
- move production validation helpers or closed child validation modules;
- make root validation child modules import each other directly;
- change closed Strategy IR child DTOs/enums/constants/public functions, protocol primitives, runtime IO, compiler/runtime/backend/executor/frontend behavior, physical `plugins/*`, or release transition.

## Next Step

BE-001RK-01 `root.contracts.qrpc_core.strategy_ir.root_validation.test_fixture` baseline_plan.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
