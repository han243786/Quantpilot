# v4.16.0 root.contracts.qrpc_core.runtime_io_contract parent residual judgment selects decision_flow

> Batch: BE-001SE-01
> Node: `root.contracts.qrpc_core.runtime_io_contract`
> Selected child: `root.contracts.qrpc_core.runtime_io_contract.decision_flow`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core.runtime_io_contract` returns to its child queue after `market_data_io` closeout.

Decision:

`next_child: root.contracts.qrpc_core.runtime_io_contract.decision_flow`

## Closed Runtime IO Children

| Child | Result |
| --- | --- |
| `contracts.qrpc_core.runtime_io_contract.market_data_io` | Closed with `stop_split: true`. |

## Open Runtime IO Residuals

| Residual | Status |
| --- | --- |
| `contracts.qrpc_core.runtime_io_contract.decision_flow` | Selected next. Owns intent/action/target/agent/risk decision DTOs. |
| `contracts.qrpc_core.runtime_io_contract.execution_io` | Queued. Owns simulated order, execution plan, fill, open order, and fill result DTOs. |
| `contracts.qrpc_core.runtime_io_contract.portfolio_state` | Queued. Owns position/exposure/portfolio state DTOs and helper methods. |
| `contracts.qrpc_core.runtime_io_contract.runtime_output` | Queued. Owns runtime event, cycle output, and session output DTOs. |
| `contracts.qrpc_core.runtime_io_contract.backtest_output` | Queued. Owns backtest equity, metrics, summary, period return, and final output DTOs. |

## Selection Rationale

`decision_flow` is selected because it is the next contiguous DTO family in `qrpc_core/src/runtime_io_contract.rs`:

- it owns `IntentSignal`;
- it owns `ProposedAction`;
- it owns `TargetWeight`;
- it owns `PortfolioTarget`;
- it owns `PortfolioTargetDecision`;
- it owns `AgentDecision`;
- it owns `RiskDecision`;
- it is pure schema with no helper methods, no mutable state, no IO, no locks, and no persistence;
- later execution, portfolio, runtime output, and backtest DTOs can keep consuming decision DTOs through the runtime IO parent facade.

## Same-Parent Parallel Judgment

`same_parent_parallel_children` remains inactive for this selection.

Decision DTOs feed execution IO and portfolio output DTOs. Serial extraction keeps the parent facade as the only cross-child communication point before later siblings move.

## Hard Boundaries

The next `decision_flow` baseline must not:

- edit Rust source code;
- change decision/action/target field names, types, serde derives, optional/default attributes, public exports, or tests;
- move execution IO, portfolio state, runtime output, backtest output, RFC execution contracts, or crate-root tests;
- change closed market data IO or other qrpc-core children, downstream runtime/backend/compiler behavior, or release transition.

## Next Step

BE-001SF-01 `root.contracts.qrpc_core.runtime_io_contract.decision_flow` baseline_plan.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
