# v4.16.0 root.contracts.core_ir.v1_contract.policy_execution_contract baseline plan

> Batch: BE-001TO-01
> Node: `root.contracts.core_ir.v1_contract.policy_execution_contract`
> Parent: `root.contracts.core_ir.v1_contract`
> Stage: `baseline_plan`
> Movement: No code movement.

---

## Summary

This baseline freezes the remaining production policy/execution surface currently embedded in `qrpc_core_ir/src/v1.rs`.

The next implementation step may move only the frozen surface into `qrpc_core_ir/src/v1/policy_execution_contract.rs`. Public names must remain available through the v1 parent and crate-root facades.

## Frozen Public Surface

| Surface | Frozen owner for next step |
| --- | --- |
| `SignalRule` | `policy_execution_contract` |
| `AgentPolicy` | `policy_execution_contract` |
| `AgentPolicyKind` | `policy_execution_contract` |
| `RebalanceSchedule` | `policy_execution_contract` |
| `RiskPolicy` | `policy_execution_contract` |
| `ExecutionSizingKind` | `policy_execution_contract` |
| `CoreTimeInForce` | `policy_execution_contract` |
| `ExecutionRule` | `policy_execution_contract` |
| `default_true` | `policy_execution_contract` |
| `default_execution_sizing_kind` | `policy_execution_contract` |
| `default_time_in_force` | `policy_execution_contract` |

## Frozen Semantics

| Contract | Frozen behavior |
| --- | --- |
| Signal rule shape | `signal_id`, `indicator_id`, `signal_kind`, and `condition` fields stay unchanged. |
| Agent policy shape | All rebalance, threshold, quantity, spread trigger, and `enabled` serde defaults stay unchanged. |
| Risk policy shape | All risk limit fields and `max_cross_symbol_leverage` serde default/skip behavior stay unchanged. |
| Execution shape | Sizing kind, cost fields, time-in-force default, and free-form `params` map stay unchanged. |
| Default helpers | Missing `enabled` defaults to `true`; sizing defaults to `EquityNotionalRatio`; time-in-force defaults to `Gtc`. |

## Allowed Movement

BE-001TO-02 may:

- create `qrpc_core_ir/src/v1/policy_execution_contract.rs`;
- add `mod policy_execution_contract;` and `pub use policy_execution_contract::*;` to `qrpc_core_ir/src/v1.rs`;
- move only the frozen DTO/enums/helper declarations listed above;
- import expression child types through the v1 parent boundary, for example `super::{ScalarExpr, SignalKind}`;
- keep all tests in `qrpc_core_ir/src/v1.rs` until the later `test_fixture` child.

## Forbidden Movement

BE-001TO-02 must not:

- move `root_graph_contract` or `data_indicator_expression_contract`;
- move local tests;
- change public names, field names, enum variants, serde derives, serde defaults, skip rules, or helper return values;
- change v4 contracts, qrpc-core contracts, compiler/runtime/backend/executor/frontend behavior;
- introduce direct sibling shortcuts or release-transition wiring.

## Parent-Child Communication

The v1 parent remains the only local gateway:

- the new child must be private under `v1.rs`;
- the parent must re-export the child;
- root graph and expression children remain unchanged;
- local tests must continue using `use super::*`.

## Equivalence Baseline

| Check | Baseline |
| --- | --- |
| Current file | `qrpc_core_ir/src/v1.rs` owns the frozen production residual. |
| Current proof | `cargo test -p qrpc-core-ir` passes the v1 round-trip test before extraction. |
| Required post-extraction proof | `cargo check -p qrpc-core-ir`, `cargo test -p qrpc-core-ir`, and governance checks must pass. |

## Next Step

BE-001TO-02 `root.contracts.core_ir.v1_contract.policy_execution_contract` extract_closeout.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
