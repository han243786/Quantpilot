# v4.16.0 root.contracts.runtime_support.core_ir_evaluator parent residual judgment selects spread_custom_expression_wave

> Batch: BE-002AN-01
> Node: `root.contracts.runtime_support.core_ir_evaluator`
> Parent: `root.contracts.runtime_support`
> Stage: `parent_residual_judgment`
> Selected child: `root.contracts.runtime_support.core_ir_evaluator.spread_custom_expression_wave`
> Movement: No Rust code movement.

---

## Parent State

Closed children:

- `contracts.runtime_support.core_ir_evaluator.classic_indicator_wave`
- `contracts.runtime_support.core_ir_evaluator.advanced_indicator_wave`

The next child in the frozen queue is `spread_custom_expression_wave`.

## Selected Child Boundary

`root.contracts.runtime_support.core_ir_evaluator.spread_custom_expression_wave` owns the Spread and Custom runtime expression evaluator surface currently embedded in `qrpc_runtime/src/core_ir_evaluator.rs`:

- Custom evaluator entrypoint and helpers;
- legacy Spread evaluator entrypoint;
- typed Spread evaluator entrypoint;
- series expression materialization;
- quote/kline field projection;
- resample, window aggregation, value aggregation, and alignment;
- custom value expression evaluation, scope discovery, and reference-price derivation.

## Exclusions

The selected child must not absorb:

- already-closed classic or advanced indicator wave functions;
- shared lookup helpers such as `find_kline_snapshot`, `find_quote_snapshot`, and `find_quote_snapshots`;
- generic numeric indicator helper formulas;
- local tests.

## Split Decision

`continue_split: true`.

Reason:

- this is the remaining large expression-evaluation block in the parent;
- Spread and Custom share typed `SeriesExpr` machinery and should move as one expression wave instead of two heavily coupled leaves;
- lookup/math helper ownership remains parent-owned until the shared helper residual judgment.

No release-transition optimization is proposed.

## Next Step

BE-002AO-01 `root.contracts.runtime_support.core_ir_evaluator.spread_custom_expression_wave` baseline_plan.

## Verification

No code moved in this step. Required gates:

- `git diff --check`
- `tools/check-utf8.ps1`
- `tools/check-full-feature-tree.ps1`
- `tools/check-matrix-governance.ps1`
