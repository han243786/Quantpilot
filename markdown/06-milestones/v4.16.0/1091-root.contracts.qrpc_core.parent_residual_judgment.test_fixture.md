# v4.16.0 root.contracts.qrpc_core parent residual judgment selects test_fixture

> Batch: BE-001TC-01
> Node: `root.contracts.qrpc_core`
> Parent: `root.contracts`
> Selected child: `root.contracts.qrpc_core.test_fixture`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core` returns to parent residual judgment after `rfc_execution_contracts` closeout.

The expected parent closeout is not yet valid because `qrpc_core/src/lib.rs` still owns crate-root cfg-test fixture and tests.

Decision:

`next_child: root.contracts.qrpc_core.test_fixture`

## Closed qrpc-core Children

| Child | Result |
| --- | --- |
| `contracts.qrpc_core.error_contract` | Closed. |
| `contracts.qrpc_core.event_envelope_proto` | Closed. |
| `contracts.qrpc_core.plugin_contract` | Closed as parent after child splits. |
| `contracts.qrpc_core.strategy_ir` | Closed as parent after child splits. |
| `contracts.qrpc_core.protocol_primitives` | Closed. |
| `contracts.qrpc_core.runtime_protocol_config` | Closed. |
| `contracts.qrpc_core.artifact_specs` | Closed as parent after child splits. |
| `contracts.qrpc_core.runtime_io_contract` | Closed as parent after child splits. |
| `contracts.qrpc_core.rfc_execution_contracts` | Closed as parent after child splits. |

## Open qrpc-core Residual

| Residual | Status |
| --- | --- |
| `contracts.qrpc_core.test_fixture` | Selected next. Owns crate-root `sample_runtime_protocol` helper plus two crate-root tests. |

## Selection Rationale

`qrpc_core/src/lib.rs` is almost a pure facade, but still contains:

- `#[cfg(test)] use std::collections::BTreeMap`;
- `#[cfg(test)] mod tests`;
- `sample_runtime_protocol`;
- `canonical_digest_is_stable_for_equivalent_payloads`;
- `run_and_backtest_specs_capture_protocol_boundary`.

Closing the qrpc-core parent before moving this residual would leave behavior inside the parent facade. The recursive rule requires selecting `test_fixture` first.

## White Box Boundary

| Item | Owner | Input | Output | Side effects |
| --- | --- | --- | --- | --- |
| Test helper | `test_fixture` | none | sample `RuntimeProtocolCoreConfig` | none |
| Canonical digest regression | `test_fixture` | equivalent JSON payloads | stable digest equality assertions | none |
| Run/backtest spec regression | `test_fixture` | sample runtime protocol and core IR digest | run/backtest spec boundary assertions | none |

## Same-Parent Parallel Judgment

`same_parent_parallel_children` remains inactive for this selection.

Reason: `test_fixture` is the only remaining qrpc-core residual before parent closeout.

## Hard Boundaries

The next `test_fixture` baseline must not:

- edit Rust source code;
- change test assertions, helper data, digest behavior, spec behavior, public exports, or production module declarations;
- reopen closed qrpc-core children;
- change `qrpc_core_ir`, `qrpc_compiler`, `qrpc_runtime`, `quantscript`, backend, executor, frontend, physical `plugins/*`, or release transition.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `test_fixture` owns crate-root cfg-test helper and tests. |
| parent_child_communication_kept | pass | Test module can remain crate-root mediated through `super::*`. |
| equivalence_baseline_freezable | pass | Helper data and assertions can be frozen before movement. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | It is not public API, but it blocks parent facade closeout. |
| state_machine_phase | no | No mutable state machine behavior. |
| strategy_branch | no | No strategy branch is selected. |
| independent_failure_mode | pass | Test fixture drift is independent from production contract ownership. |
| reuse_pressure | no | It is local regression coverage, not a reusable production surface. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | It is the last parent residual and has clear cfg-test ownership. |
| communication_cost_rises | no | A single `#[cfg(test)] mod tests;` line reduces parent clutter. |
| local_proof_missing | no | qrpc-core tests cover the move directly. |
| line_count_only | no | Split is required to close the parent facade, not for line count. |

leaf_split_decision_result

`continue_baseline: true`; `test_fixture` must be selected before qrpc-core parent closeout.

## Next Step

BE-001TD-01 `root.contracts.qrpc_core.test_fixture` baseline_plan.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
