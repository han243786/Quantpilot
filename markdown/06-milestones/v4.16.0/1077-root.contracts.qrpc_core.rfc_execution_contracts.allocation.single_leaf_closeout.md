# v4.16.0 root.contracts.qrpc_core.rfc_execution_contracts.allocation single leaf closeout

> Batch: BE-001SU-03
> Node: `root.contracts.qrpc_core.rfc_execution_contracts.allocation`
> Parent: `root.contracts.qrpc_core.rfc_execution_contracts`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`allocation` closes as one compact allocation contract leaf.

The child owns allocation taxonomy, allocation DTO shape, and the pure `apply_to_targets` projection helper. Splitting DTO and helper would separate a single contract into artificial pieces and increase parent mediation without adding independent proof value.

## Closeout Evidence

| Check | Result | Evidence |
| --- | --- | --- |
| Physical owner exists | pass | `qrpc_core/src/rfc_execution_contracts/allocation.rs` owns the selected allocation contract. |
| Parent facade preserved | pass | `qrpc_core/src/rfc_execution_contracts.rs` re-exports `allocation::*`. |
| Public crate facade preserved | pass | `qrpc_core/src/lib.rs` still re-exports `rfc_execution_contracts::*`. |
| Rust equivalence verified | pass | `cargo check -p qrpc-core`, `cargo test -p qrpc-core`, and smart pre-commit passed in BE-001SU-02. |
| Residual separation | pass | Order, feedback, and handoff contracts remain outside this closed child. |

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `allocation` is a named allocation schema plus projection helper leaf. |
| parent_child_communication_kept | pass | Parent facade mediates all public exports. |
| equivalence_baseline_freezable | pass | BE-001SU-01 froze variants, fields, derives, projection math, clamping, and insert semantics. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | pass | The child is public qrpc-core contract API. |
| state_machine_phase | no | No transition or runtime state phase exists here. |
| strategy_branch | no | No strategy branch exists here. |
| independent_failure_mode | weak | DTO and helper drift fail as one allocation contract surface. |
| reuse_pressure | pass | Consumers still use the contract through `qrpc_core::*`. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | yes | Splitting `AllocationMethod`, `Allocation`, or helper logic would create tiny fragments without separate owners. |
| communication_cost_rises | yes | DTO/helper separation would force parent-mediated calls for one pure projection contract. |
| local_proof_missing | yes | qrpc-core gates prove the same allocation surface either way. |
| line_count_only | yes | Further split would mostly be by line count or enum/helper grouping. |

leaf_split_decision_result

`stop_split: true`

`allocation` is closed. The cursor returns to `root.contracts.qrpc_core.rfc_execution_contracts`.

## Next Step

BE-001SV-01 `root.contracts.qrpc_core.rfc_execution_contracts` parent_residual_judgment selects `order_contract`.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
