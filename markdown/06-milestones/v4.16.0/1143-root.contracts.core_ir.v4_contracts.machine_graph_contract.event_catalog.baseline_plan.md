# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.event_catalog equivalence baseline and extraction plan

> Batch: BE-001UG-01
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.event_catalog`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract`
> Stage: `baseline_plan`
> Movement: No code movement.

---

## Summary

This baseline freezes the event catalog schema and local validation behavior before extraction.

The selected child may own:

- `MachineEventCatalog`
- `MachineEventTypeSpec`
- `MachineEventPayloadField`
- `MachineEventSourceKind`
- `MachineEventScope`
- `impl MachineEventCatalog::validate_static_contract`

No graph root DTO, graph edge DTO, risk-plane DTO, graph validation method, graph-level emitter/consumer check, party helper, traversal helper, or test fixture moves in this baseline.

## Frozen Schema Surface

The next extraction must preserve:

| Type | Frozen notes |
| --- | --- |
| `MachineEventCatalog` | default schema version, default `events`, default `metadata`. |
| `MachineEventTypeSpec` | `event_type`, `source_kind`, `scope`, default `payload_fields`, default `allowed_emitters`, default `allowed_consumers`, default `replayable`. |
| `MachineEventPayloadField` | `name`, `type_name`, default `required`, default `nullable`. |
| `MachineEventSourceKind` | `MarketData`, `Machine`, `RiskPlane`, `VenueProvider`, `Runtime`; `snake_case`. |
| `MachineEventScope` | `MachineInternal`, `Graph`, `Runtime`, `Venue`; `snake_case`. |

## Frozen Validation Surface

The next extraction must preserve `MachineEventCatalog::validate_static_contract` exactly:

- schema version equals `V4_MACHINE_EVENT_CATALOG_VERSION`;
- catalog must declare at least one event;
- event type is required;
- event type must be unique;
- payload field name is required;
- payload field names must be unique per event;
- payload field `type_name` is required;
- payload field cannot be both required and nullable;
- branch order and error strings remain unchanged.

## Allowed BE-001UG-02 Movement

The next extraction step may:

- create `qrpc_core_ir/src/v4/machine_graph_contract/event_catalog.rs`;
- add `mod event_catalog;` to `qrpc_core_ir/src/v4/machine_graph_contract.rs`;
- add `pub use event_catalog::*;` to `qrpc_core_ir/src/v4/machine_graph_contract.rs`;
- move only the selected event catalog DTOs and local catalog validation impl.

The child may depend on parent-mediated names:

- `default_machine_event_catalog_version`
- `default_true`
- `V4_MACHINE_EVENT_CATALOG_VERSION`

The child must keep local imports:

- `serde::{Deserialize, Serialize}`
- `serde_json::Value`
- `std::collections::{BTreeMap, BTreeSet}`

## Forbidden BE-001UG-02 Movement

The next extraction step must not move:

- `V4MachineGraphContract`;
- `MachineGraphEdge`, `MachineGraphEdgeActivation`, or `MachineGraphRiskPlane`;
- `V4MachineGraphContract` validation methods;
- `machine_event_party_allowed`;
- `collect_machine_family`;
- `machine_nested_depth`;
- graph-level event catalog presence, undeclared event, emitter, or consumer checks;
- tests.

It must not alter serde attributes, derives, field order, validation branch order, or error strings.

## Equivalence Proof Plan

BE-001UG-02 must pass:

- `cargo fmt --check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-001UG-02 `root.contracts.core_ir.v4_contracts.machine_graph_contract.event_catalog` extract_closeout.
