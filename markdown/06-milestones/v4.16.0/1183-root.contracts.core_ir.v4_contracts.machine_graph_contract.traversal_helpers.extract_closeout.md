# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.traversal_helpers actual extraction complete

> Batch: BE-001VB-02
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.traversal_helpers`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract`
> Stage: `extract_closeout`
> Movement: Code movement completed.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract.traversal_helpers` actual extraction is complete.

Implemented movement:

- `qrpc_core_ir/src/v4/machine_graph_contract.rs` now declares `mod traversal_helpers;`
- the parent re-exports `collect_machine_family` and `machine_nested_depth`
- `qrpc_core_ir/src/v4/machine_graph_contract/traversal_helpers.rs` owns both helper implementations
- helper visibility is `pub(in crate::v4)` so the parent can re-export them to existing v4 callers without widening
  them beyond the v4 module

Equivalence notes:

- traversal order and nested depth calculation remain unchanged
- existing imports in `v4.rs` remain valid through the parent re-export
- validation children still use the same parent helper path

Verification:

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir` (54 passed)

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract.rs`
- `qrpc_core_ir/src/v4/machine_graph_contract/traversal_helpers.rs`

**Markers**:
- `BE-001VB-02`

**Next step**:
BE-001VB-03 root.contracts.core_ir.v4_contracts.machine_graph_contract.traversal_helpers single_leaf_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
