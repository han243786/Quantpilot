# v4.16.0 root.contracts.quantscript.v4_static_audit parent residual judgment selects risk_plane_parser

> Batch: BE-002KN-01
> Node: `root.contracts.quantscript.v4_static_audit`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select next child:

`root.contracts.quantscript.v4_static_audit.risk_plane_parser`

## Parent Residual Scan

Closed children:

- `runtime_handoff_builder`;
- `audit_entrypoint`;
- `capability_type_parser`;
- `event_catalog_derivation`;
- `parser_utilities_diagnostics`;
- `static_document_parser`;
- `machine_block_parser`;
- `state_block_parser`;
- `transition_parser`;
- `memory_parser`;
- `edge_parser`.

Remaining parent-owned clusters:

- state-group parsing;
- risk-plane parsing;
- machine template wrapper and thin capability/type wrappers;
- public DTOs and local static-audit tests.

## Why This Child

`risk_plane_parser` owns the graph risk-plane declaration grammar and
`MachineGraphRiskPlane` assembly. It parses required machine ids, optional
priority, priority validation, and default required behavior.

This is a cohesive graph-level parser leaf and can move without changing
state-group parsing, machine-template parsing, DTO ownership, or tests.

## Proposed Ownership

| Field | Value |
| --- | --- |
| Candidate child | `risk_plane_parser` |
| Planned file | `quantscript/src/v4_static_audit/risk_plane_parser.rs` |
| Likely owned functions | `parse_risk_plane` |
| Parent wrapper | `parse_risk_plane` |
| Parent bridge dependencies | `diag`, `split_words`, `split_csv_words` |

## Exclusions For Next Baseline

- Do not move `parse_state_group`.
- Do not move `parse_machine_template`.
- Do not move public report DTOs or local tests.
- Do not introduce release-transition links.

## Next Step

BE-002KO-01 `root.contracts.quantscript.v4_static_audit.risk_plane_parser`
baseline_plan.
