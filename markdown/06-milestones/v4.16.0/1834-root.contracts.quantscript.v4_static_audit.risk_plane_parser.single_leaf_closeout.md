# v4.16.0 root.contracts.quantscript.v4_static_audit.risk_plane_parser single leaf closeout

> Batch: BE-002KO-03
> Node: `root.contracts.quantscript.v4_static_audit.risk_plane_parser`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`stop_split: true`

The extracted child is a micro risk-plane parser leaf. It owns one risk-plane
grammar rule, machine id expansion, priority validation, and
`MachineGraphRiskPlane` assembly. Further split would fragment a single cohesive
graph-level declaration.

## Leaf Granularity Evidence

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -LeafId root.contracts.quantscript.v4_static_audit.risk_plane_parser -ParentId root.contracts.quantscript.v4_static_audit -Path quantscript\src\v4_static_audit\risk_plane_parser.rs -Depth 4
```

Observed result:

| Field | Value |
| --- | --- |
| Split decision | STOP |
| Governance packaging | stop_split |
| Decision | STOP |
| Score | 24 |
| LOC | 39 |
| Functions | 1 |
| Public surface | 1 |
| Branches | 3 |
| Domains | 3 |
| Risk tags | cross_crate, public_api, trading_semantics |
| Size bucket | micro_under_100 |

## Why No Further Split

- The child is under 100 LOC.
- There is only one parent-facing function.
- Risk-plane syntax, machine id expansion, priority validation, and graph
  assembly are one cohesive declaration parser.
- State-group and machine-template parsing remain separate parent residuals.
- Parent-child communication remains explicit through the parent wrapper.

## Preserved Boundaries

- Public API unchanged.
- `static_document_parser` continues through the parent wrapper.
- `parse_state_group` and `parse_machine_template` remain parent-owned.
- Public report DTOs and local static-audit tests remain parent-owned.
- No release-transition link was introduced.

## Gates

| Gate | Result |
| --- | --- |
| leaf granularity evaluator | PASS, STOP score 24 |
| `cargo fmt --all --check` | PASS |
| `cargo test -p quantscript` | PASS, 88 tests |
| `cargo check -p quantpilot` | PASS |
| `git diff --check` | PASS |

## Next Step

BE-002KP-01 `root.contracts.quantscript.v4_static_audit`
parent_residual_judgment.
