# v4.16.0 root.contracts.runtime_support.risk_support parent residual judgment - test_harness

> Batch: BE-002CF-01
> Node: `root.contracts.runtime_support.risk_support`
> Selected child: `root.contracts.runtime_support.risk_support.test_harness`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.

---

## Decision

Select `test_harness` as the next risk-support residual child.

## Evidence

Closed risk-support children:

- `event_payload_projection`
- `direction_cross_constraints`
- `action_clamp_helpers`
- `portfolio_target_clamp_helpers`
- `exposure_math_helpers`

Remaining parent-owned production surface:

- `RiskCheckRequest`
- `RiskCheckOutput`
- `RiskCheckerProvider`
- `RiskChecker`
- `evaluate_risk_decision`
- `enforce_risk_mode`
- `push_reason_code`

The remaining large local block is the `#[cfg(test)] mod tests` harness in `qrpc_runtime/src/risk_checker.rs`. It owns sample Core IR fixtures, portfolio fixtures, and risk checker behavior tests.

## Boundary

The next baseline may move only local tests and test fixtures into a test-only child module. Production provider/evaluate behavior, closed helper children, and release-transition optimization remain outside this movement.

## Dependency Rule

The test harness must keep parent-mediated access through `use super::*`. No sibling child import or release-transition shortcut is allowed.

## Next Step

BE-002CG-01 `root.contracts.runtime_support.risk_support.test_harness` baseline_plan.
