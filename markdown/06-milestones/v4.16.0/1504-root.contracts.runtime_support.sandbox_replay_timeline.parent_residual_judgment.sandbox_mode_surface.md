# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline parent residual judgment - sandbox_mode_surface

> Batch: BE-002CT-01
> Node: `root.contracts.runtime_support.sandbox_replay_timeline`
> Selected child: `root.contracts.runtime_support.sandbox_replay_timeline.sandbox_mode_surface`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.

---

## Decision

Select `sandbox_mode_surface` as the next sandbox/replay/timeline child.

## Evidence

Timeline data providers, unified timeline, and replay builder are closed. The next cohesive boundary in `qrpc_runtime/src/sandbox/mod.rs` is the public sandbox mode/control surface before concrete sandbox implementations:

- supported runtime mode/execution-module keys;
- `RuntimeSupportBoundary` and `runtime_support_boundary`;
- deterministic test mode enums and DTO;
- `SandboxMode`;
- `SandboxSnapshot`;
- `Sandbox` trait.

This surface is consumed by both realtime and fast-backtest sandbox implementations, so it should be extracted before implementation-specific children.

## Candidate Boundary

The next baseline should cover only the public sandbox mode/control surface. It must not move:

- `RealTimeSandbox`;
- `FastBacktestSandbox`;
- benchmark/snapshot/trace helper functions;
- local sandbox tests;
- replay/timeline children.

## Dependency Rule

Concrete sandbox implementations must consume this surface through the sandbox parent. No sibling shortcut or release-transition optimization is allowed.

## Next Step

BE-002CU-01 `root.contracts.runtime_support.sandbox_replay_timeline.sandbox_mode_surface` baseline_plan.
