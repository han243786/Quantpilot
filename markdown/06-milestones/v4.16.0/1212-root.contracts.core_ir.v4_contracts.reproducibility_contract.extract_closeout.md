# v4.16.0 root.contracts.core_ir.v4_contracts.reproducibility_contract actual extraction complete

> Batch: BE-001VQ-02
> Node: `root.contracts.core_ir.v4_contracts.reproducibility_contract`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `extract_closeout`
> Movement: Code movement completed.

---

## Summary

`root.contracts.core_ir.v4_contracts.reproducibility_contract` actual extraction is complete.

Implemented movement:

- `qrpc_core_ir/src/v4.rs` now declares `mod reproducibility_contract;`
- `qrpc_core_ir/src/v4.rs` re-exports the child through `pub use reproducibility_contract::*;`
- `qrpc_core_ir/src/v4/reproducibility_contract.rs` owns reproducibility DTOs, run evidence and event envelope enums,
  reproducibility defaults, and static reproducibility validation

Equivalence notes:

- public reproducibility surface remains unchanged through the parent re-export
- required evidence order, event envelope field order, replay flags, and validation error strings remain frozen from
  BE-001VQ-01
- plugin governance, complexity budget, developer learning pipeline, static bundles, compile-time capability reports,
  compatibility bridge, bridge builders, tests, machine contract, machine graph contract, QS contracts, runtime mode,
  venue capability matrix, and version manifest remain outside this child

Verification:

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir` (54 passed)
- `cargo check -p quantpilot`

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs,qrpc_core_ir/src/v4/reproducibility_contract.rs`

**Markers**:
- `BE-001VQ-02`

**Next step**:
BE-001VQ-03 root.contracts.core_ir.v4_contracts.reproducibility_contract single_leaf_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
