# v4.16.0 root.contracts.core_ir.v1_contract.data_indicator_expression_contract single leaf closeout

> Batch: BE-001TM-03
> Node: `root.contracts.core_ir.v1_contract.data_indicator_expression_contract`
> Parent: `root.contracts.core_ir.v1_contract`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`data_indicator_expression_contract` is now a closed compact leaf.

The extraction in BE-001TM-02 created a physical owner for v1 data binding, indicator schema, series/custom/scalar expression schema, signal kind, comparison taxonomy, and expression helper builders while preserving v1 and crate-root facades.

## Closed Surface

| Surface | Result | Evidence |
| --- | --- | --- |
| Physical owner exists | pass | `qrpc_core_ir/src/v1/data_indicator_expression_contract.rs` owns the selected data, indicator, and expression contract. |
| v1 parent facade preserved | pass | `qrpc_core_ir/src/v1.rs` re-exports `data_indicator_expression_contract::*`. |
| Crate facade preserved | pass | `qrpc_core_ir/src/lib.rs` still re-exports `v1::*`. |
| Rust equivalence verified | pass | BE-001TM-02 passed `cargo check -p qrpc-core-ir`, `cargo test -p qrpc-core-ir`, and smart pre-commit. |
| Full workspace check preserved | pass | Smart pre-commit passed `cargo check -p quantpilot`. |

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | Child owns data binding, indicator schema, expression schema, signal kind, comparison taxonomy, and helper builders. |
| parent_child_communication_kept | pass | The v1 parent is the only facade; root graph and residual policy/test code reach child names through the parent namespace. |
| equivalence_baseline_freezable | pass | BE-001TM-01 froze serde shapes, public names, helper behavior, and non-owned residuals. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | pass | The child contains public qrpc-core-ir v1 schema names. |
| state_machine_phase | no | The child is pure schema/helper contract, not a state-machine phase owner. |
| strategy_branch | pass | The schema models data, indicator, signal, and expression branches of the strategy graph. |
| independent_failure_mode | guarded | Data binding, indicator schema, and expression helpers can drift independently, but current tests prove them through the same Core IR v1 round trip. |
| reuse_pressure | pass | Compiler/runtime consumers reuse these names through the v1 facade. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | pass | Splitting field/aggregation/operator enums or helper functions alone would create tiny leaves without independent ownership. |
| communication_cost_rises | pass | Series/custom/scalar expressions and helper builders reference each other densely; splitting them would add cross-child imports without better proof. |
| local_proof_missing | no | `cargo test -p qrpc-core-ir` covers the v1 round trip after extraction. |
| line_count_only | pass | The only remaining pressure is size and public item count; there is no stronger state/handler/failure boundary. |

leaf_split_decision_result

`stop_split: true`; `data_indicator_expression_contract` should remain one compact schema/helper leaf for now.

## Residual Boundary

Still outside this closed leaf:

- `contracts.core_ir.v1_contract.policy_execution_contract`;
- `contracts.core_ir.v1_contract.test_fixture`;
- v1 parent closeout;
- v4 contracts and core_ir parent closeout;
- release transition.

## Next Step

BE-001TN-01 `root.contracts.core_ir.v1_contract` parent_residual_judgment selects `policy_execution_contract`.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
