# v4.16.0 root.executor.runner.runner_support_files.okx_rest.test_harness baseline plan

> Batch: BE-002MB-01
> Node: `root.executor.runner.runner_support_files.okx_rest.test_harness`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Goal

Freeze the OKX REST local test and smoke harness boundary before Rust movement.

The next movement may extract the `#[cfg(test)]` test module from
`src-executor/okx_rest.rs` into:

`src-executor/okx_rest/test_harness.rs`

## Frozen Boundary

| Field | Value |
| --- | --- |
| Planned module | `src-executor/okx_rest/test_harness.rs` |
| Approved surface | local unit tests, credential probe helpers, retry helper, response field helper, ignored live smoke probes |
| Parent dependencies | public OKX REST types/constants/functions, signing test helpers, transport raw sender |
| Parent retained | public facade, child module wiring, constants, public types, and production code |

## Equivalent Behavior

The extracted child must preserve:

- existing test names and ignore markers;
- local serialization/profile/path assertions;
- compile-only function item checks;
- credential lookup order;
- retry behavior for ignored live smoke probes.

## Exclusions

- Do not change live smoke semantics.
- Do not move production code in this step.
- Do not delete tests in this step.
- Do not introduce release-transition links.

## Gates For Actual Extraction

| Gate | Command |
| --- | --- |
| format | `cargo fmt --all --check` |
| executor compile | `cargo check -p quantpilot --bin executor` |
| focused tests | `cargo test -p quantpilot --bin executor okx_` |
| diff hygiene | `git diff --check` |

## Next Step

BE-002MB-02
`root.executor.runner.runner_support_files.okx_rest.test_harness`
actual_extraction.
