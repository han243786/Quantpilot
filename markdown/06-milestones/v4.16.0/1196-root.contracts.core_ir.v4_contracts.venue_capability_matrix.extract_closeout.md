# v4.16.0 root.contracts.core_ir.v4_contracts.venue_capability_matrix actual extraction complete

> Batch: BE-001VI-02
> Node: `root.contracts.core_ir.v4_contracts.venue_capability_matrix`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `extract_closeout`
> Movement: Code movement completed.

---

## Summary

`root.contracts.core_ir.v4_contracts.venue_capability_matrix` actual extraction is complete.

Implemented movement:

- `qrpc_core_ir/src/v4.rs` now declares `mod venue_capability_matrix;`
- `qrpc_core_ir/src/v4.rs` re-exports the child through `pub use venue_capability_matrix::*;`
- `qrpc_core_ir/src/v4/venue_capability_matrix.rs` owns venue capability DTOs/enums/constants/defaults and matrix methods
- `default_venue_capability_matrix_version` moved into the child and remains `pub(in crate::v4)` for parent
  `V4VersionManifest` serde defaults

Equivalence notes:

- public venue capability surface remains unchanged through the parent re-export
- first-wave capability order, unsupported matrix defaults, validation error strings, and runtime-mode support checks
  remain frozen from BE-001VI-01
- plugin governance, compile-time capability reports, static bundles, runtime mode contract implementation, QS type
  system, version manifest implementation, reproducibility, complexity budget, bridge builders, tests, machine
  contract, machine graph contract, and QS profile remain outside this child

Verification:

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir` (54 passed)

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs`
- `qrpc_core_ir/src/v4/venue_capability_matrix.rs`

**Markers**:
- `BE-001VI-02`

**Next step**:
BE-001VI-03 root.contracts.core_ir.v4_contracts.venue_capability_matrix single_leaf_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
