# v4.16.0 root.contracts.quantscript.analysis_diagnostics parent residual judgment: unsupported_construct_gate

> Batch: BE-002GT-01
> Node: `root.contracts.quantscript.analysis_diagnostics`
> Selected child: `root.contracts.quantscript.analysis_diagnostics.unsupported_construct_gate`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `root.contracts.quantscript.analysis_diagnostics.unsupported_construct_gate` for the next recursive child.

## Current Parent State

Current parent:

- `quantscript/src/analysis_diagnostics/mod.rs`

Current large implementation child:

- `quantscript/src/analysis_diagnostics/analysis.rs`

Closed child:

- `quantscript/src/analysis_diagnostics/diagnostics.rs` is already structurally placed as the diagnostic DTO/span child.

## Residual Candidate Review

| Candidate | Evidence | Decision |
|-----------|----------|----------|
| `unsupported_construct_gate` | Owns unsupported import/function/statement/expression diagnostics, direct recursion detection, and non-universe for-loop checks near the top of `analysis.rs`; it is the first cohesive gate invoked by `analyze_script_module`. | Select |
| `lookahead_index_checks` | Series index and centered window lookahead checks are cohesive but should follow the unsupported construct gate. | Queue |
| `warmup_fetch_checks` | Required warmup and fetch lookback diagnostics share helper extraction but should follow lookahead checks. | Queue |
| `strategy_presence_checks` | Fetch/emit presence is smaller and can be handled after larger diagnostic families. | Queue |
| `data_source_bounds_checks` | Data-source map and index-bound warnings are cohesive but later in file order. | Queue |
| `dead_code_symbol_checks` | Dead-code emit and fetch symbol whitelist are terminal warning families; queue after higher-risk gates. | Queue |

## Selected Child Scope

The next baseline may freeze only:

- `collect_unsupported_construct_diagnostics`
- `collect_unsupported_constructs_from_stmts`
- `collect_unsupported_constructs_from_expr`
- `is_supported_formal_try_target`
- `function_contains_direct_recursion`
- `stmts_contain_direct_recursion`
- `expr_contains_direct_recursion`
- `collect_non_universe_for_loop_diagnostics`
- `collect_non_universe_for_loop_diagnostics_from_hir_stmts`

Likely child file:

- `quantscript/src/analysis_diagnostics/unsupported_construct_gate.rs`

## Forbidden Movement

The next baseline and extraction must not move:

- `ScriptAnalysis`, `analyze_script_module`, or result assembly;
- warmup, series index, centered window, fetch lookback, strategy fetch/emit, index-bound, dead-code emit, or symbol whitelist helpers;
- diagnostic DTO/span types in `diagnostics.rs`;
- evaluator normalization, runtime lowering, v4 static audit, test plans, samples, backend, executor, frontend, plugin metadata, or release transition.

## Split Judgment

This is not a terminal split decision. The selected child is behavior-bearing and part of a high-risk analysis parent, so it should receive a precision baseline before code movement.

## Next Step

BE-002GU-01 `root.contracts.quantscript.analysis_diagnostics.unsupported_construct_gate` baseline_plan.
