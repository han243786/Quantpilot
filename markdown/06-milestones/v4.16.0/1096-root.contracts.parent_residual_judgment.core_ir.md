# v4.16.0 root.contracts parent residual judgment selects core_ir

> Batch: BE-001TF-01
> Node: `root.contracts`
> Parent: `root`
> Selected child: `root.contracts.core_ir`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts` returns to its top-level queue after `qrpc_core` parent closeout.

Decision:

`next_child: root.contracts.core_ir`

## Closed Contracts Children

| Child | Result |
| --- | --- |
| `contracts.api_surface` | Closed. |
| `contracts.qrpc_core` | Closed as parent after child splits and cfg-test residual extraction. |

## Open Contracts Residuals

| Residual | Status |
| --- | --- |
| `contracts.core_ir` | Selected next. Owns `qrpc_core_ir` v1 Core IR and v4 machine/static contract surfaces. |
| `contracts.compiler_bridge` | Queued. |
| `contracts.runtime_support` | Queued. |
| `contracts.quantscript` | Queued. |
| `contracts.plugin_metadata` | Queued. |
| `executor` | Queued outside contracts parent closeout. |

## Selection Rationale

`core_ir` is the next contracts child in the v4.16 queue and has a clear physical owner:

- `qrpc_core_ir/src/lib.rs`;
- `qrpc_core_ir/src/v4.rs`;
- `qrpc_core_ir/Cargo.toml`.

It is not a single compact leaf. The selected child includes v1 Core IR DTOs/helper builders/DAG validation plus a large v4 machine/static-contract surface with validation helpers and tests. It therefore needs a baseline before recursive child judgment.

## White Box Boundary

| Item | Owner | Input | Output | Side effects |
| --- | --- | --- | --- | --- |
| v1 Core IR schema | `core_ir` | strategy metadata, data, indicators, signals, agents, risks, execution, edges | `CoreStrategyIr` data contract | none |
| v1 helper builders | `core_ir` | IDs, periods, comparison ops, thresholds | `SeriesExpr` / `ScalarExpr` helpers | none |
| v1 DAG validation | `core_ir` | explicit Core IR edges | `Result<(), Vec<String>>` cycle validation | none |
| v4 contract surface | `core_ir` | machine/static/runtime/typing/plugin/reproducibility contracts | v4 schema DTOs and validators | none |
| Local tests | `core_ir` | sample contracts/fixtures | regression assertions | none |

## Same-Parent Parallel Judgment

`same_parent_parallel_children` remains inactive for this selection.

Reason: `core_ir` is a new top-level parent-sized child. It must first freeze its own baseline and child queue before any same-parent wave is considered.

## Hard Boundaries

The next `core_ir` baseline must not:

- edit Rust source code;
- change Core IR or v4 contract struct/enum fields, serde shapes, helper behavior, validation behavior, public exports, constants, or tests;
- change qrpc-core, compiler, runtime, QuantScript, backend, executor, frontend, physical `plugins/*`, or release transition;
- introduce direct sibling shortcuts.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `core_ir` owns `qrpc_core_ir/src/lib.rs` and `qrpc_core_ir/src/v4.rs`. |
| parent_child_communication_kept | pass | Downstream crates continue importing through the `qrpc-core-ir` crate. |
| equivalence_baseline_freezable | pass | Public constants, DTOs, helpers, validators, and tests can be frozen before movement. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | pass | `qrpc-core-ir` is a public contract crate consumed by qrpc-core/compiler/runtime. |
| state_machine_phase | pass | v4 machine contracts include state machine DTOs and validation. |
| strategy_branch | guarded | v1 Core IR models strategy graph semantics but no strategy-specific branch is selected. |
| independent_failure_mode | pass | v1 Core IR drift and v4 machine/static contract drift are separable. |
| reuse_pressure | pass | Compiler/runtime support depend on this crate's public surface. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | This child is parent-sized and has multiple independent contract families. |
| communication_cost_rises | no | Baseline first; no code movement yet. |
| local_proof_missing | no | `cargo check/test -p qrpc-core-ir` can prove future moves. |
| line_count_only | no | Selection is based on top-level contract ownership and state-machine surface. |

leaf_split_decision_result

`continue_baseline: true`; `core_ir` is selected as the next contracts parent-sized child.

## Next Step

BE-001TG-01 `root.contracts.core_ir` baseline_plan.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
