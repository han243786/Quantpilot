# v4.16.0 root.contracts.core_ir.v1_contract parent residual judgment closes parent

> Batch: BE-001TR-01
> Node: `root.contracts.core_ir.v1_contract`
> Parent: `root.contracts.core_ir`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v1_contract` is now a closed parent facade.

All planned v1 child leaves have been extracted and closed:

| Child | Status |
| --- | --- |
| `contracts.core_ir.v1_contract.root_graph_contract` | Closed with `stop_split: true`. |
| `contracts.core_ir.v1_contract.data_indicator_expression_contract` | Closed with `stop_split: true`. |
| `contracts.core_ir.v1_contract.policy_execution_contract` | Closed with `stop_split: true`. |
| `contracts.core_ir.v1_contract.test_fixture` | Closed with `stop_split: true`. |

## Parent Closeout Proof

| Check | Result | Evidence |
| --- | --- | --- |
| Parent is facade-only | pass | `qrpc_core_ir/src/v1.rs` now contains child `mod` declarations, `pub use` exports, and `#[cfg(test)] mod tests;`. |
| Production children closed | pass | Root graph, data/expression, and policy/execution leaves are physical owners. |
| Test child closed | pass | `qrpc_core_ir/src/v1/tests.rs` owns the v1 local round-trip fixture. |
| Public API preserved | pass | Crate root still re-exports `v1::*`, and v1 parent re-exports all child surfaces. |
| Verification preserved | pass | BE-001TQ-02 and BE-001TQ-03 passed `cargo test -p qrpc-core-ir`; extraction commits passed smart pre-commit. |

## Residual Boundary

Still outside this closed parent:

- `contracts.core_ir.v4_contracts`;
- `contracts.core_ir` crate facade closeout;
- higher `root.contracts` closeout;
- release transition.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | v1 parent owns only child declarations and facade exports. |
| parent_child_communication_kept | pass | All child access remains through the v1 parent facade. |
| equivalence_baseline_freezable | pass | Child extraction steps froze and verified each moved surface. |

leaf_split_decision_result

`parent_closeout: true`; `v1_contract` is closed and returns control to `root.contracts.core_ir`.

## Next Step

BE-001TS-01 `root.contracts.core_ir` parent_residual_judgment selects `v4_contracts`.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
