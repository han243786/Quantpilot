# v4.16.0 root.contracts.quantscript.analysis_diagnostics.index_bounds_gate extract closeout

> Batch: BE-002HG-02
> Node: `root.contracts.quantscript.analysis_diagnostics.index_bounds_gate`
> Parent: `root.contracts.quantscript.analysis_diagnostics`
> Stage: `actual_extraction`
> governance_next_authority: active

---

## Result

`quantscript/src/analysis_diagnostics/index_bounds_gate.rs` now owns the QuantScript data source lookback map and `QS0404` index-bound diagnostic traversal.

Moved from `quantscript/src/analysis_diagnostics/analysis.rs`:

- `build_data_source_map`
- `check_all_index_bounds`
- `check_index_bounds_from_stmts`
- `check_index_bounds_from_expr`

## Parent Boundary

`quantscript/src/analysis_diagnostics/analysis.rs` remains the parent coordinator. It still owns `ScriptAnalysis`, `analyze_script_module`, diagnostic invocation order, strategy presence checks, dead-code emit checks, and the remaining diagnostic families.

The parent imports only:

- `build_data_source_map`
- `check_all_index_bounds`

`fetch_lookback` remains owned by `warmup_fetch_gate`; the analysis_diagnostics parent exposes it through an internal `pub(in crate::analysis_diagnostics)` bridge so this child does not call the warmup sibling directly.

## Unmoved Families

The extraction did not move:

- strategy fetch/emit presence checks;
- dead-code emit checks;
- diagnostic DTO/span types;
- unsupported construct, lookahead-window, warmup/fetch, indirect-recursion, symbol-whitelist, and fetch-lookback-warning children already closed;
- evaluator normalization, runtime lowering, v4 static audit, tests, authoring samples, backend, executor, frontend, plugin metadata, or release transition.

## Validation

Passed before closeout:

- `cargo fmt --check`
- `cargo test -p quantscript` (88 tests)
- `cargo check -p quantpilot`
- UTF-8 check

Docs gates should run before commit:

- `git diff --check`
- full feature tree check
- matrix governance check

## Next Step

BE-002HG-03 `root.contracts.quantscript.analysis_diagnostics.index_bounds_gate` single_leaf_closeout.
