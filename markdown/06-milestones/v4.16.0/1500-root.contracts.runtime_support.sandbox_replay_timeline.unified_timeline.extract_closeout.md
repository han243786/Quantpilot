# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline.unified_timeline extract closeout

> Batch: BE-002CQ-01
> Node: `root.contracts.runtime_support.sandbox_replay_timeline.unified_timeline`
> Parent: `root.contracts.runtime_support.sandbox_replay_timeline`
> Stage: `actual_extraction`
> Movement: Rust code movement.

---

## Scope

Extract `UnifiedTimeline` from `qrpc_runtime/src/sandbox/timeline.rs` into a dedicated child file.

Moved file:

- `qrpc_runtime/src/sandbox/unified_timeline.rs`

Moved surface:

- `UnifiedTimeline`
- `UnifiedTimeline::new`
- `UnifiedTimeline::from_providers`
- `UnifiedTimeline::collect_at`
- `UnifiedTimeline::len`
- `UnifiedTimeline::is_empty`

## Parent Boundary

`qrpc_runtime/src/sandbox/timeline.rs` now declares and re-exports both timeline children:

- timeline data providers;
- unified timeline.

The timeline parent keeps local timeline tests and preserves the existing parent-mediated import surface used by replay builder code.

## Equivalence

The extraction preserves:

- merged timestamp construction;
- slow and fast trigger index calculation;
- provider vector assembly;
- empty provider failure behavior;
- `collect_at` behavior;
- replay builder and sandbox access through the `timeline` parent surface.

## Gates

Verified for this batch:

- `cargo fmt --check`: pass
- `cargo check -p qrpc-runtime`: pass
- `cargo test -p qrpc-runtime sandbox -- --nocapture`: pass, 21 passed / 2 ignored

Remaining full gates are required before commit:

- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 / full feature tree / matrix governance

## Next Step

BE-002CQ-02 `root.contracts.runtime_support.sandbox_replay_timeline.unified_timeline` single_leaf_closeout.
