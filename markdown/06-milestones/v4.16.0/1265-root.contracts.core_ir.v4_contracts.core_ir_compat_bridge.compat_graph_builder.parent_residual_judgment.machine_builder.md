# v4.16.0 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder parent residual judgment selects machine_builder

> Batch: BE-001WS-01
> Node: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder`
> Parent: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge`
> Stage: `parent_residual_judgment`
> Movement: No code movement. This document selects the compatibility machine-builder child for extraction.

---

## Summary

root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder parent residual judgment selects machine_builder

The next child is `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder.machine_builder`.

It owns compatibility machine construction:

- `build_core_ir_observation_machine`
- `build_core_ir_decision_machine`
- `build_core_ir_execution_machine`
- `compat_machine`
- `compat_machine_metadata`
- `count_memory_field`

It does not own graph id construction, graph edge construction, risk-plane binding, event catalog construction, edge metadata, Core IR edge-label metadata, string-array helper, or compat id sanitization.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | PASS | `machine_builder` has clear Observation/Decision/Execution compatibility machine construction ownership. |
| parent_child_communication_kept | PASS | The graph-builder parent can call machine-builder functions without sibling horizontal calls. |
| equivalence_baseline_freezable | PASS | Existing bridge mapping tests exercise all three compatibility machine builders. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | PARTIAL | The child is private, but its machines are returned by the public bridge report graph. |
| state_machine_phase | PASS | It constructs Observation, Decision, and Execution machine contracts and their shared transition skeleton. |
| strategy_branch | PASS | It maps data/indicator, signal/agent/risk, and execution Core IR groups into distinct machine metadata and memory fields. |
| independent_failure_mode | PASS | Machine construction can evolve independently from graph edge/risk-plane binding and event catalog construction. |
| reuse_pressure | PARTIAL | Future bridge variants may reuse the three machine builders while changing graph assembly. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | PASS | The selected child owns a full machine construction phase, not a helper-only fragment. |
| communication_cost_rises | PASS | Machine builders can accept `CoreStrategyIr` and return `V4MachineContract`; the parent remains the graph assembler. |
| local_proof_missing | PASS | Existing bridge tests validate machine graph construction through the public bridge entrypoint. |
| line_count_only | PASS | Selection is based on machine construction ownership, not line count alone. |

leaf_split_decision_result

`selected_child: machine_builder`

The graph builder parent remains open. Remaining parent surfaces include graph assembly, graph edge construction, risk-plane binding, edge metadata, Core IR edge-label metadata, string-array helper, compat id sanitization, and the closed `event_catalog_builder`.

next_recursive_step

BE-001WT-01 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder.machine_builder baseline_plan
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/core_ir_compat_bridge/compat_graph_builder.rs`
- `qrpc_core_ir/src/v4/core_ir_compat_bridge/compat_graph_builder/event_catalog_builder.rs`

**Markers**:
- `BE-001WS-01`

**Next step**:
BE-001WT-01 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder.machine_builder baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
