# v4.16.0 root.contracts.quantscript parent residual judgment: analysis_diagnostics

> Batch: BE-002GR-01
> Node: `root.contracts.quantscript`
> Selected child: `root.contracts.quantscript.analysis_diagnostics`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `root.contracts.quantscript.analysis_diagnostics` for the next recursive child.

## Current Parent State

Closed QuantScript children:

- `root.contracts.quantscript.syntax_ast_surface`
- `root.contracts.quantscript.legacy_config_compat`
- `root.contracts.quantscript.typed_resolution`

Current parent surfaces:

- `quantscript/src/lib.rs`
- `quantscript/src/analysis.rs`
- `quantscript/src/diagnostics.rs`
- `quantscript/src/evaluator.rs`
- `quantscript/src/lowering/*`
- `quantscript/src/v4_static_audit.rs`
- `quantscript/src/test_plan.rs`
- authoring and boundary samples

## Residual Candidate Review

| Candidate | Evidence | Decision |
|-----------|----------|----------|
| `analysis_diagnostics` | `analysis.rs` owns unsupported construct checks, lookahead/warmup/fetch/emit checks, recursion/dead-code checks, and analysis result assembly; `diagnostics.rs` owns shared diagnostic DTOs and spans used by analysis and resolver/lowering surfaces. | Select |
| `evaluator_normalization` | `evaluator.rs` is large and important, but analysis diagnostics is earlier in the frozen QuantScript child queue and supplies diagnostic guardrails before normalization/lowering. | Queue |
| `runtime_lowering` | The lowering subtree is large and recursive; it should be handled after diagnostics and normalization boundaries are stable. | Queue |
| `v4_static_audit` | Important standalone surface, but it depends on prior typed/analysis/lowering contracts. | Queue |
| `test_plan_surface` | Smaller explicit surface; queue after production analysis/lowering owners. | Queue |
| `authoring_docs_samples` | Documentation/sample boundary; queue after production code surfaces. | Queue |

## Selected Child Scope

The next baseline may freeze analysis and diagnostic DTO boundaries:

- `quantscript/src/analysis.rs`
- `quantscript/src/diagnostics.rs`
- public exports from `quantscript/src/lib.rs` related to analysis and diagnostics
- local analysis tests currently owned by analysis or crate-level test surfaces

Likely child shape:

- `quantscript/src/analysis_diagnostics.rs` as a parent facade, or
- `quantscript/src/analysis_diagnostics/` with separate analysis and diagnostic type children if terminal-leaf control requires precision.

## Forbidden Movement

The next baseline and extraction must not move:

- syntax/AST, legacy config compatibility, or typed-resolution children already closed;
- evaluator normalization from `evaluator.rs`;
- runtime lowering subtree from `quantscript/src/lowering/*`;
- v4 static audit and runtime handoff report generation;
- test-plan extraction;
- authoring docs, samples, backend, executor, frontend, plugin metadata, or release transition.

## Split Judgment

This is not a terminal split decision. The selected child needs a baseline first because `analysis.rs` is large and may need recursive internal splitting after the parent child boundary is established.

## Next Step

BE-002GS-01 `root.contracts.quantscript.analysis_diagnostics` baseline_plan.
