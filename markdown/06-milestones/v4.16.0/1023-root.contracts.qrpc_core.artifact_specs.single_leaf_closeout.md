# v4.16.0 root.contracts.qrpc_core.artifact_specs single leaf closeout

> Batch: BE-001RS-03
> Node: `root.contracts.qrpc_core.artifact_specs`
> Parent: `root.contracts.qrpc_core`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core.artifact_specs` has been evaluated after BE-001RS-02 extraction.

Decision:

`continue_split: true`

The node is a coherent qrpc-core artifact spec parent, but it is not yet a final leaf. It contains multiple stable public schema/helper families that can be split further without changing the crate-root facade.

## Split Rule Evaluation

| Rule | Result |
| --- | --- |
| Physical owner | Continue. The selected surface is isolated in `qrpc_core/src/artifact_specs.rs`, but the file is now a parent-sized module. |
| Public method count | Continue. The module owns canonical digest behavior plus run/backtest projection constructors. |
| Mixed responsibility | Continue. Canonical digest, run/backtest specs, and artifact bundle DTOs are related but separable public schema families. |
| State ownership | Stop pressure absent. The module owns no runtime mutable state, locks, async lifecycle, or persistence. |
| Side effects | Stop pressure absent. No IO, filesystem writes, network calls, process lifecycle, or public API mutation are involved. |
| Parent-mediated dependency | Covered. Further children can be mediated by `artifact_specs.rs` and re-exported through the crate root. |
| Future reopen rule | The parent remains open until canonical digest, run/backtest specs, and artifact bundle DTO ownership are each judged. |

## Proposed Child Queue

| Child | Ownership |
| --- | --- |
| `contracts.qrpc_core.artifact_specs.canonical_digest` | `ArtifactDigestAlgorithm`, `ArtifactDigest`, and `canonical_json_sha256_digest`. |
| `contracts.qrpc_core.artifact_specs.run_backtest_specs` | Run/backtest modes, dataset and execution assumption projections, market data snapshot specs, `RunSpec`, and `BacktestSpec`. |
| `contracts.qrpc_core.artifact_specs.artifact_bundle_contract` | Strategy/core-IR/compile artifact DTOs and `CompileArtifactBundle`. |

## White-Box Boundary

| Input | Processing owner | Output |
| --- | --- | --- |
| Artifact schema and digest proposals | `contracts.qrpc_core.artifact_specs` parent facade | Stable digest helpers, run/backtest schemas, projection constructors, and artifact bundle contracts |

The parent may describe and guard:

- child module declarations and re-exports;
- sibling ordering and parent-mediated dependencies;
- public crate-root facade preservation;
- no runtime IO/RFC/test movement until qrpc_core parent selects those residuals.

## Rust Local Fields

| Field | Result |
| --- | --- |
| Crate | `qrpc-core` |
| Parent facade | `qrpc_core/src/artifact_specs.rs` should become the local parent facade for digest/spec/bundle children. |
| Crate facade | `qrpc_core/src/lib.rs` remains the crate-root coordinator. |
| Public exports | Existing crate-root exports must remain preserved through `pub use artifact_specs::*;`. |
| Production dependency check | Passed. Runtime IO and tests consume artifact names through the crate-root facade. |

## Non-Claims

This closeout does not claim:

- canonical digest, run/backtest specs, or artifact bundle subchildren have been extracted;
- runtime IO contracts, RFC execution contracts, digest behavior, or crate-root tests changed;
- qrpc_core parent residual judgment is complete;
- closed error/proto/plugin/strategy IR/protocol primitive/runtime config contracts changed;
- `qrpc_core_ir`, `qrpc_compiler`, `qrpc_runtime`, `quantscript`, backend, executor, frontend, physical `plugins/*`, or release transition changed.

## Proof

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-001RT-01 `root.contracts.qrpc_core.artifact_specs` parent_residual_judgment selects `canonical_digest`.
