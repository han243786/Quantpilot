# v4.16.0 root.contracts.compiler_bridge.runtime_protocol_lowering parent residual judgment selects intent_signal_lowering

> Batch: BE-001XI-01
> Node: `root.contracts.compiler_bridge.runtime_protocol_lowering`
> Parent: `root.contracts.compiler_bridge`
> Stage: `parent_residual_judgment`
> Movement: no Rust code movement; parent queue selection only.

---

## Summary

`root.contracts.compiler_bridge.runtime_protocol_lowering` selects `intent_signal_lowering` as its first internal child.

This child owns runtime intent to Core IR indicator/signal conversion, structured condition lowering, comparison metadata decoding, runtime spread-threshold condition lowering, and fallback raw-text condition descriptions.

The runtime protocol orchestration/data-binding phase, agent policy lowering, risk policy lowering, parent public facade, validation child, Strategy IR lowering, shared spread/custom helpers, tests, and release transition remain outside this selected child.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | Selected child is `root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering`. |
| parent_child_communication_kept | pass | Child remains under `runtime_protocol_lowering`; parent child can call it without sibling links. |
| equivalence_baseline_freezable | pass | Runtime intent/signal outputs are covered by qrpc-compiler tests for MA, RSI, Momentum, ZScore, QuoteObserve spread, and fallback behavior. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | true | Public runtime lowering output depends directly on this private conversion phase. |
| state_machine_phase | true | Intent to indicator/signal conversion is a distinct runtime lowering phase after data binding and before agent/risk policy projection. |
| strategy_branch | true | Runtime intent/signal conversion is separate from Strategy IR signal conversion and from runtime agent/risk projection. |
| independent_failure_mode | true | Intent kind lowering and structured comparison decoding can fail independently from agent/risk policy projection. |
| reuse_pressure | true | Comparison decoding and fallback description are reused across multiple runtime intent branches. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | false | The selected child owns a stable method group, not a tiny helper-only fragment. |
| communication_cost_rises | false | Parent-mediated calls can keep orchestration in `runtime_protocol_lowering` and conversion in the child. |
| local_proof_missing | false | Existing qrpc-compiler tests assert representative intent/signal lowering outputs. |
| line_count_only | false | Selection is based on behavior and failure domain; size is only supporting evidence. |

leaf_split_decision_result

`selected: intent_signal_lowering`

Next recursive step must freeze the child baseline before Rust movement.

next_recursive_step

BE-001XJ-01 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering baseline_plan
## Boundary

**Real files**:
- `qrpc_compiler/src/runtime_protocol_lowering.rs`

**Markers**:
- `BE-001XI-01`
- `selected intent_signal_lowering`
- `agent risk orchestration remains queued`
- `no Rust code movement`

**Next step**:
BE-001XJ-01 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
