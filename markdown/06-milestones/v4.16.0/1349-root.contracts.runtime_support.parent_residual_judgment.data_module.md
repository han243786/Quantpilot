# v4.16.0 root.contracts.runtime_support parent residual judgment selects data_module

> Batch: BE-001YX-01
> Node: `root.contracts.runtime_support`
> Stage: `parent_residual_judgment`
> Selected child: `root.contracts.runtime_support.data_module`
> Movement: No code movement.

## Context

`runtime_facade_coordinator` is closed. The next open child in the frozen runtime-support queue is `data_module`.

## Selected Child

`root.contracts.runtime_support.data_module`.

This child owns `qrpc_runtime/src/data_module.rs`, including:

- `DataCollectionRequest`, `DataCollectionOutput`, and `DataModuleProvider`
- `BuiltinDataModule` collection flow
- Core IR data-binding to `DataSourceConfig` restoration
- data source fetch diagnostics and quality snapshot construction
- quote/kline normalization
- OKX and Binance endpoint construction and payload parsing
- mock quote/kline generation and deterministic pseudo-random support
- request-interval cache and historical cache persistence
- data module unit tests

## Selection Reason

The file is the next largest independent runtime-support behavior family after the coordinator facade.

It has independent failure modes:

- external HTTP exchange payload errors
- source-hint parsing and data binding recovery
- cache TTL and request interval behavior
- data-quality flags and runtime events
- mock fallback and historical cache restore/persist
- provider trait compatibility with the coordinator

## Split Decision Gate

This child must continue through baseline before any movement.

Initial likely sub-leaves:

- provider facade and collection orchestration
- source hint and data source mapping
- diagnostics and data quality snapshot
- exchange endpoint and payload parsing
- mock data generation
- historical cache
- test harness

The baseline step may refine this queue.

## Equivalence Baseline

Required gates for the next movement:

- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-001YY-01 `root.contracts.runtime_support.data_module` baseline_plan.
