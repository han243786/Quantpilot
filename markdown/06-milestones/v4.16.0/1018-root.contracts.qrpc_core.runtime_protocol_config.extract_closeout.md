# v4.16.0 root.contracts.qrpc_core.runtime_protocol_config extract closeout

> Batch: BE-001RQ-02
> Node: `root.contracts.qrpc_core.runtime_protocol_config`
> Parent: `root.contracts.qrpc_core`
> Stage: `extract_closeout`
> Movement: Rust code extraction.

---

## Summary

`root.contracts.qrpc_core.runtime_protocol_config` has been extracted from `qrpc_core/src/lib.rs` into a private child module.

The qrpc-core crate root preserves the public facade:

- `mod runtime_protocol_config;`
- `pub use runtime_protocol_config::*;`

## Files Changed

- `qrpc_core/src/lib.rs`
- `qrpc_core/src/runtime_protocol_config.rs`

## Extraction Result

`qrpc_core/src/runtime_protocol_config.rs` now owns:

- `DataQualitySnapshot`;
- `RuntimeEventType`;
- `DataSourceConfig`;
- the private serde helper `is_false`;
- `UniverseAssetMetadataPoint`;
- `UniverseAssetRecord`;
- `UniverseSnapshot`;
- `IntentConfig`;
- `AgentConfig`;
- `RiskConfig`;
- risk and paper execution default constants;
- `RuntimeProtocolCoreConfig` and its `Default` implementation;
- `CompiledRuntimeProtocol`.

`qrpc_core/src/lib.rs` keeps later qrpc-core residuals in place:

- artifact digest and artifact spec DTOs;
- dataset and execution assumption projections;
- runtime IO DTOs;
- RFC execution contracts;
- crate-root tests.

## Rust Local Fields

| Field | Result |
| --- | --- |
| Crate | `qrpc-core` |
| Parent facade | Preserved. `lib.rs` remains the crate root facade and re-export owner. |
| Child visibility | The module is private; public item surfaces are preserved through `pub use runtime_protocol_config::*;`. |
| Public exports | Existing crate-root public names remain available through `qrpc_core::*`. |
| Parent-owned residuals | Artifact specs, runtime IO contracts, RFC execution contracts, and tests remain parent-owned for later recursion. |
| Sibling dependency check | Passed. The child imports protocol primitives and `CoreStrategyIr` through crate-level public names and does not import sibling internals. |

## Equivalence Preservation

| Surface | Preserved evidence |
| --- | --- |
| Runtime config DTO fields | Struct fields and serde attributes moved unchanged. |
| Universe config DTOs | Field names, options, vectors, and serde defaults moved unchanged. |
| Defaults | Risk and paper execution constants plus `RuntimeProtocolCoreConfig::default` moved unchanged. |
| Compiled protocol container | `CompiledRuntimeProtocol` fields moved unchanged. |
| Public facade | `pub use runtime_protocol_config::*;` preserves crate-root access. |
| Later qrpc-core schemas | Artifact specs, runtime IO, RFC contracts, digest behavior, and tests were not moved. |

## Hard Boundaries Held

BE-001RQ-02 did not:

- change runtime config field names, serde attributes, default values, public exports, or compiled protocol container shape;
- move artifact digest/spec DTOs, dataset projections, execution assumption specs, runtime IO DTOs, RFC execution contracts, or tests;
- change closed error/proto/plugin/strategy IR/protocol primitive contracts;
- change `qrpc_core_ir`, `qrpc_compiler`, `qrpc_runtime`, `quantscript`, backend, executor, frontend, physical `plugins/*`, or release transition.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-pre-commit-hook.ps1`

## Next Step

BE-001RQ-03 `root.contracts.qrpc_core.runtime_protocol_config` single_leaf_closeout.
