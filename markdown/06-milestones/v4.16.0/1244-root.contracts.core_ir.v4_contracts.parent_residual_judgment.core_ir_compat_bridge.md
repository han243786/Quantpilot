# v4.16.0 root.contracts.core_ir.v4_contracts parent residual judgment selects core_ir_compat_bridge

> Batch: BE-001WH-01
> Node: `root.contracts.core_ir.v4_contracts`
> Parent: `root.contracts.core_ir`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts` selects
`root.contracts.core_ir.v4_contracts.core_ir_compat_bridge` as the next child.

The selected child owns the remaining Core IR v4 compatibility bridge surface currently in `qrpc_core_ir/src/v4.rs`:

- `CoreIrV4CompatibilityReport`
- `CoreIrV4BridgeVerdict`
- `CoreIrV4BridgeDiagnosticSeverity`
- `CoreIrV4BridgeDiagnostic`
- `CoreIrV4CompatibilityReport::validate_for_phase4`
- `bridge_core_ir_to_v4_machine_graph`
- Core IR input validation helpers
- Core IR reference validation helpers
- Core IR compatibility graph/machine/event builders
- `push_core_ir_v4_bridge_diagnostic`
- bridge-specific serde/default helpers used by the bridge builders

The selected child does not own already extracted v4 contract families, compile-time capability reports, static bundle
contracts, tests, sample builders, or crate-level v1/Core IR definitions.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `core_ir_compat_bridge` is a named white-box child for v1 Core IR to v4 machine graph compatibility reporting. |
| parent_child_communication_kept | guarded | The child depends on extracted v4 schema families through the v4 facade and crate-level Core IR types through `crate::`. |
| equivalence_baseline_freezable | pass | Bridge report DTOs, diagnostics, validation branches, and graph/machine/event builders are finite. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | `bridge_core_ir_to_v4_machine_graph` and bridge reports remain public through `qrpc_core_ir::v4::*`. |
| state_machine_phase | satisfied | The bridge builds v4 Observation/Decision/Execution machines and event catalog graph topology. |
| strategy_branch | no | This is compatibility translation, not strategy branch behavior. |
| independent_failure_mode | pass | Core IR bridge diagnostics and graph construction fail independently from static bundle and capability report checks. |
| reuse_pressure | pass | Bridge tests reuse report validation and generated graph behavior. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The child owns public bridge DTOs plus validation and construction behavior. |
| communication_cost_rises | no | One bridge parent keeps Core IR validation and graph construction under a clear compatibility facade. |
| local_proof_missing | no | Existing qrpc-core-ir tests cover bridge rejection and accepted graph mapping. |
| line_count_only | no | Selection is based on compatibility bridge ownership. |

leaf_split_decision_result

`continue_split: true`

Selected next child:

`root.contracts.core_ir.v4_contracts.core_ir_compat_bridge`

next_recursive_step

BE-001WI-01 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge baseline_plan
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs`

**Markers**:
- `BE-001WH-01`

**Next step**:
BE-001WI-01 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
