# v4.16.0 root.contracts.qrpc_core.artifact_specs.artifact_bundle_contract extract closeout

> Batch: BE-001RY-02
> Node: `root.contracts.qrpc_core.artifact_specs.artifact_bundle_contract`
> Parent: `root.contracts.qrpc_core.artifact_specs`
> Stage: `extract_closeout`
> Movement: Rust code extraction.

---

## Summary

`root.contracts.qrpc_core.artifact_specs.artifact_bundle_contract` has been extracted from `qrpc_core/src/artifact_specs.rs` into a private child module.

The artifact specs parent preserves the local public facade:

- `mod artifact_bundle_contract;`
- `pub use artifact_bundle_contract::*;`

The qrpc-core crate root remains unchanged and continues to preserve public exports through `pub use artifact_specs::*;`.

## Files Changed

- `qrpc_core/src/artifact_specs.rs`
- `qrpc_core/src/artifact_specs/artifact_bundle_contract.rs`

## Extraction Result

`qrpc_core/src/artifact_specs/artifact_bundle_contract.rs` now owns:

- `StrategyArtifactSourceKind`;
- `StrategyArtifact`;
- `CoreIrArtifact`;
- `CompileArtifact`;
- `CompileArtifactBundle`.

`qrpc_core/src/artifact_specs.rs` is now a local parent facade over:

- `artifact_bundle_contract`;
- `canonical_digest`;
- `run_backtest_specs`.

## Rust Local Fields

| Field | Result |
| --- | --- |
| Crate | `qrpc-core` |
| Parent facade | Preserved. `artifact_specs.rs` now only declares and re-exports artifact specs children. |
| Child visibility | The module is private; public bundle surfaces are preserved through `pub use artifact_bundle_contract::*;`. |
| Crate public exports | Preserved through the existing crate-root `pub use artifact_specs::*;`. |
| Parent-owned residuals | No artifact specs residuals remain after this extraction. |
| Sibling dependency check | Passed. The child uses digest through the artifact specs parent facade and does not import sibling internals directly. |

## Equivalence Preservation

| Surface | Preserved evidence |
| --- | --- |
| Source taxonomy | Enum variants and snake_case serde remain unchanged. |
| Strategy artifact | Field names, metadata default, and digest field remain unchanged. |
| Core IR artifact | Field names and core IR payload field remain unchanged. |
| Compile artifact | Protocol/config/artifact id/digest/runtime config field shape remains unchanged. |
| Bundle contract | `strategy`, `compile`, and `core_ir` fields remain unchanged. |
| Public facade | Artifact specs and crate-root re-exports preserve public access. |

## Hard Boundaries Held

BE-001RY-02 did not:

- change enum variants, serde attributes, field names, bundle shape, public exports, or tests;
- move runtime IO DTOs, RFC execution contracts, or crate-root tests;
- change closed canonical digest, run/backtest specs, other qrpc-core children, or release transition.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-pre-commit-hook.ps1`

## Next Step

BE-001RY-03 `root.contracts.qrpc_core.artifact_specs.artifact_bundle_contract` single_leaf_closeout.
