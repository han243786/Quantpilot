# v4.16.0 root.contracts.quantscript.runtime_lowering.manual_formula_fallback parent residual judgment: manual_macd_formula

> Batch: BE-002JJ-01
> Node: `root.contracts.quantscript.runtime_lowering.manual_formula_fallback`
> Selected child: `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_macd_formula`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `manual_macd_formula` as the next internal child under
`manual_formula_fallback`.

`manual_rsi_formula` and `manual_momentum_formula` are closed. The next cohesive
owner is MACD manual fallback recognition: MACD line, MACD histogram, EMA spread
matching, and MACD line/signal pair matching all resolve to MACD binding
variants and share source/period validation semantics. Extracting this child
next preserves the subtract-branch recognizer order while removing the largest
remaining subtract fallback owner before the divide-branch ZScore/MA owners are
touched.

## White-Box Boundary

| Field | Boundary |
| --- | --- |
| Input | candidate expression, subtract operands, `BindingEnv`, and data sources |
| Output | `IndicatorBinding::MacdLine`, `IndicatorBinding::Macd`, or no-match |
| Current owner | `quantscript/src/lowering/fallback.rs` |
| Planned owner | `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_macd_formula` |
| Parent caller | `resolve_manual_formula_binding` subtract branch and direct helper users |
| Exclusions | ZScore, moving-average, boundary lookback public helper, fallback tests, closed RSI, and closed momentum |

## Symbols Approved For Next Baseline

The next baseline may freeze movement for:

- `EmaSpreadMatch`
- `MacdLineSignalMatch`
- `manual_macd_line_from_expr`
- `manual_macd_histogram_from_expr`
- `match_ema_spread`
- `match_macd_line_signal_pair`

## Split Decision Gate

| Gate | Result | Evidence |
| --- | --- | --- |
| `parent_precision_open` | PASS | `manual_formula_fallback` remains a precision parent after RSI and momentum closeout |
| `queue_order` | PASS | Parent baseline queue lists `manual_macd_formula` after `manual_momentum_formula` |
| `recognizer_order_preserved` | PASS | MACD histogram and line remain before momentum in subtract branches |
| `strong_owner_boundary` | PASS | All selected symbols produce MACD binding variants or MACD pair matches |
| `sibling_exclusion_clear` | PASS | ZScore, MA/window, boundary lookback public helper, tests, RSI, and momentum are excluded |

## Risk Classification

| Risk | Result |
| --- | --- |
| Public API / route / schema | Public(crate) MACD helpers must remain stable |
| Persistence / lock owner | No persistence ownership movement |
| Trading semantics evidence | High; MACD fallback affects inferred runtime indicators |
| Cross-crate behavior | Present through `IndicatorBinding`, `DataSourceConfig`, and typed expression contracts |

## Next Step

BE-002JK-01 `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_macd_formula` baseline_plan.
