# v4.16.0 root.executor.runner.provider_order_support baseline plan

> Batch: BE-002LP-01
> Node: `root.executor.runner.provider_order_support`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Goal

Freeze the OKX demo provider support boundary before Rust movement.

The first movement may extract provider DTOs, validation helpers, credential
loading, provider error shaping, audit payload helpers, and response projection
from `src-executor/main.rs` into `src-executor/provider_order_support.rs`.

## Frozen Boundary

| Field | Value |
| --- | --- |
| Planned file | `src-executor/provider_order_support.rs` |
| Approved helper surface | OKX demo provider request DTOs, credential set, default field helpers, provider mode gate, order request builder, field cleaners, credential loaders, provider error helpers, audit payload helpers, provider response helper |
| Parent callers | `provider_order_routes` and existing tests |
| Parent retained | route registration, process startup, `append_audit`, closed route modules, runner support files |

## Equivalent Behavior

The extracted child must preserve:

- paper-actual provider mode gate behavior;
- OKX demo order request validation and cleaning behavior;
- credential loading precedence and error behavior;
- provider error response shaping behavior;
- audit payload redaction behavior;
- provider response projection behavior.

## Exclusions

- Do not reopen `provider_order_routes`; it remains route-handler-only.
- Do not move lifecycle, market stream, or v4 deploy support leaves.
- Do not move runner setup/support files.
- Do not introduce release-transition links.

## Gates For Actual Extraction

| Gate | Command |
| --- | --- |
| format | `cargo fmt --all --check` |
| executor compile | `cargo check -p quantpilot --bin executor` |
| diff hygiene | `git diff --check` |

## Next Step

BE-002LP-02 `root.executor.runner.provider_order_support`
actual_extraction.
