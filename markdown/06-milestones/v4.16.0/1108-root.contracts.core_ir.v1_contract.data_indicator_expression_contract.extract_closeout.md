# v4.16.0 root.contracts.core_ir.v1_contract.data_indicator_expression_contract extract closeout

> Batch: BE-001TM-02
> Node: `root.contracts.core_ir.v1_contract.data_indicator_expression_contract`
> Parent: `root.contracts.core_ir.v1_contract`
> Stage: `extract_closeout`
> Movement: Rust code moved under the same v1 parent facade.

---

## Summary

`data_indicator_expression_contract` now owns the v1 data binding, indicator, expression taxonomy, scalar comparison, signal kind, expression helper builders, and custom-expression confidence default that were previously embedded in `qrpc_core_ir/src/v1.rs`.

Created:

- `qrpc_core_ir/src/v1/data_indicator_expression_contract.rs`

Updated:

- `qrpc_core_ir/src/v1.rs`

## Moved Surface

| Surface | Result |
| --- | --- |
| `DataBinding` and `DataBindingKind` | Moved into `data_indicator_expression_contract.rs` unchanged. |
| `IndicatorNode` and `CoreIndicatorKind` | Moved into `data_indicator_expression_contract.rs` unchanged. |
| `CustomExprSpec`, `CustomPredicateExpr`, `CustomValueExpr` | Moved into `data_indicator_expression_contract.rs` unchanged. |
| arithmetic, series, alignment, spread, signal, scalar, comparison enums | Moved into `data_indicator_expression_contract.rs` unchanged. |
| `close_series_expr` | Moved into `data_indicator_expression_contract.rs` unchanged. |
| `moving_average_series_expr` | Moved into `data_indicator_expression_contract.rs` unchanged. |
| `moving_average_compare_expr` | Moved into `data_indicator_expression_contract.rs` unchanged. |
| `indicator_threshold_compare_expr` | Moved into `data_indicator_expression_contract.rs` unchanged. |
| `default_custom_confidence` | Moved with `CustomExprSpec` so the serde default remains local to the child. |
| v1 parent facade | Added `mod data_indicator_expression_contract;` and `pub use data_indicator_expression_contract::*;`. |

## Preserved Parent Residuals

These declarations intentionally remain in `qrpc_core_ir/src/v1.rs`:

- `SignalRule`;
- `AgentPolicy`, `AgentPolicyKind`, `RebalanceSchedule`;
- `RiskPolicy`;
- `ExecutionSizingKind`, `CoreTimeInForce`, `ExecutionRule`;
- local v1 regression tests.

## Equivalence Proof

| Check | Result | Evidence |
| --- | --- | --- |
| Public names preserved | pass | `qrpc_core_ir/src/v1.rs` re-exports `data_indicator_expression_contract::*`, and crate root still re-exports `v1::*`. |
| Serde shape preserved | pass | All derives, `serde` tags, defaults, rename rules, and field names were mechanically moved unchanged. |
| Helper behavior preserved | pass | Helper function bodies were mechanically moved unchanged. |
| Parent-mediated dependency preserved | pass | `SignalRule` remains in the v1 parent and reaches moved `SignalKind`/`ScalarExpr` through the parent re-export namespace. |
| Sibling child dependency preserved | pass | `root_graph_contract` continues importing `DataBinding` and `IndicatorNode` through `super::...`. |
| Release transition avoided | pass | No cross-child direct wiring, performance shortcut, or release-transition proposal was introduced. |

## Parent-Child Communication

The v1 parent remains the local gateway:

- external consumers continue through `qrpc_core_ir::*`;
- v1-local consumers continue through `v1` parent re-exports;
- root graph and policy/test residuals communicate with the extracted expression child through `super::...` or the parent namespace.

## Hard Boundary Result

No struct field, enum variant, serde shape, helper behavior, test assertion, v4 contract, qrpc-core behavior, compiler behavior, runtime behavior, backend behavior, executor behavior, frontend behavior, physical `plugins/*`, or release-transition pathway was intentionally changed.

## Next Step

BE-001TM-03 `root.contracts.core_ir.v1_contract.data_indicator_expression_contract` single_leaf_closeout.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
