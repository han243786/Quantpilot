# v4.16.0 simulated_execution_engine.runtime_adapter extract closeout

> Batch: BE-002EF-01
> Node: `root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine.runtime_adapter`
> Parent: `root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> governance_next_trial: true

---

## Decision

Extract `runtime_adapter` into its own child module.

## Code Movement

Added:

- `qrpc_runtime/src/v4_runtime/simulated_execution_engine/runtime_adapter.rs`

Changed:

- `qrpc_runtime/src/v4_runtime/simulated_execution_engine.rs`

The parent `simulated_execution_engine` module now keeps only:

- child module declaration;
- parent imports;
- the simulated execution implementation include.

The new `runtime_adapter` child owns the `impl V4PaperSimulatedRuntime` adapter layer.

## Visibility

Methods called by v4 runtime sibling modules use `pub(in crate::v4_runtime)` after the extra submodule level:

- `apply_runtime_simulated_execution_for_transition`;
- `validate_simulated_order_capabilities`;
- `record_simulated_execution_events`.

This preserves parent-domain access without public API expansion.

## Verification

Observed gates:

- `cargo fmt --check`: PASS
- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime v4 -- --nocapture`: PASS, 32 tests

Remaining commit gates:

- `cargo check -p quantpilot`;
- `git diff --check`;
- UTF-8 check;
- full feature tree check;
- matrix governance check.

## Next Step

BE-002EF-02 `root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine.runtime_adapter` single_leaf_closeout.
