# v4.16.0 root.contracts.runtime_support.risk_support.exposure_math_helpers baseline plan

> Batch: BE-002CD-01
> Node: `root.contracts.runtime_support.risk_support.exposure_math_helpers`
> Parent: `root.contracts.runtime_support.risk_support`
> Stage: `baseline_plan`
> Movement: No Rust code movement.

---

## Scope

Freeze the baseline for extracting shared exposure math helpers.

Allowed movement:

- add `qrpc_runtime/src/risk_checker/exposure_math_helpers.rs`;
- add a parent child declaration in `qrpc_runtime/src/risk_checker.rs`;
- move only the selected exposure/equity helpers;
- keep all moved helpers as parent-mediated `pub(super)` helpers.

## In-Scope Helpers

- `available_sell_ratio`
- `portfolio_equity`
- `symbol_net_exposure_ratio`
- `portfolio_net_exposure_ratio`

## Out of Scope

- provider trait and public evaluate surface;
- `evaluate_risk_decision`;
- `enforce_risk_mode`;
- closed event payload projection child;
- closed direction/cross-symbol constraints child;
- closed action clamp child;
- closed portfolio target clamp child;
- tests.

## Dependency Rule

The parent imports these helpers and closed children continue to access them via parent-mediated `super::*`. No child may import this sibling directly and no release-transition shortcut may be introduced.

## Equivalence Baseline

The next actual extraction must preserve:

- sell availability ratio calculation;
- portfolio equity formula;
- symbol net exposure ratio formula;
- portfolio net exposure ratio formula;
- provider/evaluate public surface.

## Gates

Required gates for actual extraction:

- `cargo fmt`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime risk -- --nocapture`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 / full feature tree / matrix governance

## Next Step

BE-002CE-01 `root.contracts.runtime_support.risk_support.exposure_math_helpers` actual_extraction.
