# v4.16.0 root.contracts.qrpc_core.rfc_execution_contracts.data_request single leaf closeout

> Batch: BE-001SS-03
> Node: `root.contracts.qrpc_core.rfc_execution_contracts.data_request`
> Parent: `root.contracts.qrpc_core.rfc_execution_contracts`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`data_request` closes as one compact RFC-001 schema leaf.

The child owns only data request taxonomy enums, time/precision DTOs, usage taxonomy, and `DataRequest`. Further splitting by enum or DTO would fragment a single schema contract without a separate owner, proof surface, or failure mode.

## Closeout Evidence

| Check | Result | Evidence |
| --- | --- | --- |
| Physical owner exists | pass | `qrpc_core/src/rfc_execution_contracts/data_request.rs` owns the selected schema family. |
| Parent facade preserved | pass | `qrpc_core/src/rfc_execution_contracts.rs` re-exports `data_request::*`. |
| Public crate facade preserved | pass | `qrpc_core/src/lib.rs` still re-exports `rfc_execution_contracts::*`. |
| Rust equivalence verified | pass | `cargo check -p qrpc-core`, `cargo test -p qrpc-core`, and smart pre-commit passed in BE-001SS-02. |
| Residual separation | pass | Allocation, order, feedback, and handoff contracts remain outside this closed child. |

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `data_request` is a named RFC-001 schema leaf. |
| parent_child_communication_kept | pass | Parent facade mediates all public exports. |
| equivalence_baseline_freezable | pass | BE-001SS-01 froze variants, fields, derives, serde shape, and public facade behavior. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | pass | The child is public qrpc-core contract API. |
| state_machine_phase | no | No transition or runtime state phase exists here. |
| strategy_branch | no | No strategy branch exists here. |
| independent_failure_mode | weak | All local failures are schema drift within one RFC-001 request family. |
| reuse_pressure | pass | Consumers still use the contract through `qrpc_core::*`. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | yes | Splitting individual taxonomy enums or DTOs would create tiny fragments without distinct ownership. |
| communication_cost_rises | yes | More children would force the parent to coordinate many small schema pieces for no proof gain. |
| local_proof_missing | yes | qrpc-core gates would prove the same public schema surface, not independent sub-schemas. |
| line_count_only | yes | Any further split would mostly be by line count or enum grouping. |

leaf_split_decision_result

`stop_split: true`

`data_request` is closed. The cursor returns to `root.contracts.qrpc_core.rfc_execution_contracts`.

## Next Step

BE-001ST-01 `root.contracts.qrpc_core.rfc_execution_contracts` parent_residual_judgment selects `allocation`.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
