# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation single leaf closeout continues split

> Batch: BE-001UI-03
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation` remains open for deeper split.

The extracted validation file is still behavior-heavy: it owns root graph validation, DAG validation, graph event catalog usage validation, risk-plane validation, and an event-party helper. The next child should isolate `risk_plane_validation`, which has a distinct execution-safety invariant boundary.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `risk_plane_validation` is a named graph safety validation owner. |
| parent_child_communication_kept | pass | It can import graph schema, machine template taxonomy, and constants through the validation parent. |
| equivalence_baseline_freezable | pass | The risk-plane branches and error strings are finite and test-covered. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | The public root method remains in validation parent, but risk-plane helper owns a clear private behavior phase. |
| state_machine_phase | pass | Risk-plane validation is the execution-safety phase of graph validation. |
| strategy_branch | guarded | It protects execution graph semantics consumed by bridge/static bundle paths. |
| independent_failure_mode | pass | Required risk plane, min priority, decision template, inbound edge origin, and missing inbound edge failures are independent. |
| reuse_pressure | guarded | Reuse is currently through root graph validation, but the phase is independently meaningful. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | Risk-plane validation is a real safety invariant owner. |
| communication_cost_rises | no | Moving one helper keeps root validation orchestration simple. |
| local_proof_missing | no | `cargo test -p qrpc-core-ir` passed after graph validation extraction. |
| line_count_only | no | Split is by safety behavior ownership. |

leaf_split_decision_result

`continue_split: true`

Next selected child candidate:

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.risk_plane_validation`

## Boundary

**Real files**:

- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation.rs`

**Markers**:

- `BE-001UI-03`

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-001UJ-01 `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation` parent_residual_judgment selects `risk_plane_validation`.
