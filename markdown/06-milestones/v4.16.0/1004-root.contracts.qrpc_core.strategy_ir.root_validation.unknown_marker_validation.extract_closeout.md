# v4.16.0 root.contracts.qrpc_core.strategy_ir.root_validation.unknown_marker_validation extract closeout

> Batch: BE-001RI-02
> Node: `root.contracts.qrpc_core.strategy_ir.root_validation.unknown_marker_validation`
> Parent: `root.contracts.qrpc_core.strategy_ir.root_validation`
> Stage: `extract_closeout`
> Movement: Rust code extraction.

---

## Summary

`root.contracts.qrpc_core.strategy_ir.root_validation.unknown_marker_validation` has been extracted from `root_validation.rs` into a private child module.

The root validation parent preserves the original validation order:

1. identity/readiness validation;
2. signal and logic validation;
3. risk validation;
4. data requirement and execution validation;
5. unknown marker path/reason validation.

## Files Changed

- `qrpc_core/src/strategy_ir/root_validation.rs`
- `qrpc_core/src/strategy_ir/root_validation/unknown_marker_validation.rs`

## Extraction Result

`qrpc_core/src/strategy_ir/root_validation.rs` now declares:

- `mod data_execution_validation;`
- `mod identity_required_validation;`
- `mod risk_validation;`
- `mod signal_logic_validation;`
- `mod unknown_marker_validation;`

`StrategyIr::validation_errors` now delegates the selected block through:

- `unknown_marker_validation::validate_unknowns(self, &mut errors);`

The new child module owns:

- `unknowns[*].path` required-field validation;
- `unknowns[*].reason` required-field validation;
- the implementation of `validate_unknownable`;
- the implementation of `validate_unknownable_opt`.

The parent keeps private wrapper functions named:

- `validate_unknownable`;
- `validate_unknownable_opt`.

These wrappers delegate to `unknown_marker_validation` so closed sibling children still call through the parent boundary instead of importing this child directly.

## Rust Local Fields

| Field | Result |
| --- | --- |
| Crate | `qrpc-core` |
| Parent facade | Preserved. `root_validation.rs` remains the coordinator for module declarations, validation call order, and sibling helper bridge. |
| Child visibility | Preserved as `pub(super)` for child entry/helper functions; no external public API was added. |
| Public exports | No `pub use` changes. Existing `qrpc_core::strategy_ir::*` and `qrpc_core::*` surfaces remain unchanged. |
| Parent wrapper bridge | Preserved. Closed siblings still import helper wrappers from the parent. |
| Sibling dependency check | Passed. Closed validation children do not import `unknown_marker_validation` directly. |

## Equivalence Preservation

| Surface | Preserved evidence |
| --- | --- |
| Validation ordering | The parent call remains immediately after data/execution validation and before `errors` is returned. |
| Unknown path/reason diagnostics | Message text and path labels moved unchanged into the child module. |
| Unknownable diagnostics | Helper implementation moved unchanged into the child module. |
| Optional unknownable behavior | `None` skip behavior and `Some` delegation are unchanged. |
| Root public API | `StrategyIr::validation_errors` and `StrategyIr::validate` signatures are unchanged. |
| Serde shape | No DTO fields, derives, or serde attributes changed. |

## Hard Boundaries Held

BE-001RI-02 did not:

- change validation ordering, diagnostics, path labels, root DTO fields, public validation methods, serde shape, or local test expectations;
- move local tests or sample fixture;
- remove the parent-mediated helper bridge used by closed sibling children;
- make root validation child modules import each other directly;
- widen visibility beyond `pub(super)`;
- change closed Strategy IR child DTOs/enums/constants/public functions, protocol primitives, runtime IO, compiler/runtime/backend/executor/frontend behavior, physical `plugins/*`, or release transition.

## Gates

- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-pre-commit-hook.ps1`

## Next Step

BE-001RI-03 `root.contracts.qrpc_core.strategy_ir.root_validation.unknown_marker_validation` single_leaf_closeout.
