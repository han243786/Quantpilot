# v4.16.0 root.contracts.runtime_support.v4_runtime_support parent residual judgment - machine_transition_engine

> Batch: BE-002DU-01
> Node: `root.contracts.runtime_support.v4_runtime_support`
> Selected child: `root.contracts.runtime_support.v4_runtime_support.machine_transition_engine`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.

---

## Decision

Select `root.contracts.runtime_support.v4_runtime_support.machine_transition_engine` as the next v4 runtime support child.

## Evidence

Closed v4 runtime support children:

- `type_surface`
- `graph_symbol_expansion`
- `runtime_constructor_mode_gate`
- `event_replay_orchestration`

The BE-002DH-01 v4 runtime support baseline queue names `machine_transition_engine` next.

The candidate currently lives in `qrpc_runtime/src/v4_runtime.rs` and covers:

- selecting matching transitions for parent and child machine state;
- sorting transition candidates by priority and stable id;
- transition preflight around undeclared memory writes;
- applying state and memory updates;
- emitted event payload construction;
- transition matching helpers.

## Boundary

The next baseline must freeze transition mechanics only. It must not move:

- public v4 type surface;
- graph symbol expansion;
- constructor/mode gate behavior;
- replay/input orchestration;
- risk-plane approval/rejection logic;
- execution-capability policy evaluation;
- simulated execution order/fill application;
- v4 tests.

## Dependency Rule

The machine transition child may call parent-owned risk/execution gate and simulated execution hooks, but those hooks remain outside this child until their own residual judgments.

## Next Step

BE-002DV-01 `root.contracts.runtime_support.v4_runtime_support.machine_transition_engine` baseline_plan.
