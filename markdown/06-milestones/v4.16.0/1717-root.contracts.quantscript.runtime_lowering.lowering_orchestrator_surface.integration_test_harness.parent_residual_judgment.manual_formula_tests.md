# v4.16.0 root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness parent residual judgment: manual_formula_tests

> Batch: BE-002IB-01
> Node: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness`
> Selected child: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.manual_formula_tests`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `manual_formula_tests` as the next child under `integration_test_harness`.

The previous closeout kept the test harness split open under `PRECISION`. Among the internal groups, manual formula tests are the largest cohesive owner and cover a distinct lowering responsibility: recognizing formal indicator intent from user-authored helper formulas and rejecting loop-built formulas that cannot be symbolically lowered.

## White-Box Boundary

| Field | Boundary |
| --- | --- |
| Input | Test-only QuantScript fixture strings containing user-defined helper formulas |
| Output | Assertions over inferred `IntentKind`, intent params, and rejection diagnostics for unsupported loop-built formulas |
| Current owner | `quantscript/src/lowering/orchestrator/integration_test_harness.rs` |
| Planned owner | `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.manual_formula_tests` |
| Parent caller | `integration_test_harness` test module only |
| Exclusions | MA/RSI basic smoke, runtime coordinator smoke, semantic missing-helper rejection, exchange data-source decoding, spread lowering, rebalance lowering |

## Candidate Tests

The selected child should freeze and move only these existing tests:

- `lowers_manual_moving_average_helper_formula`
- `lowers_manual_momentum_formula`
- `lowers_manual_zscore_formula`
- `lowers_manual_macd_histogram_formula`
- `lowers_manual_momentum_ratio_formula`
- `lowers_manual_ma_gap_ratio_formula`
- `lowers_manual_rsi_formula_from_rs_ratio`
- `lowers_manual_rsi_formula_with_avg_gain_loss_aliases`
- `lowers_manual_ema_rsi_formula`
- `lowers_manual_cutler_rsi_formula`
- `rejects_manual_rsi_formula_from_loop_built_gain_loss_lists_in_formal_path`
- `rejects_manual_rsi_formula_from_while_loop_gain_loss_lists_in_formal_path`

## Split Decision Gate

| Gate | Result | Evidence |
| --- | --- | --- |
| `micro_leaf_without_owner` | PASS | The group contains 12 scenario tests around the same manual formula lowering responsibility. |
| `communication_cost_rises` | PASS | The child remains test-only and calls only through the parent harness/module boundary. |
| `local_proof_missing` | PASS | `cargo test -p quantscript` directly covers the moved tests. |
| `line_count_only` | PASS | Selection is based on a semantic scenario group, not raw size. |

## Risk Classification

| Risk | Result |
| --- | --- |
| Public API / route / schema | No change planned |
| Persistence / lock owner | No change planned |
| State machine semantics | No change planned |
| Trading semantics evidence | High, test-only; keep precision single-leaf governance |
| Cross-crate behavior | Indirect through runtime lowering output assertions |

## Residual Queue

After this child, remaining candidate groups are:

- `basic_runtime_smoke_tests`
- `spread_lowering_tests`
- `rebalance_lowering_tests`
- `semantic_rejection_tests`

## Next Step

BE-002IC-01 `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.manual_formula_tests` baseline_plan.
