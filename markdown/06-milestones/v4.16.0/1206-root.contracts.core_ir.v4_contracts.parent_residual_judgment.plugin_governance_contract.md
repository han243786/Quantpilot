# v4.16.0 root.contracts.core_ir.v4_contracts parent residual judgment selects plugin_governance_contract

> Batch: BE-001VN-01
> Node: `root.contracts.core_ir.v4_contracts`
> Parent: `root.contracts.core_ir`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts` selects
`root.contracts.core_ir.v4_contracts.plugin_governance_contract` as the next child.

The selected child owns the v4 plugin governance and manifest validation contract currently in
`qrpc_core_ir/src/v4.rs`:

- `PluginGovernanceContract`
- `PluginManifestSpec`
- `PluginKind`
- `PluginManifestField`
- `PluginSideEffect`
- `PluginRuntimePermission`
- `PluginNetworkPermission`
- `impl Default for PluginGovernanceContract`
- `PluginGovernanceContract::validate_static_contract`
- `PluginGovernanceContract::validate_plugin_manifest`
- `default_plugin_governance_version`
- `default_plugin_kinds`
- `default_plugin_required_fields`

The selected child does not own QS type-system implementation, runtime mode implementation, venue capability matrix
implementation, static bundles, compile-time capability reports, compatibility bridge, reproducibility, complexity
budget, developer learning pipeline, bridge builders, tests, machine contract, machine graph contract, QS profile, or
version manifest.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `plugin_governance_contract` is a named white-box child for plugin manifest policy and validation. |
| parent_child_communication_kept | pass | The child can import QS type, runtime mode, and venue matrix public surfaces through the v4 parent facade. |
| equivalence_baseline_freezable | pass | Plugin kinds, required fields, policy flags, and manifest validation branches are finite. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | Plugin governance and manifest DTOs remain public through the v4 facade. |
| state_machine_phase | no | Plugin validation is capability governance, not graph state topology. |
| strategy_branch | guarded | Plugin manifests bind QS schemas, runtime permissions, and venue capability matrices into strategy/plugin boundaries. |
| independent_failure_mode | pass | Plugin kind, required field, deterministic, permission, network, and capability-matrix failures are independent from version/reproducibility/complexity rules. |
| reuse_pressure | pass | Static bundles, compile-time reports, compatibility reports, tests, and future plugin metadata paths reuse plugin manifest validation. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The child owns public DTOs, enums, defaults, static governance validation, and manifest validation as one contract family. |
| communication_cost_rises | no | A single child preserves one plugin validation proof while using sibling contracts only through public inputs. |
| local_proof_missing | no | Existing qrpc-core-ir tests cover pure plugin network permission rejection and plugin validation through static bundle flows. |
| line_count_only | no | Selection is based on plugin governance ownership and reuse pressure. |

leaf_split_decision_result

`continue_split: true`

Selected next child:

`root.contracts.core_ir.v4_contracts.plugin_governance_contract`

next_recursive_step

BE-001VO-01 root.contracts.core_ir.v4_contracts.plugin_governance_contract baseline_plan
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs`

**Markers**:
- `BE-001VN-01`

**Next step**:
BE-001VO-01 root.contracts.core_ir.v4_contracts.plugin_governance_contract baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
