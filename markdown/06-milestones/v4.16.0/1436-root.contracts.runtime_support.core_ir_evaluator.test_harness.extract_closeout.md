# v4.16.0 root.contracts.runtime_support.core_ir_evaluator.test_harness extract closeout

> Batch: BE-002AS-02
> Node: `root.contracts.runtime_support.core_ir_evaluator.test_harness`
> Parent: `root.contracts.runtime_support.core_ir_evaluator`
> Stage: `actual_extraction`
> Movement: Rust code movement.

---

## Result

`root.contracts.runtime_support.core_ir_evaluator.test_harness` has been physically extracted.

New owner file:

- `qrpc_runtime/src/core_ir_evaluator/test_harness.rs`

Parent file:

- `qrpc_runtime/src/core_ir_evaluator.rs`

## Moved Surface

Moved into the child:

- local quote/kline fixtures;
- Spread and Custom evaluator tests;
- advanced/classic formula helper smoke tests;
- registry integration coverage through `evaluate_indicator_signal`.

Parent changes:

- replaced the inline `#[cfg(test)] mod tests` block with `#[cfg(test)] mod test_harness;`;
- preserved all production evaluator DTOs, registry dispatch, helper ownership, and child declarations.

## Preserved Parent Ownership

The parent still owns:

- public evaluator DTOs and error enum;
- `IndicatorEvaluator`, `indicator_registry`, and `evaluate_indicator_signal`;
- closed evaluator child declarations;
- parent-owned lookup/math helpers.

The child is test-only and uses `super::*`; no public API was widened.

## Equivalence Evidence

Passed:

- `cargo fmt`
- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime core_ir_evaluator`: 22 passed
- `cargo check -p quantpilot`

Remaining for commit gate:

- `git diff --check`
- `tools/check-utf8.ps1`
- `tools/check-full-feature-tree.ps1`
- `tools/check-matrix-governance.ps1`

## Next Step

BE-002AS-03 `root.contracts.runtime_support.core_ir_evaluator.test_harness` single_leaf_closeout.
