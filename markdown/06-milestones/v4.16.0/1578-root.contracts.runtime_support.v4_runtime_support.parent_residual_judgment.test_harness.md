# v4.16.0 root.contracts.runtime_support.v4_runtime_support parent residual judgment - test_harness

> Batch: BE-002EW-01
> Node: `root.contracts.runtime_support.v4_runtime_support`
> Selected child: `root.contracts.runtime_support.v4_runtime_support.test_harness`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `root.contracts.runtime_support.v4_runtime_support.test_harness` as the next v4 runtime support child.

All production children from the BE-002DH-01 v4 runtime support queue are now closed:

- `type_surface`
- `graph_symbol_expansion`
- `runtime_constructor_mode_gate`
- `event_replay_orchestration`
- `machine_transition_engine`
- `risk_execution_gate`
- `simulated_execution_engine`

The remaining planned child is the local v4 runtime test harness.

## Residual Surface

`qrpc_runtime/src/v4_runtime_tests.rs` still owns the v4 runtime local tests and fixtures:

- sample Core IR and compatibility graph builders;
- simulated and provider-native venue capability matrix fixtures;
- nested observation graph fixture;
- replay determinism tests;
- runtime event payload rejection tests;
- simulated execution fill, market price, OCO, trailing stop, GTD, and amend tests;
- execution capability and risk-plane guard tests;
- runtime cache, recovery, memory snapshot, and live-actual boundary tests.

## Boundary

The next baseline must freeze only the test harness movement. It must not move or change:

- production v4 runtime behavior;
- public v4 DTOs or constants;
- v4 runtime constructors or mode gates;
- event replay orchestration;
- transition engine;
- risk/execution gate logic;
- simulated execution child modules;
- live execution, plugin runtime, ops compatibility, metrics, QuantScript, plugin metadata, executor, backend, frontend, or release-transition optimization.

## Expected Movement Shape

The next movement should be test-only:

- add a planned test child file under the v4 runtime support module boundary;
- move the existing `qrpc_runtime/src/v4_runtime_tests.rs` contents without changing assertions;
- keep test discovery equivalent except for expected module path renaming if the physical test module path changes.

## Next Step

BE-002EX-01 `root.contracts.runtime_support.v4_runtime_support.test_harness` baseline_plan.
