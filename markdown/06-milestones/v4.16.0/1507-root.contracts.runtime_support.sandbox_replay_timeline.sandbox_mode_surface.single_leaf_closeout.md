# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline.sandbox_mode_surface single leaf closeout

> Batch: BE-002CV-02
> Node: `root.contracts.runtime_support.sandbox_replay_timeline.sandbox_mode_surface`
> Parent: `root.contracts.runtime_support.sandbox_replay_timeline`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Decision

`stop_split: true`

The sandbox mode surface is a terminal public contract leaf.

## Leaf Granularity Evidence

`tools/evaluate-leaf-granularity.ps1` result:

- decision: `STOP`
- score: `32`
- loc: `117`
- functions: `15`
- public surface: `28`
- terminal leaf control bucket: `small_100_149`
- governance mode: `stop_split`

The leaf is small and cohesive despite a broad public surface. Splitting deterministic test mode, sandbox snapshot, and the `Sandbox` trait would fragment one public sandbox contract and increase wrapper churn.

## Boundary

The leaf owns public sandbox mode/control constants, DTOs, and trait. Concrete realtime and fast-backtest sandbox implementations remain queued outside this leaf.

## Gates

Evidence inherited from BE-002CV-01:

- `cargo fmt --check`: pass
- `cargo check -p qrpc-runtime`: pass
- `cargo test -p qrpc-runtime sandbox -- --nocapture`: pass, 21 passed / 2 ignored
- `cargo check -p quantpilot`: pass
- `git diff --check`: pass
- UTF-8 / full feature tree / matrix governance: pass

## Next Step

BE-002CW-01 `root.contracts.runtime_support.sandbox_replay_timeline` parent_residual_judgment selects `realtime_sandbox`.
