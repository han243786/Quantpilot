# v4.16.0 root.executor.runner.runner_support_files.migration_api baseline plan

> Batch: BE-002MW-01
> Node: `root.executor.runner.runner_support_files.migration_api`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Frozen Boundary

`migration_api` is already physically isolated as:

`src-executor/migration_api.rs`

The leaf owns strategy migration DTOs, QS compile proof validation, strategy
package signature verification, package JSON decoding, and registration of the
migrated strategy into `ExecutorState`.

## Leaf Granularity Gate

The terminal leaf evaluator returned `STOP` with score 23 for the whole
`src-executor/migration_api.rs` file.

| Field | Value |
| --- | --- |
| LOC | 95 |
| Functions | 3 |
| Public surface | 20 |
| Branches | 4 |
| Domains | 5 |
| Governance packaging | `stop_split` |
| Size bucket | `micro_under_100` |
| Risk tags | `compiler_contract`, `cross_crate`, `lock`, `persistence`, `public_api`, `security`, `state_machine`, `trading_semantics` |

## Decision

No internal extraction is approved in this step. The file is below the
terminal-size target and remains cohesive around migration package intake.

## Required Gates

| Gate | Required |
| --- | --- |
| `cargo check -p quantpilot --bin executor` | Yes |
| `git diff --check` | Yes |
| Full feature tree | Yes |
| Matrix governance | Yes |

## Next Step

BE-002MW-02
`root.executor.runner.runner_support_files.migration_api`
single_leaf_closeout.
