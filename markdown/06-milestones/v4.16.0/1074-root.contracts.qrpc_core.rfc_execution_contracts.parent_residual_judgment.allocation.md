# v4.16.0 root.contracts.qrpc_core.rfc_execution_contracts parent residual judgment selects allocation

> Batch: BE-001ST-01
> Node: `root.contracts.qrpc_core.rfc_execution_contracts`
> Parent: `root.contracts.qrpc_core`
> Selected child: `root.contracts.qrpc_core.rfc_execution_contracts.allocation`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core.rfc_execution_contracts` returns to its child queue after `data_request` closeout.

Decision:

`next_child: root.contracts.qrpc_core.rfc_execution_contracts.allocation`

## Closed RFC Execution Children

| Child | Result |
| --- | --- |
| `contracts.qrpc_core.rfc_execution_contracts.data_request` | Closed with `stop_split: true`. |

## Open RFC Execution Residuals

| Residual | Status |
| --- | --- |
| `contracts.qrpc_core.rfc_execution_contracts.allocation` | Selected next. Owns allocation DTOs and allocation projection helper. |
| `contracts.qrpc_core.rfc_execution_contracts.order_contract` | Queued. Owns order DTOs and transition matrix helper. |
| `contracts.qrpc_core.rfc_execution_contracts.execution_feedback` | Queued. Owns execution feedback event DTOs. |
| `contracts.qrpc_core.rfc_execution_contracts.handoff_snapshot` | Queued. Owns handoff snapshot DTO and completeness validation. |

## Selection Rationale

`allocation` is selected because it is the next contiguous RFC execution family in `qrpc_core/src/rfc_execution_contracts.rs` after closed `data_request`:

- `AllocationMethod`;
- `Allocation`;
- `Allocation::apply_to_targets`.

This child owns both the allocation schema and its pure projection helper. It can move before order/feedback/handoff because those later families do not own allocation math.

## White Box Boundary

| Item | Owner | Input | Output | Side effects |
| --- | --- | --- | --- | --- |
| Allocation method taxonomy | `allocation` | allocation strategy kind | method enum schema | none |
| Allocation DTO | `allocation` | explicit weights, total budget, min/max constraints, metadata | allocation schema | none |
| Allocation projection helper | `allocation` | allocation plus `PortfolioTarget` target weights | `BTreeMap<Symbol, f64>` budget projection | none |

## Same-Parent Parallel Judgment

`same_parent_parallel_children` remains inactive for this selection.

Reason: `allocation` contains behavior (`apply_to_targets`) in addition to DTOs. Serial extraction keeps projection math and parent facade updates isolated before later DTO-only siblings are considered.

## Hard Boundaries

The next `allocation` baseline must not:

- edit Rust source code;
- move order, feedback, or handoff contracts;
- change enum variants, struct fields, derives, allocation math, public exports, or tests;
- reopen closed qrpc-core children or closed `data_request`;
- change `qrpc_core_ir`, `qrpc_compiler`, `qrpc_runtime`, `quantscript`, backend, executor, frontend, physical `plugins/*`, or release transition;
- introduce direct sibling shortcuts.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `allocation` owns `AllocationMethod`, `Allocation`, and `Allocation::apply_to_targets`. |
| parent_child_communication_kept | pass | Future child exports remain mediated through the RFC execution parent facade and crate root. |
| equivalence_baseline_freezable | pass | Enum variants, fields, derives, fallback weight behavior, budget multiplication, clamping, and insert semantics can be frozen. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | pass | Allocation is public qrpc-core contract API. |
| state_machine_phase | no | No runtime state machine exists here. |
| strategy_branch | no | No strategy branch is selected. |
| independent_failure_mode | pass | Allocation math drift is independent from order transitions, feedback schema, and handoff validation. |
| reuse_pressure | pass | Allocation projections are reusable through `qrpc_core::*`. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The selected child has both DTO and helper behavior. |
| communication_cost_rises | no | Parent facade re-export preserves current consumers. |
| local_proof_missing | no | qrpc-core compile/test gates cover the move. |
| line_count_only | no | Split is by allocation projection ownership, not raw line count. |

leaf_split_decision_result

`continue_baseline: true`; `allocation` is worth selecting as the next RFC execution child.

## Next Step

BE-001SU-01 `root.contracts.qrpc_core.rfc_execution_contracts.allocation` baseline_plan.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
