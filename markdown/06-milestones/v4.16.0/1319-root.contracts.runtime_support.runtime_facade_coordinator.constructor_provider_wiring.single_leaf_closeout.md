# v4.16.0 runtime_facade_coordinator.constructor_provider_wiring single leaf closeout

> Batch: BE-001YH-03
> Node: `root.contracts.runtime_support.runtime_facade_coordinator.constructor_provider_wiring`
> Parent: `root.contracts.runtime_support.runtime_facade_coordinator`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

## Leaf Status

`stop_split: true`.

The constructor/provider wiring child is now a terminal leaf.

## Split Decision Gate

The child does not need another recursive split.

Reasons:

- the extracted file owns one cohesive responsibility: building `RuntimeCoordinator` from core IR, compiled protocol inputs, builtin providers, or injected providers
- public constructor APIs remain shallow and directly tied to object construction
- separating builtin shortcuts from provider-object injection would introduce extra coordination without a separate owner
- initial `RuntimeState`, `ConfigTracker`, and `MergeCoordinator` construction belongs to the same constructor boundary
- no session/cycle behavior or runtime mutation behavior remains inside this leaf

## Equivalence Evidence

The actual extraction batch passed:

- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Parent Residuals

`runtime_facade_coordinator` remains open.

Remaining child queue:

1. `contracts.runtime_support.runtime_facade_coordinator.session_cycle_orchestration`
2. `contracts.runtime_support.runtime_facade_coordinator.execution_market_entrypoints`
3. `contracts.runtime_support.runtime_facade_coordinator.state_config_accessors`
4. `contracts.runtime_support.runtime_facade_coordinator.config_generation`
5. `contracts.runtime_support.runtime_facade_coordinator.portfolio_projection`
6. `contracts.runtime_support.runtime_facade_coordinator.coordinator_test_harness`

## Next Step

BE-001YI-01 `root.contracts.runtime_support.runtime_facade_coordinator` parent_residual_judgment selects the next child.
