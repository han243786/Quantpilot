# v4.16.0 root.contracts.core_ir.v4_contracts.plugin_governance_contract single leaf closeout stops split

> Batch: BE-001VO-03
> Node: `root.contracts.core_ir.v4_contracts.plugin_governance_contract`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.plugin_governance_contract` stops splitting after extracting the full plugin
governance and manifest validation contract family.

The child owns:

- plugin governance policy DTOs and flags
- plugin manifest DTOs and permission enums
- default plugin kind and required field catalogs
- static plugin governance validation
- pure/runtime/venue plugin manifest validation
- sibling contract checks against QS type refs, runtime modes, and venue capability matrices

These pieces form one plugin-governance proof: plugin type, deterministic behavior, runtime/network permissions, schema
refs, and venue capability requirements must stay together to keep manifest validation readable.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `plugin_governance_contract` names the complete plugin governance and manifest validation boundary. |
| parent_child_communication_kept | pass | The v4 parent re-exports public plugin types; sibling contracts are used only as validation inputs. |
| equivalence_baseline_freezable | pass | BE-001VO-01 froze plugin kinds, required fields, governance flags, and manifest validation behavior. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | Public plugin governance and manifest types remain available through `qrpc_core_ir::v4`. |
| state_machine_phase | no | Plugin governance is capability/manifest policy, not graph state topology. |
| strategy_branch | guarded | Plugin manifests bind QS schemas and venue/runtime capability branches into strategy plugin boundaries. |
| independent_failure_mode | guarded | Plugin kind, required field, permission, deterministic, network, and capability-matrix failures belong to one manifest policy. |
| reuse_pressure | pass | Static bundles, compile-time reports, compatibility reports, tests, and plugin metadata paths reuse plugin manifest validation. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | yes | Splitting DTO/enums/defaults from validation would create small leaves without independent contract owners. |
| communication_cost_rises | yes | Further split would route one plugin manifest proof across multiple private modules. |
| local_proof_missing | no | `cargo test -p qrpc-core-ir`, `cargo check -p quantpilot`, and commit-time gates passed after extraction. |
| line_count_only | no | The stop decision is based on cohesive plugin governance ownership. |

leaf_split_decision_result

`stop_split: true`

This leaf is complete. Return to `root.contracts.core_ir.v4_contracts` for parent residual judgment.

next_recursive_step

BE-001VP-01 root.contracts.core_ir.v4_contracts parent_residual_judgment
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/plugin_governance_contract.rs`

**Markers**:
- `BE-001VO-03`

**Next step**:
BE-001VP-01 root.contracts.core_ir.v4_contracts parent_residual_judgment

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
