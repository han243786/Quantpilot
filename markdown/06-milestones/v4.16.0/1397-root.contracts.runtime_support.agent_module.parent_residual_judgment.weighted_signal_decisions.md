# v4.16.0 root.contracts.runtime_support.agent_module parent residual judgment selects weighted_signal_decisions

> Batch: BE-001ZW-01
> Node: `root.contracts.runtime_support.agent_module`
> Stage: `parent_residual_judgment`
> Selected child: `root.contracts.runtime_support.agent_module.weighted_signal_decisions`
> Movement: No Rust code movement.

---

## Parent State

`root.contracts.runtime_support.agent_module` baseline is frozen with the following child queue:

1. `contracts.runtime_support.agent_module.weighted_signal_decisions`
2. `contracts.runtime_support.agent_module.portfolio_rebalance`
3. `contracts.runtime_support.agent_module.cross_venue_arbitrage`
4. `contracts.runtime_support.agent_module.shared_scoring_portfolio_helpers`
5. `contracts.runtime_support.agent_module.test_harness`

## Selected Child Boundary

`weighted_signal_decisions` owns the slow-cycle weighted signal policy path that converts non-observe intent signals into one or more direct `AgentDecision` values with proposed buy/sell actions.

Candidate physical surface:

- current owner: `qrpc_runtime/src/agent_module.rs`
- planned child module: `qrpc_runtime/src/agent_module/weighted_signal_decisions.rs`

Candidate functions:

- `build_weighted_agent_decisions`
- `group_signals_by_symbol`

Candidate parent/shared helper dependencies:

- `build_signal_kind_index`
- `signal_score`
- `available_position_ratio`
- constants `DEFAULT_DECISION_THRESHOLD` and `MIN_QUANTITY_RATIO`

## Why This Child First

| Criterion | Judgment |
| --- | --- |
| Queue order | It is the first child in the frozen `agent_module` child queue. |
| Cohesion | Strong: one policy family, one cycle path, one output type. |
| Size pressure | Medium: small enough for one focused extraction, useful enough to reduce parent complexity. |
| Inputs | Agent policy, related intent signals, Core IR signal rules, portfolio state, timestamp, trace id |
| Outputs | Direct agent decisions and proposed actions |
| Risk | Medium: symbol/exchange inheritance, short-side inventory limit, thresholds, and quantity ratio clamps must remain equivalent. |
| Exclusions | Portfolio rebalance target generation, arbitrage spread decisions, event emission, provider dispatch, and tests stay outside this child. |

## Equivalence Baseline For Next Step

Next baseline must preserve:

- non-observe signal filtering;
- grouping by signal exchange and symbol scope;
- confidence-weighted net score calculation;
- decision threshold and max quantity clamp behavior;
- sell-side available position clamp;
- default reference price fallback;
- long/short side and buy/sell action mapping;
- decision id, strategy tag, reason, timestamp, and trace id shape;
- existing tests that cover symbol/exchange inheritance and weighted signal decisions.

Required gates for the next movement:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-001ZX-01 `root.contracts.runtime_support.agent_module.weighted_signal_decisions` baseline_plan.
