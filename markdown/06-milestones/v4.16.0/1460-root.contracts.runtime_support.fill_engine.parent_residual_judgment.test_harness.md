# v4.16.0 root.contracts.runtime_support.fill_engine parent residual judgment selects test_harness

> Batch: BE-002BK-01
> Node: `root.contracts.runtime_support.fill_engine`
> Candidate: `root.contracts.runtime_support.fill_engine.test_harness`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.

---

## Decision

Select `root.contracts.runtime_support.fill_engine.test_harness` as the next child.

## Current Closed Children

- `root.contracts.runtime_support.fill_engine.event_projection_wave`
- `root.contracts.runtime_support.fill_engine.portfolio_reservation_accounting`
- `root.contracts.runtime_support.fill_engine.fill_report_execution_helpers`

## Residual Shape

`qrpc_runtime/src/fill_engine.rs` now keeps the public fill lifecycle surface, parent-owned orchestration, public `apply_fill_to_portfolio`, type definitions, and local tests.

The remaining independently movable pocket is the local test harness:

- sample order and plan builders;
- sample portfolio and market fixtures;
- fill engine behavior tests for market, limit, IOC, FOK, idempotency, resting order lifecycle, cash cap, and sell-position availability.

## Boundary Rules

- production `FillEngine` orchestration remains parent-owned;
- public `apply_fill_to_portfolio` remains parent-owned;
- closed helper children remain outside this movement;
- tests may use parent-private helpers through parent-mediated `super::*`;
- no production sibling shortcut may be introduced.

## Next Step

BE-002BL-01 `root.contracts.runtime_support.fill_engine.test_harness` baseline_plan.
