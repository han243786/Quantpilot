# v4.16.0 root.contracts.compiler_bridge.runtime_protocol_validation equivalence baseline and extraction plan

> Batch: BE-001XB-01
> Node: `root.contracts.compiler_bridge.runtime_protocol_validation`
> Parent: `root.contracts.compiler_bridge`
> Stage: `baseline_plan`
> Movement: no Rust code movement.

---

## Summary

`root.contracts.compiler_bridge.runtime_protocol_validation` is frozen before extraction.

This child owns validation-only behavior for `RuntimeProtocolCoreConfig`. It may be extracted as a private compiler bridge child while keeping the public `validate_runtime_protocol_config` function in the parent facade.

Decision:

`baseline_frozen: true`

## Allowed Movement For BE-001XC-01

The next extraction may create `qrpc_compiler/src/runtime_protocol_validation.rs` and move only:

- finite/positive runtime cost checks currently inside `validate_runtime_protocol_config`;
- `ensure_unique_ids`;
- `validate_data_sources`;
- `validate_intents`;
- `validate_intent`;
- `validate_agents`;
- `validate_agent`;
- `validate_risks`;
- `validate_risk`.

The parent public function `validate_runtime_protocol_config` must remain public and delegate to the child so all callers keep the same API.

## Parent-Owned Helpers

The child may call but must not move:

- `runtime_intent_is_spread`, because it is shared by runtime lowering;
- all runtime lowering helpers;
- all Strategy IR lowering helpers;
- spread/custom expression construction helpers;
- canonical config hashing and `CompiledRuntimeProtocol` assembly;
- `#[cfg(test)] mod tests`.

## Equivalence Requirements

- validation ordering remains unchanged: runtime cost checks, uniqueness, data sources, intents, agents, risks;
- error messages and failure surfaces remain unchanged;
- public function signatures remain unchanged;
- compile/lowering behavior remains unchanged;
- no new crate dependency is introduced;
- no release-transition sibling link is introduced.

## Boundary

**Real files**:
- `qrpc_compiler/src/lib.rs`

**Markers**:
- `BE-001XB-01`
- `runtime_protocol_validation baseline_frozen: true`
- `validation helpers only`
- `parent facade preserved`

**Next step**:
BE-001XC-01 root.contracts.compiler_bridge.runtime_protocol_validation extract_closeout

---

## Gates

- `cargo test -p qrpc-compiler`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
