# v4.16.0 root.contracts.runtime_support.v4_runtime_support.test_harness.payload_validation_tests single leaf closeout

> Batch: BE-002FE-02
> Node: `root.contracts.runtime_support.v4_runtime_support.test_harness.payload_validation_tests`
> Parent: `root.contracts.runtime_support.v4_runtime_support.test_harness`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Set `stop_split: true` for `payload_validation_tests`.

The child is a small cohesive test-only owner for malformed payload and malformed simulated execution config rejection behavior. Further splitting would create micro test modules around individual rejection cases without improving ownership clarity.

## Leaf Granularity Evidence

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -Path qrpc_runtime\src\v4_runtime\test_harness\payload_validation_tests.rs
```

Result:

| Field | Value |
| --- | --- |
| `split_decision` | `STOP` |
| `governance_packaging` | `stop_split` |
| `final_decision` | `STOP` |
| Score | `36` |
| LOC | `108` |
| Functions | `5` |
| Public/internal-visible surface | `0` |
| Branches | `0` |
| Domains | `4` |
| Risk tags | `state_machine`, `trading_semantics` |
| Terminal size bucket | `small_100_149` |

## Stop-Split Rationale

Rejected finer splits:

| Candidate | Decision | Reason |
| --- | --- | --- |
| catalog payload rejection tests | Reject | The tests share the same malformed external runtime event rejection surface. |
| top-level input deserialization rejection test | Reject | A one-test child would add governance cost without a separate owner. |
| simulated execution config rejection test | Reject | The non-finite config rejection check is still part of malformed runtime input/config validation coverage and remains small. |

## Evidence

Inherited from BE-002FE-01 extraction:

- `cargo fmt --check`: PASS
- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime v4 -- --nocapture`: PASS, 32 tests
- `cargo check -p quantpilot`: PASS

This closeout is docs-only and updates the recursive cursor plus governance indexes.

## Next Step

BE-002FF-01 `root.contracts.runtime_support.v4_runtime_support.test_harness` parent_residual_judgment.
