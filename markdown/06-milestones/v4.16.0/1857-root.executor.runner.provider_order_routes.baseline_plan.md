# v4.16.0 root.executor.runner.provider_order_routes baseline plan

> Batch: BE-002LH-01
> Node: `root.executor.runner.provider_order_routes`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Goal

Freeze the provider order route boundary before Rust movement.

The first extraction may move only the OKX demo provider route handlers from
`src-executor/main.rs` into `src-executor/provider_order_routes.rs`.

## Frozen Boundary

| Field | Value |
| --- | --- |
| Planned file | `src-executor/provider_order_routes.rs` |
| Approved handlers | `submit_okx_demo_order`, `query_okx_demo_order`, `cancel_okx_demo_order` |
| Parent callers | route registration in `src-executor/main.rs` |
| Parent retained | `OkxDemo*` request DTOs, provider mode gate, credential loading, request cleaning, audit payload helpers, `append_audit` |

## Equivalent Behavior

The extracted child must preserve:

- `/api/executor/provider/okx-demo/orders` behavior;
- `/api/executor/provider/okx-demo/orders/query` behavior;
- `/api/executor/provider/okx-demo/orders/cancel` behavior;
- paper-actual mode gating;
- credential lookup and OKX REST call behavior;
- audit redaction and provider response shape.

## Exclusions

- Do not move helper functions or credential loading in this first step.
- Do not move strategy lifecycle, market stream, or v4 deploy handlers.
- Do not alter `okx_rest.rs`.
- Do not introduce release-transition links.

## Gates For Actual Extraction

| Gate | Command |
| --- | --- |
| format | `cargo fmt --all --check` |
| executor compile | `cargo check -p quantpilot --bin executor` |
| diff hygiene | `git diff --check` |

## Next Step

BE-002LH-02 `root.executor.runner.provider_order_routes` actual_extraction.
