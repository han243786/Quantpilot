# v4.16.0 root.executor.runner.runner_support_files.live_runner.runner_instance_dispatch baseline plan

> Batch: BE-002MH-01
> Node: `root.executor.runner.runner_support_files.live_runner.runner_instance_dispatch`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Goal

Freeze the runner variant dispatch boundary before Rust movement.

The next movement may extract `RunnerInstance` and its impl from
`src-executor/live_runner.rs` into:

`src-executor/live_runner/runner_instance_dispatch.rs`

## Frozen Boundary

| Field | Value |
| --- | --- |
| Planned module | `src-executor/live_runner/runner_instance_dispatch.rs` |
| Approved surface | `RunnerInstance` enum and its impl |
| Approved methods | `from_strategy`, `handle_ws_event`, `subscribed_symbols`, `kline_pool`, `v4_memory_snapshot`, `set_stopped` |
| Parent dependencies | `LiveRunner`, `RunnerStatus`, `V4Runner`, `V4ExecutorEvidenceEvent`, `ActiveStrategy`, `RuntimeKind`, `TriggerEvent`, `WsEvent`, `KlinePool`, `Symbol`, `V4RuntimeMemorySnapshot` |
| Parent retained | `RunnerPool`, `V4Runner`, V4 helper functions, and local tests |

## Equivalent Behavior

The extracted child must preserve:

- V3/V4 construction based on `RuntimeKind`;
- websocket event dispatch to the selected runner variant;
- subscribed-symbol projection used by `RunnerPool`;
- K-line pool and V4 memory snapshot projection;
- stopped-state projection for V3 and V4 variants.

## Exclusions

- Do not move `RunnerPool`, `V4Runner`, V4 helper functions, or tests.
- Do not alter the public shape of `RunnerInstance`.
- Do not introduce release-transition links.

## Gates For Actual Extraction

| Gate | Command |
| --- | --- |
| format | `cargo fmt --all --check` |
| executor compile | `cargo check -p quantpilot --bin executor` |
| focused tests | `cargo test -p quantpilot --bin executor live_runner` |
| diff hygiene | `git diff --check` |

## Next Step

BE-002MH-02
`root.executor.runner.runner_support_files.live_runner.runner_instance_dispatch`
actual_extraction.
