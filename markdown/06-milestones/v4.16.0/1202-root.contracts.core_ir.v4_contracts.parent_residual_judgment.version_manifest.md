# v4.16.0 root.contracts.core_ir.v4_contracts parent residual judgment selects version_manifest

> Batch: BE-001VL-01
> Node: `root.contracts.core_ir.v4_contracts`
> Parent: `root.contracts.core_ir`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts` selects
`root.contracts.core_ir.v4_contracts.version_manifest` as the next child.

The selected child owns the v4 version manifest compatibility contract currently in `qrpc_core_ir/src/v4.rs`:

- `V4VersionManifest`
- `impl Default for V4VersionManifest`
- `V4VersionManifest::validate_static_contract`
- `default_version_manifest_version`
- `default_qs_language_version`
- version-manifest serde default mediation for QS type system, machine contract, and venue capability matrix versions

The selected child does not own schema identity constants, QS type-system implementation, machine contract
implementation, venue capability matrix implementation, plugin governance, reproducibility, complexity budget,
developer learning pipeline, static bundles, compile-time capability reports, compatibility bridge, bridge builders,
tests, machine graph contract, QS profile, or runtime mode contract.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `version_manifest` is a named white-box child for v4 schema/version compatibility rules. |
| parent_child_communication_kept | pass | The child can import version default helpers from sibling children through the v4 surface and remain re-exported by the parent. |
| equivalence_baseline_freezable | pass | Version defaults and compatibility validation branches are finite. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | `V4VersionManifest` remains public through the v4 facade. |
| state_machine_phase | no | Version compatibility is schema governance, not graph state topology. |
| strategy_branch | no | The child is version governance, not a strategy branch. |
| independent_failure_mode | pass | Version mismatch and compatibility policy failures are independent from plugin, venue, runtime, and QS type validation. |
| reuse_pressure | guarded | Static bundles and tests use the version manifest as part of the complete v4 contract bundle. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The child owns a public DTO, default construction, validation, and its version default mediation. |
| communication_cost_rises | no | One child keeps version compatibility policy together while parent only re-exports it. |
| local_proof_missing | no | Existing qrpc-core-ir tests cover version manifest schema bump behavior through the public v4 surface. |
| line_count_only | no | Selection is based on version compatibility ownership. |

leaf_split_decision_result

`continue_split: true`

Selected next child:

`root.contracts.core_ir.v4_contracts.version_manifest`

next_recursive_step

BE-001VM-01 root.contracts.core_ir.v4_contracts.version_manifest baseline_plan
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs`

**Markers**:
- `BE-001VL-01`

**Next step**:
BE-001VM-01 root.contracts.core_ir.v4_contracts.version_manifest baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
