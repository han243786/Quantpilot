# v4.16.0 root.contracts.runtime_support.fill_engine parent residual judgment selects portfolio_reservation_accounting

> Batch: BE-002BE-01
> Node: `root.contracts.runtime_support.fill_engine`
> Parent: `root.contracts.runtime_support`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> Selected child: `root.contracts.runtime_support.fill_engine.portfolio_reservation_accounting`

---

## Decision

`root.contracts.runtime_support.fill_engine.portfolio_reservation_accounting` is selected as the next fill-engine child.

This is a judgment step only. It does not authorize code movement yet.

## Evidence

Closed fill-engine children:

- `event_projection_wave`

Remaining queue from the frozen fill-engine baseline:

1. `portfolio_reservation_accounting`
2. `fill_report_execution_helpers`
3. `test_harness`

Current candidate helper group:

- `cash_limited_executable_qty`
- `reservation_for_order`
- `available_position_qty`
- `available_sell_qty_for_order`
- `sync_portfolio_reservations`
- `release_reservation_for_fill`

Baseline decision item:

- `apply_fill_to_portfolio` is public and must remain parent-owned unless the next baseline proves a re-export or parent-mediated wrapper preserves API equivalence with lower churn.

## Boundary Rules

| Boundary | Requirement |
| --- | --- |
| Parent orchestration | `FillEngine::submit_plan`, `FillEngine::on_market_update`, and `try_open_resting_order` remain parent-owned. |
| Event projection | Closed `event_projection_wave` remains outside this movement. |
| Fill report | Fill report construction remains outside this movement. |
| Public API | `apply_fill_to_portfolio` cannot move unless baseline explicitly proves public API preservation. |
| Communication | Candidate helpers may become `pub(super)` child functions consumed only by the parent. |
| Sibling shortcuts | The child must not import event projection, report-helper, or test-harness siblings. |

## Next Step

BE-002BF-01 `root.contracts.runtime_support.fill_engine.portfolio_reservation_accounting` baseline_plan.
