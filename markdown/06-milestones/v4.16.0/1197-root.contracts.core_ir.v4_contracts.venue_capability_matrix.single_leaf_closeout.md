# v4.16.0 root.contracts.core_ir.v4_contracts.venue_capability_matrix single leaf closeout stops split

> Batch: BE-001VI-03
> Node: `root.contracts.core_ir.v4_contracts.venue_capability_matrix`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.venue_capability_matrix` stops splitting after extracting the full venue
execution capability matrix contract family.

The child owns:

- venue capability matrix DTOs and support source enums
- first-wave execution capability constants
- unsupported matrix construction
- validation, lookup, required-capability, and runtime-mode support helpers
- internal v4-visible venue matrix version default helper

These pieces form one matrix proof: capability identity, support source, runtime-mode compatibility, and first-wave
coverage must stay together to keep the contract readable.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `venue_capability_matrix` names the complete venue capability matrix boundary. |
| parent_child_communication_kept | pass | The v4 parent re-exports public venue types and imports only the version default helper. |
| equivalence_baseline_freezable | pass | BE-001VI-01 froze first-wave capabilities, defaults, validation, and support lookup behavior. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | Public venue capability types remain available through `qrpc_core_ir::v4`. |
| state_machine_phase | no | Venue matrix behavior is execution capability support, not graph state topology. |
| strategy_branch | guarded | The child supports venue/runtime capability branches consumed by plugin and compile-time reports. |
| independent_failure_mode | guarded | Duplicate, unsupported, mode/source mismatch, and required-capability failures all belong to one matrix contract. |
| reuse_pressure | pass | Plugin governance, compile-time capability reports, backtest artifacts, static bundles, and tests depend on these types. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | yes | Splitting enum/constants/defaults/validation would create small leaves without independent contract owners. |
| communication_cost_rises | yes | Further split would route one venue matrix proof across multiple private modules. |
| local_proof_missing | no | `cargo test -p qrpc-core-ir` and commit-time `cargo check -p quantpilot` passed after extraction. |
| line_count_only | no | The stop decision is based on cohesive venue matrix ownership. |

leaf_split_decision_result

`stop_split: true`

This leaf is complete. Return to `root.contracts.core_ir.v4_contracts` for parent residual judgment.

next_recursive_step

BE-001VJ-01 root.contracts.core_ir.v4_contracts parent_residual_judgment
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/venue_capability_matrix.rs`

**Markers**:
- `BE-001VI-03`

**Next step**:
BE-001VJ-01 root.contracts.core_ir.v4_contracts parent_residual_judgment

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
