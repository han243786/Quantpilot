# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline parent residual judgment - unified_timeline

> Batch: BE-002CO-01
> Node: `root.contracts.runtime_support.sandbox_replay_timeline`
> Selected child: `root.contracts.runtime_support.sandbox_replay_timeline.unified_timeline`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.

---

## Decision

Select `unified_timeline` as the next sandbox/replay/timeline child.

## Evidence

`timeline_data_providers` is closed. The remaining `qrpc_runtime/src/sandbox/timeline.rs` production surface is centered on `UnifiedTimeline`, which owns:

- merged timestamp construction;
- slow/fast trigger index selection;
- provider list assembly;
- `from_providers`;
- timestamp-indexed `collect_at`;
- timeline length/empty helpers.

This is a cohesive child separate from replay builder construction and sandbox runtime implementations.

## Candidate Boundary

The next baseline should cover:

- `UnifiedTimeline`;
- `UnifiedTimeline::new`;
- `UnifiedTimeline::from_providers`;
- `UnifiedTimeline::collect_at`;
- `UnifiedTimeline::len`;
- `UnifiedTimeline::is_empty`.

Out of scope:

- timeline provider implementations, already closed;
- replay builder functions in `replay.rs`;
- sandbox mode/control surface in `mod.rs`;
- realtime and fast-backtest sandbox implementations;
- local tests unless a later test-harness baseline moves them.

## Dependency Rule

`UnifiedTimeline` may use provider types through the sandbox timeline parent. Replay builder and sandbox implementations must continue to reach timeline behavior through parent-owned imports/re-exports.

## Next Step

BE-002CP-01 `root.contracts.runtime_support.sandbox_replay_timeline.unified_timeline` baseline_plan.
