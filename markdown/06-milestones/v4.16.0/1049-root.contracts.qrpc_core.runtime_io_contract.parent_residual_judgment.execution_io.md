# v4.16.0 root.contracts.qrpc_core.runtime_io_contract parent residual judgment selects execution_io

> Batch: BE-001SG-01
> Node: `root.contracts.qrpc_core.runtime_io_contract`
> Selected child: `root.contracts.qrpc_core.runtime_io_contract.execution_io`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core.runtime_io_contract` returns to its child queue after `decision_flow` closeout.

Decision:

`next_child: root.contracts.qrpc_core.runtime_io_contract.execution_io`

## Closed Runtime IO Children

| Child | Result |
| --- | --- |
| `contracts.qrpc_core.runtime_io_contract.market_data_io` | Closed with `stop_split: true`. |
| `contracts.qrpc_core.runtime_io_contract.decision_flow` | Closed with `stop_split: true`. |

## Open Runtime IO Residuals

| Residual | Status |
| --- | --- |
| `contracts.qrpc_core.runtime_io_contract.execution_io` | Selected next. Owns simulated order, execution plan, fill report, open order, and fill result DTOs. |
| `contracts.qrpc_core.runtime_io_contract.portfolio_state` | Queued. Owns position/exposure/portfolio state DTOs and helper methods. |
| `contracts.qrpc_core.runtime_io_contract.runtime_output` | Queued. Owns runtime event, cycle output, and session output DTOs. |
| `contracts.qrpc_core.runtime_io_contract.backtest_output` | Queued. Owns backtest equity, metrics, summary, period return, and final output DTOs. |

## Selection Rationale

`execution_io` is selected because it is the next contiguous DTO family in `qrpc_core/src/runtime_io_contract.rs`:

- it owns `SimOrder`;
- it owns `ExecutionPlan`;
- it owns `FillReport`;
- it owns `OpenOrder`;
- it owns `FillResult`;
- it is pure schema with no helper methods, no mutable state, no IO, no locks, and no persistence;
- `FillResult` references `RuntimeEvent`, which remains parent-owned until the runtime output child is extracted.

## Same-Parent Parallel Judgment

`same_parent_parallel_children` remains inactive for this selection.

`FillResult` references parent-owned `RuntimeEvent`, and later runtime output/portfolio children will consume execution IO DTOs through the runtime IO parent facade. Serial extraction keeps those parent-mediated links explicit.

## Hard Boundaries

The next `execution_io` baseline must not:

- edit Rust source code;
- change simulated order, execution plan, fill report, open order, fill result field names, types, serde derives, default attributes, public exports, or tests;
- move portfolio state, runtime output, backtest output, RFC execution contracts, or crate-root tests;
- change closed market data IO, decision flow, other qrpc-core children, downstream runtime/backend/compiler behavior, or release transition.

## Next Step

BE-001SH-01 `root.contracts.qrpc_core.runtime_io_contract.execution_io` baseline_plan.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
