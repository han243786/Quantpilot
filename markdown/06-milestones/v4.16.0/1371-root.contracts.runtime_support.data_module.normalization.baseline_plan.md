# v4.16.0 root.contracts.runtime_support.data_module.normalization baseline plan

> Batch: BE-001ZI-01
> Node: `root.contracts.runtime_support.data_module.normalization`
> Stage: `baseline_plan`
> Movement: No code movement.
> Current file: `qrpc_runtime/src/data_module.rs`
> Planned child file: `qrpc_runtime/src/data_module/normalization.rs`

---

## Summary

The `normalization` baseline is frozen.

This child owns raw-to-normalized market data conversion:

- raw Kline rows to `KlineSeriesSnapshot`;
- raw quote payloads to `QuoteSnapshot`;
- OHLC validity filtering and clamping;
- default data quality attachment for newly normalized snapshots;
- quote snapshot construction from a mid price.

## Three Matrix Impact

| Matrix | Impact |
| --- | --- |
| Process matrix | Continues BE-001ZI-01 as a standard baseline before Rust movement. |
| Standard matrix | Preserves parent-child communication through `data_module`; no sibling horizontal link or release transition. |
| Guidance matrix | Adds a planned white-box child for data normalization. |

## Leaf Granularity Judge

The read-only tool cannot yet isolate a function subset from a mixed parent file, so this baseline uses manual scoring with the same fields.

| Field | Result |
| --- | --- |
| leaf_id | `root.contracts.runtime_support.data_module.normalization` |
| loc | small-to-medium semantic conversion pocket |
| function_count | three functions |
| public_surface | one parent-visible `pub(crate)` helper, two parent-private helpers |
| complexity_score | Medium: OHLC filtering, clamping, latency/status projection, data quality defaults |
| dependency_score | Medium-low: depends on `qrpc_core` DTOs and parent call sites, but not on cache, transport, or mock state |
| split_benefit | High: gives fetch, mock, and historical cache a single conversion owner |
| governance_cost | Medium-low: one child file and standard index updates |
| risk_score | Medium-low: no external public API, route/schema, persistence, lock, or state-machine changes |
| final_decision | `SPLIT` |
| reason | Normalization is a cohesive data-shape owner used by live fetch, mock data, historical replay, and quote snapshot projection. |

## Frozen Boundary

Allowed in BE-001ZI-02:

- add `mod normalization;` to `qrpc_runtime/src/data_module.rs`;
- move `normalize_kline_series`;
- move `normalize_quote`;
- move `quote_snapshot_from_price`;
- import the moved functions from the child into the parent;
- keep existing call sites and public behavior equivalent.

Explicitly excluded:

- raw OKX/Binance parser helpers already owned by `exchange_surface`;
- `mock_raw_klines`;
- `mock_raw_quote`;
- `mock_kline_bars_for_backtest`;
- historical cache path/load/persist/fetch helpers;
- HTTP transport helpers;
- in-memory cache helpers;
- `BuiltinDataModule::fetch_and_normalize`;
- tests except imports needed to keep existing behavior compiling.

## Equivalence Surface

The extraction must preserve:

- OHLC validity filtering;
- clamped high/low warning behavior;
- `NormalizedKline` field mapping;
- `KlineSeriesSnapshot` metadata and `DataQualitySnapshot::default`;
- `QuoteSnapshot` bid/ask/mid/size/latency/status mapping;
- `quote_snapshot_from_price` bid/ask spread and healthy source status;
- all current `qrpc-runtime` tests.

## Gates

The next movement must pass:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-001ZI-02 `root.contracts.runtime_support.data_module.normalization` actual_extraction.
