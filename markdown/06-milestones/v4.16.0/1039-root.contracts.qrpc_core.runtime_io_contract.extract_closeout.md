# v4.16.0 root.contracts.qrpc_core.runtime_io_contract extract closeout

> Batch: BE-001SB-02
> Node: `root.contracts.qrpc_core.runtime_io_contract`
> Parent: `root.contracts.qrpc_core`
> Stage: `extract_closeout`
> Movement: Rust code extraction.

---

## Summary

`root.contracts.qrpc_core.runtime_io_contract` has been extracted from `qrpc_core/src/lib.rs` into a private qrpc-core child module.

The qrpc-core crate root preserves the public facade:

- `mod runtime_io_contract;`
- `pub use runtime_io_contract::*;`

## Files Changed

- `qrpc_core/src/lib.rs`
- `qrpc_core/src/runtime_io_contract.rs`

## Extraction Result

`qrpc_core/src/runtime_io_contract.rs` now owns:

- raw and normalized market data DTOs from `RawKline` through `NormalizedMarketData`;
- decision and action DTOs from `IntentSignal` through `RiskDecision`;
- execution IO DTOs from `SimOrder` through `FillResult`;
- portfolio projection DTOs and helpers from `Position` through `PortfolioState::debug_assert_invariants`;
- runtime output DTOs from `RuntimeEvent` through `SessionOutput`;
- backtest output/report DTOs from `BacktestEquityPoint` through `BacktestOutput`.

`qrpc_core/src/lib.rs` now keeps qrpc-core crate-root facade declarations plus the queued RFC execution contracts beginning at `MarketScope`.

## Rust Local Fields

| Field | Result |
| --- | --- |
| Crate | `qrpc-core` |
| Parent facade | Preserved. `qrpc_core/src/lib.rs` declares and re-exports the runtime IO child. |
| Child visibility | The module is private; public runtime IO surfaces are preserved through `pub use runtime_io_contract::*;`. |
| Crate public exports | Preserved through the crate-root public re-export. |
| Parent-owned residuals | RFC execution contracts beginning at `MarketScope`, crate-root tests, and qrpc-core parent residual judgment remain parent-owned. |
| Sibling dependency check | Passed. The child imports closed qrpc-core public names through the crate root and does not import sibling internals directly. |

## Equivalence Preservation

| Surface | Preserved evidence |
| --- | --- |
| Market data DTOs | Field names, types, serde derives, nested quality/status references, and enum shape remain unchanged. |
| Decision/action DTOs | Intent, action, target, agent, and risk decision fields plus optional/default serde attributes remain unchanged. |
| Execution IO DTOs | Sim order, plan, fill report, open order, fill result fields and nested runtime event list remain unchanged. |
| Portfolio state | `deny_unknown_fields`, balance/exposure/order/position fields, `new`, and debug assertions remain unchanged. |
| Runtime output DTOs | Runtime event, cycle output, session output, and data fetch count fields remain unchanged. |
| Backtest output/report | Summary metric groups, benchmark fields, period returns, optional v4 artifact, and debug values remain unchanged. |
| Public facade | `qrpc_core::*` access remains preserved through crate-root `pub use runtime_io_contract::*;`. |

## Hard Boundaries Held

BE-001SB-02 did not:

- change runtime IO fields, serde attributes, helper method behavior, public exports, or tests;
- move RFC execution contracts beginning at `MarketScope`, crate-root tests, or closed qrpc-core child modules;
- change error/proto/plugin/strategy IR/protocol primitive/runtime config/artifact specs contracts;
- change downstream compiler/runtime/backend/executor/frontend behavior or release transition.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-pre-commit-hook.ps1`

## Next Step

BE-001SB-03 `root.contracts.qrpc_core.runtime_io_contract` single_leaf_closeout.
