# v4.16.0 root.contracts.runtime_support.data_module.historical_cache extract closeout

> Batch: BE-001ZM-02
> Node: `root.contracts.runtime_support.data_module.historical_cache`
> Parent: `root.contracts.runtime_support.data_module`
> Stage: `actual_extraction`
> Movement: Rust code movement.

---

## Extraction Summary

Created:

- `qrpc_runtime/src/data_module/historical_cache.rs`

Updated:

- `qrpc_runtime/src/data_module.rs`

Moved implementation surface:

- `HistoricalBarsCache`
- `historical_cache_path`
- `sanitize_filename_component`
- `load_historical_cache`
- `load_stale_historical_cache`
- `read_historical_cache_body`
- `persist_historical_cache`
- `shared_http_client`
- `fetch_historical_raw_klines`
- `historical_kline_bars_for_backtest`

## Parent Facade

`qrpc_runtime/src/data_module.rs` now owns only:

- `mod historical_cache;`
- `pub(crate) use historical_cache::historical_kline_bars_for_backtest;`
- parent helpers and constants used by the child through the parent boundary.

## Boundary Preservation

| Boundary | Result |
| --- | --- |
| Cache path safety | `sanitize_filename_component` moved with cache path construction. |
| Fresh/stale cache | Fresh and stale cache load behavior moved together. |
| Read bounds | Historical cache max-body checks remain in the child and use the parent constant. |
| Persistence | Directory creation, quota check, temp write, rename, and fsync behavior moved unchanged. |
| Historical fetch | OKX/Binance endpoint selection and parser dispatch are called through the parent exchange surface. |
| Backtest helper | `historical_kline_bars_for_backtest` remains crate-visible through the parent re-export. |
| Normalization | Historical raw kline normalization still goes through the parent-owned normalization import. |

## Exclusions Preserved

No movement was made to:

- request-interval in-memory cache;
- live provider fetch;
- exchange endpoint/parser ownership;
- mock generation;
- quality diagnostics;
- data-module tests.

## Next Step

BE-001ZM-03 `root.contracts.runtime_support.data_module.historical_cache` single_leaf_closeout. The closeout must run terminal leaf control against `qrpc_runtime/src/data_module/historical_cache.rs` before deciding whether to split further.
