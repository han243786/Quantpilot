# v4.16.0 root.contracts.quantscript.v4_static_audit.memory_parser extract closeout

> Batch: BE-002KK-02
> Node: `root.contracts.quantscript.v4_static_audit.memory_parser`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> governance_next_authority: active

---

## Movement

`parse_memory` has been extracted from `quantscript/src/v4_static_audit.rs`
into:

`quantscript/src/v4_static_audit/memory_parser.rs`

The parent keeps a private `parse_memory` wrapper so `machine_block_parser`
continues to call through the parent boundary.

## Ownership Now

| Field | Value |
| --- | --- |
| Child file | `quantscript/src/v4_static_audit/memory_parser.rs` |
| Owned function | `parse_memory` |
| Parent wrapper | `parse_memory` |
| Child imports | `diag`, `split_words`, `parse_qs_type_ref` |
| Owned output | `MachineMemoryField` |

## Preserved Behavior

- memory syntax validation stays on `QSV4113`;
- missing type validation stays on `QSV4114`;
- unsupported type validation stays on `QSV4117`;
- name trimming, raw type preservation, nullable detection, and default value
  behavior are unchanged;
- capability/type parser internals remain outside.

## Gates

Minimum extraction gates were run before commit.

## Next Step

BE-002KK-03 `root.contracts.quantscript.v4_static_audit.memory_parser`
single_leaf_closeout.
