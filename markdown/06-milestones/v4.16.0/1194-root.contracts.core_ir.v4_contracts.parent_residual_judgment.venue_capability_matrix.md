# v4.16.0 root.contracts.core_ir.v4_contracts parent residual judgment selects venue_capability_matrix

> Batch: BE-001VH-01
> Node: `root.contracts.core_ir.v4_contracts`
> Parent: `root.contracts.core_ir`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts` selects
`root.contracts.core_ir.v4_contracts.venue_capability_matrix` as the next child.

The selected child owns the venue execution capability matrix family currently in `qrpc_core_ir/src/v4.rs`:

- `VenueCapabilityMatrix`
- `ExecutionCapabilityKind`
- `CapabilitySupportSource`
- `VenueCapability`
- `V4_FIRST_WAVE_EXECUTION_CAPABILITIES`
- `v4_first_wave_execution_capabilities`
- `unsupported_v4_first_wave_matrix`
- `VenueCapabilityMatrix` validation, lookup, and per-runtime-mode support helpers

The selected child does not own plugin governance, compile-time capability reports, static bundles, runtime mode
contract, QS type system, version manifest, reproducibility, complexity budget, bridge builders, tests, machine
contract, machine graph contract, or QS profile.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `venue_capability_matrix` is a named white-box child for venue execution capability support. |
| parent_child_communication_kept | pass | The child can import runtime mode types through the v4 surface and remain re-exported by `qrpc_core_ir/src/v4.rs`. |
| equivalence_baseline_freezable | pass | First-wave capabilities, unsupported matrix defaults, and validation/lookup errors are finite. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | Venue matrix types remain public through the v4 facade. |
| state_machine_phase | no | The child models venue execution capability support, not graph state phases. |
| strategy_branch | guarded | It is a venue/runtime capability branch used by plugin and compile-time reports. |
| independent_failure_mode | pass | Venue capability support failures are independent from QS type system, plugin manifest rules, and graph contracts. |
| reuse_pressure | pass | Plugin governance, compile-time capability reports, backtest artifacts, and tests use venue capability types. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The child owns DTOs, constants, default matrix construction, validation, and runtime-mode support checks. |
| communication_cost_rises | no | A single child module preserves the public v4 surface through re-export. |
| local_proof_missing | no | Existing qrpc-core-ir tests cover venue matrix defaults, support lookup, and mode/source checks. |
| line_count_only | no | Selection is based on venue capability ownership. |

leaf_split_decision_result

`continue_split: true`

Selected next child:

`root.contracts.core_ir.v4_contracts.venue_capability_matrix`

next_recursive_step

BE-001VI-01 root.contracts.core_ir.v4_contracts.venue_capability_matrix baseline_plan
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs`

**Markers**:
- `BE-001VH-01`

**Next step**:
BE-001VI-01 root.contracts.core_ir.v4_contracts.venue_capability_matrix baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
