# v4.16.0 root.contracts.core_ir.v1_contract parent residual judgment selects policy_execution_contract

> Batch: BE-001TN-01
> Node: `root.contracts.core_ir.v1_contract`
> Parent: `root.contracts.core_ir`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v1_contract` returns to its child queue after `data_indicator_expression_contract` closeout.

Selected next child:

`root.contracts.core_ir.v1_contract.policy_execution_contract`

Next child owns the remaining production DTOs in `qrpc_core_ir/src/v1.rs`: signal rule, agent policy, risk policy, execution rule, related enums, and serde default helpers.

## Residual Queue

| Child | Status |
| --- | --- |
| `contracts.core_ir.v1_contract.root_graph_contract` | Closed with `stop_split: true`. |
| `contracts.core_ir.v1_contract.data_indicator_expression_contract` | Closed with `stop_split: true`. |
| `contracts.core_ir.v1_contract.policy_execution_contract` | Selected next. Owns production policy/execution DTOs and defaults. |
| `contracts.core_ir.v1_contract.test_fixture` | Queued. Owns the local v1 round-trip regression test after production children close. |

## Selection Rationale

`policy_execution_contract` is selected before `test_fixture` because it is the last production residual inside the v1 parent:

- `SignalRule` bridges indicators/expressions into agent policy inputs;
- `AgentPolicy` and related enums define decision policy shape;
- `RiskPolicy` defines risk guard schema;
- `ExecutionRule`, sizing/time-in-force enums, and default helpers define execution request shape;
- local tests must remain parent-local until the production schema owner is extracted.

## White-Box Boundary

| Surface | Owner | Input | Output | State |
| --- | --- | --- | --- | --- |
| Signal rule DTO | `policy_execution_contract` | signal id, indicator id, expression condition | v1 signal rule schema | none |
| Agent policy DTOs | `policy_execution_contract` | signal ids, rebalance config, thresholds | v1 agent policy schema | none |
| Risk policy DTO | `policy_execution_contract` | agent ids and risk limits | v1 risk policy schema | none |
| Execution DTO | `policy_execution_contract` | venue/sizing/cost/time-in-force params | v1 execution schema | none |
| Default helpers | `policy_execution_contract` | serde missing fields | default booleans, sizing kind, time-in-force | none |

## Guardrails

The next `policy_execution_contract` baseline must not:

- move tests;
- move root graph or expression child files;
- change any serde shape, default, enum variant, public name, or helper return value;
- change v4 contracts, qrpc-core contracts, compiler/runtime/backend/executor/frontend behavior;
- introduce sibling direct wiring or release-transition shortcuts.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `policy_execution_contract` owns the remaining production policy/execution DTO surface in `v1.rs`. |
| parent_child_communication_kept | pass | It will communicate through the v1 parent facade and may import expression child names through `super::...`. |
| equivalence_baseline_freezable | pass | The next baseline can freeze public names, serde defaults, and the local round-trip test evidence. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | pass | These DTOs are public qrpc-core-ir v1 API. |
| state_machine_phase | no | The child is schema/default contract, not runtime state-machine behavior. |
| strategy_branch | pass | Signal, agent, risk, and execution are distinct strategy graph layers. |
| independent_failure_mode | pass | Policy/execution drift can break downstream consumers independently from root graph/expression drift. |
| reuse_pressure | pass | Compiler/runtime consumers reuse these names through the v1 facade. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The selected child has a clear production schema owner. |
| communication_cost_rises | no | One production residual child reduces parent pressure without adding sibling shortcuts. |
| local_proof_missing | no | The v1 round-trip test remains available for the next extraction. |
| line_count_only | no | Selection is based on production ownership, not file size. |

leaf_split_decision_result

`continue_baseline: true`; `policy_execution_contract` is selected as the next v1 child.

## Next Step

BE-001TO-01 `root.contracts.core_ir.v1_contract.policy_execution_contract` baseline_plan.

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
