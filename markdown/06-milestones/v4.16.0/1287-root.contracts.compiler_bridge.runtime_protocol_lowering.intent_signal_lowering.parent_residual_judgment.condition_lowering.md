# v4.16.0 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering parent residual judgment selects condition_lowering

> Batch: BE-001XM-01
> Node: `root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering`
> Parent: `root.contracts.compiler_bridge.runtime_protocol_lowering`
> Stage: `parent_residual_judgment`
> Movement: no Rust code movement; parent queue selection only.

---

## Summary

`root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering` selects `condition_lowering` as the next internal child.

This child owns runtime structured condition construction for MA/SMA, LongTermSell, RSI, Momentum, ZScore, and QuoteObserve spread-threshold paths. It remains separate from the parent-facing intent indicator/signal facade, fallback condition descriptions, intent kind mapping, and future comparison-decode residuals unless the next baseline explicitly includes tightly coupled helpers.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | Selected child is `root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.condition_lowering`. |
| parent_child_communication_kept | pass | Child remains under `intent_signal_lowering`; parent-facing signal rule facade can call it privately. |
| equivalence_baseline_freezable | pass | Structured condition outputs are already asserted by qrpc-compiler tests for MA, RSI, Momentum, ZScore, and spread branches. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | true | Parent-facing `lower_runtime_intent_to_signal_rule` depends on this private condition phase. |
| state_machine_phase | true | Condition lowering is the expression-construction phase after intent-to-signal selection. |
| strategy_branch | true | Condition lowering is separate from fallback description and intent kind mapping. |
| independent_failure_mode | true | Unsupported comparison shape/op combinations return fallback independently of indicator construction. |
| reuse_pressure | true | Comparison decode helpers feed multiple condition branches. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | false | The selected child owns multiple structured expression branches and their local failure behavior. |
| communication_cost_rises | false | Parent-mediated calls can keep the signal facade and condition construction separated. |
| local_proof_missing | false | Existing qrpc-compiler runtime protocol tests cover representative branches. |
| line_count_only | false | Selection is based on branch/failure-domain ownership, not merely code size. |

leaf_split_decision_result

`selected: condition_lowering`

Next recursive step must freeze a baseline before any Rust movement.

next_recursive_step

BE-001XN-01 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.condition_lowering baseline_plan
## Boundary

**Real files**:
- `qrpc_compiler/src/runtime_protocol_lowering/intent_signal_lowering.rs`

**Markers**:
- `BE-001XM-01`
- `selected condition_lowering`
- `comparison decode dependency remains scoped`
- `no Rust code movement`

**Next step**:
BE-001XN-01 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.condition_lowering baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
