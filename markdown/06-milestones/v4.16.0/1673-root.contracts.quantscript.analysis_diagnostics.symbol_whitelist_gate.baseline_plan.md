# v4.16.0 root.contracts.quantscript.analysis_diagnostics.symbol_whitelist_gate baseline plan

> Batch: BE-002HC-01
> Node: `root.contracts.quantscript.analysis_diagnostics.symbol_whitelist_gate`
> Parent: `root.contracts.quantscript.analysis_diagnostics`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Scope

Freeze the QuantScript fetch symbol whitelist diagnostic before movement.

Current owner:

- `quantscript/src/analysis_diagnostics/analysis.rs`

Planned child:

- `quantscript/src/analysis_diagnostics/symbol_whitelist_gate.rs`

## Items Frozen For Movement

The next extraction may move only:

- `KNOWN_SYMBOLS`
- `check_fetch_symbol_whitelist`
- `check_fetch_symbol_whitelist_from_stmts`
- `check_fetch_symbol_whitelist_from_expr`

## Diagnostic Boundary

The child owns:

- the known instrument whitelist catalog used by the diagnostic;
- recursive AST traversal for `fetch()` and `get_data()` symbol literals;
- `QS0505` unknown symbol whitelist warning emission.

## Parent Wiring To Preserve

The analysis parent must keep:

- `ScriptAnalysis` and `analyze_script_module` public surface unchanged;
- diagnostic family invocation order unchanged;
- crate-root public exports and `crate::analysis` / `crate::diagnostics` compatibility unchanged;
- strategy presence, fetch lookback, index bounds, dead-code emit, and already closed diagnostic children unchanged.

The child may expose:

- `check_fetch_symbol_whitelist` as the only parent-callable entrypoint.

## Forbidden Movement

The next extraction must not move or change:

- strategy fetch/emit presence checks;
- fetch lookback warnings;
- data source map and index bounds checks;
- dead-code emit checks;
- diagnostic DTO/span types;
- unsupported construct, lookahead-window, warmup/fetch, and indirect-recursion children already closed;
- evaluator normalization, runtime lowering, v4 static audit, tests, authoring samples, backend, executor, frontend, plugin metadata, or release transition.

## Expected Validation

The extraction must pass:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002HC-02 `root.contracts.quantscript.analysis_diagnostics.symbol_whitelist_gate` actual_extraction.
