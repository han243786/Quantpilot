# v4.16.0 runtime_facade_coordinator.state_config_accessors baseline plan

> Batch: BE-001YN-01
> Node: `root.contracts.runtime_support.runtime_facade_coordinator.state_config_accessors`
> Parent: `root.contracts.runtime_support.runtime_facade_coordinator`
> Stage: `baseline_plan`
> Movement: No code movement.

## Physical Scope

This child freezes the public state/provider/risk/execution-assumption accessor surface currently located in `qrpc_runtime/src/runtime_facade_coordinator.rs`.

Owned methods:

- `RuntimeCoordinator::portfolio_state`
- `RuntimeCoordinator::data_fetch_counts`
- `RuntimeCoordinator::last_action_at_ms`
- `RuntimeCoordinator::last_rebalance_at_ms`
- `RuntimeCoordinator::data_module`
- `RuntimeCoordinator::intent_module`
- `RuntimeCoordinator::agent_module`
- `RuntimeCoordinator::risk_checker`
- `RuntimeCoordinator::risk_mode`
- `RuntimeCoordinator::set_risk_mode`
- `RuntimeCoordinator::set_risk_monitor`
- `RuntimeCoordinator::is_risk_stopped`
- `RuntimeCoordinator::reset_risk_stopped`
- `RuntimeCoordinator::execution_module_provider_key`
- `RuntimeCoordinator::set_execution_assumptions`

## Exclusions

This child does not own:

- `RuntimeCoordinator` struct definition
- constructor/provider wiring
- session/cycle orchestration
- execution/market entrypoints
- `swap_module_config`
- `apply_pending_module_configs`
- `current_generation`
- `generation_history`
- provider delegation helper implementations
- `portfolio_update_event`
- `refresh_portfolio_state`
- coordinator tests

## Parent/Child Communication Rules

The child must stay below `runtime_facade_coordinator`.

Allowed dependencies:

- `super::RuntimeCoordinator`
- `crate::data_module::DataModuleProvider`
- `crate::intent_module::IntentModuleProvider`
- `crate::agent_module::AgentModuleProvider`
- `crate::risk_checker::RiskCheckerProvider`
- `crate::risk_monitor::RiskMonitor`
- `crate::slippage::ExecutionAssumptions`
- `qrpc_core::{PortfolioState, RiskDecisionMode}`
- `std::collections::BTreeMap`

No sibling-to-sibling shortcut and no release-transition optimization is allowed.

## Actual Extraction Plan

BE-001YN-02 may:

1. Add `mod state_config_accessors;` inside `qrpc_runtime/src/runtime_facade_coordinator.rs`.
2. Add `qrpc_runtime/src/runtime_facade_coordinator/state_config_accessors.rs`.
3. Move only the owned accessor/control methods into the child module.
4. Keep public calls available as `RuntimeCoordinator::*`.
5. Keep config mutation and generation methods in the parent for the next child.
6. Keep crate-root exports unchanged.

## Leaf Split Decision Gate

`continue_split: false` for this child after extraction unless closeout finds concrete new pressure.

Reasons:

- the selected methods form a compact public accessor/control surface
- separating read-only getters from simple setters would create fragments without separate owners
- config generation is deliberately excluded as its own stronger behavior cluster
- provider helper implementations and portfolio projection remain parent residuals

## Equivalence Gates

Required for BE-001YN-02:

- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-001YN-02 `root.contracts.runtime_support.runtime_facade_coordinator.state_config_accessors` actual extraction.
