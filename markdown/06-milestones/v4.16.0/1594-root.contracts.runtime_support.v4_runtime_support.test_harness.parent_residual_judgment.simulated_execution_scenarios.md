# v4.16.0 root.contracts.runtime_support.v4_runtime_support.test_harness parent residual judgment - simulated_execution_scenarios

> Batch: BE-002FI-01
> Node: `root.contracts.runtime_support.v4_runtime_support.test_harness`
> Selected child: `root.contracts.runtime_support.v4_runtime_support.test_harness.simulated_execution_scenarios`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `root.contracts.runtime_support.v4_runtime_support.test_harness.simulated_execution_scenarios` as the next internal test-harness child.

## Evidence

After closing fixture builders, malformed payload validation, and graph/replay scenarios, the test harness still has 22 parent-local tests and remains above the terminal target size. The simulated execution scenario cluster is the next largest cohesive owner because it verifies paper-simulated runtime progression, simulated fills, portfolio updates, order lifecycle transitions, and deterministic tick replay metrics.

The candidate owns:

- `v4_paper_simulated_runtime_runs_compat_bridge_graph_until_execution_ready`
- `v4_runtime_simulated_execution_config_controls_fill_fee_and_asset_curve`
- `v4_runtime_updates_simulated_portfolio_from_market_price`
- `v4_runtime_triggers_stop_market_order_on_market_price_update`
- `v4_runtime_oco_bracket_fills_one_leg_and_cancels_the_other`
- `v4_runtime_trailing_stop_updates_trigger_then_fills`
- `v4_runtime_gtd_order_expires_on_advance_time`
- `v4_runtime_amends_open_order_without_replacing_order_id`
- `v4_backtest_tick_replay_is_deterministic_and_reports_micro_metrics`

## Boundary

The next baseline must freeze only simulated execution scenario test movement. It must not move:

- capability rejection tests;
- live actual mode boundary tests;
- risk-plane source rejection tests;
- runtime recovery/cache snapshot tests;
- fixture builders;
- malformed payload/config rejection tests;
- graph/replay scenario tests already extracted;
- production v4 runtime behavior;
- release-transition optimization.

## Expected Movement Shape

The next movement should be test-only:

- add `qrpc_runtime/src/v4_runtime/test_harness/simulated_execution_scenarios.rs`;
- add `mod simulated_execution_scenarios;` in `qrpc_runtime/src/v4_runtime/test_harness.rs`;
- move only the nine selected `#[test]` functions;
- preserve parent-mediated test access with `use super::*` inside the child.

## Next Step

BE-002FJ-01 `root.contracts.runtime_support.v4_runtime_support.test_harness.simulated_execution_scenarios` baseline_plan.
