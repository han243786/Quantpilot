# v4.16.0 root.executor.runner.strategy_lifecycle_routes baseline plan

> Batch: BE-002LJ-01
> Node: `root.executor.runner.strategy_lifecycle_routes`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Goal

Freeze the strategy lifecycle route boundary before Rust movement.

The first movement may extract only the route handlers from
`src-executor/main.rs` into `src-executor/strategy_lifecycle_routes.rs`.

## Frozen Boundary

| Field | Value |
| --- | --- |
| Planned file | `src-executor/strategy_lifecycle_routes.rs` |
| Approved handlers | `list_strategies`, `get_strategy_detail`, `recv_strategy`, `start_strategy`, `stop_strategy` |
| Parent callers | route registration in `src-executor/main.rs` |
| Parent retained | v4 deploy helpers, preflight helpers, DTOs, audit helper, migration API, runner support files |

## Equivalent Behavior

The extracted child must preserve:

- strategy list response shape;
- strategy detail projection;
- v4/non-v4 deploy path behavior through existing parent helpers;
- migration package loading behavior;
- start/stop behavior and audit append behavior;
- preflight gate behavior;
- runner registration and status mutation behavior.

## Exclusions

- Do not move helper functions or DTOs in this first step.
- Do not move kline or SSE handlers.
- Do not move runner support files.
- Do not alter executor state ownership.
- Do not introduce release-transition links.

## Gates For Actual Extraction

| Gate | Command |
| --- | --- |
| format | `cargo fmt --all --check` |
| executor compile | `cargo check -p quantpilot --bin executor` |
| diff hygiene | `git diff --check` |

## Next Step

BE-002LJ-02 `root.executor.runner.strategy_lifecycle_routes`
actual_extraction.
