# v4.16.0 root.contracts.core_ir.v4_contracts.compile_time_capability_report parent residual judgment selects report_builder

> Batch: BE-001WC-01
> Node: `root.contracts.core_ir.v4_contracts.compile_time_capability_report`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.compile_time_capability_report` selects
`root.contracts.core_ir.v4_contracts.compile_time_capability_report.report_builder` as the next child.

The selected child owns only:

- `V4StaticContractBundle::build_compile_time_capability_report`
- `V4StaticContractBundle::build_type_capability_entries`
- `V4StaticContractBundle::build_execution_capability_entries`
- `V4StaticContractBundle::build_plugin_capability_entries`
- `push_capability_diagnostic`

It does not own request/report DTOs, status/entry DTOs, compile-time default helpers,
`V4CompileTimeCapabilityReport::validate_for_compile`, tests, sample builders, static bundle implementation, or Core IR bridge code.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `report_builder` is a named behavior child for compile-time report construction and diagnostics. |
| parent_child_communication_kept | pass | The child can implement builder methods on `V4StaticContractBundle` while parent module owns DTO schema. |
| equivalence_baseline_freezable | pass | Builder branches, diagnostic codes, and entry assembly paths are finite. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | satisfied | `build_compile_time_capability_report` is public behavior on `V4StaticContractBundle`. |
| state_machine_phase | no | This is report construction, not state-machine topology. |
| strategy_branch | no | This is not strategy branch behavior. |
| independent_failure_mode | pass | Builder diagnostics and entry assembly fail independently from report schema and final compile validation. |
| reuse_pressure | pass | Capability report tests exercise accepted and rejected builder paths. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The child owns one public builder plus coherent private entry builders. |
| communication_cost_rises | no | One nested builder module keeps diagnostic construction together. |
| local_proof_missing | no | Existing qrpc-core-ir tests cover capability report builder behavior. |
| line_count_only | no | Selection is based on report construction ownership. |

leaf_split_decision_result

`continue_split: true`

Selected next child:

`root.contracts.core_ir.v4_contracts.compile_time_capability_report.report_builder`

next_recursive_step

BE-001WD-01 root.contracts.core_ir.v4_contracts.compile_time_capability_report.report_builder baseline_plan
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/compile_time_capability_report.rs`

**Markers**:
- `BE-001WC-01`

**Next step**:
BE-001WD-01 root.contracts.core_ir.v4_contracts.compile_time_capability_report.report_builder baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
