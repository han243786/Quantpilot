# v4.16.0 data_module.source_mapping single leaf closeout

> Batch: BE-001ZA-03
> Node: `root.contracts.runtime_support.data_module.source_mapping`
> Parent: `root.contracts.runtime_support.data_module`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

## Leaf Status

`stop_split: true`.

The source mapping child is now a terminal leaf.

## Split Decision Gate

The child does not need another recursive split.

Reasons:

- `data_sources_from_core_ir` and the hint parsers form one cohesive mapping boundary
- all helpers serve the same output construction contract, `DataSourceConfig`
- splitting by hint type would create tiny private helper fragments
- no independent runtime owner exists inside this child after extraction
- tests already validate the public mapping behavior through the parent-visible function

## Equivalence Evidence

The actual extraction batch passed:

- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

The commit hook for `d6bd96a6` also re-ran:

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`
- `cargo fmt --check`
- `cargo check -p quantpilot`

## Parent Residuals

`data_module` remains open.

Remaining child queue:

1. `contracts.runtime_support.data_module.quality_diagnostics`
2. `contracts.runtime_support.data_module.collection_orchestration`
3. `contracts.runtime_support.data_module.exchange_endpoints`
4. `contracts.runtime_support.data_module.exchange_payload_parsing`
5. `contracts.runtime_support.data_module.normalization`
6. `contracts.runtime_support.data_module.mock_data_generation`
7. `contracts.runtime_support.data_module.historical_cache`
8. `contracts.runtime_support.data_module.http_transport`
9. `contracts.runtime_support.data_module.test_harness`

## Next Step

BE-001ZB-01 `root.contracts.runtime_support.data_module` parent_residual_judgment selects the next child.
