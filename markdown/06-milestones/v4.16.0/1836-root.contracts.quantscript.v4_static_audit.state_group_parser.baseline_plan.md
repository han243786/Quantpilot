# v4.16.0 root.contracts.quantscript.v4_static_audit.state_group_parser baseline plan

> Batch: BE-002KQ-01
> Node: `root.contracts.quantscript.v4_static_audit.state_group_parser`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Goal

Freeze the state-group parser boundary before Rust movement.

This child owns one state-group declaration rule and `StateGroup`
construction.

## Frozen Boundary

| Field | Value |
| --- | --- |
| Approved function | `parse_state_group` |
| Planned file | `quantscript/src/v4_static_audit/state_group_parser.rs` |
| Inputs | state-group line text and source line number |
| Outputs | `StateGroup` or `Diagnostic` |
| Parent callers | `machine_block_parser` through parent wrapper |
| Visibility | child function may be `pub(super)` only for the parent wrapper |

## Equivalent Behavior

The extracted child must preserve:

- nested/open-brace diagnostic `QSV4111`;
- missing members diagnostic `QSV4112`;
- whitespace-based state id expansion;
- `TransitionConflictPolicy::Error` default;
- `timeout_ms: None` default.

## Exclusions

- Do not move `parse_machine_template`.
- Do not move public report DTOs or local tests.
- Do not alter already closed child modules.
- Do not introduce release-transition cross-child links.

## Allowed Rust Movement For Next Step

BE-002KQ-02 may:

1. add `mod state_group_parser;`;
2. create `quantscript/src/v4_static_audit/state_group_parser.rs`;
3. move the `parse_state_group` implementation body;
4. keep the parent private wrapper named `parse_state_group`;
5. adjust imports and `pub(super)` visibility only where required by this
   extraction.

## Gates For Actual Extraction

| Gate | Command |
| --- | --- |
| format | `cargo fmt --all --check` |
| crate tests | `cargo test -p quantscript` |
| workspace compile | `cargo check -p quantpilot` |
| diff hygiene | `git diff --check` |

## Next Step

BE-002KQ-02 `root.contracts.quantscript.v4_static_audit.state_group_parser`
actual_extraction.
