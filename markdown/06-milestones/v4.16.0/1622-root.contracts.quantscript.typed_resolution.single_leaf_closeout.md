# v4.16.0 root.contracts.quantscript.typed_resolution single leaf closeout

> Batch: BE-002GB-03
> Node: `root.contracts.quantscript.typed_resolution`
> Parent: `root.contracts.quantscript`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Close this extraction step as equivalent, but do not mark `typed_resolution` terminal.

`stop_split: false`

The structural move from `quantscript/src/resolve.rs` to `quantscript/src/resolve/mod.rs` is equivalent, but the resulting resolver parent is too large and internally multi-domain to be a final leaf.

## Evidence

BE-002GB-02 verified:

- `cargo fmt --check` PASS
- `cargo test -p quantscript` PASS, 88 tests
- `cargo check -p quantpilot` PASS
- `git diff --check` PASS
- UTF-8 check PASS
- full feature tree check PASS
- matrix governance check PASS

## Terminal Leaf Control

`tools/evaluate-leaf-granularity.ps1 -Path quantscript/src/resolve.rs` returned:

- decision: `PRECISION`
- split decision: `CONTINUE`
- governance packaging: `precision_single_leaf`
- score: 61
- effective LOC: 2543
- functions: 93
- public surface: 46
- branches: 183
- domains: 7
- risk tags: compiler contract, cross-crate, public API, state-machine, trading semantics, security, persistence, lock

## Residual Families

The next parent residual judgment should select a smaller internal child from the resolver owner. Candidate families visible in `quantscript/src/resolve/mod.rs`:

- public type surface and callable classification tables;
- resolver orchestration and AST-to-HIR lowering;
- type inference and binding insertion;
- expression semantic inference;
- manual indicator formula inference;
- callable registry and helper classification;
- resolver-local test harness.

## Boundary

The next step must not change:

- public crate-root exports from `quantscript/src/lib.rs`;
- `crate::resolve::*` downstream import compatibility;
- parser/HIR/type DTO ownership under `syntax_ast_surface`;
- deprecated config compatibility;
- analysis diagnostics, evaluator normalization, runtime lowering, v4 static audit, test plan, authoring samples, backend, runtime support, compiler bridge, executor, frontend, plugin metadata, or release transition.

## Next Step

BE-002GC-01 `root.contracts.quantscript.typed_resolution` parent_residual_judgment.
