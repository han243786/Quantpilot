# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract parent residual judgment selects event_catalog

> Batch: BE-001UF-01
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract` returns to its internal residual queue after the parent leaf decision kept it open.

Selected next child:

`root.contracts.core_ir.v4_contracts.machine_graph_contract.event_catalog`

This child owns the event catalog schema family and local catalog validation.

## Residual Queue

| Child | Status |
| --- | --- |
| `machine_graph_contract.event_catalog` | Selected next. |
| `machine_graph_contract.graph_static_validation` | Queued after event catalog extraction. |
| `machine_graph_contract.schema_and_helpers` | Parent residual after behavior children close. |

## Selection Rationale

`event_catalog` is selected because:

- it has independent public schema DTOs under machine graph contracts;
- `MachineEventCatalog::validate_static_contract` validates catalog-local invariants without needing the whole graph;
- graph-level validation can consume event catalog types through the machine graph parent;
- existing event catalog tests prove local validation behavior.

## Explicit Non-Owners

The selected child does not own:

- `V4MachineGraphContract`;
- `MachineGraphEdge`, `MachineGraphEdgeActivation`, or `MachineGraphRiskPlane`;
- `V4MachineGraphContract` validation methods;
- `machine_event_party_allowed`;
- graph traversal helpers;
- graph-level emitter/consumer checks.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `event_catalog` is a named event schema and catalog validation owner. |
| parent_child_communication_kept | pass | The graph parent can import catalog types from the child. |
| equivalence_baseline_freezable | pass | Event catalog fields and local validation branches can be frozen exactly. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | pass | Catalog DTOs are public v4 schema API. |
| state_machine_phase | pass | Catalog describes graph event typing and permissions. |
| strategy_branch | guarded | Graph validation consumes catalog data, but graph-level semantics stay outside this child. |
| independent_failure_mode | pass | Catalog validation has independent failures for event identity and payload shape. |
| reuse_pressure | pass | Graph validation and tests reuse the catalog schema. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | Catalog schema and validation are a real sub-contract. |
| communication_cost_rises | no | Moving catalog schema/validation together keeps graph parent dependency simple. |
| local_proof_missing | no | Existing event catalog tests pass. |
| line_count_only | no | Selection is by event catalog ownership. |

leaf_split_decision_result

`continue_baseline: true`; `event_catalog` is selected as the next graph child.

## Next Step

BE-001UG-01 `root.contracts.core_ir.v4_contracts.machine_graph_contract.event_catalog` baseline_plan.

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
