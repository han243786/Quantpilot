# v4.16.0 root.contracts.qrpc_core.runtime_io_contract.backtest_output baseline plan

> Batch: BE-001SN-01
> Node: `root.contracts.qrpc_core.runtime_io_contract.backtest_output`
> Parent: `root.contracts.qrpc_core.runtime_io_contract`
> Stage: `baseline_plan`
> Movement: No code movement.

---

## Summary

`backtest_output` freezes the backtest result DTO family before physical extraction.

This baseline may only prepare the next extraction of:

- `BacktestEquityPoint`
- `BacktestRiskAdjusted`
- `BacktestTradeAnalysis`
- `BacktestDrawdownAnalysis`
- `BacktestBenchmarkComparison`
- `BacktestSummary`
- `PeriodReturn`
- `BacktestOutput`

## White Box Boundary

| Item | Owner | Input | Output | Side effects |
| --- | --- | --- | --- | --- |
| `BacktestEquityPoint` | `backtest_output` | timestamp, equity, cash, net notional | equity curve point schema | none |
| Backtest metric groups | `backtest_output` | risk, trade, drawdown, benchmark values | nested metric DTO schemas | none |
| `BacktestSummary` | `backtest_output` | step/trade/return/equity and nested metrics | summary schema | none |
| `PeriodReturn` | `backtest_output` | period label, return ratio, trade count | period return schema | none |
| `BacktestOutput` | `backtest_output` | sessions, equity curves, summary, portfolio, artifact/debug data | final backtest output schema | none |

## Equivalence Baseline

The next extraction must preserve:

- `BacktestEquityPoint`: `ts_ms`, `equity`, `cash_balance`, and `net_notional`.
- `BacktestRiskAdjusted`: `sharpe_ratio`, `sortino_ratio`, `calmar_ratio`, `var_95`, and `cvar_95`, all with existing defaults.
- `BacktestTradeAnalysis`: `profit_factor`, `avg_win`, `avg_loss`, `max_consecutive_wins`, and `max_consecutive_losses`, all with existing defaults.
- `BacktestDrawdownAnalysis`: `max_drawdown_ratio`, `max_drawdown_duration_days`, and `avg_drawdown_duration_days`, all with existing defaults.
- `BacktestBenchmarkComparison`: `benchmark_total_return`, `alpha`, `beta`, and `information_ratio`, all with existing defaults.
- `BacktestSummary`: count, return/equity, defaulted profit/win/annualized fields, nested metric groups, optional benchmark comparison, skewness, and kurtosis.
- `PeriodReturn`: `period`, `return_ratio`, and `trade_count`.
- `BacktestOutput`: `mode`, timestamps, optional/default elapsed time, sessions, equity curves, benchmark equity curve skip rule, period returns skip rule, summary, final portfolio, optional v4 artifact skip rule, and optional debug values.
- Existing `Serialize` / `Deserialize` derives and `Default` derives.
- Existing parent and crate-root facade exports.

## Extraction Plan

BE-001SN-02 may:

- create `qrpc_core/src/runtime_io_contract/backtest_output.rs`;
- add `mod backtest_output;` and `pub use backtest_output::*;` to the runtime IO parent;
- move only the backtest output DTO family listed above;
- import `SessionOutput` and `PortfolioState` through the runtime IO parent facade;
- keep `qrpc_core_ir::v4::V4BacktestArtifact` and debug value map shapes unchanged.

BE-001SN-02 must not:

- move RFC execution contracts or crate-root tests;
- reopen closed runtime IO children;
- change serde default/skip rules or nested metric defaults;
- change downstream runtime/backend/compiler behavior;
- introduce release-transition optimization.

## Same-Parent Parallel Judgment

`same_parent_parallel_children` remains inactive for this leaf.

Reason: `backtest_output` is the final runtime IO residual and contains nested summary DTOs consumed by one final output container. Serial extraction keeps the final parent closeout straightforward.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `backtest_output` owns final backtest result DTOs and nested metric DTOs. |
| parent_child_communication_kept | pass | `SessionOutput` and `PortfolioState` remain parent-mediated through the runtime IO facade. |
| equivalence_baseline_freezable | pass | DTO fields, derives, serde defaults, skip rules, and nested metric groups are listed above. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | pass | Backtest output is part of qrpc-core public runtime IO contract. |
| state_machine_phase | no | No runtime state-machine phase logic is moved. |
| strategy_branch | no | No strategy branch is moved. |
| independent_failure_mode | pass | Backtest output schema drift can be proven independently from runtime output DTOs. |
| reuse_pressure | pass | Runtime/backend backtest response paths consume these DTOs through the qrpc-core facade. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The family is coherent enough for one extraction, but post-extraction closeout must still judge metric subchildren. |
| communication_cost_rises | no | Parent facade re-export keeps current consumers stable. |
| local_proof_missing | no | qrpc-core gates cover the schema move. |
| line_count_only | no | Split is by final backtest output ownership, not raw line count. |

leaf_split_decision_result

`continue_extract: true`; `backtest_output` is worth extracting as one child. BE-001SN-03 must decide whether nested metric DTOs justify further split after the child file exists.

## Next Step

BE-001SN-02 `root.contracts.qrpc_core.runtime_io_contract.backtest_output` extract_closeout.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
