# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline parent residual judgment - realtime_sandbox

> Batch: BE-002CW-01
> Node: `root.contracts.runtime_support.sandbox_replay_timeline`
> Selected child: `root.contracts.runtime_support.sandbox_replay_timeline.realtime_sandbox`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.

---

## Decision

Select `realtime_sandbox` as the next sandbox/replay/timeline child.

## Evidence

Timeline providers, unified timeline, replay builder, and sandbox mode surface are closed. The next cohesive boundary in `qrpc_runtime/src/sandbox/mod.rs` is the concrete realtime sandbox implementation:

- `RealTimeSandbox`;
- constructors and test-mode accessors;
- `Sandbox` trait implementation for realtime simulation;
- handoff/restore behavior for realtime sandbox;
- realtime risk monitor helper methods.

## Candidate Boundary

The next baseline should move only `RealTimeSandbox` and its impl blocks into a dedicated child file.

Out of scope:

- `FastBacktestSandbox`;
- benchmark/equity helper functions;
- parent shared helpers `snapshot_from`, `ensure_running`, and `trace_id`;
- local sandbox tests;
- replay/timeline/mode-surface children.

## Dependency Rule

`realtime_sandbox` must remain parent-mediated. It may use shared parent helpers through the sandbox parent, but it must not import from fast-backtest or other sibling children directly.

## Next Step

BE-002CX-01 `root.contracts.runtime_support.sandbox_replay_timeline.realtime_sandbox` baseline_plan.
