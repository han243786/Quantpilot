# v4.16.0 root.contracts.compiler_bridge parent residual judgment selects runtime_protocol_validation

> Batch: BE-001XA-01
> Node: `root.contracts.compiler_bridge`
> Parent: `root.contracts`
> Stage: `parent_residual_judgment`
> Movement: no Rust code movement; parent queue selection only.

---

## Summary

`root.contracts.compiler_bridge` selects `runtime_protocol_validation` as the first child after the compiler bridge baseline.

This child is chosen because runtime protocol validation is an independent failure domain that runs before compile/lowering, owns clear validation-only helpers, and can remain mediated through the compiler bridge parent without touching public compile/lowering entrypoints.

No Rust code moves in this batch.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | Selected child is `root.contracts.compiler_bridge.runtime_protocol_validation`. |
| parent_child_communication_kept | pass | Child remains under compiler bridge parent; public `validate_runtime_protocol_config` stays parent-mediated. |
| equivalence_baseline_freezable | pass | Source remains unchanged; next step freezes validation helper list before extraction. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | true | Validation is reached through public `validate_runtime_protocol_config`. |
| state_machine_phase | false | This child is validation-only, not a lowering/state-machine phase owner. |
| strategy_branch | true | It separates runtime protocol validation from runtime protocol lowering and Strategy IR lowering. |
| independent_failure_mode | true | Data source, intent, agent, risk, and unique-id validation fail independently before compile/lowering. |
| reuse_pressure | true | The same validation is used by compile and lowering public entrypoints. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | false | Validation helpers form a cohesive owner around `RuntimeProtocolCoreConfig`. |
| communication_cost_rises | false | Parent facade can call one child validation entrypoint without adding sibling coupling. |
| local_proof_missing | false | Existing `qrpc-compiler` tests cover runtime validation failures and compile/lowering paths. |
| line_count_only | false | Selection is based on validation ownership and failure mode, not raw line count. |

leaf_split_decision_result

`selected: runtime_protocol_validation`.

Next recursive step: BE-001XB-01 freezes the child baseline. No Rust code movement is allowed before that baseline.

next_recursive_step

BE-001XB-01 root.contracts.compiler_bridge.runtime_protocol_validation baseline_plan
## Boundary

**Real files**:
- `qrpc_compiler/src/lib.rs`

**Markers**:
- `BE-001XA-01`
- `root.contracts.compiler_bridge selected runtime_protocol_validation`
- `compiler bridge source remains frozen`
- `next runtime protocol validation baseline`

**Next step**:
BE-001XB-01 root.contracts.compiler_bridge.runtime_protocol_validation baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
