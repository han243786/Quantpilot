# v4.16.0 root.contracts.qrpc_core.artifact_specs.run_backtest_specs single leaf closeout

> Batch: BE-001RW-03
> Node: `root.contracts.qrpc_core.artifact_specs.run_backtest_specs`
> Parent: `root.contracts.qrpc_core.artifact_specs`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core.artifact_specs.run_backtest_specs` has been evaluated after BE-001RW-02 extraction.

Decision:

`stop_split: true`

The node is now a compact run/backtest schema child module with one responsibility: run/backtest modes, dataset and execution assumption projections, market data snapshot specs, `RunSpec`, and `BacktestSpec`.

## Split Rule Evaluation

| Rule | Result |
| --- | --- |
| Physical owner | Stop. The selected surface is isolated in `qrpc_core/src/artifact_specs/run_backtest_specs.rs`. |
| Public method count | Stop. The child exposes projection/constructor helpers that all serve the same run/backtest spec family. |
| Mixed responsibility | Stop. Dataset projection, execution assumptions, snapshot spec, run spec, and backtest spec are one coherent run/backtest boundary. |
| State ownership | Stop. The child owns no runtime mutable state. |
| Side effects | Stop. No IO, persistence, locks, async lifecycle, external calls, or public API mutation are involved. |
| Parent-mediated dependency | Covered. The artifact specs parent re-exports the child and keeps artifact bundle schemas parent-mediated. |
| Future reopen rule | Allowed only when a concrete run/backtest enum, projection, schema version, constructor, serde shape, or public facade proposal is made. |

## White-Box Boundary

| Input | Processing owner | Output |
| --- | --- | --- |
| Runtime protocol config, run/backtest spec inputs, and digest references | `contracts.qrpc_core.artifact_specs.run_backtest_specs` | Stable run spec, backtest spec, dataset snapshot spec, and execution assumptions |

The leaf may describe and guard:

- run mode and replay source taxonomy;
- data source to dataset projection;
- runtime config to execution assumptions;
- market data snapshot construction;
- run spec construction;
- backtest spec construction.

## Rust Local Fields

| Field | Result |
| --- | --- |
| Crate | `qrpc-core` |
| Child visibility | Private module; public items are re-exported by the artifact specs parent. |
| Public exports | Preserved through `pub use run_backtest_specs::*;` and crate-root `pub use artifact_specs::*;`. |
| Parent facade | `qrpc_core/src/artifact_specs.rs` remains the local coordinator. |
| Production dependency check | Passed. Tests and downstream crates consume run/backtest names through the parent facade. |

## Non-Claims

This closeout does not claim:

- artifact bundle DTOs have been extracted;
- runtime IO contracts, RFC execution contracts, run/backtest behavior, or crate-root tests changed;
- qrpc_core parent residual judgment is complete;
- closed qrpc-core children or release transition changed.

## Proof

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-001RX-01 `root.contracts.qrpc_core.artifact_specs` parent_residual_judgment selects `artifact_bundle_contract`.
