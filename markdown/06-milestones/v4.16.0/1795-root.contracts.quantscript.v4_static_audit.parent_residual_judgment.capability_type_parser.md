# v4.16.0 root.contracts.quantscript.v4_static_audit parent residual judgment: capability_type_parser

> Batch: BE-002JV-01
> Node: `root.contracts.quantscript.v4_static_audit`
> Selected child: `root.contracts.quantscript.v4_static_audit.capability_type_parser`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `capability_type_parser` as the next internal child under
`v4_static_audit`.

`runtime_handoff_builder` and `audit_entrypoint` are both closed with
`stop_split: true`; the parent remains oversized and high-risk. The next
highest-value low-coupling extraction is the capability/type parser cluster,
because it is a cohesive pure parsing group used by top-level static document
parsing and memory type parsing.

## Parent Split Evidence

Latest split-gate check for `quantscript/src/v4_static_audit.rs`:

| Metric | Result |
| --- | --- |
| Decision | `PRECISION` |
| Split decision | `CONTINUE` |
| Governance packaging | `precision_single_leaf` |
| Score | 60 |
| LOC | 1580 |
| Functions | 42 |
| Public surface | 26 |
| Branches | 99 |

The parent must continue precision single-leaf governance. Same-parent batching
is not selected for this high-risk static-audit parser surface.

## Selected Boundary

| Field | Value |
| --- | --- |
| Planned owner | `root.contracts.quantscript.v4_static_audit.capability_type_parser` |
| Primary movement | runtime mode, execution capability, QS type-ref, wrapper, max-arg, and top-level type arg parsing |
| Inputs | `&str` parser inputs |
| Outputs | `RuntimeTradingMode`, `ExecutionCapabilityKind`, `QsTypeRef`, `QsScalarTypeKind`, and parser `String` errors |
| Parent callers | `parse_v4_static_document`, `parse_memory` |
| Parent wrapper | Parent may keep thin private wrappers or directly call the child, to be frozen in baseline |

## Exclusions

- Do not move `parse_machine_template`; it belongs with machine block parsing.
- Do not move `parse_v4_static_document`.
- Do not move machine/state/memory/transition/edge/risk-plane parsers.
- Do not move event catalog derivation or `machine_family`.
- Do not move parser utilities such as `prepare_lines`, `split_words`, `split_csv_words`, or `diag`.
- Do not move public DTO definitions or local tests.
- Do not introduce release-transition cross-child links.

## Gates

| Gate | Result |
| --- | --- |
| precision_parent_open | PASS |
| previous_children_closed | `runtime_handoff_builder`, `audit_entrypoint` |
| boundary_clear | PASS |
| public_api_preserved | no public API movement |
| parser_side_effects | pure parser cluster only |
| code movement | none |

## Next Step

BE-002JW-01 `root.contracts.quantscript.v4_static_audit.capability_type_parser`
baseline_plan.
