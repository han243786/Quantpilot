# v4.16.0 root.contracts.quantscript parent residual judgment selects typed_resolution

> Batch: BE-002GA-01
> Node: `root.contracts.quantscript`
> Selected child: `root.contracts.quantscript.typed_resolution`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Parent State

Closed QuantScript children:

- `contracts.quantscript.syntax_ast_surface`
- `contracts.quantscript.legacy_config_compat`

Remaining QuantScript queue before this selection:

- `typed_resolution`
- `analysis_diagnostics`
- `evaluator_normalization`
- `runtime_lowering`
- `v4_static_audit`
- `test_plan_surface`
- `authoring_docs_samples`

## Decision

Select `root.contracts.quantscript.typed_resolution` as the next child.

This child owns the formal QuantScript typed-HIR resolution contract currently concentrated in `quantscript/src/resolve.rs`. The file is the largest unresolved QuantScript owner after the parser and legacy compatibility extractions, and the crate root exposes its typed resolution public surface through `pub use resolve::{...}`.

## Candidate Physical Surface

Current surface in `quantscript/src/resolve.rs`:

- `ResolveResult`
- `lower_script_to_typed_hir`
- `expr_semantic_key`
- callable classification helpers:
  - `classify_series_capability_name`
  - `classify_builtin_math_name`
  - `classify_member_mutation_name`
- typed resolution DTOs:
  - `ResolvedFunction`
  - `ResolvedCallableKind`
  - `ChangeHelperKind`
  - `KnownIndicatorHelperKind`
  - `KnownUniverseHelperKind`
  - `MovingAverageHelperKind`
  - `RsiHelperKind`
  - `ResolvedChangeSmoothingKind`
  - `ResolvedFetchSourceKind`
  - `ResolvedSeriesCapabilityKind`
  - `ResolvedSeriesBoundaryKind`
  - `ResolvedWindowAggregateKind`
  - `ResolvedBuiltinMathKind`
  - `ResolvedMemberMutationKind`
  - `ResolvedExprSemantic`
  - `ResolvedSeriesViewKind`
  - `ResolvedWindowAggregateView`
  - `ResolvedManualIndicatorFormula`
  - `ResolvedCallable`
- private resolver implementation, semantic inference, callable registry, diagnostic emission, and local tests.

## Boundary

The next baseline must freeze only typed resolution. It must not change:

- `syntax_ast_surface` parser, AST, type, and HIR DTO files already closed;
- deprecated config compatibility already closed in `legacy_config_compat`;
- analysis diagnostics, evaluator normalization, runtime lowering, v4 static audit, test-plan extraction, or authoring samples;
- backend, runtime support, compiler bridge, executor, frontend, plugin metadata, or release-transition behavior.

## Why This Child Next

| Criterion | Judgment |
| --- | --- |
| Queue order | It follows the closed syntax and legacy compatibility children in the frozen QuantScript baseline. |
| Cohesion | Strong: public `Resolved*` DTOs, callable classification, and resolver internals all serve typed HIR resolution. |
| Parent cleanup value | High: `lib.rs` can remain a facade while typed-HIR resolution gets an explicit child boundary. |
| Risk | Medium: `resolve.rs` is large and feeds analysis/lowering, so the next step must freeze public exports and typed-HIR behavior before movement. |

## Next Step

BE-002GB-01 `root.contracts.quantscript.typed_resolution` baseline_plan.
