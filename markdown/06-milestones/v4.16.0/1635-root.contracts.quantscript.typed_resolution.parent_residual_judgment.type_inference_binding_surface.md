# v4.16.0 root.contracts.quantscript.typed_resolution parent residual judgment: type_inference_binding_surface

> Batch: BE-002GI-01
> Node: `root.contracts.quantscript.typed_resolution`
> Selected child: `root.contracts.quantscript.typed_resolution.type_inference_binding_surface`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `root.contracts.quantscript.typed_resolution.type_inference_binding_surface` for the next recursive child.

## Current Parent State

Closed typed-resolution children:

- `root.contracts.quantscript.typed_resolution.public_type_surface`
- `root.contracts.quantscript.typed_resolution.callable_classification_surface`
- `root.contracts.quantscript.typed_resolution.semantic_inference_surface`

Current parent file:

- `quantscript/src/resolve/mod.rs`

The parent still owns resolver orchestration, AST-to-HIR lowering, call/member/type inference, binding insertion, condition validation, type annotation resolution, scalar helpers, resolved function projection, and resolver-local tests.

## Residual Candidate Review

| Candidate | Evidence | Decision |
|-----------|----------|----------|
| `type_inference_binding_surface` | Cohesive block from `infer_call_type` through `resolve_type`; owns call/member/helper return typing, unary/binary/list/index/sequence typing, maybe unwrapping, numeric checks, binding insertion diagnostics, condition validation, and type annotation resolution. | Select |
| `resolver_orchestration_surface` | Contains module/function/block/statement/expression lowering and should remain parent until type helper pocket shrinks. | Queue |
| `resolver_support_surface` | Scalar expression helpers, resolved function projection, and return-list helpers are smaller support residuals; they can wait until parent orchestration is thinner. | Queue |
| `resolver_test_harness` | Tests should move only after production parent pockets are reduced. | Queue |

## Selected Child Scope

The next baseline may freeze only the type inference and binding pocket:

- `infer_call_type`
- `infer_member_capability_type`
- `infer_named_helper_return_type`
- `infer_unary_type`
- `infer_binary_type`
- `common_item_type`
- `index_result_type`
- `sequence_item_type`
- `numeric_sequence_type`
- `iteration_item_type`
- `unwrap_maybe`
- `is_numeric`
- `insert_binding`
- `validate_condition_type`
- `resolve_type`

Likely child file:

- `quantscript/src/resolve/type_inference_binding_surface.rs`

## Forbidden Movement

The next baseline and extraction must not move:

- public resolver DTOs/enums already closed in `public_type_surface`;
- callable/helper classifiers and registry seeding already closed in `callable_classification_surface`;
- semantic/manual indicator inference already closed in `semantic_inference_surface`;
- `lower_script_to_typed_hir`;
- `resolve_module`, `seed_imported_callables`, `seed_function_signatures`, `resolve_function`, `lower_block`, `lower_stmt`, `lower_match_arm`, `lower_expr`, and `lower_callee_expr`;
- `make_expr`, `expr_label`, ID allocation, callable target helpers, scalar expression helpers, resolved function builder, return-list helpers, and resolver-local tests;
- analysis, lowering, evaluator, runtime, backend, executor, frontend, plugin metadata, or release transition.

## Next Step

BE-002GJ-01 `root.contracts.quantscript.typed_resolution.type_inference_binding_surface` baseline_plan.
