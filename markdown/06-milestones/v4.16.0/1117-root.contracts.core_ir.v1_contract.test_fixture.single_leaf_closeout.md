# v4.16.0 root.contracts.core_ir.v1_contract.test_fixture single leaf closeout

> Batch: BE-001TQ-03
> Node: `root.contracts.core_ir.v1_contract.test_fixture`
> Parent: `root.contracts.core_ir.v1_contract`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`test_fixture` is now a closed compact cfg-test leaf.

The extraction in BE-001TQ-02 created a physical owner for the v1 local round-trip regression fixture while preserving the v1 parent facade.

## Closed Surface

| Surface | Result | Evidence |
| --- | --- | --- |
| Physical owner exists | pass | `qrpc_core_ir/src/v1/tests.rs` owns the selected cfg-test fixture. |
| v1 parent facade preserved | pass | `qrpc_core_ir/src/v1.rs` declares `#[cfg(test)] mod tests;`. |
| Test behavior verified | pass | BE-001TQ-02 passed `cargo test -p qrpc-core-ir` and smart pre-commit. |

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | Child owns only the v1 round-trip cfg-test fixture. |
| parent_child_communication_kept | pass | Test child uses `use super::*` through the v1 parent facade. |
| equivalence_baseline_freezable | pass | BE-001TQ-01 froze the exact test fixture. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | no | Test-only child. |
| state_machine_phase | no | Test-only child. |
| strategy_branch | guarded | Test exercises all v1 strategy graph layers. |
| independent_failure_mode | pass | Fixture drift is isolated from production schema owners. |
| reuse_pressure | no | Local cfg-test only. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | pass | Splitting one test function would create ownerless micro leaves. |
| communication_cost_rises | pass | Further split would add test indirection without better proof. |
| local_proof_missing | no | The child itself is the local proof. |
| line_count_only | pass | Further split would be line-count driven only. |

leaf_split_decision_result

`stop_split: true`; `test_fixture` should remain one compact cfg-test leaf.

## Residual Boundary

All planned v1 child leaves are now closed. The next step is v1 parent closeout.

## Next Step

BE-001TR-01 `root.contracts.core_ir.v1_contract` parent_residual_judgment closes parent.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
