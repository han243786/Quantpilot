# v4.16.0 root.contracts.quantscript.analysis_diagnostics parent residual judgment: indirect_recursion_gate

> Batch: BE-002GZ-01
> Node: `root.contracts.quantscript.analysis_diagnostics`
> Selected child: `root.contracts.quantscript.analysis_diagnostics.indirect_recursion_gate`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `root.contracts.quantscript.analysis_diagnostics.indirect_recursion_gate` for the next recursive child.

## Parent Residual Scan

Current parent:

- `quantscript/src/analysis_diagnostics/mod.rs`
- `quantscript/src/analysis_diagnostics/analysis.rs`
- `quantscript/src/analysis_diagnostics/diagnostics.rs`

Closed children:

- `root.contracts.quantscript.analysis_diagnostics.unsupported_construct_gate`
- `root.contracts.quantscript.analysis_diagnostics.lookahead_window_gate`
- `root.contracts.quantscript.analysis_diagnostics.warmup_fetch_gate`

Remaining major diagnostic families in `analysis.rs`:

| Candidate | Boundary | Decision |
| --- | --- | --- |
| `strategy_presence_gate` | strategy fetch/emit existence diagnostics; still shares `contains_emit_in_stmts` with dead-code emit. | Queue |
| `fetch_lookback_warning_gate` | `lookback < 1` warning; currently depends on parent-mediated `arg_number_named`. | Queue |
| `indirect_recursion_gate` | helper call graph and indirect recursion diagnostics. | Select |
| `index_bounds_gate` | data source lookback map and array/index bounds warning; currently depends on parent-mediated `fetch_lookback`. | Queue |
| `dead_code_emit_gate` | constant-false emit warning; currently shares emit traversal logic with strategy presence. | Queue |
| `symbol_whitelist_gate` | known symbol whitelist warning. | Queue |

## Selected Boundary

The next baseline may freeze only:

- `detect_indirect_recursion`
- `collect_callee_names_from_stmts`
- `collect_callee_names_from_expr`

Expected child:

- planned `indirect_recursion_gate` child file.

## Why This Child

`indirect_recursion_gate` is the next best child because:

- it owns one isolated call-graph responsibility;
- it has no dependency on the freshly closed warmup/fetch helper boundary;
- it avoids introducing sibling calls between warning/index-bound families and `warmup_fetch_gate`;
- it can expose a single parent-callable diagnostic entrypoint while preserving current diagnostic order.

## Exclusions

Do not move in the next baseline:

- strategy fetch/emit presence checks;
- fetch lookback warnings;
- data source map and index bounds checks;
- dead-code emit checks;
- symbol whitelist checks;
- diagnostic DTO/span types;
- unsupported construct, lookahead-window, and warmup/fetch children already closed;
- evaluator normalization, runtime lowering, v4 static audit, tests, authoring samples, backend, executor, frontend, plugin metadata, or release transition.

## Next Step

BE-002HA-01 `root.contracts.quantscript.analysis_diagnostics.indirect_recursion_gate` baseline_plan.
