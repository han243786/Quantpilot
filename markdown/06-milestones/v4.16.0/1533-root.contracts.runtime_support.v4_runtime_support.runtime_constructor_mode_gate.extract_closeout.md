# v4.16.0 root.contracts.runtime_support.v4_runtime_support.runtime_constructor_mode_gate extract closeout

> Batch: BE-002DQ-01
> Node: `root.contracts.runtime_support.v4_runtime_support.runtime_constructor_mode_gate`
> Parent: `root.contracts.runtime_support.v4_runtime_support`
> Stage: `actual_extraction`
> Movement: Rust code movement.

---

## Extracted Child

`qrpc_runtime/src/v4_runtime/runtime_constructor_mode_gate.rs` now owns the v4 runtime constructor and mode-gate surface:

- `V4PaperSimulatedRuntime::new`
- `V4PaperSimulatedRuntime::new_for_mode`
- `V4PaperSimulatedRuntime::new_for_mode_inner`
- `V4PaperSimulatedRuntime::new_with_execution_capabilities`
- `V4PaperSimulatedRuntime::new_for_mode_with_execution_capabilities`
- `V4PaperSimulatedRuntime::new_for_backtest`
- `V4PaperSimulatedRuntime::with_execution_capabilities`

The v4 runtime parent now declares `mod runtime_constructor_mode_gate;` and keeps `with_simulated_execution_config`, replay orchestration, transition behavior, risk/execution gates, simulated execution internals, and tests outside this child.

## Equivalence Notes

- Public inherent constructor calls remain available through `V4PaperSimulatedRuntime` / `V4Runtime`.
- `LiveActual` still requires an explicit required Risk Plane.
- `LiveActual` still requires an execution capability policy attached during construction.
- Static graph validation still runs before runtime state creation.
- Machine family runtime state initialization still uses the parent-owned initializer.
- Initial risk-plane, execution, simulated-execution, queue/log, sequence, and provider-submit state remain equivalent.
- The v4 tests now import `RuntimeTradingMode` explicitly instead of relying on a parent include import.

## Verified Gates

- `cargo fmt --check`: PASS
- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime v4 -- --nocapture`: PASS, 32 passed
- `cargo check -p quantpilot`: PASS

Pending governance gates are expected to run before commit:

- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002DQ-02 `root.contracts.runtime_support.v4_runtime_support.runtime_constructor_mode_gate` single_leaf_closeout.
