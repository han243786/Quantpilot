# v4.16.0 runtime_facade_coordinator.provider_delegation_helpers extract closeout

> Batch: BE-001YR-02
> Node: `root.contracts.runtime_support.runtime_facade_coordinator.provider_delegation_helpers`
> Parent: `root.contracts.runtime_support.runtime_facade_coordinator`
> Stage: `actual_extraction`
> Movement: Code movement complete.

## Extracted Files

- `qrpc_runtime/src/runtime_facade_coordinator/provider_delegation_helpers.rs`

## Code Movement

The provider helper methods moved out of `qrpc_runtime/src/runtime_facade_coordinator.rs` into a child module:

- `RuntimeCoordinator::collect_normalized_data`
- `RuntimeCoordinator::evaluate_intents`
- `RuntimeCoordinator::evaluate_agents`
- `RuntimeCoordinator::merge_agent_decisions`
- `RuntimeCoordinator::merge_records`
- `RuntimeCoordinator::evaluate_risks`
- `RuntimeCoordinator::plan_execution`
- `RuntimeCoordinator::execute_plans`
- `RuntimeCoordinator::process_open_orders`

Private helpers are now `pub(super)` so sibling coordinator child modules can keep using the parent-mediated helper surface. `RuntimeCoordinator::merge_records` remains public.

## Public API Equivalence

Public calls remain available as `RuntimeCoordinator::*`.

The crate-root facade remains unchanged through `qrpc_runtime/src/lib.rs`.

## Boundary Result

The child owns provider delegation for data, intent, agent, merge, risk, execution planning, execution submit, and open-order market updates.

The parent still owns:

- `RuntimeCoordinator::portfolio_update_event`
- `RuntimeCoordinator::refresh_portfolio_state`
- `portfolio_equity_estimate`
- `quote_price_map`
- coordinator tests

## Equivalence Gates

Required gates for this batch:

- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-001YR-03 `root.contracts.runtime_support.runtime_facade_coordinator.provider_delegation_helpers` single_leaf_closeout.
