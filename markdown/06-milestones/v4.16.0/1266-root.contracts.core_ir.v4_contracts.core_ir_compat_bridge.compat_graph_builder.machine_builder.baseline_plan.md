# v4.16.0 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder.machine_builder equivalence baseline and extraction plan

> Batch: BE-001WT-01
> Node: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder.machine_builder`
> Parent: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder`
> Stage: `baseline_plan`
> Movement: No code movement. This document freezes the compatibility machine-builder boundary before extraction.

---

## Summary

root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder.machine_builder equivalence baseline and extraction plan

This child owns Observation, Decision, and Execution compatibility machine construction.

## Frozen White-Box Surface

BE-001WT-02 may move only these functions into `qrpc_core_ir/src/v4/core_ir_compat_bridge/compat_graph_builder/machine_builder.rs`:

- `build_core_ir_observation_machine`
- `build_core_ir_decision_machine`
- `build_core_ir_execution_machine`
- `compat_machine`
- `compat_machine_metadata`
- `count_memory_field`

## Forbidden Movement

BE-001WT-02 must not move:

- `build_core_ir_compat_machine_graph`
- graph edge construction
- risk-plane binding
- event catalog builder child
- `compat_edge_metadata`
- `core_ir_edge_labels`
- `string_value_array`
- `sanitize_core_ir_compat_id`

## Allowed Extraction

BE-001WT-02 may create `qrpc_core_ir/src/v4/core_ir_compat_bridge/compat_graph_builder/machine_builder.rs`, add a private nested module declaration in `compat_graph_builder.rs`, and call the machine-builder child from graph assembly.

## Equivalence Baseline

The extraction is equivalent only if these gates pass:

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `cargo check -p quantpilot`

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/core_ir_compat_bridge/compat_graph_builder.rs`

**Markers**:
- `BE-001WT-01`

**Next step**:
BE-001WT-02 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder.machine_builder extract_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
