# v4.16.0 root.contracts.quantscript.v4_static_audit.audit_entrypoint single leaf closeout

> Batch: BE-002JU-03
> Node: `root.contracts.quantscript.v4_static_audit.audit_entrypoint`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Closeout Decision

`audit_entrypoint` is closed with `stop_split: true`.

The extracted child owns one cohesive static-audit orchestration function:
parser invocation, parse-failure rejected report projection, static contract
validation mapping, capability-report integration, final verdict calculation,
and `V4QsStaticAuditReport` projection. Further splitting would only produce
micro helpers that increase governance and parent-child communication cost.

## Leaf Granularity Evidence

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -LeafId root.contracts.quantscript.v4_static_audit.audit_entrypoint -ParentId root.contracts.quantscript.v4_static_audit -Path quantscript\src\v4_static_audit\audit_entrypoint.rs -Depth 4
```

Result:

| Field | Value |
| --- | --- |
| Decision | `STOP` |
| Governance packaging | `stop_split` |
| Score | 23 |
| LOC | 85 |
| Functions | 1 |
| Public surface | 1 |
| Branches | 2 |
| Terminal leaf control | `micro_under_100`, `stop_split` |

Reason: helper-only or micro leaf; governance and communication cost exceed
split value.

## Equivalent Behavior

| Boundary | Closeout Evidence |
| --- | --- |
| Public wrapper | Parent still exposes `audit_v4_quant_script_static` |
| Parser behavior | Parser remains parent-owned and is called by the child |
| Diagnostic mapping | `QSV4200` and `QSV4300` mapping preserved by BE-002JU-02 |
| Report projection | Static audit report shape and runtime/lowering flags unchanged |
| Release transition | No release-transition cross-child connection introduced |

## Verification

Carried from BE-002JU-02 and pre-commit:

- `cargo fmt --all --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

Return to `root.contracts.quantscript.v4_static_audit` parent residual judgment:
BE-002JV-01.
