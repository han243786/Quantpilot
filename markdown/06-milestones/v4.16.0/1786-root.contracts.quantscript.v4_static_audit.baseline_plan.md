# v4.16.0 root.contracts.quantscript.v4_static_audit baseline plan

> Batch: BE-002JQ-01
> Node: `root.contracts.quantscript.v4_static_audit`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Baseline

Freeze `v4_static_audit` as a precision parent before any Rust movement.

The current file `quantscript/src/v4_static_audit.rs` is oversized and
high-risk. It combines public audit DTOs, static QS document parsing, machine
graph parsing, compile-time capability request assembly, runtime handoff report
construction, and local tests. No code may move until a child owner is selected
from the frozen queue below.

## Public Surface To Preserve

| Surface | Owner during baseline | Invariant |
| --- | --- | --- |
| `V4_QS_STATIC_AUDIT_REPORT_VERSION` | parent | Schema version string remains stable |
| `V4_QS_RUNTIME_HANDOFF_REPORT_VERSION` | parent | Runtime handoff schema version remains stable |
| `V4QsStaticAuditReport` | parent | Report shape remains stable |
| `V4QsRuntimeHandoffReport` | parent | Runtime handoff shape remains stable |
| `V4QsStaticAuditVerdict` | parent | Verdict enum remains stable |
| `audit_v4_quant_script_static` | parent | Static audit entrypoint and rejection behavior remain stable |
| `build_v4_qs_runtime_handoff` | parent | Runtime handoff acceptance rules remain stable |

## Frozen Internal Queue

| Candidate | Scope | Initial status |
| --- | --- | --- |
| `report_surface` | public constants, report DTOs, verdict enum, internal parsed document DTOs | queued |
| `audit_entrypoint` | `audit_v4_quant_script_static` and capability-report integration | queued |
| `runtime_handoff_builder` | `build_v4_qs_runtime_handoff` acceptance and diagnostics | queued |
| `static_document_parser` | `parse_v4_static_document`, top-level keywords, request assembly | queued |
| `machine_block_parser` | machine/state/memory/transition/edge/risk-plane parsers | queued |
| `capability_type_parser` | runtime mode, execution capability, QS type, wrapper and arg parsers | queued |
| `event_catalog_derivation` | event catalog derivation and machine-family traversal | queued |
| `parser_utilities_diagnostics` | line preparation, word splitting, diagnostics helper | queued |
| `static_audit_test_harness` | local v4 static audit fixtures and tests | queued |

## Exclusions

- `test_plan_surface` remains outside this precision parent and currently scores `STOP`.
- Authoring docs and samples remain outside this precision parent.
- Runtime lowering is closed and must not be reopened.
- qrpc-core-ir v4 contract definitions remain external dependencies, not child owners of this module.

## Split Gate Evidence

`tools/evaluate-leaf-granularity.ps1 -Path quantscript/src/v4_static_audit.rs` result:

| Metric | Result |
| --- | --- |
| `split_decision` | `CONTINUE` |
| `governance_packaging` | `precision_single_leaf` |
| `final_decision` | `PRECISION` |
| `normalized_split_score` | 60 |
| `loc` | 1688 |
| `functions` | 42 |
| `public_surface` | 26 |
| `branches` | 106 |
| `risk_tags` | `compiler_contract`, `cross_crate`, `lock`, `persistence`, `public_api`, `route_schema`, `state_machine`, `trading_semantics` |

## Required Gates Before Movement

- `cargo fmt --all --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- governance gates for diff whitespace, UTF-8, full feature tree, and matrix governance

## Next Step

BE-002JR-01 `root.contracts.quantscript.v4_static_audit` parent_residual_judgment.
