# v4.16.0 root.contracts.runtime_support.v4_runtime_support.test_harness.simulated_execution_scenarios extract closeout

> Batch: BE-002FK-01
> Node: `root.contracts.runtime_support.v4_runtime_support.test_harness.simulated_execution_scenarios`
> Parent: `root.contracts.runtime_support.v4_runtime_support.test_harness`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> governance_next_authority: active

---

## Decision

Extract `simulated_execution_scenarios` as a test-only child.

The extraction moved nine simulated execution scenario tests into a dedicated child module while preserving all assertions, input literals, order request fields, event expectations, and production behavior.

## Code Movement

| Path | Change |
| --- | --- |
| `qrpc_runtime/src/v4_runtime/test_harness/simulated_execution_scenarios.rs` | New test-only child owning simulated execution scenario tests. |
| `qrpc_runtime/src/v4_runtime/test_harness.rs` | Adds `mod simulated_execution_scenarios;` and removes the moved test bodies. |

Moved tests:

- `v4_paper_simulated_runtime_runs_compat_bridge_graph_until_execution_ready`
- `v4_runtime_simulated_execution_config_controls_fill_fee_and_asset_curve`
- `v4_runtime_updates_simulated_portfolio_from_market_price`
- `v4_runtime_triggers_stop_market_order_on_market_price_update`
- `v4_runtime_oco_bracket_fills_one_leg_and_cancels_the_other`
- `v4_runtime_trailing_stop_updates_trigger_then_fills`
- `v4_runtime_gtd_order_expires_on_advance_time`
- `v4_runtime_amends_open_order_without_replacing_order_id`
- `v4_backtest_tick_replay_is_deterministic_and_reports_micro_metrics`

## Equivalence

The migration preserves:

- all 32 v4 runtime tests;
- paper simulated runtime progression assertions;
- fill, fee, cash, position, and asset curve assertions;
- stop, OCO, trailing stop, GTD, and amend order lifecycle assertions;
- deterministic tick replay and microstructure metric assertions;
- parent-mediated test access through `use super::*`;
- no production runtime code movement.

Observed namespace change:

- from `v4_runtime::test_harness::*`
- to `v4_runtime::test_harness::simulated_execution_scenarios::*` for the moved tests

## Evidence

Verified after extraction:

| Gate | Result |
| --- | --- |
| `cargo fmt --check` | PASS |
| `cargo check -p qrpc-runtime` | PASS |
| `cargo test -p qrpc-runtime v4 -- --nocapture` | PASS, 32 tests |
| `cargo check -p quantpilot` | PASS |

Governance gates are run at commit time for this batch:

- `git diff --check`;
- `tools/check-utf8.ps1`;
- `tools/check-full-feature-tree.ps1`;
- `tools/check-matrix-governance.ps1`.

## Next Step

BE-002FK-02 `root.contracts.runtime_support.v4_runtime_support.test_harness.simulated_execution_scenarios` single_leaf_closeout.
