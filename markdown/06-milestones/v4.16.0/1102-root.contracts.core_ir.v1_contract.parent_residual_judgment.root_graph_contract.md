# v4.16.0 root.contracts.core_ir.v1_contract parent residual judgment selects root_graph_contract

> Batch: BE-001TJ-01
> Node: `root.contracts.core_ir.v1_contract`
> Parent: `root.contracts.core_ir`
> Selected child: `root.contracts.core_ir.v1_contract.root_graph_contract`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v1_contract` continues its child queue after BE-001TI-03 determined `continue_split: true`.

Decision:

`next_child: root.contracts.core_ir.v1_contract.root_graph_contract`

## Open v1 Residuals

| Residual | Status |
| --- | --- |
| `contracts.core_ir.v1_contract.root_graph_contract` | Selected next. Owns v1 version constants, root graph DTOs, constructor, and DAG validation. |
| `contracts.core_ir.v1_contract.data_indicator_expression_contract` | Queued. |
| `contracts.core_ir.v1_contract.policy_execution_contract` | Queued. |
| `contracts.core_ir.v1_contract.test_fixture` | Queued. |

## Selection Rationale

`root_graph_contract` is selected first because it is the top of the v1 schema:

- `CORE_IR_V1_VERSION`;
- `CUSTOM_EXPR_V1_VERSION`;
- `CoreStrategyIr`;
- `CoreIREdge`;
- `CoreStrategyIr::new`;
- `CoreStrategyIr::validate_dag`;
- `CoreMetadata`;
- `CoreSourceKind`.

This child defines the root identity and graph validity boundary. Other v1 children hang below it as data/expression and policy/execution details.

## White Box Boundary

| Item | Owner | Input | Output | Side effects |
| --- | --- | --- | --- | --- |
| v1 version constants | `root_graph_contract` | none | schema version strings | none |
| root DTO | `root_graph_contract` | metadata, child contract vectors, execution, edges | `CoreStrategyIr` | none |
| constructor | `root_graph_contract` | metadata and execution | initialized Core IR v1 graph | none |
| DAG validation | `root_graph_contract` | edge list | cycle validation result | none |
| metadata/source DTOs | `root_graph_contract` | strategy identity/source data | metadata schema | none |

## Same-Parent Parallel Judgment

`same_parent_parallel_children` remains inactive.

Reason: root graph extraction defines the v1 parent's central type and must be isolated before expression/policy child extraction.

## Hard Boundaries

The next `root_graph_contract` baseline must not:

- edit Rust source code;
- move expression, signal, agent, risk, execution, or test fixture surfaces;
- change constants, root DTO fields, serde shapes, constructor behavior, DAG validation behavior, diagnostics, public exports, or tests;
- change v4 contracts, qrpc-core, compiler, runtime, QuantScript, backend, executor, frontend, physical `plugins/*`, or release transition.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | Selected child owns root v1 schema identity and graph validation. |
| parent_child_communication_kept | pass | Future child can be mediated through the v1 parent facade and crate root. |
| equivalence_baseline_freezable | pass | Constants, root fields, constructor, and DAG validation can be frozen. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | pass | Root Core IR names are public contract API. |
| state_machine_phase | guarded | DAG validation is pure graph validation, not runtime state mutation. |
| strategy_branch | pass | Root Core IR models strategy graph identity. |
| independent_failure_mode | pass | Root graph drift is separable from expression/policy/test drift. |
| reuse_pressure | pass | Downstream compiler/runtime paths depend on root names. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | Root graph schema and DAG validation are a coherent owner. |
| communication_cost_rises | no | v1 parent re-export can preserve consumers. |
| local_proof_missing | no | qrpc-core-ir tests cover the move. |
| line_count_only | no | Split is by root graph ownership. |

leaf_split_decision_result

`continue_baseline: true`; `root_graph_contract` is selected as the next v1 child.

## Next Step

BE-001TK-01 `root.contracts.core_ir.v1_contract.root_graph_contract` baseline_plan.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
