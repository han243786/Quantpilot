# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline.test_harness extract closeout

> Batch: BE-002DE-01
> Node: `root.contracts.runtime_support.sandbox_replay_timeline.test_harness`
> Parent: `root.contracts.runtime_support.sandbox_replay_timeline`
> Stage: `actual_extraction`
> Movement: Rust test code movement completed.

---

## Extraction Result

Local sandbox tests have been moved from `qrpc_runtime/src/sandbox/mod.rs` into:

- `qrpc_runtime/src/sandbox/test_harness.rs`

The sandbox parent now declares:

- `#[cfg(test)] mod test_harness;`

## Moved Surface

- realtime sandbox session/snapshot tests
- fast backtest pre-start rejection test
- market update portfolio event test
- ignored fast backtest replay output test
- ignored latency assumption test
- runtime support boundary test
- fast backtest deterministic test mode isolation test
- sample runtime protocol fixture

## Preserved Parent Surface

The following remain parent-owned:

- `snapshot_from`
- `ensure_running`
- `trace_id`
- concrete sandbox child re-exports

## Equivalence Notes

The test names and assertions are preserved. The only expected discovery change is module path renaming from `sandbox::tests::*` to `sandbox::test_harness::*`.

## Gates

- `cargo fmt` PASS
- `cargo check -p qrpc-runtime` PASS
- `cargo test -p qrpc-runtime sandbox -- --nocapture` PASS: 21 passed, 2 ignored

## Next Step

BE-002DE-02 `root.contracts.runtime_support.sandbox_replay_timeline.test_harness` single_leaf_closeout.
