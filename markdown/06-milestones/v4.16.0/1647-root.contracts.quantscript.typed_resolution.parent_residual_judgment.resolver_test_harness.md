# v4.16.0 root.contracts.quantscript.typed_resolution parent residual judgment: resolver_test_harness

> Batch: BE-002GO-01
> Node: `root.contracts.quantscript.typed_resolution`
> Selected child: `root.contracts.quantscript.typed_resolution.resolver_test_harness`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `root.contracts.quantscript.typed_resolution.resolver_test_harness` for the next recursive child.

## Current Parent State

Closed typed-resolution children:

- `root.contracts.quantscript.typed_resolution.public_type_surface`
- `root.contracts.quantscript.typed_resolution.callable_classification_surface`
- `root.contracts.quantscript.typed_resolution.semantic_inference_surface`
- `root.contracts.quantscript.typed_resolution.type_inference_binding_surface`
- `root.contracts.quantscript.typed_resolution.resolver_orchestration_surface`
- `root.contracts.quantscript.typed_resolution.resolver_support_surface`

Current parent file:

- `quantscript/src/resolve/mod.rs`

The parent now mainly owns the public resolver entrypoint, private resolver state/type scaffolding, `MemberCapabilityUse`, and resolver-local tests.

## Residual Candidate Review

| Candidate | Evidence | Decision |
|-----------|----------|----------|
| `resolver_test_harness` | The remaining large block is local resolver tests covering AST lowering, duplicate function diagnostics, unresolved identifiers, unknown callees, non-bool conditions, callable style consistency, member capability inference, standardized expression semantics, and universe helper binding types. | Select |
| parent closeout | Not yet selected because test code still dominates parent size and obscures the production facade. | Queue after test harness judgment |
| production micro-split | No clear production owner remains after orchestration and support helpers closed; splitting `Resolver` state or tiny facade functions would create governance cost without useful module independence. | Reject |

## Selected Child Scope

The next baseline may freeze only resolver-local tests and test-only imports/fixtures:

- `lowers_ast_into_typed_hir`
- `reports_duplicate_function_definitions`
- `reports_unresolved_identifiers`
- `reports_unknown_function_call_targets`
- `reports_non_bool_conditions`
- `accepts_first_and_last_as_known_call_targets`
- `infers_builtin_and_imported_helper_call_types`
- `keeps_member_and_call_style_helper_types_consistent`
- `infers_histogram_as_member_capability_type`
- `records_standardized_expr_semantics_for_series_capabilities`
- `resolves_universe_helpers_and_for_binding_types`

Likely child file:

- `quantscript/src/resolve/resolver_test_harness.rs`

## Forbidden Movement

The next baseline and extraction must not move:

- public resolver DTOs/enums already closed in `public_type_surface`;
- callable/helper classifiers and registry seeding already closed in `callable_classification_surface`;
- semantic/manual indicator inference already closed in `semantic_inference_surface`;
- type inference and binding helpers already closed in `type_inference_binding_surface`;
- resolver orchestration already closed in `resolver_orchestration_surface`;
- support helpers already closed in `resolver_support_surface`;
- production `Resolver` state/type scaffolding, `lower_script_to_typed_hir`, or `expr_semantic_key`;
- analysis, lowering, evaluator, runtime, backend, executor, frontend, plugin metadata, or release transition.

## Split Judgment

This is not a terminal split decision. The selected child should receive a baseline first because it is test-only code movement and must preserve the exact existing assertions.

## Next Step

BE-002GP-01 `root.contracts.quantscript.typed_resolution.resolver_test_harness` baseline_plan.
