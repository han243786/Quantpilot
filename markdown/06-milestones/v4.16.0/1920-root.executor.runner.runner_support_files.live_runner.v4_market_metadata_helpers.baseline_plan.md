# v4.16.0 root.executor.runner.runner_support_files.live_runner.v4_market_metadata_helpers baseline plan

> Batch: BE-002MN-01
> Node: `root.executor.runner.runner_support_files.live_runner.v4_market_metadata_helpers`
> Stage: `baseline_plan`
> governance_next_authority: active

---

## Frozen Boundary

The next movement may extract only:

- `executor_v4_market_matrix`
- `resolve_v4_runner_venue_id`
- `resolve_v4_runner_default_symbol`
- `graph_metadata_string`

Target file:

`src-executor/live_runner/v4_market_metadata_helpers.rs`

The parent `src-executor/live_runner.rs` must keep module wiring and may expose
these helpers to sibling modules with `pub(super)`.

## Explicit Non-Movement

Do not move in BE-002MN-02:

- local `#[cfg(test)]` test harness
- `RunnerPool`
- `RunnerInstance`
- `LiveRunner`
- `V4Runner`

## Compatibility Notes

- `V4Runner::from_strategy` must continue to resolve venue, symbol, and market
  capability matrix through this helper boundary.
- The local V4 test fixture must continue to build
  `V4StaticContractBundle::venue_matrices` with the same matrix helper.
- `V4Runner::DEFAULT_REALTIME_PAPER_VENUE_ID` remains the fallback venue source.

## Required Gates

| Gate | Required |
| --- | --- |
| `cargo fmt --all --check` | Yes |
| `cargo check -p quantpilot --bin executor` | Yes |
| `cargo test -p quantpilot --bin executor live_runner` | Yes |
| `git diff --check` | Yes |

## Next Step

BE-002MN-02
`root.executor.runner.runner_support_files.live_runner.v4_market_metadata_helpers`
actual_extraction.
