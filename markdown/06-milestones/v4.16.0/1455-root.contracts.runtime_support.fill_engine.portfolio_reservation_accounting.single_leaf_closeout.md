# v4.16.0 root.contracts.runtime_support.fill_engine.portfolio_reservation_accounting single leaf closeout

> Batch: BE-002BG-02
> Node: `root.contracts.runtime_support.fill_engine.portfolio_reservation_accounting`
> Parent: `root.contracts.runtime_support.fill_engine`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Decision

`root.contracts.runtime_support.fill_engine.portfolio_reservation_accounting` is closed with `stop_split: true`.

## Leaf Granularity Result

| Metric | Value |
| --- | --- |
| LOC | 103 |
| Functions | 6 |
| Public surface | 6 `pub(super)` helpers |
| Branches | 15 |
| Domains | 1 |
| Score | 35 |
| Decision | STOP |
| Governance mode | `stop_split` |

## Rationale

The child is a compact accounting-helper wave. Splitting cash availability, reservation math, sell availability, reservation sync, and release helpers into separate leaves would produce tiny tightly-coupled leaves with higher governance cost and no clearer parent boundary.

`apply_fill_to_portfolio` remains parent-owned and public, so this child is now a terminal private helper pocket.

## Preserved Boundaries

- public `apply_fill_to_portfolio` remains outside this child;
- closed `event_projection_wave` remains outside this child;
- fill-report helpers remain outside this child;
- tests remain outside this child;
- no sibling shortcuts exist.

## Next Step

BE-002BH-01 `root.contracts.runtime_support.fill_engine` parent_residual_judgment selects `fill_report_execution_helpers`.
