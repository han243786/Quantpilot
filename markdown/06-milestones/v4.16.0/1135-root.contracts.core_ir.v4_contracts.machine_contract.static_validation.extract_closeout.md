# v4.16.0 root.contracts.core_ir.v4_contracts.machine_contract.static_validation actual extraction complete

> Batch: BE-001UB-02
> Node: `root.contracts.core_ir.v4_contracts.machine_contract.static_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_contract`
> Stage: `extract_closeout`
> Movement: Code movement complete.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_contract.static_validation` is now extracted as a private machine-contract child module.

The child owns exactly:

- `impl V4MachineContract`
- `pub fn validate_static_contract(&self) -> Result<(), Vec<String>>`

The machine contract parent now declares the child with:

- `mod static_validation;`

No schema, taxonomy, default helper, graph validation, event catalog validation, type-system contract, bridge code, or tests moved.

## Boundary

**Real files**:

- `qrpc_core_ir/src/v4/machine_contract.rs`
- `qrpc_core_ir/src/v4/machine_contract/static_validation.rs`

**Markers**:

- `BE-001UB-02`

## White-Box Result

| Surface | Owner after extraction | Parent-mediated dependencies |
| --- | --- | --- |
| machine schema/taxonomy | `machine_contract` | default helpers and `QsTypeRef` through v4 parent |
| machine static validation | `static_validation` | `V4MachineContract`, machine policy enums, `V4_MACHINE_CONTRACT_VERSION` |

## Equivalence Evidence

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir` passed: 54 tests.
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`

## Next Step

BE-001UB-03 `root.contracts.core_ir.v4_contracts.machine_contract.static_validation` single_leaf_closeout.
