# v4.16.0 root.contracts.quantscript.runtime_lowering.intent_inference parent closeout

> Batch: BE-002JC-01
> Node: `root.contracts.quantscript.runtime_lowering.intent_inference`
> Parent: `root.contracts.quantscript.runtime_lowering`
> Stage: `parent_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Closeout Decision

Close `intent_inference` as a parent module for the current recursive pass.

The parent has already extracted and closed three high-value children:

| Child | Status |
| --- | --- |
| `intent_collection_orchestration` | closed, `stop_split: true` |
| `single_indicator_intent_inference` | closed, `stop_split: true` |
| `spread_intent_inference` | closed, `stop_split: true` |

After those extractions, `quantscript/src/lowering/intents.rs` is no longer an oversized precision leaf. The remaining functions are a compact facade and helper set around public(crate) intent inference entrypoints, MA ratio fallback, legacy emit fallback, data-source canonicalization, merge behavior, and comparison/id helpers. Continuing to split `moving_average_ratio_intent`, `legacy_emit_intent`, `agent_param_projection`, `data_source_canonicalization`, or `relation_and_id_helpers` now has weaker ownership benefit than governance and forwarding cost.

## Leaf Split Decision Result

Command:

- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -LeafId root.contracts.quantscript.runtime_lowering.intent_inference -ParentId root.contracts.quantscript.runtime_lowering -Path quantscript/src/lowering/intents.rs -Depth 4`

Result:

| Field | Value |
| --- | --- |
| `split_decision` | `STOP` |
| `governance_packaging` | `stop_split` |
| `final_decision` | `STOP` |
| `normalized_split_score` | 51 |
| `loc` | 613 |
| `function_count` | 24 |
| `public_surface` | 3 |
| `branches` | 53 |
| `risk_tags` | `cross_crate`, `public_api`, `security`, `trading_semantics` |
| `reason` | medium score without same-parent wave candidate; avoid standalone governance |

## Residual Owner Rationale

The residual parent is intentionally kept as the mediator for:

- `canonicalize_data_sources`
- `inferred_agent_params`
- `infer_intents`
- `intents_from_condition`
- `moving_average_ratio_intent`
- `legacy_intent_from_emit`
- `merge_intent`
- relation/id helpers
- structured comparison keys and comparison helpers

These residuals share parent-owned dispatch state and public facade stability. Leaving them together preserves readability and avoids a low-value module fan-out under the terminal leaf control rules.

## Validation

No Rust movement in this closeout step. The preceding `intent_inference` extraction sequence was validated by:

- `cargo fmt --check`
- `cargo test -p quantscript` (88 tests)
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8, full feature tree, and matrix governance checks

## Next Step

Return to `root.contracts.quantscript.runtime_lowering` parent residual judgment.
