# v4.16.0 root.contracts.qrpc_core.runtime_io_contract.portfolio_state single leaf closeout

> Batch: BE-001SJ-03
> Node: `root.contracts.qrpc_core.runtime_io_contract.portfolio_state`
> Parent: `root.contracts.qrpc_core.runtime_io_contract`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`portfolio_state` closes as one compact runtime IO child owner.

Final owner file:

- `qrpc_core/src/runtime_io_contract/portfolio_state.rs`

Owned public DTOs and methods:

- `Position`
- `ExchangeExposure`
- `PortfolioState`
- `PortfolioState::new`
- `PortfolioState::debug_assert_invariants`

## Closeout Evidence

| Check | Result | Evidence |
| --- | --- | --- |
| Parent facade preserved | pass | `qrpc_core/src/runtime_io_contract.rs` declares `mod portfolio_state;` and `pub use portfolio_state::*;`. |
| Crate public surface preserved | pass | `qrpc_core/src/lib.rs` continues re-exporting `runtime_io_contract::*`. |
| DTO equivalence | pass | Moved DTO fields, derives, and serde attributes are unchanged. |
| Helper equivalence | pass | `PortfolioState::new` defaults and `debug_assert_invariants` assertion conditions are preserved. |
| Parent-owned dependency | pass | `OpenOrder` is imported through the runtime IO parent facade. |
| No sibling horizontal link | pass | The child does not import `execution_io` directly. |

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | The child owns a named portfolio/account state DTO family. |
| parent_child_communication_kept | pass | Parent facade mediates both public exports and the `OpenOrder` dependency. |
| equivalence_baseline_freezable | pass | BE-001SJ-01 froze fields, serde attributes, helper defaults, and debug assertions; BE-001SJ-02 preserved them. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | no further split | Public schema and helper methods are already isolated as one compact child. |
| state_machine_phase | no | No runtime state-machine phase exists in this child. |
| strategy_branch | no | No strategy branch or handler branch exists in this child. |
| independent_failure_mode | no further split | `Position`, `ExchangeExposure`, and `PortfolioState` are coupled by the portfolio aggregate. |
| reuse_pressure | no further split | Runtime/backend reuse remains satisfied through the parent facade. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | pass | Splitting helper methods away from `PortfolioState` would create tiny ownerless leaves. |
| communication_cost_rises | pass | Splitting positions/exposures away from the aggregate would add parent mediation without a new behavior boundary. |
| local_proof_missing | no | qrpc-core gates prove the current compact child. |
| line_count_only | pass | There is no ownership reason beyond line count to split further. |

leaf_split_decision_result

`stop_split: true`

`portfolio_state` is a compact schema/helper leaf. Further split is not justified until real portfolio calculation, mutation, persistence, or independent validation behavior appears.

## Next Step

BE-001SK-01 `root.contracts.qrpc_core.runtime_io_contract` parent_residual_judgment selects the next runtime IO residual candidate, expected `runtime_output`.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
