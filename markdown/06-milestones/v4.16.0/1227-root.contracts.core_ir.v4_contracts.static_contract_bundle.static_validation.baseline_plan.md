# v4.16.0 root.contracts.core_ir.v4_contracts.static_contract_bundle.static_validation equivalence baseline and extraction plan

> Batch: BE-001VY-01
> Node: `root.contracts.core_ir.v4_contracts.static_contract_bundle.static_validation`
> Parent: `root.contracts.core_ir.v4_contracts.static_contract_bundle`
> Stage: `baseline_plan`
> Movement: No code movement.

---

## Summary

This baseline freezes the `static_validation` extraction boundary before code movement.

Frozen public surface:

- `V4StaticContractBundle::validate_static_contract`

Frozen validation behavior:

- schema version must equal `V4_STATIC_CONTRACT_BUNDLE_VERSION`
- delegated validation order remains version manifest, QS profile, type system, runtime modes, plugin governance, reproducibility, complexity budget, learning pipeline
- empty `machine_graphs` still reports `static contract bundle must include at least one machine graph`
- empty `venue_matrices` still reports `static contract bundle must include at least one venue matrix`
- each graph calls `validate_static_contract`
- each graph creates `ComplexityMetrics::from_machine_graph(graph, runtime_modes.modes.len(), plugin_manifests.len())`
- each graph validates the metrics through `complexity_budget.validate_metrics`
- each venue matrix calls `validate_v4_first_wave_contract`
- each plugin manifest calls `plugin_governance.validate_plugin_manifest(manifest, &type_system, &runtime_modes)`

Allowed BE-001VY-02 movement:

- add `qrpc_core_ir/src/v4/static_contract_bundle/static_validation.rs`
- add nested `mod static_validation;` under `static_contract_bundle`
- move only `V4StaticContractBundle::validate_static_contract`

Forbidden movement:

- `V4StaticContractBundle` DTO
- `impl Default for V4StaticContractBundle`
- `default_static_contract_bundle_version`
- compile-time capability request/report DTOs, report validation, report builders, and capability diagnostics
- compatibility bridge code, tests, and sample builders

Equivalence proof target:

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `cargo check -p quantpilot`

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/static_contract_bundle.rs`

**Markers**:
- `BE-001VY-01`

**Next step**:
BE-001VY-02 root.contracts.core_ir.v4_contracts.static_contract_bundle.static_validation extract_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
