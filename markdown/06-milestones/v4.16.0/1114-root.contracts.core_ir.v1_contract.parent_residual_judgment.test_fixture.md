# v4.16.0 root.contracts.core_ir.v1_contract parent residual judgment selects test_fixture

> Batch: BE-001TP-01
> Node: `root.contracts.core_ir.v1_contract`
> Parent: `root.contracts.core_ir`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v1_contract` returns to its child queue after `policy_execution_contract` closeout.

Selected next child:

`root.contracts.core_ir.v1_contract.test_fixture`

This is the last planned v1 child and owns the local round-trip regression test currently embedded in `qrpc_core_ir/src/v1.rs`.

## Residual Queue

| Child | Status |
| --- | --- |
| `contracts.core_ir.v1_contract.root_graph_contract` | Closed with `stop_split: true`. |
| `contracts.core_ir.v1_contract.data_indicator_expression_contract` | Closed with `stop_split: true`. |
| `contracts.core_ir.v1_contract.policy_execution_contract` | Closed with `stop_split: true`. |
| `contracts.core_ir.v1_contract.test_fixture` | Selected next. Owns local cfg-test fixture and v1 round-trip regression. |

## Selection Rationale

All production v1 schema children are now extracted and closed. The parent still holds a cfg-test module plus a test-only `BTreeMap` import. Moving this test fixture lets `v1.rs` become a pure facade over closed children.

## White-Box Boundary

| Surface | Owner | Input | Output | State |
| --- | --- | --- | --- | --- |
| v1 round-trip fixture | `test_fixture` | sample metadata, data, indicator, signal, agent, risk, execution DTOs | serialized/deserialized `CoreStrategyIr` equality assertion | none |
| test-only import | `test_fixture` | local test construction | `BTreeMap::new()` in fixture | none |

## Guardrails

The next `test_fixture` baseline must not:

- move production DTOs or helper code;
- change test assertions, sample values, serde behavior, or v1 facade exports;
- change v4 contracts, qrpc-core contracts, compiler/runtime/backend/executor/frontend behavior;
- introduce release-transition wiring.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `test_fixture` owns only local cfg-test fixture code. |
| parent_child_communication_kept | pass | The test child will use `super::*` through the v1 parent facade. |
| equivalence_baseline_freezable | pass | The next baseline can freeze the exact v1 round-trip test. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | This is not public API, but it is the local proof boundary. |
| state_machine_phase | no | Test-only fixture. |
| strategy_branch | guarded | Test exercises all v1 strategy graph layers. |
| independent_failure_mode | pass | Test drift can mask production facade regressions. |
| reuse_pressure | no | Local cfg-test only. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | A cfg-test fixture has a clear owner. |
| communication_cost_rises | no | Moving one cfg-test module reduces parent residual pressure. |
| local_proof_missing | no | The fixture itself is the proof. |
| line_count_only | no | Selection is based on parent closeout readiness. |

leaf_split_decision_result

`continue_baseline: true`; `test_fixture` is selected as the final v1 child.

## Next Step

BE-001TQ-01 `root.contracts.core_ir.v1_contract.test_fixture` baseline_plan.

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
