# v4.16.0 root.executor.runner.runner_support_files parent residual judgment selects ws_client

> Batch: BE-002MR-01
> Parent: `root.executor.runner.runner_support_files`
> Stage: `parent_residual_judgment`
> Selected child: `root.executor.runner.runner_support_files.ws_client`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `ws_client` as the next child under `runner_support_files`.

## Evidence

Closed children:

- `okx_rest`
- `live_runner`

Remaining cohesive groups:

- `ws_client`
- `kline_buffer`
- `migration_api`

`ws_client` is the next largest support-file owner. It owns websocket event
types, proxy-aware connection setup, OKX public stream message parsing, ticker
and kline projection, subscription orchestration, reconnect/backoff behavior,
and local tests.

## Boundary

The next baseline may freeze movement for `src-executor/ws_client.rs` only. It
must not move `kline_buffer`, `migration_api`, or live runner modules in the
same extraction step.

## Next Step

BE-002MS-01
`root.executor.runner.runner_support_files.ws_client`
baseline_plan.
