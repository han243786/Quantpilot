# v4.16.0 root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness parent closeout

> Batch: BE-002IJ-01
> Node: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness`
> Parent: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`integration_test_harness` is closed with `parent_closeout: true`.

The closed child set now covers:

- `basic_runtime_smoke_tests`
- `manual_formula_tests`
- `spread_lowering_tests`
- `rebalance_lowering_tests`

The residual parent is a compact 28-line test harness facade with one semantic rejection test and four child declarations. The remaining `rejects_semantic_errors_before_runtime_lowering` test is intentionally retained in the parent because splitting a single test into `semantic_rejection_tests` would create a micro-leaf with more governance overhead than ownership value.

## White-Box Boundary

| Field | Result |
| --- | --- |
| Parent input | Test-only parsed QuantScript module for semantic rejection smoke |
| Parent output | Assertions over `QS0005`, `missing_helper`, and `QS0006` diagnostics before runtime lowering |
| Child declarations | `basic_runtime_smoke_tests`, `manual_formula_tests`, `spread_lowering_tests`, `rebalance_lowering_tests` |
| Production code movement | None |
| Public API impact | None |

## Closeout Evidence

- `quantscript/src/lowering/orchestrator/integration_test_harness.rs` is 28 lines.
- The only parent-owned test is `rejects_semantic_errors_before_runtime_lowering`.
- All four extracted child modules already passed their single-leaf closeout.
- Latest extraction validation passed `cargo test -p quantscript` with 88 tests.

## Next Step

Return to `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface` parent residual judgment.
