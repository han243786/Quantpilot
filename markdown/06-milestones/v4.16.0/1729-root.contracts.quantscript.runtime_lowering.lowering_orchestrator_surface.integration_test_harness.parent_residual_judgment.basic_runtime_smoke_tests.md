# v4.16.0 root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness parent residual judgment: basic_runtime_smoke_tests

> Batch: BE-002IH-01
> Node: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness`
> Selected child: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.basic_runtime_smoke_tests`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `basic_runtime_smoke_tests` as the next child under `integration_test_harness`.

`manual_formula_tests`, `spread_lowering_tests`, and `rebalance_lowering_tests` are now closed. Among the remaining parent-owned tests, the basic runtime smoke group is the only cohesive multi-test child. The single semantic rejection test is intentionally left parent-owned for now to avoid creating a standalone micro-leaf.

## White-Box Boundary

| Field | Boundary |
| --- | --- |
| Input | Test-only MA/RSI/helper/exchange QuantScript fixtures and module constants |
| Output | Assertions over runtime config data sources, intent ids/kinds, input ids, RSI thresholds, runtime coordinator session output, and exchange decoding |
| Current owner | `quantscript/src/lowering/orchestrator/integration_test_harness.rs` |
| Planned owner | `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.basic_runtime_smoke_tests` |
| Parent caller | `integration_test_harness` test module only |
| Exclusions | `rejects_semantic_errors_before_runtime_lowering`, manual formula tests, spread lowering tests, rebalance lowering tests, and production lowering code |

## Candidate Tests

The selected child should freeze and move only these existing tests plus the script constants they use:

- `MA_SCRIPT`
- `RSI_SCRIPT`
- `lowers_ma_cross_into_runtime_config`
- `lowers_ma_cross_with_aliased_data_binding_into_consistent_input_ids`
- `lowers_rsi_thresholds_into_single_runtime_intent`
- `lowered_script_runs_in_runtime`
- `lowers_user_defined_helper_function_calls`
- `lowers_fetch_exchange_argument_into_data_source`

## Split Decision Gate

| Gate | Result | Evidence |
| --- | --- | --- |
| `micro_leaf_without_owner` | PASS | Six tests plus shared constants form one basic lowering/runtime smoke surface. |
| `communication_cost_rises` | PASS | Movement is test-only and remains under the parent test harness. |
| `local_proof_missing` | PASS | `cargo test -p quantscript` directly executes the planned moved tests. |
| `line_count_only` | PASS | Selection is based on smoke-test ownership, not raw size. |

## Risk Classification

| Risk | Result |
| --- | --- |
| Public API / route / schema | No change planned |
| Persistence / lock owner | No change planned |
| Trading semantics evidence | Medium, test-only; MA/RSI/helper/exchange lowering must stay equivalent |
| Cross-crate behavior | Present through runtime coordinator and compiler config assertions |

## Residual Queue

After this child, remaining candidate group is:

- `semantic_rejection_tests`

## Next Step

BE-002II-01 `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.basic_runtime_smoke_tests` baseline_plan.
