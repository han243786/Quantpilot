# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline.unified_timeline single leaf closeout

> Batch: BE-002CQ-02
> Node: `root.contracts.runtime_support.sandbox_replay_timeline.unified_timeline`
> Parent: `root.contracts.runtime_support.sandbox_replay_timeline`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Decision

`stop_split: true`

The unified timeline child is terminal for the current pass.

## Leaf Granularity Evidence

`tools/evaluate-leaf-granularity.ps1` result:

- decision: `STOP`
- score: `32`
- loc: `114`
- functions: `5`
- domains: `1`
- terminal leaf control bucket: `small_100_149`
- governance mode: `stop_split`

The leaf is a cohesive time-axis aggregation owner. Further splitting would separate construction, `from_providers`, and `collect_at` into artificially small fragments and increase parent wrapper churn.

## Boundary

The leaf owns `UnifiedTimeline` and its implementation only. Timeline data providers are already closed, while replay builder functions, sandbox control surface, sandbox implementations, and local tests remain queued outside this leaf.

## Gates

Evidence inherited from BE-002CQ-01:

- `cargo fmt --check`: pass
- `cargo check -p qrpc-runtime`: pass
- `cargo test -p qrpc-runtime sandbox -- --nocapture`: pass, 21 passed / 2 ignored
- `cargo check -p quantpilot`: pass
- `git diff --check`: pass
- UTF-8 / full feature tree / matrix governance: pass

## Next Step

BE-002CR-01 `root.contracts.runtime_support.sandbox_replay_timeline` parent_residual_judgment selects `replay_builder`.
