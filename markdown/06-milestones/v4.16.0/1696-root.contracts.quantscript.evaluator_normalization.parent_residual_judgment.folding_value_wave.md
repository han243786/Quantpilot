# v4.16.0 root.contracts.quantscript.evaluator_normalization parent residual judgment - folding_value_wave

> Batch: BE-002HO-01
> Node: `root.contracts.quantscript.evaluator_normalization`
> Selected child: `root.contracts.quantscript.evaluator_normalization.folding_value_wave`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `root.contracts.quantscript.evaluator_normalization.folding_value_wave` as the next child.

The evaluator parent is not a terminal leaf.

Evidence:

- `tools/evaluate-leaf-granularity.ps1 -Path quantscript/src/evaluator/mod.rs`
- result: `Decision: PRECISION`
- metrics: `loc=1127`, `functions=65`, `domains=8`
- reason: oversized high-risk leaf requires precision baseline before movement

## Selected Child Boundary

`folding_value_wave` owns expression folding and value projection helpers as one same-parent wave:

| Function family | Candidate functions |
| --- | --- |
| iterable/value projection | `expr_iterable_items`, `expr_length`, `expr_sum`, `expr_mean`, `expr_min`, `expr_max`, `expr_variance`, `expr_stddev`, `expr_first`, `expr_last`, `expr_numbers`, `expr_number`, `expr_integer`, `expr_bool`, `expr_string` |
| constant folding | `fold_unary`, `fold_binary`, `fold_index`, `fold_slice`, `list_index`, `list_slice`, `normalize_slice_bound` |
| builtin folding | `fold_builtin_call`, `fold_member_builtin`, `fold_identifier_builtin`, `is_fetch_expr` |

This is intentionally coarser than micro-leaf splitting. It avoids separate governance cycles for small helper pockets that are tightly coupled through expression value readers.

## Parent Residual After Planned Extraction

The parent should retain:

- public `normalize_script_module`;
- `EvalContext`;
- `normalize_function`, `normalize_block`, `normalize_stmt`, `normalize_expr`;
- helper inline execution and control-flow evaluation;
- local tests until production child extraction is stable.

## Excluded From This Child

Do not move in the next batch:

- `normalize_script_module`;
- AST walk orchestration;
- helper inline execution;
- `ExecOutcome`, `IfStmtParts`, `BlockSnapshot`;
- evaluator tests;
- parser, lowering, analysis diagnostics, typed resolution, static audit, test plan, legacy config, or release transition code.

## Next Step

BE-002HP-01 `root.contracts.quantscript.evaluator_normalization.folding_value_wave` baseline_plan.
