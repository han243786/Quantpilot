# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation equivalence baseline and extraction plan

> Batch: BE-001UM-01
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation`
> Stage: `baseline_plan`
> Movement: No code movement.

---

## Summary

This baseline freezes graph event usage validation before extraction.

The selected child may own only:

- `V4MachineGraphContract::validate_event_catalog`

It must not own event catalog DTOs, `MachineEventCatalog::validate_static_contract`,
root graph validation orchestration, DAG validation, risk-plane validation, graph schema,
traversal helpers, bridge code, or tests.

## Frozen Validation Surface

The next extraction must preserve:

- referenced event collection from every nested machine transition event;
- action emitted event collection and empty emitted event error formatting;
- edge event collection;
- `machine graph with transition or edge events must declare event_catalog`;
- local `MachineEventCatalog::validate_static_contract` delegation;
- catalog event lookup by `event_type`;
- undeclared referenced event error formatting;
- transition consumer allowed-party validation;
- transition source allowed-emitter validation;
- action emitted event allowed-emitter validation;
- edge source allowed-emitter validation;
- edge target allowed-consumer validation;
- skip behavior for missing event specs and unknown edge endpoints;
- branch order and `Vec<String>` error accumulation behavior.

## Allowed BE-001UM-02 Movement

The next extraction step may:

- create `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/event_usage_validation.rs`;
- add `mod event_usage_validation;` to `qrpc_core_ir/src/v4/machine_graph_contract/static_validation.rs`;
- move only `V4MachineGraphContract::validate_event_catalog`;
- keep `validate_static_contract` calling `self.validate_event_catalog(&all_machines_by_id)`;
- keep using parent-owned `collect_machine_family` and `machine_event_party_allowed`.

The child may depend on:

- `std::collections::{BTreeMap, BTreeSet}`;
- `collect_machine_family`;
- `machine_event_party_allowed`;
- `V4MachineContract`;
- `V4MachineGraphContract`.

## Forbidden BE-001UM-02 Movement

The next extraction step must not move or edit behavior for:

- `V4MachineGraphContract::validate_static_contract`;
- `V4MachineGraphContract::validate_graph_acyclic`;
- `V4MachineGraphContract::validate_risk_plane`;
- `machine_event_party_allowed`;
- event catalog DTOs;
- `MachineEventCatalog::validate_static_contract`;
- graph schema DTOs;
- `collect_machine_family`;
- `machine_nested_depth`;
- complexity metric code;
- bridge builders;
- tests.

It must not alter validation branch order, error strings, event party semantics,
event lookup behavior, or unknown-edge endpoint skip behavior.

## Equivalence Proof Plan

BE-001UM-02 must pass:

- `cargo fmt --check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation.rs`

**Planned child file**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/event_usage_validation.rs`

**Markers**:
- `BE-001UM-01`

**Next step**:
BE-001UM-02 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation extract_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
