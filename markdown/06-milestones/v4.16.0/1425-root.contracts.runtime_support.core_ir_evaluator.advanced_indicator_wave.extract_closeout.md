# v4.16.0 root.contracts.runtime_support.core_ir_evaluator.advanced_indicator_wave extract closeout

> Batch: BE-002AM-02
> Node: `root.contracts.runtime_support.core_ir_evaluator.advanced_indicator_wave`
> Parent: `root.contracts.runtime_support.core_ir_evaluator`
> Stage: `actual_extraction`
> Movement: Rust code movement.

---

## Result

`root.contracts.runtime_support.core_ir_evaluator.advanced_indicator_wave` has been physically extracted.

New owner file:

- `qrpc_runtime/src/core_ir_evaluator/advanced_indicator_wave.rs`

Parent file:

- `qrpc_runtime/src/core_ir_evaluator.rs`

## Moved Surface

Moved into the child:

- `evaluate_atr`
- `evaluate_bollinger_bands`
- `evaluate_obv`
- `evaluate_cmf`
- `evaluate_adx`
- `evaluate_stochastic`
- `evaluate_cci`
- `evaluate_parabolic_sar`
- `evaluate_keltner_channel`
- `evaluate_donchian_channel`

Parent changes:

- added `mod advanced_indicator_wave;`
- imported ten parent-visible evaluator entrypoints from the child;
- preserved builtin registry calls and public `evaluate_indicator_signal` dispatch.

## Preserved Parent Ownership

The parent still owns:

- public evaluator DTOs and error enum;
- `IndicatorEvaluator`, `indicator_registry`, and `evaluate_indicator_signal`;
- closed `classic_indicator_wave`;
- Spread and Custom evaluator entrypoints;
- typed series expression machinery;
- shared lookup/math helpers and formula helpers;
- local tests.

The child calls parent-owned private helpers through `super::*`; no public API was widened.

## Equivalence Evidence

Passed:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime core_ir_evaluator`: 22 passed
- `cargo check -p quantpilot`

Remaining for commit gate:

- `git diff --check`
- `tools/check-utf8.ps1`
- `tools/check-full-feature-tree.ps1`
- `tools/check-matrix-governance.ps1`

## Next Step

BE-002AM-03 `root.contracts.runtime_support.core_ir_evaluator.advanced_indicator_wave` single_leaf_closeout.
