# v4.16.0 root.contracts.core_ir.v4_contracts.developer_learning_pipeline_contract equivalence baseline and extraction plan

> Batch: BE-001VU-01
> Node: `root.contracts.core_ir.v4_contracts.developer_learning_pipeline_contract`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `baseline_plan`
> Movement: No code movement.

---

## Summary

This baseline freezes the `developer_learning_pipeline_contract` extraction boundary before any Rust code movement.

Frozen public surface:

- `DeveloperLearningPipelineContract`
- `impl Default for DeveloperLearningPipelineContract`
- `DeveloperLearningPipelineContract::validate_static_contract`
- `default_learning_pipeline_contract_version`
- `default_learning_dir`

Frozen default behavior:

| Field/helper | Expected behavior |
| --- | --- |
| `schema_version` | Defaults to `V4_LEARNING_PIPELINE_CONTRACT_VERSION`. |
| `core_pipeline_in_repo` | Defaults to `true`; validation rejects `false` with `core learning pipeline must live in the repository`. |
| `local_learning_dir` | Defaults to `markdown/learning/`; validation rejects any other value with `local_learning_dir must be ` plus the default path. |
| `local_learning_dir_gitignored` | Defaults to `true`; validation rejects `false` with `local learning records must stay gitignored`. |
| `write_requires_explicit_user_command` | Defaults to `true`; validation rejects `false` with `learning records must require explicit user command`. |
| `included_in_regular_gates` | Defaults to `false`; validation rejects `true` with `learning pipeline must not enter regular mandatory gates`. |
| `major_closeout_question_required` | Defaults to `true`; validation rejects `false` with `MAJOR closeout must ask the learning pipeline question`. |
| `owner_first_iteration_only` | Defaults to `true`; validation rejects `false` with `first learning pipeline iteration must stay owner-first`. |

Allowed BE-001VU-02 movement:

- add `qrpc_core_ir/src/v4/developer_learning_pipeline_contract.rs`
- add `mod developer_learning_pipeline_contract;`
- add `pub use developer_learning_pipeline_contract::*;`
- move the DTO, default impl, static validator, and two helper defaults into the new child
- import only `default_true` and `V4_LEARNING_PIPELINE_CONTRACT_VERSION` from the parent facade

Forbidden movement:

- static bundles and `V4StaticContractBundle`
- compile-time capability request/report DTOs
- `CoreIrV4CompatibilityReport`, diagnostics, bridge builders, and bridge validators
- complexity budget, reproducibility, plugin governance, machine/graph, QS, runtime, venue, and version manifest families
- tests and sample builders

Equivalence proof target:

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `cargo check -p quantpilot`

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs`

**Markers**:
- `BE-001VU-01`

**Next step**:
BE-001VU-02 root.contracts.core_ir.v4_contracts.developer_learning_pipeline_contract extract_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
