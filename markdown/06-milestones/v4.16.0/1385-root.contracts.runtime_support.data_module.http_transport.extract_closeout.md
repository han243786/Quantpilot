# v4.16.0 root.contracts.runtime_support.data_module.http_transport extract closeout

> Batch: BE-001ZO-02
> Node: `root.contracts.runtime_support.data_module.http_transport`
> Parent: `root.contracts.runtime_support.data_module`
> Stage: `actual_extraction`
> Movement: Rust code movement.

---

## Extraction Summary

Created:

- `qrpc_runtime/src/data_module/http_transport.rs`

Updated:

- `qrpc_runtime/src/data_module.rs`
- `qrpc_runtime/src/data_module/historical_cache.rs`

Moved implementation surface:

- `PingProbe`
- ping probing
- OKX live kline/quote fetch
- JSON request retry/fallback
- Tokio blocking bridge
- fallback runtime singleton
- Windows PowerShell HTTP fallback

## Parent Facade

`qrpc_runtime/src/data_module.rs` now owns:

- `mod http_transport;`
- thin `BuiltinDataModule::probe_ping` delegation to the child;
- thin `BuiltinDataModule::fetch_okx` delegation to the child;
- parent-mediated re-exports for `block_on_http` and Windows `fetch_json_via_powershell`;
- parent-owned client/cache/breaker orchestration.

## Boundary Preservation

| Boundary | Result |
| --- | --- |
| Client ownership | `BuiltinDataModule.client` stays parent-owned and is passed to the transport child by reference. |
| Ping probe | Disabled path, endpoint selection, elapsed latency, and error capture moved with `PingProbe`. |
| OKX live fetch | Kline/Quote endpoint selection, parser dispatch, normalization helper usage, elapsed latency, and diagnostics are owned by the child. |
| JSON fetch | Reqwest request, 429 retry loop, status categorization, JSON parse, and Windows fallback remain in one transport owner. |
| Runtime bridge | `block_on_http` and fallback runtime moved together and are re-exported through the parent. |
| Historical cache mediation | `historical_cache` still imports transport helpers through `data_module`; no direct sibling dependency was introduced. |

## Exclusions Preserved

No movement was made to:

- request-interval cache;
- `BuiltinDataModule.cache`;
- `BuiltinDataModule.breaker`;
- circuit-breaker decision logic;
- mock generation or mock fallback construction;
- historical cache persistence/fetch orchestration;
- exchange parser ownership;
- normalization implementation;
- quality diagnostics;
- data-module tests.

## Equivalence Evidence

This extraction passed:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`

## Next Step

BE-001ZO-03 `root.contracts.runtime_support.data_module.http_transport` single_leaf_closeout. The closeout must run terminal leaf control against `qrpc_runtime/src/data_module/http_transport.rs` before deciding whether to split further.
