# v4.16.0 root.contracts.runtime_support.data_module.mock_data_generation baseline plan

> Batch: BE-001ZK-01
> Node: `root.contracts.runtime_support.data_module.mock_data_generation`
> Parent: `root.contracts.runtime_support.data_module`
> Stage: `baseline_plan`
> Movement: No Rust code movement.

---

## Scope

This baseline freezes deterministic mock data generation before extraction.

Current owner file:

- `qrpc_runtime/src/data_module.rs`

Planned child module:

- data-module child module named `mock_data_generation`

## Allowed Movement

The next extraction may move only:

- `MOCK_VOLATILITY`
- `DEFAULT_MOCK_VOLATILITY`
- `get_mock_volatility`
- `mock_raw_klines`
- `pseudo_random`
- `mock_raw_quote`
- `mock_kline_bars_for_backtest`

The parent may add:

- `mod mock_data_generation;`
- imports or re-exports needed to preserve the existing parent-visible and crate-visible surface.

## Boundaries To Preserve

| Boundary | Requirement |
| --- | --- |
| Public volatility surface | `MOCK_VOLATILITY` must remain reachable for existing callers that configure test/backtest volatility. |
| Crate-visible backtest helper | `mock_kline_bars_for_backtest` must remain crate-visible through the parent path or equivalent parent-owned re-export. |
| Provider fallback | `BuiltinDataModule::mock_data` must continue to call mock quote/kline generation with the same output semantics. |
| Normalization path | Mock kline bars still normalize through parent-owned normalization import; no sibling horizontal link is introduced. |
| Determinism | `pseudo_random` seed behavior and multi-regime price generation must be preserved. |
| Safety clamp | NaN/Inf rejection and volatility clamp must be preserved. |

## Exclusions

The next extraction must not move:

- historical cache path/load/persist/fetch helpers;
- HTTP fetch and PowerShell fallback;
- exchange endpoint or payload parsing helpers;
- provider orchestration;
- quality diagnostics;
- `data_module` tests.

## Leaf Granularity Note

The projected child is small and cohesive. After extraction, `terminal_leaf_control_v2` must run against the created mock data generation child file.

Expected governance mode: likely `stop_split`, unless the extracted file unexpectedly exceeds the target terminal range or reveals stronger independent owner evidence.

## Equivalence Gates

Required for the next extraction:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-001ZK-02 `root.contracts.runtime_support.data_module.mock_data_generation` actual_extraction.
