# v4.16.0 root.contracts.quantscript.v4_static_audit parent residual judgment selects state_group_parser

> Batch: BE-002KP-01
> Node: `root.contracts.quantscript.v4_static_audit`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select next child:

`root.contracts.quantscript.v4_static_audit.state_group_parser`

## Parent Residual Scan

Closed children:

- `runtime_handoff_builder`;
- `audit_entrypoint`;
- `capability_type_parser`;
- `event_catalog_derivation`;
- `parser_utilities_diagnostics`;
- `static_document_parser`;
- `machine_block_parser`;
- `state_block_parser`;
- `transition_parser`;
- `memory_parser`;
- `edge_parser`;
- `risk_plane_parser`.

Remaining parent-owned clusters:

- state-group parsing;
- machine template wrapper and thin capability/type wrappers;
- public DTOs and local static-audit tests.

## Why This Child

`state_group_parser` owns the flat state-group declaration grammar,
state-member expansion, conflict-policy defaulting, and `StateGroup` assembly.
It has distinct diagnostics and can move without changing machine-block,
state-block, memory, transition, edge, risk-plane, DTO, or test ownership.

## Proposed Ownership

| Field | Value |
| --- | --- |
| Candidate child | `state_group_parser` |
| Planned file | `quantscript/src/v4_static_audit/state_group_parser.rs` |
| Likely owned functions | `parse_state_group` |
| Parent wrapper | `parse_state_group` |
| Parent bridge dependencies | `diag`, `split_words`, `TransitionConflictPolicy` |

## Exclusions For Next Baseline

- Do not move `parse_machine_template`.
- Do not move public report DTOs or local tests.
- Do not alter already closed child modules.
- Do not introduce release-transition links.

## Next Step

BE-002KQ-01 `root.contracts.quantscript.v4_static_audit.state_group_parser`
baseline_plan.
