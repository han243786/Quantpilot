# v4.16.0 root.contracts.core_ir.v4_contracts.schema_identity_constants extract closeout

> Batch: BE-001TV-02
> Node: `root.contracts.core_ir.v4_contracts.schema_identity_constants`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `extract_closeout`
> Movement: Rust code moved under the same v4 parent facade.

---

## Summary

`schema_identity_constants` now owns the v4 schema/version constants, compat machine/event identifiers, and guard constants that were previously embedded near the top of `qrpc_core_ir/src/v4.rs`.

Created:

- `qrpc_core_ir/src/v4/schema_identity_constants.rs`

Updated:

- `qrpc_core_ir/src/v4.rs`

## Moved Surface

| Surface | Result |
| --- | --- |
| v4 schema version constants | Moved into `schema_identity_constants.rs` unchanged. |
| compat machine IDs | Moved into `schema_identity_constants.rs` unchanged. |
| compat event IDs | Moved into `schema_identity_constants.rs` unchanged. |
| runtime event rejected ID | Moved into `schema_identity_constants.rs` unchanged. |
| `V4_RISK_PLANE_MIN_PRIORITY` | Moved into `schema_identity_constants.rs` unchanged. |
| `V4_QS_TYPE_MAX_NESTING_DEPTH` | Moved into `schema_identity_constants.rs` unchanged. |
| v4 parent facade | Added `mod schema_identity_constants;` and `pub use schema_identity_constants::*;`. |

## Preserved Non-Owners

These public arrays intentionally remain in `qrpc_core_ir/src/v4.rs`:

- `V4_RUNTIME_EXECUTION_EVENTS`;
- `V4_FIRST_WAVE_QS_SCALAR_TYPES`;
- `V4_FIRST_WAVE_QS_COMPOSITE_TYPES`;
- `V4_FIRST_WAVE_EXECUTION_CAPABILITIES`.

## Equivalence Proof

| Check | Result | Evidence |
| --- | --- | --- |
| Public names preserved | pass | `qrpc_core_ir/src/v4.rs` re-exports `schema_identity_constants::*`. |
| Constant values preserved | pass | Constant definitions were mechanically moved unchanged. |
| Parent-mediated access preserved | pass | Existing v4 references continue resolving through the v4 parent namespace. |
| No behavior moved | pass | No DTO, enum, impl, validator, helper, bridge, public array, or test was moved. |
| Release transition avoided | pass | No production wiring shortcut or release-transition proposal was introduced. |

## Hard Boundary Result

No constant value, public name, type, serde shape, validator behavior, helper behavior, bridge output, test assertion, v1 contract, qrpc-core behavior, compiler behavior, runtime behavior, backend behavior, executor behavior, frontend behavior, physical `plugins/*`, or release-transition pathway was intentionally changed.

## Next Step

BE-001TV-03 `root.contracts.core_ir.v4_contracts.schema_identity_constants` single_leaf_closeout.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
