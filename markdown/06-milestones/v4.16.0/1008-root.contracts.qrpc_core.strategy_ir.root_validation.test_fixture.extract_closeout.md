# v4.16.0 root.contracts.qrpc_core.strategy_ir.root_validation.test_fixture extract closeout

> Batch: BE-001RK-02
> Node: `root.contracts.qrpc_core.strategy_ir.root_validation.test_fixture`
> Parent: `root.contracts.qrpc_core.strategy_ir.root_validation`
> Stage: `extract_closeout`
> Movement: Rust test code extraction.

---

## Summary

`root.contracts.qrpc_core.strategy_ir.root_validation.test_fixture` has been extracted from `root_validation.rs` into a cfg-test child file.

The root validation parent preserves production behavior and now keeps only a test module declaration:

- `#[cfg(test)] mod tests;`

## Files Changed

- `qrpc_core/src/strategy_ir/root_validation.rs`
- `qrpc_core/src/strategy_ir/root_validation/tests.rs`

## Extraction Result

`qrpc_core/src/strategy_ir/root_validation.rs` now owns:

- root `StrategyIr` DTO shape;
- public validation methods;
- validation child call order;
- parent helper wrappers;
- the cfg-test module declaration.

`qrpc_core/src/strategy_ir/root_validation/tests.rs` now owns:

- `SAMPLE_JSON`;
- `parses_and_validates_strategy_ir_v0`;
- `rejects_invalid_unknown_marker`;
- `rejects_duplicate_signal_ids`;
- `rejects_non_finite_execution_profile_costs`;
- `accepts_custom_indicator_when_runtime_support_is_declared`.

## Rust Local Fields

| Field | Result |
| --- | --- |
| Crate | `qrpc-core` |
| Parent facade | Preserved. `root_validation.rs` remains the production validation coordinator. |
| Test visibility | Preserved as `#[cfg(test)]`; no runtime public API was added. |
| Public exports | No `pub use` changes. |
| Production child modules | Closed validation children remain untouched. |
| Test path identity | Root validation tests still run under `strategy_ir::root_validation::tests::*`. |

## Equivalence Preservation

| Surface | Preserved evidence |
| --- | --- |
| Test fixture JSON | `SAMPLE_JSON` moved into `tests.rs` without behavior change. |
| Success validation test | Valid sample still parses and validates. |
| Unknown marker regression | Invalid marker test still checks `execution.order_type`. |
| Duplicate id regression | Duplicate signal id test still checks `重复的 id`. |
| Execution profile numeric regression | `fee_bps` and `slippage_bps` regressions still pass. |
| Custom indicator regression | Custom indicator support test still passes. |
| Production behavior | `StrategyIr::validation_errors`, `StrategyIr::validate`, validation ordering, diagnostics, and serde shape remain unchanged. |

## Hard Boundaries Held

BE-001RK-02 did not:

- change test assertions, fixture JSON semantics, validation ordering, diagnostics, public methods, root DTO fields, or serde shape;
- move production validation helpers or closed child validation modules;
- make root validation child modules import each other directly;
- change closed Strategy IR child DTOs/enums/constants/public functions, protocol primitives, runtime IO, compiler/runtime/backend/executor/frontend behavior, physical `plugins/*`, or release transition.

## Gates

- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-pre-commit-hook.ps1`

## Next Step

BE-001RK-03 `root.contracts.qrpc_core.strategy_ir.root_validation.test_fixture` single_leaf_closeout.
