# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline.realtime_sandbox baseline plan

> Batch: BE-002CX-01
> Node: `root.contracts.runtime_support.sandbox_replay_timeline.realtime_sandbox`
> Parent: `root.contracts.runtime_support.sandbox_replay_timeline`
> Stage: `baseline_plan`
> Movement: No Rust code movement.

---

## Scope

Freeze the baseline for extracting `RealTimeSandbox` from `qrpc_runtime/src/sandbox/mod.rs`.

Allowed movement:

- add `qrpc_runtime/src/sandbox/realtime_sandbox.rs`;
- move only `RealTimeSandbox`, its constructor/accessor impl, its `Sandbox` trait impl, and its realtime risk-monitor helper impl;
- keep `FastBacktestSandbox` in `mod.rs`;
- keep shared parent helpers in `mod.rs`.

## In-Scope Surface

- `RealTimeSandbox`
- `RealTimeSandbox::new`
- `RealTimeSandbox::with_test_mode`
- `RealTimeSandbox::from_core_ir`
- `RealTimeSandbox::from_compiled`
- `RealTimeSandbox::test_mode`
- `impl Sandbox for RealTimeSandbox`
- `RealTimeSandbox::set_risk_monitor`
- `RealTimeSandbox::is_risk_stopped`

## Out of Scope

- `FastBacktestSandbox`
- `compute_benchmark_equity`
- `snapshot_from`
- `ensure_running`
- `trace_id`
- sandbox tests
- replay/timeline/mode-surface children

## Equivalence Baseline

The next actual extraction must preserve:

- realtime start/stop/running behavior;
- realtime `SandboxMode`;
- run session / execution plan / market data forwarding through `RuntimeCoordinator`;
- handoff completeness validation and portfolio restore behavior;
- snapshot/restore behavior;
- risk monitor helper methods;
- public crate-root access to `RealTimeSandbox`.

## Dependency Rule

The realtime child may use parent-owned shared helpers through the sandbox parent. It must not import from `FastBacktestSandbox` or other sibling implementation children.

## Gates

Required gates for actual extraction:

- `cargo fmt`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime sandbox -- --nocapture`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 / full feature tree / matrix governance

## Next Step

BE-002CY-01 `root.contracts.runtime_support.sandbox_replay_timeline.realtime_sandbox` actual_extraction.
