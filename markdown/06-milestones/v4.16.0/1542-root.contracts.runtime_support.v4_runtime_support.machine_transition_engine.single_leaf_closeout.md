# v4.16.0 root.contracts.runtime_support.v4_runtime_support.machine_transition_engine single leaf closeout

> Batch: BE-002DW-02
> Node: `root.contracts.runtime_support.v4_runtime_support.machine_transition_engine`
> Parent: `root.contracts.runtime_support.v4_runtime_support`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Decision

`stop_split: true`

The v4 machine transition engine child is terminal for the current recursive pass.

## Terminal Leaf Control

`tools/evaluate-leaf-granularity.ps1 -Path qrpc_runtime/src/v4_runtime/machine_transition_engine.rs` result:

- Decision: `STOP`
- Score: `40`
- LOC: `303`
- Functions: `5`
- Public surface: `1`
- Branches: `34`
- Domains: `4`
- Risk tags: `persistence`, `public_api`, `state_machine`, `trading_semantics`
- Governance mode: `stop_split`

## Evidence

The extracted child owns one coherent state-machine transition responsibility:

- validating transition guards and memory-write preflight;
- selecting parent/child transition candidates;
- sorting candidates by priority and stable id;
- applying state and memory updates;
- updating soft-silent/recovering status;
- recording transition-applied events;
- constructing emitted-event payloads;
- matching transitions against source and freshness.

Further splitting candidate selection, preflight, payload construction, and state update would add governance overhead without a stronger owner. Risk/execution gate and simulated execution internals remain intentionally outside this child and will be handled by their own residual judgments.

## Verified Gates

Inherited from BE-002DW-01:

- `cargo fmt --check`: PASS
- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime v4 -- --nocapture`: PASS, 32 passed
- `cargo check -p quantpilot`: PASS
- `git diff --check`: PASS
- UTF-8 check: PASS
- full feature tree check: PASS
- matrix governance check: PASS

## Next Step

BE-002DX-01 `root.contracts.runtime_support.v4_runtime_support` parent_residual_judgment.
