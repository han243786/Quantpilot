# v4.16.0 root.contracts.core_ir.v4_contracts parent residual judgment selects complexity_budget_contract

> Batch: BE-001VR-01
> Node: `root.contracts.core_ir.v4_contracts`
> Parent: `root.contracts.core_ir`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts` selects
`root.contracts.core_ir.v4_contracts.complexity_budget_contract` as the next child.

The selected child owns the v4 complexity budget and metrics contract currently in `qrpc_core_ir/src/v4.rs`:

- `ComplexityBudgetContract`
- `ComplexityMetrics`
- `impl Default for ComplexityBudgetContract`
- `ComplexityBudgetContract::validate_static_contract`
- `ComplexityBudgetContract::validate_metrics`
- `impl ComplexityMetrics`
- `default_complexity_budget_contract_version`
- `default_max_nested_machine_depth`
- `default_max_event_processing_path_count`

The selected child does not own compile-time capability report DTOs, compatibility bridge reports, static bundles,
developer learning pipeline, reproducibility, plugin governance, tests, machine contract schema, machine graph schema,
QS contracts, runtime mode, venue capability matrix, or version manifest.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `complexity_budget_contract` is a named white-box child for complexity budgets and metrics calculation. |
| parent_child_communication_kept | pass | The child can use machine graph traversal helpers through existing v4 sibling mediation and remain re-exported by the parent. |
| equivalence_baseline_freezable | pass | Budget defaults, metric fields, graph metric computation, and validation branches are finite. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | Complexity budget and metric DTOs remain public through the v4 facade. |
| state_machine_phase | guarded | Metrics inspect machine graph topology but do not own graph schema or graph validation. |
| strategy_branch | guarded | Complexity budgets guard strategy graph expansion and compile-time capability checks. |
| independent_failure_mode | pass | Zero budget and over-budget metric failures are independent from report/verdict and plugin/venue validation. |
| reuse_pressure | pass | Static bundles, compile-time reports, compatibility bridge, and tests reuse complexity metrics and budget validation. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The child owns public DTOs, defaults, validation, and metrics calculation as one contract family. |
| communication_cost_rises | no | A single child keeps budget and computed metrics together while using graph helpers as inputs. |
| local_proof_missing | no | Existing qrpc-core-ir tests cover over-budget graph rejection through the public v4 surface. |
| line_count_only | no | Selection is based on complexity budget ownership and reuse pressure. |

leaf_split_decision_result

`continue_split: true`

Selected next child:

`root.contracts.core_ir.v4_contracts.complexity_budget_contract`

next_recursive_step

BE-001VS-01 root.contracts.core_ir.v4_contracts.complexity_budget_contract baseline_plan
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs`

**Markers**:
- `BE-001VR-01`

**Next step**:
BE-001VS-01 root.contracts.core_ir.v4_contracts.complexity_budget_contract baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
