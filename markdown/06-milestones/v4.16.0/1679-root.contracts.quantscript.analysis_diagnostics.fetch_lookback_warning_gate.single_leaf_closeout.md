# v4.16.0 root.contracts.quantscript.analysis_diagnostics.fetch_lookback_warning_gate single leaf closeout

> Batch: BE-002HE-03
> Node: `root.contracts.quantscript.analysis_diagnostics.fetch_lookback_warning_gate`
> Parent: `root.contracts.quantscript.analysis_diagnostics`
> Stage: `single_leaf_closeout`
> stop_split: true
> governance_next_authority: active

---

## Decision

`root.contracts.quantscript.analysis_diagnostics.fetch_lookback_warning_gate` is closed as a terminal leaf.

Terminal leaf control returned:

- `split_decision: STOP`
- `governance_packaging: stop_split`
- `normalized_split_score: 38`
- `size_bucket: small_100_149`

## Evidence

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -Path quantscript\src\analysis_diagnostics\fetch_lookback_warning_gate.rs -LeafId root.contracts.quantscript.analysis_diagnostics.fetch_lookback_warning_gate -ParentId root.contracts.quantscript.analysis_diagnostics -ParentLoc 416 -Depth 4
```

Metrics:

- `loc=127`
- `functions=3`
- `public_surface=1`
- `branches=24`
- `domains=2`
- `risk_tags=cross_crate,public_api,state_machine`

Scores:

- `split_benefit=46`
- `leaf_size_fit=15`
- `risk_penalty=65`
- `governance_cost=61`
- `system_efficiency_penalty=15`

## Boundary After Closeout

The leaf owns:

- recursive `fetch()` / `get_data()` lookback argument traversal;
- `lookback < 1` warning detection;
- `QS0503` diagnostic emission.

The parent still coordinates:

- diagnostic invocation order;
- strategy fetch/emit presence checks;
- data source map and index bounds checks;
- dead-code emit checks.

## Residual Rule

Do not split this leaf again unless a future developer proposal proves a stronger independent owner than the current fetch lookback warning gate.

## Next Step

Return to `root.contracts.quantscript.analysis_diagnostics` parent residual judgment.
