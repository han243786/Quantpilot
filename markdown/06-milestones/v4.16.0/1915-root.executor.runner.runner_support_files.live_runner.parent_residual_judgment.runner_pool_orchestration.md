# v4.16.0 root.executor.runner.runner_support_files.live_runner parent residual judgment selects runner_pool_orchestration

> Batch: BE-002MK-01
> Parent: `root.executor.runner.runner_support_files.live_runner`
> Stage: `parent_residual_judgment`
> Selected child: `root.executor.runner.runner_support_files.live_runner.runner_pool_orchestration`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `runner_pool_orchestration` as the next child under `live_runner`.

## Evidence

Closed children:

- `v3_live_runner`
- `runner_instance_dispatch`
- `v4_runner`

Remaining cohesive groups:

- `RunnerPool` orchestration;
- V4 market metadata helpers;
- local test harness.

`RunnerPool` is now the next cohesive owner. It owns runner registration,
exchange sender registration, strategy removal, symbol reverse indexing, and
websocket event fan-out across active runner instances.

## Boundary

The next baseline may freeze movement for `RunnerPool` and its direct impl only.
It must not move V4 market metadata helper functions or the local test harness
in the same extraction step.

## Next Step

BE-002ML-01
`root.executor.runner.runner_support_files.live_runner.runner_pool_orchestration`
baseline_plan.
