# v4.16.0 root.executor.runner.runner_support_files.okx_rest.signing_request_surface baseline plan

> Batch: BE-002LT-01
> Node: `root.executor.runner.runner_support_files.okx_rest.signing_request_surface`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Goal

Freeze the OKX REST signing and signed request construction boundary before
Rust movement.

The first movement may extract signing/request helpers from
`src-executor/okx_rest.rs` into:

`src-executor/okx_rest/signing_request_surface.rs`

## Frozen Boundary

| Field | Value |
| --- | --- |
| Planned module | `src-executor/okx_rest/signing_request_surface.rs` |
| Approved helper surface | credential validation, HMAC signing, OKX timestamp formatting, REST base URL resolution for signed requests, public `build_signed_request`, private timestamp-injected signed request builder |
| Tests to move | signing string/base64 test, timestamp format test, demo signed-request header/flag test, empty production credential rejection test |
| Parent facade | `okx_rest.rs` keeps public types/constants and re-exports `build_signed_request` |
| Parent retained | transport/proxy/raw send, response success checks, order/query/cancel/balance actions, lookup validation, remaining tests |

## Equivalent Behavior

The extracted child must preserve:

- HMAC signing input shape: `timestamp + method + request_path + body`;
- OKX timestamp format;
- credential empty-field rejection;
- `/api/v5/` path guard;
- REST base URL environment override and validation;
- demo simulated-trading header and SDK/audit markers.

## Exclusions

- Do not move order/query/cancel/balance action wrappers.
- Do not move proxy/agent/raw-send response handling.
- Do not move lookup validation helpers.
- Do not introduce release-transition links.

## Gates For Actual Extraction

| Gate | Command |
| --- | --- |
| format | `cargo fmt --all --check` |
| executor compile | `cargo check -p quantpilot --bin executor` |
| focused tests | `cargo test -p quantpilot --bin executor okx_` |
| diff hygiene | `git diff --check` |

## Next Step

BE-002LT-02
`root.executor.runner.runner_support_files.okx_rest.signing_request_surface`
actual_extraction.
