# v4.16.0 root.contracts.qrpc_core.strategy_ir.root_validation parent residual judgment selects unknown_marker_validation

> Batch: BE-001RH-01
> Node: `root.contracts.qrpc_core.strategy_ir.root_validation`
> Selected child: `root.contracts.qrpc_core.strategy_ir.root_validation.unknown_marker_validation`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core.strategy_ir.root_validation` returns to its child queue after `data_execution_validation` closeout.

Decision:

`next_child: root.contracts.qrpc_core.strategy_ir.root_validation.unknown_marker_validation`

## Closed Root Validation Children

| Child | Result |
| --- | --- |
| `contracts.qrpc_core.strategy_ir.root_validation.identity_required_validation` | Closed with `stop_split: true`; owns identity, required field, required collection, and duplicate id validation. |
| `contracts.qrpc_core.strategy_ir.root_validation.signal_logic_validation` | Closed with `stop_split: true`; owns signal detail, indicator support, logic rule, and logic position unknown-marker validation. |
| `contracts.qrpc_core.strategy_ir.root_validation.risk_validation` | Closed with `stop_split: true`; owns risk unknownable checks and risk profile id/numeric validation. |
| `contracts.qrpc_core.strategy_ir.root_validation.data_execution_validation` | Closed with `stop_split: true`; owns data requirement checks plus execution and execution profile validation. |

## Open Root Validation Residuals

| Residual | Status |
| --- | --- |
| `contracts.qrpc_core.strategy_ir.root_validation.unknown_marker_validation` | Selected next. Owns unknownable helper family and `unknowns[*]` path/reason validation through the root validation parent. |
| `contracts.qrpc_core.strategy_ir.root_validation.test_fixture` | Conditional. Only split after unknown marker validation closes, and only if local tests still create parent-sized pressure. |

## Selection Rationale

`unknown_marker_validation` is selected next because it is the final contiguous non-test validation residual in `StrategyIr::validation_errors` and it already owns a stable helper family:

- it owns `unknowns[{index}].path` required-field diagnostics;
- it owns `unknowns[{index}].reason` required-field diagnostics;
- it is the natural owner for `validate_unknownable`;
- it is the natural owner for `validate_unknownable_opt`;
- it can preserve validation ordering by replacing the current unknowns loop with one parent-mediated helper call;
- it can preserve closed sibling behavior because already extracted children call the parent-owned helpers today.

## Same-Parent Parallel Judgment

`same_parent_parallel_children` remains inactive for this selection.

The remaining validation residual is small but touches helper ownership used by closed sibling children. The safe high-speed path is still a single-child baseline followed by serial parent facade/helper mediation.

## Hard Boundaries

The next `unknown_marker_validation` baseline must not:

- edit Rust source code;
- change unknown-marker diagnostics, validation ordering, or test expectations;
- move local tests or public `StrategyIr` methods;
- make root validation child modules import each other directly;
- widen visibility beyond the parent-mediated boundary without a documented compatibility reason;
- change closed Strategy IR child DTOs/enums/constants/public functions, protocol primitives, runtime IO, compiler/runtime/backend/executor/frontend behavior, physical `plugins/*`, or release transition.

## Next Step

BE-001RI-01 `root.contracts.qrpc_core.strategy_ir.root_validation.unknown_marker_validation` baseline_plan.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
