# v4.16.0 root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_macd_formula single leaf closeout

> Batch: BE-002JK-03
> Node: `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_macd_formula`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`stop_split: true`.

`manual_macd_formula` is now a terminal leaf. It owns one cohesive MACD fallback
recognition decision: direct MACD line/histogram handling, EMA spread fallback,
MACD line/signal fallback, and source/period validation. Further splitting
would separate tightly coupled MACD recognition halves and raise governance
cost without improving module clarity.

## Terminal Leaf Control

`tools/evaluate-leaf-granularity.ps1 -Path quantscript/src/lowering/fallback/manual_macd_formula.rs` result:

| Metric | Result |
| --- | --- |
| `split_decision` | `STOP` |
| `governance_packaging` | `stop_split` |
| `final_decision` | `STOP` |
| `normalized_split_score` | 37 |
| `loc` | 176 |
| `functions` | 4 |
| `public_surface` | 2 |
| `branches` | 10 |
| `risk_tags` | `cross_crate`, `public_api` |
| `governance_mode` | `stop_split` |

## Closeout Rationale

| Gate | Result | Evidence |
| --- | --- | --- |
| Terminal size fit | PASS | Current child is inside the target terminal range |
| Ownership cohesion | PASS | All functions participate in one MACD recognition family |
| Public surface control | PASS | Parent wrappers preserve MACD helper surfaces |
| Split benefit | LOW | Script scored split benefit at 14 |
| Governance cost | STOP | Script recommends `stop_split` |

## Next Step

BE-002JL-01 return to `root.contracts.quantscript.runtime_lowering.manual_formula_fallback` parent residual judgment.
