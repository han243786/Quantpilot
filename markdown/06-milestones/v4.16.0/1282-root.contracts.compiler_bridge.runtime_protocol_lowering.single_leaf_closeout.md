# v4.16.0 root.contracts.compiler_bridge.runtime_protocol_lowering single leaf closeout keeps split open

> Batch: BE-001XH-01
> Node: `root.contracts.compiler_bridge.runtime_protocol_lowering`
> Parent: `root.contracts.compiler_bridge`
> Stage: `single_leaf_closeout`
> Movement: no Rust code movement; post-extraction split decision only.

---

## Summary

`root.contracts.compiler_bridge.runtime_protocol_lowering` completed its first extraction, but this leaf must remain open for recursive subdivision.

The extracted module is cohesive enough to compile and preserve public behavior, but it still contains multiple stable white-box owner pockets: runtime protocol orchestration/data binding, runtime intent/signal lowering, runtime comparison decoding/fallback descriptions, runtime agent policy lowering, and runtime risk policy lowering.

Decision:

`stop_split: false`

Next step:

Return to `root.contracts.compiler_bridge.runtime_protocol_lowering` parent residual judgment and select the first internal child. The likely first candidate is `intent_signal_lowering`, but selection must be recorded in BE-001XI-01 before any further Rust movement.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | Internal residual children can be named by stable behavior pockets: orchestration/data binding, intent/signal lowering, comparison decoding, agent policy, and risk policy. |
| parent_child_communication_kept | pass | Further children can remain under `runtime_protocol_lowering`; parent facade in `qrpc_compiler/src/lib.rs` remains the external call boundary. |
| equivalence_baseline_freezable | pass | `cargo test -p qrpc-compiler` covers runtime lowering outputs; each candidate can freeze input/output behavior before movement. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | true | The public runtime lowering entrypoint is now backed by multiple private behavioral phases. |
| state_machine_phase | true | Runtime lowering maps data, intent, agent, risk, execution, and DAG validation phases. |
| strategy_branch | true | Intent/signal lowering is separate from agent/risk policy lowering and from Strategy IR lowering. |
| independent_failure_mode | true | Intent conversion can fail independently through indicator kind/structured comparison behavior, while risk/agent lowering are mostly projection phases. |
| reuse_pressure | true | Intent comparison and fallback helpers are used by signal and spread condition paths. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | false | Candidate children have stable method groups and caller/callee boundaries. |
| communication_cost_rises | false | Parent-mediated child calls can preserve one-way flow without sibling links. |
| local_proof_missing | false | qrpc-compiler regression tests cover runtime metadata, data hints, MA/RSI/Momentum/ZScore/spread conditions, agent and risk lowering. |
| line_count_only | false | Continued split is driven by independent behavior pockets, not only the 608-line module size. |

leaf_split_decision_result

`continue_split`

`runtime_protocol_lowering` remains open. Do not add it to `closed_children` yet.

next_recursive_step

BE-001XI-01 root.contracts.compiler_bridge.runtime_protocol_lowering parent_residual_judgment
## Boundary

**Real files**:
- `qrpc_compiler/src/runtime_protocol_lowering.rs`
- `qrpc_compiler/src/lib.rs`

**Markers**:
- `BE-001XH-01`
- `root.contracts.compiler_bridge.runtime_protocol_lowering stop_split: false`
- `intent lowering residual remains`
- `agent risk residual remains`

**Next step**:
BE-001XI-01 root.contracts.compiler_bridge.runtime_protocol_lowering parent_residual_judgment

---

## Gates

- `cargo test -p qrpc-compiler`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
