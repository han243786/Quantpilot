# v4.16.0 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder.machine_builder single leaf closeout stops split

> Batch: BE-001WT-03
> Node: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder.machine_builder`
> Parent: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder`
> Stage: `single_leaf_closeout`
> Movement: No code movement. This closeout marks the compatibility machine-builder terminal.

---

## Summary

root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder.machine_builder single leaf closeout stops split

`machine_builder` is now a terminal leaf. It owns the three compatibility machine builders plus the shared `compat_machine` skeleton and helper functions required to keep the machine DTO construction coherent.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | PASS | The leaf owns Observation/Decision/Execution machine construction and shared machine skeleton helpers. |
| parent_child_communication_kept | PASS | The graph-builder parent calls three private machine-builder entrypoints; no sibling horizontal communication is needed. |
| equivalence_baseline_freezable | PASS | BE-001WT-02 passed `cargo fmt -p qrpc-core-ir`, `cargo test -p qrpc-core-ir`, and `cargo check -p quantpilot`. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | PARTIAL | The leaf is private, but its machines are returned by the public bridge report graph. |
| state_machine_phase | PASS | It builds Observation, Decision, and Execution compatibility state machines. |
| strategy_branch | PASS | It maps distinct Core IR data/indicator, signal/agent/risk, and execution groups. |
| independent_failure_mode | PARTIAL | Individual machines differ, but they share one `compat_machine` skeleton and policy set. |
| reuse_pressure | PARTIAL | Future bridge variants can reuse this leaf as a cohesive machine construction unit. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | PASS | Splitting the three small machine builders would create tiny leaves dominated by shared helper plumbing. |
| communication_cost_rises | PASS | Further split would duplicate or cross-wire `compat_machine`, metadata, memory-count, event, and policy helper state. |
| local_proof_missing | PASS | Existing bridge mapping tests validate the machines through the public bridge entrypoint. |
| line_count_only | PASS | Stop decision is based on cohesion and shared helper cost, not only the 228-line size. |

leaf_split_decision_result

`stop_split: true`

The next recursive step returns to `compat_graph_builder` parent residual judgment.

next_recursive_step

BE-001WU-01 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder parent_residual_judgment
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/core_ir_compat_bridge/compat_graph_builder/machine_builder.rs`

**Markers**:
- `BE-001WT-03`

**Next step**:
BE-001WU-01 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder parent_residual_judgment

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
