# v4.16.0 root.executor.runner parent residual judgment selects v4_deploy_support

> Batch: BE-002LM-01
> Node: `root.executor.runner`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select next child:

`root.executor.runner.v4_deploy_support`

## Parent Residual Scan

Closed runner children:

- `provider_order_routes`
- `strategy_lifecycle_routes`
- `market_stream_routes`

Remaining runner child queue:

- `v4_deploy_support`
- `runner_support_files`

## Why This Child

`v4_deploy_support` owns the v4 deployment helper surface still retained by
`src-executor/main.rs`: deploy request parsing, runtime graph resolution,
execution mode parsing, empty Core IR construction, strategy config preflight
checks, JSON path helpers, and static bundle construction.

## Proposed Ownership

| Field | Value |
| --- | --- |
| Candidate child | `v4_deploy_support` |
| Planned file | `src-executor/v4_deploy_support.rs` |
| First movement | v4 deploy DTO/helper block and preflight/static bundle helper block |
| Parent retained for first movement | route registration, runner setup, route modules, provider helpers, audit helper |

## Exclusions

- Do not move strategy lifecycle route handlers already closed.
- Do not move market stream route handlers already closed.
- Do not move OKX provider route handlers already closed.
- Do not move runner support files.
- Do not introduce release-transition links.

## Next Step

BE-002LN-01 `root.executor.runner.v4_deploy_support` baseline_plan.
