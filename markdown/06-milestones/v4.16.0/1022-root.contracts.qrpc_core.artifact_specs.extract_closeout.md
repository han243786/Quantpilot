# v4.16.0 root.contracts.qrpc_core.artifact_specs extract closeout

> Batch: BE-001RS-02
> Node: `root.contracts.qrpc_core.artifact_specs`
> Parent: `root.contracts.qrpc_core`
> Stage: `extract_closeout`
> Movement: Rust code extraction.

---

## Summary

`root.contracts.qrpc_core.artifact_specs` has been extracted from `qrpc_core/src/lib.rs` into a private child module.

The qrpc-core crate root preserves the public facade:

- `mod artifact_specs;`
- `pub use artifact_specs::*;`

## Files Changed

- `qrpc_core/src/lib.rs`
- `qrpc_core/src/artifact_specs.rs`

## Extraction Result

`qrpc_core/src/artifact_specs.rs` now owns:

- `ArtifactDigestAlgorithm`;
- `ArtifactDigest`;
- `canonical_json_sha256_digest`;
- `RunModeSpec`;
- `BacktestReplaySource`;
- `StrategyArtifactSourceKind`;
- `DatasetSpec` and `DatasetSpec::from`;
- `ExecutionAssumptionSpec`;
- `ExecutionAssumptionValueSource`;
- `ExecutionAssumptionSourceSummary`;
- `ExecutionAssumptionSpec::from`;
- `MarketDataSnapshotSpec` and `MarketDataSnapshotSpec::from_runtime_protocol`;
- `RunSpec`;
- `RunSpecRuntimeProtocolInput`;
- `RunSpec::from_runtime_protocol`;
- `BacktestSpec` and `BacktestSpec::new`;
- `StrategyArtifact`;
- `CoreIrArtifact`;
- `CompileArtifact`;
- `CompileArtifactBundle`.

`qrpc_core/src/lib.rs` keeps later qrpc-core residuals in place:

- runtime IO DTOs beginning at `RawKline`;
- RFC execution contracts;
- crate-root tests.

## Rust Local Fields

| Field | Result |
| --- | --- |
| Crate | `qrpc-core` |
| Parent facade | Preserved. `lib.rs` remains the crate root facade and re-export owner. |
| Child visibility | The module is private; public item surfaces are preserved through `pub use artifact_specs::*;`. |
| Public exports | Existing crate-root public names remain available through `qrpc_core::*`. |
| Parent-owned residuals | Runtime IO contracts, RFC execution contracts, and tests remain parent-owned for later recursion. |
| Sibling dependency check | Passed. The child imports runtime config and protocol primitives through crate-level public names and does not import sibling internals. |

## Equivalence Preservation

| Surface | Preserved evidence |
| --- | --- |
| Canonical digest | SHA-256 over `serde_json::to_vec` output remains unchanged. |
| Digest schema | Algorithm/value field shape and snake_case algorithm serde remain unchanged. |
| Run/backtest specs | Schema version use, input assignment, and constructors remain unchanged. |
| Dataset projection | `DataSourceConfig` to `DatasetSpec` field mapping remains unchanged. |
| Execution assumptions | Runtime config to execution assumption mapping remains unchanged. |
| Market data snapshot projection | Runtime data source projection into datasets remains unchanged. |
| Artifact DTOs | Strategy/core-IR/compile/bundle field shapes remain unchanged. |
| Public facade | `pub use artifact_specs::*;` preserves crate-root access. |
| Later qrpc-core schemas | Runtime IO, RFC execution contracts, and tests were not moved. |

## Hard Boundaries Held

BE-001RS-02 did not:

- change canonical digest behavior, serde attributes, schema version constants, field names, projection behavior, constructors, public exports, or tests;
- move runtime IO DTOs beginning at `RawKline`, RFC execution contracts, or crate-root tests;
- change closed error/proto/plugin/strategy IR/protocol primitive/runtime config contracts;
- change `qrpc_core_ir`, `qrpc_compiler`, `qrpc_runtime`, `quantscript`, backend, executor, frontend, physical `plugins/*`, or release transition.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-pre-commit-hook.ps1`

## Next Step

BE-001RS-03 `root.contracts.qrpc_core.artifact_specs` single_leaf_closeout.
