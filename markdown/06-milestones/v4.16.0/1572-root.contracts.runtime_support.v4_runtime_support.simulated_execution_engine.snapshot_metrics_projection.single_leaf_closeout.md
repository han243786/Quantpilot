# v4.16.0 simulated_execution_engine.snapshot_metrics_projection single leaf closeout

> Batch: BE-002ER-02
> Node: `root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine.snapshot_metrics_projection`
> Parent: `root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Set `stop_split: true` for `snapshot_metrics_projection`.

The child is a micro leaf and already cohesive: it owns one projection surface around asset curve points, portfolio snapshots, microstructure samples, and projection-only rounding. Further splitting would create tiny projection helpers with more governance overhead than ownership benefit.

## Leaf Granularity Evidence

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -Path qrpc_runtime\src\v4_runtime\simulated_execution_engine\snapshot_metrics_projection.rs
```

Result:

| Field | Value |
| --- | --- |
| `split_decision` | `STOP` |
| `governance_packaging` | `stop_split` |
| `final_decision` | `STOP` |
| Score | `22` |
| LOC | `87` |
| Functions | `5` |
| Public/internal-visible surface | `3` |
| Branches | `1` |
| Domains | `6` |
| Risk tags | `cross_crate`, `persistence`, `public_api`, `route_schema`, `state_machine`, `trading_semantics` |
| Terminal size bucket | `micro_under_100` |

## Stop-Split Rationale

`snapshot_metrics_projection` has a single read/projection owner: converting simulated execution state into asset curve, snapshot, and microstructure metrics. Its public surface is intentional and bounded to the `v4_runtime` parent layer where needed.

Rejected finer splits:

| Candidate | Decision | Reason |
| --- | --- | --- |
| `asset_curve_projection` | Reject | Only `record_asset_point` and `asset_point`; too small and directly feeds snapshot projection. |
| `snapshot_projection` | Reject | Snapshot shares `asset_point`, rounding, and the same state read model. |
| `microstructure_projection` | Reject | Small enough to stay with the read-side projection owner. |
| `round_money` utility child | Reject | Private helper with only projection callers. |

## Preserved Boundary

The parent `simulated_execution_engine` still owns the next residual candidates:

- `validation_capability_helpers`;
- direct-submit orchestration and parent facade residuals;
- tests.

No release-transition optimization, hot-path sibling horizontal connection, route/schema change, persistence owner change, runtime mode change, or test rewrite was introduced.

## Evidence

Inherited from BE-002ER-01 extraction:

- `cargo fmt --check`: PASS
- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime v4 -- --nocapture`: PASS, 32 tests
- `cargo check -p quantpilot`: PASS

This closeout is docs-only and updates the recursive cursor plus governance indexes.

## Next Step

BE-002ES-01 `root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine` parent_residual_judgment.
