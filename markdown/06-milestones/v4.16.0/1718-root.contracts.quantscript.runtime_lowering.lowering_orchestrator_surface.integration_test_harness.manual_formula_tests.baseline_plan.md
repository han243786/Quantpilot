# v4.16.0 root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.manual_formula_tests baseline plan

> Batch: BE-002IC-01
> Node: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.manual_formula_tests`
> Parent: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Baseline Decision

`manual_formula_tests` is frozen as a precision test-only child for extraction.

The next movement may create a child test module under the existing lowering orchestrator test harness and move only the manual formula recognition and unsupported loop-formula rejection tests from `quantscript/src/lowering/orchestrator/integration_test_harness.rs`.

## Frozen Input / Output

| Field | Boundary |
| --- | --- |
| Input | Test-only QuantScript helper formula fixtures for MA, momentum, zscore, MACD, RSI variants, and loop-built gain/loss lists |
| Output | Existing assertions over `IntentKind`, formula-derived params, and unsupported loop diagnostics |
| Parent caller | `integration_test_harness` test module only |
| Error surface | Existing diagnostic substrings for unsupported `for` / `while` formula lowering remain unchanged |
| Gate | `cargo test -p quantscript` |

## Symbols Approved For Movement

Move only these existing tests:

- `lowers_manual_moving_average_helper_formula`
- `lowers_manual_momentum_formula`
- `lowers_manual_zscore_formula`
- `lowers_manual_macd_histogram_formula`
- `lowers_manual_momentum_ratio_formula`
- `lowers_manual_ma_gap_ratio_formula`
- `lowers_manual_rsi_formula_from_rs_ratio`
- `lowers_manual_rsi_formula_with_avg_gain_loss_aliases`
- `lowers_manual_ema_rsi_formula`
- `lowers_manual_cutler_rsi_formula`
- `rejects_manual_rsi_formula_from_loop_built_gain_loss_lists_in_formal_path`
- `rejects_manual_rsi_formula_from_while_loop_gain_loss_lists_in_formal_path`

Move the local fixture helper functions embedded inside those raw QuantScript strings only as part of their owning tests. No production Rust helper moves are approved.

## Symbols Explicitly Not Approved For Movement

- `MA_SCRIPT`
- `RSI_SCRIPT`
- basic MA / RSI / alias / runtime coordinator smoke tests
- semantic missing-helper rejection test
- exchange data-source decoding test
- spread lowering admission/rejection tests
- rebalance lowering/rejection tests
- production lowering code, production imports, and parent test module declaration

## Parent Mediation Rule

After extraction:

- `integration_test_harness.rs` declares the child test module;
- the child uses parent-visible imports through `super::*`;
- no production sibling module is called from the child;
- test names and assertions remain equivalent.

## Split Decision Gate

| Gate | Result | Evidence |
| --- | --- | --- |
| `micro_leaf_without_owner` | PASS | The child contains 12 tests over one semantic formula recognition/rejection surface. |
| `communication_cost_rises` | PASS | Movement is test-only and remains under the parent test harness. |
| `local_proof_missing` | PASS | `cargo test -p quantscript` directly executes the moved tests. |
| `line_count_only` | PASS | The split is by scenario ownership, not by line count. |

## Validation

The actual extraction must pass:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8, full-feature-tree, and matrix governance checks

## Next Step

BE-002IC-02 actual_extraction.
