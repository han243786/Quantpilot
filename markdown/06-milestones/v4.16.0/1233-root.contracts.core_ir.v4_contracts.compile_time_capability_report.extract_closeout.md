# v4.16.0 root.contracts.core_ir.v4_contracts.compile_time_capability_report actual extraction complete

> Batch: BE-001WB-02
> Node: `root.contracts.core_ir.v4_contracts.compile_time_capability_report`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `extract_closeout`
> Movement: Code movement completed.

---

## Summary

The compile-time capability report surface is now a dedicated v4 child module.

Moved into `qrpc_core_ir/src/v4/compile_time_capability_report.rs`:

- compile-time capability request/report DTOs
- capability report verdict/diagnostic/status/entry DTOs
- `V4StaticContractBundle::build_compile_time_capability_report`
- private type/execution/plugin capability entry builders
- `V4CompileTimeCapabilityReport::validate_for_compile`
- `push_capability_diagnostic`
- compile-time capability default helpers

Parent `qrpc_core_ir/src/v4.rs` now declares and re-exports the child:

- `mod compile_time_capability_report;`
- `pub use compile_time_capability_report::*;`

Parent-retained residuals:

- Core IR compatibility report DTOs
- Core IR v4 bridge builders and validators
- bridge diagnostics
- tests and sample builders

Equivalence gates passed:

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir` (54 passed)
- `cargo check -p quantpilot`

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs`
- `qrpc_core_ir/src/v4/compile_time_capability_report.rs`

**Markers**:
- `BE-001WB-02`

**Next step**:
BE-001WB-03 root.contracts.core_ir.v4_contracts.compile_time_capability_report single_leaf_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
