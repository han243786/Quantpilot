# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline parent residual judgment: fast_backtest_sandbox

> Batch: BE-002CZ-01
> Node: `root.contracts.runtime_support.sandbox_replay_timeline`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.

---

## Decision

Select next child:

- `root.contracts.runtime_support.sandbox_replay_timeline.fast_backtest_sandbox`

## Closed Children

- `timeline_data_providers`
- `unified_timeline`
- `replay_builder`
- `sandbox_mode_surface`
- `realtime_sandbox`

## Residual Surface

The sandbox parent still owns the fast backtest implementation:

- `FastBacktestSandbox`
- fast-backtest constructors
- unified timeline replay setup
- mock replay setup
- backtest session execution
- backtest output construction
- benchmark equity projection
- parent-owned tests and shared helpers

## Boundary Rule

The next baseline must freeze only `FastBacktestSandbox` movement. It must not move:

- parent shared helpers before their own residual judgment;
- sandbox tests before the test harness child;
- already closed realtime/mode/timeline/replay children;
- release transition or sibling runtime-support modules.

## Next Step

BE-002DA-01 `root.contracts.runtime_support.sandbox_replay_timeline.fast_backtest_sandbox` baseline_plan.
