# v4.16.0 root.contracts.runtime_support.fill_engine.test_harness baseline plan

> Batch: BE-002BL-01
> Node: `root.contracts.runtime_support.fill_engine.test_harness`
> Parent: `root.contracts.runtime_support.fill_engine`
> Stage: `baseline_plan`
> Movement: No Rust code movement.

---

## Scope

Freeze the baseline for extracting the local fill-engine test harness.

Allowed movement:

- add `qrpc_runtime/src/fill_engine/test_harness.rs`;
- add `#[cfg(test)] mod test_harness;` in `qrpc_runtime/src/fill_engine.rs`;
- move the current `#[cfg(test)] mod tests` body into the child file;
- keep the test helpers and tests equivalent.

## In-Scope Test Harness

- `sample_order_with_side`
- `sample_order`
- `sample_plan`
- `sample_portfolio`
- `sample_portfolio_with_long_position`
- `sample_market`
- fill engine behavior tests for market fills, slippage, cash caps, resting orders, idempotency, later market updates, IOC, FOK, sell reservations, and sell-position rejection.

## Out of Scope

- production `FillEngine` lifecycle orchestration;
- public `apply_fill_to_portfolio`;
- type definitions and public constructor/accessor methods;
- closed `event_projection_wave`;
- closed `portfolio_reservation_accounting`;
- closed `fill_report_execution_helpers`;
- release-transition optimization.

## Equivalence Baseline

The next actual extraction must preserve:

- all existing fill-engine test names and assertions;
- `cargo test -p qrpc-runtime fill_engine -- --nocapture`;
- `cargo check -p quantpilot`;
- governance checks.

## Next Step

BE-002BM-01 `root.contracts.runtime_support.fill_engine.test_harness` actual_extraction.
