# v4.16.0 root.contracts.runtime_support.risk_support.event_payload_projection actual extraction complete

> Batch: BE-002BS-01
> Node: `root.contracts.runtime_support.risk_support.event_payload_projection`
> Parent: `root.contracts.runtime_support.risk_support`
> Stage: `actual_extraction`
> Movement: Rust code movement.

---

## Result

`root.contracts.runtime_support.risk_support.event_payload_projection` has been extracted.

New child file:

- `qrpc_runtime/src/risk_checker/event_payload_projection.rs`

Parent file changed:

- `qrpc_runtime/src/risk_checker.rs`

Moved helpers:

- `build_risk_event_payload`
- `risk_explanation_summary`
- `reason_code_name`
- `action_stats`
- `target_weight_stats`

## Public Surface Preservation

No public function, struct, trait, crate export, event payload key, serde shape, or provider entrypoint was changed.

`RiskCheckerProvider`, `RiskChecker`, `RiskCheckerProvider::evaluate`, `evaluate_risk_decision`, and `enforce_risk_mode` remain parent-owned.

## Dependency Shape

The child is parent-mediated via:

- `mod event_payload_projection;`
- `use event_payload_projection::build_risk_event_payload;`

The child uses `super::*` to access parent-owned types and exposure math helpers. No sibling shortcut was introduced.

## Preserved Boundaries

- direction and cross-symbol constraints remain outside this child;
- action clamp helpers remain outside this child;
- portfolio target clamp helpers remain outside this child;
- exposure math helpers remain parent-owned;
- tests remain outside this child;
- release-transition optimization remains out of scope.

## Gates

Current Rust gates:

- `cargo fmt --check`: PASS
- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime risk -- --nocapture`: PASS, 39 passed

Required closeout gates before commit:

- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-002BS-02 `root.contracts.runtime_support.risk_support.event_payload_projection` single_leaf_closeout.
