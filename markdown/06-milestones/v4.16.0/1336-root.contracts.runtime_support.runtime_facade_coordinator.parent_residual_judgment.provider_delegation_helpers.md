# v4.16.0 runtime_facade_coordinator parent residual judgment: provider_delegation_helpers

> Batch: BE-001YQ-01
> Node: `root.contracts.runtime_support.runtime_facade_coordinator`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

## Parent Status

`runtime_facade_coordinator` remains open.

Closed children:

- `contracts.runtime_support.runtime_facade_coordinator.constructor_provider_wiring`
- `contracts.runtime_support.runtime_facade_coordinator.session_cycle_orchestration`
- `contracts.runtime_support.runtime_facade_coordinator.execution_market_entrypoints`
- `contracts.runtime_support.runtime_facade_coordinator.state_config_accessors`
- `contracts.runtime_support.runtime_facade_coordinator.config_generation`

## Selected Child

Next child:

`root.contracts.runtime_support.runtime_facade_coordinator.provider_delegation_helpers`

## Selection Reason

The parent still owns a cohesive chain of provider delegation helpers used by session/cycle orchestration:

- `RuntimeCoordinator::collect_normalized_data`
- `RuntimeCoordinator::evaluate_intents`
- `RuntimeCoordinator::evaluate_agents`
- `RuntimeCoordinator::merge_agent_decisions`
- `RuntimeCoordinator::merge_records`
- `RuntimeCoordinator::evaluate_risks`
- `RuntimeCoordinator::plan_execution`
- `RuntimeCoordinator::execute_plans`
- `RuntimeCoordinator::process_open_orders`

These methods coordinate data, intent, agent, merge, risk, and execution providers while preserving runtime events and coordinator state mutation. They are distinct from portfolio projection and test harness ownership.

## Split Decision Gate

The selected child is worth extracting because:

- it is the remaining provider-call core under the coordinator
- it has a clear input/output chain around runtime events, market data, decisions, plans, and fills
- it reduces the parent toward a small facade plus portfolio projection and tests
- it keeps public and private provider delegation methods together instead of spreading cross-provider state mutation across unrelated files

## Remaining Parent Residuals

After this child, the remaining queue is:

1. `contracts.runtime_support.runtime_facade_coordinator.portfolio_projection`
2. `contracts.runtime_support.runtime_facade_coordinator.coordinator_test_harness`

## Next Step

BE-001YR-01 `root.contracts.runtime_support.runtime_facade_coordinator.provider_delegation_helpers` baseline_plan.
