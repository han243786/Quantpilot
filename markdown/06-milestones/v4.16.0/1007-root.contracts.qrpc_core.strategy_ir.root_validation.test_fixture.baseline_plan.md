# v4.16.0 root.contracts.qrpc_core.strategy_ir.root_validation.test_fixture baseline plan

> Batch: BE-001RK-01
> Node: `root.contracts.qrpc_core.strategy_ir.root_validation.test_fixture`
> Parent: `root.contracts.qrpc_core.strategy_ir.root_validation`
> Stage: `baseline_plan`
> Movement: No code movement.

---

## Summary

This baseline freezes the root validation local test fixture before extraction.

The next implementation step may move only the `#[cfg(test)] mod tests` block from `qrpc_core/src/strategy_ir/root_validation.rs` into a sibling test module file under the root validation module directory.

All production validation children and parent facade behavior remain authoritative. This baseline narrows the next safe movement without changing Rust source code.

## Frozen Surface

The selected child owns the current test-only surface in `qrpc_core/src/strategy_ir/root_validation.rs`:

- `#[cfg(test)] mod tests`;
- `SAMPLE_JSON`;
- `parses_and_validates_strategy_ir_v0`;
- `rejects_invalid_unknown_marker`;
- `rejects_duplicate_signal_ids`;
- `rejects_non_finite_execution_profile_costs`;
- `accepts_custom_indicator_when_runtime_support_is_declared`.

## Equivalence Baseline

| Surface | Frozen behavior |
| --- | --- |
| Test fixture JSON | `SAMPLE_JSON` content remains byte-equivalent except for formatting required by file movement. |
| Success validation test | Valid sample still parses, validates, and checks strategy IR version, signal count, and unknown risk rule. |
| Unknown marker regression | Invalid `execution.order_type` unknown marker still surfaces an `execution.order_type` diagnostic. |
| Duplicate id regression | Duplicate signal id still surfaces the duplicate-id diagnostic. |
| Execution profile numeric regression | Non-finite `fee_bps` and `slippage_bps` still surface their respective diagnostics. |
| Custom indicator regression | Custom indicator remains accepted when runtime support is declared. |
| Production behavior | `StrategyIr::validation_errors`, `StrategyIr::validate`, validation ordering, diagnostics, and serde shape remain unchanged. |

## Planned Extraction Shape

BE-001RK-02 may:

- create `qrpc_core/src/strategy_ir/root_validation/tests.rs`;
- move the full `#[cfg(test)] mod tests` body into that file;
- replace the inline test block in `root_validation.rs` with `#[cfg(test)] mod tests;`;
- keep all test helper imports as `use super::*;`;
- avoid any production code movement.

## Rust Local Refactor Fields

| Field | Baseline |
| --- | --- |
| Crate | `qrpc-core` |
| Parent facade | `qrpc_core/src/strategy_ir/root_validation.rs` remains the production facade and validation coordinator. |
| Test module visibility | Test module remains `#[cfg(test)]` only. |
| Public exports | No `pub use` change is expected. |
| Production child modules | Closed validation children remain untouched. |
| Cargo gates | `cargo fmt --check`, `cargo check -p qrpc-core`, and `cargo test -p qrpc-core` are the Rust minimum gates for extraction. |

## Same-Parent Parallel Wave Judgment

This child remains a single-child extraction for BE-001RK-02.

`same_parent_parallel_children` is not activated because the edit touches the parent test module declaration and should be merged serially with the parent facade.

## Hard Boundaries

BE-001RK-02 must not:

- change test assertions, fixture JSON semantics, validation ordering, diagnostics, public methods, root DTO fields, or serde shape;
- move production validation helpers or closed child validation modules;
- make root validation child modules import each other directly;
- change closed Strategy IR child DTOs/enums/constants/public functions, protocol primitives, runtime IO, compiler/runtime/backend/executor/frontend behavior, physical `plugins/*`, or release transition.

## Split Rule Pre-Check

| Rule | Baseline result |
| --- | --- |
| Physical owner | Worth extracting. The remaining test fixture owns a coherent test-only surface and is physically separable. |
| Public surface | Stop after extraction. No public API is involved. |
| Mixed responsibility | Stop after extraction. The child is only a test fixture and regression suite. |
| State ownership | Stop after extraction. The child owns no runtime state. |
| Side effects | Stop after extraction. No IO, persistence, locks, runtime state, or external side effects are involved. |
| Parent-mediated dependency | Required. Tests may use `super::*`; production children must remain independent. |
| Future reopen rule | Allowed only for concrete root validation fixture or regression-test proposals. |

## Next Step

BE-001RK-02 `root.contracts.qrpc_core.strategy_ir.root_validation.test_fixture` extract_closeout.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-pre-commit-hook.ps1`
