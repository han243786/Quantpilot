# v4.16.0 root.contracts.runtime_support.agent_module parent residual judgment selects shared_scoring_portfolio_helpers

> Batch: BE-002AC-01
> Node: `root.contracts.runtime_support.agent_module`
> Stage: `parent_residual_judgment`
> Selected child: `root.contracts.runtime_support.agent_module.shared_scoring_portfolio_helpers`
> Movement: No Rust code movement.

---

## Parent State

Closed agent-module children:

- `contracts.runtime_support.agent_module.weighted_signal_decisions`
- `contracts.runtime_support.agent_module.portfolio_rebalance`
- `contracts.runtime_support.agent_module.cross_venue_arbitrage`

Remaining agent-module queue from the frozen baseline:

- `contracts.runtime_support.agent_module.shared_scoring_portfolio_helpers`
- `contracts.runtime_support.agent_module.test_harness`

## Selected Child Boundary

`shared_scoring_portfolio_helpers` is the remaining shared scoring, threshold, and portfolio-ratio helper pocket used by already-closed policy children.

Candidate current owner:

- `qrpc_runtime/src/agent_module.rs`

Candidate helper surface:

- `MIN_QUANTITY_RATIO`
- `DEFAULT_DECISION_THRESHOLD`
- `SPREAD_MULTIPLIER`
- `build_signal_kind_index`
- `signal_score`
- `available_position_ratio`
- `current_position_ratio`
- `portfolio_equity`

Current child dependencies:

- `weighted_signal_decisions` uses signal-kind indexing, signal scoring, threshold constants, and available-position ratio;
- `portfolio_rebalance` uses signal-kind indexing, signal scoring, threshold constants, and current-position ratio;
- `cross_venue_arbitrage` uses available-position ratio and spread multiplier.

## Why This Child Next

| Criterion | Judgment |
| --- | --- |
| Queue order | It follows the closed policy-family children in the frozen agent-module queue. |
| Cohesion | Medium: helpers share scoring/portfolio sizing purpose but serve several policy children. |
| Boundary risk | Medium-high: extracting helpers must preserve parent-mediated communication and avoid sibling shortcuts. |
| Inputs | Core IR signal rules, intent signals, portfolio positions, exchanges, symbols, and reference prices |
| Outputs | Signal-kind index, signed score, available/current position ratios, and portfolio equity |
| Exclusions | Provider dispatch, event emission, policy-specific decision builders, and tests stay outside this child. |

## Equivalence Baseline For Next Step

Next baseline must decide whether this helper pocket should:

- remain parent-owned and close without movement; or
- move into a helper child while the parent exposes mediated wrapper/re-export access to existing policy children.

Any movement must preserve:

- parent-mediated communication only;
- no direct sibling-to-sibling dependency;
- threshold constants and spread multiplier values;
- signal rule indexing by `indicator_id`;
- signed signal score behavior for long, short, and neutral sides;
- available-position ratio using unfrozen long quantity only;
- current-position ratio using full long net quantity;
- equity calculation as `cash_balance + total_net_notional`;
- existing agent-module tests for weighted signals, portfolio rebalance, and arbitrage.

Required gates for the next movement:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-002AD-01 `root.contracts.runtime_support.agent_module.shared_scoring_portfolio_helpers` baseline_plan.
