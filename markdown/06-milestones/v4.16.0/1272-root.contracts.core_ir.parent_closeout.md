# v4.16.0 root.contracts.core_ir parent residual judgment closes parent

> Batch: BE-001WX-01
> Node: `root.contracts.core_ir`
> Parent: `root.contracts`
> Stage: `parent_residual_judgment`
> Movement: no Rust code movement; governance parent closeout only.

---

## Summary

`root.contracts.core_ir` is closed for the current Rust extraction scope.

The Core IR crate root now acts as a controlled facade: `qrpc_core_ir/src/lib.rs` wires `v1` and `v4`, `qrpc_core_ir/src/v1.rs` wires the closed v1 contract children, and `qrpc_core_ir/src/v4.rs` wires the closed v4 contract children. No remaining production Rust owner is left under Core IR after the `v1_contract` and `v4_contracts` parent closeouts.

The recursive cursor returns to `root.contracts`; the next contracts child is `contracts.compiler_bridge`.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | Parent is `root.contracts.core_ir`; concrete children are `v1_contract` and `v4_contracts`. |
| parent_child_communication_kept | pass | Crate root and version roots expose controlled facade exports only. |
| equivalence_baseline_freezable | pass | This batch has no Rust movement; qrpc-core-ir tests and quantpilot check remain the proof gates. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | false | Public Core IR surfaces are already owned by closed version children and re-exported through facades. |
| state_machine_phase | false | V4 state-machine contract ownership closed under `v4_contracts`; v1 graph/policy/data contracts closed under `v1_contract`. |
| strategy_branch | false | No separate production strategy branch remains in the Core IR crate root. |
| independent_failure_mode | false | Remaining root files are module wiring and export surfaces, not failure domains. |
| reuse_pressure | false | Splitting crate/version facade code would create helper-only leaves. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | true | Remaining crate root/version root code has no independent owner beyond facade duties. |
| communication_cost_rises | true | Further split would add re-export indirection without reducing production coupling. |
| local_proof_missing | false | Local proof is available through unchanged Rust code plus crate tests/check. |
| line_count_only | true | `v4.rs` residual size is dominated by test fixtures, already excluded from production split triggers. |

leaf_split_decision_result

`stop_split: true`; `root.contracts.core_ir parent_closeout: true`.

Next recursive step: select `contracts.compiler_bridge` from the `root.contracts` residual queue.

next_recursive_step

BE-001WY-01 root.contracts parent_residual_judgment selects contracts.compiler_bridge
## Boundary

**Real files**:
- `qrpc_core_ir/src/lib.rs`
- `qrpc_core_ir/src/v1.rs`
- `qrpc_core_ir/src/v4.rs`
- `qrpc_core_ir/src/v1/`
- `qrpc_core_ir/src/v4/`

**Markers**:
- `BE-001WX-01`
- `root.contracts.core_ir parent_closeout: true`
- `core_ir crate facade only`
- `next contracts child: compiler_bridge`

**Next step**:
BE-001WY-01 root.contracts parent_residual_judgment selects contracts.compiler_bridge

---

## Gates

- `cargo test -p qrpc-core-ir`
- `cargo check -p quantpilot`
