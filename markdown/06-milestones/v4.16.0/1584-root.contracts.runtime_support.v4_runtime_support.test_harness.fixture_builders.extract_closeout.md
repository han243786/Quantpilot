# v4.16.0 root.contracts.runtime_support.v4_runtime_support.test_harness.fixture_builders extract closeout

> Batch: BE-002FB-01
> Node: `root.contracts.runtime_support.v4_runtime_support.test_harness.fixture_builders`
> Parent: `root.contracts.runtime_support.v4_runtime_support.test_harness`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> governance_next_authority: active

---

## Decision

Extract `fixture_builders` as a test-only child.

The extraction moved shared v4 test fixture helpers out of the parent test harness while preserving all test functions, assertions, fixture semantics, and production behavior.

## Code Movement

| Path | Change |
| --- | --- |
| `qrpc_runtime/src/v4_runtime/test_harness/fixture_builders.rs` | New test-only child owning shared v4 runtime test fixture builders. |
| `qrpc_runtime/src/v4_runtime/test_harness.rs` | Adds `mod fixture_builders;` and `use fixture_builders::*;`; removes moved helper bodies while leaving all `#[test]` functions in place. |

Moved helpers:

- `sample_core_ir_for_v4_runtime`
- `sample_compat_graph`
- `runtime_simulated_market_matrix`
- `provider_native_market_matrix_for_paper`
- `provider_native_market_matrix_for_live_actual`
- `runtime_simulated_market_matrix_for_live_actual`
- `sample_runtime`
- `nested_observation_graph`

## Equivalence

The migration preserves:

- all 32 v4 runtime tests;
- all shared fixture values and graph/capability matrix setup;
- parent-mediated test access through `use super::*` in the child;
- no production runtime code movement.

## Evidence

Verified after extraction:

| Gate | Result |
| --- | --- |
| `cargo fmt --check` | PASS |
| `cargo check -p qrpc-runtime` | PASS |
| `cargo test -p qrpc-runtime v4 -- --nocapture` | PASS, 32 tests |
| `cargo check -p quantpilot` | PASS |

Governance gates are run at commit time for this batch:

- `git diff --check`;
- `tools/check-utf8.ps1`;
- `tools/check-full-feature-tree.ps1`;
- `tools/check-matrix-governance.ps1`.

## Next Step

BE-002FB-02 `root.contracts.runtime_support.v4_runtime_support.test_harness.fixture_builders` single_leaf_closeout.
