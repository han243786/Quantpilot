# v4.16.0 root.contracts.qrpc_core.rfc_execution_contracts extract closeout

> Batch: BE-001SQ-02
> Node: `root.contracts.qrpc_core.rfc_execution_contracts`
> Parent: `root.contracts.qrpc_core`
> Stage: `extract_closeout`
> Movement: Rust physical extraction complete.

---

## Summary

`rfc_execution_contracts` now owns the final RFC-style qrpc-core public contract family that was previously embedded in `qrpc_core/src/lib.rs`.

Moved file:

- `qrpc_core/src/rfc_execution_contracts.rs`

Updated parent facade:

- `qrpc_core/src/lib.rs`

## Actual Extraction

| Baseline item | Result |
| --- | --- |
| Data request taxonomy and DTOs | Moved into `qrpc_core/src/rfc_execution_contracts.rs`. |
| Allocation DTO and helper | Moved into `qrpc_core/src/rfc_execution_contracts.rs`. |
| Order DTO and transition helper | Moved into `qrpc_core/src/rfc_execution_contracts.rs`. |
| Execution feedback DTOs | Moved into `qrpc_core/src/rfc_execution_contracts.rs`. |
| Handoff snapshot DTO and validation helper | Moved into `qrpc_core/src/rfc_execution_contracts.rs`. |
| Crate root facade | Added `mod rfc_execution_contracts;` and `pub use rfc_execution_contracts::*;`. |
| Crate-root tests | Stayed in `qrpc_core/src/lib.rs`. |

## Equivalence Evidence

| Check | Result | Evidence |
| --- | --- | --- |
| Public facade preserved | pass | `qrpc_core/src/lib.rs` re-exports `rfc_execution_contracts::*`. |
| Closed sibling boundaries preserved | pass | No closed qrpc-core child file was edited. |
| RFC surface preserved | pass | The moved file contains `MarketScope` through `HandoffSnapshot` and the two helper methods. |
| Parent-child communication | pass | The child imports public closed surfaces through the crate facade; no sibling internals are imported. |
| Tests preserved | pass | Existing qrpc-core tests remain in the crate root and passed. |

## Validation Results

- `cargo fmt -p qrpc-core` passed.
- `cargo check -p qrpc-core` passed.
- `cargo test -p qrpc-core` passed: 12 unit tests passed; doc tests passed with 0 tests.

## Hard Boundaries

This extraction did not:

- change enum variants, struct fields, public helper behavior, validation messages, order transition rules, allocation math, public exports, or crate-root tests;
- move crate-root tests;
- reopen closed qrpc-core children;
- change `qrpc_core_ir`, `qrpc_compiler`, `qrpc_runtime`, `quantscript`, backend, executor, frontend, physical `plugins/*`, or release transition;
- introduce direct sibling shortcuts.

## Next Step

BE-001SQ-03 `root.contracts.qrpc_core.rfc_execution_contracts` single_leaf_closeout.

The closeout must decide whether this new parent should stop as one compact module or continue recursively into data request, allocation, order/feedback, and handoff child families.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
