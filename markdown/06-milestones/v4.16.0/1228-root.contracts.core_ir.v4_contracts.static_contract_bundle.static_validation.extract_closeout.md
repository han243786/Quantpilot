# v4.16.0 root.contracts.core_ir.v4_contracts.static_contract_bundle.static_validation actual extraction complete

> Batch: BE-001VY-02
> Node: `root.contracts.core_ir.v4_contracts.static_contract_bundle.static_validation`
> Parent: `root.contracts.core_ir.v4_contracts.static_contract_bundle`
> Stage: `extract_closeout`
> Movement: Code movement completed.

---

## Summary

`V4StaticContractBundle::validate_static_contract` is now isolated in a nested static bundle validation child.

Moved into `qrpc_core_ir/src/v4/static_contract_bundle/static_validation.rs`:

- `V4StaticContractBundle::validate_static_contract`

Parent `qrpc_core_ir/src/v4/static_contract_bundle.rs` now owns only the bundle DTO/default surface and declares:

- `mod static_validation;`

Parent-retained residuals:

- `V4StaticContractBundle`
- `impl Default for V4StaticContractBundle`
- `default_static_contract_bundle_version`
- compile-time capability report DTOs/builders
- compatibility bridge, tests, and sample builders

Equivalence gates passed:

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir` (54 passed)
- `cargo check -p quantpilot`

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/static_contract_bundle.rs`
- `qrpc_core_ir/src/v4/static_contract_bundle/static_validation.rs`

**Markers**:
- `BE-001VY-02`

**Next step**:
BE-001VY-03 root.contracts.core_ir.v4_contracts.static_contract_bundle.static_validation single_leaf_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
