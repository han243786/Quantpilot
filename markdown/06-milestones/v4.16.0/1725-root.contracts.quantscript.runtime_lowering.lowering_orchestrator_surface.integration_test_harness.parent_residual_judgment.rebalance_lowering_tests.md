# v4.16.0 root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness parent residual judgment: rebalance_lowering_tests

> Batch: BE-002IF-01
> Node: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness`
> Selected child: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.rebalance_lowering_tests`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `rebalance_lowering_tests` as the next child under `integration_test_harness`.

`manual_formula_tests` and `spread_lowering_tests` are now closed. Among the remaining parent-owned tests, rebalance lowering has the clearest cohesive boundary because it owns portfolio rebalance helper lowering, allocation variants, unsupported helper rejection, and schedule projection.

## White-Box Boundary

| Field | Boundary |
| --- | --- |
| Input | Test-only QuantScript fixtures using `symbols`, `rebalance`, `equal_weight`, `fixed_weights`, `rank_weight`, and `score_weight` helpers |
| Output | Assertions over runtime config agents, target symbols, allocation kind, rank/score/fixed weight params, schedule mapping, Core IR agent policy, and rejection diagnostics |
| Current owner | `quantscript/src/lowering/orchestrator/integration_test_harness.rs` |
| Planned owner | `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.rebalance_lowering_tests` |
| Parent caller | `integration_test_harness` test module only |
| Exclusions | Basic MA/RSI smoke, runtime coordinator smoke, user helper call smoke, semantic missing-helper rejection, exchange-only decoding, manual formula tests, spread lowering tests |

## Candidate Tests

The selected child should freeze and move only these existing tests:

- `lowers_equal_weight_rebalance_helper_into_portfolio_rebalance_agent`
- `lowers_fixed_weights_rebalance_helper_into_portfolio_rebalance_agent`
- `lowers_rank_weight_rebalance_helper_into_portfolio_rebalance_agent`
- `lowers_score_weight_rebalance_helper_into_portfolio_rebalance_agent`
- `rejects_rebalance_helper_with_unsupported_frequency`
- `rejects_rank_weight_with_unsupported_method`
- `lowers_weekly_rebalance_schedule_into_agent_schedule`

## Split Decision Gate

| Gate | Result | Evidence |
| --- | --- | --- |
| `micro_leaf_without_owner` | PASS | Seven tests form one cohesive portfolio rebalance lowering and schedule projection surface. |
| `communication_cost_rises` | PASS | Movement is test-only and remains under the parent test harness. |
| `local_proof_missing` | PASS | `cargo test -p quantscript` directly executes the planned moved tests. |
| `line_count_only` | PASS | Selection is based on rebalance lowering ownership, not raw size. |

## Risk Classification

| Risk | Result |
| --- | --- |
| Public API / route / schema | No change planned |
| Persistence / lock owner | No change planned |
| Trading semantics evidence | High, test-only; allocation and rebalance schedule semantics must stay equivalent |
| Cross-crate behavior | Present through runtime config, `qrpc_core_ir` agent policy, and `qrpc_core` schedule/symbol assertions |

## Residual Queue

After this child, remaining candidate groups are:

- `basic_runtime_smoke_tests`
- `semantic_rejection_tests`

## Next Step

BE-002IG-01 `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.rebalance_lowering_tests` baseline_plan.
