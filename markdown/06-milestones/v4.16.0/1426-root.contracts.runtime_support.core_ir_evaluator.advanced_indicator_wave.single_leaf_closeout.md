# v4.16.0 root.contracts.runtime_support.core_ir_evaluator.advanced_indicator_wave single leaf closeout

> Batch: BE-002AM-03
> Node: `root.contracts.runtime_support.core_ir_evaluator.advanced_indicator_wave`
> Parent: `root.contracts.runtime_support.core_ir_evaluator`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Decision

`stop_split: true`

`root.contracts.runtime_support.core_ir_evaluator.advanced_indicator_wave` is closed as a terminal same-parent wave.

## Evidence

Current owner file:

- `qrpc_runtime/src/core_ir_evaluator/advanced_indicator_wave.rs`

Terminal leaf control:

```text
Leaf: root.contracts.runtime_support.core_ir_evaluator.advanced_indicator_wave
Decision: STOP
Score: 44
Metrics: loc=360, functions=10, public_surface=10, branches=20, domains=3
Terminal leaf control: size_bucket=terminal_target_150_600, governance_mode=stop_split
```

## Why No Deeper Split

No deeper child is opened because:

- the file is within the target terminal range;
- single-indicator leaves would create high governance cost and little ownership gain;
- all ten functions are direct registry entrypoints using parent-owned formula helpers;
- formula helper ownership is intentionally deferred to the shared helper residual judgment.

## Equivalence Evidence

The extraction batch already passed:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime core_ir_evaluator`: 22 passed
- `cargo check -p quantpilot`
- `git diff --check`
- `tools/check-utf8.ps1`
- `tools/check-full-feature-tree.ps1`
- `tools/check-matrix-governance.ps1`

## Next Step

BE-002AN-01 `root.contracts.runtime_support.core_ir_evaluator` parent_residual_judgment selects `spread_custom_expression_wave`.
