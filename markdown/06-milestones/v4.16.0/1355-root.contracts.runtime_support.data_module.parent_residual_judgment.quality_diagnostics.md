# v4.16.0 data_module parent residual judgment selects quality_diagnostics

> Batch: BE-001ZB-01
> Node: `root.contracts.runtime_support.data_module`
> Stage: `parent_residual_judgment`
> Selected child: `root.contracts.runtime_support.data_module.quality_diagnostics`
> Movement: No code movement.

## Context

`source_mapping` is closed. The next child in the data-module queue is `quality_diagnostics`.

## Selected Child

`root.contracts.runtime_support.data_module.quality_diagnostics`.

This child owns data quality and market data preview behavior:

- `MarketDataPreview`
- `data_timestamp_ms`
- `stale_after_ms_for_source`
- `gap_count_for_data`
- `build_quality_flags`
- `source_health_from_flags`
- `build_data_quality_snapshot`
- `attach_data_quality_snapshot`
- `market_data_quality`
- `build_data_quality_summary`
- `market_data_preview`

The next baseline must decide whether `apply_snapshot_status` joins this child because it mutates freshness fields for cached/stale snapshots.

## Selection Reason

`quality_diagnostics` is the next cohesive data-module behavior family.

Reasons:

- collection orchestration uses it immediately after fetch normalization
- runtime events depend on its health, freshness, gap, and flag outputs
- quote and kline snapshots share the same quality contract
- it is independent from Core IR source mapping, HTTP transport, endpoint construction, payload parsing, mock data, and historical cache
- moving it gives later collection orchestration a cleaner dependency boundary

## Split Decision Gate

The child should proceed through baseline before movement.

Expected outcome:

- add `qrpc_runtime/src/data_module/quality_diagnostics.rs`
- keep crate-visible helpers available through the parent module
- move quality snapshot, summary, and preview helpers only
- preserve source mapping, collection, exchange parsing, mock data, cache, transport, and tests for later residual judgments

## Equivalence Baseline

Required gates for the next movement:

- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-001ZC-01 `root.contracts.runtime_support.data_module.quality_diagnostics` baseline_plan.
