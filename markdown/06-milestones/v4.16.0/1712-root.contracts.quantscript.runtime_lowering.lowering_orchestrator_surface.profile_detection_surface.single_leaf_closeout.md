# v4.16.0 root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.profile_detection_surface single leaf closeout

> Batch: BE-002HY-03
> Node: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.profile_detection_surface`
> Parent: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`profile_detection_surface` is closed with `stop_split: true`.

## Leaf Granularity Evidence

Command:

- `tools/evaluate-leaf-granularity.ps1 -LeafId root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.profile_detection_surface -ParentId root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface -Path quantscript/src/lowering/profile_detection_surface.rs -Depth 5`

Result:

- `Decision: STOP`
- `Split decision: STOP`
- `Governance packaging: stop_split`
- `Score: 48`
- `loc=368`
- `functions=12`
- `size_bucket=terminal_target_150_600`
- `governance_mode=stop_split`

## Rationale

The current child owns one cohesive profile parsing and placement-validation surface:

- risk profile DTO and parser;
- execution profile DTO and parser;
- shared top-level-only placement validation pattern;
- recursive expression scanning for nested profile calls.

Splitting risk and execution into separate leaves would duplicate traversal and validation shape, add parent forwarding, and create two smaller high-risk leaves without improving public entrypoint equivalence. The parent `orchestrator.rs` already receives two simple detection calls and remains the correct mediation point.

## Parent Residual

Return to `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface` parent residual judgment.

Remaining candidates:

- `integration_test_harness`
- parent-owned `entrypoint_runtime_config_assembly`

## Next Step

BE-002HZ-01 `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface` parent_residual_judgment.
