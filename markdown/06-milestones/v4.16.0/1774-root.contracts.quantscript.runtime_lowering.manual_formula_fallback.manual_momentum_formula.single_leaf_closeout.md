# v4.16.0 root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_momentum_formula single leaf closeout

> Batch: BE-002JI-03
> Node: `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_momentum_formula`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`stop_split: true`.

`manual_momentum_formula` is now a terminal leaf. It owns one cohesive manual
momentum fallback decision: direct momentum formula handling, ratio momentum
handling, subtract/division ratio handling, and private legacy latest/lookback
pair recovery. Further splitting would separate tiny helper steps from the same
momentum recognition path and increase governance overhead.

## Terminal Leaf Control

`tools/evaluate-leaf-granularity.ps1 -Path quantscript/src/lowering/fallback/manual_momentum_formula.rs` result:

| Metric | Result |
| --- | --- |
| `split_decision` | `STOP` |
| `governance_packaging` | `stop_split` |
| `final_decision` | `STOP` |
| `normalized_split_score` | 33 |
| `loc` | 129 |
| `functions` | 5 |
| `public_surface` | 3 |
| `branches` | 5 |
| `risk_tags` | `cross_crate`, `public_api`, `trading_semantics` |
| `governance_mode` | `stop_split` |

## Closeout Rationale

| Gate | Result | Evidence |
| --- | --- | --- |
| Terminal size fit | PASS | Current child is a small cohesive leaf |
| Ownership cohesion | PASS | All functions participate in manual momentum recognition |
| Public surface control | PASS | Parent wrappers preserve all three public(crate) helper surfaces |
| Split benefit | LOW | No independent child can be separated without fragmenting one recognizer |
| Governance cost | STOP | Script recommends `stop_split` |

## Next Step

BE-002JJ-01 return to `root.contracts.quantscript.runtime_lowering.manual_formula_fallback` parent residual judgment.
