# v4.16.0 root.contracts.runtime_support.risk_support parent residual judgment selects direction_cross_constraints

> Batch: BE-002BT-01
> Node: `root.contracts.runtime_support.risk_support`
> Candidate: `root.contracts.runtime_support.risk_support.direction_cross_constraints`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.

---

## Decision

Select `root.contracts.runtime_support.risk_support.direction_cross_constraints` as the next child.

## Current Closed Children

- `root.contracts.runtime_support.risk_support.event_payload_projection`

## Candidate Scope

This child covers the post-evaluation risk constraint passes:

- `apply_direction_conflict_check`
- `apply_cross_symbol_constraints`

## Rationale

The candidate is cohesive because both helpers post-process risk decisions after per-policy/per-agent evaluation:

- direction conflict rejects mutually opposed same-symbol agent actions;
- cross-symbol constraints clamp aggregate leverage across accepted decisions.

Both helpers mutate risk decisions and emit risk events, but they do not own the provider trait, core `evaluate_risk_decision`, action clamp internals, portfolio target clamp internals, exposure math, event payload projection, or tests.

## Preserved Boundaries

- provider/evaluate surface remains parent-owned;
- event payload projection remains closed and outside this child;
- action and portfolio target clamps remain outside this child;
- exposure math helpers remain parent-owned;
- tests remain outside this child;
- no sibling shortcut may be introduced.

## Next Step

BE-002BU-01 `root.contracts.runtime_support.risk_support.direction_cross_constraints` baseline_plan.
