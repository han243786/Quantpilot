# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline.timeline_data_providers baseline plan

> Batch: BE-002CM-01
> Node: `root.contracts.runtime_support.sandbox_replay_timeline.timeline_data_providers`
> Parent: `root.contracts.runtime_support.sandbox_replay_timeline`
> Stage: `baseline_plan`
> Movement: No Rust code movement.

---

## Scope

Freeze the baseline for extracting timeline data provider implementations from `qrpc_runtime/src/sandbox/timeline.rs`.

Allowed movement:

- add `qrpc_runtime/src/sandbox/timeline_data_providers.rs`;
- move the provider block before `UnifiedTimeline`;
- keep `UnifiedTimeline` and its tests in `timeline.rs`;
- keep replay builder imports parent-mediated through `sandbox`/`timeline` module declarations.

## In-Scope Surface

- `TimelineDataProvider`
- `KlineProvider`
- `QuoteProvider`
- `ResampleKlineProvider`
- `bar_interval_ms`

## Out of Scope

- `UnifiedTimeline`
- `UnifiedTimeline::new`
- `UnifiedTimeline::from_providers`
- timeline cursor/next timestamp logic
- replay builder functions in `replay.rs`
- sandbox mode/control surface in `mod.rs`
- realtime and fast-backtest sandbox implementations
- local unified timeline tests unless a later baseline proves a separate test harness is better

## Equivalence Baseline

The next actual extraction must preserve:

- Kline asof snapshot behavior and rolling window cap;
- Quote asof fallback behavior;
- resampled kline interval aggregation behavior;
- provider data IDs and timestamps;
- replay builder access to provider types and trait;
- public crate behavior.

## Dependency Rule

No sibling shortcut is allowed. `timeline.rs` and `replay.rs` may only consume the provider child through parent-owned module declarations/imports.

## Gates

Required gates for actual extraction:

- `cargo fmt`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime sandbox -- --nocapture`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 / full feature tree / matrix governance

## Next Step

BE-002CN-01 `root.contracts.runtime_support.sandbox_replay_timeline.timeline_data_providers` actual_extraction.
