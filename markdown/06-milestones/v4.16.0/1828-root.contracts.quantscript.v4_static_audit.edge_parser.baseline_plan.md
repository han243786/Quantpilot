# v4.16.0 root.contracts.quantscript.v4_static_audit.edge_parser baseline plan

> Batch: BE-002KM-01
> Node: `root.contracts.quantscript.v4_static_audit.edge_parser`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Goal

Freeze the graph edge parser boundary before Rust movement.

This child owns one edge declaration grammar line and `MachineGraphEdge`
construction. Risk-plane parsing remains a separate candidate.

## Frozen Boundary

| Field | Value |
| --- | --- |
| Approved function | `parse_edge` |
| Planned file | `quantscript/src/v4_static_audit/edge_parser.rs` |
| Inputs | edge line text, edge index, source line number |
| Outputs | `MachineGraphEdge` or `Diagnostic` |
| Parent callers | `static_document_parser` through parent wrapper |
| Visibility | child function may be `pub(super)` only for the parent wrapper |

## Equivalent Behavior

The extracted child must preserve:

- syntax diagnostic `QSV4120`;
- edge id format `edge.{edge_index}.{source}.{target}`;
- source and target machine id extraction;
- event type extraction;
- `MachineGraphEdgeActivation::Always`;
- `required: true`;
- empty metadata map behavior.

## Exclusions

- Do not move `parse_state_group`.
- Do not move `parse_risk_plane`.
- Do not move `parse_machine_template`.
- Do not move public report DTOs or local tests.
- Do not introduce release-transition cross-child links.

## Allowed Rust Movement For Next Step

BE-002KM-02 may:

1. add `mod edge_parser;`;
2. create `quantscript/src/v4_static_audit/edge_parser.rs`;
3. move the `parse_edge` implementation body;
4. keep the parent private wrapper named `parse_edge`;
5. adjust imports and `pub(super)` visibility only where required by this
   extraction.

## Gates For Actual Extraction

| Gate | Command |
| --- | --- |
| format | `cargo fmt --all --check` |
| crate tests | `cargo test -p quantscript` |
| workspace compile | `cargo check -p quantpilot` |
| diff hygiene | `git diff --check` |

## Next Step

BE-002KM-02 `root.contracts.quantscript.v4_static_audit.edge_parser`
actual_extraction.
