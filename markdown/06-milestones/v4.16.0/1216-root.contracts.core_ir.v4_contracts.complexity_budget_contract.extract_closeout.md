# v4.16.0 root.contracts.core_ir.v4_contracts.complexity_budget_contract actual extraction complete

> Batch: BE-001VS-02
> Node: `root.contracts.core_ir.v4_contracts.complexity_budget_contract`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `extract_closeout`
> Movement: Code movement completed.

---

## Summary

`root.contracts.core_ir.v4_contracts.complexity_budget_contract` actual extraction is complete.

Implemented movement:

- `qrpc_core_ir/src/v4.rs` now declares `mod complexity_budget_contract;`
- `qrpc_core_ir/src/v4.rs` re-exports the child through `pub use complexity_budget_contract::*;`
- `qrpc_core_ir/src/v4/complexity_budget_contract.rs` owns complexity budget DTOs, complexity metrics DTOs, budget
  defaults, static budget validation, metric validation, and graph-derived metrics calculation
- the child uses machine graph contract/traversal helpers as inputs without moving machine graph schema or validation

Equivalence notes:

- public complexity budget and metrics surface remains unchanged through the parent re-export
- default budgets, zero-budget validation, over-budget validation, graph metric counting, nested-depth handling, event
  rate estimate, order path estimate, and stale dependency default remain frozen from BE-001VS-01
- compile-time capability report DTOs, compatibility bridge reports, static bundles, developer learning pipeline,
  reproducibility, plugin governance, tests, machine contract schema, machine graph schema, QS contracts, runtime mode,
  venue capability matrix, and version manifest remain outside this child

Verification:

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir` (54 passed)
- `cargo check -p quantpilot`

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs,qrpc_core_ir/src/v4/complexity_budget_contract.rs`

**Markers**:
- `BE-001VS-02`

**Next step**:
BE-001VS-03 root.contracts.core_ir.v4_contracts.complexity_budget_contract single_leaf_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
