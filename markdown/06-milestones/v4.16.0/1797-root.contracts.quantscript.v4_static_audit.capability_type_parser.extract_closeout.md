# v4.16.0 root.contracts.quantscript.v4_static_audit.capability_type_parser extract closeout

> Batch: BE-002JW-02
> Node: `root.contracts.quantscript.v4_static_audit.capability_type_parser`
> Stage: `actual_extraction`
> Movement: Rust code movement complete.
> governance_next_authority: active

---

## Extraction Result

`quantscript/src/v4_static_audit/capability_type_parser.rs` now owns the pure
runtime/capability/type parser cluster.

The parent `quantscript/src/v4_static_audit.rs` keeps thin private wrappers for
`parse_runtime_mode`, `parse_execution_capability`, and `parse_qs_type_ref` so
existing parent parser callers stay stable while the implementation moves to
the child module.

## Boundary Preservation

| Boundary | Result |
| --- | --- |
| Public API | Unchanged; no public function or DTO moved |
| Parent callers | `parse_v4_static_document` and `parse_memory` continue calling the same parent helper names |
| Runtime mode parser | Moved to child with same accepted literals and unknown-mode error behavior |
| Execution capability parser | Moved to child with same aliases and unknown-capability error behavior |
| QS type parser | Moved to child with same recursive wrapper, list, map, max, and scalar behavior |
| Machine template parser | Remains parent-owned for future machine parser extraction |
| Parser utilities | `prepare_lines`, `split_words`, `split_csv_words`, and `diag` remain parent-owned |
| Closed children | `runtime_handoff_builder` and `audit_entrypoint` unchanged |

## Verification

Completed before governance packaging:

- `cargo fmt --all --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`

Required before commit:

- `git diff --check`
- `tools/check-utf8.ps1`
- `tools/check-full-feature-tree.ps1`
- `tools/check-matrix-governance.ps1`
- `recursive-state.json` JSON parse

## Next Step

BE-002JW-03 `root.contracts.quantscript.v4_static_audit.capability_type_parser`
single_leaf_closeout.
