# v4.16.0 data_module.quality_diagnostics extract closeout

> Batch: BE-001ZC-02
> Node: `root.contracts.runtime_support.data_module.quality_diagnostics`
> Parent: `root.contracts.runtime_support.data_module`
> Stage: `actual_extraction`
> Movement: Code movement complete.

## Extracted Files

- `qrpc_runtime/src/data_module/quality_diagnostics.rs`

## Code Movement

The normalized market data quality surface moved out of `qrpc_runtime/src/data_module.rs` into a child module:

- `MarketDataPreview`
- `data_timestamp_ms`
- `stale_after_ms_for_source`
- `gap_count_for_data`
- `build_quality_flags`
- `source_health_from_flags`
- `build_data_quality_snapshot`
- `attach_data_quality_snapshot`
- `market_data_quality`
- `build_data_quality_summary`
- `apply_snapshot_status`
- `market_data_preview`

The parent now declares `mod quality_diagnostics;`, keeps parent-visible quality helpers available through `pub(crate) use`, and imports `apply_snapshot_status` for cache flow.

## Public API Equivalence

No crate-visible call site changed.

Quality helpers remain available through the parent `data_module` boundary:

- `attach_data_quality_snapshot`
- `market_data_quality`
- `build_data_quality_summary`
- `market_data_preview`
- `MarketDataPreview`

## Boundary Result

The child owns normalized market data quality snapshots, health flags, preview projection, summary rendering, and cached snapshot status refresh.

The parent still owns:

- provider trait and `BuiltinDataModule`
- collection orchestration
- exchange endpoint and payload parsing
- normalization
- mock data and historical cache support
- data-module tests

## Equivalence Gates

Required gates for this batch:

- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-001ZC-03 `root.contracts.runtime_support.data_module.quality_diagnostics` single_leaf_closeout.
