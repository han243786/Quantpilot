# v4.16.0 root.contracts.quantscript.runtime_lowering.manual_formula_fallback parent residual judgment: manual_zscore_formula

> Batch: BE-002JL-01
> Node: `root.contracts.quantscript.runtime_lowering.manual_formula_fallback`
> Selected child: `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_zscore_formula`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `manual_zscore_formula` as the next internal child under
`manual_formula_fallback`.

`manual_rsi_formula`, `manual_momentum_formula`, and `manual_macd_formula` are
closed. The next cohesive owner is ZScore manual fallback recognition: direct
resolved ZScore formula handling plus legacy operand recovery both resolve to
the same source/window semantics and `IndicatorBinding::ZScore` output.
Extracting this child next preserves the divide-branch recognizer order before
moving-average fallback is touched.

## White-Box Boundary

| Field | Boundary |
| --- | --- |
| Input | candidate expression, divide operands, `BindingEnv`, and data sources |
| Output | `IndicatorBinding::ZScore`, `SourceSpanMatch`, or no-match |
| Current owner | `quantscript/src/lowering/fallback.rs` |
| Planned owner | `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_zscore_formula` |
| Parent caller | `resolve_manual_formula_binding` divide branch and direct helper users |
| Exclusions | moving-average/window fallback, boundary lookback public helper, fallback tests, closed RSI/momentum/MACD |

## Symbols Approved For Next Baseline

The next baseline may freeze movement for:

- `manual_zscore_from_expr`
- `match_zscore_operands`
- `legacy_zscore_operands`

## Split Decision Gate

| Gate | Result | Evidence |
| --- | --- | --- |
| `parent_precision_open` | PASS | `manual_formula_fallback` remains a precision parent after RSI, momentum, and MACD closeout |
| `queue_order` | PASS | Parent baseline queue lists `manual_zscore_formula` after `manual_macd_formula` |
| `recognizer_order_preserved` | PASS | ZScore remains before momentum-ratio and moving-average fallback in divide branches |
| `strong_owner_boundary` | PASS | Selected symbols produce ZScore binding or source/window match |
| `sibling_exclusion_clear` | PASS | MA/window, boundary lookback helper, tests, and closed children are excluded |

## Risk Classification

| Risk | Result |
| --- | --- |
| Public API / route / schema | Public(crate) ZScore helpers must remain stable |
| Persistence / lock owner | No persistence ownership movement |
| Trading semantics evidence | High; ZScore fallback affects inferred runtime indicators |
| Cross-crate behavior | Present through `IndicatorBinding`, `DataSourceConfig`, and typed expression contracts |

## Next Step

BE-002JM-01 `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_zscore_formula` baseline_plan.
