# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation.event_party_validation single leaf closeout stops split

> Batch: BE-001UO-03
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation.event_party_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation.event_party_validation`
stops splitting.

The leaf now owns one coherent event-routing permission helper:
`V4MachineGraphContract::validate_event_parties`. Further splitting would create small
transition/action/edge helper fragments and increase parameter passing without a stronger boundary.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | The file is a named white-box child for event party permission checks. |
| parent_child_communication_kept | pass | It receives resolved parent data and calls only the parent helper `machine_event_party_allowed`. |
| equivalence_baseline_freezable | pass | `cargo test -p qrpc-core-ir` passed after extraction with 54 tests passed and 0 failed. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | no | The public validator remains higher in the graph validation parent. |
| state_machine_phase | pass | The helper is a complete event-routing permission phase. |
| strategy_branch | guarded | It protects transition, action, and edge event semantics. |
| independent_failure_mode | pass | Permission mismatch failures are independent from event reference resolution. |
| reuse_pressure | no | There is no second caller requiring smaller helper fragments. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | yes | Smaller slices would be private branch helpers. |
| communication_cost_rises | yes | Sub-splitting would pass maps, machine slices, and error accumulation across more helpers. |
| local_proof_missing | no | Existing public-validator tests cover this leaf. |
| line_count_only | no | Stop decision is based on ownership and communication cost, not line count. |

leaf_split_decision_result

`stop_split: true`

Next parent residual candidate:

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation.event_reference_resolution`

next_recursive_step

BE-001UP-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation parent_residual_judgment selects event_reference_resolution
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/event_usage_validation/event_party_validation.rs`

**Markers**:
- `BE-001UO-03`

**Next step**:
BE-001UP-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation parent_residual_judgment selects event_reference_resolution

---

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
