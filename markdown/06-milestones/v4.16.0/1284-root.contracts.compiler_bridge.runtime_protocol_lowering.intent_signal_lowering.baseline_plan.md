# v4.16.0 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering equivalence baseline and extraction plan

> Batch: BE-001XJ-01
> Node: `root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering`
> Parent: `root.contracts.compiler_bridge.runtime_protocol_lowering`
> Stage: `baseline_plan`
> Movement: no Rust code movement.

---

## Summary

`root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering` is frozen before physical Rust movement.

This child owns runtime intent conversion into Core IR indicator nodes and signal rules. It also owns structured condition lowering, runtime comparison metadata decoding, QuoteObserve spread-threshold condition lowering, fallback raw-text condition descriptions, and runtime intent kind mapping.

Decision:

`baseline_frozen: true`

Next movement:

`BE-001XK-01` may create a child module under `qrpc_compiler/src/runtime_protocol_lowering/` or an equivalent private child file for this intent/signal pocket. Parent `runtime_protocol_lowering` must retain orchestration, data binding, agent policy, risk policy, public compile/lower facade delegation, validation, Strategy IR lowering, shared spread/custom helpers, tests, and release-transition guard.

## Allowed Movement Set

The extraction step may move these symbols as one child:

| Symbol | Responsibility |
| --- | --- |
| `lower_runtime_intent_to_indicator` | Convert `IntentConfig` into `IndicatorNode`, including spread detection and runtime intent variant params. |
| `lower_runtime_intent_to_signal_rule` | Convert runtime intents into Core IR `SignalRule` entries and fallback raw text. |
| `lower_runtime_intent_condition` | Build structured runtime MA/RSI/Momentum/ZScore/QuoteObserve condition expressions. |
| `describe_runtime_intent_condition` | Produce fallback raw-text descriptions for runtime intent conditions. |
| `lower_runtime_spread_threshold_condition` | Build structured QuoteObserve spread threshold comparison conditions. |
| `decode_runtime_comparison_op` | Decode numeric runtime comparison metadata into Core IR comparison ops. |
| `comparison_op_code` | Encode default comparison ops for runtime metadata fallback. |
| `RuntimeComparisonShape` | Runtime buy/sell comparison shape enum. |
| `decode_runtime_comparison_shape` | Decode numeric runtime buy/sell shape metadata. |
| `lower_runtime_intent_kind` | Convert runtime `IntentKind` into Core IR indicator kind. |

## Parent Calls Frozen

After extraction, parent `runtime_protocol_lowering` may call the child only through explicit private functions from its orchestration path:

- indicator collection must still use enabled runtime intents in original order;
- signal rule collection must still use enabled runtime intents in original order;
- agent and risk policy lowering must remain in parent or later dedicated children;
- runtime spread spec helpers may remain parent-owned and be passed/called through parent-mediated access only.

## Equivalence Baseline

BE-001XK-01 must preserve:

- runtime intent filtering and output order;
- MA/SMA structured moving-average compare output;
- RSI, Momentum, and ZScore one-sided structured threshold output;
- QuoteObserve spread threshold structured compare output;
- fallback raw-text condition strings for non-structured branches;
- comparison op and comparison shape numeric decoding semantics;
- runtime intent kind mapping to Core IR indicator kind;
- parent public function signatures and Core IR output shape.

## Hard Exclusions

This baseline must not:

- move `lower_runtime_protocol_to_core_ir_with_metadata`;
- move data binding source hint lowering;
- move `lower_runtime_agent_to_policy`;
- move `lower_runtime_risk_to_policy`;
- move `lower_runtime_rebalance_schedule_to_core_ir`;
- move `lower_runtime_spread_spec`, `runtime_intent_is_spread`, or shared spread/custom helpers unless needed through parent-mediated visibility;
- change validation behavior, canonical hash behavior, Strategy IR lowering, tests, or release-transition rules.

## Boundary

**Real files**:
- `qrpc_compiler/src/runtime_protocol_lowering.rs`

**Markers**:
- `BE-001XJ-01`
- `intent_signal_lowering baseline_frozen: true`
- `agent risk orchestration excluded`
- `no Rust code movement`

**Next step**:
BE-001XK-01 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering extract_closeout

---

## Gates

- `cargo test -p qrpc-compiler`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
