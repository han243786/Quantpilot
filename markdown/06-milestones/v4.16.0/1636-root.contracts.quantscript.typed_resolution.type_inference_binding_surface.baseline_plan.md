# v4.16.0 root.contracts.quantscript.typed_resolution.type_inference_binding_surface baseline plan

> Batch: BE-002GJ-01
> Node: `root.contracts.quantscript.typed_resolution.type_inference_binding_surface`
> Parent: `root.contracts.quantscript.typed_resolution`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Scope

Freeze type inference, binding insertion, condition validation, and type annotation resolution before movement.

Current owner:

- `quantscript/src/resolve/mod.rs`

Planned child:

- `quantscript/src/resolve/type_inference_binding_surface.rs`

## Items Frozen For Movement

The next extraction may move only:

- `infer_call_type`
- `infer_member_capability_type`
- `infer_named_helper_return_type`
- `infer_unary_type`
- `infer_binary_type`
- `common_item_type`
- `index_result_type`
- `sequence_item_type`
- `numeric_sequence_type`
- `iteration_item_type`
- `unwrap_maybe`
- `is_numeric`
- `insert_binding`
- `validate_condition_type`
- `resolve_type`

## Parent Wiring To Preserve

The parent resolver module must keep:

- `lower_script_to_typed_hir` public entrypoint unchanged;
- resolver orchestration and AST-to-HIR lowering behavior unchanged;
- calls from `lower_expr`, `lower_stmt`, and parameter/binding lowering to the moved helpers unchanged;
- diagnostics emitted by binding insertion and condition validation unchanged;
- type arena mutation semantics unchanged;
- downstream `crate::resolve::*` compatibility unchanged.

## Forbidden Movement

The next extraction must not move or change:

- public resolver DTOs/enums already closed in `public_type_surface`;
- callable/helper classifiers and registry seeding already closed in `callable_classification_surface`;
- semantic/manual indicator inference already closed in `semantic_inference_surface`;
- `resolve_module`, `seed_imported_callables`, `seed_function_signatures`, `resolve_function`, `lower_block`, `lower_stmt`, `lower_match_arm`, `lower_expr`, and `lower_callee_expr`;
- `make_expr`, `expr_label`, ID allocation, callable target helpers, scalar expression helpers, resolved function builder, return-list helpers, and resolver-local tests;
- analysis, lowering, evaluator, runtime, backend, executor, frontend, plugin metadata, or release transition.

## Expected Validation

The extraction must pass:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002GJ-02 `root.contracts.quantscript.typed_resolution.type_inference_binding_surface` actual_extraction.
