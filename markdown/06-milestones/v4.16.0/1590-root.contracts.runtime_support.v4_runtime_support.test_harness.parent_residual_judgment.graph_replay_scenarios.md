# v4.16.0 root.contracts.runtime_support.v4_runtime_support.test_harness parent residual judgment - graph_replay_scenarios

> Batch: BE-002FF-01
> Node: `root.contracts.runtime_support.v4_runtime_support.test_harness`
> Selected child: `root.contracts.runtime_support.v4_runtime_support.test_harness.graph_replay_scenarios`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `root.contracts.runtime_support.v4_runtime_support.test_harness.graph_replay_scenarios` as the next internal test-harness child.

## Evidence

After closing `fixture_builders` and `payload_validation_tests`, the test harness still has 27 parent-local tests and stays above the terminal target size. The graph/replay scenario cluster is the next clean same-parent child because it verifies graph expansion, nested state-machine behavior, deterministic bar replay, and market tick snapshot recording without owning order lifecycle, live execution mode, capability policy, or recovery-cache behavior.

The candidate owns:

- `market_price_tick_helper_records_market_snapshot`
- `v4_nested_machine_parent_transition_wins_over_child_transition`
- `v4_nested_machine_memory_is_isolated_and_visible_in_snapshot`
- `v4_backtest_bar_replay_is_deterministic`
- `v4_multi_symbol_expansion_creates_independent_machine_instances`

## Boundary

The next baseline must freeze only graph/replay scenario test movement. It must not move:

- simulated execution order lifecycle tests;
- tick microstructure replay tests that sit with execution metrics;
- runtime recovery/cache snapshot tests;
- live/capability/risk guard tests;
- fixture builders;
- malformed payload/config rejection tests;
- production v4 runtime behavior;
- release-transition optimization.

## Expected Movement Shape

The next movement should be test-only:

- add `qrpc_runtime/src/v4_runtime/test_harness/graph_replay_scenarios.rs`;
- add `mod graph_replay_scenarios;` in `qrpc_runtime/src/v4_runtime/test_harness.rs`;
- move only the five selected `#[test]` functions;
- preserve parent-mediated test access with `use super::*` inside the child.

## Next Step

BE-002FG-01 `root.contracts.runtime_support.v4_runtime_support.test_harness.graph_replay_scenarios` baseline_plan.
