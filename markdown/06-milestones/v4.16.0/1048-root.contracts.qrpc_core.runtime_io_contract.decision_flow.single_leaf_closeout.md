# v4.16.0 root.contracts.qrpc_core.runtime_io_contract.decision_flow single leaf closeout

> Batch: BE-001SF-03
> Node: `root.contracts.qrpc_core.runtime_io_contract.decision_flow`
> Parent: `root.contracts.qrpc_core.runtime_io_contract`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core.runtime_io_contract.decision_flow` has been evaluated after BE-001SF-02 extraction.

Decision:

`stop_split: true`

The node is now a compact decision flow DTO child module with one responsibility: intent/action/target/agent/risk decision data contracts.

## Split Rule Evaluation

| Rule | Result |
| --- | --- |
| Physical owner | Stop. The selected surface is isolated in `qrpc_core/src/runtime_io_contract/decision_flow.rs`. |
| Public method count | Stop. The child exposes schema structs only; there are no public behavior methods to split. |
| Mixed responsibility | Stop. Intent, proposed action, target, agent decision, and risk decision DTOs form one coherent decision flow family. |
| State ownership | Stop. The child owns no runtime mutable state. |
| Side effects | Stop. No IO, persistence, locks, async lifecycle, external calls, or public API mutation are involved. |
| Parent-mediated dependency | Covered. The runtime IO parent re-exports the child, and later runtime IO siblings consume decision DTOs through the parent facade. |
| Future reopen rule | Allowed only when a concrete decision schema, target/action semantics, serde shape, or public facade proposal is made. |

## White-Box Boundary

| Input | Processing owner | Output |
| --- | --- | --- |
| Runtime intent/action/target/agent/risk decision schema proposals | `contracts.qrpc_core.runtime_io_contract.decision_flow` | Stable decision flow DTO family |

The leaf may describe and guard:

- intent signal shape;
- proposed action shape;
- portfolio target and target decision shape;
- agent decision shape;
- risk decision shape.

## Rust Local Fields

| Field | Result |
| --- | --- |
| Crate | `qrpc-core` |
| Child visibility | Private module; public items are re-exported by the runtime IO parent. |
| Public exports | Preserved through `pub use decision_flow::*;` and crate-root `pub use runtime_io_contract::*;`. |
| Parent facade | `qrpc_core/src/runtime_io_contract.rs` remains the local coordinator. |
| Production dependency check | Passed. The child imports closed qrpc-core public names through the crate root and does not import sibling internals directly. |

## Non-Claims

This closeout does not claim:

- execution IO, portfolio state, runtime output, or backtest output have been extracted;
- runtime IO parent residual judgment is complete;
- RFC execution contracts, crate-root tests, qrpc-core parent residuals, or downstream behavior changed;
- closed qrpc-core children or release transition changed.

## Proof

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-001SG-01 `root.contracts.qrpc_core.runtime_io_contract` parent_residual_judgment selects `execution_io`.
