# v4.16.0 root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.rebalance_lowering_tests extract closeout

> Batch: BE-002IG-02
> Node: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.rebalance_lowering_tests`
> Parent: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness`
> Stage: `actual_extraction`
> Movement: Test-only Rust movement.
> governance_next_authority: active

---

## Extraction Summary

`rebalance_lowering_tests` has been extracted into:

- `quantscript/src/lowering/orchestrator/integration_test_harness/rebalance_lowering_tests.rs`

The parent test harness now declares `mod rebalance_lowering_tests;` and no longer directly owns the portfolio rebalance helper lowering tests.

## Symbols Moved

- `lowers_equal_weight_rebalance_helper_into_portfolio_rebalance_agent`
- `lowers_fixed_weights_rebalance_helper_into_portfolio_rebalance_agent`
- `lowers_rank_weight_rebalance_helper_into_portfolio_rebalance_agent`
- `lowers_score_weight_rebalance_helper_into_portfolio_rebalance_agent`
- `rejects_rebalance_helper_with_unsupported_frequency`
- `rejects_rank_weight_with_unsupported_method`
- `lowers_weekly_rebalance_schedule_into_agent_schedule`

Moved child-only imports:

- `qrpc_core::{RebalanceSchedule, Symbol}`

## Parent Boundary Kept

The parent remains owner for:

- basic MA / RSI / alias smoke tests
- runtime coordinator smoke
- user-defined helper function smoke
- semantic missing-helper rejection test
- exchange-only data-source decoding test
- child module declarations

The parent production owner remains `quantscript/src/lowering/orchestrator.rs`; no production lowering code moved.

## Validation

Passed after extraction:

- `cargo fmt --check`
- `cargo test -p quantscript` (88 tests)
- `cargo check -p quantpilot`
- `git diff --check`

## Next Step

BE-002IG-03 single_leaf_closeout.
