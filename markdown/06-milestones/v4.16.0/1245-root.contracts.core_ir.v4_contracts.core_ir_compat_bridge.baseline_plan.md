# v4.16.0 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge equivalence baseline and extraction plan

> Batch: BE-001WI-01
> Node: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `baseline_plan`
> Movement: No code movement. This document freezes the equivalence baseline before extraction.

---

## Summary

root.contracts.core_ir.v4_contracts.core_ir_compat_bridge equivalence baseline and extraction plan

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs`

**Markers**:
- `BE-001WI-01`

**Next step**:
BE-001WI-02 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge extract_closeout

---

## Frozen White-Box Surface

This child owns the Core IR to v4 machine-graph compatibility bridge:

- `CoreIrV4CompatibilityReport`
- `CoreIrV4BridgeVerdict`
- `CoreIrV4BridgeDiagnosticSeverity`
- `CoreIrV4BridgeDiagnostic`
- `CoreIrV4CompatibilityReport::validate_for_phase4`
- `bridge_core_ir_to_v4_machine_graph`
- `validate_core_ir_for_v4_bridge`
- `collect_core_ir_node_ids`
- `validate_core_ir_edges_for_v4_bridge`
- `validate_core_ir_references_for_v4_bridge`
- `validate_core_ir_scalar_expr_refs`
- `validate_core_ir_series_expr_refs`
- `validate_core_ir_custom_value_refs`
- `build_core_ir_compat_machine_graph`
- `build_core_ir_observation_machine`
- `build_core_ir_decision_machine`
- `build_core_ir_execution_machine`
- `build_core_ir_compat_event_catalog`
- `push_core_ir_v4_bridge_diagnostic`
- `default_core_ir_compat_bridge_version`
- bridge-builder defaults currently used only by this bridge path:
  `default_machine_contract_version`, `default_machine_graph_contract_version`,
  `default_machine_event_catalog_version`, `default_transition_conflict_policy`,
  `default_machine_graph_edge_activation`, and `default_risk_plane_min_priority`

## Forbidden Movement

BE-001WI-02 must not move these surfaces:

- already extracted sibling v4 contract families
- compile-time capability report files
- static bundle contract files
- tests and sample builders still hosted in `qrpc_core_ir/src/v4.rs`
- crate-level v1/Core IR definitions

## Allowed Extraction

BE-001WI-02 may create `qrpc_core_ir/src/v4/core_ir_compat_bridge.rs`, add a private module declaration plus public re-export in `qrpc_core_ir/src/v4.rs`, and move only the frozen bridge DTOs, validation helpers, bridge builders, bridge diagnostics, and bridge-specific default helpers listed above.

## Equivalence Baseline

The extraction is equivalent only if the public v4 bridge API remains reachable through `qrpc_core_ir::v4::*` and these gates pass:

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `cargo check -p quantpilot`

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
