# v4.16.0 root.contracts.runtime_support.v4_runtime_support.runtime_constructor_mode_gate single leaf closeout

> Batch: BE-002DQ-02
> Node: `root.contracts.runtime_support.v4_runtime_support.runtime_constructor_mode_gate`
> Parent: `root.contracts.runtime_support.v4_runtime_support`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Decision

`stop_split: true`

The v4 runtime constructor/mode-gate child is terminal for the current recursive pass.

## Terminal Leaf Control

`tools/evaluate-leaf-granularity.ps1 -Path qrpc_runtime/src/v4_runtime/runtime_constructor_mode_gate.rs` result:

- Decision: `STOP`
- Score: `33`
- LOC: `145`
- Functions: `7`
- Public surface: `6`
- Branches: `7`
- Domains: `5`
- Risk tags: `cross_crate`, `public_api`, `state_machine`, `trading_semantics`
- Governance mode: `stop_split`

## Evidence

The extracted child owns one coherent admission responsibility:

- accepting a v4 graph into `PaperSimulated` or `LiveActual` runtime mode;
- rejecting unsupported runtime modes;
- enforcing LiveActual risk-plane and execution-policy preconditions;
- validating the static v4 graph before runtime state creation;
- initializing machine family state, risk-plane state, execution state, simulated execution state, queue/log, sequence, and provider-submit state;
- attaching validated execution capability policy.

Further splitting `new*` constructors from `with_execution_capabilities` would separate the LiveActual precondition from the policy attachment that satisfies it. Splitting initial state construction would create a high-risk micro leaf with no stronger owner and extra parent/private-state coupling.

## Verified Gates

Inherited from BE-002DQ-01:

- `cargo fmt --check`: PASS
- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime v4 -- --nocapture`: PASS, 32 passed
- `cargo check -p quantpilot`: PASS
- `git diff --check`: PASS
- UTF-8 check: PASS
- full feature tree check: PASS
- matrix governance check: PASS

## Next Step

BE-002DR-01 `root.contracts.runtime_support.v4_runtime_support` parent_residual_judgment.
