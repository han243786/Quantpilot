# v4.16.0 root.contracts.quantscript.runtime_lowering.intent_inference parent residual judgment: spread_intent_inference

> Batch: BE-002JA-01
> Node: `root.contracts.quantscript.runtime_lowering.intent_inference`
> Selected child: `root.contracts.quantscript.runtime_lowering.intent_inference.spread_intent_inference`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `spread_intent_inference` as the next child under `intent_inference`.

`intent_collection_orchestration` and `single_indicator_intent_inference` are closed. The largest remaining cohesive owner is the formal spread condition builder: it recognizes supported `spread(...)` expressions, decodes spread operands, validates alignment/resample semantics, and builds `QuoteObserve` intent params. This owner is distinct from MA ratio intent construction, legacy emit fallback, agent param projection, data-source canonicalization, and relation/id helper ownership.

## White-Box Boundary

| Field | Boundary |
| --- | --- |
| Input | indicator expression, binary relation, threshold expression, `BindingEnv`, and optional runtime id hint |
| Output | optional `IntentConfig` or `anyhow::Error` |
| Current owner | `quantscript/src/lowering/intents.rs` |
| Planned owner | `root.contracts.quantscript.runtime_lowering.intent_inference.spread_intent_inference` |
| Parent caller | `intents_from_condition` |
| Exclusions | MA ratio inference, legacy emit fallback, agent param projection, data-source canonicalization implementation, comparison/id helpers, and public(crate) facades |

## Symbols Approved For Next Baseline

The next baseline may freeze movement for:

- `SpreadSeriesOperand`
- `SpreadOutputKind`
- `SpreadMatch`
- `spread_intent_from_condition`
- `match_formal_admitted_spread_expr`
- `decode_spread_operand`
- `decode_spread_operand_or_source`
- `decode_explicit_spread_operand`
- `decode_formal_admitted_spread_operand`
- `default_spread_operand`
- `parse_spread_output_kind`
- `parse_field_code`
- `parse_aggregation_code`
- `parse_align_direction_code`
- `parse_resample_period_ms`
- `merge_optional_f64`
- `spread_params`
- `inferred_resample_period_ms`
- `interval_to_ms`

The moved child may call parent-owned data-source canonicalization, comparison helpers, runtime id helpers, and shared expression/binding helpers through the parent module boundary.

## Split Decision Gate

| Gate | Result | Evidence |
| --- | --- | --- |
| `parent_still_oversized` | PASS | `intent_inference` remains 1049 LOC with 39 functions after the first two child closeouts. |
| `strong_owner_boundary` | PASS | Spread matching, operand decoding, alignment/resample parsing, and params construction form one cohesive spread intent owner. |
| `parent_facade_preserved` | PASS | Parent can keep `intents_from_condition` as dispatcher and call the child. |
| `sibling_exclusion_clear` | PASS | MA ratio, legacy emit, agent params, data-source canonicalization, and relation/id helper bodies remain outside. |

## Leaf Granularity Evidence

Command:

- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -LeafId root.contracts.quantscript.runtime_lowering.intent_inference -ParentId root.contracts.quantscript.runtime_lowering -Path quantscript/src/lowering/intents.rs -Depth 4`

Result:

| Field | Value |
| --- | --- |
| `split_decision` | `CONTINUE` |
| `governance_packaging` | `precision_single_leaf` |
| `final_decision` | `PRECISION` |
| `normalized_split_score` | 56 |
| `loc` | 1049 |
| `function_count` | 39 |
| `public_surface` | 3 |
| `branches` | 91 |
| `risk_tags` | `cross_crate`, `public_api`, `security`, `trading_semantics` |
| `reason` | oversized high-risk leaf requires precision baseline before movement |

## Risk Classification

| Risk | Result |
| --- | --- |
| Public API / route / schema | Parent-visible intent inference signatures must remain stable |
| Persistence / lock owner | No persistence ownership movement |
| Trading semantics evidence | High; spread params and `QuoteObserve` output shape affect strategy behavior |
| Cross-crate behavior | Present through `IntentConfig`, `IntentKind`, data-source configs, and binding contracts |

## Next Step

BE-002JB-01 `root.contracts.quantscript.runtime_lowering.intent_inference.spread_intent_inference` baseline_plan.
