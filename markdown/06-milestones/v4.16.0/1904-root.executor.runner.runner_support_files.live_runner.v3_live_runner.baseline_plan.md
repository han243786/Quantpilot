# v4.16.0 root.executor.runner.runner_support_files.live_runner.v3_live_runner baseline plan

> Batch: BE-002MF-01
> Node: `root.executor.runner.runner_support_files.live_runner.v3_live_runner`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Goal

Freeze the V3 live runner boundary before Rust movement.

The next movement may extract the V3 runner group from
`src-executor/live_runner.rs` into:

`src-executor/live_runner/v3_live_runner.rs`

## Frozen Boundary

| Field | Value |
| --- | --- |
| Planned module | `src-executor/live_runner/v3_live_runner.rs` |
| Approved public surface | `LiveRunner`, `RunnerStatus` |
| Approved impl surface | `from_strategy`, `handle_ws_event`, `run_fast_cycle`, `run_slow_cycle`, `detect_trigger`, `is_subscribed_to` |
| Parent dependencies | `ActiveStrategy`, `ExecutionMode`, `TriggerEvent`, `KlinePool`, `WsEvent`, runtime coordinator/event types |
| Parent retained | `RunnerInstance`, `RunnerPool`, `V4Runner`, V4 helpers, and local tests |

## Equivalent Behavior

The extracted child must preserve:

- V3 initial status behavior for provider-attached and simulated modes;
- ticker/kline event filtering by subscribed symbols;
- fast cycle interval guard;
- slow cycle daily order count guard;
- trigger projection for intent, agent decision, and execution fill events.

## Exclusions

- Do not move `RunnerInstance`, `RunnerPool`, `V4Runner`, or V4 helper functions.
- Do not move tests in this step.
- Do not alter websocket event schema or kline buffer behavior.
- Do not introduce release-transition links.

## Gates For Actual Extraction

| Gate | Command |
| --- | --- |
| format | `cargo fmt --all --check` |
| executor compile | `cargo check -p quantpilot --bin executor` |
| focused tests | `cargo test -p quantpilot --bin executor live_runner` |
| diff hygiene | `git diff --check` |

## Next Step

BE-002MF-02
`root.executor.runner.runner_support_files.live_runner.v3_live_runner`
actual_extraction.
