# v4.16.0 root.contracts.quantscript.v4_static_audit.edge_parser extract closeout

> Batch: BE-002KM-02
> Node: `root.contracts.quantscript.v4_static_audit.edge_parser`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> governance_next_authority: active

---

## Movement

`parse_edge` has been extracted from `quantscript/src/v4_static_audit.rs`
into:

`quantscript/src/v4_static_audit/edge_parser.rs`

The parent keeps a private `parse_edge` wrapper so `static_document_parser`
continues to call through the parent boundary.

## Ownership Now

| Field | Value |
| --- | --- |
| Child file | `quantscript/src/v4_static_audit/edge_parser.rs` |
| Owned function | `parse_edge` |
| Parent wrapper | `parse_edge` |
| Child imports | `diag`, `split_words`, `BTreeMap` |
| Owned output | `MachineGraphEdge` |

## Preserved Behavior

- edge syntax validation stays on `QSV4120`;
- edge id construction is unchanged;
- source, target, and event extraction are unchanged;
- activation, required flag, and metadata defaults are unchanged;
- risk-plane parsing remains outside.

## Gates

Minimum extraction gates were run before commit.

## Next Step

BE-002KM-03 `root.contracts.quantscript.v4_static_audit.edge_parser`
single_leaf_closeout.
