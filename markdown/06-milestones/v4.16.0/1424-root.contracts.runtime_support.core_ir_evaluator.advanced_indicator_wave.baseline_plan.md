# v4.16.0 root.contracts.runtime_support.core_ir_evaluator.advanced_indicator_wave baseline plan

> Batch: BE-002AM-01
> Node: `root.contracts.runtime_support.core_ir_evaluator.advanced_indicator_wave`
> Parent: `root.contracts.runtime_support.core_ir_evaluator`
> Stage: `baseline_plan`
> Movement: No Rust code movement.

---

## Scope

This baseline freezes the advanced indicator wave before physical extraction.

Current owner file:

- `qrpc_runtime/src/core_ir_evaluator.rs`

Planned child file:

- `qrpc_runtime/src/core_ir_evaluator/advanced_indicator_wave.rs`

## Moved Surface

The next movement may move only these evaluator entrypoints:

- `evaluate_atr`
- `evaluate_bollinger_bands`
- `evaluate_obv`
- `evaluate_cmf`
- `evaluate_adx`
- `evaluate_stochastic`
- `evaluate_cci`
- `evaluate_parabolic_sar`
- `evaluate_keltner_channel`
- `evaluate_donchian_channel`

The parent registry may import these parent-visible entrypoints from the child module.

## Preserved Parent Ownership

The parent must keep:

- public evaluator DTOs, error enum, trait, registry, and `evaluate_indicator_signal`;
- closed `classic_indicator_wave`;
- Spread and Custom evaluator entrypoints;
- typed series expression machinery;
- shared lookup/math helper formulas such as `average_true_range`, `bollinger_bands`, `on_balance_volume`, `chaikin_money_flow`, `average_directional_index`, `stochastic_oscillator`, `commodity_channel_index`, `parabolic_sar`, `keltner_channel`, and `donchian_channel`;
- local tests until the dedicated test-harness step.

## White-Box Contract

The child may call parent-owned private helpers through `super::*`.

The child must expose only parent-visible evaluator entrypoints as `pub(super)` functions. Public API remains unchanged.

## Split Decision

`continue_split: true`

Reason:

- these ten functions are direct builtin registry targets and form one stable advanced-indicator wave;
- extracting them removes a dense block from the parent without single-indicator governance churn;
- keeping formula helpers parent-owned avoids premature helper movement before shared-helper residual judgment.

## Equivalence Requirements

The extraction must preserve:

- registry keys and evaluator order;
- output fields, derived metrics, reason strings, TTLs, and error behavior;
- helper ownership and parent-mediated access;
- local tests covering advanced indicators.

## Gates

Required for the next movement:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime core_ir_evaluator`
- `cargo check -p quantpilot`
- `git diff --check`
- `tools/check-utf8.ps1`
- `tools/check-full-feature-tree.ps1`
- `tools/check-matrix-governance.ps1`

## Next Step

BE-002AM-02 `root.contracts.runtime_support.core_ir_evaluator.advanced_indicator_wave` actual_extraction.
