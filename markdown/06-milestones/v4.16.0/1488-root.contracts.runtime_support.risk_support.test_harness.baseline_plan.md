# v4.16.0 root.contracts.runtime_support.risk_support.test_harness baseline plan

> Batch: BE-002CG-01
> Node: `root.contracts.runtime_support.risk_support.test_harness`
> Parent: `root.contracts.runtime_support.risk_support`
> Stage: `baseline_plan`
> Movement: No Rust code movement.

---

## Scope

Freeze the baseline for extracting the local risk-checker test harness from `qrpc_runtime/src/risk_checker.rs`.

Allowed movement:

- add `qrpc_runtime/src/risk_checker/test_harness.rs`;
- add a parent test-only child declaration with `#[cfg(test)] mod test_harness;`;
- move only the current `#[cfg(test)] mod tests` contents into the new child;
- keep tests parent-mediated through `use super::*`.

## In-Scope Test Surface

- `sample_core_ir_with_risk`
- portfolio and decision fixtures inside the current test module;
- risk checker interval/inventory/symbol behavior tests;
- portfolio-target clamp behavior tests;
- action-list exposure behavior tests;
- risk event payload behavior test.

## Out of Scope

- public provider/evaluate surface;
- `evaluate_risk_decision`;
- `enforce_risk_mode`;
- `push_reason_code`;
- closed event payload projection child;
- closed direction/cross-symbol constraints child;
- closed action clamp child;
- closed portfolio target clamp child;
- closed exposure math child.

## Equivalence Baseline

The next actual extraction must preserve:

- all existing risk-checker test names and assertions;
- `cargo test -p qrpc-runtime risk -- --nocapture` passing;
- parent-owned production API and helper visibility;
- no sibling child imports.

## Gates

Required gates for actual extraction:

- `cargo fmt`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime risk -- --nocapture`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 / full feature tree / matrix governance

## Next Step

BE-002CH-01 `root.contracts.runtime_support.risk_support.test_harness` actual_extraction.
