# v4.16.0 root.executor.runner.runner_support_files.okx_rest.transport_response_surface extract closeout

> Batch: BE-002LV-02
> Node: `root.executor.runner.runner_support_files.okx_rest.transport_response_surface`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> governance_next_authority: active

---

## Movement

The OKX REST transport and response-handling surface has been extracted from
`src-executor/okx_rest.rs` into:

`src-executor/okx_rest/transport_response_surface.rs`

The parent keeps public types/constants, signing request facade, order/query/
cancel/balance action wrappers, lookup validation, and smoke test orchestration.

## Ownership Now

| Field | Value |
| --- | --- |
| Child file | `src-executor/okx_rest/transport_response_surface.rs` |
| Owned helpers | proxy URL selection, `ureq::Agent` timeout/proxy construction, raw signed request dispatch, signed request dispatch with OKX success check, OKX top-level/per-order success validation |
| Parent callers | order/query/cancel/balance wrappers and ignored smoke tests |
| Parent retained | public types/constants facade, signing request child, action wrappers, lookup validation, smoke test orchestration |

## Preserved Behavior

- REST proxy environment precedence is unchanged.
- Connect/read/write timeout values are unchanged.
- OKX status responses are still parsed as JSON payloads.
- Non-status transport errors remain errors.
- Top-level `code != "0"` rejection behavior is unchanged.
- First-item `sCode != "0"` rejection behavior is unchanged.

## Gates

| Gate | Result |
| --- | --- |
| `cargo fmt --all --check` | PASS |
| `cargo check -p quantpilot --bin executor` | PASS |
| `cargo test -p quantpilot --bin executor okx_` | PASS, 12 passed / 2 ignored |
| `git diff --check` | PASS |

## Next Step

BE-002LV-03
`root.executor.runner.runner_support_files.okx_rest.transport_response_surface`
single_leaf_closeout.
