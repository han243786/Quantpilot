# v4.16.0 root.contracts.quantscript.runtime_lowering.manual_formula_fallback baseline plan

> Batch: BE-002JE-01
> Node: `root.contracts.quantscript.runtime_lowering.manual_formula_fallback`
> Parent: `root.contracts.quantscript.runtime_lowering`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Baseline Decision

Freeze `manual_formula_fallback` as a precision parent before any Rust movement.

The next movement must not split the full `quantscript/src/lowering/fallback.rs` file opportunistically. It must first choose one internal child owner and preserve all public(crate) fallback recognition entrypoints, no-match behavior, error behavior, typed expression matching, and numeric period/span extraction semantics.

## Public Boundary To Preserve

| Public surface | Owner during baseline | Stability rule |
| --- | --- | --- |
| `resolve_manual_formula_binding` | `manual_formula_fallback` parent facade until a child is approved | Resolved manual indicator binding shape and priority must remain stable |
| `manual_rsi_from_expr` / `match_manual_rsi_formula` | parent facade until a child is approved | RSI period, smoothing, source, and pair matching semantics must remain stable |
| `manual_momentum_from_expr` / ratio helpers | parent facade until a child is approved | Momentum lookback/source and ratio matching semantics must remain stable |
| `manual_macd_line_from_expr` / `manual_macd_histogram_from_expr` | parent facade until a child is approved | MACD period/source inference and line/signal/histogram recognition must remain stable |
| `manual_zscore_from_expr` / `match_zscore_operands` | parent facade until a child is approved | ZScore mean/stddev/source/window matching must remain stable |
| `moving_average_from_expr` / `match_manual_moving_average_window` | parent facade until a child is approved | Moving-average source/window recognition must remain stable |
| `resolve_boundary_lookback_source_span` | parent facade until a child is approved | Boundary lookback source/span fallback must remain stable |

## Internal Candidate Queue

| Candidate child | Scope | Status |
| --- | --- | --- |
| `manual_rsi_formula` | RSI avg gain/loss, RS pair, balanced smoothed change pair, and smoothing method recognition | queued |
| `manual_momentum_formula` | momentum direct and ratio formula fallback recognition | queued |
| `manual_macd_formula` | MACD line, signal, histogram, EMA spread, and line/signal pair recognition | queued |
| `manual_zscore_formula` | ZScore expression and operand fallback recognition | queued |
| `manual_moving_average_window` | moving average expression and window fallback recognition | queued |
| `boundary_lookback_fallback` | latest/lookback boundary source-span recovery | queued |
| `fallback_test_harness` | colocated fallback tests and helper fixtures | queued |

## Leaf Granularity Evidence

Command:

- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -LeafId root.contracts.quantscript.runtime_lowering.manual_formula_fallback -ParentId root.contracts.quantscript.runtime_lowering -Path quantscript/src/lowering/fallback.rs -Depth 4`

Result:

| Field | Value |
| --- | --- |
| `split_decision` | `CONTINUE` |
| `governance_packaging` | `precision_single_leaf` |
| `final_decision` | `PRECISION` |
| `normalized_split_score` | 63 |
| `loc` | 1001 |
| `function_count` | 44 |
| `public_surface` | 13 |
| `branches` | 57 |
| `risk_tags` | `cross_crate`, `public_api`, `trading_semantics` |
| `reason` | oversized high-risk leaf requires precision baseline before movement |

## Risk Controls

- Do not change the order in which manual fallback recognizers are tried.
- Do not change returned `IndicatorBinding` variants, periods, smoothing methods, source identities, or lookback spans.
- Do not change no-match behavior: unsupported formulas must keep returning `None` rather than errors.
- Do not move binding source decoding, source recovery, semantic aliases, shared arg helpers, or typed resolver code in this baseline.
- Do not introduce sibling horizontal links; all child calls must go through the parent module boundary.

## Equivalence Baseline

Required validation after future Rust movement:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8, full feature tree, and matrix governance checks

Behavior that must stay equivalent:

- manual RSI formula recognition and rejection behavior;
- manual momentum and momentum-ratio formula recognition;
- manual MACD line/signal/histogram recognition;
- manual ZScore formula recognition;
- manual moving-average window recognition;
- boundary lookback source/span fallback;
- colocated fallback unit tests and integration coverage.

## Next Step

BE-002JF-01 `root.contracts.quantscript.runtime_lowering.manual_formula_fallback` parent_residual_judgment.
