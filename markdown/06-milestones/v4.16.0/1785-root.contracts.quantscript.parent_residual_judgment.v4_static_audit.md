# v4.16.0 root.contracts.quantscript parent residual judgment: v4_static_audit

> Batch: BE-002JP-01
> Node: `root.contracts.quantscript`
> Selected child: `root.contracts.quantscript.v4_static_audit`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `v4_static_audit` as the next child under `quantscript`.

`v4_static_audit.rs` remains the only queued QuantScript child that clearly
requires continued recursive work. Terminal leaf control returned `PRECISION`
over an oversized, high-risk file, so the next step must freeze a dedicated
baseline before any Rust movement.

## Candidate Evaluation

| Candidate | File / scope | Decision | Score | Reason |
| --- | --- | --- | --- | --- |
| `v4_static_audit` | `quantscript/src/v4_static_audit.rs` | `PRECISION` | 60 | Oversized high-risk static-audit and runtime-handoff contract |
| `test_plan_surface` | `quantscript/src/test_plan.rs` | `STOP` | 43 | Terminal-sized surface; standalone split not justified |
| `authoring_docs_samples` | authoring docs and sample QS files | queued for later closeout | n/a | Documentation/sample ownership should not block code recursion |

## Selected Boundary

| Field | Value |
| --- | --- |
| Planned owner | `root.contracts.quantscript.v4_static_audit` |
| Primary file | `quantscript/src/v4_static_audit.rs` |
| Public surface | static audit report DTOs, runtime handoff DTO, audit entrypoint, handoff builder |
| Risk profile | compiler contract, state machine, route/schema, trading semantics |

## Exclusions

- `test_plan_surface` remains outside this selected child.
- `authoring_docs_samples` remains outside this selected child.
- Runtime lowering is already closed and must not be reopened by this selection.
- Plugin metadata and executor remain outside `contracts.quantscript`.

## Gates

| Gate | Result |
| --- | --- |
| parent_open | PASS |
| candidate_requires_work | PASS, `v4_static_audit` returned `PRECISION` |
| lower-risk candidates | PASS, `test_plan_surface` returned `STOP` |
| code movement | none |

## Next Step

BE-002JQ-01 `root.contracts.quantscript.v4_static_audit` baseline_plan.
