# v4.16.0 simulated_execution_engine.runtime_adapter baseline plan

> Batch: BE-002EE-01
> Node: `root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine.runtime_adapter`
> Parent: `root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_trial: true

---

## Decision

Freeze `runtime_adapter` as a terminal extraction candidate.

The next movement may create a child module under `qrpc_runtime/src/v4_runtime/simulated_execution_engine/` and move only the `impl V4PaperSimulatedRuntime` adapter layer out of `qrpc_runtime/src/v4_runtime/simulated_execution_engine.rs`.

## Movement Boundary

Move only:

- `with_simulated_execution_config`;
- `update_simulated_market_price`;
- `simulated_execution_snapshot`;
- `apply_runtime_simulated_execution_for_transition`;
- `build_simulated_order_request`;
- `latest_market_price`;
- `validate_simulated_order_capabilities`;
- `record_simulated_execution_events`.

Keep in the parent `simulated_execution_engine` module:

- `include!("../v4_simulated_execution.rs")`;
- simulated order lifecycle internals;
- trigger helpers;
- fill and ledger accounting;
- snapshot and metrics internals;
- validation, parsing, and capability helper functions.

## Equivalence Baseline

The extraction must preserve:

- public runtime methods and return types;
- existing parent-mediated access for replay and transition siblings;
- simulated order request field mapping;
- capability rejection behavior;
- event source string `runtime.execution_simulator`;
- all v4 runtime tests.

## Terminal Leaf Control

`runtime_adapter` evaluates to:

- `split_decision`: `STOP`
- `governance_packaging`: `stop_split`
- `final_decision`: `STOP`
- Score: `36`

The child should stop after extraction unless a later developer decision changes the module boundary.

## Next Step

BE-002EF-01 `root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine.runtime_adapter` actual_extraction.
