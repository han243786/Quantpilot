# v4.16.0 root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_momentum_formula extract closeout

> Batch: BE-002JI-02
> Node: `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_momentum_formula`
> Stage: `actual_extraction`
> Movement: Rust code movement complete.
> governance_next_authority: active

---

## Extraction Result

`quantscript/src/lowering/fallback/manual_momentum_formula.rs` now owns manual momentum fallback recognition:

- direct `ResolvedManualIndicatorFormula::Momentum` handling;
- division-ratio momentum fallback;
- subtract/division `ratio - 1` momentum fallback;
- private legacy latest/lookback source-span pair recovery;
- conversion from source/span matches into `IndicatorBinding::Momentum`.

The parent `quantscript/src/lowering/fallback.rs` keeps the stable public(crate) surface:

- `manual_momentum_from_expr` delegates to the child implementation;
- `manual_momentum_ratio_from_division` delegates to the child implementation;
- `manual_momentum_ratio_from_subtract_division` delegates to the child implementation;
- `resolve_boundary_lookback_source_span` remains parent-owned for the queued boundary child.

## Boundary Preservation

| Contract | Result |
| --- | --- |
| Recognizer order | Preserved; MACD remains before direct momentum in subtract branches and ZScore remains before ratio momentum in divide branches |
| Public(crate) wrappers | Preserved in the parent module |
| Boundary helper ownership | Preserved in parent |
| Non-momentum fallback owners | Not moved |
| Trading semantics | Preserved by existing manual momentum tests and full `quantscript` test suite |
| Parent/child communication | Parent delegates through `mod manual_momentum_formula`; child helpers remain private |

## Verification

| Gate | Result |
| --- | --- |
| `cargo fmt --all --check` | PASS |
| `cargo test -p quantscript` | PASS, 88 tests |
| `cargo check -p quantpilot` | PASS, no warnings |

Additional governance gates are run before commit for diff whitespace, UTF-8, full feature tree, and matrix governance.

## Next Step

BE-002JI-03 `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_momentum_formula` single_leaf_closeout.
