# v4.16.0 root.contracts.quantscript.evaluator_normalization.helper_inline_execution_wave baseline plan

> Batch: BE-002HR-01
> Node: `root.contracts.quantscript.evaluator_normalization.helper_inline_execution_wave`
> Parent: `root.contracts.quantscript.evaluator_normalization`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Scope

Freeze the evaluator helper inline execution wave before movement.

Current owner:

- `quantscript/src/evaluator/mod.rs`

Planned child:

- `quantscript/src/evaluator/helper_inline_execution_wave.rs`

The next movement may create the child module and move only the helper execution functions listed below.

## Functions And Types To Move

The planned child owns:

- `ExecOutcome`
- `maybe_inline_function`
- `should_skip_inline`
- `MAX_RECURSION_DEPTH`
- `MAX_FOR_ITERATIONS`
- `evaluate_function`
- `execute_function_body`
- `execute_stmt`
- `IfStmtParts`
- `execute_if_stmt`
- `execute_for_stmt`
- `execute_while_stmt`
- `execute_match_stmt`
- `execute_block_in_place`
- `BlockSnapshot`
- `execute_block_snapshot`
- `execute_match_arm_snapshot`
- `execute_match_arm_body`
- `pattern_matches`

## Parent-Mediated Access

The child may expose:

- `pub(super) fn maybe_inline_function(...) -> Result<Option<Expr>>`

All other helper execution internals should remain private inside the child where possible.

The child needs parent-mediated access to:

- `EvalContext`
- `normalize_expr`

The parent may declare:

- `mod helper_inline_execution_wave;`
- `use helper_inline_execution_wave::maybe_inline_function;`

## Dependencies

The child may call the closed folding/value wave through parent-sibling imports:

- `folding_value_wave::expr_bool`
- `folding_value_wave::expr_iterable_items`
- `folding_value_wave::fold_binary`

This preserves the parent-mediated module tree while avoiding duplicate folding helpers.

## Equivalence Requirements

The next extraction must preserve:

- recursion detection and `MAX_RECURSION_DEPTH`;
- `for` iteration budget;
- helper function argument binding;
- helper function return handling;
- `if` branch convergence and divergent symbolic branch skip behavior;
- `for`, `while`, and `match` symbolic execution semantics;
- pattern matching through folded equality;
- unsupported helper-statement skip behavior;
- public `normalize_script_module` behavior.

## Forbidden Movement

The next movement must not move:

- `normalize_script_module`;
- `EvalContext`;
- `normalize_function`, `normalize_block`, `normalize_stmt`, `normalize_expr`;
- `folding_value_wave`;
- evaluator tests;
- parser, lowering, analysis diagnostics, typed resolution, static audit, test plan, legacy config, or release transition code.

## Expected Validation

The extraction must pass:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002HR-02 `root.contracts.quantscript.evaluator_normalization.helper_inline_execution_wave` extract_closeout.
