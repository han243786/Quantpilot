# v4.16.0 root.contracts.runtime_support.fill_engine baseline plan

> Batch: BE-002BA-01
> Node: `root.contracts.runtime_support.fill_engine`
> Parent: `root.contracts.runtime_support`
> Stage: `baseline_plan`
> Movement: No Rust code movement.

---

## Scope

`root.contracts.runtime_support.fill_engine` freezes the current simulated fill engine boundary before same-parent wave extraction.

Physical scope:

- `qrpc_runtime/src/fill_engine.rs`

Owned public surface to preserve:

- `MarketState`
- `MarketState::from_mid_price`
- `MarketState::from_quote`
- `FillEngine`
- `FillEngine::with_assumptions`
- `FillEngine::set_assumptions`
- `FillEngine::assumptions`
- `FillEngine::submit_plan`
- `FillEngine::on_market_update`
- `apply_fill_to_portfolio`

External boundaries to preserve:

- execution planning remains owned by closed `execution_module`;
- slippage computation remains owned by `slippage`;
- runtime facade/provider wiring remains owned by closed `runtime_facade_coordinator`;
- risk checks, sandbox/replay/timeline, v4 runtime, live execution, plugin runtime, ops compatibility, backtest metrics, QuantScript, executor, and release transition remain outside this child.

## White-Box Responsibilities

This child owns simulated order-fill behavior after an execution plan is produced:

- market-state projection into slippage assumptions;
- fill engine state for processed plan idempotency and resting orders;
- plan submission lifecycle for FOK, IOC, and GTC orders;
- market-update lifecycle for resting order fills;
- open/partial/fill/cancel/reject event projection;
- cash, position, and reservation accounting for simulated orders;
- fill report construction and portfolio mutation;
- local fill-engine tests and fixtures.

## Leaf Granularity Gate

The leaf granularity tool returned conservative STOP:

| Metric | Value |
| --- | --- |
| LOC | 1253 |
| Functions | 50 |
| Public surface | 16 |
| Branches | 72 |
| Domains | 5 |
| Score | 58 |
| Tool decision | STOP |
| Terminal mode | `stop_split` |

The baseline overrides standalone STOP only into a cost-controlled same-parent wave queue, because the file is oversized, state-machine-heavy, and has separable helper ownership pockets. This does not authorize precision splitting of every helper.

## Child Queue

The next recursive child queue is:

1. `contracts.runtime_support.fill_engine.event_projection_wave`
2. `contracts.runtime_support.fill_engine.portfolio_reservation_accounting`
3. `contracts.runtime_support.fill_engine.fill_report_execution_helpers`
4. `contracts.runtime_support.fill_engine.test_harness`

## Parent-Owned Surface

The parent must keep:

- `MarketState` and `FillEngine` structs;
- public constructor, assumption, plan submission, and market-update methods;
- `RestingOrderOpenRequest`;
- orchestration flow for FOK/IOC/GTC and resting-order state;
- `apply_fill_to_portfolio` unless a later baseline proves public re-export equivalence with lower churn;
- all child module declarations and parent-mediated helper imports.

## First Child Selection Preview

The next parent residual judgment should select `contracts.runtime_support.fill_engine.event_projection_wave`.

Reason:

- event builders are cohesive and behaviorally pure;
- movement can be parent-mediated through `pub(super)` functions;
- the wave does not mutate portfolio state, open-order maps, or execution planning;
- it is the lowest-risk extraction before accounting and fill-report helpers.

## Boundary Rules

| Boundary | Requirement |
| --- | --- |
| Parent-child communication | Children may use parent-owned types and helper imports through `super::*`, but sibling shortcuts remain forbidden. |
| Public API | No public struct, function, field, enum, serde shape, or crate export may change. |
| Trading semantics | FOK/IOC/GTC behavior, resting-order lifecycle, reservation release, and portfolio mutation must stay equivalent. |
| Text cleanup | Legacy mojibake comments may be normalized only when touched by extraction or required by formatter/compiler clarity; no semantic rewrite is authorized. |
| Release transition | No performance shortcut or horizontal child connection is proposed. |

## Equivalence Baseline

Current baseline evidence for this no-code movement step:

- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime fill_engine -- --nocapture`: PASS, 12 passed
- leaf granularity evaluator: PASS, conservative STOP score 58

Required gates for the next movement:

- `cargo fmt`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime fill_engine`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-002BB-01 `root.contracts.runtime_support.fill_engine` parent_residual_judgment selects `event_projection_wave`.
