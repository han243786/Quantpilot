# v4.16.0 root.contracts.quantscript.v4_static_audit.edge_parser single leaf closeout

> Batch: BE-002KM-03
> Node: `root.contracts.quantscript.v4_static_audit.edge_parser`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`stop_split: true`

The extracted child is a micro graph-edge parser leaf. It owns one edge grammar
rule and `MachineGraphEdge` assembly. Further split would only fragment source,
target, event, and default-field assignment helpers.

## Leaf Granularity Evidence

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -LeafId root.contracts.quantscript.v4_static_audit.edge_parser -ParentId root.contracts.quantscript.v4_static_audit -Path quantscript\src\v4_static_audit\edge_parser.rs -Depth 4
```

Observed result:

| Field | Value |
| --- | --- |
| Split decision | STOP |
| Governance packaging | stop_split |
| Decision | STOP |
| Score | 27 |
| LOC | 27 |
| Functions | 1 |
| Public surface | 1 |
| Branches | 1 |
| Domains | 3 |
| Risk tags | cross_crate, public_api |
| Size bucket | micro_under_100 |

## Why No Further Split

- The child is under 100 LOC.
- There is only one parent-facing function.
- Edge syntax, id construction, and default edge fields are one graph topology rule.
- Risk-plane parsing remains a separate candidate.
- Parent-child communication remains explicit through the parent wrapper.

## Preserved Boundaries

- Public API unchanged.
- `static_document_parser` continues through the parent wrapper.
- `parse_state_group` and `parse_risk_plane` remain parent-owned.
- Public report DTOs and local static-audit tests remain parent-owned.
- No release-transition link was introduced.

## Gates

| Gate | Result |
| --- | --- |
| leaf granularity evaluator | PASS, STOP score 27 |
| `cargo fmt --all --check` | PASS |
| `cargo test -p quantscript` | PASS, 88 tests |
| `cargo check -p quantpilot` | PASS |
| `git diff --check` | PASS |

## Next Step

BE-002KN-01 `root.contracts.quantscript.v4_static_audit`
parent_residual_judgment.
