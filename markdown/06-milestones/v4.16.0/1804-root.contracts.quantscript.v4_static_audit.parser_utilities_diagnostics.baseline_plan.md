# v4.16.0 root.contracts.quantscript.v4_static_audit.parser_utilities_diagnostics baseline plan

> Batch: BE-002KA-01
> Node: `root.contracts.quantscript.v4_static_audit.parser_utilities_diagnostics`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Goal

Freeze the shared parser utility and diagnostic helper boundary before Rust
movement.

This child owns utility behavior used by the remaining static document and
machine parser clusters. Making it explicit first reduces repeated helper
movement in later parser extractions.

## Frozen Boundary

| Field | Value |
| --- | --- |
| Approved type | `PreparedLine` |
| Approved functions | `prepare_lines`, `split_words`, `split_csv_words`, `diag` |
| Planned file | `quantscript/src/v4_static_audit/parser_utilities_diagnostics.rs` |
| Parent callers | static document parser, machine parser, state parser, edge/risk parser |
| Visibility | `pub(super)` for the type, fields, and helpers |

## Equivalent Behavior

The extracted child must preserve:

- line enumeration as one-based source line numbers;
- removal of `//` comments before `#` comments;
- trimming and dropping empty prepared lines;
- whitespace splitting for parser words;
- comma splitting with trim and empty-value filtering;
- diagnostic construction through `Diagnostic::error`;
- diagnostic span shape `Span::module(format!("line {line_number}"))`.

## Exclusions

- Do not move static document parser logic.
- Do not move machine/state/memory/transition/edge/risk-plane parser logic.
- Do not move machine template or capability/type parser wrappers.
- Do not move event catalog derivation.
- Do not move DTO definitions outside `PreparedLine`.
- Do not move local tests.
- Do not introduce release-transition cross-child links.

## Allowed Rust Movement For Next Step

BE-002KA-02 may:

1. add `mod parser_utilities_diagnostics;`;
2. create `quantscript/src/v4_static_audit/parser_utilities_diagnostics.rs`;
3. move `PreparedLine`, `prepare_lines`, `split_words`, `split_csv_words`, and
   `diag`;
4. import those child-owned helpers into `v4_static_audit.rs`;
5. adjust parent imports only where required by the extraction.

## Gates For Actual Extraction

| Gate | Command |
| --- | --- |
| format | `cargo fmt --all --check` |
| crate tests | `cargo test -p quantscript` |
| workspace compile | `cargo check -p quantpilot` |
| diff hygiene | `git diff --check` |
| UTF-8 | `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1` |
| full tree | `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1` |
| matrix governance | `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1` |

## Next Step

BE-002KA-02 `root.contracts.quantscript.v4_static_audit.parser_utilities_diagnostics`
actual_extraction.
