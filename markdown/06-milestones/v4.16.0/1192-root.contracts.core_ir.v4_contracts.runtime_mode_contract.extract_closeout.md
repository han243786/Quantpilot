# v4.16.0 root.contracts.core_ir.v4_contracts.runtime_mode_contract actual extraction complete

> Batch: BE-001VG-02
> Node: `root.contracts.core_ir.v4_contracts.runtime_mode_contract`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `extract_closeout`
> Movement: Code movement completed.

---

## Summary

`root.contracts.core_ir.v4_contracts.runtime_mode_contract` actual extraction is complete.

Implemented movement:

- `qrpc_core_ir/src/v4.rs` now declares `mod runtime_mode_contract;`
- `qrpc_core_ir/src/v4.rs` re-exports the child through `pub use runtime_mode_contract::*;`
- `qrpc_core_ir/src/v4/runtime_mode_contract.rs` owns runtime mode DTOs, enums, constants, defaults, validation,
  lookup helpers, and runtime execution event defaults
- `required_runtime_trading_modes` moved into the child and remains `pub(in crate::v4)` for parent plugin governance
  validation

Equivalence notes:

- public v4 runtime mode surface remains unchanged through the parent re-export
- runtime mode validation error strings, default specs, execution event order, and settlement lookup behavior remain
  frozen from BE-001VG-01
- venue matrix, QS type system, plugin governance, version manifest, reproducibility, complexity budget, capability,
  bridge, machine, graph, QS profile, and tests remain outside this child

Verification:

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir` (54 passed)

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs`
- `qrpc_core_ir/src/v4/runtime_mode_contract.rs`

**Markers**:
- `BE-001VG-02`

**Next step**:
BE-001VG-03 root.contracts.core_ir.v4_contracts.runtime_mode_contract single_leaf_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
