# v4.16.0 root.contracts.runtime_support.risk_support.exposure_math_helpers extract closeout

> Batch: BE-002CE-01
> Node: `root.contracts.runtime_support.risk_support.exposure_math_helpers`
> Parent: `root.contracts.runtime_support.risk_support`
> Stage: `actual_extraction`
> Movement: Rust code movement.

---

## Scope

Extract the shared exposure/equity math helper pocket from `qrpc_runtime/src/risk_checker.rs` into a dedicated child module.

Moved file:

- `qrpc_runtime/src/risk_checker/exposure_math_helpers.rs`

Moved helpers:

- `available_sell_ratio`
- `portfolio_equity`
- `symbol_net_exposure_ratio`
- `portfolio_net_exposure_ratio`

## Parent Boundary

`qrpc_runtime/src/risk_checker.rs` now declares `mod exposure_math_helpers;` and imports the four helpers through the parent. Closed sibling children keep parent-mediated access through their existing `super::*` boundary.

The parent still owns:

- public risk checker provider/evaluate surface;
- `evaluate_risk_decision`;
- `enforce_risk_mode`;
- `push_reason_code`;
- closed projection/constraint/clamp child composition;
- local tests.

## Equivalence

The extracted functions preserve the baseline formulas:

- sell availability ratio still uses positive unfrozen inventory and reference price;
- portfolio equity remains `cash_balance + total_net_notional`;
- symbol net exposure ratio still sums symbol notional and divides by equity;
- portfolio net exposure ratio still uses absolute total net notional over equity.

No public API was added, removed, or renamed.

## Dependency Rule

No child-to-child import was introduced. The exposure helper child is internal to the risk-support parent and exposes only `pub(super)` helpers.

## Gates

Required gates for this batch:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime risk -- --nocapture`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 / full feature tree / matrix governance

## Next Step

BE-002CE-02 `root.contracts.runtime_support.risk_support.exposure_math_helpers` single_leaf_closeout.
