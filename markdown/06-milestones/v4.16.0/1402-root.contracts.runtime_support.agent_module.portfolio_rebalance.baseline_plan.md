# v4.16.0 root.contracts.runtime_support.agent_module.portfolio_rebalance baseline plan

> Batch: BE-001ZZ-01
> Node: `root.contracts.runtime_support.agent_module.portfolio_rebalance`
> Parent: `root.contracts.runtime_support.agent_module`
> Stage: `baseline_plan`
> Movement: No Rust code movement.

---

## Scope

This baseline freezes the slow-cycle portfolio rebalance policy path before extraction.

Current owner file:

- `qrpc_runtime/src/agent_module.rs`

Planned child file:

- `qrpc_runtime/src/agent_module/portfolio_rebalance.rs`

## Allowed Movement

The next extraction may move only:

- `portfolio_rebalance_due`
- `RebalanceSymbolPlan`
- `build_portfolio_rebalance_decision`
- `assign_target_weights`

The parent may add:

- `mod portfolio_rebalance;`
- calls to `portfolio_rebalance::portfolio_rebalance_due`;
- calls to `portfolio_rebalance::build_portfolio_rebalance_decision`;
- imports required by the child.

## Parent-Owned Surface To Keep

The parent keeps:

- public request/output/provider/builtin types;
- `BuiltinAgentModule::evaluate_agents` dispatch and event assembly;
- `evaluated_rebalance_agent_ids` insertion semantics;
- shared helper ownership:
  - `build_signal_kind_index`
  - `signal_score`
  - `current_position_ratio`
  - `MIN_QUANTITY_RATIO`
  - `DEFAULT_DECISION_THRESHOLD`
- weighted signal child, cross-venue arbitrage, shared helper queue, and tests.

Child access to parent-owned helpers is allowed through normal child-module visibility. No sibling shortcut is allowed.

## Boundaries To Preserve

| Boundary | Requirement |
| --- | --- |
| Dispatch | `evaluate_agents` continues to select this path only for `AgentPolicyKind::PortfolioRebalance` on `slow` cycle. |
| Cadence | `EverySlow`, `Every1d`, `Weekly`, and missing schedule behavior remain unchanged. |
| Rebalance id tracking | The parent still inserts `agent.agent_id` into `evaluated_rebalance_agent_ids` only after cadence allows evaluation. |
| Signal filtering | Observe signal rules and `IntentKind::QuoteObserve` remain excluded from rebalance signal scoring. |
| Universe | Explicit rebalance universe still overrides signal-derived universe. |
| Threshold | Selected-symbol threshold uses `agent.decision_threshold.unwrap_or(DEFAULT_DECISION_THRESHOLD).max(0.0)`. |
| Current weights | Current position ratio still uses signal/position exchange, reference price fallback, and parent-owned `current_position_ratio`. |
| Allocation | Equal, fixed, rank, inverse-rank, and score-weight allocation behavior remains equivalent. |
| Output shape | Portfolio target decision, target id, target weights, reason, net side, net strength, timestamp, and trace id remain equivalent. |

## Exclusions

The next extraction must not move:

- weighted signal direct action child;
- cross-venue arbitrage decisions;
- event emission;
- provider trait or built-in provider type;
- shared scoring/portfolio helper ownership;
- tests.

## Risk Notes

This is a high-value medium-risk extraction. It touches allocation semantics and rebalance cadence, but it is a cohesive policy family with strong tests. Prefer preserving function signatures and delegating from the parent rather than reshaping DTOs.

## Equivalence Gates

Required for the next extraction:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-001ZZ-02 `root.contracts.runtime_support.agent_module.portfolio_rebalance` actual_extraction.
