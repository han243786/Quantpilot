# v4.16.0 root.contracts.quantscript.analysis_diagnostics.index_bounds_gate baseline plan

> Batch: BE-002HG-01
> Node: `root.contracts.quantscript.analysis_diagnostics.index_bounds_gate`
> Parent: `root.contracts.quantscript.analysis_diagnostics`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Scope

Freeze the QuantScript index-bound diagnostic before movement.

Current owner:

- `quantscript/src/analysis_diagnostics/analysis.rs`

Planned child:

- `quantscript/src/analysis_diagnostics/index_bounds_gate.rs`

## Items Frozen For Movement

The next extraction may move only:

- `build_data_source_map`
- `check_all_index_bounds`
- `check_index_bounds_from_stmts`
- `check_index_bounds_from_expr`

## Diagnostic Boundary

The child owns:

- local data source lookback map construction from `let` bindings;
- recursive AST traversal for index expressions;
- `QS0404` possible out-of-bounds index warning emission.

## Parent Wiring To Preserve

The analysis parent must keep:

- `ScriptAnalysis` and `analyze_script_module` public surface unchanged;
- diagnostic family invocation order unchanged;
- crate-root public exports and `crate::analysis` / `crate::diagnostics` compatibility unchanged;
- `fetch_lookback` imported from `warmup_fetch_gate` and passed through the parent boundary as the only helper needed by this child;
- strategy presence, dead-code emit, and already closed diagnostic children unchanged.

The child may expose:

- `build_data_source_map`
- `check_all_index_bounds`

## Forbidden Movement

The next extraction must not move or change:

- strategy fetch/emit presence checks;
- dead-code emit checks;
- diagnostic DTO/span types;
- unsupported construct, lookahead-window, warmup/fetch, indirect-recursion, symbol-whitelist, and fetch-lookback-warning children already closed;
- evaluator normalization, runtime lowering, v4 static audit, tests, authoring samples, backend, executor, frontend, plugin metadata, or release transition.

## Expected Validation

The extraction must pass:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002HG-02 `root.contracts.quantscript.analysis_diagnostics.index_bounds_gate` actual_extraction.
