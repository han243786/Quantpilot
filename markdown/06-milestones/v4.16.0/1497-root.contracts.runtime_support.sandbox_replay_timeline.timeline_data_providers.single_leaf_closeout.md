# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline.timeline_data_providers single leaf closeout

> Batch: BE-002CN-02
> Node: `root.contracts.runtime_support.sandbox_replay_timeline.timeline_data_providers`
> Parent: `root.contracts.runtime_support.sandbox_replay_timeline`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Decision

`stop_split: true`

The timeline provider child is terminal for the current pass.

## Leaf Granularity Evidence

`tools/evaluate-leaf-granularity.ps1` result:

- decision: `STOP`
- score: `48`
- loc: `327`
- functions: `27`
- public surface: `16`
- domains: `4`
- terminal leaf control bucket: `terminal_target_150_600`
- governance mode: `stop_split`

The provider leaf sits inside the preferred terminal LOC range. Kline, quote, and resample providers are related by the common `TimelineDataProvider` contract and replay/timeline ownership, so splitting them further would add governance overhead without improving runtime modularity.

## Boundary

The leaf owns provider trait/types and provider-local interval conversion. `UnifiedTimeline`, replay builder functions, sandbox control surface, sandbox implementations, and tests remain queued outside this leaf.

## Gates

Evidence inherited from BE-002CN-01:

- `cargo fmt --check`: pass
- `cargo check -p qrpc-runtime`: pass, no warning noise
- `cargo test -p qrpc-runtime sandbox -- --nocapture`: pass, 21 passed / 2 ignored
- `cargo check -p quantpilot`: pass
- `git diff --check`: pass
- UTF-8 / full feature tree / matrix governance: pass

## Next Step

BE-002CO-01 `root.contracts.runtime_support.sandbox_replay_timeline` parent_residual_judgment selects `unified_timeline`.
