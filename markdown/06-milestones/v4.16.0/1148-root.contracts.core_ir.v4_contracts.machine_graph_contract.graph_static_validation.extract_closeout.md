# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation actual extraction complete

> Batch: BE-001UI-02
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract`
> Stage: `extract_closeout`
> Movement: Code movement complete.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation` is now extracted as a private graph child module.

The child owns exactly:

- `V4MachineGraphContract::validate_static_contract`
- `V4MachineGraphContract::validate_graph_acyclic`
- `V4MachineGraphContract::validate_event_catalog`
- `V4MachineGraphContract::validate_risk_plane`
- `machine_event_party_allowed`

The graph parent now declares:

- `mod static_validation;`

Graph schema, event catalog, traversal helpers, default helpers, complexity metrics, bridge builders, and tests did not move.

## Boundary

**Real files**:

- `qrpc_core_ir/src/v4/machine_graph_contract.rs`
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation.rs`

**Markers**:

- `BE-001UI-02`

## White-Box Result

| Surface | Owner after extraction | Parent-mediated dependencies |
| --- | --- | --- |
| graph schema | `machine_graph_contract` | default helpers and machine contract types |
| event catalog schema/local validation | `event_catalog` | catalog default helper and version constant |
| graph static validation | `static_validation` | graph schema, traversal helper, machine contract types, graph/risk constants |
| traversal helpers | `machine_graph_contract` | retained as graph parent helpers for validation and v4 parent complexity metrics |

## Equivalence Evidence

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir` passed: 54 tests.
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`

## Next Step

BE-001UI-03 `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation` single_leaf_closeout.
