# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation.event_reference_resolution equivalence baseline and extraction plan

> Batch: BE-001UQ-01
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation.event_reference_resolution`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation`
> Stage: `baseline_plan`
> Movement: No code movement.

---

## Summary

This baseline freezes event reference resolution before extraction.

The selected child may own only:

- collecting all nested machines visible to graph event usage validation;
- collecting transition event references;
- collecting action emitted-event references;
- collecting edge event references;
- preserving empty action emitted-event error reporting;
- a small parent-returned resolution result structure.

It must not own event catalog presence checks, local event catalog validation,
event spec map construction, undeclared event checks, event party validation,
event catalog DTOs, graph validation orchestration, DAG validation, or risk-plane validation.

## Frozen Validation Surface

The next extraction must preserve:

- nested machine discovery through `collect_machine_family`;
- transition `event.event_type` insertion when non-empty;
- action `emits` insertion when non-empty;
- empty action `emits` error:
  `machine `{machine_id}` transition `{transition_id}` action emits an empty event_type`;
- edge `event_type` insertion when non-empty;
- ordering of parser errors before catalog presence/local validation/undeclared event checks;
- parent ability to pass `all_machines` to event party validation.

## Allowed BE-001UQ-02 Movement

The next extraction step may:

- create `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/event_usage_validation/event_reference_resolution.rs`;
- add `mod event_reference_resolution;` to `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/event_usage_validation.rs`;
- move only event reference resolution into a child helper;
- introduce a small child-owned result struct with `all_machines`, `referenced_events`, and `errors`;
- keep `validate_event_catalog` as the parent orchestration entry.

The child may depend on:

- `std::collections::BTreeSet`;
- `collect_machine_family`;
- `V4MachineContract`;
- `V4MachineGraphContract`.

## Forbidden BE-001UQ-02 Movement

The next extraction step must not move or edit behavior for:

- event catalog presence requirement;
- `MachineEventCatalog::validate_static_contract` delegation;
- event spec map construction;
- undeclared event rejection;
- `validate_event_parties` calls;
- event party validation helper file;
- event catalog DTOs;
- graph validation parent orchestration;
- DAG/risk-plane validation;
- tests.

It must not alter validation branch order, error strings, or event lookup behavior.

## Equivalence Proof Plan

BE-001UQ-02 must pass:

- `cargo fmt --check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/event_usage_validation.rs`

**Planned child file**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/event_usage_validation/event_reference_resolution.rs`

**Markers**:
- `BE-001UQ-01`

**Next step**:
BE-001UQ-02 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation.event_reference_resolution extract_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
