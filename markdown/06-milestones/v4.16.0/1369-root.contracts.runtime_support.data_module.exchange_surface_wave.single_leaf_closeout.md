# v4.16.0 root.contracts.runtime_support.data_module.exchange_surface_wave single leaf closeout

> Batch: BE-001ZG-03
> Node: `root.contracts.runtime_support.data_module.exchange_surface_wave`
> Stage: `single_leaf_closeout`
> Movement: No code movement.
> Parent: `root.contracts.runtime_support.data_module`
> Baseline: `1366-root.contracts.runtime_support.data_module.exchange_surface_wave.baseline_plan.md`
> Extraction: `1367-root.contracts.runtime_support.data_module.exchange_surface_wave.extract_closeout.md`

---

## Summary

The `exchange_surface_wave` single leaf closeout is complete.

The wave closes both child concerns:

- `contracts.runtime_support.data_module.exchange_endpoints`
- `contracts.runtime_support.data_module.exchange_payload_parsing`

`qrpc_runtime/src/data_module/exchange_surface.rs` remains one exchange surface child. It should not be split further right now.

## Three Matrix Impact

| Matrix | Impact |
| --- | --- |
| Process matrix | Uses the new read-only leaf granularity judge before closeout. |
| Standard matrix | Keeps parent-child communication; no sibling horizontal link or release transition. |
| Guidance matrix | Marks the exchange surface wave terminal and returns the cursor to the `data_module` parent. |

## Leaf Granularity Judge Evidence

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -LeafId root.contracts.runtime_support.data_module.exchange_surface_wave -ParentId root.contracts.runtime_support.data_module -Path qrpc_runtime/src/data_module/exchange_surface.rs -Depth 4 -SameParentWaveCandidate
```

Result:

| Metric | Value |
| --- | --- |
| `normalized_split_score` | 61 |
| `decision` | `WAVE` |
| `loc` | 243 |
| `function_count` | 18 |
| `public_surface` | 11 |
| `risk_tags` | `cross_crate`, `public_api` |

Interpretation: the child has real structure, but the correct governance action is to keep it as the already-executed same-parent wave. Splitting endpoint helpers and payload parser helpers into standalone leaves would raise governance and communication cost without a stronger owner boundary.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | PASS | `exchange_surface.rs` owns exchange endpoint/provider helpers and raw payload parser helpers. |
| parent_child_communication_kept | PASS | Parent imports child helpers; child does not call sibling modules. |
| equivalence_baseline_freezable | PASS | BE-001ZG-01 baseline and BE-001ZG-02 extraction record define and verify the equivalence surface. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | PARTIAL | The surface is `pub(super)` for parent use, not a new external public API. |
| state_machine_phase | NO | No state-machine phase movement. |
| strategy_branch | NO | No strategy branch owner. |
| independent_failure_mode | PARTIAL | Endpoint construction and parser errors are distinct but share one exchange data contract. |
| reuse_pressure | LOW | Reuse pressure is currently parent-local. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | PASS | Endpoint-only and parser-only leaves would become micro leaves. |
| communication_cost_rises | PASS | Splitting would add parent forwarding and import churn. |
| local_proof_missing | NO | Local proof exists through qrpc-runtime tests and compile gates. |
| line_count_only | PASS | The remaining split case would rely mostly on function grouping, not a stronger owner. |

leaf_split_decision_result

`exchange_surface_wave stop_split: true`; `exchange_endpoints stop_split: true`; `exchange_payload_parsing stop_split: true`.

next_recursive_step

BE-001ZH-01 `root.contracts.runtime_support.data_module` parent_residual_judgment. The next queued child is expected to be `normalization`.

## Residuals

Still open under `root.contracts.runtime_support.data_module`:

- `normalization`
- `mock_data_generation`
- `historical_cache`
- `http_transport`
- `test_harness`

## Gates

- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -LeafId root.contracts.runtime_support.data_module.exchange_surface_wave -ParentId root.contracts.runtime_support.data_module -Path qrpc_runtime/src/data_module/exchange_surface.rs -Depth 4 -SameParentWaveCandidate`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-001ZH-01 `root.contracts.runtime_support.data_module` parent_residual_judgment.
