# v4.16.0 simulated_execution_engine parent residual judgment - snapshot_metrics_projection

> Batch: BE-002EP-01
> Node: `root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine`
> Selected child: `root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine.snapshot_metrics_projection`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `snapshot_metrics_projection` as the next child under `simulated_execution_engine`.

After closing `runtime_adapter`, `order_lifecycle_flow`, `market_trigger_flow`, and `fill_ledger_accounting`, the parent residual still owns the read-side projection layer for portfolio snapshots, asset curve points, and backtest microstructure metrics. This is the next strongest same-parent child because it is projection-oriented, cohesive, and can be isolated without moving direct order orchestration or validation helpers.

## Current Parent Residual Evidence

`tools/evaluate-leaf-granularity.ps1 -Path qrpc_runtime/src/v4_simulated_execution.rs -SameParentWaveCandidate` result:

| Field | Value |
| --- | --- |
| `split_decision` | `STOP` |
| `governance_packaging` | `same_parent_wave` |
| `final_decision` | `WAVE` |
| Score | `56` |
| LOC | `620` |
| Functions | `29` |
| Public surface | `4` |
| Branches | `76` |
| Domains | `8` |
| Risk tags | `cross_crate`, `persistence`, `public_api`, `route_schema`, `security`, `state_machine`, `trading_semantics` |

The parent no longer requires standalone precision splitting, but same-parent wave treatment is still justified for one clear projection child before deciding whether the remaining validation/helper surface can close.

## Candidate Boundary

`snapshot_metrics_projection` should evaluate the read/projection helper cluster:

- `record_asset_point`;
- `asset_point`;
- `snapshot`;
- `microstructure_metrics`;
- `round_money` only if the baseline confirms it is exclusively projection-owned.

The candidate owns portfolio snapshot assembly, asset curve trimming, order/fill count projection, last order/fill projection, and microstructure sample projection.

## Exclusions

The selected child must not own:

- `submit_order` or direct-submit orchestration;
- fill execution and ledger mutation already closed in `fill_ledger_accounting`;
- market trigger flow already closed in `market_trigger_flow`;
- order lifecycle internals already closed in `order_lifecycle_flow`;
- request validation and rejection helper families;
- conditional order helpers and capability derivation helpers;
- payload parsing helpers;
- runtime adapter behavior already closed in `runtime_adapter`;
- v4 tests.

## Stop Conditions

Stop and request scope review if the next baseline or extraction requires:

1. changing snapshot field names, event payload shapes, or microstructure metrics semantics;
2. moving direct-submit orchestration or validation helpers into the projection child;
3. adding child-to-child calls that bypass the simulated execution parent;
4. changing public runtime API, route/schema, persistence owner, lock owner, or runtime mode;
5. entering release-transition performance optimization.

## QPCursor

```text
QPC://v4.16.0/RM:R2/GH:G3/FFT:root.contracts.runtime_support/MT:root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine.snapshot_metrics_projection/LEAF:qrpc_runtime/src/v4_simulated_execution.rs
```

## Next Step

BE-002EQ-01 `root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine.snapshot_metrics_projection` baseline_plan.
