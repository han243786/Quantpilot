# v4.16.0 root.contracts.quantscript.v4_static_audit parent residual judgment selects memory_parser

> Batch: BE-002KJ-01
> Node: `root.contracts.quantscript.v4_static_audit`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select next child:

`root.contracts.quantscript.v4_static_audit.memory_parser`

## Parent Residual Scan

Closed children:

- `runtime_handoff_builder`;
- `audit_entrypoint`;
- `capability_type_parser`;
- `event_catalog_derivation`;
- `parser_utilities_diagnostics`;
- `static_document_parser`;
- `machine_block_parser`;
- `state_block_parser`;
- `transition_parser`.

Remaining parent-owned clusters:

- state-group parsing;
- memory field parsing;
- edge and risk-plane parsing;
- machine template wrapper and thin capability/type wrappers;
- public DTOs and local static-audit tests.

## Why This Child

`memory_parser` owns a complete field declaration rule:
`memory <name>: <type> [nullable]`.

The leaf validates required type syntax, delegates type resolution through the
parent capability/type wrapper, and constructs `MachineMemoryField`. This keeps
memory-field grammar separate from transition and graph-edge parsing without
moving DTO or type-system ownership.

## Proposed Ownership

| Field | Value |
| --- | --- |
| Candidate child | `memory_parser` |
| Planned file | `quantscript/src/v4_static_audit/memory_parser.rs` |
| Likely owned functions | `parse_memory` |
| Parent wrapper | `parse_memory` |
| Parent bridge dependencies | `parse_qs_type_ref`, `diag`, `split_words` |

## Exclusions For Next Baseline

- Do not move `parse_state_group`.
- Do not move `parse_edge`.
- Do not move `parse_risk_plane`.
- Do not move `parse_machine_template`.
- Do not move capability/type parser internals.
- Do not move public report DTOs or local tests.
- Do not introduce release-transition links.

## Next Step

BE-002KK-01 `root.contracts.quantscript.v4_static_audit.memory_parser`
baseline_plan.
