# v4.16.0 root.contracts.runtime_support.agent_module.cross_venue_arbitrage extract closeout

> Batch: BE-002AB-02
> Node: `root.contracts.runtime_support.agent_module.cross_venue_arbitrage`
> Parent: `root.contracts.runtime_support.agent_module`
> Stage: `actual_extraction`
> Movement: Rust code movement.

---

## Result

`root.contracts.runtime_support.agent_module.cross_venue_arbitrage` has been physically extracted.

New owner file:

- `qrpc_runtime/src/agent_module/cross_venue_arbitrage.rs`

Parent file:

- `qrpc_runtime/src/agent_module.rs`

## Moved Surface

Moved into the child:

- `build_arb_agent_decision`
- `build_arb_decision_from_spread_signal`
- `total_cost_buffer_ratio`

Parent changes:

- added `mod cross_venue_arbitrage;`
- replaced the inline `CrossVenueArbitrage` branch with `cross_venue_arbitrage::build_arb_agent_decision(...)`;
- removed now-unused arbitrage action imports from the parent.

## Preserved Parent Ownership

The parent still owns:

- public request/output/provider/builtin types;
- `BuiltinAgentModule::evaluate_agents` dispatch and event assembly;
- weighted signal and portfolio rebalance child dispatch;
- shared helper ownership for `available_position_ratio`, `portfolio_equity`, and `SPREAD_MULTIPLIER`;
- local test module.

The child uses parent-owned helpers only through the parent module boundary. No sibling shortcut was introduced.

## Equivalence Notes

The extraction keeps the original behavior for:

- explicit spread-signal precedence;
- Binance/OKX quote-pair inference;
- total cost buffer calculation;
- policy/default spread trigger;
- lower-price buy leg and higher-price sell leg selection;
- available sell-position clamp;
- quantity suppression for invalid or tiny ratios;
- proposed action tags and reason strings.

## Gates

Passed:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`: 227 passed, 2 ignored
- `cargo check -p quantpilot`

Remaining for commit gate:

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-002AB-03 `root.contracts.runtime_support.agent_module.cross_venue_arbitrage` single_leaf_closeout.
