# v4.16.0 root.contracts.core_ir.v1_contract single leaf closeout continues split

> Batch: BE-001TI-03
> Node: `root.contracts.core_ir.v1_contract`
> Parent: `root.contracts.core_ir`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`v1_contract` is extracted but should not be closed as a final leaf yet.

The physical move in BE-001TI-02 made `qrpc_core_ir/src/lib.rs` a facade and moved v1 Core IR into `qrpc_core_ir/src/v1.rs`. The new child is still parent-sized: it contains root graph schema/DAG validation, data/indicator/expression contracts, signal/agent/risk/execution contracts, default helpers, and local tests.

Decision:

`continue_split: true`

## Current Physical State

| Surface | Result |
| --- | --- |
| Physical owner exists | pass | `qrpc_core_ir/src/v1.rs` owns the v1 contract surface. |
| Crate facade preserved | pass | `qrpc_core_ir/src/lib.rs` re-exports `v1::*` and keeps `pub mod v4`. |
| Rust equivalence verified | pass | `cargo check -p qrpc-core-ir`, `cargo test -p qrpc-core-ir`, and smart pre-commit passed in BE-001TI-02. |
| Full workspace check preserved | pass | Smart pre-commit passed `cargo check -p quantpilot`. |

## Proposed Child Queue

| Order | Child | Owner |
| --- | --- | --- |
| 1 | `root.contracts.core_ir.v1_contract.root_graph_contract` | v1 constants, `CoreStrategyIr`, `CoreIREdge`, metadata/source DTOs, constructor, and DAG validation. |
| 2 | `root.contracts.core_ir.v1_contract.data_indicator_expression_contract` | data binding, indicator, series/custom/scalar expression DTOs, helper builders, field/aggregation/comparison enums. |
| 3 | `root.contracts.core_ir.v1_contract.policy_execution_contract` | signal, agent, risk, execution DTOs, policy enums, and default helpers. |
| 4 | `root.contracts.core_ir.v1_contract.test_fixture` | local v1 round-trip regression test. |

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | Proposed child owners map to separable v1 schema, helper, policy/execution, and test regions. |
| parent_child_communication_kept | pass | Future children can be mediated through the v1 parent with `pub use` exports. |
| equivalence_baseline_freezable | pass | Each child has concrete public names and local test gates. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | pass | v1 Core IR public names remain exported from qrpc-core-ir. |
| state_machine_phase | guarded | DAG validation is graph validation, not mutable runtime state. |
| strategy_branch | pass | v1 models strategy graph semantics. |
| independent_failure_mode | pass | Root graph/DAG drift, expression helper drift, policy DTO drift, and test fixture drift are separable. |
| reuse_pressure | pass | Compiler/runtime bridge paths consume this surface. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | v1 still has multiple clear child owners. |
| communication_cost_rises | no | A parent facade can re-export child names without changing consumers. |
| local_proof_missing | no | `cargo test -p qrpc-core-ir` covers the local move. |
| line_count_only | no | Continue split is based on mixed responsibilities, not raw line count. |

leaf_split_decision_result

`continue_split: true`; `v1_contract` remains a local parent. Select `root_graph_contract` next.

## Next Step

BE-001TJ-01 `root.contracts.core_ir.v1_contract` parent_residual_judgment selects `root_graph_contract`.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
