# v4.16.0 root.contracts.runtime_support.fill_engine parent residual judgment selects fill_report_execution_helpers

> Batch: BE-002BH-01
> Node: `root.contracts.runtime_support.fill_engine`
> Parent: `root.contracts.runtime_support`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> Selected child: `root.contracts.runtime_support.fill_engine.fill_report_execution_helpers`

---

## Decision

`root.contracts.runtime_support.fill_engine.fill_report_execution_helpers` is selected as the next fill-engine child.

This is a judgment step only. It does not authorize code movement yet.

## Evidence

Closed fill-engine children:

- `event_projection_wave`
- `portfolio_reservation_accounting`

Remaining queue from the frozen fill-engine baseline:

1. `fill_report_execution_helpers`
2. `test_harness`

Current candidate helper group:

- `execution_status_for`
- `market_state_for`
- `executable_qty`
- `execution_assumptions_for_order`
- `sim_order_from_open`
- `is_marketable`
- `can_rest`
- `build_fill_report`
- `build_fill_report_from_open`

Baseline decision items:

- determine whether all helper functions move together or whether state/orchestration dependencies require a smaller wave;
- keep public `apply_fill_to_portfolio` parent-owned;
- keep closed event/accounting children parent-mediated and avoid sibling shortcuts.

## Boundary Rules

| Boundary | Requirement |
| --- | --- |
| Parent orchestration | `FillEngine::submit_plan`, `FillEngine::on_market_update`, and `try_open_resting_order` remain parent-owned. |
| Event projection | Closed `event_projection_wave` remains outside this movement. |
| Accounting | Closed `portfolio_reservation_accounting` remains outside this movement. |
| Public API | `apply_fill_to_portfolio` stays parent-owned. |
| Communication | Candidate helpers may become `pub(super)` child functions consumed only by the parent. |
| Sibling shortcuts | The child must not import event or accounting siblings directly. |

## Next Step

BE-002BI-01 `root.contracts.runtime_support.fill_engine.fill_report_execution_helpers` baseline_plan.
