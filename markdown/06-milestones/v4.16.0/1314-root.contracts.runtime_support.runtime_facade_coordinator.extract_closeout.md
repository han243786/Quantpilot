# v4.16.0 root.contracts.runtime_support.runtime_facade_coordinator extract closeout

> Batch: BE-001YF-02
> Node: `root.contracts.runtime_support.runtime_facade_coordinator`
> Parent: `root.contracts.runtime_support`
> Stage: `actual_extraction`
> Movement: Code movement complete.

## Code Movement

Created:

- `qrpc_runtime/src/runtime_facade_coordinator.rs`

Changed:

- `qrpc_runtime/src/lib.rs`

Moved from `qrpc_runtime/src/lib.rs` into the new child:

- `ConfigGenerationEntry`
- `RuntimeCoordinator`
- `impl Debug for RuntimeCoordinator`
- all `RuntimeCoordinator` constructors and provider-injection APIs
- session and cycle orchestration
- execution submission and market data update entry points
- runtime state getters and provider getters
- risk mode and risk monitor controls
- module config swap/apply/generation history behavior
- execution assumptions setter
- portfolio update event projection
- private coordinator helpers, including provider delegation, merge fallback, risk/execution planning, portfolio refresh, equity estimate, and quote-price mapping
- crate-root coordinator tests

Stayed in `qrpc_runtime/src/lib.rs`:

- module declarations
- public re-export policy
- `set_mock_volatility`
- all non-coordinator runtime support re-exports
- crate-root availability for `RuntimeCoordinator` and `ConfigGenerationEntry`

## Public Surface

Preserved:

- `qrpc_runtime::RuntimeCoordinator`
- `qrpc_runtime::ConfigGenerationEntry`
- every public `RuntimeCoordinator::*` method covered by BE-001YF-01
- existing sandbox references through `crate::RuntimeCoordinator`
- existing backend/test-runner imports through crate-root exports

## Boundary Notes

The extracted child imports sibling implementation modules through the runtime-support parent crate path. It does not create direct child-to-child shortcuts outside the crate parent boundary.

The root facade now re-exports:

- `pub use runtime_facade_coordinator::{ConfigGenerationEntry, RuntimeCoordinator};`

No release-transition optimization was introduced.

## Gates

- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime`: PASS, 227 passed, 2 ignored
- `cargo check -p quantpilot`: PASS
- `git diff --check`: PASS
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`: PASS
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`: PASS

The full feature tree gate initially failed because the new active file was not registered; this closeout registers it and requires a rerun before commit.

## Next Step

BE-001YF-03 `root.contracts.runtime_support.runtime_facade_coordinator` single_leaf_closeout.
