# v4.16.0 root.contracts.core_ir.v4_contracts baseline plan

> Batch: BE-001TT-01
> Node: `root.contracts.core_ir.v4_contracts`
> Parent: `root.contracts.core_ir`
> Stage: `baseline_plan`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts` is frozen as the next recursive parent after `v1_contract` closeout.

The selected physical owner is `qrpc_core_ir/src/v4.rs`. It is parent-sized and must be split recursively by contract family while preserving `qrpc_core_ir::v4::*`.

Decision:

`baseline_frozen: true`

Next selected child:

`root.contracts.core_ir.v4_contracts.schema_identity_constants`

## Physical Scope

| Child | Physical area | Current owner note |
| --- | --- | --- |
| `contracts.core_ir.v4_contracts.schema_identity_constants` | `qrpc_core_ir/src/v4.rs` lines near top | v4 schema version strings, compat machine/event identifiers, risk-plane/type limits. |
| `contracts.core_ir.v4_contracts.backtest_artifact_contract` | `qrpc_core_ir/src/v4.rs` early DTOs | v4 backtest artifact DTOs and nested metric/trajectory/risk/capability records. |
| `contracts.core_ir.v4_contracts.machine_contract` | `qrpc_core_ir/src/v4.rs` machine DTOs and `V4MachineContract` impl | machine templates, machine state/transition/action/memory DTOs, machine validation. |
| `contracts.core_ir.v4_contracts.machine_graph_contract` | `qrpc_core_ir/src/v4.rs` graph DTOs and graph impl | machine graph DTOs, event catalog DTOs, graph validation helpers. |
| `contracts.core_ir.v4_contracts.qs_state_machine_profile` | `qrpc_core_ir/src/v4.rs` QS policy structs and impls | QS machine profile defaults and validation. |
| `contracts.core_ir.v4_contracts.runtime_mode_contract` | `qrpc_core_ir/src/v4.rs` runtime mode structs/enums/defaults | runtime trading modes, execution event taxonomy, runtime mode validation. |
| `contracts.core_ir.v4_contracts.qs_type_system_contract` | `qrpc_core_ir/src/v4.rs` type-system DTOs/defaults/impl | scalar/composite type taxonomy, type refs, nesting validation. |
| `contracts.core_ir.v4_contracts.static_bundle_and_manifest` | `qrpc_core_ir/src/v4.rs` manifest/plugin/repro/complexity/learning/static bundle | default builders and static contract bundle validators. |
| `contracts.core_ir.v4_contracts.capability_report_contract` | `qrpc_core_ir/src/v4.rs` capability request/report DTOs and impls | compile-time capability request/report diagnostics and validators. |
| `contracts.core_ir.v4_contracts.core_ir_compat_bridge` | `qrpc_core_ir/src/v4.rs` bridge functions | v1 Core IR validation, v4 machine graph construction, compat metadata helpers. |
| `contracts.core_ir.v4_contracts.venue_capability_matrix` | `qrpc_core_ir/src/v4.rs` venue capability DTOs and helpers | first-wave execution capabilities, unsupported matrix, matrix validation. |
| `contracts.core_ir.v4_contracts.default_helper_surface` | `qrpc_core_ir/src/v4.rs` default helper functions | serde default helpers shared by v4 contract DTOs. |
| `contracts.core_ir.v4_contracts.test_fixture` | `qrpc_core_ir/src/v4.rs` cfg-test module | v4 contract, bridge, runtime/type/venue regression tests. |

## Key Public Surfaces To Track

| Surface | Public examples |
| --- | --- |
| schema identity constants | `V4_MACHINE_CONTRACT_VERSION`, `V4_STATIC_CONTRACT_BUNDLE_VERSION`, `V4_CORE_IR_COMPAT_BRIDGE_VERSION`, compat machine IDs/events. |
| machine contract | `V4MachineContract`, `MachineState`, `StateGroup`, `MachineTransition`, `V4MachineContract::validate_static_contract`. |
| machine graph contract | `V4MachineGraphContract`, `MachineGraphEdge`, `MachineEventCatalog`, graph validation helpers. |
| QS/runtime/type contracts | `QsStateMachineProfile`, `RuntimeModeContract`, `QsTypeSystemContract`, defaults and validators. |
| static bundle contracts | `V4StaticContractBundle`, version manifest, plugin governance, reproducibility, complexity, learning contracts. |
| capability report | `V4CompileTimeCapabilityRequest`, `V4CompileTimeCapabilityReport`, diagnostics, verdicts, capability entries. |
| Core IR bridge | `bridge_core_ir_to_v4_machine_graph`, bridge diagnostics and compatibility report. |
| venue matrix | `VenueCapabilityMatrix`, `ExecutionCapabilityKind`, first-wave capability helpers. |
| local tests | v4 machine, graph, QS profile, type system, runtime mode, static bundle, venue, bridge, and learning regression tests. |

## Recursive Child Queue

| Order | Child | Stage to enter | Split note |
| --- | --- | --- | --- |
| 1 | `schema_identity_constants` | `parent_residual_judgment` | Small stable public constants, no behavior, low-risk first extraction. |
| 2 | `backtest_artifact_contract` | `baseline_plan` | Compact DTO family before machine validators. |
| 3 | `machine_contract` | `baseline_plan` | Parent-sized validation family; may split states/transitions/actions/validation helpers if needed. |
| 4 | `machine_graph_contract` | `baseline_plan` | Parent-sized graph/event validation family; may split graph DTOs and event catalog. |
| 5 | `qs_state_machine_profile` | `baseline_plan` | QS profile policy/default validation family. |
| 6 | `runtime_mode_contract` | `baseline_plan` | Runtime mode schema/default/validation family. |
| 7 | `qs_type_system_contract` | `baseline_plan` | Type-system schema/default/validation family. |
| 8 | `static_bundle_and_manifest` | `baseline_plan` | Large bundle/manifest/plugin/repro/complexity/learning family. |
| 9 | `capability_report_contract` | `baseline_plan` | Capability request/report DTOs and validators. |
| 10 | `core_ir_compat_bridge` | `baseline_plan` | v1-to-v4 bridge validation/build family. |
| 11 | `venue_capability_matrix` | `baseline_plan` | Venue capability taxonomy and matrix validation family. |
| 12 | `default_helper_surface` | `parent_residual_judgment` | Only if default helpers remain parent-owned after family extraction. |
| 13 | `test_fixture` | `baseline_plan` | Only after production contract families close. |

## Hard Boundaries

This baseline must not:

- edit Rust source code;
- change v4 public names, constants, fields, enum variants, serde attributes, validation diagnostics, validation ordering, default values, helper behavior, bridge output shape, or tests;
- change v1 contracts, qrpc-core, compiler, runtime, QuantScript, backend, executor, frontend, physical `plugins/*`, or release transition;
- introduce direct sibling shortcuts;
- move production code into cfg-test modules.

## Equivalence Baseline

| Area | Frozen behavior |
| --- | --- |
| v4 public module | Existing `qrpc_core_ir::v4::*` surface must remain available. |
| serde shape | All derives, tags, rename rules, defaults, and skip rules remain unchanged. |
| validation semantics | Existing validator diagnostics, ordering, and failure conditions remain unchanged. |
| default builders | Existing default contracts and helper values remain unchanged. |
| bridge behavior | Core IR v1 bridge validation and generated machine graph/event catalog shape remain unchanged. |
| tests | Existing `cargo test -p qrpc-core-ir` v4 tests must continue passing. |

## Same-Parent Parallel Wave Judgment

`same_parent_parallel_children` remains inactive for the first v4 step.

Reason: the first v4 extraction establishes the parent facade pattern for `v4.rs`. Serial extraction protects `qrpc_core_ir::v4::*` public export compatibility before considering parallel sibling batches.

## Split Rule Pre-Check

| Rule | Baseline result |
| --- | --- |
| Physical owner | Continue. `v4.rs` is parent-sized and mixed-responsibility. |
| Public surface | Public v4 contract API consumed by downstream crates. |
| Mixed responsibility | Continue. At least thirteen natural contract-family children exist. |
| State ownership | Guarded. State-machine DTOs are pure schema/validation, not mutable runtime state. |
| Side effects | No IO, locks, async lifecycle, or persistence in selected extraction. |

## Next Step

BE-001TU-01 `root.contracts.core_ir.v4_contracts` parent_residual_judgment selects `schema_identity_constants`.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
