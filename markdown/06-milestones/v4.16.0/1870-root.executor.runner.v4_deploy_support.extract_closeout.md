# v4.16.0 root.executor.runner.v4_deploy_support extract closeout

> Batch: BE-002LN-02
> Node: `root.executor.runner.v4_deploy_support`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> governance_next_authority: active

---

## Movement

The v4 deploy/preflight/static bundle helper surface has been extracted from
`src-executor/main.rs` into:

`src-executor/v4_deploy_support.rs`

The parent keeps route registration, route modules, runner setup, provider
helpers, and the shared audit append helper.

## Ownership Now

| Field | Value |
| --- | --- |
| Child file | `src-executor/v4_deploy_support.rs` |
| Owned helpers | v4 deploy request DTO, deploy detection, deploy registration, execution mode parsing, graph/source resolution, empty Core IR construction, strategy config preflight gate, JSON path helpers, static bundle helpers |
| Parent retained | route registration, route modules, runner setup, provider helpers, audit helper, runner support files |
| Parent route modules | `strategy_lifecycle_routes` now calls v4 helpers through `v4_deploy_support` |

## Preserved Behavior

- v4 deployment request detection behavior is unchanged.
- v4 graph/source resolution behavior is unchanged.
- Execution mode parsing and live-mode rejection behavior are unchanged.
- v4 strategy registration payload behavior is unchanged.
- Strategy config preflight start gate behavior is unchanged.
- JSON path helper semantics are unchanged.
- Executor v4 static bundle construction behavior is unchanged.

## Gates

| Gate | Result |
| --- | --- |
| `cargo fmt --all --check` | PASS |
| `cargo check -p quantpilot --bin executor` | PASS |
| `git diff --check` | PASS |

## Next Step

BE-002LN-03 `root.executor.runner.v4_deploy_support`
single_leaf_closeout.
