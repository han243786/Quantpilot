# v4.16.0 root.contracts.quantscript.typed_resolution.semantic_inference_surface extract closeout

> Batch: BE-002GH-02
> Node: `root.contracts.quantscript.typed_resolution.semantic_inference_surface`
> Parent: `root.contracts.quantscript.typed_resolution`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> governance_next_authority: active

---

## Movement

Created:

- `quantscript/src/resolve/semantic_inference_surface.rs`

Updated:

- `quantscript/src/resolve/mod.rs`

Moved expression semantic inference and manual indicator formula helper methods into the child module. The parent now declares `mod semantic_inference_surface;`; the child provides `pub(super)` resolver helper methods through an `impl Resolver` block so the parent resolver orchestration can call the same methods without widening crate public API.

## Moved Owner Surface

Moved only the baseline-frozen semantic inference pocket:

- expression semantic classification;
- manual MACD line/signal/histogram formula inference;
- manual moving-average and z-score formula inference;
- window aggregate, boundary lookback, smoothed change, alias, series view, and target-expression shape helpers;
- EMA-like helper name classification for semantic inference.

## Preserved Boundary

Preserved:

- `lower_script_to_typed_hir` public entrypoint;
- `Resolver::resolve_module`, `resolve_function`, `lower_block`, `lower_stmt`, `lower_expr`, and `lower_callee_expr` behavior;
- diagnostics and `expr_semantics` map insertion behavior;
- public DTO/enums in `quantscript/src/resolve/public_type_surface.rs`;
- callable/helper classification and registry seeding in `quantscript/src/resolve/callable_classification_surface.rs`;
- type inference, binding insertion, condition validation, scalar helpers, ID allocation, callable target helpers, resolved function builder, return-list helpers, and resolver-local tests.

Excluded from movement:

- analysis, lowering, evaluator, runtime, backend, executor, frontend, plugin metadata, and release transition.

## Evidence

- `cargo fmt --check` PASS
- `cargo test -p quantscript` PASS, 88 tests
- `cargo check -p quantpilot` PASS

## Next Step

BE-002GH-03 `root.contracts.quantscript.typed_resolution.semantic_inference_surface` single_leaf_closeout.
