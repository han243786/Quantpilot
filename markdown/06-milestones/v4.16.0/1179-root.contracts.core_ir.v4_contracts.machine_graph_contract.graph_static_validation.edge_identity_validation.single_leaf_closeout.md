# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.edge_identity_validation single leaf closeout stops split

> Batch: BE-001UY-03
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.edge_identity_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.edge_identity_validation`
stops splitting after the extracted child reaches a compact single-loop edge identity boundary.

The child owns:

- edge ID required and duplicate edge ID checks
- edge event type required checks
- source/target machine endpoint existence checks
- self-loop rejection
- the local duplicate edge ID set

No internal child has enough independent ownership to justify another recursive split.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `edge_identity_validation` is a named white-box child for graph edge inventory invariants. |
| parent_child_communication_kept | pass | The parent passes only `machines_by_id`; the child returns `Vec<String>`. |
| equivalence_baseline_freezable | pass | BE-001UY-01 froze all edge identity error strings and branch order. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | no | The public graph validator remains in the parent. |
| state_machine_phase | guarded | Edge identity validation is one graph-static phase, already isolated as a leaf. |
| strategy_branch | no | The child does not branch into separate strategy families. |
| independent_failure_mode | guarded | Edge ID, endpoint, event type, and self-loop failures are edge inventory invariants in one loop. |
| reuse_pressure | no | No second caller or reusable public surface exists. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | yes | Splitting ID/event/endpoint/self-loop checks would create tiny helpers without stable standalone owners. |
| communication_cost_rises | yes | Further split would pass edge context and machine map through multiple private helpers for no behavior gain. |
| local_proof_missing | no | `cargo test -p qrpc-core-ir` passed after extraction. |
| line_count_only | no | The stop decision is ownership-based, not line-count based. |

leaf_split_decision_result

`stop_split: true`

This leaf is complete. Return to the graph static validation parent for residual judgment.

next_recursive_step

BE-001UZ-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation single_leaf_closeout
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/edge_identity_validation.rs`

**Markers**:
- `BE-001UY-03`

**Next step**:
BE-001UZ-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation single_leaf_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
