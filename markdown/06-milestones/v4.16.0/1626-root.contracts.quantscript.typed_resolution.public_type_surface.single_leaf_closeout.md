# v4.16.0 root.contracts.quantscript.typed_resolution.public_type_surface single leaf closeout

> Batch: BE-002GD-03
> Node: `root.contracts.quantscript.typed_resolution.public_type_surface`
> Parent: `root.contracts.quantscript.typed_resolution`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Close `root.contracts.quantscript.typed_resolution.public_type_surface` with `stop_split: true`.

The child now owns only the public typed-resolution DTO and enum surface in:

- `quantscript/src/resolve/public_type_surface.rs`

## Terminal Leaf Control

`tools/evaluate-leaf-granularity.ps1` returned:

- decision: `STOP`
- split decision: `STOP`
- governance packaging: `stop_split`
- score: 35
- effective LOC: 157
- functions: 1
- public surface: 41
- domains: 2
- risk tags: cross-crate, public API

## Why No Further Split

| Rule | Judgment |
| --- | --- |
| Target size | Pass. 157 LOC is within the terminal target range. |
| Cohesion | Pass. The file is one public resolver type contract. |
| Split benefit | Low. Splitting enums from DTO structs would create artificial micro leaves. |
| Governance cost | High relative to benefit because the surface is public but not behavior-heavy. |
| Parent communication | Pass. Parent re-exports the child; downstream `crate::resolve::*` imports remain stable. |

## Evidence

BE-002GD-02 verified:

- `cargo fmt --check` PASS
- `cargo test -p quantscript` PASS, 88 tests
- `cargo check -p quantpilot` PASS
- `git diff --check` PASS
- UTF-8 check PASS
- full feature tree check PASS
- matrix governance check PASS

## Residuals

Remaining typed-resolution internal families:

- callable classification helper functions;
- resolver orchestration and AST-to-HIR lowering;
- type inference and binding insertion;
- expression semantic inference;
- manual indicator formula inference;
- callable registry and helper classification tables;
- resolver-local test harness.

## Next Step

BE-002GE-01 `root.contracts.quantscript.typed_resolution` parent_residual_judgment.
