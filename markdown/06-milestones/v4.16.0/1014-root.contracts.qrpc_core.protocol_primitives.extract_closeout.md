# v4.16.0 root.contracts.qrpc_core.protocol_primitives extract closeout

> Batch: BE-001RO-02
> Node: `root.contracts.qrpc_core.protocol_primitives`
> Parent: `root.contracts.qrpc_core`
> Stage: `extract_closeout`
> Movement: Rust code extraction.

---

## Summary

`root.contracts.qrpc_core.protocol_primitives` has been extracted from `qrpc_core/src/lib.rs` into a private child module.

The qrpc-core crate root preserves the public facade:

- `mod protocol_primitives;`
- `pub use protocol_primitives::*;`

## Files Changed

- `qrpc_core/src/lib.rs`
- `qrpc_core/src/protocol_primitives.rs`

## Extraction Result

`qrpc_core/src/protocol_primitives.rs` now owns:

- qrpc-core version constants;
- `Exchange`;
- `Symbol`, `Symbol::parse`, `Symbol::as_str`, and `Symbol` serde behavior;
- `MarketType`;
- `RebalanceSchedule`;
- `DataKind`;
- `IntentKind`;
- `SignalSide`;
- `DecisionStatus`;
- `RiskDecisionMode` and its default;
- `OrderSide` and its display behavior;
- `OrderType`;
- `TimeInForce`;
- `ExecutionStatus`;
- `RiskReasonCode`;
- `SourceStatus`;
- `SourceHealth` and its default.

`qrpc_core/src/lib.rs` keeps later qrpc-core residuals in place:

- `DataQualitySnapshot`;
- runtime protocol config;
- artifact specs;
- runtime IO DTOs;
- RFC execution contracts;
- crate-root tests.

## Rust Local Fields

| Field | Result |
| --- | --- |
| Crate | `qrpc-core` |
| Parent facade | Preserved. `lib.rs` remains the crate root facade and re-export owner. |
| Child visibility | The module is private; public item surfaces are preserved through `pub use protocol_primitives::*;`. |
| Public exports | Existing crate-root public names remain available. |
| Parent-owned residuals | Runtime config, artifact specs, runtime IO, RFC execution contracts, and tests remain parent-owned for later recursion. |
| Sibling dependency check | Passed. No direct sibling shortcut to later qrpc-core residuals was added. |

## Equivalence Preservation

| Surface | Preserved evidence |
| --- | --- |
| Version constants | Values moved unchanged into the child module. |
| Symbol behavior | Parse/as_str/serde behavior moved unchanged. |
| Primitive enum serde | Derives and serde attributes moved unchanged. |
| Defaults | `RiskDecisionMode::default` and `SourceHealth::default` moved unchanged. |
| OrderSide display | Display implementation moved unchanged. |
| Public facade | `pub use protocol_primitives::*;` preserves crate-root access. |
| Later qrpc-core schemas | No runtime config, artifact, runtime IO, RFC, digest, or test body was moved. |

## Hard Boundaries Held

BE-001RO-02 did not:

- change version strings, enum variants, serde attributes, symbol parsing behavior, display behavior, default values, public exports, tests, digest behavior, or schema field shapes;
- move `DataQualitySnapshot`, `DataSourceConfig`, runtime protocol config structs, artifact specs, runtime IO DTOs, RFC execution contracts, or tests;
- change closed error/proto/plugin/strategy IR contracts;
- change `qrpc_core_ir`, `qrpc_compiler`, `qrpc_runtime`, `quantscript`, backend, executor, frontend, physical `plugins/*`, or release transition.

## Gates

- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-pre-commit-hook.ps1`

## Next Step

BE-001RO-03 `root.contracts.qrpc_core.protocol_primitives` single_leaf_closeout.
