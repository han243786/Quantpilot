# v4.16.0 root.contracts.core_ir.v4_contracts.compile_time_capability_report.report_validation equivalence baseline and extraction plan

> Batch: BE-001WF-01
> Node: `root.contracts.core_ir.v4_contracts.compile_time_capability_report.report_validation`
> Parent: `root.contracts.core_ir.v4_contracts.compile_time_capability_report`
> Stage: `baseline_plan`
> Movement: No code movement.

---

## Summary

This baseline freezes the `report_validation` extraction boundary before code movement.

Frozen public behavior:

- `V4CompileTimeCapabilityReport::validate_for_compile`

Frozen validation behavior:

- report schema version must equal `V4_COMPILE_TIME_CAPABILITY_REPORT_VERSION`
- verdict must be `Accepted`
- graph id, venue id, and runtime mode must resolve
- execution submission must not be attached
- error diagnostics are surfaced as `code target: message`
- each required execution capability must have a required supported execution entry
- each required plugin id must have a required accepted plugin entry

Allowed BE-001WF-02 movement:

- add `qrpc_core_ir/src/v4/compile_time_capability_report/report_validation.rs`
- add nested `mod report_validation;`
- move only `V4CompileTimeCapabilityReport::validate_for_compile`

Forbidden movement:

- request/report DTOs
- status/entry/diagnostic DTOs
- compile-time default helpers
- report builder
- tests, sample builders, static bundle implementation, and Core IR bridge code

Equivalence proof target:

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `cargo check -p quantpilot`

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/compile_time_capability_report.rs`

**Markers**:
- `BE-001WF-01`

**Next step**:
BE-001WF-02 root.contracts.core_ir.v4_contracts.compile_time_capability_report.report_validation extract_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
