# v4.16.0 data_module.source_mapping extract closeout

> Batch: BE-001ZA-02
> Node: `root.contracts.runtime_support.data_module.source_mapping`
> Parent: `root.contracts.runtime_support.data_module`
> Stage: `actual_extraction`
> Movement: Code movement complete.

## Extracted Files

- `qrpc_runtime/src/data_module/source_mapping.rs`

## Code Movement

The Core IR to runtime data source mapping surface moved out of `qrpc_runtime/src/data_module.rs` into a child module:

- `data_sources_from_core_ir`
- `parse_exchange_hint`
- `parse_symbol_hint`
- `parse_bool_hint`
- `parse_u64_hint`

The parent now declares `mod source_mapping;` and keeps `pub(crate) use source_mapping::data_sources_from_core_ir;`.

## Public API Equivalence

No crate-visible call site changed.

`data_sources_from_core_ir` remains available through the parent `data_module` boundary.

## Boundary Result

The child owns only source mapping and source-hint parsing.

The parent still owns:

- provider trait and `BuiltinDataModule`
- collection orchestration
- cache and diagnostics
- exchange endpoint and payload parsing
- normalization
- mock data and historical cache support
- data-module tests

## Equivalence Gates

Required gates for this batch:

- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-001ZA-03 `root.contracts.runtime_support.data_module.source_mapping` single_leaf_closeout.
