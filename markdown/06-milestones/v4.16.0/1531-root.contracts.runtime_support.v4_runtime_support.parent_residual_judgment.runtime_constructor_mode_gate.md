# v4.16.0 root.contracts.runtime_support.v4_runtime_support parent residual judgment - runtime_constructor_mode_gate

> Batch: BE-002DO-01
> Node: `root.contracts.runtime_support.v4_runtime_support`
> Selected child: `root.contracts.runtime_support.v4_runtime_support.runtime_constructor_mode_gate`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.

---

## Decision

Select `root.contracts.runtime_support.v4_runtime_support.runtime_constructor_mode_gate` as the next v4 runtime support child.

## Evidence

`type_surface` and `graph_symbol_expansion` are closed. The BE-002DH-01 v4 runtime support baseline queue names `runtime_constructor_mode_gate` next.

The candidate currently lives in `qrpc_runtime/src/v4_runtime.rs` and owns:

- `V4PaperSimulatedRuntime::new`
- `V4PaperSimulatedRuntime::new_for_mode`
- `V4PaperSimulatedRuntime::new_for_mode_inner`
- `V4PaperSimulatedRuntime::new_with_execution_capabilities`
- `V4PaperSimulatedRuntime::new_for_mode_with_execution_capabilities`
- `V4PaperSimulatedRuntime::new_for_backtest`
- runtime mode guard behavior for `PaperSimulated` and `LiveActual`
- required risk-plane and execution-capability policy preconditions
- initial runtime state construction

This is a coherent constructor and mode-gating owner. It should be handled before replay orchestration, transition engine, risk/execution gate behavior, and simulated execution behavior because those later children depend on the initial runtime state shape.

## Boundary

The next baseline must freeze only constructor, mode gate, initial state, and capability-policy attachment behavior before movement. It must not move:

- public v4 type surface;
- graph symbol expansion;
- event replay orchestration;
- machine transition engine;
- risk/execution runtime decision evaluation;
- simulated execution engine internals;
- v4 tests.

## Dependency Rule

The constructor/mode gate child may use v4 type surface DTOs, graph validation, private runtime state structs, and execution capability policy structs only through the v4 runtime support parent. It must not call live execution, plugin runtime, ops compatibility, metrics, QuantScript, plugin metadata, executor, backend, frontend, or release-transition optimization paths.

## Next Step

BE-002DP-01 `root.contracts.runtime_support.v4_runtime_support.runtime_constructor_mode_gate` baseline_plan.
