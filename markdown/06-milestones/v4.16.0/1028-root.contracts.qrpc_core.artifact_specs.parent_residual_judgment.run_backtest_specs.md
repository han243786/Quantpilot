# v4.16.0 root.contracts.qrpc_core.artifact_specs parent residual judgment selects run_backtest_specs

> Batch: BE-001RV-01
> Node: `root.contracts.qrpc_core.artifact_specs`
> Selected child: `root.contracts.qrpc_core.artifact_specs.run_backtest_specs`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core.artifact_specs` returns to its child queue after `canonical_digest` closeout.

Decision:

`next_child: root.contracts.qrpc_core.artifact_specs.run_backtest_specs`

## Closed Artifact Specs Children

| Child | Result |
| --- | --- |
| `contracts.qrpc_core.artifact_specs.canonical_digest` | Closed with `stop_split: true`; owns digest algorithm, digest DTO, and canonical JSON SHA-256 helper. |

## Open Artifact Specs Children

| Child | Status |
| --- | --- |
| `contracts.qrpc_core.artifact_specs.run_backtest_specs` | Selected next. Owns run/backtest modes, dataset and execution assumption projections, market data snapshot specs, `RunSpec`, and `BacktestSpec`. |
| `contracts.qrpc_core.artifact_specs.artifact_bundle_contract` | Queued. Owns strategy/core-IR/compile artifact DTOs and `CompileArtifactBundle`. |

## Selection Rationale

`run_backtest_specs` is selected because it is the next contiguous public schema/projection family after canonical digest:

- it owns `RunModeSpec` and `BacktestReplaySource`;
- it owns `StrategyArtifactSourceKind` while artifact bundle source-kind DTOs remain adjacent until the child baseline decides final placement;
- it owns `DatasetSpec` and `DatasetSpec::from`;
- it owns `ExecutionAssumptionSpec`, `ExecutionAssumptionValueSource`, `ExecutionAssumptionSourceSummary`, and `ExecutionAssumptionSpec::from`;
- it owns `MarketDataSnapshotSpec` and `MarketDataSnapshotSpec::from_runtime_protocol`;
- it owns `RunSpec`, `RunSpecRuntimeProtocolInput`, and `RunSpec::from_runtime_protocol`;
- it owns `BacktestSpec` and `BacktestSpec::new`;
- it can move before artifact bundle DTOs because bundle DTOs consume `ArtifactDigest`, `CoreStrategyIr`, and runtime config through public names.

## Same-Parent Parallel Judgment

`same_parent_parallel_children` remains inactive for this selection.

Run/backtest specs carry public projection behavior and schema version use. Serial extraction protects `run_and_backtest_specs_capture_protocol_boundary` before artifact bundle DTOs move.

## Hard Boundaries

The next `run_backtest_specs` baseline must not:

- edit Rust source code;
- change enum variants, serde attributes, schema version use, field names, projection behavior, constructor behavior, public exports, or tests;
- move artifact bundle DTOs, runtime IO DTOs, RFC execution contracts, or crate-root tests;
- change closed canonical digest, other qrpc-core children, or release transition.

## Next Step

BE-001RW-01 `root.contracts.qrpc_core.artifact_specs.run_backtest_specs` baseline_plan.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
