# v4.16.0 root.contracts.quantscript.runtime_lowering parent residual judgment: intent_inference

> Batch: BE-002IU-01
> Node: `root.contracts.quantscript.runtime_lowering`
> Selected child: `root.contracts.quantscript.runtime_lowering.intent_inference`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `intent_inference` as the next child under `runtime_lowering`.

`lowering_orchestrator_surface` and `universe_lowering` are closed. The remaining runtime lowering subtree still contains several production owners. `quantscript/src/lowering/intents.rs` is the next frozen queue item and is oversized, high risk, and cohesive enough to require precision single-leaf governance before any Rust movement.

## White-Box Boundary

| Field | Boundary |
| --- | --- |
| Input | QuantScript statements, conditions, emit-intent calls, binding environment, data sources, and runtime lowering context |
| Output | `IntentConfig` map, inferred agent params, canonicalized data sources, and validation diagnostics |
| Current owner | `quantscript/src/lowering/intents.rs` |
| Planned owner | `root.contracts.quantscript.runtime_lowering.intent_inference` |
| Parent caller | `runtime_lowering` module facade through `mod intents` and existing re-exports |
| Exclusions | binding source decoding, indicator binding resolution, manual formula fallback, source recovery bridge, semantic bridge, helper env, context surface, shared arg helpers, diagnostic formatting, and already closed orchestrator/universe children |

## Leaf Granularity Evidence

Command:

- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -LeafId root.contracts.quantscript.runtime_lowering.intent_inference -ParentId root.contracts.quantscript.runtime_lowering -Path quantscript/src/lowering/intents.rs -Depth 4`

Result:

| Field | Value |
| --- | --- |
| `split_decision` | `CONTINUE` |
| `governance_packaging` | `precision_single_leaf` |
| `final_decision` | `PRECISION` |
| `normalized_split_score` | 56 |
| `loc` | 1370 |
| `function_count` | 40 |
| `public_surface` | 3 |
| `branches` | 134 |
| `risk_tags` | `cross_crate`, `public_api`, `security`, `state_machine`, `trading_semantics` |
| `reason` | oversized high-risk leaf requires precision baseline before movement |

## Split Decision Gate

| Gate | Result | Evidence |
| --- | --- | --- |
| `oversized_leaf` | PASS | `intents.rs` is above the 800 LOC split-pressure threshold. |
| `strong_owner_boundary` | PASS | Intent inference owns signal/intent extraction, spread intent recognition, legacy emit handling, and inferred agent params. |
| `batch_downgrade_forbidden` | PASS | The leaf is high risk and must use precision single-leaf governance before movement. |
| `parent_child_boundary` | PASS | Parent remains the `runtime_lowering` module facade; no sibling horizontal connection is proposed. |

## Risk Classification

| Risk | Result |
| --- | --- |
| Public API / route / schema | Public(crate) intent inference entrypoints and runtime config shape must remain stable |
| Persistence / lock owner | No persistence ownership movement planned |
| Trading semantics evidence | High; inferred intents affect strategy signal and action behavior |
| Cross-crate behavior | Present through `qrpc_core` runtime protocol config contracts |

## Next Step

BE-002IV-01 `root.contracts.quantscript.runtime_lowering.intent_inference` baseline_plan.
