# v4.16.0 root.contracts.quantscript.runtime_lowering parent residual judgment: manual_formula_fallback

> Batch: BE-002JD-01
> Node: `root.contracts.quantscript.runtime_lowering`
> Selected child: `root.contracts.quantscript.runtime_lowering.manual_formula_fallback`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `manual_formula_fallback` as the next child under `runtime_lowering`.

`lowering_orchestrator_surface`, `universe_lowering`, and `intent_inference` are closed. The remaining runtime lowering subtree still contains a large manual formula fallback owner in `quantscript/src/lowering/fallback.rs`. This file owns handwritten RSI, momentum, MACD, ZScore, moving-average, boundary-lookback, and related fallback pattern recognition. It is oversized, high-risk, and has enough independent public(crate) entrypoints to require precision single-leaf governance before any Rust movement.

## White-Box Boundary

| Field | Boundary |
| --- | --- |
| Input | QuantScript expressions, typed binding environment, data-source metadata, and helper-call shape |
| Output | manual indicator/source matches, lookback spans, formula operands, or no-match results |
| Current owner | `quantscript/src/lowering/fallback.rs` |
| Planned owner | `root.contracts.quantscript.runtime_lowering.manual_formula_fallback` |
| Parent caller | `runtime_lowering` module facade through existing `mod fallback` and call sites in lowering children |
| Exclusions | binding source decoding, indicator binding resolution, source recovery bridge, semantic bridge, helper env, context surface, shared arg helpers, diagnostics, and already closed orchestrator/universe/intent children |

## Leaf Granularity Evidence

Command:

- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -LeafId root.contracts.quantscript.runtime_lowering.manual_formula_fallback -ParentId root.contracts.quantscript.runtime_lowering -Path quantscript/src/lowering/fallback.rs -Depth 4`

Result:

| Field | Value |
| --- | --- |
| `split_decision` | `CONTINUE` |
| `governance_packaging` | `precision_single_leaf` |
| `final_decision` | `PRECISION` |
| `normalized_split_score` | 63 |
| `loc` | 1001 |
| `function_count` | 44 |
| `public_surface` | 13 |
| `branches` | 57 |
| `risk_tags` | `cross_crate`, `public_api`, `trading_semantics` |
| `reason` | oversized high-risk leaf requires precision baseline before movement |

## Split Decision Gate

| Gate | Result | Evidence |
| --- | --- | --- |
| `oversized_leaf` | PASS | `fallback.rs` is above the 800 LOC split-pressure threshold. |
| `strong_owner_boundary` | PASS | Manual formula recognition is a cohesive fallback domain separate from data-source decoding and indicator binding resolution. |
| `batch_downgrade_forbidden` | PASS | The leaf is high risk and must use precision single-leaf governance before movement. |
| `parent_child_boundary` | PASS | Parent remains the `runtime_lowering` module facade; no sibling horizontal connection is proposed. |

## Risk Classification

| Risk | Result |
| --- | --- |
| Public API / route / schema | Public(crate) fallback recognition entrypoints must remain stable |
| Persistence / lock owner | No persistence ownership movement planned |
| Trading semantics evidence | High; manual formula fallback affects inferred indicators and runtime intent lowering |
| Cross-crate behavior | Present through runtime data-source and typed expression contracts |

## Next Step

BE-002JE-01 `root.contracts.quantscript.runtime_lowering.manual_formula_fallback` baseline_plan.
