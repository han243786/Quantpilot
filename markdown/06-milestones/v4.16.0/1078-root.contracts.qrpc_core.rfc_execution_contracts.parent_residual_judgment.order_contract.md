# v4.16.0 root.contracts.qrpc_core.rfc_execution_contracts parent residual judgment selects order_contract

> Batch: BE-001SV-01
> Node: `root.contracts.qrpc_core.rfc_execution_contracts`
> Parent: `root.contracts.qrpc_core`
> Selected child: `root.contracts.qrpc_core.rfc_execution_contracts.order_contract`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core.rfc_execution_contracts` returns to its child queue after `allocation` closeout.

Decision:

`next_child: root.contracts.qrpc_core.rfc_execution_contracts.order_contract`

## Closed RFC Execution Children

| Child | Result |
| --- | --- |
| `contracts.qrpc_core.rfc_execution_contracts.data_request` | Closed with `stop_split: true`. |
| `contracts.qrpc_core.rfc_execution_contracts.allocation` | Closed with `stop_split: true`. |

## Open RFC Execution Residuals

| Residual | Status |
| --- | --- |
| `contracts.qrpc_core.rfc_execution_contracts.order_contract` | Selected next. Owns order DTOs and the pure order transition matrix. |
| `contracts.qrpc_core.rfc_execution_contracts.execution_feedback` | Queued. Owns execution feedback event DTOs. |
| `contracts.qrpc_core.rfc_execution_contracts.handoff_snapshot` | Queued. Owns handoff snapshot DTO and completeness validation. |

## Selection Rationale

`order_contract` is selected because it is the next contiguous RFC execution family in `qrpc_core/src/rfc_execution_contracts.rs` after closed `allocation`:

- `OrderStatus`;
- `OrderStatus::can_transition_to`;
- `Order`.

This child owns order schema compatibility plus a pure transition guard. It can move before feedback/handoff because those later families consume order identity/status but do not own the order transition matrix.

## White Box Boundary

| Item | Owner | Input | Output | Side effects |
| --- | --- | --- | --- | --- |
| Order status taxonomy | `order_contract` | current and next status | status enum schema | none |
| Order transition helper | `order_contract` | `self` status plus next status | allowed-transition boolean | none |
| Order DTO | `order_contract` | exchange, instrument, side, order type, quantity, execution, IDs, timestamps | order schema | none |

## Same-Parent Parallel Judgment

`same_parent_parallel_children` remains inactive for this selection.

Reason: `order_contract` contains a pure transition matrix. Serial extraction keeps transition compatibility isolated before feedback and handoff residuals are moved.

## Hard Boundaries

The next `order_contract` baseline must not:

- edit Rust source code;
- move feedback or handoff contracts;
- change enum variants, struct fields, derives, transition matrix behavior, public exports, or tests;
- reopen closed qrpc-core children, `data_request`, or `allocation`;
- change `qrpc_core_ir`, `qrpc_compiler`, `qrpc_runtime`, `quantscript`, backend, executor, frontend, physical `plugins/*`, or release transition;
- introduce direct sibling shortcuts.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `order_contract` owns `OrderStatus`, `OrderStatus::can_transition_to`, and `Order`. |
| parent_child_communication_kept | pass | Future child exports remain mediated through the RFC execution parent facade and crate root. |
| equivalence_baseline_freezable | pass | Enum variants, fields, derives, terminal rejection, and allowed transition matrix can be frozen. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | pass | Order contract is public qrpc-core contract API. |
| state_machine_phase | guarded | The transition helper is a pure contract matrix, not a mutable runtime state machine. |
| strategy_branch | no | No strategy branch is selected. |
| independent_failure_mode | pass | Order transition drift is independent from feedback schema and handoff validation. |
| reuse_pressure | pass | Runtime/order paths can consume these names through `qrpc_core::*`. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The selected child has schema and transition helper behavior. |
| communication_cost_rises | no | Parent facade re-export preserves current consumers. |
| local_proof_missing | no | qrpc-core compile/test gates cover the move. |
| line_count_only | no | Split is by order contract ownership, not raw line count. |

leaf_split_decision_result

`continue_baseline: true`; `order_contract` is worth selecting as the next RFC execution child.

## Next Step

BE-001SW-01 `root.contracts.qrpc_core.rfc_execution_contracts.order_contract` baseline_plan.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
