# v4.16.0 root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.entrypoint_runtime_config_assembly single leaf closeout

> Batch: BE-002IL-03
> Node: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.entrypoint_runtime_config_assembly`
> Parent: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`entrypoint_runtime_config_assembly` is closed with `stop_split: true`.

## Leaf Granularity Evidence

Command:

- `tools/evaluate-leaf-granularity.ps1 -LeafId root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.entrypoint_runtime_config_assembly -ParentId root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface -Path quantscript/src/lowering/orchestrator/entrypoint_runtime_config_assembly.rs -Depth 5`

Result:

- `Decision: STOP`
- `Split decision: STOP`
- `Governance packaging: stop_split`
- `Score: 28`
- `loc=162`
- `functions=1`
- `public_surface=1`
- `branches=6`
- `domains=5`
- `risk_tags=cross_crate,persistence,public_api,trading_semantics`
- `size_bucket=terminal_target_150_600`
- `governance_mode=stop_split`

## Rationale

The child now owns one cohesive runtime config assembly entrypoint behind the parent facade. It sits inside the preferred terminal leaf size range, has a single parent-visible function, and concentrates runtime lowering assembly without exposing new public API outside the parent module.

Further splitting would fragment a single assembly transaction into micro leaves while increasing governance cost and parent-child call surface. The current boundary keeps public lowering entrypoints in `orchestrator.rs` and keeps implementation details parent-visible through `pub(super)`.

## Parent Residual

Return to `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface` parent residual judgment.

Closed children under this parent:

- `profile_detection_surface`
- `integration_test_harness`
- `entrypoint_runtime_config_assembly`

## Next Step

BE-002IM-01 `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface` parent_residual_judgment.
