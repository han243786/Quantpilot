# v4.16.0 root.contracts.quantscript.analysis_diagnostics.indirect_recursion_gate baseline plan

> Batch: BE-002HA-01
> Node: `root.contracts.quantscript.analysis_diagnostics.indirect_recursion_gate`
> Parent: `root.contracts.quantscript.analysis_diagnostics`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Scope

Freeze the indirect recursion diagnostic gate before movement.

Current owner:

- `quantscript/src/analysis_diagnostics/analysis.rs`

Planned child:

- `quantscript/src/analysis_diagnostics/indirect_recursion_gate.rs`

## Items Frozen For Movement

The next extraction may move only:

- `detect_indirect_recursion`
- `collect_callee_names_from_stmts`
- `collect_callee_names_from_expr`

## Diagnostic Boundary

The child owns:

- helper-function call graph construction from script AST;
- indirect recursion cycle diagnostics;
- `QS0605` recursion diagnostic emission.

## Parent Wiring To Preserve

The analysis parent must keep:

- `ScriptAnalysis` and `analyze_script_module` public surface unchanged;
- diagnostic family invocation order unchanged;
- crate-root public exports and `crate::analysis` / `crate::diagnostics` compatibility unchanged;
- all other diagnostic family ownership unchanged.

The child may expose:

- `detect_indirect_recursion` as the only parent-callable entrypoint.

## Forbidden Movement

The next extraction must not move or change:

- strategy fetch/emit presence checks;
- fetch lookback warnings;
- data source map and index bounds checks;
- dead-code emit checks;
- symbol whitelist checks;
- diagnostic DTO/span types;
- unsupported construct, lookahead-window, and warmup/fetch children already closed;
- evaluator normalization, runtime lowering, v4 static audit, tests, authoring samples, backend, executor, frontend, plugin metadata, or release transition.

## Expected Validation

The extraction must pass:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002HA-02 `root.contracts.quantscript.analysis_diagnostics.indirect_recursion_gate` actual_extraction.
