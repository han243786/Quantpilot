# v4.16.0 root.contracts.runtime_support.runtime_facade_coordinator single leaf closeout

> Batch: BE-001YF-03
> Node: `root.contracts.runtime_support.runtime_facade_coordinator`
> Parent: `root.contracts.runtime_support`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

## Current Shape

`qrpc_runtime/src/runtime_facade_coordinator.rs` is extracted and equivalent, but it is not a terminal leaf yet.

Observed shape:

- about 1297 Rust lines
- public DTO: `ConfigGenerationEntry`
- public owner: `RuntimeCoordinator`
- many constructor and provider-injection APIs
- session and cycle orchestration
- execution submission and market update entry points
- state/provider/risk/config accessors
- hot-swap config generation logic
- portfolio update event projection
- private provider delegation helpers
- portfolio refresh and quote mapping helpers
- coordinator tests and sample runtime protocol fixture

## Split Decision

`continue_split: true`.

Required trigger rules:

- `size_or_behavior_pressure`: the extracted child is still large.
- `public_surface_cluster`: constructor/provider APIs form a coherent public API group.
- `private_behavior_cluster`: session/cycle orchestration and provider delegation are separable from constructors.
- `test_cluster`: coordinator test fixture and provider override tests are separable from runtime implementation.
- `future_parent_facade_value`: keeping `runtime_facade_coordinator.rs` as a parent facade after child extraction will reduce churn for runtime support siblings.

Stop rules do not apply because further split candidates have independent owner boundaries and are not mere helper fragments.

## Child Queue

The next internal queue is:

1. `contracts.runtime_support.runtime_facade_coordinator.constructor_provider_wiring`
2. `contracts.runtime_support.runtime_facade_coordinator.session_cycle_orchestration`
3. `contracts.runtime_support.runtime_facade_coordinator.execution_market_entrypoints`
4. `contracts.runtime_support.runtime_facade_coordinator.state_config_accessors`
5. `contracts.runtime_support.runtime_facade_coordinator.config_generation`
6. `contracts.runtime_support.runtime_facade_coordinator.portfolio_projection`
7. `contracts.runtime_support.runtime_facade_coordinator.coordinator_test_harness`

## Next Step

BE-001YG-01 `root.contracts.runtime_support.runtime_facade_coordinator` parent_residual_judgment selects `constructor_provider_wiring`.

## Gates

- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime`: PASS, 227 passed, 2 ignored
- `cargo check -p quantpilot`: PASS
- `git diff --check`: PASS
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`: PASS
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`: PASS
