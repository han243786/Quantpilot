# v4.16.0 root.contracts.quantscript parent residual judgment selects test_plan_surface

> Batch: BE-002KS-01
> Node: `root.contracts.quantscript`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select next child:

`root.contracts.quantscript.test_plan_surface`

## Parent Residual Scan

Closed quantscript children include syntax AST, legacy config compatibility,
typed resolution, analysis diagnostics, evaluator normalization, runtime
lowering, and v4 static audit.

Remaining quantscript-owned residuals:

- `test_plan_surface`;
- `authoring_docs_samples`.

## Why This Child

`test_plan_surface` owns the existing public test-plan DTOs and extraction
helpers in `quantscript/src/test_plan.rs`. It is already isolated as a file and
does not need code movement before closeout.

## Proposed Ownership

| Field | Value |
| --- | --- |
| Candidate child | `test_plan_surface` |
| Existing file | `quantscript/src/test_plan.rs` |
| Owned public API | `extract_test_plan`, `split_test_items`, `TestPlan`, `TestStep`, `TestActionDef`, `TestParamValueDef` |
| Parent export | `quantscript/src/lib.rs` re-export surface |

## Exclusions

- Do not move authoring docs or samples.
- Do not alter syntax AST or parser ownership.
- Do not introduce release-transition links.

## Next Step

BE-002KT-01 `root.contracts.quantscript.test_plan_surface`
single_leaf_closeout.
