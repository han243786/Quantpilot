# v4.16.0 root.contracts.quantscript.legacy_config_compat single leaf closeout

> Batch: BE-002FZ-03
> Node: `root.contracts.quantscript.legacy_config_compat`
> Parent: `root.contracts.quantscript`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Close `root.contracts.quantscript.legacy_config_compat` with `stop_split: true`.

The deprecated config-style QuantScript compatibility surface is now physically isolated in:

- `quantscript/src/legacy_config_compat.rs`

## Terminal Leaf Control

| Rule | Judgment |
| --- | --- |
| White-box boundary | Pass. The child owns deprecated config-style DTOs, parser helpers, compile helpers, source-kind detection, and legacy tests. |
| Parent-mediated communication | Pass. Public names still flow through the crate root; formal QS code does not import the child directly. |
| Split benefit | Low. Tokenizer, parser, DTO conversion, and tests are one compatibility owner for a deprecated path. |
| Governance cost | High if split further, because sub-leaves would be historical compatibility mechanics rather than independent product owners. |
| Runtime risk | Low after extraction; tests and crate check passed. |

## Evidence

BE-002FZ-02 verified:

- `cargo fmt --check` PASS
- `cargo test -p quantscript` PASS, 88 tests
- `cargo check -p quantpilot` PASS
- `git diff --check` PASS
- UTF-8 check PASS
- full feature tree check PASS
- matrix governance check PASS

## Residuals

No further legacy-config child is selected in this pass.

Remaining QuantScript queue:

- `typed_resolution`
- `analysis_diagnostics`
- `evaluator_normalization`
- `runtime_lowering`
- `v4_static_audit`
- `test_plan_surface`
- `authoring_docs_samples`

## Next Step

BE-002GA-01 `root.contracts.quantscript` parent_residual_judgment.
