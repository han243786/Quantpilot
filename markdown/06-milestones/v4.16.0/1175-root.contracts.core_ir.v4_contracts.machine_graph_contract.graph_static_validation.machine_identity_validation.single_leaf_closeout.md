# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.machine_identity_validation single leaf closeout stops split

> Batch: BE-001UW-03
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.machine_identity_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.machine_identity_validation`
stops splitting.

The leaf now owns one coherent machine inventory validation helper and its result structure.
Further splitting would separate map construction, nested duplicate detection, and delegated machine
validation into small private fragments without a stronger boundary.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | The file is a named white-box child for graph machine identity validation. |
| parent_child_communication_kept | pass | It returns maps and errors to the graph validation parent in one result. |
| equivalence_baseline_freezable | pass | `cargo test -p qrpc-core-ir` passed after extraction with 54 tests passed and 0 failed. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | no | The public graph validator remains in the parent. |
| state_machine_phase | pass | The helper is a complete machine inventory phase. |
| strategy_branch | guarded | It protects machine IDs, nested machines, and delegated machine validation. |
| independent_failure_mode | pass | Machine identity/delegation failures are independent from edge/DAG/event/risk phases. |
| reuse_pressure | no | There is no second caller requiring smaller helper fragments. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | yes | Smaller slices would be private map/delegation fragments. |
| communication_cost_rises | yes | Sub-splitting would pass maps, family vectors, and errors through more helpers. |
| local_proof_missing | no | Existing graph/machine validation tests prove this leaf through the public validator. |
| line_count_only | no | Stop decision is based on ownership and communication cost. |

leaf_split_decision_result

`stop_split: true`

Next parent residual candidate:

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.edge_identity_validation`

next_recursive_step

BE-001UX-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation parent_residual_judgment selects edge_identity_validation
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/machine_identity_validation.rs`

**Markers**:
- `BE-001UW-03`

**Next step**:
BE-001UX-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation parent_residual_judgment selects edge_identity_validation

---

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
