# v4.16.0 root.contracts.quantscript.syntax_ast_surface actual extraction

> Batch: BE-002FX-02
> Node: `root.contracts.quantscript.syntax_ast_surface`
> Parent: `root.contracts.quantscript`
> Stage: `actual_extraction`
> Movement: Rust module movement.
> governance_next_authority: active

---

## Movement

`syntax_ast_surface` is now a physical QuantScript child module.

Moved files:

| From | To |
| --- | --- |
| `quantscript/src/script.rs` | `quantscript/src/syntax_ast_surface/script.rs` |
| `quantscript/src/types.rs` | `quantscript/src/syntax_ast_surface/types.rs` |
| `quantscript/src/hir.rs` | `quantscript/src/syntax_ast_surface/hir.rs` |

Added:

- `quantscript/src/syntax_ast_surface.rs`

Updated:

- `quantscript/src/lib.rs`

## Facade Shape

The crate root now owns only the parent-facing facade:

- `mod syntax_ast_surface;`
- public re-exports for parser, AST, HIR, and type DTOs from `syntax_ast_surface`;
- `pub(crate) use syntax_ast_surface::{hir, script, types};` to preserve existing internal `crate::script`, `crate::types`, and `crate::hir` references.

The public `quantscript::*` import surface is preserved. External callers do not need to switch to a child-module path.

## Equivalence

No function body, parser branch, AST/HIR/type DTO field, diagnostic text, lowering behavior, runtime behavior, backend behavior, or test behavior was intentionally changed.

Observed gate:

- `cargo check -p quantscript` PASS

## Split Judgment For Next Step

The child should proceed to single-leaf closeout rather than immediately splitting parser internals. `script.rs` is large, but its internal lexer/parser/AST ownership remains one cohesive syntax surface. Splitting token, expression, statement, test-block, and function parser internals now would increase governance cost and path churn before the higher-value QuantScript parents are handled.

## Next Step

BE-002FX-03 `root.contracts.quantscript.syntax_ast_surface` single_leaf_closeout.
