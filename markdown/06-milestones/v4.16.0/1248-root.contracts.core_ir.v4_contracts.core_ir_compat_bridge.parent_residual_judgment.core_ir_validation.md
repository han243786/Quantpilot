# v4.16.0 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge parent residual judgment selects core_ir_validation

> Batch: BE-001WJ-01
> Node: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `parent_residual_judgment`
> Movement: No code movement. This document selects the next child for extraction.

---

## Summary

root.contracts.core_ir.v4_contracts.core_ir_compat_bridge parent residual judgment selects core_ir_validation

The next child is `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation`.

It owns Core IR validation before v4 graph construction:

- `validate_core_ir_for_v4_bridge`
- `collect_core_ir_node_ids`
- `insert_core_ir_node_id`
- `validate_core_ir_edges_for_v4_bridge`
- `validate_core_ir_references_for_v4_bridge`
- `validate_core_ir_scalar_expr_refs`
- `validate_core_ir_series_expr_refs`
- `validate_core_ir_custom_value_refs`

It does not own compatibility report DTOs, `CoreIrV4CompatibilityReport::validate_for_phase4`, `bridge_core_ir_to_v4_machine_graph`, v4 graph/machine/event builders, bridge metadata helpers, id sanitization, or bridge-specific default helpers.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | PASS | `core_ir_validation` has a clear Core IR input/reference validation surface. |
| parent_child_communication_kept | PASS | The parent bridge entrypoint can call the validation child before graph construction without sibling horizontal communication. |
| equivalence_baseline_freezable | PASS | Existing bridge tests cover accepted graph mapping plus missing data, missing risk, cycle, and unknown edge endpoint rejection. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | PARTIAL | The child is private, but it guards the public bridge entrypoint's acceptance/rejection decision. |
| state_machine_phase | FAIL | This child validates Core IR before machine construction; the state-machine construction phase remains outside. |
| strategy_branch | PASS | It validates data bindings, indicators, signals, agent policies, risk policies, execution config, and DAG/edge references. |
| independent_failure_mode | PASS | `V4BRIDGE000`-`046` Core IR failures are independent from `V4BRIDGE900` v4 static graph failures. |
| reuse_pressure | PARTIAL | The validator is likely reusable for future bridge variants, but current reuse remains internal. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | PASS | The selected child owns a coherent validation pipeline, not a helper-only fragment. |
| communication_cost_rises | PASS | The validation child only needs Core IR input plus diagnostic sink; it can stay parent-mediated. |
| local_proof_missing | PASS | Current tests exercise the validation failure paths enough for extraction equivalence. |
| line_count_only | PASS | Selection is based on ownership and failure mode, not line count alone. |

leaf_split_decision_result

`selected_child: core_ir_validation`

The parent remains open. Remaining candidates after this child are expected to include compatibility graph construction and bridge report/phase4 validation.

next_recursive_step

BE-001WK-01 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation baseline_plan
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/core_ir_compat_bridge.rs`

**Markers**:
- `BE-001WJ-01`

**Next step**:
BE-001WK-01 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
