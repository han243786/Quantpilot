# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.edge_identity_validation actual extraction complete

> Batch: BE-001UY-02
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.edge_identity_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation`
> Stage: `extract_closeout`
> Movement: Code movement completed.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.edge_identity_validation`
actual extraction is complete.

Implemented movement:

- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation.rs` now declares `mod edge_identity_validation;`
- the parent validator keeps public orchestration and calls `self.validate_edge_identity(&machine_identity.machines_by_id)`
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/edge_identity_validation.rs` owns the edge inventory loop
- the child owns the local duplicate edge ID set, event type required checks, endpoint existence checks, and self-loop rejection

Equivalence notes:

- error text and branch order remain frozen from BE-001UY-01
- parent-child communication stays vertical: parent passes only the machine identity map; child returns `Vec<String>`
- DAG, event usage, and risk-plane validation still run after edge identity validation
- schema/graph ID and empty machine-list checks remain in the parent

Verification:

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir` (54 passed)

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation.rs`
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/edge_identity_validation.rs`

**Markers**:
- `BE-001UY-02`

**Next step**:
BE-001UY-03 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.edge_identity_validation single_leaf_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
