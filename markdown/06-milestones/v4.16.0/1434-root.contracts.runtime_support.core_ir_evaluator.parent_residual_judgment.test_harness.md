# v4.16.0 root.contracts.runtime_support.core_ir_evaluator parent residual judgment selects test_harness

> Batch: BE-002AR-01
> Node: `root.contracts.runtime_support.core_ir_evaluator`
> Parent: `root.contracts.runtime_support`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> Selected child: `root.contracts.runtime_support.core_ir_evaluator.test_harness`

---

## Decision

`root.contracts.runtime_support.core_ir_evaluator.test_harness` is selected as the next residual candidate.

This is a judgment step only. It does not authorize code movement yet.

## Evidence

Closed children:

- `classic_indicator_wave`
- `advanced_indicator_wave`
- `spread_custom_expression_wave`
- `shared_lookup_math_helpers`

Remaining local test surface:

- `#[cfg(test)] mod tests`
- quote/kline/test data fixtures;
- Spread and Custom evaluator behavior tests;
- helper smoke tests for advanced/classic formulas;
- registry integration coverage through `evaluate_indicator_signal`.

## Candidate Shape

The candidate should become a test-only child module if the next baseline confirms that:

- only the local `#[cfg(test)] mod tests` moves;
- production registry, DTOs, helper ownership, and child declarations remain parent-owned;
- test-only access uses `super::*` and does not expose helpers publicly.

## Boundary Rules

| Boundary | Requirement |
| --- | --- |
| Production boundary | No production evaluator behavior moves in this step. |
| Helper boundary | Helper ownership remains parent-owned; tests may access helpers through `super::*`. |
| Public API | No public API widening is allowed. |
| Release transition | No horizontal shortcut or performance bypass is proposed. |

## Next Step

BE-002AS-01 `root.contracts.runtime_support.core_ir_evaluator.test_harness` baseline_plan.
