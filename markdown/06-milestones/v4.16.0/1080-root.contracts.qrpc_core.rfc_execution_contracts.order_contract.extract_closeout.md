# v4.16.0 root.contracts.qrpc_core.rfc_execution_contracts.order_contract extract closeout

> Batch: BE-001SW-02
> Node: `root.contracts.qrpc_core.rfc_execution_contracts.order_contract`
> Parent: `root.contracts.qrpc_core.rfc_execution_contracts`
> Stage: `extract_closeout`
> Movement: Rust physical extraction complete.

---

## Summary

`order_contract` now owns the order DTO, order status taxonomy, and pure transition matrix that were previously embedded in `qrpc_core/src/rfc_execution_contracts.rs`.

Moved file:

- `qrpc_core/src/rfc_execution_contracts/order_contract.rs`

Updated parent facade:

- `qrpc_core/src/rfc_execution_contracts.rs`

## Actual Extraction

| Baseline item | Result |
| --- | --- |
| `OrderStatus` | Moved into `order_contract.rs`. |
| `OrderStatus::can_transition_to` | Moved into `order_contract.rs`. |
| `Order` | Moved into `order_contract.rs`. |
| Parent facade | Added `mod order_contract;` and `pub use order_contract::*;`. |

## Equivalence Evidence

| Check | Result | Evidence |
| --- | --- | --- |
| Public facade preserved | pass | `qrpc_core/src/rfc_execution_contracts.rs` re-exports `order_contract::*`, and `qrpc_core/src/lib.rs` still re-exports RFC execution contracts. |
| Closed sibling boundaries preserved | pass | `data_request` and `allocation` remain closed; feedback and handoff code stayed in the RFC execution parent. |
| Order surface preserved | pass | The moved file contains `OrderStatus`, `OrderStatus::can_transition_to`, and `Order`. |
| Parent-child communication | pass | The child imports only serde and public qrpc-core primitive/runtime types through the crate facade. |

## Validation Results

- `cargo fmt -p qrpc-core` passed.
- `cargo check -p qrpc-core` passed.
- `cargo test -p qrpc-core` passed: 12 unit tests passed; doc tests passed with 0 tests.

## Hard Boundaries

This extraction did not:

- change enum variants, struct fields, derives, serde shape, terminal guard behavior, transition matrix, public exports, or tests;
- move feedback or handoff contracts;
- reopen closed qrpc-core child modules, closed `data_request`, or closed `allocation`;
- change `qrpc_core_ir`, `qrpc_compiler`, `qrpc_runtime`, `quantscript`, backend, executor, frontend, physical `plugins/*`, or release transition;
- introduce direct sibling shortcuts.

## Next Step

BE-001SW-03 `root.contracts.qrpc_core.rfc_execution_contracts.order_contract` single_leaf_closeout.

The closeout must decide whether order DTO and transition matrix behavior should stay together or split into smaller subleaves.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
