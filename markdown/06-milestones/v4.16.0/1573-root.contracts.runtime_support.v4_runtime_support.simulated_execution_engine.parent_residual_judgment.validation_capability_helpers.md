# v4.16.0 simulated_execution_engine parent residual judgment - validation_capability_helpers

> Batch: BE-002ES-01
> Node: `root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine`
> Selected child: `root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine.validation_capability_helpers`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `validation_capability_helpers` as the next child under `simulated_execution_engine`.

After closing projection, fill/accounting, trigger flow, order lifecycle, and runtime adapter children, the parent residual still owns request validation, position admission, order capability derivation, conditional-order classification, fill-price helpers, and marketability checks. This is the next strongest same-parent child because these helpers are pure boundary/admission logic and can be isolated without moving direct-submit orchestration or payload parsing.

## Current Parent Residual Evidence

`tools/evaluate-leaf-granularity.ps1 -Path qrpc_runtime/src/v4_simulated_execution.rs -SameParentWaveCandidate` result:

| Field | Value |
| --- | --- |
| `split_decision` | `STOP` |
| `governance_packaging` | `same_parent_wave` |
| `final_decision` | `WAVE` |
| Score | `50` |
| LOC | `536` |
| Functions | `24` |
| Public surface | `2` |
| Branches | `75` |
| Domains | `7` |
| Risk tags | `persistence`, `public_api`, `route_schema`, `security`, `state_machine`, `trading_semantics` |

The parent is now terminal-sized overall, but a same-parent helper child remains useful because validation/capability logic is shared by the parent direct-submit path and already extracted children.

## Candidate Boundary

`validation_capability_helpers` should evaluate the admission and helper cluster:

- `validate_position_action`;
- `pre_execution_rejection_reason`;
- `non_executable_resting_reason`;
- `V4SimulatedPositionAction::side`;
- `V4SimulatedPositionAction::is_reducing`;
- `is_conditional_order_type`;
- `conditional_order_execution_type`;
- `conditional_order_is_triggered`;
- `validate_simulated_execution_config`;
- `validate_simulated_order_request`;
- `simulated_order_required_capabilities`;
- `compute_simulated_fill_price`;
- `limit_order_is_marketable`.

The next baseline must decide the exact visibility/re-export strategy for free helpers so sibling child modules keep their current call surface without adding horizontal child-to-child bypasses.

## Exclusions

The selected child must not own:

- `submit_order` or direct-submit orchestration;
- payload parsing helpers (`payload_string`, `payload_f64`, `payload_u64`, `payload_bool`, `metadata_string`, parse helpers, and `normalize_token`);
- snapshot and metrics projection already closed in `snapshot_metrics_projection`;
- fill execution and ledger mutation already closed in `fill_ledger_accounting`;
- market trigger flow already closed in `market_trigger_flow`;
- order lifecycle internals already closed in `order_lifecycle_flow`;
- runtime adapter behavior already closed in `runtime_adapter`;
- v4 tests.

## Stop Conditions

Stop and request scope review if the next baseline or extraction requires:

1. moving `submit_order` wholesale;
2. changing validation error text, capability sets, order type conversion, marketability, fill price, or trigger semantics;
3. moving payload parsing into the same child without a separate baseline decision;
4. adding direct child-to-child calls that bypass the simulated execution parent;
5. changing public runtime API, route/schema, persistence owner, lock owner, or runtime mode;
6. entering release-transition performance optimization.

## QPCursor

```text
QPC://v4.16.0/RM:R2/GH:G3/FFT:root.contracts.runtime_support/MT:root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine.validation_capability_helpers/LEAF:qrpc_runtime/src/v4_simulated_execution.rs
```

## Next Step

BE-002ET-01 `root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine.validation_capability_helpers` baseline_plan.
