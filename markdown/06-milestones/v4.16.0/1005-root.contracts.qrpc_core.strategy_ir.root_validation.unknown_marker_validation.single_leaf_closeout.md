# v4.16.0 root.contracts.qrpc_core.strategy_ir.root_validation.unknown_marker_validation single leaf closeout

> Batch: BE-001RI-03
> Node: `root.contracts.qrpc_core.strategy_ir.root_validation.unknown_marker_validation`
> Parent: `root.contracts.qrpc_core.strategy_ir.root_validation`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core.strategy_ir.root_validation.unknown_marker_validation` has been evaluated after BE-001RI-02 extraction.

Decision:

`stop_split: true`

The node is now a compact child module with one responsibility: Strategy IR unknown marker path/reason validation and unknownable helper implementation behind a parent wrapper bridge.

## Split Rule Evaluation

| Rule | Result |
| --- | --- |
| Physical owner | Stop. The selected surface is isolated in `qrpc_core/src/strategy_ir/root_validation/unknown_marker_validation.rs`. |
| Public method count | Stop. The child exposes three parent-local helpers: `validate_unknowns`, `validate_unknownable`, and `validate_unknownable_opt`. |
| Mixed responsibility | Stop. Unknowns path/reason checks and unknownable helper checks are one contiguous unknown marker validation family. |
| State ownership | Stop. The child is stateless and appends diagnostics through the parent-provided `errors` vector. |
| Side effects | Stop. No IO, persistence, locks, runtime state, external calls, or public API mutation are involved. |
| Parent-mediated dependency | Covered. Closed siblings still call private parent wrappers, so no child imports this module directly. |
| Shared helper pressure | Covered. Helper implementation is child-owned while the parent wrapper remains the stable sibling bridge. |
| Future reopen rule | Allowed only when a concrete unknown marker, unknownable helper, or parent wrapper bridge proposal is made. |

## White-Box Boundary

| Input | Processing owner | Output |
| --- | --- | --- |
| Root `StrategyIr` unknown marker state and `KnownOrUnknown` helper inputs | `contracts.qrpc_core.strategy_ir.root_validation.unknown_marker_validation` | Appended validation errors for `unknowns[*]` path/reason and invalid unknownable markers |

The leaf may describe and guard:

- `validate_unknowns`;
- `validate_unknownable`;
- `validate_unknownable_opt`;
- parent wrapper bridge mediation for closed sibling children;
- `unknowns[*].path` and `unknowns[*].reason` required-field checks.

## Rust Local Fields

| Field | Result |
| --- | --- |
| Crate | `qrpc-core` |
| Child visibility | `pub(super)` only. |
| Public exports | No `pub use` changes. |
| Parent facade | `root_validation.rs` remains the validation coordinator. |
| Parent wrapper bridge | Preserved for closed sibling validation children. |
| Sibling dependency check | Passed. Closed validation children do not import `unknown_marker_validation` directly. |

## Non-Claims

This closeout does not claim:

- local validation tests or sample fixture changed;
- public `StrategyIr` fields, serde shape, or closed Strategy IR child DTOs changed;
- root validation parent residual judgment is complete;
- protocol primitives, runtime IO, compiler/runtime/backend/executor/frontend behavior, physical `plugins/*`, or release transition changed.

## Proof

- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-001RJ-01 `root.contracts.qrpc_core.strategy_ir.root_validation` parent_residual_judgment evaluates remaining local test fixture and parent facade residuals.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
