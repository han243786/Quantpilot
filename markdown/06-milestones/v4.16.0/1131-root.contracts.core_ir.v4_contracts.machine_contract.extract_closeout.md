# v4.16.0 root.contracts.core_ir.v4_contracts.machine_contract actual extraction complete

> Batch: BE-001TZ-02
> Node: `root.contracts.core_ir.v4_contracts.machine_contract`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `extract_closeout`
> Movement: Code movement complete.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_contract` is now extracted as a private v4 child module.

The child owns the frozen machine schema and validation family:

- machine taxonomy enums;
- `V4MachineContract`;
- state, group, transition, selector, action, and memory DTOs;
- `V4MachineContract::validate_static_contract`.

The v4 parent now declares and re-exports the child:

- `mod machine_contract;`
- `pub use machine_contract::*;`

## Boundary

**Real files**:

- `qrpc_core_ir/src/v4.rs`
- `qrpc_core_ir/src/v4/machine_contract.rs`

**Markers**:

- `BE-001TZ-02`

## White-Box Result

| Surface | Owner after extraction | Parent-mediated dependencies |
| --- | --- | --- |
| machine taxonomy | `machine_contract` | none |
| machine root DTO | `machine_contract` | `default_machine_contract_version`, `V4_MACHINE_CONTRACT_VERSION` |
| state/group/transition/action DTOs | `machine_contract` | `default_transition_conflict_policy` |
| memory field DTO | `machine_contract` | `QsTypeRef`, `serde_json::Value` |
| static validation method | `machine_contract` | `V4_MACHINE_CONTRACT_VERSION` |

## Non-Moved Surfaces

The extraction did not move:

- `RuntimeTradingMode`;
- graph contract DTOs, graph edge DTOs, graph risk plane, or graph validation;
- event catalog DTOs or validation;
- `QsTypeRef` and type-system contracts;
- default helper functions;
- compat bridge builders;
- test fixtures.

## Equivalence Evidence

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir` passed: 54 tests.
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`

## Next Step

BE-001TZ-03 `root.contracts.core_ir.v4_contracts.machine_contract` single_leaf_closeout.
