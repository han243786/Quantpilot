# v4.16.0 root.contracts.runtime_support.core_ir_evaluator.classic_indicator_wave single leaf closeout

> Batch: BE-002AK-03
> Node: `root.contracts.runtime_support.core_ir_evaluator.classic_indicator_wave`
> Parent: `root.contracts.runtime_support.core_ir_evaluator`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Decision

`stop_split: true`

`root.contracts.runtime_support.core_ir_evaluator.classic_indicator_wave` is closed as a terminal same-parent wave.

## Evidence

Current owner file:

- `qrpc_runtime/src/core_ir_evaluator/classic_indicator_wave.rs`

Terminal leaf control:

```text
Leaf: root.contracts.runtime_support.core_ir_evaluator.classic_indicator_wave
Decision: STOP
Score: 39
Metrics: loc=317, functions=6, public_surface=6, branches=22, domains=3
Terminal leaf control: size_bucket=terminal_target_150_600, governance_mode=stop_split
```

## Why No Deeper Split

No deeper child is opened because:

- the file is within the target terminal range;
- each single indicator would become a small high-governance-cost leaf;
- the six functions share parent-mediated lookup/math helpers and public registry dispatch;
- further split would add module declarations/imports without improving ownership clarity.

## Equivalence Evidence

The extraction batch already passed:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime core_ir_evaluator`: 22 passed
- `cargo check -p quantpilot`
- `git diff --check`
- `tools/check-utf8.ps1`
- `tools/check-full-feature-tree.ps1`
- `tools/check-matrix-governance.ps1`

## Next Step

BE-002AL-01 `root.contracts.runtime_support.core_ir_evaluator` parent_residual_judgment selects `advanced_indicator_wave`.
