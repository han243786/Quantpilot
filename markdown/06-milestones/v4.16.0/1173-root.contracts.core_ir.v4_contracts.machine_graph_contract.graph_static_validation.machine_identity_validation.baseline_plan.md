# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.machine_identity_validation equivalence baseline and extraction plan

> Batch: BE-001UW-01
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.machine_identity_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation`
> Stage: `baseline_plan`
> Movement: No code movement.

---

## Summary

This baseline freezes graph machine identity validation before extraction.

The selected child may own only:

- top-level machine ID required checks;
- duplicate top-level machine ID checks;
- nested machine family collection through the parent helper;
- duplicate ID checks across top-level and nested machines;
- per-machine static contract delegation and error prefixing;
- `machines_by_id` and `all_machines_by_id` result maps;
- a small result structure carrying maps and errors back to the parent.

It must not own schema version checks, graph ID checks, graph empty-machine check,
edge identity/endpoint/self-loop checks, DAG validation, event usage validation, risk-plane validation,
event party helper, graph schema, traversal helpers, bridge code, or tests.

## Frozen Validation Surface

The next extraction must preserve:

- `machine_id is required`;
- `duplicate machine `{machine_id}``;
- nested family collection through `collect_machine_family`;
- `duplicate machine `{machine_id}` across top-level and nested machines`;
- delegated `machine.validate_static_contract()` behavior;
- delegated machine error prefix:
  `machine `{machine_id}` failed static contract: {machine_error}`;
- top-level `machines_by_id` map semantics;
- all-machine `all_machines_by_id` map semantics;
- parent call order before edge validation.

## Allowed BE-001UW-02 Movement

The next extraction step may:

- create `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/machine_identity_validation.rs`;
- add `mod machine_identity_validation;` to `qrpc_core_ir/src/v4/machine_graph_contract/static_validation.rs`;
- move only machine identity/delegation validation;
- introduce a small result struct with `machines_by_id`, `all_machines_by_id`, and `errors`;
- keep `validate_static_contract` as the parent orchestration entry.

The child may depend on:

- `std::collections::BTreeMap`;
- `collect_machine_family`;
- `V4MachineContract`;
- `V4MachineGraphContract`.

## Forbidden BE-001UW-02 Movement

The next extraction step must not move or edit behavior for:

- schema version check;
- graph ID required check;
- empty graph machine-list check;
- edge ID/event/source/target/self-loop checks;
- graph acyclicity validation;
- event usage validation;
- risk-plane validation;
- `machine_event_party_allowed`;
- graph schema DTOs;
- traversal helpers;
- tests.

It must not alter validation branch order, error strings, or map ownership semantics.

## Equivalence Proof Plan

BE-001UW-02 must pass:

- `cargo fmt --check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation.rs`

**Planned child file**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/machine_identity_validation.rs`

**Markers**:
- `BE-001UW-01`

**Next step**:
BE-001UW-02 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.machine_identity_validation extract_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
