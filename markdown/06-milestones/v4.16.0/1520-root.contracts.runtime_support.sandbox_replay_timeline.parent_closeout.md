# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline parent closeout

> Batch: BE-002DF-01
> Node: `root.contracts.runtime_support.sandbox_replay_timeline`
> Parent: `root.contracts.runtime_support`
> Stage: `parent_closeout`
> Movement: No Rust code movement.

---

## Decision

`parent_closeout: true`

All planned sandbox/replay/timeline children from the BE-002CK-01 baseline are closed.

## Closed Children

- `timeline_data_providers`
- `unified_timeline`
- `replay_builder`
- `sandbox_mode_surface`
- `realtime_sandbox`
- `fast_backtest_sandbox`
- `test_harness`

## Remaining Parent Surface

`qrpc_runtime/src/sandbox/mod.rs` is now a compact parent facade and shared helper owner:

- child declarations;
- replay child re-exports;
- concrete sandbox child re-exports;
- mode surface re-exports;
- `snapshot_from`;
- `ensure_running`;
- `trace_id`.

This residual does not justify another production child. The three helper functions are shared by concrete sandbox children and remain parent coordination glue.

## Gates

Latest verified gates from the extraction sequence:

- `cargo fmt --check` PASS
- `cargo check -p quantpilot` PASS
- `cargo test -p qrpc-runtime sandbox -- --nocapture` PASS: 21 passed, 2 ignored
- UTF-8 / full feature tree / matrix governance PASS

## Next Step

BE-002DG-01 `root.contracts.runtime_support` parent_residual_judgment.
