# v4.16.0 root.contracts.runtime_support.data_module.mock_data_generation single leaf closeout

> Batch: BE-001ZK-03
> Node: `root.contracts.runtime_support.data_module.mock_data_generation`
> Parent: `root.contracts.runtime_support.data_module`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Leaf Status

`root.contracts.runtime_support.data_module.mock_data_generation` is closed as a terminal leaf.

The child owns:

- mock volatility config;
- deterministic pseudo-random generation;
- raw mock kline generation;
- raw mock quote generation;
- normalized mock kline bars for backtest compatibility.

## Leaf Granularity Judge Evidence

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -LeafId root.contracts.runtime_support.data_module.mock_data_generation -ParentId root.contracts.runtime_support.data_module -Path qrpc_runtime/src/data_module/mock_data_generation.rs -Depth 4
```

Result:

| Field | Value |
| --- | --- |
| `decision` | `STOP` |
| `normalized_split_score` | 30 |
| `governance_mode` | `stop_split` |
| `size_bucket` | `micro_under_100` |
| `loc` | 99 |
| `function_count` | 5 |
| `public_surface` | 5 |
| `micro_leaf_default_stop` | `true` |
| `high_cost_leaf_needs_wave_or_stop` | `true` |

Reason: the extracted child is compact and cohesive. Splitting volatility config, pseudo-random generation, quote mock, and kline mock would create micro leaves with higher governance and parent forwarding cost than ownership benefit.

## White-Box Boundary

| Boundary | Decision |
| --- | --- |
| Input | `DataSourceConfig`, `now_ms`, exchange/symbol metadata, volatility atomic |
| Output | `RawKline`, `RawQuote`, normalized mock backtest bars |
| Owner | `qrpc_runtime/src/data_module/mock_data_generation.rs` |
| Parent facade | `qrpc_runtime/src/data_module.rs` re-exports compatibility surfaces and calls mock raw generation |
| Excluded | historical cache, HTTP transport, exchange parsing, provider orchestration, quality diagnostics, and tests |

## Equivalence Evidence

The preceding extraction step passed:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Closeout Decision

`stop_split: true`

No further child queue is opened for mock data generation.

Next step: BE-001ZL-01 `root.contracts.runtime_support.data_module` parent_residual_judgment selects the next remaining data-module child, expected `contracts.runtime_support.data_module.historical_cache`.
