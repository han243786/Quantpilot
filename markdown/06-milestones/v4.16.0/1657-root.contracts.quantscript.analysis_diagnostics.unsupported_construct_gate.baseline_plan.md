# v4.16.0 root.contracts.quantscript.analysis_diagnostics.unsupported_construct_gate baseline plan

> Batch: BE-002GU-01
> Node: `root.contracts.quantscript.analysis_diagnostics.unsupported_construct_gate`
> Parent: `root.contracts.quantscript.analysis_diagnostics`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Scope

Freeze the unsupported construct diagnostic gate before movement.

Current owner:

- `quantscript/src/analysis_diagnostics/analysis.rs`

Planned child:

- `quantscript/src/analysis_diagnostics/unsupported_construct_gate.rs`

## Items Frozen For Movement

The next extraction may move only:

- `collect_unsupported_construct_diagnostics`
- `collect_unsupported_constructs_from_stmts`
- `collect_unsupported_constructs_from_expr`
- `is_supported_formal_try_target`
- `function_contains_direct_recursion`
- `stmts_contain_direct_recursion`
- `expr_contains_direct_recursion`
- `collect_non_universe_for_loop_diagnostics`
- `collect_non_universe_for_loop_diagnostics_from_hir_stmts`

## Parent Wiring To Preserve

The analysis parent must keep:

- `ScriptAnalysis` and `analyze_script_module` public surface unchanged;
- diagnostic family invocation order unchanged;
- `ResolveResult` and `TypedHirModule` based gate behavior unchanged;
- diagnostic codes `QS0601` through `QS0610` unchanged;
- crate-root public exports and `crate::analysis` / `crate::diagnostics` compatibility unchanged.

## Forbidden Movement

The next extraction must not move or change:

- `ScriptAnalysis`, `analyze_script_module`, or analysis result assembly;
- series index, centered window, warmup, fetch lookback, strategy fetch/emit, data-source bounds, dead-code emit, or fetch symbol whitelist helpers;
- diagnostic DTO/span types in `diagnostics.rs`;
- evaluator normalization, runtime lowering, v4 static audit, test plans, samples, backend, executor, frontend, plugin metadata, or release transition.

## Expected Validation

The extraction must pass:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002GU-02 `root.contracts.quantscript.analysis_diagnostics.unsupported_construct_gate` actual_extraction.
