# v4.16.0 root.executor parent residual judgment selects runner

> Batch: BE-002LE-01
> Node: `root.executor`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select next child:

`root.executor.runner`

## Parent Residual Scan

Closed executor children:

- `executor.state`

Remaining executor child:

- `executor.runner`

## Why This Child

`executor.runner` owns strategy lifecycle route behavior, migration package
loading, runner pool registration, live/v4 runner orchestration, market data
routes, websocket feed handling, and OKX demo provider routes.

## Proposed Ownership

| Field | Value |
| --- | --- |
| Candidate child | `executor.runner` |
| Existing files | `live_runner.rs`, `migration_api.rs`, `kline_buffer.rs`, `ws_client.rs`, `okx_rest.rs` |
| Parent residual | route registration, process startup, route handlers still in `main.rs` |
| First likely movement | extract OKX demo provider routes or strategy lifecycle routes from `main.rs` |

## Exclusions

- Do not reopen `executor.state`.
- Do not move backend/runtime state ownership.
- Do not include frontend-executor UI.
- Do not introduce release-transition links.

## Next Step

BE-002LF-01 `root.executor.runner` baseline_plan.
