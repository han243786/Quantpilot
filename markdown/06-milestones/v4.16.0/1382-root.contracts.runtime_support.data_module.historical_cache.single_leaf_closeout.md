# v4.16.0 root.contracts.runtime_support.data_module.historical_cache single leaf closeout

> Batch: BE-001ZM-03
> Node: `root.contracts.runtime_support.data_module.historical_cache`
> Parent: `root.contracts.runtime_support.data_module`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Leaf Status

`root.contracts.runtime_support.data_module.historical_cache` is closed as a terminal leaf.

The child owns:

- historical cache path safety;
- fresh cache load;
- stale cache fallback;
- bounded cache body reads;
- persistence with quota, temp write, rename, and fsync;
- historical raw kline fetch orchestration;
- backtest historical kline bars.

## Leaf Granularity Judge Evidence

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -LeafId root.contracts.runtime_support.data_module.historical_cache -ParentId root.contracts.runtime_support.data_module -Path qrpc_runtime/src/data_module/historical_cache.rs -Depth 4
```

Result:

| Field | Value |
| --- | --- |
| `decision` | `STOP` |
| `normalized_split_score` | 42 |
| `governance_mode` | `stop_split` |
| `size_bucket` | `terminal_target_150_600` |
| `loc` | 183 |
| `function_count` | 9 |
| `public_surface` | 1 |
| `high_cost_leaf_needs_wave_or_stop` | `true` |

Reason: the child is within the terminal target size and all sub-parts are one historical replay cache owner. Splitting path safety, cache read/write, stale fallback, and historical fetch would add governance and parent forwarding cost without a stronger independent owner.

## White-Box Boundary

| Boundary | Decision |
| --- | --- |
| Input | `DataSourceConfig`, `now_ms`, exchange endpoint/parser helpers, parent normalization helper |
| Output | cached or fetched normalized historical kline bars |
| Owner | `qrpc_runtime/src/data_module/historical_cache.rs` |
| Parent facade | `qrpc_runtime/src/data_module.rs` re-exports `historical_kline_bars_for_backtest` |
| Excluded | request-interval in-memory cache, live provider fetch, exchange parser ownership, mock generation, quality diagnostics, and tests |

## Equivalence Evidence

The preceding extraction step passed:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Closeout Decision

`stop_split: true`

No further child queue is opened for historical cache.

Next step: BE-001ZN-01 `root.contracts.runtime_support.data_module` parent_residual_judgment selects the next remaining data-module child, expected `contracts.runtime_support.data_module.http_transport`.
