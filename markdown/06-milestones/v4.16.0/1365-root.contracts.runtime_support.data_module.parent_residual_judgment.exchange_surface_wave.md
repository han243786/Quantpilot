# v4.16.0 root.contracts.runtime_support.data_module parent residual judgment selects exchange_surface_wave

> Batch: BE-001ZF-01
> Node: `root.contracts.runtime_support.data_module`
> Stage: `parent_residual_judgment`
> Selected wave: `root.contracts.runtime_support.data_module.exchange_surface_wave`
> Movement: No code movement.

---

## Summary

`data_module` parent residual judgment selects a same-parent wave for the exchange surface.

The next baseline should evaluate these children together:

- `root.contracts.runtime_support.data_module.exchange_endpoints`
- `root.contracts.runtime_support.data_module.exchange_payload_parsing`

## Why Wave Instead Of Single Leaf

The endpoint helper pocket is too small to justify a standalone full recursive chain. Payload parsing is larger, but both children share the same exchange surface context: OKX/Binance endpoint contracts, source kind branching, exchange constants, symbol formatting, and remote payload assumptions.

Using a `same_parent_wave` reduces governance cost while keeping child-level evidence rows.

## Leaf Granularity Smart Judge

| Child | Decision | Reason |
| --- | --- | --- |
| `exchange_endpoints` | `WAVE` | Low-hundreds-or-smaller endpoint/provider helper pocket; standalone governance would cost more than the split. |
| `exchange_payload_parsing` | `WAVE` | Larger parsing pocket, but it shares exchange payload assumptions with endpoint contracts and can be proven in the same wave. |

## Frozen Scope

Included in the next baseline:

- provider key mapping;
- endpoint selection;
- ping endpoint selection;
- Binance endpoint construction;
- OKX endpoint construction;
- OKX instrument id helpers;
- OKX/Binance payload parser families;
- parser field coercion helpers.

Excluded:

- normalization into `NormalizedMarketData`;
- fetch/transport execution;
- cache and historical cache;
- mock data generation;
- quality diagnostics;
- provider collection orchestration;
- tests unless needed as evidence.

## Guardrails

- No public trait or DTO shape changes.
- No route/schema/persistence/lock owner movement.
- No sibling horizontal link.
- Parent `data_module` keeps orchestration and transport ownership.
- If baseline finds endpoint and parser evidence cannot be shared, split the wave back into separate child baselines.

## Next Step

BE-001ZG-01 `root.contracts.runtime_support.data_module.exchange_surface_wave` baseline_plan.
