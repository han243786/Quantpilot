# v4.16.0 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.fallback_description actual extraction complete

> Batch: BE-001XR-02
> Node: `root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.fallback_description`
> Parent: `root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering`
> Stage: `extract_closeout`
> Movement: Rust code movement completed.

---

## Summary

`root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.fallback_description` has been physically extracted.

New child file:

- `qrpc_compiler/src/runtime_protocol_lowering/intent_signal_lowering/fallback_description.rs`

The child now owns:

- `describe_runtime_intent_condition`

The parent `qrpc_compiler/src/runtime_protocol_lowering/intent_signal_lowering.rs` now keeps:

- `mod fallback_description`;
- parent-facing `lower_runtime_intent_to_indicator`;
- parent-facing `lower_runtime_intent_to_signal_rule`;
- fallback delegation through `fallback_description::describe_runtime_intent_condition(intent)`;
- `lower_runtime_intent_kind`.

## Equivalence Notes

The extraction preserves:

- `ScalarExpr::RawText { source }` fallback construction in `lower_runtime_intent_to_signal_rule`;
- all fallback string formats and parameter defaults;
- spread observe fallback formatting through parent/shared decode helpers;
- closed `condition_lowering` ownership;
- runtime indicator and signal public output shape.

## Boundary

**Changed Rust files**:
- `qrpc_compiler/src/runtime_protocol_lowering/intent_signal_lowering.rs`
- `qrpc_compiler/src/runtime_protocol_lowering/intent_signal_lowering/fallback_description.rs`

**Unchanged ownership**:
- `condition_lowering` remains closed.
- `lower_runtime_intent_kind` remains parent residual.
- parent-facing indicator/signal facade remains parent residual.
- runtime orchestration, data binding, agent policy, risk policy, Strategy IR lowering, tests, canonical hash, and release transition remain outside this child.

## Next Step

BE-001XR-03 `root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.fallback_description` single_leaf_closeout.

## Gates

- `cargo fmt --check`
- `cargo test -p qrpc-compiler`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
