# v4.16.0 root.contracts.compiler_bridge.runtime_protocol_lowering.agent_policy_lowering equivalence baseline and extraction plan

> Batch: BE-001XU-01
> Node: `root.contracts.compiler_bridge.runtime_protocol_lowering.agent_policy_lowering`
> Parent: `root.contracts.compiler_bridge.runtime_protocol_lowering`
> Stage: `baseline_plan`
> Movement: no Rust code movement.

---

## Summary

`root.contracts.compiler_bridge.runtime_protocol_lowering.agent_policy_lowering` is frozen before physical Rust movement.

This child owns runtime `AgentConfig` to Core IR `AgentPolicy` conversion and runtime rebalance schedule mapping.

Decision:

`baseline_frozen: true`

Next movement:

`BE-001XU-02` may create `qrpc_compiler/src/runtime_protocol_lowering/agent_policy_lowering.rs` or equivalent private child module and move only the agent policy symbols listed below.

## Allowed Movement Set

The extraction step may move these symbols:

| Symbol | Responsibility |
| --- | --- |
| `lower_runtime_agent_to_policy` | Convert one runtime `AgentConfig` and referenced runtime intents into one Core IR `AgentPolicy`. |
| `lower_runtime_rebalance_schedule_to_core_ir` | Map runtime rebalance schedule variants to Core IR rebalance schedule variants. |

## Parent Calls Frozen

After extraction:

- `lower_runtime_protocol_to_core_ir_with_metadata` may call the child while building `core_ir.agent_policies`.
- The child may receive `&AgentConfig` and `&[IntentConfig]` and return `AgentPolicy`.
- Parent keeps validation delegation, data binding construction, indicator/signal lowering, risk policy lowering, DAG validation, and public runtime compile/lower facades.

## Equivalence Baseline

BE-001XU-02 must preserve:

- referenced intent filtering by `agent.input_intent_ids`;
- `all_observe` detection requiring non-empty referenced intents and every referenced intent being `IntentKind::QuoteObserve`;
- `portfolio_rebalance` detection from `agent.params["portfolio_rebalance"] > 0.5`;
- agent kind selection order: CrossVenueArbitrage, then PortfolioRebalance, then WeightedSignals;
- `agent_id`, `name`, `input_signal_ids`, `rebalance_symbols`, `rebalance_allocation_kind`, `rebalance_rank_method`, `rebalance_score_normalize`, `rebalance_target_weights`, and `enabled` field projection;
- rebalance schedule mapping for EverySlow, Every1d, and Weekly;
- `decision_threshold` absent for all-observe agents and defaulting to `0.05` for non-observe agents;
- `max_quantity_ratio` defaulting to `0.5` for all-observe, `1.0` for portfolio rebalance, and `0.8` otherwise;
- `spread_trigger_bps` existing only for all-observe agents and defaulting to `50.0`;
- public runtime Core IR output shape.

## Hard Exclusions

This baseline must not:

- move or edit `lower_runtime_protocol_to_core_ir_with_metadata`;
- move or edit `lower_runtime_risk_to_policy`;
- move or edit `intent_signal_lowering`;
- change runtime validation delegation, data binding construction, DAG validation, Strategy IR lowering, shared spread/custom helpers, canonical hash, tests, or release transition;
- mix Strategy IR agent lowering from `qrpc_compiler/src/lib.rs` into this runtime child.

## Boundary

**Real files**:
- `qrpc_compiler/src/runtime_protocol_lowering.rs`

**Markers**:
- `BE-001XU-01`
- `agent_policy_lowering baseline_frozen: true`
- `only runtime agent policy symbols may move`
- `risk policy excluded`

**Next step**:
BE-001XU-02 root.contracts.compiler_bridge.runtime_protocol_lowering.agent_policy_lowering extract_closeout

---

## Gates

- `cargo test -p qrpc-compiler`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
