# v4.16.0 root.contracts.quantscript parent residual judgment selects legacy_config_compat

> Batch: BE-002FY-01
> Node: `root.contracts.quantscript`
> Selected child: `root.contracts.quantscript.legacy_config_compat`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Parent State

Closed QuantScript children:

- `contracts.quantscript.syntax_ast_surface`

Remaining QuantScript queue:

- `legacy_config_compat`
- `typed_resolution`
- `analysis_diagnostics`
- `evaluator_normalization`
- `runtime_lowering`
- `v4_static_audit`
- `test_plan_surface`
- `authoring_docs_samples`

## Decision

Select `root.contracts.quantscript.legacy_config_compat` as the next child.

This child owns deprecated config-style QuantScript compatibility that remains in the crate root after the formal syntax surface extraction. It is separate from the formal parser/AST child and should be isolated so the root facade can become a thinner parent.

## Candidate Physical Surface

Current surface in `quantscript/src/lib.rs`:

- `QuantScriptProgram`
- `RuntimeSection`
- `DataSection`
- `IntentSection`
- `AgentSection`
- `RiskSection`
- `Token`
- `Value`
- `QuantScriptSource`
- `parse_quant_script`
- `parse_quant_script_source`
- `parse_quant_script_config`
- `compile_quant_script`
- `compile_quant_script_file`
- `compile_program_to_config`
- legacy tokenizer/parser helpers and legacy config tests.

## Boundary

The next baseline must freeze only legacy config compatibility. It must not change:

- formal parser/AST/type/HIR surfaces already closed under `syntax_ast_surface`;
- typed resolution, analysis, evaluator, lowering, v4 static audit, or test-plan behavior;
- backend, runtime, executor, frontend, plugin metadata, or release-transition behavior.

## Why This Child Next

| Criterion | Judgment |
| --- | --- |
| Queue order | It follows the closed syntax/AST surface in the frozen QuantScript baseline. |
| Cohesion | Strong: all selected items support deprecated config-style compatibility. |
| Root cleanup value | High: extracting this child will leave `lib.rs` closer to a true parent facade. |
| Risk | Medium-low if public deprecated entrypoints and test expectations are preserved. |

## Next Step

BE-002FZ-01 `root.contracts.quantscript.legacy_config_compat` baseline_plan.
