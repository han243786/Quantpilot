# v4.16.0 root.contracts.core_ir.v4_contracts.static_contract_bundle single leaf closeout continues split

> Batch: BE-001VW-03
> Node: `root.contracts.core_ir.v4_contracts.static_contract_bundle`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`static_contract_bundle` is extracted, but it should continue recursively as a parent module rather than close as one final leaf.

Current ownership:

- `V4StaticContractBundle` aggregate DTO
- default construction for all sibling static contracts
- static bundle schema version default helper
- `V4StaticContractBundle::validate_static_contract`

The schema/default surface and the cross-contract validation behavior have separate change reasons. The next child should isolate `static_validation` so bundle field ownership and validation orchestration can be governed separately while preserving the public facade.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `static_contract_bundle` is named and extracted, but contains both schema/default and validation behavior. |
| parent_child_communication_kept | pass | A nested validation child can use the parent bundle type and keep the public facade unchanged. |
| equivalence_baseline_freezable | pass | Validation order, delegated validations, and error strings are finite and already covered by qrpc-core-ir tests. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | satisfied | `V4StaticContractBundle::validate_static_contract` is public behavior on the static bundle API. |
| state_machine_phase | no | Static bundle validation is contract aggregation, not state-machine topology. |
| strategy_branch | no | This is not strategy branch behavior. |
| independent_failure_mode | pass | Validation orchestration can fail independently from DTO/default field ownership. |
| reuse_pressure | pass | Capability reports and tests reuse static validation through the public bundle method. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | `static_validation` would own a public method and a coherent validation phase. |
| communication_cost_rises | no | One nested child avoids scattering validation while separating it from schema/default ownership. |
| local_proof_missing | no | `cargo test -p qrpc-core-ir` and `cargo check -p quantpilot` passed after the parent extraction. |
| line_count_only | no | Continue decision is based on schema-vs-validation ownership separation. |

leaf_split_decision_result

`continue_split: true`

Selected next child:

`root.contracts.core_ir.v4_contracts.static_contract_bundle.static_validation`

next_recursive_step

BE-001VX-01 root.contracts.core_ir.v4_contracts.static_contract_bundle parent_residual_judgment selects static_validation
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/static_contract_bundle.rs`

**Markers**:
- `BE-001VW-03`

**Next step**:
BE-001VX-01 root.contracts.core_ir.v4_contracts.static_contract_bundle parent_residual_judgment selects static_validation

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
