# v4.16.0 root.contracts.runtime_support.v4_runtime_support.test_harness.fixture_builders single leaf closeout

> Batch: BE-002FB-02
> Node: `root.contracts.runtime_support.v4_runtime_support.test_harness.fixture_builders`
> Parent: `root.contracts.runtime_support.v4_runtime_support.test_harness`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Set `stop_split: true` for `fixture_builders`.

The child is inside the target terminal size band and has one clear owner: shared v4 runtime test fixture construction. Further splitting would create micro-children around fixture families that are intentionally shared by the scenario tests.

## Leaf Granularity Evidence

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -Path qrpc_runtime\src\v4_runtime\test_harness\fixture_builders.rs
```

Result:

| Field | Value |
| --- | --- |
| `split_decision` | `STOP` |
| `governance_packaging` | `stop_split` |
| `final_decision` | `STOP` |
| Score | `36` |
| LOC | `229` |
| Functions | `8` |
| Public/internal-visible surface | `8` |
| Branches | `4` |
| Domains | `6` |
| Risk tags | `compiler_contract`, `cross_crate`, `persistence`, `public_api`, `route_schema`, `state_machine`, `trading_semantics` |
| Terminal size bucket | `terminal_target_150_600` |

## Stop-Split Rationale

Rejected finer splits:

| Candidate | Decision | Reason |
| --- | --- | --- |
| Core IR fixture child | Reject | `sample_core_ir_for_v4_runtime` only feeds the shared compat graph fixture. |
| capability matrix fixture child | Reject | The matrix helpers are small variants of the same capability fixture owner. |
| nested graph fixture child | Reject | `nested_observation_graph` shares the base compat graph and is still fixture construction. |

## Evidence

Inherited from BE-002FB-01 extraction:

- `cargo fmt --check`: PASS
- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime v4 -- --nocapture`: PASS, 32 tests
- `cargo check -p quantpilot`: PASS

This closeout is docs-only and updates the recursive cursor plus governance indexes.

## Next Step

BE-002FC-01 `root.contracts.runtime_support.v4_runtime_support.test_harness` parent_residual_judgment.
