# v4.16.0 root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface parent closeout

> Batch: BE-002IM-01
> Node: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface`
> Parent: `root.contracts.quantscript.runtime_lowering`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`lowering_orchestrator_surface` is closed with `parent_closeout: true`.

The closed child set now covers:

- `profile_detection_surface`
- `integration_test_harness`
- `entrypoint_runtime_config_assembly`

## Parent Residual Judgment

The residual parent `quantscript/src/lowering/orchestrator.rs` is a compact public facade:

- `lower_script_to_runtime_config`
- `lower_script_to_runtime_config_with_context`
- `entrypoint_runtime_config_assembly` child declaration
- `integration_test_harness` test declaration

There is no remaining cohesive implementation body to extract. The public entrypoints intentionally stay parent-owned because they are the stable interface for runtime lowering callers.

## Leaf Granularity Evidence

Command:

- `tools/evaluate-leaf-granularity.ps1 -LeafId root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface -ParentId root.contracts.quantscript.runtime_lowering -Path quantscript/src/lowering/orchestrator.rs -Depth 4`

Result:

- `Decision: STOP`
- `Split decision: STOP`
- `Governance packaging: stop_split`
- `Score: 24`
- `loc=15`
- `functions=2`
- `public_surface=2`
- `branches=0`
- `domains=3`
- `risk_tags=cross_crate,public_api`
- `size_bucket=micro_under_100`
- `governance_mode=stop_split`

## White-Box Boundary

| Field | Result |
| --- | --- |
| Parent input | `ScriptModule` plus optional `LoweringContext` |
| Parent output | `RuntimeProtocolCoreConfig` or `anyhow::Error` |
| Child implementation owner | `entrypoint_runtime_config_assembly` |
| Test owner | `integration_test_harness` |
| Public API impact | None; facade signatures unchanged |

## Closeout Evidence

- `profile_detection_surface` is closed.
- `integration_test_harness` is closed.
- `entrypoint_runtime_config_assembly` is closed.
- Latest extraction validation passed `cargo test -p quantscript` with 88 tests and `cargo check -p quantpilot`.

## Next Step

Return to `root.contracts.quantscript.runtime_lowering` parent residual judgment.
