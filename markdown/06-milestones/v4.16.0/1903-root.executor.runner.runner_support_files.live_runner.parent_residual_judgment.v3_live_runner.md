# v4.16.0 root.executor.runner.runner_support_files.live_runner parent residual judgment selects v3_live_runner

> Batch: BE-002ME-02
> Parent: `root.executor.runner.runner_support_files.live_runner`
> Stage: `parent_residual_judgment`
> Selected child: `root.executor.runner.runner_support_files.live_runner.v3_live_runner`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `v3_live_runner` as the first child under the `live_runner` parent.

## Evidence

`src-executor/live_runner.rs` begins with a cohesive V3 runner group:

- `LiveRunner`
- `RunnerStatus`
- `LiveRunner::from_strategy`
- `LiveRunner::handle_ws_event`
- `run_fast_cycle`
- `run_slow_cycle`
- `detect_trigger`
- `is_subscribed_to`

This group owns V3 websocket event handling, K-line buffering, fast/slow runtime
cycle invocation, daily order count guard, and trigger broadcast projection.

## Boundary

The next baseline may freeze movement for the V3 live runner group only. It
must not move `RunnerInstance`, `RunnerPool`, `V4Runner`, V4 market helpers, or
local tests in the same extraction step.

## Next Step

BE-002MF-01
`root.executor.runner.runner_support_files.live_runner.v3_live_runner`
baseline_plan.
