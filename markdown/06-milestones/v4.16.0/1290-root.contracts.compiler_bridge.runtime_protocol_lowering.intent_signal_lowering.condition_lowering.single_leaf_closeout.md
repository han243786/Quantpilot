# v4.16.0 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.condition_lowering single leaf closeout stops split

> Batch: BE-001XP-01
> Node: `root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.condition_lowering`
> Parent: `root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering`
> Stage: `single_leaf_closeout`
> Movement: no Rust code movement; post-extraction closeout only.

---

## Summary

`root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.condition_lowering` is closed as a stable child leaf.

The leaf owns one parent-facing structured condition function and tightly coupled comparison metadata helpers. Further splitting would create helper-only micro leaves (`comparison_decode`, spread threshold helper, shape enum) without independent public owner and would increase parent-child communication cost.

Decision:

`stop_split: true`

Next step:

Return to `root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering` parent residual judgment.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | Leaf is named `condition_lowering` and owns structured runtime condition construction plus comparison decode helpers. |
| parent_child_communication_kept | pass | Parent `intent_signal_lowering` calls one child entrypoint and receives `Option<ScalarExpr>`. |
| equivalence_baseline_freezable | pass | qrpc-compiler tests cover MA, RSI, Momentum, ZScore, and spread structured condition outputs. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | false | The leaf is private and not a public facade. |
| state_machine_phase | true | It owns the structured expression-construction phase within intent signal lowering. |
| strategy_branch | true | It is separate from fallback description and intent kind mapping. |
| independent_failure_mode | true | Unsupported comparison metadata returns `None` and falls back in the parent. |
| reuse_pressure | true | Comparison decoding is reused by multiple structured condition branches. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | true | Further children would be private helper-only fragments without independent owner. |
| communication_cost_rises | true | Splitting comparison decode/spread helper would add plumbing across a cohesive condition leaf. |
| local_proof_missing | false | Existing tests prove the leaf's representative outputs. |
| line_count_only | false | Stop decision is based on ownership and communication cost, not line count. |

leaf_split_decision_result

`stop_split_true`

Add `contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.condition_lowering` to `closed_children`.

next_recursive_step

BE-001XQ-01 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering parent_residual_judgment
## Boundary

**Real files**:
- `qrpc_compiler/src/runtime_protocol_lowering/intent_signal_lowering/condition_lowering.rs`

**Markers**:
- `BE-001XP-01`
- `condition_lowering stop_split: true`
- `comparison decode kept local`
- `return to intent_signal_lowering residual judgment`

**Next step**:
BE-001XQ-01 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering parent_residual_judgment

---

## Gates

- `cargo test -p qrpc-compiler`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
