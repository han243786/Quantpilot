# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline.unified_timeline baseline plan

> Batch: BE-002CP-01
> Node: `root.contracts.runtime_support.sandbox_replay_timeline.unified_timeline`
> Parent: `root.contracts.runtime_support.sandbox_replay_timeline`
> Stage: `baseline_plan`
> Movement: No Rust code movement.

---

## Scope

Freeze the baseline for extracting `UnifiedTimeline` from `qrpc_runtime/src/sandbox/timeline.rs`.

Allowed movement:

- add `qrpc_runtime/src/sandbox/unified_timeline.rs`;
- move only `UnifiedTimeline` and its `impl` block;
- keep `timeline.rs` as the parent that declares/re-exports provider and unified-timeline children;
- keep local tests in `timeline.rs` until a dedicated test-harness baseline.

## In-Scope Surface

- `UnifiedTimeline`
- `UnifiedTimeline::new`
- `UnifiedTimeline::from_providers`
- `UnifiedTimeline::collect_at`
- `UnifiedTimeline::len`
- `UnifiedTimeline::is_empty`

## Out of Scope

- `TimelineDataProvider`, `KlineProvider`, `QuoteProvider`, `ResampleKlineProvider`;
- replay builder functions in `replay.rs`;
- sandbox mode/control surface in `mod.rs`;
- realtime and fast-backtest sandbox implementations;
- timeline tests.

## Equivalence Baseline

The next actual extraction must preserve:

- merged timestamp construction;
- slow and fast trigger index calculation;
- provider vector assembly;
- empty provider failure behavior;
- `collect_at` behavior;
- replay builder and sandbox access through the existing `timeline` parent surface.

## Dependency Rule

The unified timeline child may use provider types through its parent. Replay builder and sandbox implementation code must continue to access `UnifiedTimeline` through parent-owned declarations/re-exports.

## Gates

Required gates for actual extraction:

- `cargo fmt`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime sandbox -- --nocapture`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 / full feature tree / matrix governance

## Next Step

BE-002CQ-01 `root.contracts.runtime_support.sandbox_replay_timeline.unified_timeline` actual_extraction.
