# v4.16.0 root.contracts.core_ir.v4_contracts.backtest_artifact_contract equivalence baseline and extraction plan

> Batch: BE-001TX-01
> Node: `root.contracts.core_ir.v4_contracts.backtest_artifact_contract`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `baseline_plan`
> Movement: No code movement.

---

## Summary

This baseline freezes the exact public DTO family that may be extracted from `qrpc_core_ir/src/v4.rs` into the next v4 child module.

The selected child owns only the v4 backtest artifact schema family:

- `V4BacktestArtifact`
- `V4BacktestMicrostructureMetrics`
- `V4BacktestMachineTrajectoryPoint`
- `V4BacktestRiskPlaneDecisionRecord`
- `V4BacktestExecutionCapabilitySourceRecord`

No taxonomy enums, version constants, validators, runtime handlers, persistence surfaces, frontend consumers, or tests move in this baseline.

## Frozen Schema Surface

### `V4BacktestArtifact`

The extracted child must preserve these fields and serde attributes exactly:

| Field | Type | Serde/default behavior |
| --- | --- | --- |
| `schema_version` | `String` | `#[serde(default = "default_v4_backtest_artifact_version")]` |
| `graph_id` | `String` | required |
| `started_at_ms` | `u64` | required |
| `ended_at_ms` | `u64` | required |
| `replay_mode` | `String` | required |
| `input_bar_count` | `usize` | required |
| `input_tick_count` | `Option<usize>` | `#[serde(skip_serializing_if = "Option::is_none")]` |
| `symbols` | `Vec<String>` | `#[serde(default)]` |
| `machine_trajectory` | `Vec<V4BacktestMachineTrajectoryPoint>` | `#[serde(default)]` |
| `risk_plane_decisions` | `Vec<V4BacktestRiskPlaneDecisionRecord>` | `#[serde(default)]` |
| `execution_capability_sources` | `Vec<V4BacktestExecutionCapabilitySourceRecord>` | `#[serde(default)]` |
| `microstructure_metrics` | `Option<V4BacktestMicrostructureMetrics>` | `#[serde(skip_serializing_if = "Option::is_none")]` |
| `final_snapshot` | `Option<Value>` | `#[serde(skip_serializing_if = "Option::is_none")]` |

### `V4BacktestMicrostructureMetrics`

The extracted child must preserve `Default` derive and these fields exactly:

- `submitted_order_count: u64`
- `filled_order_count: u64`
- `fill_rate: f64`
- `average_slippage_bps: f64`
- `queue_position_estimate: f64`
- `vwap_deviation_bps: f64`

### `V4BacktestMachineTrajectoryPoint`

The extracted child must preserve:

- `ts_ms: u64`
- `event_sequence: u64`
- `machine_id: String`
- `template: MachineTemplateKind`
- `state_id: String`
- `status: String`
- `symbol: Option<String>` with `#[serde(skip_serializing_if = "Option::is_none")]`

### `V4BacktestRiskPlaneDecisionRecord`

The extracted child must preserve:

- `decision_id: String`
- `target_machine_id: String`
- `source_machine_id: String`
- `event_type: String`
- `approved: bool`
- `reason: String`
- `ts_ms: u64`
- `sequence: u64`
- `symbol: Option<String>` with `#[serde(skip_serializing_if = "Option::is_none")]`

### `V4BacktestExecutionCapabilitySourceRecord`

The extracted child must preserve:

- `decision_id: String`
- `target_machine_id: String`
- `venue_id: String`
- `runtime_mode: RuntimeTradingMode`
- `accepted: bool`
- `reason: String`
- `capability: ExecutionCapabilityKind`
- `source: CapabilitySupportSource`
- `status: String`
- `ts_ms: u64`
- `sequence: u64`
- `symbol: Option<String>` with `#[serde(skip_serializing_if = "Option::is_none")]`

## Explicit Non-Owners

This child does not own and must not move:

- `MachineTemplateKind`; it remains machine-contract taxonomy.
- `RuntimeTradingMode`; it remains runtime-mode contract taxonomy.
- `ExecutionCapabilityKind` and `CapabilitySupportSource`; they remain venue capability matrix taxonomy.
- `default_v4_backtest_artifact_version`; it remains default helper surface unless a later separate judgment moves it.
- `V4_BACKTEST_ARTIFACT_VERSION`; it remains in `schema_identity_constants`.
- Validators, serde tests, runtime execution, projection, persistence, backend handlers, or frontend artifact handling.

## Allowed BE-001TX-02 Movement

The next extraction step may:

- create `qrpc_core_ir/src/v4/backtest_artifact_contract.rs`;
- add `mod backtest_artifact_contract;` to `qrpc_core_ir/src/v4.rs`;
- add `pub use backtest_artifact_contract::*;` to `qrpc_core_ir/src/v4.rs`;
- move only the five frozen DTO definitions into the child module;
- import parent-mediated dependencies with `super::{...}` from the child module.

The child may depend on parent-mediated names:

- `default_v4_backtest_artifact_version`
- `CapabilitySupportSource`
- `ExecutionCapabilityKind`
- `MachineTemplateKind`
- `RuntimeTradingMode`

## Forbidden BE-001TX-02 Movement

The next extraction step must not:

- move `MachineTemplateKind`;
- move `RuntimeTradingMode`;
- move `ExecutionCapabilityKind`;
- move `CapabilitySupportSource`;
- move `default_v4_backtest_artifact_version`;
- move any version constant from `schema_identity_constants`;
- alter serde field names, defaults, skip rules, derives, visibility, or field order;
- alter behavior outside module wiring.

## Equivalence Proof Plan

BE-001TX-02 must pass:

- `cargo fmt --check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-001TX-02 `root.contracts.core_ir.v4_contracts.backtest_artifact_contract` extract_closeout.
