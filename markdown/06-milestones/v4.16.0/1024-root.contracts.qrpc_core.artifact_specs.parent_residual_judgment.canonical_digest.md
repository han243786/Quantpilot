# v4.16.0 root.contracts.qrpc_core.artifact_specs parent residual judgment selects canonical_digest

> Batch: BE-001RT-01
> Node: `root.contracts.qrpc_core.artifact_specs`
> Selected child: `root.contracts.qrpc_core.artifact_specs.canonical_digest`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core.artifact_specs` returns to its child queue after BE-001RS-03 kept the parent open.

Decision:

`next_child: root.contracts.qrpc_core.artifact_specs.canonical_digest`

## Open Artifact Specs Children

| Child | Status |
| --- | --- |
| `contracts.qrpc_core.artifact_specs.canonical_digest` | Selected next. Owns `ArtifactDigestAlgorithm`, `ArtifactDigest`, and `canonical_json_sha256_digest`. |
| `contracts.qrpc_core.artifact_specs.run_backtest_specs` | Queued. Owns run/backtest modes, dataset and execution assumption projections, market data snapshot specs, `RunSpec`, and `BacktestSpec`. |
| `contracts.qrpc_core.artifact_specs.artifact_bundle_contract` | Queued. Owns strategy/core-IR/compile artifact DTOs and `CompileArtifactBundle`. |

## Selection Rationale

`canonical_digest` is selected because it is the smallest independent artifact specs child:

- it owns the digest algorithm enum;
- it owns the digest result DTO;
- it owns the canonical JSON SHA-256 helper;
- it has direct test evidence through `canonical_digest_is_stable_for_equivalent_payloads`;
- it can move before run/backtest specs because those later children only consume `ArtifactDigest` as a public type.

## Same-Parent Parallel Judgment

`same_parent_parallel_children` remains inactive for this selection.

The digest helper is small, public, and deterministic. Serial extraction keeps the parent facade simple and protects the canonical digest test before other artifact spec children move.

## Hard Boundaries

The next `canonical_digest` baseline must not:

- edit Rust source code;
- change digest algorithm variant names, serde rename behavior, digest DTO field names, helper signature, JSON serialization source, SHA-256 hashing, hex formatting, public exports, or tests;
- move run/backtest specs, artifact bundle DTOs, runtime IO DTOs, RFC execution contracts, or crate-root tests;
- change closed qrpc-core children or release transition.

## Next Step

BE-001RU-01 `root.contracts.qrpc_core.artifact_specs.canonical_digest` baseline_plan.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
