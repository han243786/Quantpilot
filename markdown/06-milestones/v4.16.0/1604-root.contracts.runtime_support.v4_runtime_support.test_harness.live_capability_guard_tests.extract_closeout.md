# v4.16.0 root.contracts.runtime_support.v4_runtime_support.test_harness.live_capability_guard_tests extract closeout

> Batch: BE-002FQ-01
> Node: `root.contracts.runtime_support.v4_runtime_support.test_harness.live_capability_guard_tests`
> Parent: `root.contracts.runtime_support.v4_runtime_support.test_harness`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> governance_next_authority: active

---

## Decision

Extract `live_capability_guard_tests` as a test-only child.

The extraction moved eleven live/capability/risk guard tests into a dedicated child module while preserving all assertions, error-message checks, event expectations, capability decision checks, and production behavior.

## Code Movement

| Path | Change |
| --- | --- |
| `qrpc_runtime/src/v4_runtime/test_harness/live_capability_guard_tests.rs` | New test-only child owning live/capability/risk guard tests. |
| `qrpc_runtime/src/v4_runtime/test_harness.rs` | Adds `mod live_capability_guard_tests;` and removes the moved test bodies. |

Moved tests:

- `v4_runtime_rejects_order_when_request_uses_undeclared_capability`
- `v4_runtime_rejects_live_simulated_mode_until_supported`
- `v4_live_actual_boundary_allows_provider_actual_mode_but_keeps_submit_detached`
- `v4_live_actual_rejects_runtime_simulated_capability_source`
- `v4_live_actual_without_risk_plane_is_rejected`
- `v4_live_actual_without_execution_capability_policy_is_rejected`
- `v4_runtime_rejects_unsupported_execution_capability_before_execution`
- `v4_runtime_rejects_provider_native_capability_in_paper_simulated`
- `v4_runtime_rejects_missing_execution_capability_policy`
- `v4_runtime_rejects_forged_external_risk_plane_event_for_execution`
- `v4_runtime_rejects_execution_event_from_non_risk_plane_source`

## Equivalence

The migration preserves:

- all 32 v4 runtime tests;
- live actual and live simulated mode boundary checks;
- execution capability policy missing/unsupported/mode rejected checks;
- provider-native versus runtime-simulated source checks;
- risk-plane source guard checks;
- undeclared simulated order capability rejection checks;
- parent-mediated test access through `use super::*`;
- no production runtime code movement.

Observed namespace change:

- from `v4_runtime::test_harness::*`
- to `v4_runtime::test_harness::live_capability_guard_tests::*` for the moved tests

## Evidence

Verified after extraction:

| Gate | Result |
| --- | --- |
| `cargo fmt --check` | PASS |
| `cargo check -p qrpc-runtime` | PASS |
| `cargo test -p qrpc-runtime v4 -- --nocapture` | PASS, 32 tests |
| `cargo check -p quantpilot` | PASS |

Governance gates are run at commit time for this batch:

- `git diff --check`;
- `tools/check-utf8.ps1`;
- `tools/check-full-feature-tree.ps1`;
- `tools/check-matrix-governance.ps1`.

## Next Step

BE-002FQ-02 `root.contracts.runtime_support.v4_runtime_support.test_harness.live_capability_guard_tests` single_leaf_closeout.
