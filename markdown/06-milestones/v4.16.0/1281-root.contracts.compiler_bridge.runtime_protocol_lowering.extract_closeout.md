# v4.16.0 root.contracts.compiler_bridge.runtime_protocol_lowering actual extraction complete

> Batch: BE-001XG-01
> Node: `root.contracts.compiler_bridge.runtime_protocol_lowering`
> Parent: `root.contracts.compiler_bridge`
> Stage: `extract_closeout`
> Movement: Rust code movement complete.

---

## Summary

`root.contracts.compiler_bridge.runtime_protocol_lowering` actual extraction is complete.

`qrpc_compiler/src/runtime_protocol_lowering.rs` now owns runtime protocol Core IR lowering and runtime-only helper functions. The parent `qrpc_compiler/src/lib.rs` preserves public compile/lower facades and delegates `lower_runtime_protocol_to_core_ir_with_metadata` into the child.

## Extracted Ownership

Moved into the child module:

- runtime protocol Core IR construction and DAG validation;
- data binding source hint lowering;
- runtime intent to indicator/signal conversion;
- runtime structured comparison decoding;
- runtime agent/risk policy lowering;
- runtime fallback condition description.

Kept in the parent:

- public compile facade and canonical config hash assembly;
- public default metadata wrapper;
- Strategy IR lowering;
- shared spread/custom expression helpers;
- test fixture module;
- validation child delegation.

## Equivalence Evidence

- `cargo test -p qrpc-compiler` passed with 28 tests.
- `cargo check -p quantpilot` passed.
- Public signatures remain unchanged.
- Parent-child communication is through the compiler bridge parent facade; no sibling link or release-transition shortcut was introduced.

## Boundary

**Real files**:
- `qrpc_compiler/src/lib.rs`
- `qrpc_compiler/src/runtime_protocol_lowering.rs`
- `qrpc_compiler/src/runtime_protocol_validation.rs`

**Markers**:
- `BE-001XG-01`
- `root.contracts.compiler_bridge.runtime_protocol_lowering extracted: true`
- `runtime lowering child module created`
- `parent public facades preserved`

**Next step**:
BE-001XH-01 root.contracts.compiler_bridge.runtime_protocol_lowering single_leaf_closeout

---

## Gates

- `cargo test -p qrpc-compiler`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
