# v4.16.0 root.contracts.runtime_support.agent_module.test_harness baseline plan

> Batch: BE-002AF-01
> Node: `root.contracts.runtime_support.agent_module.test_harness`
> Parent: `root.contracts.runtime_support.agent_module`
> Stage: `baseline_plan`
> Movement: No Rust code movement.

---

## Scope

This baseline freezes the local agent-module unit-test harness before extraction.

Current owner file:

- `qrpc_runtime/src/agent_module.rs`

Planned child file:

- `qrpc_runtime/src/agent_module/test_harness.rs`

## Allowed Movement

The next extraction may move only:

- `#[cfg(test)] mod tests` from `qrpc_runtime/src/agent_module.rs` into a test-only child module.

The parent may add:

- `#[cfg(test)] mod test_harness;`

The child file may keep:

- `use super::*;`
- existing test-only imports;
- all existing fixture helpers and tests.

## Parent-Owned Surface To Keep

The parent keeps:

- production dispatch and event assembly;
- public request/output/provider/builtin types;
- policy child declarations;
- shared helper ownership;
- helper constants, including `DEFAULT_COST_BUFFER_BPS`.

## Boundaries To Preserve

| Boundary | Requirement |
| --- | --- |
| Production | No production behavior, imports, signatures, or helper ownership changes. |
| Test access | Test child continues to access parent-owned private helpers through the parent module boundary. |
| Fixtures | Sample execution rule, Core IR, portfolio, and signal fixtures remain equivalent. |
| Assertions | All weighted signal, arbitrage, rebalance, cadence, and inheritance assertions remain equivalent. |
| Visibility | No helper is made more public solely for tests. |

## Exclusions

The next extraction must not move:

- production agent dispatch;
- policy child code;
- shared helper functions or constants;
- provider trait or public DTOs.

## Risk Notes

This is a low production-risk extraction with medium test-visibility risk. Prefer moving the whole test module as one unit and preserving `use super::*` rather than rewriting fixtures or widening visibility.

## Equivalence Gates

Required for the next extraction:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-002AF-02 `root.contracts.runtime_support.agent_module.test_harness` actual_extraction.
