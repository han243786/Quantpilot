# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.risk_plane_validation equivalence baseline and extraction plan

> Batch: BE-001UK-01
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.risk_plane_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation`
> Stage: `baseline_plan`
> Movement: No code movement.

---

## Summary

This baseline freezes `V4MachineGraphContract::validate_risk_plane` before extraction.

The selected child may own only:

- `V4MachineGraphContract::validate_risk_plane`

No graph root validation orchestration, DAG validation, event catalog usage validation,
event party helper, graph schema, traversal helper, event catalog schema, bridge code,
or tests move in this baseline.

## Frozen Validation Surface

The next extraction must preserve:

- execution machine detection by `MachineTemplateKind::Execution`;
- missing risk plane error:
  `execution machine graphs must declare a dedicated risk_plane`;
- execution graph required flag error:
  `execution machine graphs must require the risk_plane`;
- required risk-plane empty machine list error:
  `required risk_plane must list at least one machine_id`;
- risk-plane minimum priority error:
  `risk_plane min_priority must be at least {V4_RISK_PLANE_MIN_PRIORITY}`;
- risk-plane machine lookup against the top-level `machines_by_id` map;
- risk-plane machine Decision template requirement;
- risk-plane machine priority floor comparison;
- unknown risk-plane machine error formatting;
- execution inbound edge scan against `self.edges`;
- inbound edge source restriction to risk-plane machines;
- missing risk-plane inbound edge error formatting;
- branch order and returned `Vec<String>` error accumulation behavior.

## Allowed BE-001UK-02 Movement

The next extraction step may:

- create `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/risk_plane_validation.rs`;
- add `mod risk_plane_validation;` to `qrpc_core_ir/src/v4/machine_graph_contract/static_validation.rs`;
- move only `V4MachineGraphContract::validate_risk_plane`;
- keep `validate_static_contract` calling `self.validate_risk_plane(&machines_by_id)`.

The child may depend on:

- `std::collections::{BTreeMap, BTreeSet}`;
- `MachineTemplateKind`;
- `V4MachineContract`;
- `V4MachineGraphContract`;
- `V4_RISK_PLANE_MIN_PRIORITY`.

## Forbidden BE-001UK-02 Movement

The next extraction step must not move or edit behavior for:

- `V4MachineGraphContract::validate_static_contract`;
- `V4MachineGraphContract::validate_graph_acyclic`;
- `V4MachineGraphContract::validate_event_catalog`;
- `machine_event_party_allowed`;
- graph schema DTOs;
- event catalog DTOs or `MachineEventCatalog::validate_static_contract`;
- `collect_machine_family`;
- `machine_nested_depth`;
- complexity metric code;
- bridge builders;
- tests.

It must not alter validation branch order, error strings, risk-plane membership semantics,
or execution inbound-edge validation semantics.

## Equivalence Proof Plan

BE-001UK-02 must pass:

- `cargo fmt --check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation.rs`

**Planned child file**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/risk_plane_validation.rs`

**Markers**:
- `BE-001UK-01`

**Next step**:
BE-001UK-02 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.risk_plane_validation extract_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
