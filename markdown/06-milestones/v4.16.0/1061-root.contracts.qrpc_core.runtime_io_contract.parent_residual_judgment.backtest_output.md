# v4.16.0 root.contracts.qrpc_core.runtime_io_contract parent residual judgment selects backtest_output

> Batch: BE-001SM-01
> Node: `root.contracts.qrpc_core.runtime_io_contract`
> Selected child: `root.contracts.qrpc_core.runtime_io_contract.backtest_output`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core.runtime_io_contract` returns to its child queue after `runtime_output` closeout.

Decision:

`next_child: root.contracts.qrpc_core.runtime_io_contract.backtest_output`

## Closed Runtime IO Children

| Child | Result |
| --- | --- |
| `contracts.qrpc_core.runtime_io_contract.market_data_io` | Closed with `stop_split: true`. |
| `contracts.qrpc_core.runtime_io_contract.decision_flow` | Closed with `stop_split: true`. |
| `contracts.qrpc_core.runtime_io_contract.execution_io` | Closed with `stop_split: true`. |
| `contracts.qrpc_core.runtime_io_contract.portfolio_state` | Closed with `stop_split: true`. |
| `contracts.qrpc_core.runtime_io_contract.runtime_output` | Closed with `stop_split: true`. |

## Open Runtime IO Residuals

| Residual | Status |
| --- | --- |
| `contracts.qrpc_core.runtime_io_contract.backtest_output` | Selected next. Owns backtest equity, metric groups, summary, period returns, and final backtest output DTOs. |

## Selection Rationale

`backtest_output` is selected because it is the final contiguous runtime IO family in `qrpc_core/src/runtime_io_contract.rs`:

- it owns `BacktestEquityPoint`;
- it owns backtest metric group DTOs;
- it owns `BacktestSummary`;
- it owns `PeriodReturn`;
- it owns `BacktestOutput`;
- it consumes closed `SessionOutput` and `PortfolioState` through the runtime IO parent facade.

## Same-Parent Parallel Judgment

`same_parent_parallel_children` remains inactive for this selection.

Reason: `backtest_output` is the final residual under the runtime IO parent. Serial extraction is simpler and avoids splitting nested summary DTOs before the leaf closeout decision.

## Hard Boundaries

The next `backtest_output` baseline must not:

- edit Rust source code;
- change backtest output field names, types, serde derives, default attributes, skip-serialization attributes, public exports, or nested summary shapes;
- move RFC execution contracts or crate-root tests;
- reopen closed runtime IO children;
- change downstream runtime/backend/compiler behavior or release transition.

## Next Step

BE-001SN-01 `root.contracts.qrpc_core.runtime_io_contract.backtest_output` baseline_plan.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
