# v4.16.0 root.contracts.core_ir.v4_contracts.static_contract_bundle.static_validation single leaf closeout stops split

> Batch: BE-001VY-03
> Node: `root.contracts.core_ir.v4_contracts.static_contract_bundle.static_validation`
> Parent: `root.contracts.core_ir.v4_contracts.static_contract_bundle`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`static_validation` is terminal for the current recursive pass.

Closed ownership:

- `V4StaticContractBundle::validate_static_contract`
- schema-version validation
- delegated validation order across version manifest, QS profile, type system, runtime modes, plugin governance, reproducibility, complexity budget, and learning pipeline
- required machine graph and venue matrix checks
- graph complexity metric construction and validation
- venue matrix validation delegation
- plugin manifest validation delegation

Further splitting is rejected because the method is one ordered validation phase. Smaller children would fragment shared error aggregation and increase risk of changing validation order or error strings.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `static_validation` is a named validation behavior leaf. |
| parent_child_communication_kept | pass | The method lives in one nested child and stays public through `V4StaticContractBundle`. |
| equivalence_baseline_freezable | pass | Validation order and error strings were frozen and verified. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | satisfied | The leaf owns one public validation method. |
| state_machine_phase | no | This is static contract validation, not state-machine topology. |
| strategy_branch | no | This is not strategy branch behavior. |
| independent_failure_mode | satisfied | Bundle validation failure is a distinct behavior phase. |
| reuse_pressure | low | Capability report builders call the public method through the parent type. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | yes | Splitting delegated checks into separate micro leaves would not create stronger owners. |
| communication_cost_rises | yes | More children would add routing around one ordered error accumulator. |
| local_proof_missing | no | `cargo test -p qrpc-core-ir` and `cargo check -p quantpilot` passed after extraction. |
| line_count_only | no | Stop decision is based on ordered validation cohesion. |

leaf_split_decision_result

`stop_split: true`

next_recursive_step

BE-001VZ-01 root.contracts.core_ir.v4_contracts.static_contract_bundle parent_residual_judgment closes parent
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/static_contract_bundle/static_validation.rs`

**Markers**:
- `BE-001VY-03`

**Next step**:
BE-001VZ-01 root.contracts.core_ir.v4_contracts.static_contract_bundle parent_residual_judgment closes parent

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
