# v4.16.0 root.executor.runner.runner_support_files.live_runner.v4_runner extract closeout

> Batch: BE-002MJ-02
> Node: `root.executor.runner.runner_support_files.live_runner.v4_runner`
> Stage: `actual_extraction`
> governance_next_authority: active

---

## Result

`root.executor.runner.runner_support_files.live_runner.v4_runner` has been
extracted into:

`src-executor/live_runner/v4_runner.rs`

The child owns `V4Runner`, `V4ExecutorEvidenceEvent`, and the direct
`impl V4Runner`. The parent re-exports the V4 runner surface and retains
`RunnerPool`, V4 market metadata helpers, and local tests.

## Preserved Surface

- `V4Runner`
- `V4ExecutorEvidenceEvent`
- V4 websocket event handling
- V4 evidence projection
- V4 subscribed-symbol filtering

`V4Runner::DEFAULT_REALTIME_PAPER_VENUE_ID` remains parent-reachable for
`resolve_v4_runner_venue_id`.

## Gates

| Gate | Result |
| --- | --- |
| `cargo fmt --all --check` | PASS |
| `cargo check -p quantpilot --bin executor` | PASS |
| `cargo test -p quantpilot --bin executor live_runner` | PASS |

## Next Step

BE-002MJ-03
`root.executor.runner.runner_support_files.live_runner.v4_runner`
single_leaf_closeout.
