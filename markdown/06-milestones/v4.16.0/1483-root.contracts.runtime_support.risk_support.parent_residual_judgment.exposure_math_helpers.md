# v4.16.0 root.contracts.runtime_support.risk_support parent residual judgment selects exposure_math_helpers

> Batch: BE-002CC-01
> Node: `root.contracts.runtime_support.risk_support`
> Candidate: `root.contracts.runtime_support.risk_support.exposure_math_helpers`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.

---

## Decision

Select `root.contracts.runtime_support.risk_support.exposure_math_helpers` as the next child.

## Current Closed Children

- `root.contracts.runtime_support.risk_support.event_payload_projection`
- `root.contracts.runtime_support.risk_support.direction_cross_constraints`
- `root.contracts.runtime_support.risk_support.action_clamp_helpers`
- `root.contracts.runtime_support.risk_support.portfolio_target_clamp_helpers`

## Candidate Scope

This child covers shared risk exposure math helpers:

- `available_sell_ratio`
- `portfolio_equity`
- `symbol_net_exposure_ratio`
- `portfolio_net_exposure_ratio`

## Rationale

The candidate is cohesive because these helpers compute the common exposure and equity metrics consumed by closed action clamp, portfolio target clamp, and event payload projection children. Extracting them gives shared math an explicit white-box owner while preserving parent-mediated access.

## Preserved Boundaries

- provider/evaluate surface remains parent-owned;
- closed projection, direction-constraint, action-clamp, and portfolio-target clamp children remain outside this child;
- tests remain outside this child;
- no sibling shortcut may be introduced.

## Next Step

BE-002CD-01 `root.contracts.runtime_support.risk_support.exposure_math_helpers` baseline_plan.
