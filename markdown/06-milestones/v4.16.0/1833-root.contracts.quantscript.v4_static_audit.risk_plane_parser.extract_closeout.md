# v4.16.0 root.contracts.quantscript.v4_static_audit.risk_plane_parser extract closeout

> Batch: BE-002KO-02
> Node: `root.contracts.quantscript.v4_static_audit.risk_plane_parser`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> governance_next_authority: active

---

## Movement

`parse_risk_plane` has been extracted from
`quantscript/src/v4_static_audit.rs` into:

`quantscript/src/v4_static_audit/risk_plane_parser.rs`

The parent keeps a private `parse_risk_plane` wrapper so
`static_document_parser` continues to call through the parent boundary.

## Ownership Now

| Field | Value |
| --- | --- |
| Child file | `quantscript/src/v4_static_audit/risk_plane_parser.rs` |
| Owned function | `parse_risk_plane` |
| Parent wrapper | `parse_risk_plane` |
| Child imports | `diag`, `split_words`, `split_csv_words` |
| Owned output | `MachineGraphRiskPlane` |

## Preserved Behavior

- missing machine validation stays on `QSV4121`;
- missing priority value validation stays on `QSV4122`;
- invalid priority validation stays on `QSV4123`;
- CSV machine id expansion is unchanged;
- default priority and `required: true` are unchanged.

## Gates

Minimum extraction gates were run before commit.

## Next Step

BE-002KO-03 `root.contracts.quantscript.v4_static_audit.risk_plane_parser`
single_leaf_closeout.
