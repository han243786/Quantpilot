# v4.16.0 data_module parent residual judgment selects source_mapping

> Batch: BE-001YZ-01
> Node: `root.contracts.runtime_support.data_module`
> Stage: `parent_residual_judgment`
> Selected child: `root.contracts.runtime_support.data_module.source_mapping`
> Movement: No code movement.

## Context

BE-001YY-01 froze the `data_module` baseline and identified the child queue for the current data-module file.

## Selected Child

`root.contracts.runtime_support.data_module.source_mapping`.

This child owns the Core IR data-binding to runtime source-config mapping surface:

- `data_sources_from_core_ir`
- `parse_exchange_hint`
- `parse_symbol_hint`
- `parse_bool_hint`
- `parse_u64_hint`

## Selection Reason

`source_mapping` is the lowest-risk first data-module split.

Reasons:

- it has a single upstream input: `CoreStrategyIr`
- it produces a single downstream output shape: `Vec<DataSourceConfig>`
- it is independent from HTTP transport, exchange parsing, mock generation, historical cache, and diagnostics
- it is already covered by the data-module request-control restoration test
- extracting it first gives later collection and exchange leaves a stable source-config boundary

## Split Decision Gate

The child should proceed through baseline before movement.

Expected outcome:

- add `qrpc_runtime/src/data_module/source_mapping.rs`
- keep `data_sources_from_core_ir` crate-visible through the parent module
- move only source-hint parsing and source config restoration
- leave provider trait, collection orchestration, quality, exchange parsing, mock data, cache, and tests in the parent for later residual judgment

## Equivalence Baseline

Required gates for the next movement:

- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-001ZA-01 `root.contracts.runtime_support.data_module.source_mapping` baseline_plan.
