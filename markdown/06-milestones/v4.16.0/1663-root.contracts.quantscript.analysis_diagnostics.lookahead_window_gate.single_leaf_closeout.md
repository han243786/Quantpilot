# v4.16.0 root.contracts.quantscript.analysis_diagnostics.lookahead_window_gate single leaf closeout

> Batch: BE-002GW-03
> Node: `root.contracts.quantscript.analysis_diagnostics.lookahead_window_gate`
> Parent: `root.contracts.quantscript.analysis_diagnostics`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`root.contracts.quantscript.analysis_diagnostics.lookahead_window_gate` is closed with `stop_split: true`.

The child is now a cohesive terminal gate for QS0401/QS0402/QS0403 lookahead-window diagnostics.

## Terminal Leaf Control

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -Path quantscript\src\analysis_diagnostics\lookahead_window_gate.rs -LeafId root.contracts.quantscript.analysis_diagnostics.lookahead_window_gate -ParentId root.contracts.quantscript.analysis_diagnostics -ParentLoc 992 -Depth 4
```

Result:

| Field | Value |
| --- | --- |
| `loc` | 288 |
| `functions` | 9 |
| `public_surface` | 2 |
| `branches` | 47 |
| `normalized_split_score` | 45 |
| `split_decision` | STOP |
| `governance_packaging` | stop_split |
| `final_decision` | STOP |
| `size_bucket` | terminal_target_150_600 |

Reason: medium score without same-parent wave candidate; avoid standalone governance.

## Why Stop

Stop conditions:

- the child is within the target terminal range of 150-600 LOC;
- the two parent-visible functions are both part of one lookahead-window diagnostic family;
- private helpers are small and self-contained;
- continuing to split negative index and centered-window traversal would duplicate AST traversal boilerplate and increase parent import surface;
- no public API, route, schema, persistence owner, lock owner, runtime behavior, or release-transition boundary changes are involved.

## Preserved Boundary

Preserved:

- parent-only invocation through `analysis.rs`;
- diagnostic order after `unsupported_construct_gate`;
- diagnostic codes `QS0401`, `QS0402`, and `QS0403`;
- spans, message text, and severity;
- no sibling horizontal calls;
- warmup and fetch lookback logic outside this child.

## Validation

The immediately preceding extraction passed:

- `cargo fmt --check` PASS
- `cargo test -p quantscript` PASS, 88 tests
- `cargo check -p quantpilot` PASS
- `git diff --check` PASS
- UTF-8 check PASS
- full feature tree check PASS
- matrix governance check PASS

This closeout is docs-only and requires:

- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002GX-01 `root.contracts.quantscript.analysis_diagnostics` parent_residual_judgment.
