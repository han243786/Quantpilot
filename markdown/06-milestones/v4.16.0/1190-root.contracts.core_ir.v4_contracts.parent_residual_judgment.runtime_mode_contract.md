# v4.16.0 root.contracts.core_ir.v4_contracts parent residual judgment selects runtime_mode_contract

> Batch: BE-001VF-01
> Node: `root.contracts.core_ir.v4_contracts`
> Parent: `root.contracts.core_ir`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts` selects
`root.contracts.core_ir.v4_contracts.runtime_mode_contract` as the next child.

The selected child owns the runtime trading mode contract family currently in `qrpc_core_ir/src/v4.rs`:

- `RuntimeTradingMode`
- `RuntimeModeContract`
- `RuntimeTradingModeSpec`
- `RuntimeAccountDomain`
- `RuntimeSettlementAuthority`
- `RuntimeExecutionEventSource`
- `RuntimeExecutionEventKind`
- `V4_RUNTIME_EXECUTION_EVENTS`
- `v4_runtime_execution_events`
- `default_v4_runtime_mode_contract`
- `RuntimeModeContract::validate_static_contract`
- `RuntimeModeContract::mode_spec`
- `RuntimeModeContract::settlement_authority_for`

The selected child does not own `VenueCapabilityMatrix`, venue capability enums/specs, QS type system, plugin
governance, version manifest, reproducibility, complexity budget, capability reports, bridge builders, tests,
machine contract, machine graph contract, or QS profile.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `runtime_mode_contract` is a named white-box child for runtime mode/account/settlement/event contracts. |
| parent_child_communication_kept | pass | The child can import shared constants/default helpers and remain re-exported from `qrpc_core_ir/src/v4.rs`. |
| equivalence_baseline_freezable | pass | Runtime mode default specs, validation branches, and settlement lookup behavior are finite. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | Runtime mode types remain public through the v4 facade. |
| state_machine_phase | guarded | Runtime mode governs execution lifecycle modes rather than machine topology. |
| strategy_branch | no | The child is runtime contract behavior, not a strategy branch. |
| independent_failure_mode | pass | Runtime mode validation failures are independent from QS type system, venue, plugin, and graph contracts. |
| reuse_pressure | pass | Backtest artifact and runtime contract bundle code depend on runtime trading mode types. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The child owns multiple runtime mode DTOs, defaults, constants, and validation methods. |
| communication_cost_rises | no | A single child module preserves the public v4 surface through re-export. |
| local_proof_missing | no | Existing qrpc-core-ir tests cover runtime mode defaults and validation. |
| line_count_only | no | Selection is based on runtime contract ownership. |

leaf_split_decision_result

`continue_split: true`

Selected next child:

`root.contracts.core_ir.v4_contracts.runtime_mode_contract`

next_recursive_step

BE-001VG-01 root.contracts.core_ir.v4_contracts.runtime_mode_contract baseline_plan
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs`

**Markers**:
- `BE-001VF-01`

**Next step**:
BE-001VG-01 root.contracts.core_ir.v4_contracts.runtime_mode_contract baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
