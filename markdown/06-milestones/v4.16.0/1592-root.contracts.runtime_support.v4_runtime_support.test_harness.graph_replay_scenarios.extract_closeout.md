# v4.16.0 root.contracts.runtime_support.v4_runtime_support.test_harness.graph_replay_scenarios extract closeout

> Batch: BE-002FH-01
> Node: `root.contracts.runtime_support.v4_runtime_support.test_harness.graph_replay_scenarios`
> Parent: `root.contracts.runtime_support.v4_runtime_support.test_harness`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> governance_next_authority: active

---

## Decision

Extract `graph_replay_scenarios` as a test-only child.

The extraction moved five graph/replay scenario tests into a dedicated child module while preserving all assertions, input literals, and production behavior.

## Code Movement

| Path | Change |
| --- | --- |
| `qrpc_runtime/src/v4_runtime/test_harness/graph_replay_scenarios.rs` | New test-only child owning graph/replay scenario tests. |
| `qrpc_runtime/src/v4_runtime/test_harness.rs` | Adds `mod graph_replay_scenarios;` and removes the moved test bodies. |

Moved tests:

- `market_price_tick_helper_records_market_snapshot`
- `v4_nested_machine_parent_transition_wins_over_child_transition`
- `v4_nested_machine_memory_is_isolated_and_visible_in_snapshot`
- `v4_backtest_bar_replay_is_deterministic`
- `v4_multi_symbol_expansion_creates_independent_machine_instances`

## Equivalence

The migration preserves:

- all 32 v4 runtime tests;
- graph expansion and validation assertions;
- nested state-machine transition and memory assertions;
- deterministic bar replay assertions;
- market tick snapshot event assertions;
- parent-mediated test access through `use super::*`;
- no production runtime code movement.

Observed namespace change:

- from `v4_runtime::test_harness::*`
- to `v4_runtime::test_harness::graph_replay_scenarios::*` for the moved tests

## Evidence

Verified after extraction:

| Gate | Result |
| --- | --- |
| `cargo fmt --check` | PASS |
| `cargo check -p qrpc-runtime` | PASS |
| `cargo test -p qrpc-runtime v4 -- --nocapture` | PASS, 32 tests |
| `cargo check -p quantpilot` | PASS |

Governance gates are run at commit time for this batch:

- `git diff --check`;
- `tools/check-utf8.ps1`;
- `tools/check-full-feature-tree.ps1`;
- `tools/check-matrix-governance.ps1`.

## Next Step

BE-002FH-02 `root.contracts.runtime_support.v4_runtime_support.test_harness.graph_replay_scenarios` single_leaf_closeout.
