# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline.test_harness baseline plan

> Batch: BE-002DD-01
> Node: `root.contracts.runtime_support.sandbox_replay_timeline.test_harness`
> Parent: `root.contracts.runtime_support.sandbox_replay_timeline`
> Stage: `baseline_plan`
> Movement: No Rust code movement.

---

## Scope

Freeze the baseline for extracting local sandbox tests from `qrpc_runtime/src/sandbox/mod.rs`.

Allowed movement:

- add `qrpc_runtime/src/sandbox/test_harness.rs`;
- move only the existing `#[cfg(test)] mod tests` body into the test harness child;
- add `#[cfg(test)] mod test_harness;` in the sandbox parent;
- preserve all test function names and assertions.

## In-Scope Surface

- realtime sandbox session test
- fast backtest pre-start rejection test
- market update portfolio event test
- ignored fast backtest replay output test
- ignored latency assumption test
- runtime support boundary test
- realtime sandbox deterministic test mode snapshot test
- fast backtest deterministic test mode isolation test
- sample runtime protocol fixture

## Out of Scope

- `RealTimeSandbox`
- `FastBacktestSandbox`
- `snapshot_from`
- `ensure_running`
- `trace_id`
- replay/timeline/mode-surface production children
- release transition

## Equivalence Baseline

The next actual extraction must preserve:

- test names;
- ignored test markers and reasons;
- sample config shape;
- all assertions;
- `cargo test -p qrpc-runtime sandbox -- --nocapture` result.

## Dependency Rule

The test harness child may use the sandbox parent as its only local boundary through `use super::*;`. It must not introduce production sibling calls or release transition behavior.

## Gates

Required gates for actual extraction:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime sandbox -- --nocapture`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 / full feature tree / matrix governance

## Next Step

BE-002DE-01 `root.contracts.runtime_support.sandbox_replay_timeline.test_harness` actual_extraction.
