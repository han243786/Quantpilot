# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation single leaf closeout continues split

> Batch: BE-001UU-01
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation`
remains open for deeper split.

The parent already closed major child phases:

- `graph_acyclic_validation`
- `event_usage_validation`
- `risk_plane_validation`

The remaining parent still owns graph root validation orchestration, schema version/graph ID checks,
machine identity and nested duplicate checks, per-machine validation delegation, and edge identity/
endpoint/self-loop checks. The next child should isolate `machine_identity_validation` because
machine ID, duplicate, nested duplicate, and per-machine validation delegation are one independent
graph input failure plane.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `machine_identity_validation` is a named white-box child for graph machine identity/delegation checks. |
| parent_child_communication_kept | pass | It can return `machines_by_id`, `all_machines_by_id`, and errors to the parent without sibling calls. |
| equivalence_baseline_freezable | pass | The branch set is finite: empty machine ID, duplicate top-level ID, nested duplicate ID, and machine validation delegation. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | The public graph validator remains in this parent, but machine identity is a stable private graph-input phase. |
| state_machine_phase | pass | Machine identity validation is the machine inventory phase before edge/event/risk checks. |
| strategy_branch | guarded | It protects machine IDs and nested machine contracts consumed by graph validation. |
| independent_failure_mode | pass | Machine ID/duplicate/delegation failures are independent from DAG, event, risk, and edge endpoint checks. |
| reuse_pressure | guarded | Reuse remains through the parent validator, but isolating this phase makes the remaining parent reviewable. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | Machine identity/delegation owns a real graph input invariant. |
| communication_cost_rises | no | Returning maps and errors keeps communication parent-mediated. |
| local_proof_missing | no | Existing qrpc-core-ir tests cover machine/graph static validation through the public validator. |
| line_count_only | no | Continue decision is based on remaining behavior ownership, not only 102-line size. |

leaf_split_decision_result

`continue_split: true`

Next selected child candidate:

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.machine_identity_validation`

next_recursive_step

BE-001UV-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation parent_residual_judgment selects machine_identity_validation
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation.rs`

**Markers**:
- `BE-001UU-01`

**Next step**:
BE-001UV-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation parent_residual_judgment selects machine_identity_validation

---

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
