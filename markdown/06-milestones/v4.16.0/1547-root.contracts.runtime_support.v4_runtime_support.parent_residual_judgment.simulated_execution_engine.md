# v4.16.0 root.contracts.runtime_support.v4_runtime_support parent residual judgment - simulated_execution_engine

> Batch: BE-002EA-01
> Node: `root.contracts.runtime_support.v4_runtime_support`
> Selected child: `root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_trial: true

---

## Decision

Select `root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine` as the next v4 runtime support child.

## Evidence

Closed v4 runtime support children:

- `type_surface`
- `graph_symbol_expansion`
- `runtime_constructor_mode_gate`
- `event_replay_orchestration`
- `machine_transition_engine`
- `risk_execution_gate`

The BE-002DH-01 v4 runtime support baseline queue names `simulated_execution_engine` after `risk_execution_gate`.

The candidate currently spans `qrpc_runtime/src/v4_runtime.rs` and `qrpc_runtime/src/v4_simulated_execution.rs` and covers:

- simulated execution config attachment;
- simulated market price updates;
- simulated execution snapshot projection;
- runtime-to-simulator order request construction;
- simulated order capability validation;
- simulated execution event recording;
- simulated order submit/reject/amend/fill/expire behavior;
- OCO sibling cancellation, trailing stop updates, ledger/accounting, asset curve, and microstructure metrics.

## Terminal Leaf Control

`tools/evaluate-leaf-granularity.ps1 -Path qrpc_runtime/src/v4_simulated_execution.rs` result:

- Decision: `WAVE`
- Score: `56`
- LOC: `1402`
- Functions: `48`
- Public surface: `0`
- Branches: `138`
- Domains: `8`
- Risk tags: `cross_crate`, `persistence`, `route_schema`, `security`, `state_machine`, `trading_semantics`
- Governance mode: `same_parent_wave`

## Boundary

The next baseline must freeze only simulated execution behavior. It must not move:

- public v4 type surface;
- graph symbol expansion;
- constructor/mode gate behavior;
- replay/input orchestration;
- machine transition mechanics;
- risk/execution gate behavior;
- memory snapshot helpers not specific to simulated execution;
- event catalog payload validation;
- v4 tests until a dedicated test-harness residual judgment.

## Dependency Rule

The selected child may call already closed gate helpers only through the v4 runtime parent boundary. It must not introduce sibling horizontal links or release-transition shortcuts.

## QPCursor Trial Observation

The QPCursor wrapper is useful here because the heat level is G4 and the candidate is large enough that a new agent needs:

- the authoritative legacy cursor;
- the selected module-tree coordinate;
- the exact allowed workset;
- the explicit stop-if rules around runtime semantics and trading behavior;
- evidence that release transition is not active.

## Next Step

BE-002EB-01 `root.contracts.runtime_support.v4_runtime_support.simulated_execution_engine` baseline_plan.
