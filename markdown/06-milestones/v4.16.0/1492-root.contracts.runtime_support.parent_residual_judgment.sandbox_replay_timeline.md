# v4.16.0 root.contracts.runtime_support parent residual judgment - sandbox_replay_timeline

> Batch: BE-002CJ-01
> Node: `root.contracts.runtime_support`
> Selected child: `root.contracts.runtime_support.sandbox_replay_timeline`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.

---

## Decision

Select `root.contracts.runtime_support.sandbox_replay_timeline` as the next runtime-support child.

## Evidence

Closed runtime-support children include:

- `runtime_facade_coordinator`
- `data_module`
- `intent_module`
- `agent_module`
- `core_ir_evaluator`
- `execution_module`
- `fill_engine`
- `risk_support`

The runtime-support baseline queue names `sandbox_replay_timeline` after `risk_support`. The current sandbox boundary is concentrated in:

- `qrpc_runtime/src/sandbox/mod.rs`
- `qrpc_runtime/src/sandbox/replay.rs`
- `qrpc_runtime/src/sandbox/timeline.rs`

This boundary owns sandbox modes, deterministic test mode, sandbox snapshots, real-time and fast-backtest sandbox implementations, deterministic replay bar/tick construction, timeline data providers, unified timeline assembly, and local sandbox/timeline tests.

## Boundary

The next baseline must freeze the sandbox/replay/timeline public surface before any movement. It must not touch runtime facade, data/agent/risk/execution/fill children, live execution, plugin runtime support, v4 runtime support, QuantScript, executor, or release-transition optimization.

## Dependency Rule

Parent-child communication remains strict. No sandbox child may shortcut into another runtime-support sibling. Any helper movement must stay inside the sandbox parent or be explicitly parent-mediated.

## Next Step

BE-002CK-01 `root.contracts.runtime_support.sandbox_replay_timeline` baseline_plan.
