# v4.16.0 root.contracts.runtime_support.data_module parent residual judgment selects normalization

> Batch: BE-001ZH-01
> Node: `root.contracts.runtime_support.data_module`
> Stage: `parent_residual_judgment`
> Selected child: `root.contracts.runtime_support.data_module.normalization`
> Movement: No code movement.

---

## Summary

`data_module` parent residual judgment selects `normalization` as the next child.

Closed children under this parent:

- `source_mapping`
- `quality_diagnostics`
- `collection_orchestration`
- `exchange_surface_wave`
- `exchange_endpoints`
- `exchange_payload_parsing`

Remaining residual families:

- `normalization`
- `mock_data_generation`
- `historical_cache`
- `http_transport`
- `test_harness`

## Selected Child

`root.contracts.runtime_support.data_module.normalization`.

This child owns conversion from raw exchange/mock payloads into normalized runtime market data:

- `normalize_kline_series`
- `normalize_quote`
- OHLC validity filtering
- source latency/status projection into normalized snapshots
- data quality default attachment for newly normalized snapshots
- backtest quote snapshot construction through `quote_snapshot_from_price`

## Selection Reason

`normalization` is the next cohesive data-module behavior family.

Reasons:

- exchange endpoint/payload parsing is already closed, so raw exchange output now has a clean parent handoff into normalization;
- mock data and historical fetch both depend on normalized Kline/Quote output, so extracting normalization first reduces later residual coupling;
- normalization owns data-shape semantics rather than transport, cache, mock generation, or provider orchestration;
- existing qrpc-runtime tests cover parser-to-normalized behavior, mock quote/kline behavior, mixed source collection, and request interval cache behavior.

## Leaf Granularity Judge Note

The whole `data_module.rs` parent was not used as terminal-leaf evidence because it is still an open aggregate parent. Running the read-only judge on the parent file produces a medium score but cannot replace residual-family selection.

The next baseline must evaluate the `normalization` child itself. Expected initial result: `SPLIT`, because normalization is a semantic conversion owner used by live fetch, historical backtest, mock data, and quote snapshot helpers.

## Frozen Scope For Next Baseline

Included for BE-001ZI-01 consideration:

- `normalize_kline_series`;
- `normalize_quote`;
- `quote_snapshot_from_price`;
- any normalization-only imports needed to compile the child.

Excluded:

- raw OKX/Binance parser helpers already owned by `exchange_surface`;
- `mock_raw_klines`, `mock_raw_quote`, `mock_kline_bars_for_backtest`;
- historical cache path/load/persist/fetch helpers;
- HTTP transport helpers;
- in-memory cache helpers;
- `BuiltinDataModule::fetch_and_normalize`;
- tests except imports needed to keep existing test behavior compiling.

## Guardrails

- No public trait or DTO shape change.
- No route/schema/persistence/lock owner movement.
- No sibling horizontal link.
- Parent `data_module` keeps orchestration, fetch/cache, transport, mock, historical cache, and test ownership until their own baselines.
- If the baseline finds quote snapshot construction should remain parent-owned, it may exclude `quote_snapshot_from_price`.

## Next Step

BE-001ZI-01 `root.contracts.runtime_support.data_module.normalization` baseline_plan.
