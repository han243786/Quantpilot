# v4.16.0 root.contracts.compiler_bridge.runtime_protocol_lowering.agent_policy_lowering actual extraction complete

> Batch: BE-001XU-02
> Node: `root.contracts.compiler_bridge.runtime_protocol_lowering.agent_policy_lowering`
> Parent: `root.contracts.compiler_bridge.runtime_protocol_lowering`
> Stage: `extract_closeout`
> Movement: Rust code movement completed.

---

## Summary

`root.contracts.compiler_bridge.runtime_protocol_lowering.agent_policy_lowering` has been physically extracted.

New child file:

- `qrpc_compiler/src/runtime_protocol_lowering/agent_policy_lowering.rs`

The child now owns:

- `lower_runtime_agent_to_policy`
- `lower_runtime_rebalance_schedule_to_core_ir`

The parent `qrpc_compiler/src/runtime_protocol_lowering.rs` now keeps:

- `mod agent_policy_lowering`;
- top-level runtime protocol orchestration;
- data binding construction;
- `intent_signal_lowering` delegation;
- risk policy lowering;
- DAG validation;
- parent-mediated agent policy delegation through `agent_policy_lowering::lower_runtime_agent_to_policy(agent, &config.intents)`.

## Equivalence Notes

The extraction preserves:

- CrossVenueArbitrage, PortfolioRebalance, and WeightedSignals kind selection order;
- all agent policy projected fields and defaults;
- runtime rebalance schedule mapping;
- all-observe and portfolio-rebalance detection semantics;
- risk policy lowering, data binding construction, indicator/signal lowering, validation delegation, and runtime Core IR output shape.

## Boundary

**Changed Rust files**:
- `qrpc_compiler/src/runtime_protocol_lowering.rs`
- `qrpc_compiler/src/runtime_protocol_lowering/agent_policy_lowering.rs`

**Unchanged ownership**:
- `intent_signal_lowering` remains closed.
- `lower_runtime_risk_to_policy` remains parent residual.
- top-level runtime orchestration remains parent residual.
- Strategy IR lowering, tests, canonical hash, and release transition remain outside this child.

## Next Step

BE-001XU-03 `root.contracts.compiler_bridge.runtime_protocol_lowering.agent_policy_lowering` single_leaf_closeout.

## Gates

- `cargo fmt --check`
- `cargo test -p qrpc-compiler`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
