# v4.16.0 root.contracts.compiler_bridge parent residual judgment selects strategy_ir_lowering

> Batch: BE-001XY-01
> Node: `root.contracts.compiler_bridge`
> Stage: `parent_residual_judgment`
> Movement: No code movement.
> Decision: Select `contracts.compiler_bridge.strategy_ir_lowering` as the next child.

---

## Parent State

`root.contracts.compiler_bridge` has two closed children:

| Child | Status |
| --- | --- |
| `contracts.compiler_bridge.runtime_protocol_validation` | Closed with `stop_split: true`. |
| `contracts.compiler_bridge.runtime_protocol_lowering` | Closed as parent facade over `intent_signal_lowering`, `agent_policy_lowering`, `risk_policy_lowering`. |

## Remaining Residuals in qrpc_compiler/src/lib.rs

After closed children, `qrpc_compiler/src/lib.rs` contains:

| Surface | Lines (approx.) | Ownership |
| --- | ---: | --- |
| `lower_strategy_ir_to_core_ir` + 16 private helpers | ~420 | **Cohesive candidate for extraction** |
| Shared decode / spread / parse helpers | ~250 | Parent-owned (used by both runtime and strategy children) |
| Public compile/lower facades | ~40 | Parent-owned (external API boundary) |
| Tests (runtime + strategy) | ~1050 | Parent-owned (tests both modules from parent scope) |

## Leaf Split Decision Gate

### leaf_split_base_gate

| Rule | Result | Detail |
| --- | --- | --- |
| `white_box_boundary_named` | pass | Child: `strategy_ir_lowering`. Input: `&StrategyIr`. Output: `Result<CoreStrategyIr>`. Caller: `qrpc_compiler::lower_strategy_ir_to_core_ir` (public facade). Excludes: shared spread/decode helpers, tests, public compile facades. |
| `parent_child_communication_kept` | pass | Child is called by parent facade; no sibling horizontal link needed. |
| `equivalence_baseline_freezable` | pass | `cargo test -p qrpc-compiler` (28 tests) covers both runtime and strategy IR lowering paths. |

### leaf_split_positive_trigger

| Trigger | Detail |
| --- | --- |
| `public_or_handler_boundary` | `lower_strategy_ir_to_core_ir` is a public entry point exposed as `qrpc_compiler::lower_strategy_ir_to_core_ir`. |
| `strategy_branch` | Contains independent strategy indicator kind lowering, logic rule lowering, spread/custom expression lowering branches. |
| `reuse_pressure` | Called from `src/graph_api.rs` and `src/compile_api.rs` via the public facade. |

### leaf_split_stop_condition

None triggered.

### leaf_split_decision_result

`continue_split`: select `contracts.compiler_bridge.strategy_ir_lowering` as the next child.

## Selected Child

| Field | Value |
| --- | --- |
| Node | `root.contracts.compiler_bridge.strategy_ir_lowering` |
| Physical target | `qrpc_compiler/src/strategy_ir_lowering.rs` |
| Entry point | `lower_strategy_ir_to_core_ir(&StrategyIr) -> Result<CoreStrategyIr>` |
| Cohesive private helpers | `lower_strategy_indicator_kind`, `lower_strategy_logic_rule`, `lower_strategy_logic_condition`, `lower_strategy_ma_cross_condition`, `lower_strategy_indicator_threshold_condition`, `lower_strategy_spread_threshold_condition`, `lower_strategy_spread_spec`, `lower_strategy_custom_expr`, `validate_custom_expr_spec`, `validate_custom_value_expr`, `validate_custom_data_id`, `validate_window_agg_field`, `lower_logic_action`, `parse_time_in_force`, `known_or_unknown_f64` |
| Shared helpers (stay in parent) | `parse_strategy_signal_compare`, `strategy_indicator_usize_param`, `lower_rebalance_schedule`, `runtime_intent_is_spread`, `lower_runtime_spread_spec`, `build_spread_spec`, `build_spread_series_expr`, `param_with_fallback`, `decode_series_field`, `decode_series_aggregation`, `decode_align_direction`, `decode_spread_output` |
| Public facade preserved | `qrpc_compiler::lower_strategy_ir_to_core_ir` delegates to child |
| Tests | Stay in `qrpc_compiler/src/lib.rs` `mod tests` (tests both runtime and strategy from parent scope) |

## Next Step

BE-001XZ-01 `root.contracts.compiler_bridge.strategy_ir_lowering` baseline_plan.

## Gates

- `cargo test -p qrpc-compiler`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
