# v4.16.0 root.contracts.core_ir.v4_contracts.compile_time_capability_report.report_validation actual extraction complete

> Batch: BE-001WF-02
> Node: `root.contracts.core_ir.v4_contracts.compile_time_capability_report.report_validation`
> Parent: `root.contracts.core_ir.v4_contracts.compile_time_capability_report`
> Stage: `extract_closeout`
> Movement: Code movement completed.

---

## Summary

`report_validation` is now isolated in a nested compile-time capability report validation child.

Moved into `qrpc_core_ir/src/v4/compile_time_capability_report/report_validation.rs`:

- `V4CompileTimeCapabilityReport::validate_for_compile`

Parent `qrpc_core_ir/src/v4/compile_time_capability_report.rs` now owns DTO schema, status/entry/diagnostic DTOs, and compile-time default helpers, and declares:

- `mod report_validation;`

Equivalence gates passed:

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir` (54 passed)
- `cargo check -p quantpilot`

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/compile_time_capability_report.rs`
- `qrpc_core_ir/src/v4/compile_time_capability_report/report_validation.rs`

**Markers**:
- `BE-001WF-02`

**Next step**:
BE-001WF-03 root.contracts.core_ir.v4_contracts.compile_time_capability_report.report_validation single_leaf_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
