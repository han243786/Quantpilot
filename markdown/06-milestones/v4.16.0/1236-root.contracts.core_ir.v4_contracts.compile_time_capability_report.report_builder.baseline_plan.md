# v4.16.0 root.contracts.core_ir.v4_contracts.compile_time_capability_report.report_builder equivalence baseline and extraction plan

> Batch: BE-001WD-01
> Node: `root.contracts.core_ir.v4_contracts.compile_time_capability_report.report_builder`
> Parent: `root.contracts.core_ir.v4_contracts.compile_time_capability_report`
> Stage: `baseline_plan`
> Movement: No code movement.

---

## Summary

This baseline freezes the `report_builder` extraction boundary before code movement.

Frozen public behavior:

- `V4StaticContractBundle::build_compile_time_capability_report`

Frozen private behavior:

- `V4StaticContractBundle::build_type_capability_entries`
- `V4StaticContractBundle::build_execution_capability_entries`
- `V4StaticContractBundle::build_plugin_capability_entries`
- `push_capability_diagnostic`

Frozen diagnostic and assembly behavior:

- preserve all `V4CAP000` through `V4CAP301` diagnostic codes, targets, and message formatting
- preserve static bundle validation delegation before graph/venue/runtime lookups
- preserve graph/venue lookup flags and missing-resource diagnostics
- preserve runtime mode lookup and complexity metrics construction
- preserve first-wave execution capability union with required capabilities
- preserve unsupported/not declared/mode rejected/supported status mapping
- preserve plugin accepted/rejected/missing status mapping
- preserve verdict calculation based on any error diagnostic
- preserve `execution_submission_attached: false`

Allowed BE-001WD-02 movement:

- add `qrpc_core_ir/src/v4/compile_time_capability_report/report_builder.rs`
- add nested `mod report_builder;`
- move only the public report builder, three private entry builders, and `push_capability_diagnostic`

Forbidden movement:

- request/report DTOs
- status/entry/diagnostic DTOs
- compile-time default helpers
- `V4CompileTimeCapabilityReport::validate_for_compile`
- tests, sample builders, static bundle implementation, and Core IR bridge code

Equivalence proof target:

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `cargo check -p quantpilot`

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/compile_time_capability_report.rs`

**Markers**:
- `BE-001WD-01`

**Next step**:
BE-001WD-02 root.contracts.core_ir.v4_contracts.compile_time_capability_report.report_builder extract_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
