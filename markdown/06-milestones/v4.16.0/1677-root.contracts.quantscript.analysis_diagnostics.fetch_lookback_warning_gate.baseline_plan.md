# v4.16.0 root.contracts.quantscript.analysis_diagnostics.fetch_lookback_warning_gate baseline plan

> Batch: BE-002HE-01
> Node: `root.contracts.quantscript.analysis_diagnostics.fetch_lookback_warning_gate`
> Parent: `root.contracts.quantscript.analysis_diagnostics`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Scope

Freeze the QuantScript fetch lookback warning diagnostic before movement.

Current owner:

- `quantscript/src/analysis_diagnostics/analysis.rs`

Planned child:

- `quantscript/src/analysis_diagnostics/fetch_lookback_warning_gate.rs`

## Items Frozen For Movement

The next extraction may move only:

- `collect_fetch_lookback_warnings`
- `collect_fetch_lookback_warnings_from_stmts`
- `collect_fetch_lookback_warnings_from_expr`

## Diagnostic Boundary

The child owns:

- recursive AST traversal for `fetch()` and `get_data()` calls;
- detection of named `lookback` arguments smaller than `1`;
- `QS0503` fetch lookback warning emission.

## Parent Wiring To Preserve

The analysis parent must keep:

- `ScriptAnalysis` and `analyze_script_module` public surface unchanged;
- diagnostic family invocation order unchanged;
- crate-root public exports and `crate::analysis` / `crate::diagnostics` compatibility unchanged;
- `arg_number_named` imported from `warmup_fetch_gate` and passed through the parent boundary as the only helper needed by this child;
- strategy presence, index bounds, dead-code emit, and already closed diagnostic children unchanged.

The child may expose:

- `collect_fetch_lookback_warnings` as the only parent-callable entrypoint.

## Forbidden Movement

The next extraction must not move or change:

- strategy fetch/emit presence checks;
- data source map and index bounds checks;
- dead-code emit checks;
- diagnostic DTO/span types;
- unsupported construct, lookahead-window, warmup/fetch, indirect-recursion, and symbol-whitelist children already closed;
- evaluator normalization, runtime lowering, v4 static audit, tests, authoring samples, backend, executor, frontend, plugin metadata, or release transition.

## Expected Validation

The extraction must pass:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002HE-02 `root.contracts.quantscript.analysis_diagnostics.fetch_lookback_warning_gate` actual_extraction.
