# v4.16.0 root.contracts.quantscript.analysis_diagnostics.unsupported_construct_gate single leaf closeout

> Batch: BE-002GU-03
> Node: `root.contracts.quantscript.analysis_diagnostics.unsupported_construct_gate`
> Parent: `root.contracts.quantscript.analysis_diagnostics`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`root.contracts.quantscript.analysis_diagnostics.unsupported_construct_gate` is closed with `stop_split: true`.

The child is now a cohesive terminal diagnostic gate for:

- unsupported import, async, await, while, match, try target, mutable list, and result-helper diagnostics;
- direct recursion detection;
- non-universe for-loop diagnostics over typed HIR.

## Terminal Leaf Control

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -Path quantscript\src\analysis_diagnostics\unsupported_construct_gate.rs -LeafId root.contracts.quantscript.analysis_diagnostics.unsupported_construct_gate -ParentId root.contracts.quantscript.analysis_diagnostics -ParentLoc 1275 -Depth 4
```

Result:

| Field | Value |
| --- | --- |
| `loc` | 410 |
| `functions` | 9 |
| `public_surface` | 1 |
| `branches` | 74 |
| `normalized_split_score` | 50 |
| `split_decision` | STOP |
| `governance_packaging` | stop_split |
| `final_decision` | STOP |
| `size_bucket` | terminal_target_150_600 |

Reason: medium score without same-parent wave candidate; avoid standalone governance.

## Why Stop

Stop conditions:

- the child is inside the normal terminal range of 150-600 LOC;
- all 9 functions serve one gate family and share the same diagnostic owner;
- splitting direct recursion, unsupported expression traversal, and non-universe loop checks would create extra parent wiring without reducing public API, schema, persistence, lock, or runtime owner risk;
- the only parent-visible surface is `collect_unsupported_construct_diagnostics`;
- remaining complexity is traversal logic, not a separate owner boundary.

## Preserved Boundary

Preserved:

- parent-only invocation through `analysis.rs`;
- no sibling horizontal calls;
- diagnostic codes `QS0601` through `QS0610`;
- spans, message text, and severity;
- `ScriptAnalysis` and `analyze_script_module` behavior;
- crate-root public exports and internal compatibility aliases.

## Validation

The immediately preceding extraction passed:

- `cargo fmt --check` PASS
- `cargo test -p quantscript` PASS, 88 tests
- `cargo check -p quantpilot` PASS
- `git diff --check` PASS
- UTF-8 check PASS
- full feature tree check PASS
- matrix governance check PASS

This closeout is docs-only and requires:

- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002GV-01 `root.contracts.quantscript.analysis_diagnostics` parent_residual_judgment.
