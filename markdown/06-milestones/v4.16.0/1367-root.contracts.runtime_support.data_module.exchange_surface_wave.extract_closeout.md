# v4.16.0 root.contracts.runtime_support.data_module.exchange_surface_wave extract closeout

> Batch: BE-001ZG-02
> Node: `root.contracts.runtime_support.data_module.exchange_surface_wave`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> Parent: `root.contracts.runtime_support.data_module`
> Baseline: `1366-root.contracts.runtime_support.data_module.exchange_surface_wave.baseline_plan.md`

---

## Summary

The `exchange_surface_wave` actual extraction is complete.

`qrpc_runtime/src/data_module/exchange_surface.rs` now owns exchange endpoint/provider-key helpers and OKX/Binance raw payload parsing. The parent `qrpc_runtime/src/data_module.rs` keeps fetch/transport, cache, normalization, mock data, provider collection orchestration, and tests.

## Three Matrix Impact

| Matrix | Impact |
| --- | --- |
| Process matrix | Executes the frozen `same_parent_wave` as one verifiable Rust step. |
| Standard matrix | Keeps parent-child communication through `data_module`; no sibling horizontal link or release transition. |
| Guidance matrix | Adds the concrete exchange surface file to the full tree and records child-level white-box movement. |

## Wave Child Rows

| child | movement | equivalence gate | closeout decision | residuals |
| --- | --- | --- | --- | --- |
| `exchange_endpoints` | Moved provider key, source endpoint, ping endpoint, OKX/Binance endpoint construction, OKX instrument id, OKX spot symbol, OKX bar, Binance symbol, and interval helper into `exchange_surface.rs`. | `cargo check -p qrpc-runtime`, `cargo test -p qrpc-runtime`, `cargo check -p quantpilot`. | Pending BE-001ZG-03; expected `stop_split: true`. | Fetch/transport, cache, provider collection, and tests remain parent-owned. |
| `exchange_payload_parsing` | Moved OKX candle/ticker parsing, Binance kline parsing, OKX status guard, and numeric coercion helpers into `exchange_surface.rs`. | Existing parser tests through `cargo test -p qrpc-runtime`; parent call sites compile through child imports. | Pending BE-001ZG-03; expected `stop_split: true`. | Normalization and mock data remain parent-owned. |

## Extracted Surface

New child file:

- `qrpc_runtime/src/data_module/exchange_surface.rs`

Parent facade updates:

- `mod exchange_surface;`
- controlled parent imports for endpoint helpers, parser helpers, `bar_interval_ms`, and `binance_symbol`;
- no public API change;
- no route/schema/persistence/lock owner change.

## Preserved Boundaries

Still excluded from this wave:

- `fetch_and_normalize`;
- `probe_ping`;
- `fetch_okx`;
- `fetch_json`;
- `fetch_json_via_powershell`;
- `fetch_historical_raw_klines`;
- cache load/persist functions;
- normalization functions;
- mock data functions;
- `DataModuleProvider` and collection orchestration;
- test harness ownership.

## Leaf Granularity Evidence

This extraction confirms the baseline judgment:

| Signal | Result |
| --- | --- |
| Endpoint helpers alone | Too small for standalone governance; kept inside the wave. |
| Parser helpers alone | Cohesive with exchange contracts; kept inside the wave. |
| Combined exchange surface | Worth extracting because it removes exchange-specific endpoint/payload logic from the parent without creating micro leaves. |
| Further split now | Deferred to BE-001ZG-03; current evidence leans `STOP` because the child remains one exchange surface owner. |

## Gates

Required before commit:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-001ZG-03 `root.contracts.runtime_support.data_module.exchange_surface_wave` single_leaf_closeout.
