# v4.16.0 root.contracts.runtime_support.fill_engine.event_projection_wave actual extraction complete

> Batch: BE-002BD-01
> Node: `root.contracts.runtime_support.fill_engine.event_projection_wave`
> Parent: `root.contracts.runtime_support.fill_engine`
> Stage: `actual_extraction`
> Movement: Rust code movement.

---

## Result

`root.contracts.runtime_support.fill_engine.event_projection_wave` has been extracted.

New child file:

- `qrpc_runtime/src/fill_engine/event_projection_wave.rs`

Parent file changed:

- `qrpc_runtime/src/fill_engine.rs`

Moved functions:

- `open_event`
- `partial_event`
- `cancel_event`
- `reject_event`
- `fill_event`

## Equivalence

The moved helpers remain parent-mediated `pub(super)` functions. Parent orchestration still calls the same helper names through the fill-engine parent module.

No event payload field, event id format, source id, trace id, timestamp, event type, lifecycle string, or public API was changed.

## Preserved Boundaries

- `FillEngine::submit_plan` remains parent-owned.
- `FillEngine::on_market_update` remains parent-owned.
- `try_open_resting_order` remains parent-owned.
- reservation/accounting helpers remain parent-owned.
- fill-report helpers remain parent-owned.
- portfolio mutation remains parent-owned.
- tests remain parent-owned.
- no sibling shortcut was introduced.

## Gates

Current Rust gates:

- `cargo fmt`: PASS
- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime fill_engine -- --nocapture`: PASS, 12 passed

Required closeout gates before commit:

- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-002BD-02 `root.contracts.runtime_support.fill_engine.event_projection_wave` single_leaf_closeout.
