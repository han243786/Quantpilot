# v4.16.0 root.contracts.quantscript.analysis_diagnostics single leaf closeout

> Batch: BE-002GS-03
> Node: `root.contracts.quantscript.analysis_diagnostics`
> Parent: `root.contracts.quantscript`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Result

`stop_split: false`

The analysis diagnostics parent is structurally extracted and equivalent, but it is not a terminal leaf for the current recursive pass.

## Evidence

Leaf granularity evaluation:

- Path: `quantscript/src/analysis_diagnostics/analysis.rs`
- LOC: 1673 reported by script, 1680 physical lines
- Functions: 49
- Public surface: 5
- Branches: 303
- Domains: 5
- Risk tags: `cross_crate`, `lock`, `public_api`, `state_machine`
- Split decision: `CONTINUE`
- Governance packaging: `precision_single_leaf`
- Decision: `PRECISION`
- Score: 70
- Terminal leaf control: `oversized_over_800`

Gate evidence from the extraction step remains valid:

- `cargo fmt --check` PASS
- `cargo test -p quantscript` PASS, 88 tests
- `cargo check -p quantpilot` PASS

## Continue Split Rationale

`analysis_diagnostics` now has a useful parent boundary, but `analysis.rs` still owns several independent diagnostic families:

- unsupported construct diagnostics;
- direct and indirect recursion checks;
- series index and centered window lookahead checks;
- warmup and fetch lookback checks;
- strategy fetch/emit presence checks;
- data source index-bound checks;
- dead-code emit and fetch symbol whitelist warnings;
- analysis result assembly and diagnostic DTO routing.

The leaf is oversized and high-risk. Further splitting must use precision single-leaf governance rather than batching away evidence.

## Preserved Boundary

Preserved:

- `quantscript/src/analysis_diagnostics/mod.rs` remains the parent facade;
- `quantscript/src/analysis_diagnostics/diagnostics.rs` remains the diagnostic DTO and span child;
- crate-root public exports and `crate::analysis` / `crate::diagnostics` compatibility remain preserved;
- evaluator normalization, runtime lowering, v4 static audit, test plans, samples, backend, executor, frontend, plugin metadata, and release transition remain outside.

## Next Step

BE-002GT-01 `root.contracts.quantscript.analysis_diagnostics` parent_residual_judgment.
