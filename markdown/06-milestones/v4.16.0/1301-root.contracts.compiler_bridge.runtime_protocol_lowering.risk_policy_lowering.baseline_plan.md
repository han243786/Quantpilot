# v4.16.0 root.contracts.compiler_bridge.runtime_protocol_lowering.risk_policy_lowering equivalence baseline and extraction plan

> Batch: BE-001XW-01
> Node: `root.contracts.compiler_bridge.runtime_protocol_lowering.risk_policy_lowering`
> Parent: `root.contracts.compiler_bridge.runtime_protocol_lowering`
> Stage: `baseline_plan`
> Movement: no Rust code movement.

---

## Summary

`root.contracts.compiler_bridge.runtime_protocol_lowering.risk_policy_lowering` is frozen before physical Rust movement.

This child owns runtime `RiskConfig` to Core IR `RiskPolicy` conversion.

Decision:

`baseline_frozen: true`

Next movement:

`BE-001XW-02` may create `qrpc_compiler/src/runtime_protocol_lowering/risk_policy_lowering.rs` or equivalent private child module and move only the risk policy symbol listed below.

## Allowed Movement Set

The extraction step may move this symbol:

| Symbol | Responsibility |
| --- | --- |
| `lower_runtime_risk_to_policy` | Convert one runtime `RiskConfig` into one Core IR `RiskPolicy`. |

## Parent Calls Frozen

After extraction:

- `lower_runtime_protocol_to_core_ir_with_metadata` may call the child while building `core_ir.risk_policies`.
- The child may receive `&RiskConfig` and return `RiskPolicy`.
- Parent keeps validation delegation, data binding construction, indicator/signal lowering, agent policy lowering, DAG validation, and public runtime compile/lower facades.

## Equivalence Baseline

BE-001XW-02 must preserve projection of:

- `risk_id` to `policy_id`;
- `name`;
- `observed_agent_ids`;
- `max_position_ratio`;
- `max_single_weight`;
- `max_concentration_ratio`;
- `max_symbol_net_exposure_ratio`;
- `max_portfolio_net_exposure_ratio`;
- `max_turnover`;
- `min_trade_weight`;
- `max_new_positions_per_rebalance`;
- `max_total_leverage`;
- `max_exchange_leverage`;
- `min_action_interval_ms`;
- `enabled`;
- `max_cross_symbol_leverage: None`;
- public runtime Core IR output shape.

## Hard Exclusions

This baseline must not:

- move or edit `lower_runtime_protocol_to_core_ir_with_metadata`;
- move or edit `agent_policy_lowering`;
- move or edit `intent_signal_lowering`;
- change runtime validation delegation, data binding construction, DAG validation, Strategy IR lowering, shared spread/custom helpers, canonical hash, tests, or release transition.

## Boundary

**Real files**:
- `qrpc_compiler/src/runtime_protocol_lowering.rs`

**Markers**:
- `BE-001XW-01`
- `risk_policy_lowering baseline_frozen: true`
- `only lower_runtime_risk_to_policy may move`
- `agent policy excluded`

**Next step**:
BE-001XW-02 root.contracts.compiler_bridge.runtime_protocol_lowering.risk_policy_lowering extract_closeout

---

## Gates

- `cargo test -p qrpc-compiler`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
