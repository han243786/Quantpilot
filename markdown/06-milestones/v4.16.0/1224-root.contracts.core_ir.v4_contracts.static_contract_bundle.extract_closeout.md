# v4.16.0 root.contracts.core_ir.v4_contracts.static_contract_bundle actual extraction complete

> Batch: BE-001VW-02
> Node: `root.contracts.core_ir.v4_contracts.static_contract_bundle`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `extract_closeout`
> Movement: Code movement completed.

---

## Summary

`V4StaticContractBundle` static aggregation is now a dedicated v4 child module.

Moved into `qrpc_core_ir/src/v4/static_contract_bundle.rs`:

- `V4StaticContractBundle`
- `impl Default for V4StaticContractBundle`
- `V4StaticContractBundle::validate_static_contract`
- `default_static_contract_bundle_version`

Parent `qrpc_core_ir/src/v4.rs` now declares and re-exports the child:

- `mod static_contract_bundle;`
- `pub use static_contract_bundle::*;`

Parent-retained residuals:

- `V4StaticContractBundle::build_compile_time_capability_report`
- private capability entry builders
- capability request/report DTOs and report validation
- compatibility bridge DTOs/builders/validators
- tests and sample builders

Equivalence gates passed:

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir` (54 passed)
- `cargo check -p quantpilot`

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs`
- `qrpc_core_ir/src/v4/static_contract_bundle.rs`

**Markers**:
- `BE-001VW-02`

**Next step**:
BE-001VW-03 root.contracts.core_ir.v4_contracts.static_contract_bundle single_leaf_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
