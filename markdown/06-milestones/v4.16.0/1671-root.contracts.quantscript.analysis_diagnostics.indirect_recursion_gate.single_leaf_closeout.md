# v4.16.0 root.contracts.quantscript.analysis_diagnostics.indirect_recursion_gate single leaf closeout

> Batch: BE-002HA-03
> Node: `root.contracts.quantscript.analysis_diagnostics.indirect_recursion_gate`
> Parent: `root.contracts.quantscript.analysis_diagnostics`
> Stage: `single_leaf_closeout`
> stop_split: true
> governance_next_authority: active

---

## Decision

`root.contracts.quantscript.analysis_diagnostics.indirect_recursion_gate` is closed as a terminal leaf.

Terminal leaf control returned:

- `split_decision: STOP`
- `governance_packaging: stop_split`
- `normalized_split_score: 36`
- `size_bucket: small_100_149`

## Evidence

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -Path quantscript\src\analysis_diagnostics\indirect_recursion_gate.rs -LeafId root.contracts.quantscript.analysis_diagnostics.indirect_recursion_gate -ParentId root.contracts.quantscript.analysis_diagnostics -ParentLoc 657 -Depth 4
```

Metrics:

- `loc=142`
- `functions=3`
- `public_surface=1`
- `branches=27`
- `domains=2`
- `risk_tags=cross_crate,public_api,state_machine`

Scores:

- `split_benefit=39`
- `leaf_size_fit=15`
- `risk_penalty=65`
- `governance_cost=61`
- `system_efficiency_penalty=15`

## Boundary After Closeout

The leaf owns:

- helper-function call graph construction from script AST;
- indirect recursion cycle detection;
- `QS0605` diagnostic emission.

The parent still coordinates:

- diagnostic invocation order;
- strategy fetch/emit presence checks;
- fetch lookback warnings;
- data source map and index bounds checks;
- dead-code emit checks;
- symbol whitelist checks.

## Residual Rule

Do not split this leaf again unless a future developer proposal proves a stronger independent owner than the current indirect-recursion gate.

## Next Step

Return to `root.contracts.quantscript.analysis_diagnostics` parent residual judgment.
