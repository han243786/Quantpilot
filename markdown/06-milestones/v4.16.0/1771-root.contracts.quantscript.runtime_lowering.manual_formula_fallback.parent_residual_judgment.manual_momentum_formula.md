# v4.16.0 root.contracts.quantscript.runtime_lowering.manual_formula_fallback parent residual judgment: manual_momentum_formula

> Batch: BE-002JH-01
> Node: `root.contracts.quantscript.runtime_lowering.manual_formula_fallback`
> Selected child: `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_momentum_formula`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `manual_momentum_formula` as the next internal child under
`manual_formula_fallback`.

`manual_rsi_formula` is closed. The next cohesive owner is manual momentum
fallback recognition: direct momentum expressions, division-ratio momentum, and
subtract/division ratio forms all resolve to the same `IndicatorBinding::Momentum`
source/lookback output. Extracting this child next preserves the recognizer order
while reducing the remaining fallback parent before the larger MACD/ZScore/MA
owners are touched.

## White-Box Boundary

| Field | Boundary |
| --- | --- |
| Input | candidate expression, `BindingEnv`, and data sources |
| Output | `IndicatorBinding::Momentum` or no-match |
| Current owner | `quantscript/src/lowering/fallback.rs` |
| Planned owner | `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_momentum_formula` |
| Parent caller | `resolve_manual_formula_binding` subtract/divide branches and direct helper users |
| Shared helper dependency | `resolve_boundary_lookback_source_span` and `legacy_latest_lookback_pair` may be delegated through the parent boundary unless the baseline proves they belong inside this child |
| Exclusions | MACD, ZScore, moving-average, boundary lookback public helper, fallback tests, and RSI closed child |

## Symbols Approved For Next Baseline

The next baseline may freeze movement for:

- `manual_momentum_from_expr`
- `manual_momentum_ratio_from_division`
- `manual_momentum_ratio_from_subtract_division`

The baseline must decide whether `legacy_latest_lookback_pair` is private to
momentum or must remain parent-owned with `resolve_boundary_lookback_source_span`
for the later `boundary_lookback_fallback` child.

## Split Decision Gate

| Gate | Result | Evidence |
| --- | --- | --- |
| `parent_precision_open` | PASS | `manual_formula_fallback` remains a precision parent after RSI closeout |
| `queue_order` | PASS | Parent baseline queue lists `manual_momentum_formula` after `manual_rsi_formula` |
| `recognizer_order_preserved` | PASS | Momentum remains after MACD checks in subtract branches and after ZScore in divide branches |
| `strong_owner_boundary` | PASS | All selected entrypoints return `IndicatorBinding::Momentum` |
| `sibling_exclusion_clear` | PASS | MACD, ZScore, MA/window, boundary lookback public helper, tests, and RSI are excluded |

## Risk Classification

| Risk | Result |
| --- | --- |
| Public API / route / schema | Public(crate) momentum helpers must remain stable |
| Persistence / lock owner | No persistence ownership movement |
| Trading semantics evidence | High; momentum fallback affects inferred runtime indicators |
| Cross-crate behavior | Present through `IndicatorBinding`, `DataSourceConfig`, and typed expression contracts |

## Next Step

BE-002JI-01 `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_momentum_formula` baseline_plan.
