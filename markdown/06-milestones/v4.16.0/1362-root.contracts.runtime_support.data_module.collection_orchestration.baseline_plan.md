# v4.16.0 root.contracts.runtime_support.data_module.collection_orchestration baseline plan

> Batch: BE-001ZE-01
> Node: `root.contracts.runtime_support.data_module.collection_orchestration`
> Stage: `baseline_plan`
> Movement: No code movement.
> Current file: `qrpc_runtime/src/data_module.rs`
> Planned child file: `qrpc_runtime/src/data_module/collection_orchestration.rs`

---

## Summary

The `collection_orchestration` baseline is frozen.

This child owns the runtime data provider collection transaction: the `BuiltinDataModule` implementation of `DataModuleProvider`, source de-duplication, collection ordering, normalized data accumulation, `RuntimeEventType::DataUpdated` event assembly, and data-quality warning/error event assembly.

The next movement may extract only the `impl DataModuleProvider for BuiltinDataModule` block into `qrpc_runtime/src/data_module/collection_orchestration.rs`. The parent module must keep the public trait, request/output DTOs, `BuiltinDataModule`, `fetch_and_normalize`, cache, transport, normalization, exchange parsing, mock data, historical cache, source mapping, and quality diagnostics ownership.

## Three Matrix Impact

| Matrix | Impact |
| --- | --- |
| Process matrix | Continues BE-001ZE-01 as a standard single-child baseline before code movement. |
| Standard matrix | Preserves parent-child communication through `data_module`; no sibling horizontal link or release transition. |
| Guidance matrix | Adds a planned white-box child boundary for data collection orchestration. |

## Leaf Granularity Smart Judge

| Field | Result |
| --- | --- |
| leaf_id | `root.contracts.runtime_support.data_module.collection_orchestration` |
| loc | low-hundreds provider transaction pocket |
| function_count | one public trait method implementation |
| public_surface | Implements existing `DataModuleProvider::collect`; no trait or DTO shape change |
| complexity_score | Medium: source loop, duplicate suppression, quality diagnostics, two event payload families |
| dependency_score | Medium: calls parent-owned fetch/normalize, source mapping, quality diagnostics, and runtime event DTOs |
| split_benefit | High: separates runtime provider entrypoint from transport/cache/mock/exchange implementation details |
| governance_cost | Medium-low: one child file plus standard indexes; no standalone precision chain required |
| risk_score | Medium-low: public behavior is preserved and no route/schema/persistence/lock owner moves |
| final_decision | `SPLIT` |
| reason | Although the code pocket is small, it is a cohesive runtime transaction owner and gives later data-module children a clear parent orchestration boundary. |

## Frozen Boundary

Allowed in the next movement:

- add `mod collection_orchestration;` to `qrpc_runtime/src/data_module.rs`;
- move `impl DataModuleProvider for BuiltinDataModule` into `qrpc_runtime/src/data_module/collection_orchestration.rs`;
- import parent-owned DTOs, helpers, and runtime event types through `super`;
- keep `DataModuleProvider`, `DataCollectionRequest`, `DataCollectionOutput`, and `BuiltinDataModule` parent-visible.

Explicitly excluded:

- `fetch_and_normalize`;
- `rate_limited_snapshot`;
- `cached_snapshot`;
- `store_cache`;
- `probe_ping`;
- `fetch_okx`;
- `mock_data`;
- endpoint builders;
- payload parsing;
- normalization helpers;
- historical cache helpers;
- source mapping;
- quality diagnostics;
- tests.

## Equivalence Surface

The extraction must preserve:

- provider key and trait object behavior;
- enabled-source filtering;
- duplicate `data_id` suppression;
- data fetch count mutation through `DataCollectionRequest`;
- data quality snapshot attachment;
- `DataUpdated` event shape;
- `RuntimeWarning` / `RuntimeError` quality event shape;
- normalized data output order.

## Gates

The next movement must pass:

- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-001ZE-02 `root.contracts.runtime_support.data_module.collection_orchestration` actual extraction.
