# v4.16.0 root.contracts.core_ir.v4_contracts.machine_contract parent residual judgment selects static_validation

> Batch: BE-001UA-01
> Node: `root.contracts.core_ir.v4_contracts.machine_contract`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_contract` returns to its internal residual queue after the parent leaf decision kept it open.

Selected next child:

`root.contracts.core_ir.v4_contracts.machine_contract.static_validation`

This child owns only the behavior surface of `V4MachineContract::validate_static_contract`.

## Residual Queue

| Child | Status |
| --- | --- |
| `machine_contract.schema_and_taxonomy` | Parent residual after validation extraction; may close as the local schema facade. |
| `machine_contract.static_validation` | Selected next. |
| `machine_contract.default_helper_surface` | Conditional residual; helpers stay parent-mediated unless a later judgment moves them. |

## Selection Rationale

`static_validation` is selected because it is the only behavior-heavy part of the current machine contract child:

- it is a public invariant gate;
- it owns state/group/transition/memory/child-machine/policy validation failure modes;
- existing tests directly assert several of its positive and negative paths;
- moving it into a sibling child lets `machine_contract.rs` become a schema/taxonomy facade over behavior.

## Explicit Non-Owners

The selected child does not own:

- machine DTO field definitions;
- machine taxonomy enum definitions;
- `default_machine_contract_version`;
- `default_transition_conflict_policy`;
- `QsTypeRef`;
- graph contract validation;
- event catalog validation;
- test fixture helpers.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | The child owns one named public validation method. |
| parent_child_communication_kept | pass | The child can import machine schema and constants through the machine parent. |
| equivalence_baseline_freezable | pass | The method's branch order and error strings can be frozen exactly before movement. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | pass | `validate_static_contract` is public API. |
| state_machine_phase | pass | It validates the local state-machine contract. |
| strategy_branch | guarded | Strategy bridge users depend on the invariant, but bridge behavior remains outside this child. |
| independent_failure_mode | pass | Validation has independent failures for identity, references, memory, nesting, and policy conflict. |
| reuse_pressure | pass | Graph/static bundle/bridge/test surfaces reuse the same validation. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The validation method has a real behavior owner. |
| communication_cost_rises | no | Moving one impl keeps imports local and avoids splitting individual checks. |
| local_proof_missing | no | Machine validation tests already exist under `qrpc_core_ir/src/v4.rs`. |
| line_count_only | no | Split is behavior-vs-schema ownership. |

leaf_split_decision_result

`continue_baseline: true`; `static_validation` is selected as the next machine contract child.

## Next Step

BE-001UB-01 `root.contracts.core_ir.v4_contracts.machine_contract.static_validation` baseline_plan.

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
