# v4.16.0 root.contracts.quantscript.typed_resolution.semantic_inference_surface single leaf closeout

> Batch: BE-002GH-03
> Node: `root.contracts.quantscript.typed_resolution.semantic_inference_surface`
> Parent: `root.contracts.quantscript.typed_resolution`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Result

`stop_split: true`

The semantic inference child is a terminal leaf for the current recursive pass.

## Evidence

Leaf granularity evaluation:

- Path: `quantscript/src/resolve/semantic_inference_surface.rs`
- LOC: 567
- Functions: 20
- Public surface: 20, limited to `pub(super)` parent-visible resolver helpers
- Domains: 1
- Split decision: `STOP`
- Governance packaging: `stop_split`
- Score: 63
- Terminal leaf control: `terminal_target_150_600`

Gate evidence from the extraction step remains valid:

- `cargo fmt --check` PASS
- `cargo test -p quantscript` PASS, 88 tests
- `cargo check -p quantpilot` PASS

## Stop Split Rationale

The child owns one cohesive semantic domain:

- expression semantic classification;
- manual indicator formula inference;
- series view and window aggregate shape inference;
- alias resolution required by formula detection;
- smoothing/change-pair helper recognition required by semantic formulas.

Although the file has 20 parent-visible helper methods, they are all part of the same semantic recognition graph. Splitting the MACD, moving average, z-score, and series-shape helpers would create sibling coupling inside one logical inference owner and increase governance overhead without improving the module tree.

## Preserved Boundary

Preserved:

- `quantscript/src/resolve/mod.rs` remains the typed-resolution parent;
- `public_type_surface` and `callable_classification_surface` remain closed children;
- type inference, binding insertion, resolver orchestration, scalar helpers, tests, analysis, lowering, evaluator, backend, executor, frontend, and plugin metadata remain outside this leaf.

## Next Step

BE-002GI-01 `root.contracts.quantscript.typed_resolution` parent_residual_judgment.
