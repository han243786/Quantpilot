# v4.16.0 root.contracts.qrpc_core.runtime_io_contract parent residual judgment selects runtime_output

> Batch: BE-001SK-01
> Node: `root.contracts.qrpc_core.runtime_io_contract`
> Selected child: `root.contracts.qrpc_core.runtime_io_contract.runtime_output`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core.runtime_io_contract` returns to its child queue after `portfolio_state` closeout.

Decision:

`next_child: root.contracts.qrpc_core.runtime_io_contract.runtime_output`

## Closed Runtime IO Children

| Child | Result |
| --- | --- |
| `contracts.qrpc_core.runtime_io_contract.market_data_io` | Closed with `stop_split: true`. |
| `contracts.qrpc_core.runtime_io_contract.decision_flow` | Closed with `stop_split: true`. |
| `contracts.qrpc_core.runtime_io_contract.execution_io` | Closed with `stop_split: true`. |
| `contracts.qrpc_core.runtime_io_contract.portfolio_state` | Closed with `stop_split: true`. |

## Open Runtime IO Residuals

| Residual | Status |
| --- | --- |
| `contracts.qrpc_core.runtime_io_contract.runtime_output` | Selected next. Owns runtime event, runtime cycle output, and session output DTOs. |
| `contracts.qrpc_core.runtime_io_contract.backtest_output` | Queued. Owns backtest equity, metrics, summary, period return, and final output DTOs. |

## Selection Rationale

`runtime_output` is selected because it is the next contiguous runtime IO family in `qrpc_core/src/runtime_io_contract.rs`:

- it owns `RuntimeEvent`;
- it owns `RuntimeCycleOutput`;
- it owns `SessionOutput`;
- it aggregates closed market data, decision flow, execution IO, and portfolio state DTOs through the runtime IO parent facade;
- it is consumed by later backtest output and downstream runtime/backend response mapping.

## Same-Parent Parallel Judgment

`same_parent_parallel_children` remains inactive for this selection.

Reason: `RuntimeCycleOutput` consumes several closed runtime IO children, and the next `backtest_output` residual consumes `SessionOutput`. Serial extraction keeps the aggregate path parent-mediated and avoids mixing output aggregation with backtest summary ownership.

## Hard Boundaries

The next `runtime_output` baseline must not:

- edit Rust source code;
- change runtime event, cycle output, or session output field names, types, serde derives, public exports, or aggregation ordering;
- move backtest output DTOs, RFC execution contracts, or crate-root tests;
- reopen closed market data IO, decision flow, execution IO, or portfolio state children;
- change downstream runtime/backend/compiler behavior or release transition.

## Next Step

BE-001SL-01 `root.contracts.qrpc_core.runtime_io_contract.runtime_output` baseline_plan.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
