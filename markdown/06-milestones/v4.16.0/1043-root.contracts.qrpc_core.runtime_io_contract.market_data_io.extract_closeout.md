# v4.16.0 root.contracts.qrpc_core.runtime_io_contract.market_data_io extract closeout

> Batch: BE-001SD-02
> Node: `root.contracts.qrpc_core.runtime_io_contract.market_data_io`
> Parent: `root.contracts.qrpc_core.runtime_io_contract`
> Stage: `extract_closeout`
> Movement: Rust code extraction.

---

## Summary

`root.contracts.qrpc_core.runtime_io_contract.market_data_io` has been extracted from `qrpc_core/src/runtime_io_contract.rs` into a private runtime IO child module.

The runtime IO parent preserves the local public facade:

- `mod market_data_io;`
- `pub use market_data_io::*;`

The qrpc-core crate root remains unchanged and continues to preserve public exports through `pub use runtime_io_contract::*;`.

## Files Changed

- `qrpc_core/src/runtime_io_contract.rs`
- `qrpc_core/src/runtime_io_contract/market_data_io.rs`

## Extraction Result

`qrpc_core/src/runtime_io_contract/market_data_io.rs` now owns:

- `RawKline`;
- `RawQuote`;
- `NormalizedKline`;
- `KlineSeriesSnapshot`;
- `QuoteSnapshot`;
- `NormalizedMarketData`.

`qrpc_core/src/runtime_io_contract.rs` now keeps the runtime IO parent facade plus queued decision flow, execution IO, portfolio state, runtime output, and backtest output DTO families.

## Rust Local Fields

| Field | Result |
| --- | --- |
| Crate | `qrpc-core` |
| Parent facade | Preserved. `runtime_io_contract.rs` declares and re-exports the market data child. |
| Child visibility | The module is private; public market data surfaces are preserved through `pub use market_data_io::*;`. |
| Crate public exports | Preserved through qrpc-core crate-root `pub use runtime_io_contract::*;`. |
| Parent-owned residuals | Decision flow, execution IO, portfolio state, runtime output, backtest output, RFC execution contracts, and tests remain parent-owned. |
| Sibling dependency check | Passed. The child imports closed qrpc-core public names through the crate root and does not import sibling internals directly. |

## Equivalence Preservation

| Surface | Preserved evidence |
| --- | --- |
| Raw kline input | `RawKline` fields and numeric types remain unchanged. |
| Raw quote input | `RawQuote` fields and numeric types remain unchanged. |
| Normalized kline | Instrument, interval, timestamp, OHLC, and volume fields remain unchanged. |
| Kline series snapshot | Data identity, instrument, bars, timing, latency, source status, and defaulted data quality fields remain unchanged. |
| Quote snapshot | Data identity, instrument, quote prices/sizes, mid price, timing, latency, source status, and defaulted data quality fields remain unchanged. |
| Normalized data enum | `KlineSeries` and `Quote` variants remain unchanged. |
| Public facade | Parent and crate-root re-exports preserve `qrpc_core::*` access. |

## Hard Boundaries Held

BE-001SD-02 did not:

- change market data fields, serde derives, enum variants, default behavior, public exports, or tests;
- move decision flow, execution IO, portfolio state, runtime output, backtest output, RFC execution contracts, or crate-root tests;
- change closed qrpc-core children, downstream runtime/backend/compiler behavior, or release transition.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-pre-commit-hook.ps1`

## Next Step

BE-001SD-03 `root.contracts.qrpc_core.runtime_io_contract.market_data_io` single_leaf_closeout.
