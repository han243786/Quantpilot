# v4.16.0 root.contracts.runtime_support.risk_support.event_payload_projection baseline plan

> Batch: BE-002BR-01
> Node: `root.contracts.runtime_support.risk_support.event_payload_projection`
> Parent: `root.contracts.runtime_support.risk_support`
> Stage: `baseline_plan`
> Movement: No Rust code movement.

---

## Scope

Freeze the baseline for extracting risk event payload projection helpers.

Allowed movement:

- add `qrpc_runtime/src/risk_checker/event_payload_projection.rs`;
- add a parent child declaration in `qrpc_runtime/src/risk_checker.rs`;
- move only the selected projection helpers;
- keep helper visibility parent-mediated with `pub(super)` where needed.

## In-Scope Helpers

- `build_risk_event_payload`
- `risk_explanation_summary`
- `reason_code_name`
- `action_stats`
- `target_weight_stats`

## Out of Scope

- `RiskCheckRequest`
- `RiskCheckOutput`
- `RiskCheckerProvider`
- `RiskChecker`
- `RiskCheckerProvider::evaluate`
- `evaluate_risk_decision`
- `enforce_risk_mode`
- direction and cross-symbol constraints
- action clamp helpers
- portfolio target clamp helpers
- exposure math helpers such as `portfolio_equity`, `symbol_net_exposure_ratio`, and `portfolio_net_exposure_ratio`
- tests

## Dependency Rule

The child may use `super::*` to access parent-owned types and exposure math helpers. It must not import siblings directly and must not introduce a release-transition shortcut.

## Equivalence Baseline

The next actual extraction must preserve:

- risk event payload keys;
- reason-code string names;
- explanation summary text;
- action-list stats shape;
- portfolio-target stats shape;
- provider/evaluate public surface.

## Gates

Required gates for actual extraction:

- `cargo fmt`
- `cargo check -p qrpc-runtime`
- risk-related qrpc-runtime tests when available
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 / full feature tree / matrix governance

## Next Step

BE-002BS-01 `root.contracts.runtime_support.risk_support.event_payload_projection` actual_extraction.
