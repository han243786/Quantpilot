# v4.16.0 root.contracts.qrpc_core.runtime_io_contract.backtest_output single leaf closeout

> Batch: BE-001SN-03
> Node: `root.contracts.qrpc_core.runtime_io_contract.backtest_output`
> Parent: `root.contracts.qrpc_core.runtime_io_contract`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`backtest_output` closes as one compact runtime IO child owner.

Final owner file:

- `qrpc_core/src/runtime_io_contract/backtest_output.rs`

Owned public DTOs:

- `BacktestEquityPoint`
- `BacktestRiskAdjusted`
- `BacktestTradeAnalysis`
- `BacktestDrawdownAnalysis`
- `BacktestBenchmarkComparison`
- `BacktestSummary`
- `PeriodReturn`
- `BacktestOutput`

## Closeout Evidence

| Check | Result | Evidence |
| --- | --- | --- |
| Parent facade preserved | pass | `qrpc_core/src/runtime_io_contract.rs` declares `mod backtest_output;` and `pub use backtest_output::*;`. |
| Crate public surface preserved | pass | `qrpc_core/src/lib.rs` continues re-exporting `runtime_io_contract::*`. |
| DTO equivalence | pass | Moved DTO fields, derives, defaults, and skip rules are unchanged. |
| Parent-owned dependency | pass | `SessionOutput` and `PortfolioState` are imported through the runtime IO parent facade. |
| No sibling horizontal link | pass | The child does not import closed child modules directly. |
| Parent shape | pass | `qrpc_core/src/runtime_io_contract.rs` is now a pure facade over runtime IO children. |

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | The child owns a named final backtest output DTO family. |
| parent_child_communication_kept | pass | Parent facade mediates `SessionOutput` and `PortfolioState` dependencies. |
| equivalence_baseline_freezable | pass | BE-001SN-01 froze fields, derives, serde defaults, skip rules, and nested metric shapes; BE-001SN-02 preserved them. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | no further split | Public backtest output schema is already isolated as a single child. |
| state_machine_phase | no | No state-machine phase logic exists in this child. |
| strategy_branch | no | No strategy branch or handler branch exists in this child. |
| independent_failure_mode | no further split | Metric groups are nested into `BacktestSummary` and consumed by `BacktestOutput`. |
| reuse_pressure | no further split | Runtime/backend reuse remains satisfied through the parent facade. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | pass | Splitting metric DTOs away from summary/output would create tiny schema-only leaves. |
| communication_cost_rises | pass | Splitting nested metric groups would add parent mediation without a new behavior boundary. |
| local_proof_missing | no | qrpc-core gates prove the current compact child. |
| line_count_only | pass | There is no ownership reason beyond line count to split further. |

leaf_split_decision_result

`stop_split: true`

`backtest_output` is a compact final schema leaf. Further split is not justified until independent metric calculation, validation, persistence, or behavior owner appears.

## Next Step

BE-001SO-01 `root.contracts.qrpc_core.runtime_io_contract` parent_residual_judgment closes the runtime IO parent.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
