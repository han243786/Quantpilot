# v4.16.0 root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_rsi_formula extract closeout

> Batch: BE-002JG-02
> Node: `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_rsi_formula`
> Stage: `actual_extraction`
> Movement: Rust code movement complete.
> governance_next_authority: active

---

## Extraction Result

`quantscript/src/lowering/fallback/manual_rsi_formula.rs` now owns RSI manual fallback recognition:

- RSI shell parsing for `100 - 100 / (1 + RS)`;
- RS pair denominator recognition for `1 + RS` and `RS + 1`;
- average gain/loss operand extraction;
- balanced smoothed gain/loss source, period, and smoothing checks;
- `ChangeSmoothing` to `RsiMethod` mapping.

The parent `quantscript/src/lowering/fallback.rs` keeps the stable public(crate) surface:

- `manual_rsi_from_expr` delegates to the child implementation;
- `match_manual_rsi_formula` delegates to the child implementation for direct internal tests and compatibility;
- `resolve_manual_formula_binding` still tries RSI first before MACD, momentum, ZScore, and moving-average fallback recognizers.

## Boundary Preservation

| Contract | Result |
| --- | --- |
| Recognizer order | Preserved; `resolve_manual_formula_binding` still calls RSI first |
| Public(crate) wrappers | Preserved in the parent module |
| Non-RSI fallback owners | Not moved |
| Trading semantics | Preserved by existing RSI formula tests and full `quantscript` test suite |
| Parent/child communication | Parent delegates through `mod manual_rsi_formula`; child helpers remain private |

## Verification

| Gate | Result |
| --- | --- |
| `cargo fmt --all --check` | PASS |
| `cargo test -p quantscript` | PASS, 88 tests |
| `cargo check -p quantpilot` | PASS, no warnings |

Additional governance gates are run before commit for diff whitespace, UTF-8, full feature tree, and matrix governance.

## Next Step

BE-002JG-03 `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_rsi_formula` single_leaf_closeout.
