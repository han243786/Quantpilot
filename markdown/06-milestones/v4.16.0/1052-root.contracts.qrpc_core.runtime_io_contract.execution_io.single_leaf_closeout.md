# v4.16.0 root.contracts.qrpc_core.runtime_io_contract.execution_io single leaf closeout

> Batch: BE-001SH-03
> Node: `root.contracts.qrpc_core.runtime_io_contract.execution_io`
> Parent: `root.contracts.qrpc_core.runtime_io_contract`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`execution_io` closes as one compact runtime IO child owner.

Final owner file:

- `qrpc_core/src/runtime_io_contract/execution_io.rs`

Owned public DTOs:

- `SimOrder`
- `ExecutionPlan`
- `FillReport`
- `OpenOrder`
- `FillResult`

## Closeout Evidence

| Check | Result | Evidence |
| --- | --- | --- |
| Parent facade preserved | pass | `qrpc_core/src/runtime_io_contract.rs` declares `mod execution_io;` and `pub use execution_io::*;`. |
| Crate public surface preserved | pass | `qrpc_core/src/lib.rs` continues re-exporting `runtime_io_contract::*`. |
| DTO equivalence | pass | Moved DTO fields, derives, and serde defaults are unchanged. |
| Parent-owned dependency | pass | `FillResult.events` uses parent-owned `RuntimeEvent`; `runtime_output` remains a separate residual. |
| No sibling horizontal link | pass | The child imports only parent-owned `RuntimeEvent` and qrpc-core primitive facade types. |
| No behavior movement | pass | The child owns schema only; no functions, methods, mutable state, IO, locks, or persistence were moved. |

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | The child owns a named execution IO DTO family. |
| parent_child_communication_kept | pass | Parent facade mediates both public exports and the `RuntimeEvent` dependency. |
| equivalence_baseline_freezable | pass | BE-001SH-01 froze the field/derive/serde facade baseline, and BE-001SH-02 preserved it. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | no further split | Public schema is already isolated as a single compact child. |
| state_machine_phase | no | No state machine phase exists in this child. |
| strategy_branch | no | No strategy branch or handler branch exists in this child. |
| independent_failure_mode | no further split | Fill/open-order schema failures are coupled through `FillResult`. |
| reuse_pressure | no further split | Runtime/backend reuse remains satisfied through the parent facade. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | pass | Splitting five DTOs into smaller children would create micro leaves without stronger owners. |
| communication_cost_rises | pass | Splitting `FillResult`, `FillReport`, and `OpenOrder` would add extra parent mediation with no new behavior boundary. |
| local_proof_missing | no | qrpc-core gates prove the current compact child. |
| line_count_only | pass | There is no ownership reason beyond line count to split further. |

leaf_split_decision_result

`stop_split: true`

`execution_io` is a compact schema leaf. Further split is not justified until new execution behavior, state ownership, public protocol evolution, or independent validation logic appears.

## Next Step

BE-001SI-01 `root.contracts.qrpc_core.runtime_io_contract` parent_residual_judgment selects the next runtime IO residual candidate, expected `portfolio_state`.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
