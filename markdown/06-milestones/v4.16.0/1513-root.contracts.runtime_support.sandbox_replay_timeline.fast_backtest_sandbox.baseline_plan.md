# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline.fast_backtest_sandbox baseline plan

> Batch: BE-002DA-01
> Node: `root.contracts.runtime_support.sandbox_replay_timeline.fast_backtest_sandbox`
> Parent: `root.contracts.runtime_support.sandbox_replay_timeline`
> Stage: `baseline_plan`
> Movement: No Rust code movement.

---

## Scope

Freeze the baseline for extracting `FastBacktestSandbox` from `qrpc_runtime/src/sandbox/mod.rs`.

Allowed movement:

- add `qrpc_runtime/src/sandbox/fast_backtest_sandbox.rs`;
- move `FastBacktestSandbox`, its constructor/accessor impl, its `run_backtest` implementation, and its `Sandbox` trait impl;
- move `compute_benchmark_equity`, because it is a fast-backtest-only benchmark projection helper;
- keep parent shared helpers in `mod.rs`: `snapshot_from`, `ensure_running`, and `trace_id`;
- keep sandbox tests in `mod.rs` until the test harness child is selected.

## In-Scope Surface

- `FastBacktestSandbox`
- `FastBacktestSandbox::new`
- `FastBacktestSandbox::with_test_mode`
- `FastBacktestSandbox::with_unified_timeline_from_core_ir`
- `FastBacktestSandbox::with_unified_timeline_from_core_ir_and_test_mode`
- `FastBacktestSandbox::with_mock_unified_timeline_from_core_ir`
- `FastBacktestSandbox::with_mock_unified_timeline_from_core_ir_and_test_mode`
- v1.0.7 replay compatibility constructors
- `FastBacktestSandbox::test_mode`
- `FastBacktestSandbox::with_execution_assumptions`
- `FastBacktestSandbox::set_execution_assumptions`
- `FastBacktestSandbox::set_risk_monitor`
- `FastBacktestSandbox::is_risk_stopped`
- `FastBacktestSandbox::replay_timestamps`
- `FastBacktestSandbox::run_backtest`
- `impl Sandbox for FastBacktestSandbox`
- `compute_benchmark_equity`

## Out of Scope

- `RealTimeSandbox`
- `snapshot_from`
- `ensure_running`
- `trace_id`
- sandbox tests
- replay/timeline/mode-surface children
- release transition

## Equivalence Baseline

The next actual extraction must preserve:

- fast backtest start/stop/running behavior;
- `SandboxMode::FastBacktest`;
- unified timeline and mock replay setup behavior;
- v1.0.7 replay timestamp compatibility;
- execution assumptions and risk monitor helper behavior;
- `run_backtest` summary, sessions, equity curve, benchmark curve, debug values, and final portfolio behavior;
- snapshot/restore behavior through parent-owned shared helpers;
- public crate-root access to `FastBacktestSandbox`.

## Dependency Rule

The fast backtest child may use parent-owned shared helpers through the sandbox parent. It must not depend on `RealTimeSandbox` or directly bypass the replay/timeline/mode-surface child boundaries.

## Gates

Required gates for actual extraction:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime sandbox -- --nocapture`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 / full feature tree / matrix governance

## Next Step

BE-002DB-01 `root.contracts.runtime_support.sandbox_replay_timeline.fast_backtest_sandbox` actual_extraction.
