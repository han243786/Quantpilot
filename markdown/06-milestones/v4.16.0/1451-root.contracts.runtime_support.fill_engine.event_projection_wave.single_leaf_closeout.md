# v4.16.0 root.contracts.runtime_support.fill_engine.event_projection_wave single leaf closeout

> Batch: BE-002BD-02
> Node: `root.contracts.runtime_support.fill_engine.event_projection_wave`
> Parent: `root.contracts.runtime_support.fill_engine`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Decision

`root.contracts.runtime_support.fill_engine.event_projection_wave` is closed with `stop_split: true`.

## Leaf Granularity Result

| Metric | Value |
| --- | --- |
| LOC | 130 |
| Functions | 5 |
| Public surface | 5 `pub(super)` helpers |
| Branches | 2 |
| Domains | 1 |
| Score | 31 |
| Decision | STOP |
| Governance mode | `stop_split` |

## Rationale

The child is a cohesive event-projection wave. Splitting it into individual event helpers would create five tiny leaves, increase governance cost, and reduce module clarity without improving parent-child boundaries.

The current child size is below the default terminal range but above micro-leaf territory. Because all five functions share one lifecycle-projection purpose, this is a valid compact terminal wave.

## Preserved Boundaries

- parent fill lifecycle orchestration remains outside this child;
- reservation/accounting helpers remain outside this child;
- fill report helpers remain outside this child;
- portfolio mutation remains outside this child;
- tests remain outside this child;
- no sibling shortcuts exist.

## Next Step

BE-002BE-01 `root.contracts.runtime_support.fill_engine` parent_residual_judgment selects `portfolio_reservation_accounting`.
