# v4.16.0 root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.basic_runtime_smoke_tests baseline plan

> Batch: BE-002II-01
> Node: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.basic_runtime_smoke_tests`
> Parent: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Baseline Decision

`basic_runtime_smoke_tests` is frozen as a precision test-only child for extraction.

The next movement may create a child test module under the existing lowering orchestrator test harness and move only the basic MA/RSI/helper/exchange smoke tests plus their shared script constants from `quantscript/src/lowering/orchestrator/integration_test_harness.rs`.

## Frozen Input / Output

| Field | Boundary |
| --- | --- |
| Input | Test-only MA, RSI, aliased data binding, user helper, runtime coordinator, and exchange argument QuantScript fixtures |
| Output | Existing assertions over data source count/id/exchange, intent count/id/kind, input ids, RSI threshold params, and runtime coordinator session output |
| Parent caller | `integration_test_harness` test module only |
| Error surface | No rejection diagnostics move in this batch |
| Gate | `cargo test -p quantscript` |

## Symbols Approved For Movement

Move only these existing constants and tests:

- `MA_SCRIPT`
- `RSI_SCRIPT`
- `lowers_ma_cross_into_runtime_config`
- `lowers_ma_cross_with_aliased_data_binding_into_consistent_input_ids`
- `lowers_rsi_thresholds_into_single_runtime_intent`
- `lowered_script_runs_in_runtime`
- `lowers_user_defined_helper_function_calls`
- `lowers_fetch_exchange_argument_into_data_source`

Move any imports needed only by those tests into the child if parent imports become unused. No production Rust helper moves are approved.

## Symbols Explicitly Not Approved For Movement

- `rejects_semantic_errors_before_runtime_lowering`
- `manual_formula_tests` child
- `spread_lowering_tests` child
- `rebalance_lowering_tests` child
- production lowering code, production imports, and parent test module declaration

## Parent Mediation Rule

After extraction:

- `integration_test_harness.rs` declares the child test module;
- the child uses parent-visible or direct test imports only;
- no production sibling module is called from the child;
- smoke test names and assertions remain equivalent.

## Split Decision Gate

| Gate | Result | Evidence |
| --- | --- | --- |
| `micro_leaf_without_owner` | PASS | Six tests share one basic lowering/runtime smoke surface. |
| `communication_cost_rises` | PASS | Movement is test-only and remains under the parent test harness. |
| `local_proof_missing` | PASS | `cargo test -p quantscript` directly executes the moved tests. |
| `line_count_only` | PASS | The split follows smoke-test ownership rather than line count. |

## Validation

The actual extraction must pass:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8, full-feature-tree, and matrix governance checks

## Next Step

BE-002II-02 actual_extraction.
