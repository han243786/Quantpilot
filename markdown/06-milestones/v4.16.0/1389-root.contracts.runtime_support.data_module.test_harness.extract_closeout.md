# v4.16.0 root.contracts.runtime_support.data_module.test_harness extract closeout

> Batch: BE-001ZQ-02
> Node: `root.contracts.runtime_support.data_module.test_harness`
> Parent: `root.contracts.runtime_support.data_module`
> Stage: `actual_extraction`
> Movement: Rust test code movement.

---

## Extraction Summary

Created:

- `qrpc_runtime/src/data_module/test_harness.rs`

Updated:

- `qrpc_runtime/src/data_module.rs`

Moved implementation surface:

- local data-module unit-test fixtures;
- OKX parser tests;
- built-in data module provider behavior tests;
- source hint request-control test;
- request-interval cache test;
- pseudo-random mock generator tests.

## Parent Facade

`qrpc_runtime/src/data_module.rs` now owns only:

- `#[cfg(test)] mod test_harness;`
- production facade/cache/transport/mock/normalization wiring;
- parent-private helpers used by the test harness through child-module visibility.

## Boundary Preservation

| Boundary | Result |
| --- | --- |
| Production behavior | No production logic moved. |
| Test scope | Existing local data-module assertions moved together. |
| Parent-private access | Test harness remains a direct child of `data_module`, so private helper access is preserved. |
| Parser coverage | OKX candle/ticker parser tests still run. |
| Provider coverage | Mock/non-OKX and mixed-source fast-cycle tests still run. |
| Source hint coverage | Request-control source hint restoration test still runs. |
| Cache coverage | Request-interval cache test still runs. |
| Mock generator coverage | Pseudo-random determinism, range, and difference tests still run. |

## Exclusions Preserved

No movement was made to:

- production parent facade;
- request cache;
- breaker logic;
- HTTP transport;
- historical cache;
- mock generation;
- normalization;
- source mapping;
- exchange surface;
- quality diagnostics.

## Equivalence Evidence

This extraction passed:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`

## Next Step

BE-001ZQ-03 `root.contracts.runtime_support.data_module.test_harness` single_leaf_closeout. The closeout must run terminal leaf control against `qrpc_runtime/src/data_module/test_harness.rs` before deciding whether to split further.
