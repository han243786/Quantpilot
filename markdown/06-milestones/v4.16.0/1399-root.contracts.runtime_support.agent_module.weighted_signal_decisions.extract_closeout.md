# v4.16.0 root.contracts.runtime_support.agent_module.weighted_signal_decisions extract closeout

> Batch: BE-001ZX-02
> Node: `root.contracts.runtime_support.agent_module.weighted_signal_decisions`
> Parent: `root.contracts.runtime_support.agent_module`
> Stage: `actual_extraction`
> Movement: Rust code movement.

---

## Extracted Child

Created:

- `qrpc_runtime/src/agent_module/weighted_signal_decisions.rs`

Updated:

- `qrpc_runtime/src/agent_module.rs`

## Movement Summary

Moved from parent into the child:

- `build_weighted_agent_decisions`
- `group_signals_by_symbol`

Parent retained:

- public request/output/provider/builtin types;
- `BuiltinAgentModule::evaluate_agents` dispatch and event assembly;
- shared helper ownership:
  - `build_signal_kind_index`
  - `signal_score`
  - `available_position_ratio`
  - `MIN_QUANTITY_RATIO`
  - `DEFAULT_DECISION_THRESHOLD`
- portfolio rebalance, cross-venue arbitrage, shared helper queue, and tests.

## Parent Mediation

The parent now declares:

- `mod weighted_signal_decisions;`

The parent dispatches `AgentPolicyKind::WeightedSignals` on `slow` cycle through:

- `weighted_signal_decisions::build_weighted_agent_decisions(...)`

The child uses parent-owned helpers through `super::...`; no sibling dependency or release-transition shortcut is introduced.

## Equivalence Preserved

The extracted function keeps:

- observe signal filtering;
- grouping by first exchange and symbol scope with fallback defaults;
- confidence-weighted net score calculation;
- decision threshold behavior;
- max quantity clamp;
- sell-side inventory clamp;
- long/short action mapping;
- decision id, reason, strategy tag, timestamp, and trace id shape.

## Size Impact

- `qrpc_runtime/src/agent_module.rs`: reduced to 1570 lines after extraction.
- `qrpc_runtime/src/agent_module/weighted_signal_decisions.rs`: 119 lines.

## Gates

- `cargo fmt`: PASS
- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime`: PASS, 227 passed, 2 ignored
- `cargo check -p quantpilot`: PASS
- `git diff --check`: PASS
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`: PASS
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`: PASS
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`: PASS

## Next Step

BE-001ZX-03 `root.contracts.runtime_support.agent_module.weighted_signal_decisions` single_leaf_closeout.
