# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.graph_acyclic_validation equivalence baseline and extraction plan

> Batch: BE-001UT-01
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.graph_acyclic_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation`
> Stage: `baseline_plan`
> Movement: No code movement.

---

## Summary

This baseline freezes graph acyclicity validation before extraction.

The selected child may own only:

- `V4MachineGraphContract::validate_graph_acyclic`
- its local adjacency construction;
- its local DFS helper;
- cycle path reversal and error formatting.

It must not own root graph validation orchestration, schema/version checks, machine ID checks,
edge identity/endpoint/self-loop checks, event usage validation, risk-plane validation,
event party helper, graph schema, traversal helpers, bridge code, or tests.

## Frozen Validation Surface

The next extraction must preserve:

- adjacency construction from every edge source to target;
- visited and in-stack DFS semantics;
- cycle path collection and reversal;
- first-cycle return behavior;
- success return when no cycle exists;
- error formatting:
  `machine graph must be acyclic, cycle: ...`;
- parent call order after edge validation and before event usage/risk-plane validation.

## Allowed BE-001UT-02 Movement

The next extraction step may:

- create `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/graph_acyclic_validation.rs`;
- add `mod graph_acyclic_validation;` to `qrpc_core_ir/src/v4/machine_graph_contract/static_validation.rs`;
- move only `V4MachineGraphContract::validate_graph_acyclic`;
- keep `validate_static_contract` calling `self.validate_graph_acyclic().err().unwrap_or_default()`.

The child may depend on:

- `std::collections::{BTreeMap, BTreeSet}`;
- `V4MachineGraphContract`.

## Forbidden BE-001UT-02 Movement

The next extraction step must not move or edit behavior for:

- `V4MachineGraphContract::validate_static_contract`;
- graph schema version check;
- graph ID and machine presence checks;
- machine ID, duplicate, and nested duplicate checks;
- edge ID/event/source/target/self-loop checks;
- event usage validation children;
- risk-plane validation;
- `machine_event_party_allowed`;
- graph schema DTOs;
- traversal helpers;
- tests.

It must not alter validation branch order, cycle path formatting, or error strings.

## Equivalence Proof Plan

BE-001UT-02 must pass:

- `cargo fmt --check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation.rs`

**Planned child file**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/graph_acyclic_validation.rs`

**Markers**:
- `BE-001UT-01`

**Next step**:
BE-001UT-02 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.graph_acyclic_validation extract_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
