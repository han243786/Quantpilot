# v4.16.0 root.contracts.quantscript.runtime_lowering parent residual judgment: lowering_orchestrator_surface

> Batch: BE-002HV-01
> Node: `root.contracts.quantscript.runtime_lowering`
> Selected child: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface` is selected as the next child.

Reason:

- it owns the public lowering entrypoints re-exported from `quantscript/src/lowering/mod.rs`;
- it coordinates the first observable runtime config output shape;
- it carries profile detection and integration tests that protect runtime-lowering equivalence;
- freezing this boundary first reduces risk before touching deeper universe, intent, binding, fallback, or semantic bridge children.

## White-Box Boundary

| Field | Boundary |
| --- | --- |
| Input | `ScriptModule`, optional `LoweringContext`, normalized strategy body, current helper/function maps |
| Output | `RuntimeProtocolCoreConfig`, accumulated diagnostics, risk profile config, execution profile config |
| Owner | `quantscript/src/lowering/orchestrator.rs` |
| Parent caller | `quantscript/src/lowering/mod.rs` public re-export surface |
| Child dependencies | Universe expansion, data-source inference, binding collection, intent inference, diagnostic formatting |
| Exclusions | Universe implementation internals, intent inference internals, binding decoder internals, fallback/manual formula internals, source recovery internals, semantic bridge internals |

## Split Gate

| Gate | Result | Evidence |
| --- | --- | --- |
| `micro_leaf_without_owner` | NO | The child owns public entrypoints and integration tests, not a helper fragment. |
| `communication_cost_rises` | NO | Selecting this first keeps parent-child calls anchored at the current public facade. |
| `local_proof_missing` | NO | Existing tests in `orchestrator.rs` cover the current public lowering behavior. |
| `line_count_only` | NO | Selection is based on public entry ownership and risk reduction, not size alone. |

## Risk Level

This child must use precision single-leaf governance for movement:

- public API re-export surface is involved;
- runtime config output shape is involved;
- trading intent/profile semantics are indirectly involved;
- integration tests are colocated with the current owner.

No Rust movement is authorized until BE-002HW-01 freezes the exact baseline.

## Residuals

Still open under `root.contracts.quantscript.runtime_lowering`:

- `lowering_orchestrator_surface`
- `universe_lowering`
- `intent_inference`
- `binding_source_decoding`
- `indicator_binding_resolution`
- `manual_formula_fallback`
- `source_recovery_bridge`
- `semantic_bridge`
- `helper_env`
- `context_surface`
- `shared_arg_helpers`
- `diagnostic_formatting`

## Next Step

BE-002HW-01 `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface` baseline_plan.
