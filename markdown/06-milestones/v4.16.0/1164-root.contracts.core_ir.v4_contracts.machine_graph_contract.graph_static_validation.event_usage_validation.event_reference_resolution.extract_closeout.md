# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation.event_reference_resolution actual extraction complete

> Batch: BE-001UQ-02
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation.event_reference_resolution`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation`
> Stage: `extract_closeout`
> Movement: Code movement completed.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation.event_reference_resolution`
actual extraction is complete.

The child now owns nested machine discovery, transition event reference collection,
action emitted-event reference collection, edge event reference collection, empty action-emits
error reporting, and the `EventReferenceResolution` result. The parent event usage validation
still owns catalog presence checks, local catalog validation, event spec map construction,
undeclared event checks, party validation dispatch, and orchestration.

## Movement

Moved:

- nested machine discovery through `collect_machine_family`;
- transition event reference collection;
- action emitted-event reference collection;
- edge event reference collection;
- empty action-emits error reporting;
- `EventReferenceResolution` result structure.

Kept in parent:

- event catalog presence requirement;
- `MachineEventCatalog::validate_static_contract` delegation;
- event spec map construction;
- undeclared event rejection;
- `validate_event_parties` dispatch;
- `validate_event_catalog` orchestration.

## Equivalence Result

- `cargo fmt -p qrpc-core-ir` passed.
- `cargo test -p qrpc-core-ir` passed: 54 tests passed, 0 failed.

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/event_usage_validation.rs`
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/event_usage_validation/event_reference_resolution.rs`

**Markers**:
- `BE-001UQ-02`

**Next step**:
BE-001UQ-03 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation.event_reference_resolution single_leaf_closeout

---

## Gates

- `git diff --check`
- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
