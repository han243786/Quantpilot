# v4.16.0 root.executor.runner.runner_support_files.live_runner.runner_pool_orchestration baseline plan

> Batch: BE-002ML-01
> Node: `root.executor.runner.runner_support_files.live_runner.runner_pool_orchestration`
> Stage: `baseline_plan`
> governance_next_authority: active

---

## Frozen Boundary

The next movement may extract only:

- `RunnerPool`
- `impl RunnerPool`

Target file:

`src-executor/live_runner/runner_pool_orchestration.rs`

The parent `src-executor/live_runner.rs` must keep the module wiring and may
re-export `RunnerPool`.

## Explicit Non-Movement

Do not move in BE-002ML-02:

- `executor_v4_market_matrix`
- `resolve_v4_runner_venue_id`
- `resolve_v4_runner_default_symbol`
- `graph_metadata_string`
- local `#[cfg(test)]` test harness

The V4 market metadata helpers and test harness remain parent-owned for this
step.

## Compatibility Notes

- `RunnerPool::new`, `register_exchange`, `register`, `remove`, and
  `broadcast_ws_event` must retain their current public behavior.
- `RunnerPool` must continue to use `RunnerInstance` as the only runner variant
  bridge.
- The V4 evidence broadcast channel remains owned by `RunnerPool`.

## Required Gates

| Gate | Required |
| --- | --- |
| `cargo fmt --all --check` | Yes |
| `cargo check -p quantpilot --bin executor` | Yes |
| `cargo test -p quantpilot --bin executor live_runner` | Yes |
| `git diff --check` | Yes |

## Next Step

BE-002ML-02
`root.executor.runner.runner_support_files.live_runner.runner_pool_orchestration`
actual_extraction.
