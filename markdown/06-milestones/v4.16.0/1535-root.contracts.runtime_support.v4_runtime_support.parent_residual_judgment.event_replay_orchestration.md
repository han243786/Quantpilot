# v4.16.0 root.contracts.runtime_support.v4_runtime_support parent residual judgment - event_replay_orchestration

> Batch: BE-002DR-01
> Node: `root.contracts.runtime_support.v4_runtime_support`
> Selected child: `root.contracts.runtime_support.v4_runtime_support.event_replay_orchestration`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.

---

## Decision

Select `root.contracts.runtime_support.v4_runtime_support.event_replay_orchestration` as the next v4 runtime support child.

## Evidence

Closed v4 runtime support children:

- `type_surface`
- `graph_symbol_expansion`
- `runtime_constructor_mode_gate`

The BE-002DH-01 v4 runtime support baseline queue names `event_replay_orchestration` next.

The candidate currently lives in `qrpc_runtime/src/v4_runtime.rs` and covers the runtime-facing event/replay surface:

- `run_backtest_bars`
- `run_backtest_ticks`
- `submit_market_price_tick`
- `submit_market_bar_closed`
- `advance_time`
- event enqueue/drain helpers
- runtime snapshot/event projection paths needed by replay output

This child should be handled before machine transition, risk/execution gate, and simulated execution engine splits because replay orchestration defines how external market inputs become queued runtime events and backtest artifacts.

## Boundary

The next baseline must freeze event replay orchestration only. It must not move:

- public v4 type surface;
- graph symbol expansion;
- constructor/mode gate behavior;
- machine transition matching/application internals;
- risk-plane and execution-capability decision evaluation internals;
- simulated execution order/fill engine internals;
- v4 tests.

## Dependency Rule

The event replay child may use closed v4 type, graph symbol, and constructor children only through the v4 runtime support parent. It may call parent-owned transition/gate/simulated-execution internals but must not own them until their own residual judgments.

## Next Step

BE-002DS-01 `root.contracts.runtime_support.v4_runtime_support.event_replay_orchestration` baseline_plan.
