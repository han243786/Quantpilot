# v4.16.0 root.contracts.quantscript.analysis_diagnostics extract closeout

> Batch: BE-002GS-02
> Node: `root.contracts.quantscript.analysis_diagnostics`
> Parent: `root.contracts.quantscript`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> governance_next_authority: active

---

## Result

`root.contracts.quantscript.analysis_diagnostics` structural extraction is complete.

New parent child:

- `quantscript/src/analysis_diagnostics/mod.rs`

Moved children:

- `quantscript/src/analysis_diagnostics/analysis.rs`
- `quantscript/src/analysis_diagnostics/diagnostics.rs`

Parent wiring:

- `quantscript/src/lib.rs` now declares `mod analysis_diagnostics;`.
- `quantscript/src/lib.rs` publicly re-exports `analyze_script_module`, `ScriptAnalysis`, `Diagnostic`, `DiagnosticSeverity`, `Span`, and `SpanContext` from the new parent child.
- `quantscript/src/lib.rs` keeps crate-internal `crate::analysis` and `crate::diagnostics` compatibility through `pub(crate) use analysis_diagnostics::{analysis, diagnostics};`.

## Moved Surface

Moved into the child:

- analysis result DTO and `analyze_script_module`;
- unsupported construct, direct/indirect recursion, non-universe loop, series index, centered window, warmup, fetch/emit presence, lookback, index-bound, dead-code emit, and symbol whitelist diagnostics;
- `DiagnosticSeverity`, `SpanContext`, `Span`, `Diagnostic`, and diagnostic construction helpers.

## Preserved Boundary

Preserved:

- public `quantscript::*` analysis and diagnostic exports;
- internal `crate::analysis::*` and `crate::diagnostics::*` paths used by resolver, lowering, syntax HIR, and tests;
- `analyze_formal_quant_script` behavior and return shape;
- diagnostic codes, severity, span, message, and hint semantics;
- evaluator normalization, runtime lowering, v4 static audit, test plans, and samples outside this child.

## Validation

Executed during this batch:

- `cargo fmt` PASS
- `cargo test -p quantscript` PASS, 88 tests
- `cargo check -p quantpilot` PASS

Remaining closeout gates must run before the next commit:

- `cargo fmt --check`
- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002GS-03 `root.contracts.quantscript.analysis_diagnostics` single_leaf_closeout.
