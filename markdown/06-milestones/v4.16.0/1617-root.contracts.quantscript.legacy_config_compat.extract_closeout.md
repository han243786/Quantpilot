# v4.16.0 root.contracts.quantscript.legacy_config_compat actual extraction

> Batch: BE-002FZ-02
> Node: `root.contracts.quantscript.legacy_config_compat`
> Parent: `root.contracts.quantscript`
> Stage: `actual_extraction`
> Movement: Rust module movement.
> governance_next_authority: active

---

## Movement

Deprecated config-style QuantScript compatibility moved out of the crate root into:

- `quantscript/src/legacy_config_compat.rs`

Updated:

- `quantscript/src/lib.rs`

## Extracted Surface

The new child owns:

- `QuantScriptProgram`
- `RuntimeSection`
- `DataSection`
- `IntentSection`
- `AgentSection`
- `RiskSection`
- `QuantScriptSource`
- deprecated parser and compile entrypoints;
- `compile_program_to_config`;
- private `Token`, `Value`, `Parser`, tokenizer, field decoders, parse helpers, and source-kind detection helpers;
- legacy config compatibility tests.

## Parent Facade

`quantscript/src/lib.rs` now keeps only:

- `mod legacy_config_compat;`
- crate-root public re-exports for the deprecated compatibility API;
- formal QS facade functions and formal tests;
- syntax/AST, resolve, analysis, lowering, test plan, and v4 audit re-exports.

The deprecated public names remain importable through `quantscript::*`.

## Evidence

Observed before this closeout:

- `cargo test -p quantscript` PASS, 88 tests
- `cargo check -p quantpilot` PASS

The re-export uses local `#[allow(deprecated)]` so the compatibility API remains deprecated for callers without warning inside the crate facade.

## Next Step

BE-002FZ-03 `root.contracts.quantscript.legacy_config_compat` single_leaf_closeout.
