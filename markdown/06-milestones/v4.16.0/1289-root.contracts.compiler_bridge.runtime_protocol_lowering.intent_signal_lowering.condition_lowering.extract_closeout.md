# v4.16.0 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.condition_lowering actual extraction complete

> Batch: BE-001XO-01
> Node: `root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.condition_lowering`
> Parent: `root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering`
> Stage: `extract_closeout`
> Movement: Rust code movement complete.

---

## Summary

`root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.condition_lowering` actual extraction is complete.

`qrpc_compiler/src/runtime_protocol_lowering/intent_signal_lowering/condition_lowering.rs` now owns runtime structured condition construction and tightly coupled comparison metadata decoding.

Parent `intent_signal_lowering.rs` preserves the parent-facing indicator/signal conversion facade, fallback condition description, and runtime intent kind mapping.

## Extracted Ownership

Moved into the child:

- `lower_runtime_intent_condition`
- `lower_runtime_spread_threshold_condition`
- `decode_runtime_comparison_op`
- `comparison_op_code`
- `RuntimeComparisonShape`
- `decode_runtime_comparison_shape`

Kept outside:

- `lower_runtime_intent_to_indicator`
- `lower_runtime_intent_to_signal_rule`
- `describe_runtime_intent_condition`
- `lower_runtime_intent_kind`
- runtime orchestration/data binding, agent/risk lowering, validation, Strategy IR lowering, tests, canonical hash, and release transition.

## Equivalence Evidence

- `cargo test -p qrpc-compiler` passed after extraction.
- Parent public and parent-facing private function signatures remain unchanged.
- Fallback raw-text behavior remains parent-owned and still triggers when child returns `None`.

## Boundary

**Real files**:
- `qrpc_compiler/src/runtime_protocol_lowering/intent_signal_lowering.rs`
- `qrpc_compiler/src/runtime_protocol_lowering/intent_signal_lowering/condition_lowering.rs`

**Markers**:
- `BE-001XO-01`
- `condition_lowering extracted: true`
- `fallback description preserved in parent`
- `intent kind mapping preserved in parent`

**Next step**:
BE-001XP-01 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.condition_lowering single_leaf_closeout

---

## Gates

- `cargo test -p qrpc-compiler`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
