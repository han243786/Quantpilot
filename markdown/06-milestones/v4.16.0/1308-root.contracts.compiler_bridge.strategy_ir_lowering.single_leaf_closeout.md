# v4.16.0 root.contracts.compiler_bridge.strategy_ir_lowering single leaf closeout

> Batch: BE-001YA-02
> Node: `root.contracts.compiler_bridge.strategy_ir_lowering`
> Stage: `single_leaf_closeout`
> Movement: No code movement — confirm stop_split and register child as closed.
> Base: BE-001YA-01 `root.contracts.compiler_bridge.strategy_ir_lowering` extract closeout.

---

## Summary

`root.contracts.compiler_bridge.strategy_ir_lowering` was extracted in BE-001YA-01 from `qrpc_compiler/src/lib.rs` into `qrpc_compiler/src/strategy_ir_lowering.rs`. This document records the formal single-leaf closeout with the full leaf split decision gate.

## Closed Child Surface

| Item | Detail |
| --- | --- |
| Physical file | `qrpc_compiler/src/strategy_ir_lowering.rs` (~563 lines) |
| Parent facade | `qrpc_compiler/src/lib.rs` — `pub fn lower_strategy_ir_to_core_ir` thin delegation |
| Module visibility | `mod strategy_ir_lowering` (private to parent; external callers use parent facade) |
| Entry function | `pub(super) fn lower_strategy_ir_to_core_ir(&StrategyIr) -> Result<CoreStrategyIr>` |
| Private helpers | 15 functions (indicator kind mapping, logic rule lowering, condition lowering, spread/custom expression lowering, validation) |
| Shared helper imports | `build_spread_spec`, `lower_rebalance_schedule`, `parse_strategy_signal_compare`, `strategy_indicator_usize_param` (all via `use super::`) |

## Equivalence Evidence

| Evidence | Result |
| --- | --- |
| `cargo test -p qrpc-compiler` | 28 passed, 0 failed, 0 warnings |
| `cargo check -p quantpilot` | pass, 0 warnings |
| Public API | `qrpc_compiler::lower_strategy_ir_to_core_ir` unchanged |
| Downstream callers | `src/graph_api.rs`, `src/compile_api.rs` — no import path change |
| Error codes | `CUSTOM001`–`CUSTOM011`, `QPSTRATSPREAD001`–`QPSTRATSPREAD004` preserved |
| Child-to-sibling links | None (both `runtime_protocol_lowering` and `strategy_ir_lowering` access shared helpers via `use super::` only) |

## Leaf Split Decision Gate

### leaf_split_base_gate

| Rule | Result | Detail |
| --- | --- | --- |
| `white_box_boundary_named` | pass | Child: `strategy_ir_lowering`. Input: `&StrategyIr`. Output: `Result<CoreStrategyIr>`. Excludes shared spread/decode helpers and parent-owned compile facades. |
| `parent_child_communication_kept` | pass | Parent delegates via single-line call. Child accesses shared helpers via `use super::{...}`. No sibling horizontal link. |
| `equivalence_baseline_freezable` | pass | 28 tests; `cargo check -p quantpilot`; public API unchanged. |

### leaf_split_positive_trigger

| Trigger | Detail |
| --- | --- |
| `public_or_handler_boundary` | `lower_strategy_ir_to_core_ir` is a public entry point via parent delegation. |
| `strategy_branch` | Contains independent lowering branches for indicator kind, logic rule condition, spread threshold, and custom expressions. |
| `independent_failure_mode` | Error codes `CUSTOM001`–`CUSTOM011` and `QPSTRATSPREAD001`–`QPSTRATSPREAD004` are localized to this child. |

### leaf_split_stop_condition

| Condition | Result |
| --- | --- |
| `micro_leaf_without_owner` | Not triggered — the 16 functions form a cohesive unit with a single public entry point. |
| `communication_cost_rises` | Not triggered — only 4 shared helper imports; further subdivision would require more cross-module imports. |
| `local_proof_missing` | Not triggered — 28 tests cover both runtime protocol and strategy IR lowering. |
| `line_count_only` | Not triggered — 563 lines with clear owner and independent failure modes. |

### leaf_split_decision_result

**`stop_split: true`**

The child is cohesive and self-contained. Further subdivision into individual indicator lowering helpers or condition lowering helpers would create micro-leaves without independent owners and increase communication cost through shared helper imports.

### next_recursive_step

Return to `root.contracts.compiler_bridge` parent residual judgment (BE-001YB-01).

## Post-Closeout Hard Boundaries

1. Do not further subdivide `strategy_ir_lowering` into per-indicator or per-condition children.
2. Do not introduce sibling horizontal links between `strategy_ir_lowering` and `runtime_protocol_lowering`.
3. Do not move shared helpers (`build_spread_spec`, `parse_strategy_signal_compare`, decode functions) into `strategy_ir_lowering`.
4. Do not make `strategy_ir_lowering` public — external callers go through parent facade.
5. Do not propose release transition for performance optimization.
6. New indicator kinds or condition shapes added to `lower_strategy_indicator_kind` or `lower_strategy_logic_condition` do not reopen this child — they extend the existing cohesive surface.

## Next Step

BE-001YB-01 `root.contracts.compiler_bridge` parent_residual_judgment.

## Gates

- `cargo test -p qrpc-compiler`: 28 passed, 0 failed, 0 warnings
- `cargo check -p quantpilot`: pass, 0 warnings
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
