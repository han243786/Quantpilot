# v4.16.0 root.contracts.runtime_support.fill_engine.event_projection_wave baseline plan

> Batch: BE-002BC-01
> Node: `root.contracts.runtime_support.fill_engine.event_projection_wave`
> Parent: `root.contracts.runtime_support.fill_engine`
> Stage: `baseline_plan`
> Movement: No Rust code movement.

---

## Scope

`root.contracts.runtime_support.fill_engine.event_projection_wave` freezes the event-builder helper boundary before actual extraction.

Physical source:

- `qrpc_runtime/src/fill_engine.rs`

Planned child file:

- `qrpc_runtime/src/fill_engine/event_projection_wave.rs`

Functions authorized for movement:

- `open_event`
- `partial_event`
- `cancel_event`
- `reject_event`
- `fill_event`

## White-Box Responsibilities

This child owns fill-engine runtime event projection only:

- event id construction for open, partial, cancel, reject, and fill events;
- event type mapping;
- trace id, source id, and timestamp propagation;
- JSON payload projection for lifecycle status, reason text, fill ids, quantities, prices, fees, and summaries.

## Parent-Owned Surface

The parent keeps:

- `MarketState`
- `FillEngine`
- `RestingOrderOpenRequest`
- `submit_plan`
- `on_market_update`
- `try_open_resting_order`
- execution status, market-state, executable-quantity, reservation/accounting, fill-report, and portfolio helpers;
- tests.

## Extraction Shape

The next movement may:

- create `qrpc_runtime/src/fill_engine/event_projection_wave.rs`;
- add `mod event_projection_wave;` in `qrpc_runtime/src/fill_engine.rs`;
- import the five event builders from the child into the parent;
- change the five moved functions from private parent functions to `pub(super)` child functions.

The next movement must not:

- change any event payload field, string, id format, timestamp, source id, or event type;
- move accounting, fill report, portfolio, executable quantity, or test code;
- introduce sibling imports;
- change public API or crate exports.

## Equivalence Gates

Required gates for the extraction:

- `cargo fmt`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime fill_engine`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-002BD-01 `root.contracts.runtime_support.fill_engine.event_projection_wave` actual_extraction.
