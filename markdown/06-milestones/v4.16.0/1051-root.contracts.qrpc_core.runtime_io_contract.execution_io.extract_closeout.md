# v4.16.0 root.contracts.qrpc_core.runtime_io_contract.execution_io extract closeout

> Batch: BE-001SH-02
> Node: `root.contracts.qrpc_core.runtime_io_contract.execution_io`
> Parent: `root.contracts.qrpc_core.runtime_io_contract`
> Stage: `extract_closeout`
> Movement: Physical Rust extraction.

---

## Summary

`execution_io` is now a private runtime IO child module.

Moved from `qrpc_core/src/runtime_io_contract.rs` into `qrpc_core/src/runtime_io_contract/execution_io.rs`:

- `SimOrder`
- `ExecutionPlan`
- `FillReport`
- `OpenOrder`
- `FillResult`

The parent still owns:

- `mod execution_io;`
- `pub use execution_io::*;`
- parent-owned `RuntimeEvent`
- portfolio, runtime output, and backtest output residual DTOs

## Equivalence Proof

| Surface | Result |
| --- | --- |
| Public facade | Preserved through `pub use execution_io::*;` in `qrpc_core/src/runtime_io_contract.rs` and existing crate-root re-export. |
| DTO fields | Unchanged for all five moved DTOs. |
| serde derives/defaults | Unchanged. |
| Parent-owned event boundary | `FillResult.events` still uses `RuntimeEvent`, imported through the parent. |
| Sibling links | None added. |
| Runtime behavior | No behavior logic moved; this is a schema-only extraction. |

## Files

| File | Role |
| --- | --- |
| `qrpc_core/src/runtime_io_contract.rs` | Runtime IO parent facade and remaining residual owner. |
| `qrpc_core/src/runtime_io_contract/execution_io.rs` | Execution IO DTO child owner. |

## Boundary Notes

`execution_io` imports primitive execution types from the qrpc-core crate facade and imports `RuntimeEvent` from the parent module. This keeps the development-time parent-child communication rule intact.

`Position`, `ExchangeExposure`, `PortfolioState`, `RuntimeEvent`, `RuntimeCycleOutput`, `SessionOutput`, and backtest output DTOs remain parent-owned until their own residual judgments.

## Next Step

BE-001SH-03 `root.contracts.qrpc_core.runtime_io_contract.execution_io` single_leaf_closeout.

## Gates

- `git diff --check`
- `cargo fmt`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
