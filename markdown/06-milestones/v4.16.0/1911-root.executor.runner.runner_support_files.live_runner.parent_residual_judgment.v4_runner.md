# v4.16.0 root.executor.runner.runner_support_files.live_runner parent residual judgment selects v4_runner

> Batch: BE-002MI-01
> Parent: `root.executor.runner.runner_support_files.live_runner`
> Stage: `parent_residual_judgment`
> Selected child: `root.executor.runner.runner_support_files.live_runner.v4_runner`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `v4_runner` as the next child under `live_runner`.

## Evidence

Closed children:

- `v3_live_runner`
- `runner_instance_dispatch`

Remaining cohesive groups:

- `V4Runner` and `V4ExecutorEvidenceEvent`;
- `RunnerPool` orchestration;
- V4 market metadata helpers;
- local test harness.

`V4Runner` is now the next cohesive runtime owner. It contains V4 simulated
runtime state, websocket event handling, evidence event projection, subscription
filtering, and status transitions for the V4 live runner path.

## Boundary

The next baseline may freeze movement for `V4Runner`, `V4ExecutorEvidenceEvent`,
and their direct impl methods only. It must not move `RunnerPool`, V4 market
metadata helper functions, or tests in the same extraction step.

## Next Step

BE-002MJ-01
`root.executor.runner.runner_support_files.live_runner.v4_runner`
baseline_plan.
