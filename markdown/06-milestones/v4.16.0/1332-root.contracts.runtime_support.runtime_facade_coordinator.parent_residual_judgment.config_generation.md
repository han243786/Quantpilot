# v4.16.0 runtime_facade_coordinator parent residual judgment: config_generation

> Batch: BE-001YO-01
> Node: `root.contracts.runtime_support.runtime_facade_coordinator`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

## Parent Status

`runtime_facade_coordinator` remains open.

Closed children:

- `contracts.runtime_support.runtime_facade_coordinator.constructor_provider_wiring`
- `contracts.runtime_support.runtime_facade_coordinator.session_cycle_orchestration`
- `contracts.runtime_support.runtime_facade_coordinator.execution_market_entrypoints`
- `contracts.runtime_support.runtime_facade_coordinator.state_config_accessors`

## Selected Child

Next child:

`root.contracts.runtime_support.runtime_facade_coordinator.config_generation`

## Selection Reason

The parent still owns a compact but cohesive configuration generation cluster:

- `RuntimeCoordinator::swap_module_config`
- `RuntimeCoordinator::apply_pending_module_configs`
- `RuntimeCoordinator::current_generation`
- `RuntimeCoordinator::generation_history`

These methods share `pending_module_configs`, deployment revision generation, config generation counters, and generation history retention. They are not provider helper implementations and should not be mixed with portfolio projection.

## Split Decision Gate

The selected child is worth extracting because:

- it has a distinct mutation/read surface for module config generation
- it owns bounded history retention and deterministic revision generation behavior
- it is a public coordinator capability rather than a private helper cluster
- moving it leaves the parent closer to a residual facade over separate child modules

## Remaining Parent Residuals

After this child, the remaining queue is:

1. `contracts.runtime_support.runtime_facade_coordinator.provider_delegation_helpers`
2. `contracts.runtime_support.runtime_facade_coordinator.portfolio_projection`
3. `contracts.runtime_support.runtime_facade_coordinator.coordinator_test_harness`

## Next Step

BE-001YP-01 `root.contracts.runtime_support.runtime_facade_coordinator.config_generation` baseline_plan.
