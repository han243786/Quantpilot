# v4.16.0 root.contracts.quantscript.evaluator_normalization baseline plan

> Batch: BE-002HN-01
> Node: `root.contracts.quantscript.evaluator_normalization`
> Parent: `root.contracts.quantscript`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Scope

Freeze the QuantScript evaluator normalization module before movement.

Current owner:

- `quantscript/src/evaluator.rs`

Planned module form:

- `quantscript/src/evaluator/mod.rs`

The next movement may convert `evaluator.rs` into an `evaluator/` directory module without changing the crate-level module name.

## Public Surface To Preserve

The public surface is:

- `normalize_script_module(module: &ScriptModule) -> Result<ScriptModule>`

Public re-export to preserve:

- `quantscript/src/lib.rs`: `pub use evaluator::normalize_script_module;`

Known callers:

- `quantscript/src/lib.rs`
- `quantscript/src/lowering/orchestrator.rs`
- `quantscript/src/lowering/binding_sources.rs` tests
- `quantscript/src/lowering/fallback.rs` tests

## Frozen Internal Families

The current file contains these candidate child families:

| Candidate | Current owner | Notes |
| --- | --- | --- |
| `normalization_orchestrator` | `normalize_script_module`, `normalize_function`, `normalize_block`, `normalize_stmt`, `normalize_expr`, `EvalContext` | Public entry and AST walk coordinator. |
| `helper_inline_execution` | `maybe_inline_function`, helper execution, `ExecOutcome`, `IfStmtParts`, `BlockSnapshot`, loop/match execution helpers, recursion/iteration constants | Executes user-defined helper functions during normalization. |
| `constant_folding` | `fold_unary`, `fold_binary`, `fold_index`, `fold_slice`, pattern/list slice helpers | Pure expression folding and list/slice mechanics. |
| `builtin_folding` | `fold_builtin_call`, member/identifier builtin folding, fetch health helpers | Builtin call folding. |
| `expr_value_helpers` | `expr_length`, numeric/bool/string/list helper readers | Shared value projection helpers. |
| `test_harness` | evaluator-local tests | Should remain local until production children are stable. |

## Next Movement

BE-002HN-02 may do only the module-directory conversion:

1. Create `quantscript/src/evaluator/mod.rs` with the current evaluator implementation.
2. Remove `quantscript/src/evaluator.rs`.
3. Keep `mod evaluator;` and `pub use evaluator::normalize_script_module;` unchanged in `quantscript/src/lib.rs`.
4. Do not split internal evaluator children in the same step.

This is intentionally a shell conversion so later recursive steps can add child modules without mixing a file move with logic partitioning.

## Forbidden Movement

The next movement must not change:

- `normalize_script_module` signature or behavior;
- helper execution semantics for `if` / `for` / `while` / `match`;
- constant folding semantics;
- builtin folding semantics;
- `quantscript/src/lowering/` behavior;
- parser, analysis diagnostics, typed resolution, static audit, test plan, or legacy config code;
- release transition behavior.

## Expected Validation

The conversion must pass:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002HN-02 `root.contracts.quantscript.evaluator_normalization` module_shell_extraction.
