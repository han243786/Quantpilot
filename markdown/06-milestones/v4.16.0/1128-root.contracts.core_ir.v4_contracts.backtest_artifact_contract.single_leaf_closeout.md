# v4.16.0 root.contracts.core_ir.v4_contracts.backtest_artifact_contract single leaf closeout stops split

> Batch: BE-001TX-03
> Node: `root.contracts.core_ir.v4_contracts.backtest_artifact_contract`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.backtest_artifact_contract` closes as a compact schema leaf.

The leaf owns five tightly coupled DTOs for one artifact family and has no impl blocks, validators, handler logic, persistence, or runtime side effects. Further split would be line-count driven and would increase parent-mediated communication across the artifact root and nested record types.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | The white-box owner is the v4 backtest artifact DTO family. |
| parent_child_communication_kept | pass | External dependencies remain parent-mediated: machine template, runtime mode, venue capability/source taxonomy, and the default helper. |
| equivalence_baseline_freezable | pass | BE-001TX-01 froze all moved fields, serde defaults, skip rules, derives, and visibility. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | The DTO family is public schema API, but no child-internal public sub-boundary exists beyond the root/nested DTO split already visible in the file. |
| state_machine_phase | no | The trajectory point references machine taxonomy but does not own state-machine validation or transitions. |
| strategy_branch | no | Runtime/backtest consumers use the DTOs, but no strategy branch logic lives in this leaf. |
| independent_failure_mode | guarded | Artifact schema drift is independently testable, but each nested record is too small to justify a separate owner. |
| reuse_pressure | guarded | The artifact family is reused by projection/runtime consumers, but the nested DTOs are reused as part of the same artifact schema. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | pass | Splitting metrics, trajectory, risk decision, and capability-source records would create tiny DTO-only files with no independent owner. |
| communication_cost_rises | pass | A finer split would require the artifact root to import all nested children and preserve the same taxonomy dependencies across more files. |
| local_proof_missing | no | `cargo test -p qrpc-core-ir` passed after extraction. |
| line_count_only | pass | Further subdivision would be based on file size aesthetics rather than behavior or ownership. |

leaf_split_decision_result

`stop_split: true`

This child is closed. The recursion returns to `root.contracts.core_ir.v4_contracts` and selects `machine_contract` for the next parent residual judgment.

## Boundary

**Real files**:

- `qrpc_core_ir/src/v4/backtest_artifact_contract.rs`

**Markers**:

- `BE-001TX-03`

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-001TY-01 `root.contracts.core_ir.v4_contracts` parent_residual_judgment selects `machine_contract`.
