# v4.16.0 root.executor.runner.runner_support_files.live_runner.v4_runner baseline plan

> Batch: BE-002MJ-01
> Node: `root.executor.runner.runner_support_files.live_runner.v4_runner`
> Stage: `baseline_plan`
> governance_next_authority: active

---

## Frozen Boundary

The next movement may extract only:

- `V4Runner`
- `V4ExecutorEvidenceEvent`
- `impl V4Runner`

Target file:

`src-executor/live_runner/v4_runner.rs`

The parent `src-executor/live_runner.rs` must keep the public module wiring and
may re-export `V4Runner` plus `V4ExecutorEvidenceEvent`.

## Explicit Non-Movement

Do not move in BE-002MJ-02:

- `RunnerPool`
- `executor_v4_market_matrix`
- `resolve_v4_runner_venue_id`
- `resolve_v4_runner_default_symbol`
- `graph_metadata_string`
- local `#[cfg(test)]` test harness

The V4 market metadata helpers remain parent-owned for this step. If they still
justify ownership after the V4 runner extraction, they will be considered as a
separate child.

## Compatibility Notes

- `V4Runner::DEFAULT_REALTIME_PAPER_VENUE_ID` is used by the parent helper
  `resolve_v4_runner_venue_id`; it must remain reachable by the parent after
  extraction.
- `V4Runner::from_strategy` continues to call the parent-owned helper functions
  through the parent module boundary.
- `RunnerInstance` must continue to construct and dispatch `V4Runner` without
  changing public behavior.

## Required Gates

| Gate | Required |
| --- | --- |
| `cargo fmt --all --check` | Yes |
| `cargo check -p quantpilot --bin executor` | Yes |
| `cargo test -p quantpilot --bin executor live_runner` | Yes |
| `git diff --check` | Yes |

## Next Step

BE-002MJ-02
`root.executor.runner.runner_support_files.live_runner.v4_runner`
actual_extraction.
