# v4.16.0 runtime_facade_coordinator parent residual judgment selects constructor_provider_wiring

> Batch: BE-001YG-01
> Node: `root.contracts.runtime_support.runtime_facade_coordinator`
> Parent: `root.contracts.runtime_support`
> Stage: `parent_residual_judgment`
> Selected child: `root.contracts.runtime_support.runtime_facade_coordinator.constructor_provider_wiring`
> Movement: No code movement.

## Parent State

BE-001YF-03 kept `runtime_facade_coordinator` open with `continue_split: true`.

The first child selected from its internal queue is `constructor_provider_wiring`.

## Selected Child Boundary

`constructor_provider_wiring` owns the public constructors and provider injection surface for `RuntimeCoordinator`:

- `RuntimeCoordinator::from_core_ir`
- `RuntimeCoordinator::new`
- `RuntimeCoordinator::with_modules_from_core_ir`
- `RuntimeCoordinator::with_modules`
- `RuntimeCoordinator::with_data_module_from_core_ir`
- `RuntimeCoordinator::with_data_module`
- `RuntimeCoordinator::with_intent_module_from_core_ir`
- `RuntimeCoordinator::with_intent_module`
- `RuntimeCoordinator::with_agent_module_from_core_ir`
- `RuntimeCoordinator::with_agent_module`
- `RuntimeCoordinator::with_risk_checker_from_core_ir`
- `RuntimeCoordinator::with_risk_checker`
- `RuntimeCoordinator::with_execution_module_from_core_ir`
- `RuntimeCoordinator::with_execution_module`
- `RuntimeCoordinator::with_module_providers_from_core_ir`
- `RuntimeCoordinator::with_module_providers`

It also owns initial runtime-state/config/merge coordinator construction performed by `with_module_providers_from_core_ir`.

## Exclusions

The selected child does not own:

- `RuntimeCoordinator` struct definition
- session/cycle orchestration
- execution submission or market data update entry points
- state/config accessors
- module config swap/apply behavior
- portfolio update projection
- provider delegation internals
- coordinator tests, except constructor/provider override tests if the extraction needs a local test harness

## Split Decision

`continue_split: true`.

Reasons:

- The constructor/provider API group is public and cohesive.
- It initializes dependencies but does not run sessions.
- It can be moved behind parent wrapper functions or a private builder child without changing crate-root exports.
- Separating it reduces parent weight before touching session orchestration.

No release-transition optimization is proposed.

## Next Step

BE-001YH-01 `root.contracts.runtime_support.runtime_facade_coordinator.constructor_provider_wiring` baseline_plan.

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`
