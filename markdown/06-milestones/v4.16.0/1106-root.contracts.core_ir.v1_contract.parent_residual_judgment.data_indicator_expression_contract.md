# v4.16.0 root.contracts.core_ir.v1_contract parent residual judgment selects data_indicator_expression_contract

> Batch: BE-001TL-01
> Node: `root.contracts.core_ir.v1_contract`
> Parent: `root.contracts.core_ir`
> Selected child: `root.contracts.core_ir.v1_contract.data_indicator_expression_contract`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v1_contract` returns to its child queue after `root_graph_contract` closeout.

Decision:

`next_child: root.contracts.core_ir.v1_contract.data_indicator_expression_contract`

## Closed v1 Children

| Child | Result |
| --- | --- |
| `contracts.core_ir.v1_contract.root_graph_contract` | Closed with `stop_split: true`. |

## Open v1 Residuals

| Residual | Status |
| --- | --- |
| `contracts.core_ir.v1_contract.data_indicator_expression_contract` | Selected next. Owns data binding, indicator, series/custom/scalar expression DTOs, helper builders, and expression taxonomy. |
| `contracts.core_ir.v1_contract.policy_execution_contract` | Queued. |
| `contracts.core_ir.v1_contract.test_fixture` | Queued. |

## Selection Rationale

`data_indicator_expression_contract` is selected because it is the next contiguous v1 schema family below the root graph:

- `DataBinding` and `DataBindingKind`;
- `IndicatorNode` and `CoreIndicatorKind`;
- custom expression DTOs and arithmetic enums;
- `SeriesExpr`, series field/aggregation/alignment/spread DTOs;
- scalar expression DTOs and comparison enum;
- expression helper builders.

`SignalRule`, agent/risk policy DTOs, execution DTOs, and the local test stay outside this child for the later policy/execution and test fixture steps.

## White Box Boundary

| Item | Owner | Input | Output | Side effects |
| --- | --- | --- | --- | --- |
| Data binding DTOs | `data_indicator_expression_contract` | data IDs/source hints | v1 data binding schema | none |
| Indicator DTOs | `data_indicator_expression_contract` | indicator ID/kind/inputs/params | v1 indicator schema | none |
| Custom/series/scalar expressions | `data_indicator_expression_contract` | data fields, operators, thresholds, nested expressions | expression schema | none |
| Helper builders | `data_indicator_expression_contract` | data IDs, periods, ops, thresholds | expression helper outputs | none |

## Same-Parent Parallel Judgment

`same_parent_parallel_children` remains inactive.

Reason: expression helper ownership is public and referenced by tests; serial extraction protects helper compatibility before policy/execution DTOs move.

## Hard Boundaries

The next `data_indicator_expression_contract` baseline must not:

- edit Rust source code;
- move root graph, signal/agent/risk/execution, or test fixture surfaces;
- change struct/enum fields, serde shapes, helper behavior, public exports, or tests;
- edit `qrpc_core_ir/src/v4.rs`;
- change qrpc-core, compiler, runtime, QuantScript, backend, executor, frontend, physical `plugins/*`, or release transition.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | Selected child owns v1 data, indicator, expression DTOs and helper builders. |
| parent_child_communication_kept | pass | Future child can be mediated through the v1 parent facade and crate root. |
| equivalence_baseline_freezable | pass | DTO fields, serde shapes, enum variants, and helper behavior can be frozen. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | pass | These DTOs and helpers are public qrpc-core-ir API. |
| state_machine_phase | no | No mutable runtime state machine ownership. |
| strategy_branch | pass | Expressions model strategy signal inputs. |
| independent_failure_mode | pass | Expression/helper drift is separable from root graph and policy/execution drift. |
| reuse_pressure | pass | Compiler and tests consume expression helpers and DTOs. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The child has a substantial schema/helper owner. |
| communication_cost_rises | no | v1 parent re-export can preserve consumers. |
| local_proof_missing | no | qrpc-core-ir tests cover the move. |
| line_count_only | no | Split is by expression/data ownership. |

leaf_split_decision_result

`continue_baseline: true`; `data_indicator_expression_contract` is selected as the next v1 child.

## Next Step

BE-001TM-01 `root.contracts.core_ir.v1_contract.data_indicator_expression_contract` baseline_plan.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
