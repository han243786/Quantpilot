# v4.16.0 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder.event_catalog_builder equivalence baseline and extraction plan

> Batch: BE-001WR-01
> Node: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder.event_catalog_builder`
> Parent: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder`
> Stage: `baseline_plan`
> Movement: No code movement. This document freezes the event-catalog builder boundary before extraction.

---

## Summary

root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder.event_catalog_builder equivalence baseline and extraction plan

This child owns the v4 compatibility bridge event catalog construction phase.

## Frozen White-Box Surface

BE-001WR-02 may move only `build_core_ir_compat_event_catalog` into `qrpc_core_ir/src/v4/core_ir_compat_bridge/compat_graph_builder/event_catalog_builder.rs`.

## Forbidden Movement

BE-001WR-02 must not move:

- `build_core_ir_compat_machine_graph`
- compatibility machine builders
- `compat_machine`
- graph edge construction
- risk-plane binding
- metadata helpers
- memory-count helper
- Core IR edge-label helper
- string-array helper
- compat id sanitization

## Allowed Extraction

BE-001WR-02 may create `qrpc_core_ir/src/v4/core_ir_compat_bridge/compat_graph_builder/event_catalog_builder.rs`, add a private nested module declaration in `compat_graph_builder.rs`, and call `build_core_ir_compat_event_catalog` through the graph-builder parent.

## Equivalence Baseline

The extraction is equivalent only if these gates pass:

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `cargo check -p quantpilot`

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/core_ir_compat_bridge/compat_graph_builder.rs`

**Markers**:
- `BE-001WR-01`

**Next step**:
BE-001WR-02 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder.event_catalog_builder extract_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
