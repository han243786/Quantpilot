# v4.16.0 root.executor.runner parent residual judgment selects provider_order_support

> Batch: BE-002LO-01
> Node: `root.executor.runner`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select discovered residual child:

`root.executor.runner.provider_order_support`

## Parent Residual Scan

Closed runner children:

- `provider_order_routes`
- `strategy_lifecycle_routes`
- `market_stream_routes`
- `v4_deploy_support`

Remaining runner residuals:

- `provider_order_support`
- `runner_support_files`

## Why This Child

After route handler extraction, `src-executor/main.rs` still owns OKX demo
provider DTOs, request validation, credential loading, provider error shaping,
audit payload helpers, and provider response projection. These helpers are a
cohesive support leaf for the already closed `provider_order_routes` child and
should be extracted before closing the runner parent.

## Proposed Ownership

| Field | Value |
| --- | --- |
| Candidate child | `provider_order_support` |
| Planned file | `src-executor/provider_order_support.rs` |
| First movement | OKX demo provider DTOs, validation helpers, credential loading helpers, provider error helpers, audit payload helpers, provider response helper |
| Parent retained for first movement | route registration, process startup, `append_audit`, route modules, runner support files |

## Exclusions

- Do not reopen `provider_order_routes`; it remains the route handler leaf.
- Do not move strategy lifecycle, market stream, or v4 deploy support leaves.
- Do not move runner setup/support files.
- Do not introduce release-transition links.

## Next Step

BE-002LP-01 `root.executor.runner.provider_order_support` baseline_plan.
