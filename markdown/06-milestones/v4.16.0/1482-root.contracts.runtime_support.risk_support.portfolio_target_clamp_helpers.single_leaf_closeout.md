# v4.16.0 root.contracts.runtime_support.risk_support.portfolio_target_clamp_helpers single leaf closeout

> Batch: BE-002CB-02
> Node: `root.contracts.runtime_support.risk_support.portfolio_target_clamp_helpers`
> Parent: `root.contracts.runtime_support.risk_support`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Decision

`root.contracts.runtime_support.risk_support.portfolio_target_clamp_helpers` is closed with `stop_split: true`.

## Leaf Granularity Result

| Metric | Value |
| --- | --- |
| LOC | 301 |
| Functions | 9 |
| Public surface | 1 `pub(super)` helper |
| Branches | 46 |
| Domains | 2 |
| Score | 38 |
| Decision | STOP |
| Governance mode | `stop_split` |

## Rationale

The child is a cohesive portfolio-target clamp helper group. Splitting current-weight refresh, new-position trimming, single-weight caps, turnover scaling, buy headroom, exchange headroom, minimum trade removal, and portfolio net exposure scaling would create tightly-coupled tiny leaves with more governance cost and no clearer parent boundary.

`clamp_portfolio_target_limits` remains the single parent-facing entrypoint.

## Preserved Boundaries

- provider/evaluate surface remains parent-owned;
- closed projection, direction-constraint, and action-clamp children remain outside this child;
- exposure math helpers remain parent-owned;
- tests remain outside this child;
- no sibling shortcut exists.

## Next Step

BE-002CC-01 `root.contracts.runtime_support.risk_support` parent_residual_judgment selects `exposure_math_helpers`.
