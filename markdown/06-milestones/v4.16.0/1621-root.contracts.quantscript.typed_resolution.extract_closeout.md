# v4.16.0 root.contracts.quantscript.typed_resolution structural extraction closeout

> Batch: BE-002GB-02
> Node: `root.contracts.quantscript.typed_resolution`
> Parent: `root.contracts.quantscript`
> Stage: `actual_extraction_or_structural_closeout`
> Movement: Structural Rust module movement.
> governance_next_authority: active

---

## Movement

`typed_resolution` already had a physical owner, so this step performed a structural extraction instead of semantic code movement:

- moved `quantscript/src/resolve.rs`
- to `quantscript/src/resolve/mod.rs`

The crate root still declares `mod resolve;`, so Rust module resolution preserves the public and crate-internal `resolve` module path. No function body, public type, callable classifier, diagnostic rule, test assertion, or downstream import was intentionally changed.

## Boundary Preserved

Preserved surfaces:

- `quantscript/src/lib.rs` public re-exports from `resolve`;
- `crate::resolve::*` imports used by analysis and lowering modules;
- `resolve::tests::*` test module path;
- typed-HIR behavior for parser-to-resolver handoff;
- downstream analysis/lowering use of `ResolveResult`, `ResolvedCallable`, `ResolvedExprSemantic`, and helper classification.

Excluded surfaces:

- parser, AST, type, and HIR DTO ownership under `syntax_ast_surface`;
- deprecated config compatibility under `legacy_config_compat`;
- analysis diagnostics, evaluator normalization, runtime lowering, static audit, test-plan, authoring samples, backend, runtime support, compiler bridge, executor, frontend, plugin metadata, and release transition.

## Evidence

- `cargo fmt --check` PASS
- `cargo test -p quantscript` PASS, 88 tests
- `cargo check -p quantpilot` PASS

## Why This Is Not A Terminal Closeout

The leaf-granularity evaluator returned `PRECISION / CONTINUE` for the typed-resolution owner:

- effective LOC: 2543
- functions: 93
- public surface: 46
- domains: 7
- score: 61
- recommendation: precision single-leaf governance, do not batch away high-risk evidence

This means the structural move is only a setup step. The next single-leaf closeout must record `stop_split: false` unless stronger evidence proves that the oversized resolver should remain terminal.

## Next Step

BE-002GB-03 `root.contracts.quantscript.typed_resolution` single_leaf_closeout.
