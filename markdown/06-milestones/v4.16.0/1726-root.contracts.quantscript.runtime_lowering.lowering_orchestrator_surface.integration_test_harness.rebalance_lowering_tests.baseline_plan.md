# v4.16.0 root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.rebalance_lowering_tests baseline plan

> Batch: BE-002IG-01
> Node: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.rebalance_lowering_tests`
> Parent: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Baseline Decision

`rebalance_lowering_tests` is frozen as a precision test-only child for extraction.

The next movement may create a child test module under the existing lowering orchestrator test harness and move only the portfolio rebalance helper lowering, allocation variant, rejection, and schedule projection tests from `quantscript/src/lowering/orchestrator/integration_test_harness.rs`.

## Frozen Input / Output

| Field | Boundary |
| --- | --- |
| Input | Test-only QuantScript fixtures with `symbols`, `rebalance`, `equal_weight`, `fixed_weights`, `rank_weight`, and `score_weight` helpers |
| Output | Existing assertions over runtime config agent params, rebalance symbols, allocation kind, target weights, rank/score metadata, schedule mapping, Core IR agent policy, and unsupported rebalance diagnostics |
| Parent caller | `integration_test_harness` test module only |
| Error surface | Existing unsupported frequency and unsupported rank method rejection assertions remain unchanged |
| Gate | `cargo test -p quantscript` |

## Symbols Approved For Movement

Move only these existing tests:

- `lowers_equal_weight_rebalance_helper_into_portfolio_rebalance_agent`
- `lowers_fixed_weights_rebalance_helper_into_portfolio_rebalance_agent`
- `lowers_rank_weight_rebalance_helper_into_portfolio_rebalance_agent`
- `lowers_score_weight_rebalance_helper_into_portfolio_rebalance_agent`
- `rejects_rebalance_helper_with_unsupported_frequency`
- `rejects_rank_weight_with_unsupported_method`
- `lowers_weekly_rebalance_schedule_into_agent_schedule`

Move any imports needed only by those tests into the child if parent imports become unused. No production Rust helper moves are approved.

## Symbols Explicitly Not Approved For Movement

- basic MA / RSI / alias smoke tests
- runtime coordinator smoke
- user-defined helper function smoke
- semantic missing-helper rejection test
- exchange-only data-source decoding test
- `manual_formula_tests` child
- `spread_lowering_tests` child
- production lowering code, production imports, and parent test module declaration

## Parent Mediation Rule

After extraction:

- `integration_test_harness.rs` declares the child test module;
- the child uses parent-visible or direct test imports only;
- no production sibling module is called from the child;
- rebalance test names and assertions remain equivalent.

## Split Decision Gate

| Gate | Result | Evidence |
| --- | --- | --- |
| `micro_leaf_without_owner` | PASS | Seven tests share one portfolio rebalance lowering and schedule projection surface. |
| `communication_cost_rises` | PASS | Movement is test-only and remains under the parent test harness. |
| `local_proof_missing` | PASS | `cargo test -p quantscript` directly executes the moved tests. |
| `line_count_only` | PASS | The split follows rebalance scenario ownership rather than line count. |

## Validation

The actual extraction must pass:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8, full-feature-tree, and matrix governance checks

## Next Step

BE-002IG-02 actual_extraction.
