# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline.realtime_sandbox extract closeout

> Batch: BE-002CY-01
> Node: `root.contracts.runtime_support.sandbox_replay_timeline.realtime_sandbox`
> Parent: `root.contracts.runtime_support.sandbox_replay_timeline`
> Stage: `actual_extraction`
> Movement: Rust code movement completed.

---

## Extraction Result

`RealTimeSandbox` has been moved from `qrpc_runtime/src/sandbox/mod.rs` into:

- `qrpc_runtime/src/sandbox/realtime_sandbox.rs`

The sandbox parent now declares `mod realtime_sandbox;` and re-exports:

- `pub use self::realtime_sandbox::RealTimeSandbox;`

## Moved Surface

- `RealTimeSandbox`
- `RealTimeSandbox::new`
- `RealTimeSandbox::with_test_mode`
- `RealTimeSandbox::from_core_ir`
- `RealTimeSandbox::from_compiled`
- `RealTimeSandbox::test_mode`
- `impl Sandbox for RealTimeSandbox`
- `RealTimeSandbox::set_risk_monitor`
- `RealTimeSandbox::is_risk_stopped`

## Preserved Parent Surface

The following remain parent-owned:

- `FastBacktestSandbox`
- `compute_benchmark_equity`
- `snapshot_from`
- `ensure_running`
- `trace_id`
- sandbox tests

## Equivalence Notes

No public API has been removed. Existing callers still access `RealTimeSandbox` through the sandbox parent re-export. The realtime child continues to use parent-owned helpers through the sandbox parent and does not directly depend on `FastBacktestSandbox`.

## Gates

- `cargo fmt --check` PASS
- `cargo check -p qrpc-runtime` PASS
- `cargo test -p qrpc-runtime sandbox -- --nocapture` PASS: 21 passed, 2 ignored

## Next Step

BE-002CY-02 `root.contracts.runtime_support.sandbox_replay_timeline.realtime_sandbox` single_leaf_closeout.
