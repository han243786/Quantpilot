# v4.16.0 root.contracts.runtime_support.agent_module.portfolio_rebalance single leaf closeout

> Batch: BE-001ZZ-03
> Node: `root.contracts.runtime_support.agent_module.portfolio_rebalance`
> Parent: `root.contracts.runtime_support.agent_module`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Decision

`stop_split: true`

`root.contracts.runtime_support.agent_module.portfolio_rebalance` is closed as a terminal child.

## Evidence

Current owner file:

- `qrpc_runtime/src/agent_module/portfolio_rebalance.rs`

Terminal leaf control:

```text
Leaf: root.contracts.runtime_support.agent_module.portfolio_rebalance
Decision: STOP
Score: 34
Metrics: loc=314, functions=3, public_surface=2, branches=33, domains=4
Terminal leaf control: size_bucket=terminal_target_150_600, governance_mode=stop_split
```

## Why No Deeper Split

No deeper child is opened because:

- the child is within the terminal target size range;
- the three functions form one cohesive rebalance policy owner;
- splitting equal/fixed/rank/score allocation modes would create algorithm micro-leaves rather than stronger module boundaries;
- shared scoring/current-weight helpers remain parent-mediated for later children;
- rebalance trading semantics make unnecessary movement costly.

## Equivalence Evidence

The extraction batch already passed:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`: 227 passed, 2 ignored
- `cargo check -p quantpilot`
- `git diff --check`
- `tools/check-utf8.ps1`
- `tools/check-full-feature-tree.ps1`
- `tools/check-matrix-governance.ps1`

## Next Step

BE-002AA-01 `root.contracts.runtime_support.agent_module` parent_residual_judgment selects `cross_venue_arbitrage`.
