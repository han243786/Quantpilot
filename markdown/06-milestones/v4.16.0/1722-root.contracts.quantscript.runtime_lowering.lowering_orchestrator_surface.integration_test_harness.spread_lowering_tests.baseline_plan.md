# v4.16.0 root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.spread_lowering_tests baseline plan

> Batch: BE-002IE-01
> Node: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.spread_lowering_tests`
> Parent: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Baseline Decision

`spread_lowering_tests` is frozen as a precision test-only child for extraction.

The next movement may create a child test module under the existing lowering orchestrator test harness and move only the spread admission/rejection and Core IR spread spec tests from `quantscript/src/lowering/orchestrator/integration_test_harness.rs`.

## Frozen Input / Output

| Field | Boundary |
| --- | --- |
| Input | Test-only QuantScript fixtures with Binance/OKX sources, spread helpers, field/resample/align helpers, and non-admitted cross-source formulas |
| Output | Existing assertions over `IntentKind::QuoteObserve`, spread params, `qrpc_core_ir::SpreadValueKind`, align/resample fields, and `QPQSLOW001` rejection diagnostics |
| Parent caller | `integration_test_harness` test module only |
| Error surface | Existing `QPQSLOW001` rejection assertions remain unchanged |
| Gate | `cargo test -p quantscript` |

## Symbols Approved For Movement

Move only these existing tests:

- `rejects_non_admitted_cross_source_spread_formula_for_formal_lowering`
- `rejects_non_admitted_asymmetric_window_spread_formula_for_formal_lowering`
- `lowers_admitted_explicit_spread_helper_into_quote_observe`
- `rejects_non_admitted_helper_annotated_formula_spread`

Move any imports needed only by those tests into the child if parent imports become unused. No production Rust helper moves are approved.

## Symbols Explicitly Not Approved For Movement

- basic MA / RSI / alias smoke tests
- runtime coordinator smoke
- user-defined helper function smoke
- semantic missing-helper rejection test
- exchange-only data-source decoding test
- rebalance lowering/rejection tests
- `manual_formula_tests` child
- production lowering code, production imports, and parent test module declaration

## Parent Mediation Rule

After extraction:

- `integration_test_harness.rs` declares the child test module;
- the child uses parent-visible or direct test imports only;
- no production sibling module is called from the child;
- spread test names and assertions remain equivalent.

## Split Decision Gate

| Gate | Result | Evidence |
| --- | --- | --- |
| `micro_leaf_without_owner` | PASS | Four tests share one spread admission/rejection and Core IR projection surface. |
| `communication_cost_rises` | PASS | Movement is test-only and remains under the parent test harness. |
| `local_proof_missing` | PASS | `cargo test -p quantscript` directly executes the moved tests. |
| `line_count_only` | PASS | The split follows spread scenario ownership rather than line count. |

## Validation

The actual extraction must pass:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8, full-feature-tree, and matrix governance checks

## Next Step

BE-002IE-02 actual_extraction.
