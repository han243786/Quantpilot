# v4.16.0 root.contracts.quantscript.typed_resolution.public_type_surface extract closeout

> Batch: BE-002GD-02
> Node: `root.contracts.quantscript.typed_resolution.public_type_surface`
> Parent: `root.contracts.quantscript.typed_resolution`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> governance_next_authority: active

---

## Movement

Created:

- `quantscript/src/resolve/public_type_surface.rs`

Updated:

- `quantscript/src/resolve/mod.rs`

Moved only the public typed-resolution DTO and enum surface plus `ResolveResult::has_errors` into the child module. The parent now declares `mod public_type_surface;` and re-exports the same public names with `pub use public_type_surface::{...};`.

## Preserved Boundary

Preserved:

- crate-root `pub use resolve::{...}` compatibility in `quantscript/src/lib.rs`;
- downstream `crate::resolve::*` imports in analysis and lowering modules;
- resolver module path `resolve::tests::*`;
- resolver behavior, callable classification, semantic inference, callable registry, type inference, and local tests.

Excluded from movement:

- `lower_script_to_typed_hir`
- `expr_semantic_key`
- public callable classifier functions
- `FunctionSignature`, `CallableTarget`, `BindingInfo`, `Resolver`
- tests, analysis, lowering, evaluator, v4 audit, test plan, backend, runtime support, compiler bridge, executor, frontend, plugin metadata, release transition.

## Evidence

- `cargo fmt --check` PASS
- `cargo test -p quantscript` PASS, 88 tests
- `cargo check -p quantpilot` PASS

## Next Step

BE-002GD-03 `root.contracts.quantscript.typed_resolution.public_type_surface` single_leaf_closeout.
