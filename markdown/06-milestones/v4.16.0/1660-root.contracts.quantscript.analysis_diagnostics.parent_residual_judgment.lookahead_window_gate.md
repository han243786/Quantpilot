# v4.16.0 root.contracts.quantscript.analysis_diagnostics parent residual judgment: lookahead_window_gate

> Batch: BE-002GV-01
> Node: `root.contracts.quantscript.analysis_diagnostics`
> Selected child: `root.contracts.quantscript.analysis_diagnostics.lookahead_window_gate`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `root.contracts.quantscript.analysis_diagnostics.lookahead_window_gate` for the next recursive child.

## Parent Residual Scan

Current parent:

- `quantscript/src/analysis_diagnostics/mod.rs`
- `quantscript/src/analysis_diagnostics/analysis.rs`
- `quantscript/src/analysis_diagnostics/diagnostics.rs`

Closed child:

- `root.contracts.quantscript.analysis_diagnostics.unsupported_construct_gate`

Remaining major diagnostic families in `analysis.rs`:

| Candidate | Boundary | Decision |
| --- | --- | --- |
| `lookahead_window_gate` | QS0401/QS0402/QS0403 negative index, trailing-window, and centered-window lookahead diagnostics. | Select |
| `warmup_fetch_gate` | required warmup inference, fetch lookback collection, and insufficient lookback diagnostics. | Queue |
| `strategy_presence_gate` | strategy fetch/emit existence diagnostics. | Queue |
| `fetch_lookback_warning_gate` | `lookback < 1` warning. | Queue |
| `indirect_recursion_gate` | helper call graph and indirect recursion diagnostics. | Queue |
| `index_bounds_gate` | fetch lookback map and array/index bounds warning. | Queue |
| `dead_code_emit_gate` | constant-false emit warning. | Queue |
| `symbol_whitelist_gate` | known symbol whitelist warning. | Queue |

## Selected Boundary

The next baseline may freeze only:

- `collect_series_index_diagnostics`
- `collect_series_index_from_stmts`
- `collect_series_index_from_expr`
- `collect_centered_window_diagnostics`
- `collect_centered_window_from_stmts`
- `collect_centered_window_from_expr`
- `centered_window_uses_future_bars`
- `arg_bool_named`
- `expr_integer`

Expected child file:

- `quantscript/src/analysis_diagnostics/lookahead_window_gate.rs`

## Why This Child

`lookahead_window_gate` is the next best child because:

- it is adjacent near the top of `analysis.rs`;
- it owns one coherent diagnostic meaning: prevent future-bar lookahead in series indexing and rolling-window calls;
- it has self-contained helper needs (`arg_bool_named`, `expr_integer`);
- it does not need `fetch_lookback`, `arg_number_named`, warmup inference, symbol whitelist, or data-source map helpers;
- extracting it reduces repeated AST traversal code from the parent without crossing public API, persistence, route, schema, runtime, or release-transition boundaries.

## Exclusions

Do not move in the next baseline:

- `infer_required_warmup_bars` or `required_warmup_for_semantic`;
- `collect_warmup_diagnostics`, fetch lookback collection, or `fetch_lookback`;
- `arg_number_named`;
- strategy fetch/emit presence checks;
- fetch lookback warnings;
- indirect recursion checks;
- data source map and index bounds checks;
- dead-code emit checks;
- symbol whitelist checks;
- diagnostic DTO/span types;
- evaluator normalization, runtime lowering, v4 static audit, tests, authoring samples, backend, executor, frontend, plugin metadata, or release transition.

## Next Step

BE-002GW-01 `root.contracts.quantscript.analysis_diagnostics.lookahead_window_gate` baseline_plan.
