# v4.16.0 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering actual extraction complete

> Batch: BE-001XK-01
> Node: `root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering`
> Parent: `root.contracts.compiler_bridge.runtime_protocol_lowering`
> Stage: `extract_closeout`
> Movement: Rust code movement complete.

---

## Summary

`root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering` actual extraction is complete.

`qrpc_compiler/src/runtime_protocol_lowering/intent_signal_lowering.rs` now owns runtime intent indicator/signal conversion, structured condition lowering, comparison metadata decoding, spread-threshold condition lowering, fallback raw-text descriptions, and runtime intent kind mapping.

Parent `qrpc_compiler/src/runtime_protocol_lowering.rs` keeps runtime protocol orchestration, data binding, agent policy lowering, risk policy lowering, and DAG validation. Parent calls the child through explicit private module functions.

## Extracted Ownership

Moved into the child:

- `lower_runtime_intent_to_indicator`
- `lower_runtime_intent_to_signal_rule`
- `lower_runtime_intent_condition`
- `describe_runtime_intent_condition`
- `lower_runtime_spread_threshold_condition`
- `decode_runtime_comparison_op`
- `comparison_op_code`
- `RuntimeComparisonShape`
- `decode_runtime_comparison_shape`
- `lower_runtime_intent_kind`

Kept outside:

- runtime protocol orchestration and data binding;
- runtime agent and risk policy projection;
- runtime spread spec helper and shared spread/custom helper pocket;
- public compile/lower facades and canonical hash assembly;
- Strategy IR lowering, validation child, tests, and release transition.

## Equivalence Evidence

- `cargo test -p qrpc-compiler` passed with 28 tests after extraction.
- `cargo check -p quantpilot` passed after extraction.
- Parent public signatures remain unchanged.
- No sibling link or release-transition shortcut was introduced.

## Boundary

**Real files**:
- `qrpc_compiler/src/runtime_protocol_lowering.rs`
- `qrpc_compiler/src/runtime_protocol_lowering/intent_signal_lowering.rs`

**Markers**:
- `BE-001XK-01`
- `intent_signal_lowering extracted: true`
- `parent orchestration preserved`
- `agent risk remains queued`

**Next step**:
BE-001XL-01 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering single_leaf_closeout

---

## Gates

- `cargo test -p qrpc-compiler`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
