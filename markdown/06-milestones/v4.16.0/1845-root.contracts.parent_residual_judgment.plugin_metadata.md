# v4.16.0 root.contracts parent residual judgment selects plugin_metadata

> Batch: BE-002KX-01
> Node: `root.contracts`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select next child:

`root.contracts.plugin_metadata`

## Parent Residual Scan

Closed contracts children:

- `contracts.api_surface`
- `contracts.qrpc_core`
- `contracts.core_ir`
- `contracts.compiler_bridge`
- `contracts.runtime_support`
- `contracts.quantscript`

Remaining contracts-owned residual:

- `contracts.plugin_metadata`

The separate `executor` residual remains queued outside `root.contracts`.

## Why This Child

`plugin_metadata` owns physical plugin metadata and registry placeholder
directories under `plugins/*`. The Rust plugin manifest contract and registry
types are already closed under `contracts.qrpc_core.plugin_contract`; this child
only records the physical metadata surface.

## Proposed Ownership

| Field | Value |
| --- | --- |
| Candidate child | `plugin_metadata` |
| Primary paths | `plugins/builtin/data/kline/plugin.json`, `plugins/cache/.gitkeep`, `plugins/disabled/.gitkeep`, `plugins/installed/.gitkeep` |
| Owned surface | physical plugin manifest metadata and registry placeholders |
| Code movement | none |

## Exclusions

- Do not alter `qrpc_core/src/plugin*` Rust contracts.
- Do not alter runtime plugin execution behavior.
- Do not alter executor state, backend behavior, or frontend callers.
- Do not introduce release-transition links.

## Next Step

BE-002KY-01 `root.contracts.plugin_metadata` single_leaf_closeout.
