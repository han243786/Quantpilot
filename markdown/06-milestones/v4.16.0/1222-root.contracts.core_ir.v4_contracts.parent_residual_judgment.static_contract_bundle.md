# v4.16.0 root.contracts.core_ir.v4_contracts parent residual judgment selects static_contract_bundle

> Batch: BE-001VV-01
> Node: `root.contracts.core_ir.v4_contracts`
> Parent: `root.contracts.core_ir`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts` selects
`root.contracts.core_ir.v4_contracts.static_contract_bundle` as the next child.

The selected child owns the v4 static contract bundle aggregation surface currently in `qrpc_core_ir/src/v4.rs`:

- `V4StaticContractBundle`
- `impl Default for V4StaticContractBundle`
- `V4StaticContractBundle::validate_static_contract`
- `default_static_contract_bundle_version`

The selected child does not own compile-time capability request/report DTOs, capability report entries, capability report validators, capability report builders, compatibility bridge DTOs, bridge builders, bridge validators, tests, sample builders, or already extracted sibling contract families.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `static_contract_bundle` is a named white-box child for static bundle aggregation and cross-contract validation. |
| parent_child_communication_kept | guarded | The child depends on already extracted sibling contracts through the parent facade and remains re-exported by the parent. |
| equivalence_baseline_freezable | pass | Bundle fields, defaults, required graph/matrix checks, and delegated validation order are finite. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | `V4StaticContractBundle` remains public through `qrpc_core_ir::v4::*`. |
| state_machine_phase | no | This is static contract aggregation, not state-machine topology. |
| strategy_branch | no | This is contract governance, not strategy behavior. |
| independent_failure_mode | pass | Static bundle validation failure is independent from compile-time capability report construction and core-ir bridge compatibility. |
| reuse_pressure | pass | Capability report builders and tests reuse the bundle surface. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The child owns a public aggregate DTO plus default and validation behavior. |
| communication_cost_rises | no | Keeping bundle aggregation together avoids scattering cross-contract validation. |
| local_proof_missing | no | Existing qrpc-core-ir tests cover complete static bundle acceptance and capability report flows. |
| line_count_only | no | Selection is based on aggregation ownership and failure-mode separation. |

leaf_split_decision_result

`continue_split: true`

Selected next child:

`root.contracts.core_ir.v4_contracts.static_contract_bundle`

next_recursive_step

BE-001VW-01 root.contracts.core_ir.v4_contracts.static_contract_bundle baseline_plan
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs`

**Markers**:
- `BE-001VV-01`

**Next step**:
BE-001VW-01 root.contracts.core_ir.v4_contracts.static_contract_bundle baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
