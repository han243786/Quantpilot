# v4.16.0 root.contracts.quantscript.analysis_diagnostics parent residual judgment: warmup_fetch_gate

> Batch: BE-002GX-01
> Node: `root.contracts.quantscript.analysis_diagnostics`
> Selected child: `root.contracts.quantscript.analysis_diagnostics.warmup_fetch_gate`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `root.contracts.quantscript.analysis_diagnostics.warmup_fetch_gate` for the next recursive child.

## Parent Residual Scan

Current parent:

- `quantscript/src/analysis_diagnostics/mod.rs`
- `quantscript/src/analysis_diagnostics/analysis.rs`
- `quantscript/src/analysis_diagnostics/diagnostics.rs`

Closed children:

- `root.contracts.quantscript.analysis_diagnostics.unsupported_construct_gate`
- `root.contracts.quantscript.analysis_diagnostics.lookahead_window_gate`

Remaining major diagnostic families in `analysis.rs`:

| Candidate | Boundary | Decision |
| --- | --- | --- |
| `warmup_fetch_gate` | Required warmup inference, fetch/get_data lookback collection, insufficient warmup diagnostics, and numeric lookback helper. | Select |
| `strategy_presence_gate` | strategy fetch/emit existence diagnostics. | Queue |
| `fetch_lookback_warning_gate` | `lookback < 1` warning. | Queue |
| `indirect_recursion_gate` | helper call graph and indirect recursion diagnostics. | Queue |
| `index_bounds_gate` | data source lookback map and array/index bounds warning. | Queue |
| `dead_code_emit_gate` | constant-false emit warning. | Queue |
| `symbol_whitelist_gate` | known symbol whitelist warning. | Queue |

## Selected Boundary

The next baseline may freeze only:

- `infer_required_warmup_bars`
- `required_warmup_for_semantic`
- `collect_warmup_diagnostics`
- `collect_warmup_from_function`
- `collect_fetch_lookbacks_from_stmts`
- `collect_fetch_lookbacks_from_expr`
- `fetch_lookback`
- `arg_number_named`

Expected child file:

- `quantscript/src/analysis_diagnostics/warmup_fetch_gate.rs`

## Why This Child

`warmup_fetch_gate` is the next best child because:

- it owns the top-level `required_warmup_bars` value used by `ScriptAnalysis`;
- it has one coherent responsibility: infer required warmup and compare it against fetch/get_data lookback supply;
- `fetch_lookback` and `arg_number_named` are already shared local helpers for fetch lookback semantics;
- extracting it reduces the parent before touching later route-like warning gates;
- it can be parent-visible through a small surface while preserving diagnostic order and public analysis DTO shape.

## Shared Helper Constraint

`fetch_lookback` is currently also used by the later index-bounds family. The next extraction must preserve parent access to `fetch_lookback` through the parent-child boundary without introducing sibling horizontal calls. The later `index_bounds_gate` may consume this parent-visible helper only through `analysis.rs` or a future parent-owned re-export.

## Exclusions

Do not move in the next baseline:

- strategy fetch/emit presence checks;
- fetch lookback warnings;
- indirect recursion checks;
- data source map and index bounds checks;
- dead-code emit checks;
- symbol whitelist checks;
- diagnostic DTO/span types;
- lookahead-window diagnostics already closed;
- unsupported construct diagnostics already closed;
- evaluator normalization, runtime lowering, v4 static audit, tests, authoring samples, backend, executor, frontend, plugin metadata, or release transition.

## Next Step

BE-002GY-01 `root.contracts.quantscript.analysis_diagnostics.warmup_fetch_gate` baseline_plan.
