# v4.16.0 root.contracts.qrpc_core.artifact_specs.run_backtest_specs extract closeout

> Batch: BE-001RW-02
> Node: `root.contracts.qrpc_core.artifact_specs.run_backtest_specs`
> Parent: `root.contracts.qrpc_core.artifact_specs`
> Stage: `extract_closeout`
> Movement: Rust code extraction.

---

## Summary

`root.contracts.qrpc_core.artifact_specs.run_backtest_specs` has been extracted from `qrpc_core/src/artifact_specs.rs` into a private child module.

The artifact specs parent preserves the local public facade:

- `mod run_backtest_specs;`
- `pub use run_backtest_specs::*;`

The qrpc-core crate root remains unchanged and continues to preserve public exports through `pub use artifact_specs::*;`.

## Files Changed

- `qrpc_core/src/artifact_specs.rs`
- `qrpc_core/src/artifact_specs/run_backtest_specs.rs`

## Extraction Result

`qrpc_core/src/artifact_specs/run_backtest_specs.rs` now owns:

- `RunModeSpec`;
- `BacktestReplaySource`;
- `DatasetSpec` and `DatasetSpec::from`;
- `ExecutionAssumptionSpec`;
- `ExecutionAssumptionValueSource`;
- `ExecutionAssumptionSourceSummary`;
- `ExecutionAssumptionSpec::from`;
- `MarketDataSnapshotSpec` and `MarketDataSnapshotSpec::from_runtime_protocol`;
- `RunSpec`;
- `RunSpecRuntimeProtocolInput`;
- `RunSpec::from_runtime_protocol`;
- `BacktestSpec` and `BacktestSpec::new`.

`qrpc_core/src/artifact_specs.rs` keeps later artifact specs residuals in place:

- `StrategyArtifactSourceKind`;
- `StrategyArtifact`;
- `CoreIrArtifact`;
- `CompileArtifact`;
- `CompileArtifactBundle`.

## Rust Local Fields

| Field | Result |
| --- | --- |
| Crate | `qrpc-core` |
| Parent facade | Preserved. `artifact_specs.rs` remains the local parent facade and re-export owner. |
| Child visibility | The module is private; public run/backtest surfaces are preserved through `pub use run_backtest_specs::*;`. |
| Crate public exports | Preserved through the existing crate-root `pub use artifact_specs::*;`. |
| Parent-owned residuals | Strategy artifact source taxonomy and artifact bundle DTOs remain parent-owned for later recursion. |
| Sibling dependency check | Passed. The child uses digest via the artifact specs parent facade and does not import sibling internals directly. |

## Equivalence Preservation

| Surface | Preserved evidence |
| --- | --- |
| Run/backtest modes | Enum variants and snake_case serde remain unchanged. |
| Dataset projection | `DataSourceConfig` to `DatasetSpec` mapping remains unchanged. |
| Execution assumptions | Runtime config to execution assumption mapping remains unchanged. |
| Snapshot projection | Runtime data source projection into datasets remains unchanged. |
| Run spec projection | Schema version use, input assignment, datasets, execution assumptions, source summary, and digest remain unchanged. |
| Backtest constructor | Schema version and constructor assignment remain unchanged. |
| Public facade | Artifact specs and crate-root re-exports preserve public access. |

## Hard Boundaries Held

BE-001RW-02 did not:

- change enum variants, serde attributes, schema version use, field names, projection behavior, constructor behavior, public exports, or tests;
- move `StrategyArtifactSourceKind`, artifact bundle DTOs, runtime IO DTOs, RFC execution contracts, or crate-root tests;
- change closed canonical digest, other qrpc-core children, or release transition.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-pre-commit-hook.ps1`

## Next Step

BE-001RW-03 `root.contracts.qrpc_core.artifact_specs.run_backtest_specs` single_leaf_closeout.
