# v4.16.0 root.contracts.runtime_support parent residual judgment selects agent_module

> Batch: BE-001ZU-01
> Node: `root.contracts.runtime_support`
> Stage: `parent_residual_judgment`
> Selected child: `root.contracts.runtime_support.agent_module`
> Movement: No Rust code movement.

---

## Parent State

Closed runtime-support children:

- `contracts.runtime_support.runtime_facade_coordinator`
- `contracts.runtime_support.data_module`
- `contracts.runtime_support.intent_module`

Remaining runtime-support queue from the frozen baseline:

- `contracts.runtime_support.agent_module`
- `contracts.runtime_support.core_ir_evaluator`
- `contracts.runtime_support.execution_module`
- `contracts.runtime_support.fill_engine`
- `contracts.runtime_support.risk_support`
- `contracts.runtime_support.sandbox_replay_timeline`
- `contracts.runtime_support.v4_runtime_support`
- `contracts.runtime_support.live_execution`
- `contracts.runtime_support.plugin_runtime_support`
- `contracts.runtime_support.ops_compat_support`
- `contracts.runtime_support.backtest_metrics`

## Selected Child Boundary

`root.contracts.runtime_support.agent_module` owns runtime agent decision generation from intent signals, Core IR agent policies, and current portfolio state.

Candidate physical surface:

- `qrpc_runtime/src/agent_module.rs`

Candidate public/parent-facing surface:

- `AgentEvaluationRequest`
- `AgentEvaluationOutput`
- `AgentModuleProvider`
- `BuiltinAgentModule`

## Why This Child Next

| Criterion | Judgment |
| --- | --- |
| Queue order | It is the next runtime-support child after the closed `intent_module` in the frozen baseline. |
| Cohesion | Strong: it converts intent signals and portfolio state into agent decisions. |
| Size pressure | High: current file is about 1678 lines and contains multiple policy families plus tests. |
| Inputs | Intent signals, Core IR agent policies, portfolio state, rebalance timestamps, cycle name, timestamp, trace id |
| Outputs | Agent decisions, runtime events, and evaluated rebalance agent ids |
| Risk | High: trading decision semantics, portfolio rebalance behavior, symbol/exchange inheritance, and arbitrage behavior must remain equivalent. |
| Exclusions | Intent generation, risk checks, execution planning, fill simulation, sandbox, v4 runtime, live execution, plugin runtime, and metrics stay outside this child. |

## Initial Split Hypothesis

The next baseline should inspect whether `agent_module` should split into a small set of cohesive children instead of helper-level micro-leaves:

- weighted signal decision flow;
- portfolio rebalance decision flow;
- cross-venue arbitrage decision flow;
- shared scoring/portfolio helper surface;
- local test harness.

This is only a hypothesis. The baseline must freeze the actual code boundary before approving movement.

## Leaf Split Decision Gate

`agent_module` should continue to baseline because:

- it is still an unclosed runtime-support child from the frozen queue;
- the physical file is too large for terminal leaf closeout;
- behavior families have distinct failure modes;
- public provider surface is parent-facing and must remain stable;
- tests cover multiple policy families and must be preserved.

## Equivalence Baseline For Next Step

Next baseline must preserve:

- agent provider trait and built-in provider behavior;
- weighted signal decisions for slow cycles;
- portfolio rebalance targets, schedules, and target-weight variants;
- cross-venue arbitrage behavior for fast cycles;
- symbol/exchange inheritance from signals;
- portfolio inventory/cash guard behavior used by agent decisions;
- runtime event payload shape;
- existing agent module tests.

Required gates for the next movement:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-001ZV-01 `root.contracts.runtime_support.agent_module` baseline_plan.
