# v4.16.0 root.contracts.qrpc_core.runtime_io_contract.portfolio_state extract closeout

> Batch: BE-001SJ-02
> Node: `root.contracts.qrpc_core.runtime_io_contract.portfolio_state`
> Parent: `root.contracts.qrpc_core.runtime_io_contract`
> Stage: `extract_closeout`
> Movement: Physical Rust extraction.

---

## Summary

`portfolio_state` is now a private runtime IO child module.

Moved from `qrpc_core/src/runtime_io_contract.rs` into `qrpc_core/src/runtime_io_contract/portfolio_state.rs`:

- `Position`
- `ExchangeExposure`
- `PortfolioState`
- `PortfolioState::new`
- `PortfolioState::debug_assert_invariants`

The parent still owns:

- `mod portfolio_state;`
- `pub use portfolio_state::*;`
- closed runtime IO child re-exports
- runtime output and backtest output residual DTOs

## Equivalence Proof

| Surface | Result |
| --- | --- |
| Public facade | Preserved through `pub use portfolio_state::*;` in `qrpc_core/src/runtime_io_contract.rs` and existing crate-root re-export. |
| DTO fields | Unchanged for all moved DTOs. |
| serde derives/attributes | Unchanged, including `#[serde(deny_unknown_fields)]` on `PortfolioState`. |
| Helper methods | `PortfolioState::new` defaults and `debug_assert_invariants` assertions are preserved. |
| Parent-owned dependency | `PortfolioState.open_orders` uses `OpenOrder`, imported through the runtime IO parent. |
| Sibling links | None added. |

## Files

| File | Role |
| --- | --- |
| `qrpc_core/src/runtime_io_contract.rs` | Runtime IO parent facade and remaining residual owner. |
| `qrpc_core/src/runtime_io_contract/portfolio_state.rs` | Portfolio/account state DTO child owner. |

## Boundary Notes

`portfolio_state` imports `OpenOrder` through the parent module instead of importing the execution child directly. This preserves the parent-child communication rule and keeps the closed `execution_io` child isolated.

`RuntimeEvent`, `RuntimeCycleOutput`, `SessionOutput`, and backtest output DTOs remain parent-owned until their own residual judgments.

## Next Step

BE-001SJ-03 `root.contracts.qrpc_core.runtime_io_contract.portfolio_state` single_leaf_closeout.

## Gates

- `git diff --check`
- `cargo fmt`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
