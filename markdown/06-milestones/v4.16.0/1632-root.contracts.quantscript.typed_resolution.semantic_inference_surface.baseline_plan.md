# v4.16.0 root.contracts.quantscript.typed_resolution.semantic_inference_surface baseline plan

> Batch: BE-002GH-01
> Node: `root.contracts.quantscript.typed_resolution.semantic_inference_surface`
> Parent: `root.contracts.quantscript.typed_resolution`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Scope

Freeze expression semantic inference and manual indicator formula inference before movement.

Current owner:

- `quantscript/src/resolve/mod.rs`

Planned child:

- `quantscript/src/resolve/semantic_inference_surface.rs`

## Items Frozen For Movement

The next extraction may move only:

- `infer_expr_semantic`
- `infer_manual_indicator_formula`
- `infer_manual_macd_histogram_formula`
- `infer_manual_macd_line_formula`
- `infer_manual_macd_signal_formula`
- `infer_manual_macd_line_shape`
- `infer_signal_line_shape`
- `infer_ema_call`
- `infer_manual_moving_average_formula`
- `infer_manual_zscore_formula`
- `infer_window_aggregate_view`
- `infer_boundary_lookback_pair`
- `infer_balanced_smoothed_change_pair`
- `infer_smoothed_change_binding`
- `infer_change_source_call`
- `resolve_alias_expr`
- `infer_series_view_shape`
- `series_capability_target_expr`
- `window_aggregate_target_expr`
- `is_ema_like_name`

## Parent Wiring To Preserve

The parent resolver module must keep:

- `lower_script_to_typed_hir` public entrypoint unchanged;
- `Resolver::resolve_module`, `resolve_function`, `lower_block`, `lower_stmt`, `lower_expr`, and `lower_callee_expr` behavior unchanged;
- all diagnostics and `expr_semantics` map insertion behavior unchanged;
- public exports from `public_type_surface` and `callable_classification_surface` unchanged;
- downstream `crate::resolve::*` compatibility unchanged.

## Forbidden Movement

The next extraction must not move or change:

- public resolver DTOs/enums already closed in `public_type_surface`;
- callable/helper classifiers and registry seeding already closed in `callable_classification_surface`;
- type inference helpers from `infer_call_type` through `unwrap_maybe`;
- binding insertion, condition validation, and type annotation resolution;
- `make_expr`, `expr_label`, ID allocation, callable target helpers, scalar helper functions, resolved function builder, return-list helpers, and resolver-local tests;
- analysis, lowering, evaluator, runtime, backend, executor, frontend, plugin metadata, or release transition.

## Expected Validation

The extraction must pass:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002GH-02 `root.contracts.quantscript.typed_resolution.semantic_inference_surface` actual_extraction.
