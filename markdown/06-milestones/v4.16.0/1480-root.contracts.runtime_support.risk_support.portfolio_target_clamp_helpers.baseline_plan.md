# v4.16.0 root.contracts.runtime_support.risk_support.portfolio_target_clamp_helpers baseline plan

> Batch: BE-002CA-01
> Node: `root.contracts.runtime_support.risk_support.portfolio_target_clamp_helpers`
> Parent: `root.contracts.runtime_support.risk_support`
> Stage: `baseline_plan`
> Movement: No Rust code movement.

---

## Scope

Freeze the baseline for extracting portfolio-target clamp helpers.

Allowed movement:

- add `qrpc_runtime/src/risk_checker/portfolio_target_clamp_helpers.rs`;
- add a parent child declaration in `qrpc_runtime/src/risk_checker.rs`;
- move only the selected portfolio-target clamp helpers;
- keep `clamp_portfolio_target_limits` as the parent-facing `pub(super)` entrypoint.

## In-Scope Helpers

- `clamp_portfolio_target_limits`
- `refresh_portfolio_target_current_weights`
- `clamp_portfolio_target_new_positions`
- `clamp_portfolio_target_single_weight`
- `clamp_portfolio_target_turnover`
- `clamp_portfolio_target_total_buy_headroom`
- `clamp_portfolio_target_exchange_headroom`
- `clamp_portfolio_target_min_trade_weight`
- `clamp_portfolio_target_portfolio_net_exposure`

## Out of Scope

- provider trait and public evaluate surface;
- `evaluate_risk_decision`;
- `enforce_risk_mode`;
- closed event payload projection child;
- closed direction/cross-symbol constraints child;
- closed action clamp child;
- `push_reason_code`;
- exposure math helpers such as `available_sell_ratio`, `portfolio_equity`, and `portfolio_net_exposure_ratio`;
- tests.

## Dependency Rule

The child may use `super::*` to access parent-owned `push_reason_code` and exposure math helpers. It must not import sibling children directly and must not introduce a release-transition shortcut.

## Equivalence Baseline

The next actual extraction must preserve:

- current-weight refresh semantics;
- new-position limit ranking and tie-breakers;
- single-weight/concentration/symbol exposure clamps;
- turnover scaling;
- total/exchange buy headroom scaling;
- minimum trade removal;
- portfolio net exposure scaling;
- reason-code mutation;
- provider/evaluate public surface.

## Gates

Required gates for actual extraction:

- `cargo fmt`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime risk -- --nocapture`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 / full feature tree / matrix governance

## Next Step

BE-002CB-01 `root.contracts.runtime_support.risk_support.portfolio_target_clamp_helpers` actual_extraction.
