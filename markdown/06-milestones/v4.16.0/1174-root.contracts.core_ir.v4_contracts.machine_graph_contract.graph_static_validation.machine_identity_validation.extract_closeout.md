# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.machine_identity_validation actual extraction complete

> Batch: BE-001UW-02
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.machine_identity_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation`
> Stage: `extract_closeout`
> Movement: Code movement completed.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.machine_identity_validation`
actual extraction is complete.

The child now owns graph machine inventory validation and returns the machine maps needed by later
parent-dispatched edge, event, and risk phases.

## Movement

Moved:

- machine ID required checks;
- duplicate top-level machine ID checks;
- nested machine family collection;
- duplicate top-level/nested machine ID checks;
- delegated per-machine static contract validation;
- machine validation error prefixing;
- top-level/all-machine map construction;
- `MachineIdentityValidation` result structure.

Kept in parent:

- schema version check;
- graph ID and empty graph machine-list checks;
- edge identity/endpoint/self-loop checks;
- DAG/event/risk child dispatch;
- `machine_event_party_allowed`.

## Equivalence Result

- `cargo fmt -p qrpc-core-ir` passed.
- `cargo test -p qrpc-core-ir` passed: 54 tests passed, 0 failed.

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation.rs`
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/machine_identity_validation.rs`

**Markers**:
- `BE-001UW-02`

**Next step**:
BE-001UW-03 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.machine_identity_validation single_leaf_closeout

---

## Gates

- `git diff --check`
- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
