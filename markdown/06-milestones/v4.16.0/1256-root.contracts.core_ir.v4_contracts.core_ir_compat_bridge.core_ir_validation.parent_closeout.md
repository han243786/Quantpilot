# v4.16.0 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation parent residual judgment closes parent

> Batch: BE-001WN-01
> Node: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation`
> Parent: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge`
> Stage: `parent_residual_judgment`
> Movement: No code movement. This document closes the validation parent after its reference-validation child was closed.

---

## Summary

root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation parent residual judgment closes parent

`core_ir_validation` is now a closed parent facade. It retains the validation entrypoint, Core IR version/required-field checks, node id collection, edge endpoint validation, DAG validation, and orchestration of the closed `reference_validation` child.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | PASS | The parent owns Core IR validation orchestration and node/edge checks, with `reference_validation` as a closed child. |
| parent_child_communication_kept | PASS | Parent calls its private `reference_validation` child directly; public bridge communication remains parent-mediated. |
| equivalence_baseline_freezable | PASS | BE-001WM-02 passed `cargo fmt -p qrpc-core-ir`, `cargo test -p qrpc-core-ir`, and `cargo check -p quantpilot`; BE-001WM-03 closed the child. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | PARTIAL | The parent remains private, but it is the validation gate for the public bridge entrypoint. |
| state_machine_phase | FAIL | No v4 machine construction occurs in this parent. |
| strategy_branch | PASS | Parent validation still branches over Core IR metadata, data/risk/execution required fields, node identity, edges, references, and DAG validation. |
| independent_failure_mode | PASS | Reference validation is separated; remaining version/node/edge/DAG failures are cohesive bridge-input validation concerns. |
| reuse_pressure | PARTIAL | The validation parent may be reused by future bridge variants through its single private entrypoint. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | PASS | Further splitting node id insertion or edge checks would create helper-only leaves. |
| communication_cost_rises | PASS | Additional children would increase diagnostic and id-set plumbing across small helpers. |
| local_proof_missing | PASS | Current bridge tests continue to cover accepted and rejected validation paths through the parent entrypoint. |
| line_count_only | PASS | Parent closes because remaining responsibilities are cohesive and small, not because of line count alone. |

leaf_split_decision_result

`parent_closeout: true`

The next recursive step returns to `core_ir_compat_bridge` parent residual judgment.

next_recursive_step

BE-001WO-01 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge parent_residual_judgment
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/core_ir_compat_bridge/core_ir_validation.rs`
- `qrpc_core_ir/src/v4/core_ir_compat_bridge/core_ir_validation/reference_validation.rs`

**Markers**:
- `BE-001WN-01`

**Next step**:
BE-001WO-01 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge parent_residual_judgment

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
