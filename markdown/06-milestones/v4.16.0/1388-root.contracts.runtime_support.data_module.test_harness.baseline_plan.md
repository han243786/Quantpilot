# v4.16.0 root.contracts.runtime_support.data_module.test_harness baseline plan

> Batch: BE-001ZQ-01
> Node: `root.contracts.runtime_support.data_module.test_harness`
> Parent: `root.contracts.runtime_support.data_module`
> Stage: `baseline_plan`
> Movement: No Rust code movement.

---

## Scope

This baseline freezes the local data-module unit-test harness before extraction.

Current owner file:

- `qrpc_runtime/src/data_module.rs`

Planned child file:

- `qrpc_runtime/src/data_module/test_harness.rs`

## Allowed Movement

The next extraction may move only:

- the existing `#[cfg(test)] mod tests` body;
- local test fixtures:
  - `sample_core_ir_with_quote_binding`
  - `sample_core_ir_with_quote_and_kline_bindings`
- all existing data-module unit tests inside that module.

The parent may add:

- a `#[cfg(test)]` child module declaration using `qrpc_runtime/src/data_module/test_harness.rs`;
- test-only imports needed by the child.

## Boundaries To Preserve

| Boundary | Requirement |
| --- | --- |
| Production behavior | No production logic may move in this step. |
| Test scope | Existing test assertions and fixture construction must stay equivalent. |
| Parent-private access | The child must remain under `data_module` so it can access parent-private facade/cache helpers through normal Rust child-module visibility. |
| Parser coverage | OKX candle and ticker parser tests must still run. |
| Provider coverage | Mock/non-OKX and mixed-source fast-cycle behavior tests must still run. |
| Source hint coverage | Request-control source hint restoration must still run. |
| Cache coverage | Request-interval cache behavior must still run. |
| Mock generator coverage | Pseudo-random determinism, range, and difference tests must still run. |

## Exclusions

The next extraction must not move:

- production parent facade;
- request cache;
- breaker logic;
- HTTP transport;
- historical cache;
- mock generation;
- normalization;
- source mapping;
- exchange surface;
- quality diagnostics.

## Risk Notes

This is a low-risk test-only extraction. If Rust visibility or test path behavior becomes noisy, keep the module as a direct child of `data_module` and avoid deeper test micro-leaves.

## Leaf Granularity Note

After extraction, `terminal_leaf_control_v2` must evaluate the created test harness child file before any deeper split. A deeper split into parser tests, provider tests, cache tests, and mock generator tests is not approved by this baseline.

## Equivalence Gates

Required for the next extraction:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-001ZQ-02 `root.contracts.runtime_support.data_module.test_harness` actual_extraction.
