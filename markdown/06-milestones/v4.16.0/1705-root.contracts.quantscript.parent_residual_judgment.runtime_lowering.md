# v4.16.0 root.contracts.quantscript parent residual judgment: runtime_lowering

> Batch: BE-002HT-01
> Node: `root.contracts.quantscript`
> Selected child: `root.contracts.quantscript.runtime_lowering`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`root.contracts.quantscript.runtime_lowering` is selected as the next child.

Reason:

- `syntax_ast_surface`, `legacy_config_compat`, `typed_resolution`, `analysis_diagnostics`, and `evaluator_normalization` are already closed;
- the remaining `quantscript/src/lowering/` subtree is the largest active QuantScript behavior owner;
- it owns the path from parsed QuantScript and semantic context into runtime config, intent specs, universe specs, binding sources, fallback data, helper environment, and source recovery;
- the subtree already exists as a module directory, so the next step should freeze its current white-box boundary before any code movement.

## White-Box Boundary

| Field | Boundary |
| --- | --- |
| Input | Parsed QuantScript script/module data, lowering context, semantic resolution output, fallback/source recovery inputs |
| Output | Runtime config artifacts, runtime intent/config structures, universe/data binding projections, diagnostics and recovered source metadata |
| Owner | `quantscript/src/lowering/mod.rs` and the existing `quantscript/src/lowering/` subtree |
| Parent caller | `quantscript` crate public lowering surface |
| Exclusions | v4 static audit, test plan surface, authoring docs/samples, plugin metadata, executor, release transition |

## Existing Subtree Snapshot

| File | Current role |
| --- | --- |
| `quantscript/src/lowering/mod.rs` | Lowering module entry and public re-export surface |
| `quantscript/src/lowering/orchestrator.rs` | Main lowering orchestration and public entry flow |
| `quantscript/src/lowering/universe.rs` | Universe and data universe projection |
| `quantscript/src/lowering/intents.rs` | Runtime intent inference and projection |
| `quantscript/src/lowering/fallback.rs` | Fallback/manual formula lowering |
| `quantscript/src/lowering/binding_sources.rs` | Binding source inference and source mapping |
| `quantscript/src/lowering/bindings.rs` | Binding projection helpers |
| `quantscript/src/lowering/source_recovery.rs` | Source recovery support |
| `quantscript/src/lowering/semantic.rs` | Semantic lowering bridge |
| `quantscript/src/lowering/helper_env.rs` | Helper environment construction |
| `quantscript/src/lowering/context.rs` | Lowering context types |
| `quantscript/src/lowering/shared.rs` | Shared lowering helpers |
| `quantscript/src/lowering/diagnostics.rs` | Lowering diagnostics support |

## Split Gate

The selected child is not a terminal leaf. It is an existing parent subtree with multiple durable child owners and a large code surface.

| Gate | Result | Evidence |
| --- | --- | --- |
| `micro_leaf_without_owner` | NO | This is a directory-level runtime lowering owner, not a helper fragment. |
| `communication_cost_rises` | NO | Selecting the existing parent subtree reduces cross-surface ambiguity before child extraction. |
| `local_proof_missing` | NO | The existing files provide clear owner pockets for the next baseline. |
| `line_count_only` | NO | Selection is based on behavior ownership, not size alone. |

## Residuals

Still open after this selection:

- `contracts.quantscript.runtime_lowering`
- `contracts.quantscript.v4_static_audit`
- `contracts.quantscript.test_plan_surface`
- `contracts.quantscript.authoring_docs_samples`
- `contracts.plugin_metadata`
- `executor`

## Next Step

BE-002HU-01 `root.contracts.quantscript.runtime_lowering` baseline_plan.
