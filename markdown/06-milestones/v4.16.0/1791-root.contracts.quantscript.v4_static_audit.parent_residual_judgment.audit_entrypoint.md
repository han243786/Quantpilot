# v4.16.0 root.contracts.quantscript.v4_static_audit parent residual judgment: audit_entrypoint

> Batch: BE-002JT-01
> Node: `root.contracts.quantscript.v4_static_audit`
> Selected child: `root.contracts.quantscript.v4_static_audit.audit_entrypoint`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `audit_entrypoint` as the next internal child under `v4_static_audit`.

`runtime_handoff_builder` is closed with `stop_split: true`; the parent remains
oversized and high-risk. The current parent still contains public audit DTOs,
the public static-audit entrypoint, static document parsing, machine graph
parsing, capability request assembly, event catalog derivation, parser utility
helpers, and local tests.

`audit_entrypoint` is the next safest extraction because it is the public audit
orchestration layer. It can follow the proven pattern from
`runtime_handoff_builder`: keep the public function in the parent as a stable
wrapper, then move the implementation body to a private child.

## Parent Split Evidence

Latest split-gate check for `quantscript/src/v4_static_audit.rs`:

| Metric | Result |
| --- | --- |
| Decision | `PRECISION` |
| Split decision | `CONTINUE` |
| Governance packaging | `precision_single_leaf` |
| Score | 60 |
| LOC | 1649 |
| Functions | 42 |
| Public surface | 26 |
| Branches | 101 |

The parent must continue precision single-leaf governance; same-parent batching
is not selected for this high-risk compiler/static-audit contract.

## Selected Boundary

| Field | Value |
| --- | --- |
| Planned owner | `root.contracts.quantscript.v4_static_audit.audit_entrypoint` |
| Primary movement | `audit_v4_quant_script_static` implementation body |
| Input | `&str`, `&V4StaticContractBundle` |
| Output | `V4QsStaticAuditReport` |
| Parent wrapper | public `audit_v4_quant_script_static` remains in `v4_static_audit.rs` |
| Child access | child may call parent-owned parser and use parent-owned DTOs during this pass |

## Exclusions

- Do not move public DTO definitions in this pass.
- Do not move `parse_v4_static_document`.
- Do not move machine/state/memory/transition parser helpers.
- Do not move capability type parsers.
- Do not move event catalog derivation.
- Do not move parser utilities or diagnostics helper.
- Do not move local tests.
- Do not introduce release-transition cross-child links.

## Gates

| Gate | Result |
| --- | --- |
| precision_parent_open | PASS |
| previous_child_closed | `runtime_handoff_builder stop_split: true` |
| public_api_preserved | parent wrapper required |
| parser_side_effects | parser remains parent-owned |
| code movement | none |

## Next Step

BE-002JU-01 `root.contracts.quantscript.v4_static_audit.audit_entrypoint`
baseline_plan.
