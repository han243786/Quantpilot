# v4.16.0 root.contracts.quantscript.typed_resolution.resolver_test_harness baseline plan

> Batch: BE-002GP-01
> Node: `root.contracts.quantscript.typed_resolution.resolver_test_harness`
> Parent: `root.contracts.quantscript.typed_resolution`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Scope

Freeze resolver-local tests before movement.

Current owner:

- `quantscript/src/resolve/mod.rs`

Planned child:

- `quantscript/src/resolve/resolver_test_harness.rs`

## Items Frozen For Movement

The next extraction may move only the resolver-local test module and its test-only imports:

- `lowers_ast_into_typed_hir`
- `reports_duplicate_function_definitions`
- `reports_unresolved_identifiers`
- `reports_unknown_function_call_targets`
- `reports_non_bool_conditions`
- `accepts_first_and_last_as_known_call_targets`
- `infers_builtin_and_imported_helper_call_types`
- `keeps_member_and_call_style_helper_types_consistent`
- `infers_histogram_as_member_capability_type`
- `records_standardized_expr_semantics_for_series_capabilities`
- `resolves_universe_helpers_and_for_binding_types`

## Parent Wiring To Preserve

The parent resolver module must keep:

- `lower_script_to_typed_hir` public entrypoint unchanged;
- `expr_semantic_key` public helper unchanged;
- public DTO and enum re-exports unchanged;
- `FunctionSignature`, `CallableTarget`, `BindingInfo`, `Resolver`, and `MemberCapabilityUse` ownership unchanged;
- production child module boundaries unchanged;
- all test assertions and source snippets behaviorally unchanged.

## Forbidden Movement

The next extraction must not move or change:

- public resolver DTOs/enums already closed in `public_type_surface`;
- callable/helper classifiers and registry seeding already closed in `callable_classification_surface`;
- semantic/manual indicator inference already closed in `semantic_inference_surface`;
- type inference, binding insertion, condition validation, and type annotation resolution already closed in `type_inference_binding_surface`;
- module/function/block/statement/expression lowering already closed in `resolver_orchestration_surface`;
- support helpers already closed in `resolver_support_surface`;
- production resolver state/type scaffolding, public entrypoints, or diagnostics behavior;
- analysis, lowering, evaluator, runtime, backend, executor, frontend, plugin metadata, or release transition.

## Expected Validation

The extraction must pass:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002GP-02 `root.contracts.quantscript.typed_resolution.resolver_test_harness` actual_extraction.
