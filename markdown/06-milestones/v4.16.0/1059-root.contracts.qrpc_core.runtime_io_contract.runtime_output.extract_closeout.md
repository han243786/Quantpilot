# v4.16.0 root.contracts.qrpc_core.runtime_io_contract.runtime_output extract closeout

> Batch: BE-001SL-02
> Node: `root.contracts.qrpc_core.runtime_io_contract.runtime_output`
> Parent: `root.contracts.qrpc_core.runtime_io_contract`
> Stage: `extract_closeout`
> Movement: Physical Rust extraction.

---

## Summary

`runtime_output` is now a private runtime IO child module.

Moved from `qrpc_core/src/runtime_io_contract.rs` into `qrpc_core/src/runtime_io_contract/runtime_output.rs`:

- `RuntimeEvent`
- `RuntimeCycleOutput`
- `SessionOutput`

The parent still owns:

- `mod runtime_output;`
- `pub use runtime_output::*;`
- closed runtime IO child re-exports
- backtest output residual DTOs

## Equivalence Proof

| Surface | Result |
| --- | --- |
| Public facade | Preserved through `pub use runtime_output::*;` in `qrpc_core/src/runtime_io_contract.rs` and existing crate-root re-export. |
| DTO fields | Unchanged for all three moved DTOs. |
| serde derives | Unchanged. |
| Runtime event payload | `RuntimeEvent.payload` still uses `serde_json::Value`. |
| Fetch counts | `RuntimeCycleOutput` and `SessionOutput` still use `BTreeMap<String, u32>`. |
| Parent-owned dependencies | Closed child DTO dependencies are imported through the runtime IO parent. |
| Sibling links | None added. |

## Files

| File | Role |
| --- | --- |
| `qrpc_core/src/runtime_io_contract.rs` | Runtime IO parent facade and remaining backtest output residual owner. |
| `qrpc_core/src/runtime_io_contract/runtime_output.rs` | Runtime event/cycle/session output DTO child owner. |

## Boundary Notes

`runtime_output` imports closed DTO dependencies through `super::{...}` so the parent facade remains the only bridge. It does not import `market_data_io`, `decision_flow`, `execution_io`, or `portfolio_state` child modules directly.

Backtest output DTOs remain parent-owned until their residual judgment.

## Next Step

BE-001SL-03 `root.contracts.qrpc_core.runtime_io_contract.runtime_output` single_leaf_closeout.

## Gates

- `git diff --check`
- `cargo fmt`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
