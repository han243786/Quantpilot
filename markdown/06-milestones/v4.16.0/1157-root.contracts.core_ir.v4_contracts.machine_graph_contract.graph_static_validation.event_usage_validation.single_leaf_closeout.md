# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation single leaf closeout continues split

> Batch: BE-001UM-03
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation`
remains open for a deeper split.

The extracted child still owns event reference discovery, catalog presence/declared-event checks,
transition emitter/consumer checks, action emitted-event checks, and edge emitter/consumer checks.
The next child should isolate `event_party_validation`, because party permission checks are a
distinct event-routing failure plane after event references have been resolved.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `event_party_validation` is a named child for emitter/consumer permission checks. |
| parent_child_communication_kept | pass | It can receive resolved machine/event spec maps from the event usage parent without sibling calls. |
| equivalence_baseline_freezable | pass | Existing tests cover emitter/consumer failures through the public graph validator. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | The public validator remains in graph validation parent, but event party checks are a stable private phase. |
| state_machine_phase | pass | Party validation is the event-routing permission phase after event catalog lookup. |
| strategy_branch | guarded | It protects transition/action/edge event routing semantics used by graph contracts. |
| independent_failure_mode | pass | Consumer mismatch, source emitter mismatch, emitted-event emitter mismatch, and edge party mismatch fail independently from catalog presence. |
| reuse_pressure | guarded | Reuse remains through the event usage parent, but the section is large enough to isolate for review. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | Party validation owns a real permission-check boundary. |
| communication_cost_rises | no | The child can receive `all_machines`, `event_specs`, and `machines_by_id`; no lateral child link is needed. |
| local_proof_missing | no | qrpc-core-ir tests passed after extraction. |
| line_count_only | no | Continue decision is based on separate failure planes, not only 132-line size. |

leaf_split_decision_result

`continue_split: true`

Next selected child candidate:

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation.event_party_validation`

next_recursive_step

BE-001UN-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation parent_residual_judgment selects event_party_validation
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/event_usage_validation.rs`

**Markers**:
- `BE-001UM-03`

**Next step**:
BE-001UN-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation parent_residual_judgment selects event_party_validation

---

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
