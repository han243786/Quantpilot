# v4.16.0 root.contracts.quantscript.runtime_lowering.universe_lowering parent residual judgment: universe_construct_expansion

> Batch: BE-002IR-01
> Node: `root.contracts.quantscript.runtime_lowering.universe_lowering`
> Selected child: `root.contracts.quantscript.runtime_lowering.universe_lowering.universe_construct_expansion`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `universe_construct_expansion` as the next child under `universe_lowering`.

`rebalance_directive_detection` is closed. The next remaining cohesive owner is the expansion surface that rewrites `ScriptModule` strategy bodies after evaluating universe constructs. It owns the parent-visible `expand_universe_constructs` facade plus statement expansion, local binding uniquification, universe binding substitution, and per-symbol substitution.

## White-Box Boundary

| Field | Boundary |
| --- | --- |
| Input | `ScriptModule`, `LoweringContext`, and parent-owned universe expression evaluation |
| Output | expanded `ScriptModule` preserving non-strategy items and rewritten strategy body |
| Current owner | `quantscript/src/lowering/universe.rs` |
| Planned owner | `root.contracts.quantscript.runtime_lowering.universe_lowering.universe_construct_expansion` |
| Parent caller | `expand_universe_constructs` facade |
| Exclusions | rebalance directive detection, raw universe expression evaluation, symbol literal decoding, metadata selection/sorting, and tests |

## Symbols Approved For Next Baseline

The next baseline may freeze movement for:

- `expand_universe_constructs`
- `expand_stmts`
- `uniquify_local_bindings`
- `collect_binding_names`
- `rename_stmt_bindings`
- `rename_expr_bindings`
- `renamed_identifier`
- `substitute_universe_stmt`
- `substitute_universe_expr`
- `substitute_symbol_in_stmt`
- `substitute_symbol_in_expr`

The moved child may call parent-owned `evaluate_universe_expr` and share parent-owned `UniverseValue` through a `pub(super)` boundary.

## Split Decision Gate

| Gate | Result | Evidence |
| --- | --- | --- |
| `oversized_parent_requires_child` | PASS | `universe_lowering` remains above the terminal target range after the previous extraction. |
| `strong_owner_boundary` | PASS | Script expansion is a distinct AST rewrite surface with stable input/output. |
| `parent_facade_preserved` | PASS | Parent can keep `expand_universe_constructs` as a thin facade. |
| `sibling_exclusion_clear` | PASS | Expression evaluation, metadata selection, rebalance detection, and tests remain outside. |

## Risk Classification

| Risk | Result |
| --- | --- |
| Public API / route / schema | Parent-visible Rust signature must remain stable |
| Persistence / lock owner | Reads `LoweringContext`; no persistence movement |
| Trading semantics evidence | High; expansion rewrites strategy bodies over universe symbols |
| Cross-crate behavior | Present through `ScriptModule`, `Symbol`, and runtime lowering tests |

## Next Step

BE-002IS-01 `root.contracts.quantscript.runtime_lowering.universe_lowering.universe_construct_expansion` baseline_plan.
