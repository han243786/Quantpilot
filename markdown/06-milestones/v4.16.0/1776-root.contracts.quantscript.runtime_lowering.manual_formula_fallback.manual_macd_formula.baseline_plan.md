# v4.16.0 root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_macd_formula baseline plan

> Batch: BE-002JK-01
> Node: `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_macd_formula`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Objective

Freeze the manual MACD fallback boundary before extraction.

The next movement may extract only MACD fallback recognition from
`quantscript/src/lowering/fallback.rs` into a child module. It must preserve:

- direct `ResolvedManualIndicatorFormula::MacdLine` handling;
- direct `ResolvedManualIndicatorFormula::MacdHistogram` handling;
- EMA spread fallback recognition for MACD line;
- MACD line/signal pair fallback recognition for histogram;
- source, fast/slow/signal period validation;
- no-match behavior for unsupported shapes.

## Frozen Rust Symbols

| Symbol | Current owner | Planned owner | Contract |
| --- | --- | --- | --- |
| `EmaSpreadMatch` | `fallback.rs` | child private | Carries source plus fast/slow EMA periods |
| `MacdLineSignalMatch` | `fallback.rs` | child private | Carries source plus fast/slow/signal periods |
| `manual_macd_line_from_expr` | `fallback.rs` | parent wrapper + child implementation | Returns `IndicatorBinding::MacdLine` or `None` |
| `manual_macd_histogram_from_expr` | `fallback.rs` | parent wrapper + child implementation | Returns `IndicatorBinding::Macd` or `None` |
| `match_ema_spread` | `fallback.rs` | child private | Requires same source and EMA methods |
| `match_macd_line_signal_pair` | `fallback.rs` | child private | Requires MACD line plus EMA signal on same source/periods |

## Parent-Owned Exclusions

The extraction must not move or rewrite:

- `resolve_manual_formula_binding` recognizer order except for delegating MACD
  branches through child wrappers;
- ZScore, moving-average, boundary lookback public helper, fallback tests, RSI
  closed child, and momentum closed child;
- binding source decoders, data source reference resolution, semantic bridge,
  and indicator binding resolution ownership beyond child calls.

## White-Box Contract

| Field | Contract |
| --- | --- |
| Input | full expression, subtract operands, `BindingEnv`, and data sources |
| Output | `IndicatorBinding::MacdLine`, `IndicatorBinding::Macd`, or no-match |
| Parent API | Keep public(crate) MACD helpers callable from `fallback.rs` |
| Child visibility | Child implementation functions may be `pub(super)` only where parent wrappers require delegation |
| Error behavior | Preserve `anyhow::Result` propagation from source and binding resolution |
| Trading semantics | Preserve source identity and fast/slow/signal periods exactly |
| Recognizer order | Preserve MACD histogram then line before momentum in subtract branches |

## Equivalence Evidence Required Next

BE-002JK-02 must run:

- `cargo fmt --all --check`;
- `cargo test -p quantscript`;
- `cargo check -p quantpilot`;
- `git diff --check`;
- UTF-8, full feature tree, and matrix governance checks.

## Next Step

BE-002JK-02 `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_macd_formula` actual_extraction.
