# v4.16.0 root.contracts.runtime_support.risk_support.test_harness extract closeout

> Batch: BE-002CH-01
> Node: `root.contracts.runtime_support.risk_support.test_harness`
> Parent: `root.contracts.runtime_support.risk_support`
> Stage: `actual_extraction`
> Movement: Rust code movement.

---

## Scope

Extract the local risk-checker test harness from `qrpc_runtime/src/risk_checker.rs` into a dedicated test-only child module.

Moved file:

- `qrpc_runtime/src/risk_checker/test_harness.rs`

Parent change:

- `qrpc_runtime/src/risk_checker.rs` now keeps only `#[cfg(test)] mod test_harness;` for the local test module.

## Equivalence

The migration preserves:

- all local risk checker test bodies and assertions;
- sample Core IR fixtures and portfolio/action fixtures;
- parent-mediated test access through `use super::*`;
- production risk checker API and helper ownership.

Observed test namespace changes from `risk_checker::tests::*` to `risk_checker::test_harness::*`; this is expected from the child module name and does not change behavior.

## Out of Scope

- production provider/evaluate behavior;
- risk decision mutation;
- risk mode enforcement;
- closed helper children;
- release-transition optimization.

## Gates

Verified for this batch:

- `cargo fmt --check`: pass
- `cargo check -p qrpc-runtime`: pass
- `cargo test -p qrpc-runtime risk -- --nocapture`: pass, 39 tests

Remaining full gates are required before commit:

- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 / full feature tree / matrix governance

## Next Step

BE-002CH-02 `root.contracts.runtime_support.risk_support.test_harness` single_leaf_closeout.
