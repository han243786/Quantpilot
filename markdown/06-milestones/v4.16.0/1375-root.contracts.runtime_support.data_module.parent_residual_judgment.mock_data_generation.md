# v4.16.0 root.contracts.runtime_support.data_module parent residual judgment selects mock_data_generation

> Batch: BE-001ZJ-01
> Node: `root.contracts.runtime_support.data_module`
> Stage: `parent_residual_judgment`
> Selected child: `root.contracts.runtime_support.data_module.mock_data_generation`
> Movement: No Rust code movement.

---

## Parent State

Closed data-module children:

- `contracts.runtime_support.data_module.source_mapping`
- `contracts.runtime_support.data_module.quality_diagnostics`
- `contracts.runtime_support.data_module.collection_orchestration`
- `contracts.runtime_support.data_module.exchange_surface_wave`
- `contracts.runtime_support.data_module.exchange_endpoints`
- `contracts.runtime_support.data_module.exchange_payload_parsing`
- `contracts.runtime_support.data_module.normalization`

Remaining data-module residuals:

- `contracts.runtime_support.data_module.mock_data_generation`
- `contracts.runtime_support.data_module.historical_cache`
- `contracts.runtime_support.data_module.http_transport`
- `contracts.runtime_support.data_module.test_harness`
- parent facade closeout

## Selected Child Boundary

`root.contracts.runtime_support.data_module.mock_data_generation` owns deterministic mock market data generation and backtest mock bars.

Candidate implementation surface in `qrpc_runtime/src/data_module.rs`:

- `MOCK_VOLATILITY`
- `DEFAULT_MOCK_VOLATILITY`
- `get_mock_volatility`
- `mock_raw_klines`
- `pseudo_random`
- `mock_raw_quote`
- `mock_kline_bars_for_backtest`

## Why This Child Next

| Criterion | Judgment |
| --- | --- |
| Cohesion | Strong: volatility config, deterministic pseudo-random generation, raw mock quote/kline generation, and backtest bar projection belong to one mock data owner. |
| Inputs | `DataSourceConfig`, `now_ms`, volatility atomic, exchange/symbol metadata |
| Outputs | `RawKline`, `RawQuote`, and normalized backtest bars via parent-owned normalization import |
| Risk | Low to medium: public `MOCK_VOLATILITY` and crate-visible `mock_kline_bars_for_backtest` must stay compatible, but no route/schema/persistence/lock owner changes are required beyond the existing atomic. |
| Exclusions | Historical cache, HTTP transport, exchange payload parsing, provider orchestration, quality diagnostics, and tests remain outside this child. |
| Parent communication | Child must be called through `data_module` parent imports; no sibling horizontal link is allowed. |

## Leaf Split Decision Gate

`mock_data_generation` should continue to baseline/extraction because:

- it has a stable deterministic mock owner;
- it is separate from live exchange transport and historical cache;
- it has a clear parent-visible compatibility surface;
- extracting it reduces `data_module.rs` residual complexity without changing behavior.

Further split inside mock generation is not yet approved. After extraction, `terminal_leaf_control_v2` must evaluate whether pseudo-random generation, volatility config, quote mock, and kline mock should remain one terminal leaf. Expected outcome is likely `STOP` unless the extracted child grows beyond the target terminal range or exposes stronger independent owner evidence.

## Equivalence Baseline For Next Step

Next baseline must preserve:

- deterministic mock bars for backtest;
- configurable volatility behavior and NaN/Inf clamp;
- quote/kline mock fallback behavior used by `BuiltinDataModule::mock_data`;
- `mock_kline_bars_for_backtest` crate-visible compatibility;
- parent-owned normalization call path.

Required gates for the next movement:

- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-001ZK-01 `root.contracts.runtime_support.data_module.mock_data_generation` baseline_plan.
