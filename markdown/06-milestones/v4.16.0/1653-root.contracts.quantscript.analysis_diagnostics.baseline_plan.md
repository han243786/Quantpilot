# v4.16.0 root.contracts.quantscript.analysis_diagnostics baseline plan

> Batch: BE-002GS-01
> Node: `root.contracts.quantscript.analysis_diagnostics`
> Parent: `root.contracts.quantscript`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Scope

Freeze the analysis and diagnostic DTO boundary before movement.

Current owners:

- `quantscript/src/analysis.rs`
- `quantscript/src/diagnostics.rs`
- `quantscript/src/lib.rs` analysis/diagnostic module declarations and public re-exports

Planned parent child:

- `quantscript/src/analysis_diagnostics/`

## Items Frozen For Movement

The next extraction may move only:

- `quantscript/src/analysis.rs` into the `analysis_diagnostics` child;
- `quantscript/src/diagnostics.rs` into the `analysis_diagnostics` child;
- `ScriptAnalysis`, `analyze_script_module`, and analysis helper functions;
- `DiagnosticSeverity`, `SpanContext`, `Span`, `Diagnostic`, and their impl helpers;
- crate-root module declarations/re-exports required to preserve existing `crate::analysis`, `crate::diagnostics`, and public `quantscript::*` compatibility;
- analysis-related crate-root tests only if the movement requires a test-only module path adjustment.

## Parent Wiring To Preserve

The QuantScript crate parent must keep:

- public exports for `analyze_script_module`, `ScriptAnalysis`, `Diagnostic`, `DiagnosticSeverity`, `Span`, and `SpanContext`;
- internal `crate::analysis` and `crate::diagnostics` paths or equivalent crate-root aliases used by resolver, lowering, static audit, and tests;
- `analyze_formal_quant_script` behavior unchanged;
- diagnostic codes, severity, span context, messages, and hints unchanged;
- analysis result shape and required warmup calculation unchanged.

## Forbidden Movement

The next extraction must not move or change:

- syntax/AST, legacy config compatibility, or typed-resolution children already closed;
- evaluator normalization from `evaluator.rs`;
- runtime lowering subtree from `quantscript/src/lowering/*`;
- v4 static audit and runtime handoff report generation;
- test-plan extraction;
- authoring docs, samples, backend, executor, frontend, plugin metadata, or release transition;
- analysis helper internals into finer children before the structural parent boundary exists.

## Expected Validation

The extraction must pass:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002GS-02 `root.contracts.quantscript.analysis_diagnostics` actual_extraction_or_structural_closeout.
