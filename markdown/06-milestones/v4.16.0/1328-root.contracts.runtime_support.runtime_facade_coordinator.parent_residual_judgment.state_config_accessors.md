# v4.16.0 runtime_facade_coordinator parent residual judgment selects state_config_accessors

> Batch: BE-001YM-01
> Node: `root.contracts.runtime_support.runtime_facade_coordinator`
> Parent: `root.contracts.runtime_support`
> Stage: `parent_residual_judgment`
> Selected child: `root.contracts.runtime_support.runtime_facade_coordinator.state_config_accessors`
> Movement: No code movement.

## Parent State

BE-001YL-03 closed `execution_market_entrypoints` with `stop_split: true`.

`runtime_facade_coordinator` remains open because state/config accessors, config generation, provider delegation helpers, portfolio projection, and coordinator tests still remain in the parent.

## Selected Child Boundary

`state_config_accessors` owns the public state, provider, risk, and execution-assumption accessor/control methods:

- `RuntimeCoordinator::portfolio_state`
- `RuntimeCoordinator::data_fetch_counts`
- `RuntimeCoordinator::last_action_at_ms`
- `RuntimeCoordinator::last_rebalance_at_ms`
- `RuntimeCoordinator::data_module`
- `RuntimeCoordinator::intent_module`
- `RuntimeCoordinator::agent_module`
- `RuntimeCoordinator::risk_checker`
- `RuntimeCoordinator::risk_mode`
- `RuntimeCoordinator::set_risk_mode`
- `RuntimeCoordinator::set_risk_monitor`
- `RuntimeCoordinator::is_risk_stopped`
- `RuntimeCoordinator::reset_risk_stopped`
- `RuntimeCoordinator::execution_module_provider_key`
- `RuntimeCoordinator::set_execution_assumptions`

## Exclusions

The selected child does not own:

- `RuntimeCoordinator` struct definition
- constructor/provider wiring
- session/cycle orchestration
- execution/market entrypoints
- `swap_module_config`
- `apply_pending_module_configs`
- `current_generation`
- `generation_history`
- provider delegation helper implementations
- `portfolio_update_event`
- `refresh_portfolio_state`
- coordinator tests

`current_generation` and `generation_history` remain excluded because they are part of the later `config_generation` child.

## Split Decision

`continue_split: true`.

Reasons:

- the selected methods are a compact public accessor/control surface
- config mutation and generation behavior remains a separate stronger owner
- provider delegation helpers and portfolio projection still have independent residual boundaries
- extracting accessors reduces parent weight without touching runtime behavior

No release-transition optimization is proposed.

## Next Step

BE-001YN-01 `root.contracts.runtime_support.runtime_facade_coordinator.state_config_accessors` baseline_plan.

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`
