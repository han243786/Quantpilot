# v4.16.0 root.contracts.quantscript.syntax_ast_surface single leaf closeout

> Batch: BE-002FX-03
> Node: `root.contracts.quantscript.syntax_ast_surface`
> Parent: `root.contracts.quantscript`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Close `root.contracts.quantscript.syntax_ast_surface` with `stop_split: true`.

The child now has a clear physical parent:

- `quantscript/src/syntax_ast_surface.rs`
- `quantscript/src/syntax_ast_surface/script.rs`
- `quantscript/src/syntax_ast_surface/types.rs`
- `quantscript/src/syntax_ast_surface/hir.rs`

The crate-root facade preserves public `quantscript::*` exports and internal compatibility aliases for existing `crate::script`, `crate::types`, and `crate::hir` references.

## Terminal Leaf Control

| Rule | Judgment |
| --- | --- |
| White-box boundary | Pass. The child owns syntax parsing, AST DTOs, type annotations, and HIR DTOs. |
| Parent-mediated communication | Pass. External callers still use the crate root; internal modules still resolve through parent aliases. |
| Split benefit | Low for this pass. Token, expression, statement, test-block, and function parser internals would be micro-owners without separate product-facing owners. |
| Governance cost | High if parser internals are split now, because follow-up baselines would multiply before higher-value QuantScript parents are handled. |
| Risk | Medium if over-split; current parent facade already captures the intended module boundary. |

## Evidence

BE-002FX-02 verified:

- `cargo fmt --check` PASS
- `cargo test -p quantscript` PASS, 88 tests
- `cargo check -p quantpilot` PASS
- `git diff --check` PASS
- UTF-8 check PASS
- full feature tree check PASS
- matrix governance check PASS

## Residuals

No further syntax-surface child is selected in this pass.

Remaining QuantScript queue:

- `legacy_config_compat`
- `typed_resolution`
- `analysis_diagnostics`
- `evaluator_normalization`
- `runtime_lowering`
- `v4_static_audit`
- `test_plan_surface`
- `authoring_docs_samples`

## Next Step

BE-002FY-01 `root.contracts.quantscript` parent_residual_judgment.
