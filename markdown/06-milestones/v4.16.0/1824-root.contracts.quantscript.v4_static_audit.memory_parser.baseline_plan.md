# v4.16.0 root.contracts.quantscript.v4_static_audit.memory_parser baseline plan

> Batch: BE-002KK-01
> Node: `root.contracts.quantscript.v4_static_audit.memory_parser`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Goal

Freeze the memory field parser boundary before Rust movement.

This child owns `memory` declaration parsing and `MachineMemoryField`
construction. Type resolution continues to go through the parent
`parse_qs_type_ref` wrapper.

## Frozen Boundary

| Field | Value |
| --- | --- |
| Approved function | `parse_memory` |
| Planned file | `quantscript/src/v4_static_audit/memory_parser.rs` |
| Inputs | memory line text and source line number |
| Outputs | `MachineMemoryField` or `Diagnostic` |
| Parent callers | `machine_block_parser` through parent wrapper |
| Visibility | child function may be `pub(super)` only for the parent wrapper |

## Equivalent Behavior

The extracted child must preserve:

- syntax diagnostic `QSV4113`;
- missing type diagnostic `QSV4114`;
- unsupported type diagnostic `QSV4117`;
- field name trimming before assignment;
- raw type name preservation;
- resolved `QsTypeRef` attachment;
- `nullable` flag handling;
- unchanged default value behavior.

## Exclusions

- Do not move `parse_state_group`.
- Do not move `parse_edge`.
- Do not move `parse_risk_plane`.
- Do not move `parse_machine_template`.
- Do not move capability/type parser internals.
- Do not move public report DTOs or local tests.
- Do not introduce release-transition cross-child links.

## Allowed Rust Movement For Next Step

BE-002KK-02 may:

1. add `mod memory_parser;`;
2. create `quantscript/src/v4_static_audit/memory_parser.rs`;
3. move the `parse_memory` implementation body;
4. keep the parent private wrapper named `parse_memory`;
5. adjust imports and `pub(super)` visibility only where required by this
   extraction.

## Gates For Actual Extraction

| Gate | Command |
| --- | --- |
| format | `cargo fmt --all --check` |
| crate tests | `cargo test -p quantscript` |
| workspace compile | `cargo check -p quantpilot` |
| diff hygiene | `git diff --check` |

## Next Step

BE-002KK-02 `root.contracts.quantscript.v4_static_audit.memory_parser`
actual_extraction.
