# v4.16.0 root.executor.runner.provider_order_routes single leaf closeout

> Batch: BE-002LH-03
> Node: `root.executor.runner.provider_order_routes`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`stop_split: true`

The extracted provider order route child is a compact OKX demo route surface.
It owns submit/query/cancel handlers while provider helper and credential
loading remain parent-owned for the current runner pass.

## Leaf Granularity Evidence

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -LeafId root.executor.runner.provider_order_routes -ParentId root.executor.runner -Path src-executor\provider_order_routes.rs -Depth 3
```

Observed result:

| Field | Value |
| --- | --- |
| Split decision | STOP |
| Governance packaging | stop_split |
| Decision | STOP |
| Score | 21 |
| LOC | 128 |
| Functions | 3 |
| Public surface | 3 |
| Branches | 3 |
| Domains | 4 |
| Risk tags | live_execution, lock, public_api, route_schema, security, state_machine, trading_semantics |
| Size bucket | small_100_149 |

## Why No Further Split

- The child is small and cohesive.
- Submit/query/cancel share provider mode, credential, audit, and REST behavior.
- Splitting individual handlers would create one-route leaves with higher
  governance and communication cost.

## Preserved Boundaries

- Public API unchanged.
- Route paths and response shapes unchanged.
- Provider helper functions remain parent-owned for this pass.
- Strategy lifecycle and market stream handlers remain outside this leaf.
- No release-transition link was introduced.

## Gates

| Gate | Result |
| --- | --- |
| leaf granularity evaluator | PASS, STOP score 21 |
| `cargo fmt --all --check` | PASS |
| `cargo check -p quantpilot --bin executor` | PASS |
| `git diff --check` | PASS |

## Next Step

BE-002LI-01 `root.executor.runner` parent_residual_judgment.
