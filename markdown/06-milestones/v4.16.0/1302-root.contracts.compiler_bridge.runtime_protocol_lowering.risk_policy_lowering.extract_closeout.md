# v4.16.0 root.contracts.compiler_bridge.runtime_protocol_lowering.risk_policy_lowering actual extraction complete

> Batch: BE-001XW-02
> Node: `root.contracts.compiler_bridge.runtime_protocol_lowering.risk_policy_lowering`
> Parent: `root.contracts.compiler_bridge.runtime_protocol_lowering`
> Stage: `extract_closeout`
> Movement: Rust code movement completed.

---

## Summary

`root.contracts.compiler_bridge.runtime_protocol_lowering.risk_policy_lowering` has been physically extracted.

New child file:

- `qrpc_compiler/src/runtime_protocol_lowering/risk_policy_lowering.rs`

The child now owns:

- `lower_runtime_risk_to_policy`

The parent `qrpc_compiler/src/runtime_protocol_lowering.rs` now keeps:

- `mod risk_policy_lowering`;
- top-level runtime protocol orchestration;
- data binding construction;
- closed `intent_signal_lowering` delegation;
- closed `agent_policy_lowering` delegation;
- DAG validation;
- parent-mediated risk policy delegation through `risk_policy_lowering::lower_runtime_risk_to_policy`.

## Equivalence Notes

The extraction preserves:

- all runtime risk field projections to Core IR risk policy fields;
- `max_cross_symbol_leverage: None`;
- validation delegation, data binding construction, indicator/signal lowering, agent policy lowering, DAG validation, and runtime Core IR output shape.

## Boundary

**Changed Rust files**:
- `qrpc_compiler/src/runtime_protocol_lowering.rs`
- `qrpc_compiler/src/runtime_protocol_lowering/risk_policy_lowering.rs`

**Unchanged ownership**:
- `intent_signal_lowering` remains closed.
- `agent_policy_lowering` remains closed.
- top-level runtime orchestration remains parent residual.
- Strategy IR lowering, tests, canonical hash, and release transition remain outside this child.

## Next Step

BE-001XW-03 `root.contracts.compiler_bridge.runtime_protocol_lowering.risk_policy_lowering` single_leaf_closeout.

## Gates

- `cargo fmt --check`
- `cargo test -p qrpc-compiler`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
