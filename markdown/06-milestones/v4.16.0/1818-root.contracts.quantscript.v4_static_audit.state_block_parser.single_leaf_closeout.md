# v4.16.0 root.contracts.quantscript.v4_static_audit.state_block_parser single leaf closeout

> Batch: BE-002KG-03
> Node: `root.contracts.quantscript.v4_static_audit.state_block_parser`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`stop_split: true`

The extracted child is a terminal-sized cohesive state parser. It owns flat state
declarations, state blocks, nested child-machine handoff, block skipping, and
invalid child-machine fallback as one boundary.

## Leaf Granularity Evidence

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -LeafId root.contracts.quantscript.v4_static_audit.state_block_parser -ParentId root.contracts.quantscript.v4_static_audit -Path quantscript\src\v4_static_audit\state_block_parser.rs -Depth 4
```

Observed result:

| Field | Value |
| --- | --- |
| Split decision | STOP |
| Governance packaging | stop_split |
| Decision | STOP |
| Score | 32 |
| LOC | 163 |
| Functions | 4 |
| Public surface | 2 |
| Branches | 14 |
| Domains | 6 |
| Risk tags | cross_crate, lock, persistence, public_api, route_schema, state_machine |
| Size bucket | terminal_target_150_600 |

## Why No Further Split

- The leaf is inside the target terminal size range.
- `parse_state` and `parse_state_block` are the only parent-facing surface.
- `skip_block` and `empty_child_machine` are private helpers for state-block
  recovery only.
- Splitting child-machine fallback from state-block parsing would increase
  governance and call-through cost without creating a clearer owner.
- Parent-child communication remains explicit through parent wrappers.

## Preserved Boundaries

- Public API unchanged.
- `machine_block_parser` continues through parent wrappers.
- `parse_state_group`, `parse_memory`, `parse_transition`, `parse_edge`, and
  `parse_risk_plane` remain parent-owned.
- Public report DTOs and local static-audit tests remain parent-owned.
- No release-transition link was introduced.

## Gates

| Gate | Result |
| --- | --- |
| leaf granularity evaluator | PASS, STOP score 32 |
| `git diff --check` | PASS |
| `check-utf8.ps1` | PASS |
| `check-full-feature-tree.ps1` | PASS |
| `check-matrix-governance.ps1` | PASS |

## Next Step

BE-002KH-01 `root.contracts.quantscript.v4_static_audit`
parent_residual_judgment.
