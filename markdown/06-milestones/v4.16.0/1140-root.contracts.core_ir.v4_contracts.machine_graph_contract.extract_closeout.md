# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract actual extraction complete

> Batch: BE-001UE-02
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `extract_closeout`
> Movement: Code movement complete.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract` is now extracted as a private v4 child module.

The child owns:

- graph schema DTOs;
- event catalog schema DTOs;
- event catalog validation;
- graph static validation;
- acyclic/event/risk-plane validation helpers;
- graph traversal helpers `collect_machine_family` and `machine_nested_depth`.

The v4 parent now declares and re-exports the child:

- `mod machine_graph_contract;`
- `pub use machine_graph_contract::*;`

The traversal helpers are imported parent-only for complexity metrics:

- `use machine_graph_contract::{collect_machine_family, machine_nested_depth};`

## Boundary

**Real files**:

- `qrpc_core_ir/src/v4.rs`
- `qrpc_core_ir/src/v4/machine_graph_contract.rs`

**Markers**:

- `BE-001UE-02`

## White-Box Result

| Surface | Owner after extraction | Parent-mediated dependencies |
| --- | --- | --- |
| graph schema | `machine_graph_contract` | v4 default helpers and version constants |
| event catalog schema | `machine_graph_contract` | v4 default helpers and version constants |
| graph/event/risk-plane validation | `machine_graph_contract` | closed machine contract schema/validation |
| traversal helpers | `machine_graph_contract` | exposed as `pub(super)` for v4 parent complexity metrics only |
| complexity metrics | v4 parent residual | uses parent-only graph traversal helpers |

## Non-Moved Surfaces

The extraction did not move:

- QS state-machine profile DTOs or validation;
- `ComplexityMetrics` and complexity budget validation;
- compat bridge builders;
- default helper function bodies;
- test fixtures.

## Equivalence Evidence

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir` passed: 54 tests.
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`

## Next Step

BE-001UE-03 `root.contracts.core_ir.v4_contracts.machine_graph_contract` single_leaf_closeout.
