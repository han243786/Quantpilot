# v4.16.0 root.contracts.runtime_support.fill_engine.portfolio_reservation_accounting actual extraction complete

> Batch: BE-002BG-01
> Node: `root.contracts.runtime_support.fill_engine.portfolio_reservation_accounting`
> Parent: `root.contracts.runtime_support.fill_engine`
> Stage: `actual_extraction`
> Movement: Rust code movement.

---

## Result

`root.contracts.runtime_support.fill_engine.portfolio_reservation_accounting` has been extracted.

New child file:

- `qrpc_runtime/src/fill_engine/portfolio_reservation_accounting.rs`

Parent file changed:

- `qrpc_runtime/src/fill_engine.rs`

Moved functions:

- `cash_limited_executable_qty`
- `reservation_for_order`
- `available_position_qty`
- `available_sell_qty_for_order`
- `sync_portfolio_reservations`
- `release_reservation_for_fill`

## Public Surface Preservation

`apply_fill_to_portfolio` remains parent-owned and public.

No public function, struct, field, crate export, event payload shape, or serde shape was changed.

## Equivalence Fix

During extraction, the FOK rejection test exposed that question-mark-terminated reject reasons must not receive an extra trailing period in `explanation_summary`.

The closed event-projection child was adjusted only to preserve that observed equivalence:

- question-mark reasons: `Order rejected: {reason}`
- other reasons: `Order rejected: {reason}.`

This preserves the existing FOK payload while keeping the normal reject-event summary behavior for other reasons.

## Preserved Boundaries

- closed `event_projection_wave` remains a sibling child and is not imported by this child;
- fill lifecycle orchestration remains parent-owned;
- fill report helpers remain parent-owned;
- portfolio mutation through public `apply_fill_to_portfolio` remains parent-owned;
- tests remain parent-owned;
- no sibling shortcut was introduced.

## Gates

Current Rust gates:

- `cargo fmt`: PASS
- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime fill_engine -- --nocapture`: PASS, 12 passed

Required closeout gates before commit:

- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-002BG-02 `root.contracts.runtime_support.fill_engine.portfolio_reservation_accounting` single_leaf_closeout.
