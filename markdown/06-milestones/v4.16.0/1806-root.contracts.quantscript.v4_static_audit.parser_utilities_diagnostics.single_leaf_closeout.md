# v4.16.0 root.contracts.quantscript.v4_static_audit.parser_utilities_diagnostics single leaf closeout

> Batch: BE-002KA-03
> Node: `root.contracts.quantscript.v4_static_audit.parser_utilities_diagnostics`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`parser_utilities_diagnostics stop_split: true`.

The child is a terminal parser utility leaf. It owns prepared-line construction,
word splitting, CSV splitting, and diagnostic construction. Splitting those
helpers into smaller leaves would produce micro modules and increase import and
governance cost without improving parser ownership.

## Leaf Granularity Evidence

`tools/evaluate-leaf-granularity.ps1 -Path quantscript/src/v4_static_audit/parser_utilities_diagnostics.rs`
result:

| Metric | Result |
| --- | --- |
| Decision | `STOP` |
| Split decision | `STOP` |
| Governance packaging | `stop_split` |
| Score | 22 |
| LOC | 49 |
| Functions | 4 |
| Public surface | 7 |
| Branches | 1 |
| Size bucket | `micro_under_100` |
| Governance mode | `stop_split` |

The micro-leaf default stop rule applies.

## Equivalence Evidence

| Gate | Result |
| --- | --- |
| `cargo fmt --all --check` | PASS |
| `cargo test -p quantscript` | PASS, 88 tests |
| `cargo check -p quantpilot` | PASS, no warnings |
| `git diff --check` | PASS |
| `check-utf8.ps1` | PASS |
| `check-full-feature-tree.ps1` | PASS |
| `check-matrix-governance.ps1` | PASS |

## Residuals

Remain in `root.contracts.quantscript.v4_static_audit`:

- static document parser;
- machine/state/memory/transition/edge/risk-plane parsers;
- machine template parser;
- DTO definitions outside `PreparedLine`;
- local static-audit tests.

Closed siblings:

- `runtime_handoff_builder`;
- `audit_entrypoint`;
- `capability_type_parser`;
- `event_catalog_derivation`;
- `parser_utilities_diagnostics`.

## Next Step

BE-002KB-01 `root.contracts.quantscript.v4_static_audit`
parent_residual_judgment.
