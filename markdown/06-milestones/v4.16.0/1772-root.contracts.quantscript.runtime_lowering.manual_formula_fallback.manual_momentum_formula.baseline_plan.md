# v4.16.0 root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_momentum_formula baseline plan

> Batch: BE-002JI-01
> Node: `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_momentum_formula`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Objective

Freeze the manual momentum fallback boundary before extraction.

The next movement may extract only momentum fallback recognition from
`quantscript/src/lowering/fallback.rs` into a child module. It must preserve:

- direct `ResolvedManualIndicatorFormula::Momentum` handling;
- boundary lookback source/span fallback handling;
- legacy latest/lookback binary pair handling;
- division-ratio momentum handling;
- subtract/division `ratio - 1` momentum handling;
- no-match behavior for unsupported shapes.

## Frozen Rust Symbols

| Symbol | Current owner | Planned owner | Contract |
| --- | --- | --- | --- |
| `manual_momentum_from_expr` | `fallback.rs` | parent wrapper + child implementation | Returns `IndicatorBinding::Momentum` or `None` |
| `manual_momentum_ratio_from_division` | `fallback.rs` | parent wrapper + child implementation | Preserves direct momentum semantics and ratio fallback semantics |
| `manual_momentum_ratio_from_subtract_division` | `fallback.rs` | parent wrapper + child implementation | Requires subtract shape, numeric offset `1.0`, and left division shape |
| `legacy_latest_lookback_pair` | `fallback.rs` | child private | Recovers current/lookback source-span pair for momentum fallback |

The extraction may add a child-private helper to build
`IndicatorBinding::Momentum` from `SourceSpanMatch` if it only removes local
duplication and does not change behavior.

## Parent-Owned Exclusions

The extraction must not move or rewrite:

- `resolve_manual_formula_binding` recognizer order except for delegating the
  momentum branches through child wrappers;
- `resolve_boundary_lookback_source_span`, because it is a public(crate)
  boundary fallback surface queued as a later child;
- MACD, ZScore, moving-average, RSI closed child, and fallback tests;
- binding source decoders, data source reference resolution, semantic bridge,
  and shared expression number helper ownership.

## White-Box Contract

| Field | Contract |
| --- | --- |
| Input | `Expr`, `BindingEnv`, and `DataSourceConfig` slice |
| Output | `IndicatorBinding::Momentum` or no-match |
| Parent API | Keep all three public(crate) momentum helpers callable from `fallback.rs` |
| Child visibility | Child implementation functions may be `pub(super)` only where parent wrappers require delegation |
| Error behavior | Preserve `anyhow::Result` propagation from source decoding and data source resolution |
| Trading semantics | Preserve source identity and lookback span exactly |
| Recognizer order | Preserve MACD before direct momentum in subtract branches and ZScore before momentum ratio in divide branches |

## Equivalence Evidence Required Next

BE-002JI-02 must run:

- `cargo fmt --all --check`;
- `cargo test -p quantscript`;
- `cargo check -p quantpilot`;
- `git diff --check`;
- UTF-8, full feature tree, and matrix governance checks.

## Next Step

BE-002JI-02 `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_momentum_formula` actual_extraction.
