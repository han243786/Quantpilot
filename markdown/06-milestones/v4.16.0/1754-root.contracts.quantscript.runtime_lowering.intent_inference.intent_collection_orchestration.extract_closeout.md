# v4.16.0 root.contracts.quantscript.runtime_lowering.intent_inference.intent_collection_orchestration extract closeout

> Batch: BE-002IX-02
> Node: `root.contracts.quantscript.runtime_lowering.intent_inference.intent_collection_orchestration`
> Parent: `root.contracts.quantscript.runtime_lowering.intent_inference`
> Stage: `actual_extraction`
> Movement: Production Rust movement.
> governance_next_authority: active

---

## Extraction Summary

`intent_collection_orchestration` has been extracted into:

- `quantscript/src/lowering/intents/intent_collection_orchestration.rs`

The parent `quantscript/src/lowering/intents.rs` now keeps the public(crate) `infer_intents` facade and delegates top-level intent traversal into the child module.

## Symbols Moved

Moved into the child:

- implementation body behind `infer_intents`
- `collect_intents_from_stmts`

The child calls parent-owned condition inference, legacy emit fallback, merge, and relation helpers through the parent-child boundary. Specialized intent builders remain parent-owned.

## Parent Boundary Kept

The parent remains owner for:

- `canonicalize_data_sources`
- `inferred_agent_params`
- `infer_intents` public(crate) facade
- `intents_from_condition`
- `single_indicator_intent`
- `spread_intent_from_condition`
- `moving_average_ratio_intent`
- `legacy_intent_from_emit`
- `merge_intent`
- relation/id helpers
- data-source canonicalization and spread operand helpers

## Validation

Passed after extraction:

- `cargo fmt`
- `cargo fmt --check`
- `cargo test -p quantscript` (88 tests)
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8, full feature tree, and matrix governance checks

## Next Step

BE-002IX-03 single_leaf_closeout.
