# v4.16.0 runtime_facade_coordinator.portfolio_projection extract closeout

> Batch: BE-001YT-02
> Node: `root.contracts.runtime_support.runtime_facade_coordinator.portfolio_projection`
> Parent: `root.contracts.runtime_support.runtime_facade_coordinator`
> Stage: `actual_extraction`
> Movement: Code movement complete.

## Extracted Files

- `qrpc_runtime/src/runtime_facade_coordinator/portfolio_projection.rs`

## Code Movement

The portfolio projection functions moved out of `qrpc_runtime/src/runtime_facade_coordinator.rs` into a child module:

- `RuntimeCoordinator::portfolio_update_event`
- `RuntimeCoordinator::refresh_portfolio_state`
- `portfolio_equity_estimate`
- `quote_price_map`

`RuntimeCoordinator::refresh_portfolio_state` and `portfolio_equity_estimate` are now `pub(super)` so sibling coordinator child modules can keep using the parent-mediated projection surface.

## Public API Equivalence

Public calls remain available as `RuntimeCoordinator::*`.

The crate-root facade remains unchanged through `qrpc_runtime/src/lib.rs`.

## Boundary Result

The child owns portfolio update events, quote maps, mark-price refresh, exposure projection, leverage calculation, and equity estimates.

The parent still owns:

- `RuntimeCoordinator` struct definition
- `ConfigGenerationEntry`
- debug projection
- child module declarations
- coordinator tests

## Equivalence Gates

Required gates for this batch:

- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-001YT-03 `root.contracts.runtime_support.runtime_facade_coordinator.portfolio_projection` single_leaf_closeout.
