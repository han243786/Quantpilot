# v4.16.0 root.contracts.core_ir.v4_contracts.static_contract_bundle parent residual judgment selects static_validation

> Batch: BE-001VX-01
> Node: `root.contracts.core_ir.v4_contracts.static_contract_bundle`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.static_contract_bundle` selects
`root.contracts.core_ir.v4_contracts.static_contract_bundle.static_validation` as the next child.

The selected child owns only:

- `V4StaticContractBundle::validate_static_contract`

It does not own `V4StaticContractBundle`, default construction, static bundle version default, compile-time capability report builders, compatibility bridge code, tests, sample builders, or sibling contract implementations.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `static_validation` is a named behavior child for bundle-level validation. |
| parent_child_communication_kept | pass | The child can implement the method on the parent bundle type without changing the public facade. |
| equivalence_baseline_freezable | pass | Validation order, delegated validations, metrics calculation, and error strings are finite. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | satisfied | `V4StaticContractBundle::validate_static_contract` is public bundle API behavior. |
| state_machine_phase | no | This is static contract validation, not state topology. |
| strategy_branch | no | This is not strategy behavior. |
| independent_failure_mode | pass | Static validation failures are independent from bundle DTO/default construction. |
| reuse_pressure | guarded | Capability report builders reuse this validator through the public bundle method. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The child owns one public method and a coherent validation phase. |
| communication_cost_rises | no | One nested validation module keeps all bundle validation together. |
| local_proof_missing | no | Existing qrpc-core-ir tests exercise bundle validation and capability report paths. |
| line_count_only | no | Selection is based on validation behavior ownership. |

leaf_split_decision_result

`continue_split: true`

Selected next child:

`root.contracts.core_ir.v4_contracts.static_contract_bundle.static_validation`

next_recursive_step

BE-001VY-01 root.contracts.core_ir.v4_contracts.static_contract_bundle.static_validation baseline_plan
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/static_contract_bundle.rs`

**Markers**:
- `BE-001VX-01`

**Next step**:
BE-001VY-01 root.contracts.core_ir.v4_contracts.static_contract_bundle.static_validation baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
