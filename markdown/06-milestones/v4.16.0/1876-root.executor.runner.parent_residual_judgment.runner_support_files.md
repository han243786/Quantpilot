# v4.16.0 root.executor.runner parent residual judgment selects runner_support_files

> Batch: BE-002LQ-01
> Node: `root.executor.runner`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select next child:

`root.executor.runner.runner_support_files`

## Parent Residual Scan

Closed runner children:

- `provider_order_routes`
- `strategy_lifecycle_routes`
- `market_stream_routes`
- `v4_deploy_support`
- `provider_order_support`

Remaining runner residuals:

- `runner_support_files`

## Why This Child

`runner_support_files` is the remaining runner support cluster. The files are
already physically separated, but they still need governance ownership closure
before `root.executor.runner` can be closed as a parent.

## Proposed Ownership

| Field | Value |
| --- | --- |
| Candidate child | `runner_support_files` |
| Existing files | `live_runner.rs`, `migration_api.rs`, `kline_buffer.rs`, `ws_client.rs`, `okx_rest.rs` |
| First movement | No required code movement in the baseline; freeze the existing support-file ownership and decide whether any file requires separate recursive treatment |
| Parent retained | process startup, route registration, `append_audit`, `internal_error`, closed route/helper children |

## Exclusions

- Do not reopen closed route/helper children.
- Do not split support files before the baseline records their current
  ownership and residual risk.
- Do not introduce release-transition links.

## Next Step

BE-002LR-01 `root.executor.runner.runner_support_files` baseline_plan.
