# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation equivalence baseline and extraction plan

> Batch: BE-001UI-01
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract`
> Stage: `baseline_plan`
> Movement: No code movement.

---

## Summary

This baseline freezes graph-level static validation behavior before extraction.

The selected child may own:

- `impl V4MachineGraphContract`
- `V4MachineGraphContract::validate_static_contract`
- `V4MachineGraphContract::validate_graph_acyclic`
- `V4MachineGraphContract::validate_event_catalog`
- `V4MachineGraphContract::validate_risk_plane`
- `machine_event_party_allowed`

No graph schema DTO, event catalog DTO, traversal helper, default helper, complexity metric, bridge builder, or test fixture moves in this baseline.

## Frozen Validation Surface

The next extraction must preserve:

- graph schema version check;
- graph ID required check;
- at least one top-level machine required check;
- top-level machine ID required and duplicate checks;
- nested duplicate machine checks across top-level and child machines;
- per-machine `validate_static_contract` delegation and error prefixing;
- edge ID required and duplicate checks;
- edge event type required check;
- edge source/target unknown machine checks;
- edge self-loop rejection;
- graph acyclicity detection and `machine graph must be acyclic, cycle: ...` formatting;
- graph event catalog presence requirement when referenced events exist;
- event catalog local validation delegation;
- undeclared event rejection;
- transition consumer and source emitter checks;
- action emitted event allowed-emitter checks;
- edge source emitter and target consumer checks;
- execution graph risk-plane requirement;
- risk-plane required flag check;
- required risk-plane machine list check;
- risk-plane min-priority check;
- risk-plane machine unknown/template/priority checks;
- execution inbound-edge risk-plane origin checks;
- execution machine missing risk-plane inbound edge checks.

## Allowed BE-001UI-02 Movement

The next extraction step may:

- create `qrpc_core_ir/src/v4/machine_graph_contract/static_validation.rs`;
- add `mod static_validation;` to `qrpc_core_ir/src/v4/machine_graph_contract.rs`;
- move only `impl V4MachineGraphContract` and `machine_event_party_allowed`;
- import graph parent names with `super::{...}`.

The child may depend on graph-parent names:

- `collect_machine_family`
- `MachineTemplateKind`
- `V4MachineContract`
- `V4MachineGraphContract`
- `V4_MACHINE_GRAPH_CONTRACT_VERSION`
- `V4_RISK_PLANE_MIN_PRIORITY`

The child must keep local imports:

- `std::collections::{BTreeMap, BTreeSet}`

## Forbidden BE-001UI-02 Movement

The next extraction step must not move:

- `V4MachineGraphContract` field definitions;
- `MachineGraphEdge`, `MachineGraphEdgeActivation`, `MachineGraphRiskPlane`;
- event catalog DTOs or local catalog validation;
- `collect_machine_family`;
- `machine_nested_depth`;
- default helper function bodies;
- complexity metric code;
- bridge builders;
- tests.

It must not alter validation branch order, error strings, recursive traversal behavior, or event party semantics.

## Equivalence Proof Plan

BE-001UI-02 must pass:

- `cargo fmt --check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-001UI-02 `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation` extract_closeout.
