# v4.16.0 root.contracts.runtime_support.risk_support parent residual judgment selects action_clamp_helpers

> Batch: BE-002BW-01
> Node: `root.contracts.runtime_support.risk_support`
> Candidate: `root.contracts.runtime_support.risk_support.action_clamp_helpers`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.

---

## Decision

Select `root.contracts.runtime_support.risk_support.action_clamp_helpers` as the next child.

## Current Closed Children

- `root.contracts.runtime_support.risk_support.event_payload_projection`
- `root.contracts.runtime_support.risk_support.direction_cross_constraints`

## Candidate Scope

This child covers action-list clamping helpers:

- `clamp_actions`
- `clamp_sell_actions_to_inventory`
- `clamp_buy_actions_to_total_headroom`
- `clamp_buy_actions_to_exchange_headroom`
- `clamp_buy_actions_to_symbol_net_exposure`
- `clamp_buy_actions_to_portfolio_net_exposure`

## Rationale

The candidate is cohesive because these helpers mutate proposed action lists after risk policy evaluation. They share inventory, total headroom, exchange headroom, symbol exposure, and portfolio exposure constraints for action-list sizing.

## Preserved Boundaries

- provider/evaluate surface remains parent-owned;
- closed event payload projection remains outside this child;
- closed direction/cross-symbol constraints remain outside this child;
- portfolio target clamp helpers remain outside this child;
- exposure math helpers remain parent-owned;
- tests remain outside this child;
- no sibling shortcut may be introduced.

## Next Step

BE-002BX-01 `root.contracts.runtime_support.risk_support.action_clamp_helpers` baseline_plan.
