# v4.16.0 root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.basic_runtime_smoke_tests extract closeout

> Batch: BE-002II-02
> Node: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.basic_runtime_smoke_tests`
> Parent: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness`
> Stage: `actual_extraction`
> Movement: Test-only Rust movement.
> governance_next_authority: active

---

## Extraction Summary

`basic_runtime_smoke_tests` has been extracted into:

- `quantscript/src/lowering/orchestrator/integration_test_harness/basic_runtime_smoke_tests.rs`

The parent test harness now declares `mod basic_runtime_smoke_tests;` and no longer directly owns the basic MA/RSI/helper/exchange runtime smoke tests.

## Symbols Moved

- `MA_SCRIPT`
- `RSI_SCRIPT`
- `lowers_ma_cross_into_runtime_config`
- `lowers_ma_cross_with_aliased_data_binding_into_consistent_input_ids`
- `lowers_rsi_thresholds_into_single_runtime_intent`
- `lowered_script_runs_in_runtime`
- `lowers_user_defined_helper_function_calls`
- `lowers_fetch_exchange_argument_into_data_source`

Moved child-only imports:

- `qrpc_compiler::compile_runtime_protocol_config`
- `qrpc_core::{Exchange, IntentKind}`
- `qrpc_runtime::RuntimeCoordinator`

## Parent Boundary Kept

The parent remains owner for:

- `rejects_semantic_errors_before_runtime_lowering`
- child module declarations

The parent production owner remains `quantscript/src/lowering/orchestrator.rs`; no production lowering code moved.

## Validation

Passed after extraction:

- `cargo fmt --check`
- `cargo test -p quantscript` (88 tests)
- `cargo check -p quantpilot`
- `git diff --check`

## Next Step

BE-002II-03 single_leaf_closeout.
