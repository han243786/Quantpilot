# v4.16.0 root.contracts.qrpc_core.runtime_io_contract single leaf closeout

> Batch: BE-001SB-03
> Node: `root.contracts.qrpc_core.runtime_io_contract`
> Parent: `root.contracts.qrpc_core`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core.runtime_io_contract` has been evaluated after BE-001SB-02 extraction.

Decision:

`continue_split: true`

The node now has a stable physical owner, but it is not yet a compact leaf. The extracted module is 431 lines and owns multiple public DTO families across market data, decision flow, execution IO, portfolio state, runtime output, and backtest output.

## Split Rule Evaluation

| Rule | Result |
| --- | --- |
| Physical owner | Continue. The surface is isolated in `qrpc_core/src/runtime_io_contract.rs`, so child splitting can now proceed safely under one parent facade. |
| Public method count | Continue. The module owns many public DTOs plus `PortfolioState::new` and `PortfolioState::debug_assert_invariants`. |
| Mixed responsibility | Continue. Market data IO, decision flow, execution IO, portfolio state, runtime output, and backtest output are distinct DTO families. |
| State ownership | Continue with caution. `PortfolioState` is a DTO/projection owner with helper behavior and should be isolated before closeout. |
| Side effects | Stop for side-effect risk. No IO, persistence, locks, async lifecycle, or external calls are involved. |
| Parent-mediated dependency | Covered. The qrpc-core crate root re-exports this parent, and future children must communicate through the runtime IO parent facade. |
| Future reopen rule | Required after child extraction. Each child family must close or split based on its own white-box boundary. |

## Proposed Child Queue

| Candidate child | Owns | Initial judgment |
| --- | --- | --- |
| `contracts.qrpc_core.runtime_io_contract.market_data_io` | `RawKline`, `RawQuote`, `NormalizedKline`, `KlineSeriesSnapshot`, `QuoteSnapshot`, `NormalizedMarketData` | Select first. Compact market-data DTO family. |
| `contracts.qrpc_core.runtime_io_contract.decision_flow` | `IntentSignal`, `ProposedAction`, `TargetWeight`, `PortfolioTarget`, `PortfolioTargetDecision`, `AgentDecision`, `RiskDecision` | Queue. Decision/action DTO family. |
| `contracts.qrpc_core.runtime_io_contract.execution_io` | `SimOrder`, `ExecutionPlan`, `FillReport`, `OpenOrder`, `FillResult` | Queue. Execution request/result DTO family. |
| `contracts.qrpc_core.runtime_io_contract.portfolio_state` | `Position`, `ExchangeExposure`, `PortfolioState`, `PortfolioState::new`, `PortfolioState::debug_assert_invariants` | Queue. Contains helper behavior and must be handled serially. |
| `contracts.qrpc_core.runtime_io_contract.runtime_output` | `RuntimeEvent`, `RuntimeCycleOutput`, `SessionOutput` | Queue. Runtime event/cycle/session output DTO family. |
| `contracts.qrpc_core.runtime_io_contract.backtest_output` | `BacktestEquityPoint`, metric groups, `BacktestSummary`, `PeriodReturn`, `BacktestOutput` | Queue. Larger report DTO family, likely worth its own child. |

## White-Box Boundary

| Input | Processing owner | Output |
| --- | --- | --- |
| Runtime market data, decision, execution, portfolio, event, session, and backtest DTO proposals | `contracts.qrpc_core.runtime_io_contract` | Stable parent facade for runtime IO children and qrpc-core public re-exports |

The parent may describe and guard:

- runtime IO child module declarations;
- runtime IO public re-export surface;
- parent-mediated child ownership boundaries;
- no-release-transition sibling dependency rules.

## Non-Claims

This closeout does not claim:

- runtime IO child families have been extracted;
- RFC execution contracts, crate-root tests, qrpc-core parent residuals, or downstream behavior changed;
- runtime IO parent closeout is complete;
- closed qrpc-core children or release transition changed.

## Proof

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-001SC-01 `root.contracts.qrpc_core.runtime_io_contract` parent_residual_judgment selects `market_data_io`.
