# v4.16.0 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation actual extraction complete

> Batch: BE-001WK-02
> Node: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation`
> Parent: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge`
> Stage: `extract_closeout`
> Movement: Extracted the Core IR validation pipeline into a nested private bridge child module.

---

## Summary

root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation actual extraction complete

BE-001WK-02 created `qrpc_core_ir/src/v4/core_ir_compat_bridge/core_ir_validation.rs` and moved only the Core IR validation pipeline:

- `validate_core_ir_for_v4_bridge`
- `collect_core_ir_node_ids`
- `insert_core_ir_node_id`
- `validate_core_ir_edges_for_v4_bridge`
- `validate_core_ir_references_for_v4_bridge`
- `validate_core_ir_scalar_expr_refs`
- `validate_core_ir_series_expr_refs`
- `validate_core_ir_custom_value_refs`

The parent bridge module now declares `mod core_ir_validation;` and imports `validate_core_ir_for_v4_bridge` for the public bridge entrypoint. Report DTOs, report validation, graph/machine/event builders, bridge metadata helpers, id sanitization, bridge defaults, and diagnostic push helper stayed in the parent bridge module.

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/core_ir_compat_bridge.rs`
- `qrpc_core_ir/src/v4/core_ir_compat_bridge/core_ir_validation.rs`

**Markers**:
- `BE-001WK-02`

**Next step**:
BE-001WK-03 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation single_leaf_closeout

---

## Equivalence Evidence

- `cargo fmt -p qrpc-core-ir`: passed.
- `cargo test -p qrpc-core-ir`: passed, 54 tests.
- `cargo check -p quantpilot`: passed.

## Residual Judgment Input

BE-001WK-03 should decide whether this validation child is terminal. Initial evidence leans toward `stop_split: true`: it is a single ordered validation pipeline, all helper functions share the same diagnostic sink, and further split would mostly separate expression-reference helpers from their caller without independent public ownership.

---

## Gates

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `cargo check -p quantpilot`
