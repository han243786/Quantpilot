# v4.16.0 root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_zscore_formula baseline plan

> Batch: BE-002JM-01
> Node: `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_zscore_formula`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Objective

Freeze the manual ZScore fallback boundary before extraction.

The next movement may extract only ZScore fallback recognition from
`quantscript/src/lowering/fallback.rs` into a child module. It must preserve:

- direct `ResolvedManualIndicatorFormula::ZScore` handling;
- divide numerator shape validation for `(current - mean) / stddev`;
- resolved ZScore source/window matching;
- legacy current/mean/stddev operand recovery;
- no-match behavior for unsupported shapes.

## Frozen Rust Symbols

| Symbol | Current owner | Planned owner | Contract |
| --- | --- | --- | --- |
| `manual_zscore_from_expr` | `fallback.rs` | parent wrapper + child implementation | Returns `IndicatorBinding::ZScore` or `None` |
| `match_zscore_operands` | `fallback.rs` | parent wrapper + child implementation | Returns `SourceSpanMatch` or `None` |
| `legacy_zscore_operands` | `fallback.rs` | child private | Recovers source/window from current, mean, and stddev operands |

## Parent-Owned Exclusions

The extraction must not move or rewrite:

- `resolve_manual_formula_binding` recognizer order except for delegating the
  ZScore branch through child wrappers;
- moving-average/window fallback, boundary lookback public helper, fallback
  tests, and closed RSI/momentum/MACD children;
- shared window aggregate matching helpers used by moving-average;
- binding source decoders and data source reference resolution ownership beyond
  child calls.

## White-Box Contract

| Field | Contract |
| --- | --- |
| Input | full expression, divide operands, `BindingEnv`, and data sources |
| Output | `IndicatorBinding::ZScore`, `SourceSpanMatch`, or no-match |
| Parent API | Keep public(crate) ZScore helpers callable from `fallback.rs` |
| Child visibility | Child implementation functions may be `pub(super)` only where parent wrappers require delegation |
| Error behavior | Preserve `anyhow::Result` propagation from source and window binding decoding |
| Trading semantics | Preserve source identity and window span exactly |
| Recognizer order | Preserve ZScore before momentum ratio and moving-average in divide branches |

## Equivalence Evidence Required Next

BE-002JM-02 must run:

- `cargo fmt --all --check`;
- `cargo test -p quantscript`;
- `cargo check -p quantpilot`;
- `git diff --check`;
- UTF-8, full feature tree, and matrix governance checks.

## Next Step

BE-002JM-02 `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_zscore_formula` actual_extraction.
