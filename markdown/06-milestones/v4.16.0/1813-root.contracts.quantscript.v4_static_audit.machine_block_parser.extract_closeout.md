# v4.16.0 root.contracts.quantscript.v4_static_audit.machine_block_parser extract closeout

> Batch: BE-002KE-02
> Node: `root.contracts.quantscript.v4_static_audit.machine_block_parser`
> Stage: `actual_extraction`
> Movement: Rust code movement complete.
> governance_next_authority: active

---

## Movement Summary

`quantscript/src/v4_static_audit/machine_block_parser.rs` now owns the machine
block parser boundary.

Moved implementation:

- `ParsedMachine`;
- `parse_machine_block` implementation body;
- machine header validation;
- template and priority validation;
- machine-level state, group, memory, transition, and nested-machine delegation;
- `V4MachineContract` assembly with cache, silence, recovery, priority, and
  metadata defaults.

The parent `quantscript/src/v4_static_audit.rs` now declares
`mod machine_block_parser;` and keeps a private wrapper named
`parse_machine_block`.

## Preserved Boundaries

- Public API unchanged.
- `static_document_parser` and nested state parsing still call the parent
  `parse_machine_block` wrapper.
- `parse_state`, `parse_state_block`, `parse_memory`, `parse_transition`,
  `skip_block`, `empty_child_machine`, and `parse_state_group` remain
  parent-owned.
- Edge and risk-plane parsers remain parent-owned.
- Capability/type parser helpers remain in the closed `capability_type_parser`
  child.
- Public report DTOs and local static-audit tests remain parent-owned.
- No release-transition link was introduced.

## Gates

| Gate | Result |
| --- | --- |
| `cargo fmt --all --check` | PASS |
| `cargo test -p quantscript` | PASS, 88 tests |
| `cargo check -p quantpilot` | PASS, no warnings |
| `git diff --check` | PASS |
| `check-utf8.ps1` | PASS |
| `check-full-feature-tree.ps1` | PASS |
| `check-matrix-governance.ps1` | PASS |

## Next Step

BE-002KE-03 `root.contracts.quantscript.v4_static_audit.machine_block_parser`
single_leaf_closeout.
