# v4.16.0 root.contracts.core_ir.v4_contracts.qs_state_machine_profile actual extraction complete

> Batch: BE-001VE-02
> Node: `root.contracts.core_ir.v4_contracts.qs_state_machine_profile`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `extract_closeout`
> Movement: Code movement completed.

---

## Summary

`root.contracts.core_ir.v4_contracts.qs_state_machine_profile` actual extraction is complete.

Implemented movement:

- `qrpc_core_ir/src/v4.rs` now declares `mod qs_state_machine_profile;`
- `qrpc_core_ir/src/v4.rs` re-exports the child through `pub use qs_state_machine_profile::*;`
- `qrpc_core_ir/src/v4/qs_state_machine_profile.rs` owns the QS profile policy structs, defaults, and validation
- private serde default helpers moved with the child

Equivalence notes:

- public v4 surface remains unchanged through the parent re-export
- validation error strings and branch order remain frozen from BE-001VE-01
- runtime mode, QS type system, plugin governance, version manifest, reproducibility, complexity budget, capability,
  bridge, venue, machine, and graph contracts remain outside this child

Verification:

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir` (54 passed)

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs`
- `qrpc_core_ir/src/v4/qs_state_machine_profile.rs`

**Markers**:
- `BE-001VE-02`

**Next step**:
BE-001VE-03 root.contracts.core_ir.v4_contracts.qs_state_machine_profile single_leaf_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
