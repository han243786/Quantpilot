# v4.16.0 root.contracts.runtime_support.risk_support parent residual judgment selects event_payload_projection

> Batch: BE-002BQ-01
> Node: `root.contracts.runtime_support.risk_support`
> Candidate: `root.contracts.runtime_support.risk_support.event_payload_projection`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.

---

## Decision

Select `root.contracts.runtime_support.risk_support.event_payload_projection` as the next child.

## Candidate Scope

This child covers the risk-event payload and summary projection helpers:

- `build_risk_event_payload`
- `risk_explanation_summary`
- `reason_code_name`
- `action_stats`
- `target_weight_stats`

## Rationale

The candidate is a low-risk first extraction because it only projects existing risk decision and portfolio/action state into JSON/event-facing summaries. It does not own risk mode enforcement, action clamp mutation, portfolio target clamp mutation, cross-symbol constraints, provider traits, or the public evaluation entrypoint.

## Preserved Boundaries

- `RiskCheckerProvider::evaluate` remains parent-owned;
- `evaluate_risk_decision` remains parent-owned;
- action and portfolio target clamps remain outside this child;
- direction and cross-symbol constraints remain outside this child;
- exposure math helpers remain outside this child;
- tests remain outside this child;
- no sibling shortcut may be introduced.

## Next Step

BE-002BR-01 `root.contracts.runtime_support.risk_support.event_payload_projection` baseline_plan.
