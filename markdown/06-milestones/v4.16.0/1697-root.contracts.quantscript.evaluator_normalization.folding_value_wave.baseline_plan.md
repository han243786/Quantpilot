# v4.16.0 root.contracts.quantscript.evaluator_normalization.folding_value_wave baseline plan

> Batch: BE-002HP-01
> Node: `root.contracts.quantscript.evaluator_normalization.folding_value_wave`
> Parent: `root.contracts.quantscript.evaluator_normalization`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Scope

Freeze the evaluator folding and value projection wave before movement.

Current owner:

- `quantscript/src/evaluator/mod.rs`

Planned child:

- `quantscript/src/evaluator/folding_value_wave.rs`

The next movement may create the child module and move only the functions listed below.

## Functions To Move

The planned child owns these functions:

- `expr_iterable_items`
- `fold_unary`
- `fold_index`
- `fold_slice`
- `fold_binary`
- `fold_builtin_call`
- `fold_member_builtin`
- `fold_identifier_builtin`
- `is_fetch_expr`
- `expr_length`
- `expr_sum`
- `expr_mean`
- `expr_min`
- `expr_max`
- `expr_variance`
- `expr_stddev`
- `expr_first`
- `expr_last`
- `expr_numbers`
- `expr_number`
- `expr_integer`
- `expr_bool`
- `expr_string`
- `list_index`
- `list_slice`
- `normalize_slice_bound`

## Parent-Mediated Access

The child may expose `pub(super)` functions required by the parent:

- `expr_iterable_items`
- `expr_bool`
- `expr_number`
- `fold_unary`
- `fold_index`
- `fold_slice`
- `fold_binary`
- `fold_builtin_call`

Private helpers should remain private inside the child where possible.

The parent may import through:

- `mod folding_value_wave;`
- `use folding_value_wave::{...};`

## Equivalence Requirements

The next extraction must preserve:

- numeric, bool, string, list, index, and slice folding;
- builtin member calls such as `len`, `sum`, `mean`, `stddev`, `first`, `last`, `ok`, and `retryable`;
- identifier builtin calls such as `abs`, `sqrt`, `sum`, `mean`, `stddev`, `variance`, `first`, `last`, and `pow`;
- fetch/get_data health helper behavior;
- division/modulo zero diagnostic behavior;
- helper inline execution behavior that depends on `expr_bool` and `expr_iterable_items`;
- public `normalize_script_module` behavior.

## Forbidden Movement

The next movement must not move:

- `normalize_script_module`;
- `EvalContext`;
- `normalize_function`, `normalize_block`, `normalize_stmt`, `normalize_expr`;
- `maybe_inline_function`, `should_skip_inline`, or helper execution functions;
- `ExecOutcome`, `IfStmtParts`, `BlockSnapshot`;
- evaluator test module;
- any file outside `quantscript/src/evaluator/` and governance indexes.

## Expected Validation

The extraction must pass:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002HP-02 `root.contracts.quantscript.evaluator_normalization.folding_value_wave` extract_closeout.
