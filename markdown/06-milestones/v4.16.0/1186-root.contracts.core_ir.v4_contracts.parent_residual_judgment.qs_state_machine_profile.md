# v4.16.0 root.contracts.core_ir.v4_contracts parent residual judgment selects qs_state_machine_profile

> Batch: BE-001VD-01
> Node: `root.contracts.core_ir.v4_contracts`
> Parent: `root.contracts.core_ir`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts` selects
`root.contracts.core_ir.v4_contracts.qs_state_machine_profile` as the next child.

The selected child owns the QS state-machine profile contract family currently in `qrpc_core_ir/src/v4.rs`:

- `QsStateMachineProfile`
- `QsStatePolicy`
- `QsActionBlockPolicy`
- `QsMemoryPolicy`
- `QsEventPolicy`
- `QsPriorityPolicy`
- `QsRiskPlanePolicy`
- profile defaults and `QsStateMachineProfile::validate_static_contract`

The selected child may depend on shared v4 schema constants and machine template enums, but does not own machine
contract schema, machine graph contract schema, runtime mode contract, QS type system contract, plugin governance,
version manifest, reproducibility, complexity budget, capability reports, venue matrix, bridge builders, or tests.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `qs_state_machine_profile` is a named white-box contract family. |
| parent_child_communication_kept | pass | The child can import shared constants/types from the v4 parent surface and return validation errors locally. |
| equivalence_baseline_freezable | pass | Profile defaults and validation error branches are finite and covered by qrpc-core-ir tests. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | The contract remains public through the v4 module re-export surface. |
| state_machine_phase | pass | The profile defines QS state-machine policy and validation. |
| strategy_branch | guarded | It constrains state/action/memory/event/priority/risk policy families. |
| independent_failure_mode | pass | Profile validation failures are independent from runtime mode, QS type system, plugin, and venue contracts. |
| reuse_pressure | guarded | Existing tests and builders use the profile through the v4 surface. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The profile family owns multiple policy structs plus defaults and validation. |
| communication_cost_rises | no | A single child module can preserve the v4 public surface through `pub use`. |
| local_proof_missing | no | Existing qrpc-core-ir tests cover QS profile defaults and validation. |
| line_count_only | no | Selection is based on contract-family ownership. |

leaf_split_decision_result

`continue_split: true`

Selected next child:

`root.contracts.core_ir.v4_contracts.qs_state_machine_profile`

next_recursive_step

BE-001VE-01 root.contracts.core_ir.v4_contracts.qs_state_machine_profile baseline_plan
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs`

**Markers**:
- `BE-001VD-01`

**Next step**:
BE-001VE-01 root.contracts.core_ir.v4_contracts.qs_state_machine_profile baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
