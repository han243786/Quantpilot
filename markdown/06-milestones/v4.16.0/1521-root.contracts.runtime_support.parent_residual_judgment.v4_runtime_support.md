# v4.16.0 root.contracts.runtime_support parent residual judgment - v4_runtime_support

> Batch: BE-002DG-01
> Node: `root.contracts.runtime_support`
> Selected child: `root.contracts.runtime_support.v4_runtime_support`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.

---

## Decision

Select `root.contracts.runtime_support.v4_runtime_support` as the next runtime-support child.

## Evidence

Closed runtime-support children include:

- `runtime_facade_coordinator`
- `data_module`
- `intent_module`
- `agent_module`
- `core_ir_evaluator`
- `execution_module`
- `fill_engine`
- `risk_support`
- `sandbox_replay_timeline`

The runtime-support baseline queue names `v4_runtime_support` immediately after `sandbox_replay_timeline`. The current v4 runtime boundary is concentrated in:

- `qrpc_runtime/src/v4_runtime.rs`
- `qrpc_runtime/src/v4_simulated_execution.rs`
- `qrpc_runtime/src/v4_runtime_types.rs`
- `qrpc_runtime/src/v4_runtime_tests.rs`

This boundary owns v4 paper runtime session orchestration, simulated execution, runtime event processing, v4 runtime DTOs/types, and local v4 runtime tests. It is large enough to require its own baseline before movement.

## Boundary

The next baseline must freeze the v4 runtime public surface before any movement. It must not touch live execution, plugin runtime support, ops compatibility support, backtest metrics, QuantScript, plugin metadata, executor, backend routes, frontend callers, or release-transition optimization.

Sibling runtime-support children already closed remain parent-mediated. No v4 child may shortcut into data, agent, risk, execution, fill, or sandbox internals.

## Dependency Rule

Parent-child communication remains strict. The v4 runtime support child must communicate through the runtime-support parent facade and existing public/provider surfaces. Horizontal sibling connection proposals remain forbidden unless the developer explicitly declares release transition.

## Next Step

BE-002DH-01 `root.contracts.runtime_support.v4_runtime_support` baseline_plan.
