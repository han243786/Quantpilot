# v4.16.0 root.contracts.quantscript.analysis_diagnostics.fetch_lookback_warning_gate extract closeout

> Batch: BE-002HE-02
> Node: `root.contracts.quantscript.analysis_diagnostics.fetch_lookback_warning_gate`
> Parent: `root.contracts.quantscript.analysis_diagnostics`
> Stage: `actual_extraction`
> governance_next_authority: active

---

## Result

`quantscript/src/analysis_diagnostics/fetch_lookback_warning_gate.rs` now owns the QuantScript fetch lookback warning diagnostic.

Moved from `quantscript/src/analysis_diagnostics/analysis.rs`:

- `collect_fetch_lookback_warnings`
- `collect_fetch_lookback_warnings_from_stmts`
- `collect_fetch_lookback_warnings_from_expr`

## Parent Boundary

`quantscript/src/analysis_diagnostics/analysis.rs` remains the parent coordinator. It still owns `ScriptAnalysis`, `analyze_script_module`, diagnostic invocation order, and the remaining diagnostic families.

The parent imports only:

- `collect_fetch_lookback_warnings`

`arg_number_named` remains owned by `warmup_fetch_gate`; the analysis_diagnostics parent exposes it through an internal `pub(in crate::analysis_diagnostics)` bridge so this child does not call the warmup sibling directly.

## Unmoved Families

The extraction did not move:

- strategy fetch/emit presence checks;
- data source map and index bounds checks;
- dead-code emit checks;
- diagnostic DTO/span types;
- unsupported construct, lookahead-window, warmup/fetch, indirect-recursion, and symbol-whitelist children already closed;
- evaluator normalization, runtime lowering, v4 static audit, tests, authoring samples, backend, executor, frontend, plugin metadata, or release transition.

## Validation

Passed:

- `cargo fmt`
- `cargo test -p quantscript` (88 tests)
- `cargo check -p quantpilot`

Docs gates should run before commit:

- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002HE-03 `root.contracts.quantscript.analysis_diagnostics.fetch_lookback_warning_gate` single_leaf_closeout.
