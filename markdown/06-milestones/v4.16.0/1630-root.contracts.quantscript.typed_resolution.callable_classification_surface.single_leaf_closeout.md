# v4.16.0 root.contracts.quantscript.typed_resolution.callable_classification_surface single leaf closeout

> Batch: BE-002GF-03
> Node: `root.contracts.quantscript.typed_resolution.callable_classification_surface`
> Parent: `root.contracts.quantscript.typed_resolution`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Result

`stop_split: true`

The callable classification child is a terminal leaf for the current recursive pass.

## Evidence

Leaf granularity evaluation:

- Path: `quantscript/src/resolve/callable_classification_surface.rs`
- LOC: 410
- Functions: 9
- Public surface: 7
- Domains: 2
- Split decision: `STOP`
- Governance packaging: `stop_split`
- Score: 41
- Terminal leaf control: `terminal_target_150_600`

Gate evidence from the extraction step remains valid:

- `cargo fmt --check` PASS
- `cargo test -p quantscript` PASS, 88 tests
- `cargo check -p quantpilot` PASS

## Stop Split Rationale

The child owns one cohesive taxonomy and registry-seeding surface:

- public callable classifier functions;
- imported helper classifier;
- change smoothing classifier;
- fetch source classifier;
- registry seeding for built-in, imported, indicator, universe, and user callables.

Further splitting would separate tightly coupled lookup tables from the public classification surface and increase governance overhead without creating a stronger independent owner. The file sits inside the target terminal range and should remain one leaf unless a later feature introduces a clearly separate callable taxonomy owner.

## Preserved Boundary

Preserved:

- `quantscript/src/resolve/mod.rs` remains the typed-resolution parent;
- `quantscript/src/resolve/public_type_surface.rs` remains the public DTO/enum child;
- downstream `crate::resolve::*` compatibility remains unchanged;
- resolver orchestration, type inference, expression semantic inference, manual indicator formula inference, lowering, analysis, evaluator, and tests remain outside this leaf.

## Next Step

BE-002GG-01 `root.contracts.quantscript.typed_resolution` parent_residual_judgment.
