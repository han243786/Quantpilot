# v4.16.0 root.contracts.quantscript.analysis_diagnostics.warmup_fetch_gate extract closeout

> Batch: BE-002GY-02
> Node: `root.contracts.quantscript.analysis_diagnostics.warmup_fetch_gate`
> Parent: `root.contracts.quantscript.analysis_diagnostics`
> Stage: `actual_extraction`
> governance_next_authority: active

---

## Result

`quantscript/src/analysis_diagnostics/warmup_fetch_gate.rs` now owns the required warmup and fetch/get_data lookback diagnostic gate.

Moved from `quantscript/src/analysis_diagnostics/analysis.rs`:

- `infer_required_warmup_bars`
- `required_warmup_for_semantic`
- `collect_warmup_diagnostics`
- `collect_warmup_from_function`
- `collect_fetch_lookbacks_from_stmts`
- `collect_fetch_lookbacks_from_expr`
- `fetch_lookback`
- `arg_number_named`

## Parent Boundary

`quantscript/src/analysis_diagnostics/analysis.rs` remains the parent coordinator. It still owns `ScriptAnalysis`, `analyze_script_module`, diagnostic invocation order, and the remaining diagnostic families.

The parent imports only:

- `infer_required_warmup_bars`
- `collect_warmup_diagnostics`
- `fetch_lookback`
- `arg_number_named`

`fetch_lookback` remains parent-visible for the still-parent-owned index-bounds family. `arg_number_named` remains parent-visible for the still-parent-owned fetch lookback warning family. Both are temporary parent-mediated helper edges, not sibling horizontal links.

## Unmoved Families

The extraction did not move:

- strategy fetch/emit presence checks;
- fetch lookback warnings;
- indirect recursion checks;
- data source map and index bounds checks;
- dead-code emit checks;
- symbol whitelist checks;
- diagnostic DTO/span types;
- evaluator normalization, runtime lowering, v4 static audit, tests, authoring samples, backend, executor, frontend, plugin metadata, or release transition.

## Validation

Passed:

- `cargo fmt --check`
- `cargo test -p quantscript` (88 tests)
- `cargo check -p quantpilot`

Docs gates should run before commit:

- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002GY-03 `root.contracts.quantscript.analysis_diagnostics.warmup_fetch_gate` single_leaf_closeout.
