# v4.16.0 root.contracts.quantscript.runtime_lowering.manual_formula_fallback parent residual judgment: manual_rsi_formula

> Batch: BE-002JF-01
> Node: `root.contracts.quantscript.runtime_lowering.manual_formula_fallback`
> Selected child: `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_rsi_formula`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `manual_rsi_formula` as the first internal child under `manual_formula_fallback`.

The parent baseline is frozen. The first cohesive owner is RSI manual fallback recognition: it recognizes the outer `100 - 100 / (1 + RS)` shell, resolves average-gain / average-loss pairs, checks balanced smoothing/source/period semantics, and maps the matched smoothing into `RsiMethod`. This is the first recognizer in `resolve_manual_formula_binding`, so isolating it first preserves recognizer order while reducing the parent surface.

## White-Box Boundary

| Field | Boundary |
| --- | --- |
| Input | candidate expression, `BindingEnv`, and data sources |
| Output | `IndicatorBinding::Rsi`, `SourcePeriodSmoothingMatch`, or no-match |
| Current owner | `quantscript/src/lowering/fallback.rs` |
| Planned owner | `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_rsi_formula` |
| Parent caller | `resolve_manual_formula_binding` and direct `manual_rsi_from_expr` users |
| Exclusions | momentum, MACD, ZScore, moving-average, boundary lookback, and fallback tests |

## Symbols Approved For Next Baseline

The next baseline may freeze movement for:

- `RsiRsPairMatch`
- `manual_rsi_from_expr`
- `match_manual_rsi_formula`
- `match_rsi_rs_pair`
- `match_rs_pair_from_denominator`
- `match_rs_pair_expr`
- `match_balanced_smoothed_change_pair`
- `balanced_smoothed_change_pair_source`

The moved child may call parent/sibling-level lowering helpers only through the parent module boundary and existing upstream modules.

## Split Decision Gate

| Gate | Result | Evidence |
| --- | --- | --- |
| `parent_precision_open` | PASS | `manual_formula_fallback` is frozen as `PRECISION` with 1001 LOC and 44 functions. |
| `recognizer_order_preserved` | PASS | RSI is the first fallback recognizer in `resolve_manual_formula_binding`. |
| `strong_owner_boundary` | PASS | RSI shell parsing and average gain/loss pair matching form a cohesive owner. |
| `sibling_exclusion_clear` | PASS | Momentum, MACD, ZScore, MA/window, boundary lookback, and tests remain outside. |

## Risk Classification

| Risk | Result |
| --- | --- |
| Public API / route / schema | Public(crate) `manual_rsi_from_expr` and `match_manual_rsi_formula` must remain stable |
| Persistence / lock owner | No persistence ownership movement |
| Trading semantics evidence | High; RSI fallback affects inferred runtime indicators |
| Cross-crate behavior | Present through `IndicatorBinding`, `DataSourceConfig`, and typed expression contracts |

## Next Step

BE-002JG-01 `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_rsi_formula` baseline_plan.
