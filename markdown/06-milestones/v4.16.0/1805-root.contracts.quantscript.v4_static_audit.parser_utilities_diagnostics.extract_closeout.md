# v4.16.0 root.contracts.quantscript.v4_static_audit.parser_utilities_diagnostics extract closeout

> Batch: BE-002KA-02
> Node: `root.contracts.quantscript.v4_static_audit.parser_utilities_diagnostics`
> Stage: `actual_extraction`
> Movement: Rust code movement complete.
> governance_next_authority: active

---

## Movement Summary

`quantscript/src/v4_static_audit/parser_utilities_diagnostics.rs` now owns the
shared v4 static-audit parser utility surface:

- `PreparedLine`;
- `prepare_lines`;
- `split_words`;
- `split_csv_words`;
- `diag`.

The parent `quantscript/src/v4_static_audit.rs` now declares
`mod parser_utilities_diagnostics;` and imports those helpers through
`pub(super)` visibility.

## Preserved Boundaries

- Public API unchanged.
- Static document parser remains parent-owned.
- Machine, state, memory, transition, edge, and risk-plane parsers remain
  parent-owned.
- Capability/type parsing and event catalog derivation remain in closed child
  modules.
- DTO definitions outside `PreparedLine` remain parent-owned.
- Local static-audit tests remain parent-owned.
- No release-transition link was introduced.

## Gates

| Gate | Result |
| --- | --- |
| `cargo fmt --all --check` | PASS |
| `cargo test -p quantscript` | PASS, 88 tests |
| `cargo check -p quantpilot` | PASS, no warnings |
| `git diff --check` | pending final rerun |
| `check-utf8.ps1` | pending final rerun |
| `check-full-feature-tree.ps1` | pending final rerun |
| `check-matrix-governance.ps1` | pending final rerun |

## Next Step

BE-002KA-03 `root.contracts.quantscript.v4_static_audit.parser_utilities_diagnostics`
single_leaf_closeout.
