# v4.16.0 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation.reference_validation equivalence baseline and extraction plan

> Batch: BE-001WM-01
> Node: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation.reference_validation`
> Parent: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation`
> Stage: `baseline_plan`
> Movement: No code movement. This document freezes the reference-validation child boundary before extraction.

---

## Summary

root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation.reference_validation equivalence baseline and extraction plan

This child owns Core IR object and expression reference validation after the parent validation module has access to the declared data, indicator, signal, and agent id sets.

## Frozen White-Box Surface

BE-001WM-02 may move only these functions into `qrpc_core_ir/src/v4/core_ir_compat_bridge/core_ir_validation/reference_validation.rs`:

- `validate_core_ir_references_for_v4_bridge`
- `validate_core_ir_scalar_expr_refs`
- `validate_core_ir_series_expr_refs`
- `validate_core_ir_custom_value_refs`

## Forbidden Movement

BE-001WM-02 must not move:

- `validate_core_ir_for_v4_bridge`
- `collect_core_ir_node_ids`
- `insert_core_ir_node_id`
- `validate_core_ir_edges_for_v4_bridge`
- Core IR version and required-field checks
- DAG validation
- any bridge DTO, graph builder, event builder, or diagnostic push helper from the parent bridge module

## Allowed Extraction

BE-001WM-02 may create `qrpc_core_ir/src/v4/core_ir_compat_bridge/core_ir_validation/reference_validation.rs`, add a private nested module declaration in `core_ir_validation.rs`, and call the child through parent-mediated imports. The parent validation entrypoint remains the only caller exposed to the bridge parent.

## Equivalence Baseline

The extraction is equivalent only if these gates pass:

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `cargo check -p quantpilot`

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/core_ir_compat_bridge/core_ir_validation.rs`

**Markers**:
- `BE-001WM-01`

**Next step**:
BE-001WM-02 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation.reference_validation extract_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
