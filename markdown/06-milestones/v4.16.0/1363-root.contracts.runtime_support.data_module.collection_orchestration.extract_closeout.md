# v4.16.0 root.contracts.runtime_support.data_module.collection_orchestration extract closeout

> Batch: BE-001ZE-02
> Node: `root.contracts.runtime_support.data_module.collection_orchestration`
> Stage: `extract_closeout`
> Movement: Actual Rust extraction.

---

## Summary

`collection_orchestration` has been extracted into `qrpc_runtime/src/data_module/collection_orchestration.rs`.

The parent `qrpc_runtime/src/data_module.rs` now declares the child module and keeps the data-module facade, public trait, request/output DTOs, `BuiltinDataModule`, fetch/cache/transport/mock/exchange/normalization helpers, source mapping, quality diagnostics, and tests.

## Code Movement

Moved:

- `impl DataModuleProvider for BuiltinDataModule`
- `collect`
- source de-duplication set
- normalized data accumulation
- `DataUpdated` runtime event assembly
- quality warning/error runtime event assembly

Not moved:

- `DataModuleProvider`
- `DataCollectionRequest`
- `DataCollectionOutput`
- `BuiltinDataModule`
- `fetch_and_normalize`
- cache helpers
- ping/HTTP helpers
- exchange endpoint builders
- payload parsers
- normalization helpers
- mock data helpers
- historical cache helpers
- source mapping
- quality diagnostics
- tests

## Equivalence

Public behavior is unchanged:

- `BuiltinDataModule` still implements `DataModuleProvider`.
- Runtime callers still use the same crate-root re-export surface.
- Provider key, collection ordering, duplicate suppression, data fetch counts, normalized data output, and runtime event payload shapes remain unchanged.
- All communication stays through the `data_module` parent boundary.

## Leaf Granularity Result

The baseline decision remains `SPLIT`.

This extraction created one cohesive child around the provider collection transaction. It did not split the transaction into micro leaves.

## Gates

Passed:

- `cargo fmt`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`

Remaining for commit:

- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-001ZE-03 `root.contracts.runtime_support.data_module.collection_orchestration` single_leaf_closeout.
