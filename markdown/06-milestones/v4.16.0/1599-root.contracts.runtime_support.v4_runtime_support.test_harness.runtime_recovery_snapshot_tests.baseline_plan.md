# v4.16.0 root.contracts.runtime_support.v4_runtime_support.test_harness.runtime_recovery_snapshot_tests baseline plan

> Batch: BE-002FM-01
> Node: `root.contracts.runtime_support.v4_runtime_support.test_harness.runtime_recovery_snapshot_tests`
> Parent: `root.contracts.runtime_support.v4_runtime_support.test_harness`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Freeze the `runtime_recovery_snapshot_tests` equivalence baseline.

The next movement may create a test-only child module for runtime recovery/cache snapshot tests and must preserve all assertions, event expectations, runtime status checks, and parent-mediated fixture access.

## Frozen Test Set

Only these tests may move in the next extraction:

- `v4_runtime_returns_last_cache_and_recovers_soft_silent_machine`
- `v4_runtime_memory_snapshot_records_machine_memory_and_cache`

## Boundary

The extraction must not move:

- simulated order capability rejection tests;
- live actual mode boundary tests;
- capability policy rejection tests;
- risk-plane source rejection tests;
- fixture builders;
- malformed payload/config rejection tests;
- graph/replay scenario tests;
- simulated execution scenario tests;
- production v4 runtime code;
- any release-transition optimization.

## Expected Code Shape

| Path | Expected change |
| --- | --- |
| `qrpc_runtime/src/v4_runtime/test_harness/runtime_recovery_snapshot_tests.rs` | New test-only child with the two frozen tests and `use super::*;`. |
| `qrpc_runtime/src/v4_runtime/test_harness.rs` | Add `mod runtime_recovery_snapshot_tests;` and remove only the moved test bodies. |

No public API, runtime state machine behavior, or production module boundary changes are allowed.

## Equivalence Checks

Run after extraction:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime v4 -- --nocapture`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-002FN-01 `root.contracts.runtime_support.v4_runtime_support.test_harness.runtime_recovery_snapshot_tests` actual_extraction.
