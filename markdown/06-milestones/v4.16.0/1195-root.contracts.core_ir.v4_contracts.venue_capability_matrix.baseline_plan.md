# v4.16.0 root.contracts.core_ir.v4_contracts.venue_capability_matrix equivalence baseline and extraction plan

> Batch: BE-001VI-01
> Node: `root.contracts.core_ir.v4_contracts.venue_capability_matrix`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `baseline_plan`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.venue_capability_matrix` freezes the equivalence baseline for extracting the
venue execution capability matrix family from `qrpc_core_ir/src/v4.rs`.

Frozen public surface:

- `VenueCapabilityMatrix`
- `ExecutionCapabilityKind`
- `CapabilitySupportSource`
- `VenueCapability`
- `V4_FIRST_WAVE_EXECUTION_CAPABILITIES`
- `v4_first_wave_execution_capabilities`
- `unsupported_v4_first_wave_matrix`
- `VenueCapabilityMatrix::validate_static_contract`
- `VenueCapabilityMatrix::capability_entry`
- `VenueCapabilityMatrix::support_source`
- `VenueCapabilityMatrix::require_supported`
- `VenueCapabilityMatrix::require_supported_for_mode`
- `VenueCapabilityMatrix::validate_required_capability_sources`
- `VenueCapabilityMatrix::validate_v4_first_wave_contract`

Frozen behavior:

- the 24 first-wave execution capability list and order remain unchanged
- unsupported first-wave matrix default entries remain Unsupported with empty supported modes and constraints
- schema version, blank venue ID, duplicate capability, missing mode list, unsupported capability, missing capability,
  runtime-mode support, provider-native, runtime-simulated, and required-capability error strings remain unchanged
- venue runtime-mode support checks keep using `RuntimeModeContract::mode_spec` and `RuntimeSettlementAuthority`

Allowed extraction:

- add `mod venue_capability_matrix;` to `qrpc_core_ir/src/v4.rs`
- re-export the child through `pub use venue_capability_matrix::*;`
- create `qrpc_core_ir/src/v4/venue_capability_matrix.rs`
- move venue capability DTOs/enums/constants/defaults and venue matrix impl methods
- move `default_venue_capability_matrix_version`
- import runtime mode types from the v4 surface

Forbidden movement:

- plugin governance, compile-time capability reports, static bundles, runtime mode contract implementation, QS type
  system, version manifest, reproducibility, complexity budget, bridge builders, tests, machine contract, machine graph
  contract, and QS profile

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs`

**Markers**:
- `BE-001VI-01`

**Next step**:
BE-001VI-02 root.contracts.core_ir.v4_contracts.venue_capability_matrix extract_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
