# v4.16.0 root.contracts.runtime_support.v4_runtime_support parent residual judgment - graph_symbol_expansion

> Batch: BE-002DL-01
> Node: `root.contracts.runtime_support.v4_runtime_support`
> Selected child: `root.contracts.runtime_support.v4_runtime_support.graph_symbol_expansion`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.

---

## Decision

Select `root.contracts.runtime_support.v4_runtime_support.graph_symbol_expansion` as the next v4 runtime support child.

## Evidence

`type_surface` is closed. The BE-002DH-01 v4 runtime support baseline queue names `graph_symbol_expansion` next.

The candidate currently lives in `qrpc_runtime/src/v4_runtime_types.rs` and owns:

- `expand_v4_graph_for_symbols`
- `normalize_v4_backtest_symbols`
- symbol-safe machine id expansion helpers
- event party expansion helpers
- multi-symbol metadata and risk-plane expansion behavior

This is a coherent graph preprocessing owner and does not need to mix with runtime constructors, replay orchestration, transition evaluation, risk/execution gates, simulated execution behavior, or tests.

## Boundary

The next baseline must freeze only graph/symbol expansion behavior before movement. It must not move:

- v4 DTO/type surface;
- private runtime state structs;
- runtime constructors and mode gates;
- event replay orchestration;
- machine transition engine;
- risk/execution capability gates;
- simulated execution behavior;
- v4 tests.

## Dependency Rule

The graph symbol expansion child may use v4 type surface DTOs through the v4 parent. It must not call live execution, plugin runtime, ops compatibility, metrics, QuantScript, plugin metadata, executor, backend, frontend, or release-transition optimization paths.

## Next Step

BE-002DM-01 `root.contracts.runtime_support.v4_runtime_support.graph_symbol_expansion` baseline_plan.
