# v4.16.0 root.contracts.runtime_support.runtime_facade_coordinator baseline plan

> Batch: BE-001YF-01
> Node: `root.contracts.runtime_support.runtime_facade_coordinator`
> Parent: `root.contracts.runtime_support`
> Stage: `baseline_plan`
> Movement: No code movement.

## Physical Scope

This child freezes the coordinator/facade portion of `qrpc_runtime/src/lib.rs`.

Owned source ranges before extraction:

- `qrpc_runtime/src/lib.rs`: module declarations and crate-root re-export policy
- `qrpc_runtime/src/lib.rs`: `set_mock_volatility`
- `qrpc_runtime/src/lib.rs`: `ConfigGenerationEntry`
- `qrpc_runtime/src/lib.rs`: `RuntimeCoordinator`
- `qrpc_runtime/src/lib.rs`: `impl Debug for RuntimeCoordinator`
- `qrpc_runtime/src/lib.rs`: `impl RuntimeCoordinator`
- `qrpc_runtime/src/lib.rs`: coordinator-local helpers `portfolio_equity_estimate` and `quote_price_map`
- `qrpc_runtime/src/lib.rs`: crate-root tests for provider override and coordinator behavior

## Public Methods To Preserve

The extraction must keep these public APIs callable from `qrpc_runtime` exactly as before:

- `set_mock_volatility`
- `RuntimeCoordinator::from_core_ir`
- `RuntimeCoordinator::new`
- `RuntimeCoordinator::with_modules_from_core_ir`
- `RuntimeCoordinator::with_modules`
- `RuntimeCoordinator::with_data_module_from_core_ir`
- `RuntimeCoordinator::with_data_module`
- `RuntimeCoordinator::with_intent_module_from_core_ir`
- `RuntimeCoordinator::with_intent_module`
- `RuntimeCoordinator::with_agent_module_from_core_ir`
- `RuntimeCoordinator::with_agent_module`
- `RuntimeCoordinator::with_risk_checker_from_core_ir`
- `RuntimeCoordinator::with_risk_checker`
- `RuntimeCoordinator::with_execution_module_from_core_ir`
- `RuntimeCoordinator::with_execution_module`
- `RuntimeCoordinator::with_module_providers_from_core_ir`
- `RuntimeCoordinator::with_module_providers`
- `RuntimeCoordinator::run_session`
- `RuntimeCoordinator::run_slow_cycle`
- `RuntimeCoordinator::run_fast_cycle`
- `RuntimeCoordinator::submit_execution_plan`
- `RuntimeCoordinator::on_market_data`
- `RuntimeCoordinator::portfolio_state`
- `RuntimeCoordinator::data_fetch_counts`
- `RuntimeCoordinator::last_action_at_ms`
- `RuntimeCoordinator::last_rebalance_at_ms`
- `RuntimeCoordinator::data_module`
- `RuntimeCoordinator::intent_module`
- `RuntimeCoordinator::agent_module`
- `RuntimeCoordinator::risk_checker`
- `RuntimeCoordinator::risk_mode`
- `RuntimeCoordinator::set_risk_mode`
- `RuntimeCoordinator::set_risk_monitor`
- `RuntimeCoordinator::is_risk_stopped`
- `RuntimeCoordinator::reset_risk_stopped`
- `RuntimeCoordinator::swap_module_config`
- `RuntimeCoordinator::apply_pending_module_configs`
- `RuntimeCoordinator::current_generation`
- `RuntimeCoordinator::generation_history`
- `RuntimeCoordinator::execution_module_provider_key`
- `RuntimeCoordinator::set_execution_assumptions`
- `RuntimeCoordinator::portfolio_update_event`
- `RuntimeCoordinator::merge_records`

## Private Behavior To Preserve

The extraction must preserve private coordinator behavior:

- slow/fast cycle intent-kind selection
- cycle trace ID generation
- market data collection through `DataModuleProvider`
- intent, agent, risk, and execution provider delegation
- merge engine fallback behavior and bounded merge record retention
- open-order processing and execution plan submission
- portfolio mark-price refresh and leverage/exposure recomputation
- RiskMonitor stop-state update after session completion
- config generation history append and bounded retention
- deterministic revision generation through canonical JSON digest

## Parent/Child Communication Rules

The extracted child may use parent-owned sibling modules only through the runtime-support parent path:

- `super::data_module`
- `super::intent_module`
- `super::agent_module`
- `super::execution_module`
- `super::risk_checker`
- `super::runtime_state`
- `super::config_tracker`
- `super::merge_coordinator`

The crate root must keep the existing public facade:

- module declarations stay visible from `qrpc_runtime/src/lib.rs`
- crate-root `pub use` entries continue to expose the same names
- `RuntimeCoordinator` and `ConfigGenerationEntry` are re-exported from the extracted child

No sibling-to-sibling shortcut and no release-transition optimization is allowed.

## Actual Extraction Plan

BE-001YF-02 may:

1. Add `qrpc_runtime/src/runtime_facade_coordinator.rs`.
2. Move `ConfigGenerationEntry`, `RuntimeCoordinator`, its impls, coordinator-local helpers, and coordinator tests into that file.
3. Keep `qrpc_runtime/src/lib.rs` as the runtime support parent facade.
4. Add `mod runtime_facade_coordinator;` and `pub use runtime_facade_coordinator::{ConfigGenerationEntry, RuntimeCoordinator};`.
5. Keep all existing non-coordinator module declarations and public re-exports unchanged.
6. Keep `set_mock_volatility` in the crate root unless the extraction shows it is strictly coupled to coordinator tests; if moved, it must remain crate-root re-exported.

## Leaf Split Decision Gate

`continue_split: true`.

Reasons:

- `lib.rs` still contains a large coordinator implementation and public facade policy.
- The coordinator has separable internal pockets after first extraction: constructor/provider wiring, session/cycle orchestration, config generation, portfolio projection, and tests.
- First extraction should only separate the runtime facade coordinator from crate-root export policy; deeper splitting is decided after closeout.

Stop rules do not apply yet.

## Equivalence Gates

Required for BE-001YF-02:

- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-001YF-02 `root.contracts.runtime_support.runtime_facade_coordinator` actual extraction.
