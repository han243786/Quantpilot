# v4.16.0 root.contracts.runtime_support.agent_module.weighted_signal_decisions single leaf closeout

> Batch: BE-001ZX-03
> Node: `root.contracts.runtime_support.agent_module.weighted_signal_decisions`
> Parent: `root.contracts.runtime_support.agent_module`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Decision

`stop_split: true`

`root.contracts.runtime_support.agent_module.weighted_signal_decisions` is closed as a terminal child.

## Evidence

Current owner file:

- `qrpc_runtime/src/agent_module/weighted_signal_decisions.rs`

Terminal leaf control:

```text
Leaf: root.contracts.runtime_support.agent_module.weighted_signal_decisions
Decision: STOP
Score: 21
Metrics: loc=119, functions=2, public_surface=1, branches=10, domains=2
Terminal leaf control: size_bucket=small_100_149, governance_mode=stop_split
```

## Why No Deeper Split

No deeper child is opened because:

- only two functions remain in the child;
- the file is below the normal terminal target range;
- deeper split would separate grouping from the weighted decision flow, increasing governance cost and reducing local readability;
- shared scoring and portfolio helpers remain parent-mediated for future children;
- trading semantics and public-adjacent provider behavior make unnecessary movement undesirable.

## Equivalence Evidence

The extraction batch already passed:

- `cargo fmt`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`: 227 passed, 2 ignored
- `cargo check -p quantpilot`
- `git diff --check`
- `tools/check-utf8.ps1`
- `tools/check-full-feature-tree.ps1`
- `tools/check-matrix-governance.ps1`

## Next Step

BE-001ZY-01 `root.contracts.runtime_support.agent_module` parent_residual_judgment selects `portfolio_rebalance`.
