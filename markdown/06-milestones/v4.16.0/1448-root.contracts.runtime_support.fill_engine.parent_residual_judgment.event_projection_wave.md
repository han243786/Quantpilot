# v4.16.0 root.contracts.runtime_support.fill_engine parent residual judgment selects event_projection_wave

> Batch: BE-002BB-01
> Node: `root.contracts.runtime_support.fill_engine`
> Parent: `root.contracts.runtime_support`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> Selected child: `root.contracts.runtime_support.fill_engine.event_projection_wave`

---

## Decision

`root.contracts.runtime_support.fill_engine.event_projection_wave` is selected as the next fill-engine child.

This is a judgment step only. It does not authorize code movement yet.

## Evidence

The frozen `fill_engine` baseline opened a cost-controlled same-parent wave queue:

1. `event_projection_wave`
2. `portfolio_reservation_accounting`
3. `fill_report_execution_helpers`
4. `test_harness`

`event_projection_wave` is selected first because it is the lowest-risk cohesive helper group.

Current owner functions:

- `open_event`
- `partial_event`
- `cancel_event`
- `reject_event`
- `fill_event`

Current behavior owned by this candidate:

- runtime event id construction;
- `RuntimeEventType` assignment for order lifecycle projections;
- lifecycle stage and status payload projection;
- trace/source/timestamp propagation;
- fill payload projection from `FillReport`.

## Boundary Rules

| Boundary | Requirement |
| --- | --- |
| Parent orchestration | `FillEngine::submit_plan`, `FillEngine::on_market_update`, and `try_open_resting_order` remain parent-owned. |
| Accounting | Reservation, cash, position, and portfolio mutation helpers remain outside this movement. |
| Fill report | Fill report construction remains outside this movement. |
| Public API | Event builders are private helpers; no public API change is allowed. |
| Communication | The child may expose `pub(super)` event-builder functions consumed only by the parent. |
| Sibling shortcuts | The child must not import future accounting, report-helper, or test-harness siblings. |

## Next Step

BE-002BC-01 `root.contracts.runtime_support.fill_engine.event_projection_wave` baseline_plan.
