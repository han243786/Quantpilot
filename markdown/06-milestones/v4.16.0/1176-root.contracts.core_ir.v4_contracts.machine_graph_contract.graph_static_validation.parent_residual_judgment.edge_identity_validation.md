# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation parent residual judgment selects edge_identity_validation

> Batch: BE-001UX-01
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation` selects
`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.edge_identity_validation`
as the next child.

The selected child owns edge inventory validation: edge ID required/duplicate checks, edge event type
required checks, unknown source/target machine checks, and self-loop rejection. It does not own schema
version checks, graph ID checks, machine identity validation, DAG validation, event usage validation,
risk-plane validation, event party helper, graph schema, or traversal helpers.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `edge_identity_validation` is a named white-box child for graph edge inventory validation. |
| parent_child_communication_kept | pass | It can receive the parent-produced `machines_by_id` map and return edge errors. |
| equivalence_baseline_freezable | pass | Edge ID, duplicate ID, event type, unknown endpoint, and self-loop branches are finite. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | The public graph validator remains in the parent, while edge identity is a private graph input phase. |
| state_machine_phase | pass | Edge inventory validation occurs before DAG/event/risk phases. |
| strategy_branch | guarded | It protects graph edge endpoints and event references consumed by later phases. |
| independent_failure_mode | pass | Edge identity/endpoint/self-loop failures are independent from machine, DAG, event, and risk validation. |
| reuse_pressure | guarded | Later phases depend on valid edge shape, but the parent remains the only caller. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The child owns a real graph edge invariant. |
| communication_cost_rises | no | It needs only the parent machine map and `self.edges`. |
| local_proof_missing | no | Existing qrpc-core-ir tests cover graph edge validation through the public validator. |
| line_count_only | no | Selection is based on edge inventory ownership. |

leaf_split_decision_result

`continue_split: true`

Selected next child:

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.edge_identity_validation`

next_recursive_step

BE-001UY-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.edge_identity_validation baseline_plan
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation.rs`

**Markers**:
- `BE-001UX-01`

**Next step**:
BE-001UY-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.edge_identity_validation baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
