# v4.16.0 root.contracts.runtime_support.v4_runtime_support.test_harness.live_capability_guard_tests single leaf closeout

> Batch: BE-002FQ-02
> Node: `root.contracts.runtime_support.v4_runtime_support.test_harness.live_capability_guard_tests`
> Parent: `root.contracts.runtime_support.v4_runtime_support.test_harness`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Set `stop_split: true` for `live_capability_guard_tests`.

The child is inside the target terminal size band and has one clear owner: live mode, execution capability policy, provider/runtime source, unsupported capability, and risk-plane source guard coverage. Further splitting would separate small rejection variants that still share the same guard surface and fixture access.

## Leaf Granularity Evidence

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -Path qrpc_runtime\src\v4_runtime\test_harness\live_capability_guard_tests.rs
```

Result:

| Field | Value |
| --- | --- |
| `split_decision` | `STOP` |
| `governance_packaging` | `stop_split` |
| `final_decision` | `STOP` |
| Score | `36` |
| LOC | `308` |
| Functions | `11` |
| Public/internal-visible surface | `0` |
| Branches | `0` |
| Domains | `5` |
| Risk tags | `persistence`, `route_schema`, `security`, `state_machine`, `trading_semantics` |
| Terminal size bucket | `terminal_target_150_600` |

## Stop-Split Rationale

Rejected finer splits:

| Candidate | Decision | Reason |
| --- | --- | --- |
| live mode boundary tests | Reject | These are small variants of the same live/capability guard surface. |
| execution capability rejection tests | Reject | Unsupported, provider-native, policy-missing, and undeclared capability checks share the same admission owner. |
| risk-plane source guard tests | Reject | Two tests share setup and fail-closed event/source assertions with the same guard owner. |

## Evidence

Inherited from BE-002FQ-01 extraction:

- `cargo fmt --check`: PASS
- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime v4 -- --nocapture`: PASS, 32 tests
- `cargo check -p quantpilot`: PASS

This closeout is docs-only and updates the recursive cursor plus governance indexes.

## Next Step

BE-002FR-01 `root.contracts.runtime_support.v4_runtime_support.test_harness` parent_residual_judgment.
