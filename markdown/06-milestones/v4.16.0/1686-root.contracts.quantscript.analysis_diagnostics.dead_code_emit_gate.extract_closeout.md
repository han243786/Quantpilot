# v4.16.0 root.contracts.quantscript.analysis_diagnostics.dead_code_emit_gate extract closeout

> Batch: BE-002HI-02
> Node: `root.contracts.quantscript.analysis_diagnostics.dead_code_emit_gate`
> Parent: `root.contracts.quantscript.analysis_diagnostics`
> Stage: `actual_extraction`
> governance_next_authority: active

---

## Result

`quantscript/src/analysis_diagnostics/dead_code_emit_gate.rs` now owns the QuantScript dead-code emit diagnostic.

Moved from `quantscript/src/analysis_diagnostics/analysis.rs`:

- `check_dead_code_emit`
- `check_dead_code_emit_from_stmts`
- `is_constant_false`

## Parent Boundary

`quantscript/src/analysis_diagnostics/analysis.rs` remains the parent coordinator. It still owns `ScriptAnalysis`, `analyze_script_module`, diagnostic invocation order, strategy presence checks, and the shared emit traversal helper.

The parent imports only:

- `check_dead_code_emit`

`contains_emit_in_stmts` remains owned by the analysis parent; the analysis_diagnostics parent exposes it through an internal `pub(in crate::analysis_diagnostics)` bridge so this child does not call a sibling directly.

## Unmoved Families

The extraction did not move:

- strategy fetch/emit presence checks;
- `contains_emit_in_stmts`;
- diagnostic DTO/span types;
- unsupported construct, lookahead-window, warmup/fetch, indirect-recursion, symbol-whitelist, fetch-lookback-warning, and index-bounds children already closed;
- evaluator normalization, runtime lowering, v4 static audit, tests, authoring samples, backend, executor, frontend, plugin metadata, or release transition.

## Validation

Passed before closeout:

- `cargo fmt`
- `cargo test -p quantscript` (88 tests)

Remaining gates should run before commit:

- `cargo fmt --check`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002HI-03 `root.contracts.quantscript.analysis_diagnostics.dead_code_emit_gate` single_leaf_closeout.
