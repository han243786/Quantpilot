# v4.16.0 root.contracts.quantscript.v4_static_audit.transition_parser baseline plan

> Batch: BE-002KI-01
> Node: `root.contracts.quantscript.v4_static_audit.transition_parser`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Goal

Freeze the transition parser boundary before Rust movement.

This child owns one transition grammar line and the conversion into a
`MachineTransition`. State-group, memory, edge, risk-plane, machine-template,
DTO, and test ownership remain parent-controlled or separate future candidates.

## Frozen Boundary

| Field | Value |
| --- | --- |
| Approved function | `parse_transition` |
| Planned file | `quantscript/src/v4_static_audit/transition_parser.rs` |
| Inputs | transition line text, machine id, transition index, source line number |
| Outputs | `MachineTransition` or `Diagnostic` |
| Parent callers | `machine_block_parser` through parent wrapper |
| Visibility | child function may be `pub(super)` only for the parent wrapper |

## Equivalent Behavior

The extracted child must preserve:

- syntax diagnostic `QSV4115`;
- unsupported modifier diagnostic `QSV4116`;
- event type extraction from the first word;
- `from_state` and `to_state` extraction;
- transition id shape `{machine_id}.t{transition_index}`;
- optional `emit` CSV token accumulation;
- optional `write` CSV token accumulation;
- default guard, priority, source, freshness, and diagnostics fields.

## Exclusions

- Do not move `parse_state_group`.
- Do not move `parse_memory`.
- Do not move `parse_edge`.
- Do not move `parse_risk_plane`.
- Do not move `parse_machine_template`.
- Do not move public report DTOs or local tests.
- Do not introduce release-transition cross-child links.

## Allowed Rust Movement For Next Step

BE-002KI-02 may:

1. add `mod transition_parser;`;
2. create `quantscript/src/v4_static_audit/transition_parser.rs`;
3. move the `parse_transition` implementation body;
4. keep the parent private wrapper named `parse_transition`;
5. adjust imports and `pub(super)` visibility only where required by this
   extraction.

## Gates For Actual Extraction

| Gate | Command |
| --- | --- |
| format | `cargo fmt --all --check` |
| crate tests | `cargo test -p quantscript` |
| workspace compile | `cargo check -p quantpilot` |
| diff hygiene | `git diff --check` |
| UTF-8 | `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1` |
| full tree | `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1` |
| matrix governance | `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1` |

## Next Step

BE-002KI-02 `root.contracts.quantscript.v4_static_audit.transition_parser`
actual_extraction.
