# v4.16.0 root.executor.runner.runner_support_files.kline_buffer baseline plan

> Batch: BE-002MU-01
> Node: `root.executor.runner.runner_support_files.kline_buffer`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Frozen Boundary

`kline_buffer` is already physically isolated as:

`src-executor/kline_buffer.rs`

The leaf owns `KlinePool`, symbol buffer creation and eviction, direct K-line
merge behavior, ticker-to-bar projection, recent-bar projection, and
latest-price lookup.

## Leaf Granularity Gate

The terminal leaf evaluator returned `STOP` with score 40 for the whole
`src-executor/kline_buffer.rs` file.

| Field | Value |
| --- | --- |
| LOC | 106 |
| Functions | 6 |
| Public surface | 10 |
| Branches | 14 |
| Domains | 2 |
| Governance packaging | `stop_split` |
| Size bucket | `small_100_149` |
| Risk tags | `cross_crate`, `public_api`, `state_machine` |

## Decision

No internal extraction is approved in this step. `kline_buffer` remains a
terminal support-file leaf because its stateful ring-buffer behavior is compact
and cohesive.

## Required Gates

| Gate | Required |
| --- | --- |
| `cargo check -p quantpilot --bin executor` | Yes |
| `git diff --check` | Yes |
| Full feature tree | Yes |
| Matrix governance | Yes |

## Next Step

BE-002MU-02
`root.executor.runner.runner_support_files.kline_buffer`
single_leaf_closeout.
