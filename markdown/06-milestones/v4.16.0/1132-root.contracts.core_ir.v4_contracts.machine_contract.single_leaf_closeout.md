# v4.16.0 root.contracts.core_ir.v4_contracts.machine_contract single leaf closeout continues split

> Batch: BE-001TZ-03
> Node: `root.contracts.core_ir.v4_contracts.machine_contract`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_contract` remains open for one deeper split.

The extracted file is no longer a tiny DTO leaf: it contains machine taxonomy/schema plus a substantial public validation method. The next recursive child should isolate `static_validation` so schema ownership and validation behavior can be governed separately without changing public API.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `static_validation` is a clear behavior owner for `V4MachineContract::validate_static_contract`. |
| parent_child_communication_kept | pass | A validation child can import `V4MachineContract`, policy enums, and `V4_MACHINE_CONTRACT_VERSION` through the machine parent. |
| equivalence_baseline_freezable | pass | Validation branches and error messages are finite and already covered by local v4 tests. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | pass | `validate_static_contract` is a public method and an invariant gate. |
| state_machine_phase | pass | The method validates states, state groups, transitions, memory writes, and nested child-machine depth. |
| strategy_branch | guarded | Strategy graph/bridge consumers rely on valid machines, but bridge behavior stays outside this child. |
| independent_failure_mode | pass | Validation drift has independent failures: duplicate states/groups/transitions, unknown references, memory defaults, child-machine recursion, and policy conflicts. |
| reuse_pressure | pass | Graph contract, static bundle, compat bridge, and tests all rely on this validation behavior. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The validation method is a real behavior owner, not a tiny schema fragment. |
| communication_cost_rises | no | Moving the impl into a child keeps one parent-mediated schema dependency and avoids splitting each validation branch separately. |
| local_proof_missing | no | `cargo test -p qrpc-core-ir` passed after the parent extraction and includes machine validation tests. |
| line_count_only | no | The split is by schema-versus-validation behavior, not file size. |

leaf_split_decision_result

`continue_split: true`

Next selected child candidate:

`root.contracts.core_ir.v4_contracts.machine_contract.static_validation`

## Boundary

**Real files**:

- `qrpc_core_ir/src/v4/machine_contract.rs`

**Markers**:

- `BE-001TZ-03`

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-001UA-01 `root.contracts.core_ir.v4_contracts.machine_contract` parent_residual_judgment selects `static_validation`.
