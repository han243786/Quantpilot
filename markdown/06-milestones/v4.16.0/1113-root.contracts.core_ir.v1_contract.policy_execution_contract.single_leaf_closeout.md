# v4.16.0 root.contracts.core_ir.v1_contract.policy_execution_contract single leaf closeout

> Batch: BE-001TO-03
> Node: `root.contracts.core_ir.v1_contract.policy_execution_contract`
> Parent: `root.contracts.core_ir.v1_contract`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`policy_execution_contract` is now a closed compact leaf.

The extraction in BE-001TO-02 created a physical owner for v1 signal rules, agent policy schema, risk policy schema, execution schema, and serde default helpers while preserving v1 and crate-root facades.

## Closed Surface

| Surface | Result | Evidence |
| --- | --- | --- |
| Physical owner exists | pass | `qrpc_core_ir/src/v1/policy_execution_contract.rs` owns the selected production policy/execution contract. |
| v1 parent facade preserved | pass | `qrpc_core_ir/src/v1.rs` re-exports `policy_execution_contract::*`. |
| Crate facade preserved | pass | `qrpc_core_ir/src/lib.rs` still re-exports `v1::*`. |
| Rust equivalence verified | pass | BE-001TO-02 passed `cargo check -p qrpc-core-ir`, `cargo test -p qrpc-core-ir`, and smart pre-commit. |
| Full workspace check preserved | pass | Smart pre-commit passed `cargo check -p quantpilot`. |

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | Child owns signal, agent policy, risk policy, execution DTOs, and serde default helpers. |
| parent_child_communication_kept | pass | The child imports expression names through `super::{ScalarExpr, SignalKind}` and remains parent-reexported. |
| equivalence_baseline_freezable | pass | BE-001TO-01 froze public names, serde defaults, and helper return values. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | pass | The child contains public qrpc-core-ir v1 schema names. |
| state_machine_phase | no | The child is pure schema/default contract, not runtime state-machine behavior. |
| strategy_branch | pass | Signal, agent, risk, and execution are strategy graph layers. |
| independent_failure_mode | guarded | The layers can drift independently, but this compact v1 contract is still only 131 lines and has one facade proof. |
| reuse_pressure | pass | Compiler/runtime consumers reuse these names through the v1 facade. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | pass | Splitting signal, agent, risk, execution enums/defaults now would create thin DTO leaves. |
| communication_cost_rises | pass | Additional child files would add re-export/import churn without stronger local proof. |
| local_proof_missing | no | `cargo test -p qrpc-core-ir` covers the v1 round trip after extraction. |
| line_count_only | pass | Further split would be driven mostly by taxonomy labels rather than actual complexity. |

leaf_split_decision_result

`stop_split: true`; `policy_execution_contract` should remain one compact production schema/default leaf for now.

## Residual Boundary

Still outside this closed leaf:

- `contracts.core_ir.v1_contract.test_fixture`;
- v1 parent closeout;
- v4 contracts and core_ir parent closeout;
- release transition.

## Next Step

BE-001TP-01 `root.contracts.core_ir.v1_contract` parent_residual_judgment selects `test_fixture`.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
