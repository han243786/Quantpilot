# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline.replay_builder baseline closeout

> Batch: BE-002CS-01
> Node: `root.contracts.runtime_support.sandbox_replay_timeline.replay_builder`
> Parent: `root.contracts.runtime_support.sandbox_replay_timeline`
> Stage: `baseline_plan + single_leaf_closeout`
> Movement: No Rust code movement.

---

## Decision

`stop_split: true`

`replay_builder` is already physically extracted as `qrpc_runtime/src/sandbox/replay.rs`, so no additional code movement is required for this pass.

## Baseline

The leaf owns:

- `build_v4_deterministic_replay_bars`;
- `sort_v4_replay_ticks_deterministically`;
- `build_kline_providers`;
- `build_quote_providers`;
- `build_resampled_providers`;
- `build_unified_timeline`;
- `build_mock_unified_timeline`;
- replay-local tests.

Out of scope:

- closed timeline data provider child;
- closed unified timeline child;
- sandbox mode/control surface;
- realtime and fast-backtest sandbox implementations;
- release-transition optimization.

## Leaf Granularity Evidence

`tools/evaluate-leaf-granularity.ps1` result:

- decision: `STOP`
- score: `44`
- loc: `241`
- functions: `10`
- public surface: `6`
- domains: `5`
- terminal leaf control bucket: `terminal_target_150_600`
- governance mode: `stop_split`

The replay builder is inside the preferred terminal LOC range and already groups a coherent provider/timeline assembly concern. Further splitting would create artificial sub-leaves around fallback quote construction, resample assembly, and mock-vs-real timeline construction.

## Gates

Verified for this batch:

- `cargo test -p qrpc-runtime sandbox::replay -- --nocapture`: pass, 2 tests

Required before commit:

- `git diff --check`
- UTF-8 / full feature tree / matrix governance

## Next Step

BE-002CT-01 `root.contracts.runtime_support.sandbox_replay_timeline` parent_residual_judgment selects `sandbox_mode_surface`.
