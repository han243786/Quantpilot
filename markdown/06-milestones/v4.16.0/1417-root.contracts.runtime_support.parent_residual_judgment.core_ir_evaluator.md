# v4.16.0 root.contracts.runtime_support parent residual judgment selects core_ir_evaluator

> Batch: BE-002AH-01
> Node: `root.contracts.runtime_support`
> Parent: `root.contracts`
> Stage: `parent_residual_judgment`
> Selected child: `root.contracts.runtime_support.core_ir_evaluator`
> Movement: No Rust code movement.

---

## Parent State

Closed runtime-support children:

- `contracts.runtime_support.runtime_facade_coordinator`
- `contracts.runtime_support.data_module`
- `contracts.runtime_support.intent_module`
- `contracts.runtime_support.agent_module`

The frozen runtime-support child queue now advances to `core_ir_evaluator`.

## Selected Child Boundary

`root.contracts.runtime_support.core_ir_evaluator` owns Core IR indicator evaluation in `qrpc_runtime/src/core_ir_evaluator.rs`.

White-box responsibilities to freeze in the next baseline:

- `CoreIrIndicatorEvaluation` and `CoreIrIndicatorEvaluatorError`;
- `IndicatorEvaluator`, builtin evaluator registration, and `evaluate_indicator_signal`;
- built-in indicator families: MA, RSI, MACD, Momentum, Spread, ZScore, Custom, QuoteObserve, ATR, Bollinger Bands, OBV, CMF, ADX, Stochastic, CCI, Parabolic SAR, Keltner Channel, and Donchian Channel;
- custom expression and typed spread expression evaluation;
- series materialization, resampling, alignment, window aggregation, and numeric helper calculations;
- local indicator-evaluator tests and fixtures.

## Exclusions

The selected child must not absorb queued runtime-support siblings:

- execution planning internals in `execution_module`;
- fill simulation internals in `fill_engine`;
- risk checker and risk monitor internals;
- sandbox replay/timeline internals;
- v4 runtime implementation internals;
- live execution internals;
- plugin runtime, plugin market, and plugin sandbox internals;
- compatibility, hotswap, merge, reconcile, circuit breaker, and backtest metric support.

It must also not change backend routes, frontend callers, executor state, QuantScript parser/lowering semantics, plugin metadata files, or release-transition pathways.

## Split Decision

`continue_split: true`.

Reason:

- the owner file is oversized and mixes public evaluator API, registry wiring, many built-in indicator families, expression evaluators, series utilities, and tests;
- individual behavior families have independent failure modes and can be isolated without changing the public `evaluate_indicator_signal` facade;
- a child-level baseline can decide whether to use same-parent waves for indicator families and helper/test ownership, avoiding micro-leaf over-splitting.

No release-transition optimization is proposed.

## Next Step

BE-002AI-01 `root.contracts.runtime_support.core_ir_evaluator` baseline_plan.

## Verification

No code moved in this step. Required gates:

- `git diff --check`
- `tools/check-utf8.ps1`
- `tools/check-full-feature-tree.ps1`
- `tools/check-matrix-governance.ps1`
