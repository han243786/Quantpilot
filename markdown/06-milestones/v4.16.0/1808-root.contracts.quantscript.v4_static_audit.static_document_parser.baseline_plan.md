# v4.16.0 root.contracts.quantscript.v4_static_audit.static_document_parser baseline plan

> Batch: BE-002KC-01
> Node: `root.contracts.quantscript.v4_static_audit.static_document_parser`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Goal

Freeze the top-level v4 static document parser boundary before Rust movement.

This child owns parsing the `v4_strategy` document envelope into a parsed graph
and compile-time capability request. It delegates lower-level machine, edge,
risk-plane, capability/type, event-catalog, and parser utility behavior through
the parent-controlled boundary.

## Frozen Boundary

| Field | Value |
| --- | --- |
| Approved type | `ParsedV4QsStaticDocument` |
| Approved function | `parse_v4_static_document` implementation body |
| Planned file | `quantscript/src/v4_static_audit/static_document_parser.rs` |
| Input | source text |
| Output | `Result<ParsedV4QsStaticDocument, Vec<Diagnostic>>` |
| Parent caller | `audit_entrypoint` through parent wrapper |
| Visibility | parsed result type and fields remain visible inside the parent module family |

## Equivalent Behavior

The extracted child must preserve:

- empty document diagnostic `QSV4000`;
- v4 strategy header validation `QSV4001`;
- venue, mode, required capability, required type, and required plugin parsing;
- unsupported top-level statement diagnostic `QSV4006`;
- content-after-close diagnostic `QSV4007`;
- missing venue and missing runtime mode diagnostics `QSV4008` and `QSV4009`;
- machine block delegation with error accumulation and next-index handoff;
- edge and risk-plane delegation;
- graph metadata keys `default_venue_id` and `market_event_source`;
- graph construction with `V4_MACHINE_GRAPH_CONTRACT_VERSION`;
- event catalog attachment through the existing event catalog derivation owner;
- compile-time capability request construction with
  `V4_COMPILE_TIME_CAPABILITY_REQUEST_VERSION`.

## Exclusions

- Do not move machine/state/memory/transition parser bodies.
- Do not move edge or risk-plane parser bodies.
- Do not move machine template parsing.
- Do not move capability/type parser wrappers.
- Do not move event catalog derivation implementation.
- Do not move parser utilities or diagnostics helper.
- Do not move public report DTOs or local tests.
- Do not introduce release-transition cross-child links.

## Allowed Rust Movement For Next Step

BE-002KC-02 may:

1. add `mod static_document_parser;`;
2. create `quantscript/src/v4_static_audit/static_document_parser.rs`;
3. move `ParsedV4QsStaticDocument`;
4. move the implementation body of `parse_v4_static_document`;
5. keep a parent private wrapper named `parse_v4_static_document`;
6. adjust imports and visibility only where required by this extraction.

## Gates For Actual Extraction

| Gate | Command |
| --- | --- |
| format | `cargo fmt --all --check` |
| crate tests | `cargo test -p quantscript` |
| workspace compile | `cargo check -p quantpilot` |
| diff hygiene | `git diff --check` |
| UTF-8 | `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1` |
| full tree | `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1` |
| matrix governance | `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1` |

## Next Step

BE-002KC-02 `root.contracts.quantscript.v4_static_audit.static_document_parser`
actual_extraction.
