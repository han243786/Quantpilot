# v4.16.0 root.contracts.runtime_support parent residual judgment selects execution_module

> Batch: BE-002AU-01
> Node: `root.contracts.runtime_support`
> Parent: `root.contracts`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> Selected child: `root.contracts.runtime_support.execution_module`

---

## Decision

`root.contracts.runtime_support.execution_module` is selected as the next runtime-support child.

This is a judgment step only. It does not authorize code movement yet.

## Evidence

Closed runtime-support children:

- `runtime_facade_coordinator`
- `data_module`
- `intent_module`
- `agent_module`
- `core_ir_evaluator`

Current owner file:

- `qrpc_runtime/src/execution_module.rs`

Current file shape:

- provider request/output DTOs and traits;
- `BuiltinExecutionModule` planning and submission entrypoints;
- action-order and portfolio-target-order construction;
- execution semantics and event payload projection;
- portfolio/quote/market helper functions;
- local test harness.

## Candidate Shape

The next baseline should decide a child queue for `execution_module`.

Initial candidate groups:

- `provider_facade`: public request/output DTOs, planner/submitter traits, provider alias, and builtin provider struct;
- `order_planning`: risk-adjusted action orders and portfolio-target rebalance orders;
- `execution_payload_semantics`: execution semantics resolution, time-in-force parsing, and execution plan event payloads;
- `market_portfolio_helpers`: portfolio equity/position ratio and quote/market state maps;
- `test_harness`: local execution-module tests and fixtures.

The baseline may merge small groups into same-parent waves if terminal leaf control shows separate leaves would add governance cost.

## Boundary Rules

| Boundary | Requirement |
| --- | --- |
| Fill boundary | `fill_engine` remains a sibling child; execution module may call it only through the existing parent-owned dependency. |
| Risk boundary | Risk decisions are input contracts; risk checking remains outside this child. |
| Data boundary | Normalized market data is caller-provided; collection/normalization remains outside this child. |
| Runtime facade | `RuntimeCoordinator` wiring remains in the closed coordinator parent. |
| Release transition | No sibling shortcut or performance bypass is proposed. |

## Next Step

BE-002AV-01 `root.contracts.runtime_support.execution_module` baseline_plan.
