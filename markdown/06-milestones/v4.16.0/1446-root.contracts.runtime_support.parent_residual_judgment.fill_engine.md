# v4.16.0 root.contracts.runtime_support parent residual judgment selects fill_engine

> Batch: BE-002AZ-01
> Node: `root.contracts.runtime_support`
> Parent: `root.contracts`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> Selected child: `root.contracts.runtime_support.fill_engine`

---

## Decision

`root.contracts.runtime_support.fill_engine` is selected as the next runtime-support child.

This is a judgment step only. It does not authorize code movement yet.

## Evidence

Closed runtime-support children:

- `runtime_facade_coordinator`
- `data_module`
- `intent_module`
- `agent_module`
- `core_ir_evaluator`
- `execution_module`

Current owner file:

- `qrpc_runtime/src/fill_engine.rs`

Current file shape:

- `MarketState` and `FillEngine` public surfaces;
- plan submission and market-update lifecycle;
- resting order open/partial/fill/cancel/reject handling;
- executable quantity, assumptions, liquidity, and reservation helpers;
- event builders and fill report builders;
- portfolio application and reservation synchronization;
- local test harness.

## Candidate Shape

The next baseline should decide a cost-controlled child queue for `fill_engine`.

Initial candidate groups:

- `event_projection_wave`: open, partial, cancel, reject, and fill event builders;
- `portfolio_reservation_accounting`: reservation, available cash/position, release, synchronization, and portfolio application helpers;
- `fill_report_execution_helpers`: executable quantity, marketability, assumptions, and fill report construction;
- `test_harness`: local fill-engine tests and fixtures.

The parent should keep `MarketState`, `FillEngine`, public lifecycle methods, and parent-mediated helper imports unless the next baseline proves a cleaner boundary.

## Boundary Rules

| Boundary | Requirement |
| --- | --- |
| Execution boundary | Execution planning remains in the already closed `execution_module`. |
| Slippage boundary | Slippage math remains in `slippage`; fill engine calls the existing slippage surface only. |
| Portfolio boundary | Portfolio mutation remains owned by fill engine for simulated fills. |
| Runtime facade | Coordinator/provider wiring remains outside this child. |
| Release transition | No sibling shortcut or performance bypass is proposed. |

## Next Step

BE-002BA-01 `root.contracts.runtime_support.fill_engine` baseline_plan.
