# v4.16.0 root.contracts.quantscript.typed_resolution.resolver_support_surface extract closeout

> Batch: BE-002GN-02
> Node: `root.contracts.quantscript.typed_resolution.resolver_support_surface`
> Parent: `root.contracts.quantscript.typed_resolution`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> governance_next_authority: active

---

## Result

`root.contracts.quantscript.typed_resolution.resolver_support_surface` actual extraction is complete.

New child:

- `quantscript/src/resolve/resolver_support_surface.rs`

Parent wiring:

- `quantscript/src/resolve/mod.rs` now declares `mod resolver_support_surface;`.
- `lower_script_to_typed_hir` and resolver orchestration remain parent-mediated.
- Public resolver DTO exports remain owned by `public_type_surface`.
- `Resolver` state ownership, `MemberCapabilityUse`, and resolver-local tests remain parent-owned.

## Moved Surface

Moved into the child as `pub(super)` resolver support methods:

- `make_expr`
- `expr_label`
- `alloc_def_id`
- `alloc_expr_id`
- `callable_target`
- `call_target_return_type`

Moved into the child as `pub(super)` support helpers:

- `expr_number`
- `expr_integer`
- `series_index_view_kind`
- `series_window_span`
- `build_resolved_functions`
- `first_return_expr`
- `returned_list_target`

## Preserved Boundary

Preserved in parent or existing children:

- public resolver DTOs/enums in `public_type_surface`;
- callable/helper classifiers and registry seeding in `callable_classification_surface`;
- semantic/manual indicator inference in `semantic_inference_surface`;
- type inference, binding insertion, condition validation, and type annotation resolution in `type_inference_binding_surface`;
- module/function/block/statement/expression lowering in `resolver_orchestration_surface`;
- resolver state, local tests, and top-level parent-mediated wiring in `mod.rs`.

## Validation

Executed during this batch:

- `cargo fmt --check` PASS
- `cargo test -p quantscript` PASS, 88 tests
- `cargo check -p quantpilot` PASS

Remaining closeout gates must run before the next commit:

- `git diff --check`
- JSON parse
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002GN-03 `root.contracts.quantscript.typed_resolution.resolver_support_surface` single_leaf_closeout.
