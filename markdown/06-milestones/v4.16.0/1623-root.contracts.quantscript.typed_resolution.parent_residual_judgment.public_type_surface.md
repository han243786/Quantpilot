# v4.16.0 root.contracts.quantscript.typed_resolution parent residual judgment selects public_type_surface

> Batch: BE-002GC-01
> Node: `root.contracts.quantscript.typed_resolution`
> Selected child: `root.contracts.quantscript.typed_resolution.public_type_surface`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Parent State

`typed_resolution` is now a resolver parent at:

- `quantscript/src/resolve/mod.rs`

The previous closeout set:

- `stop_split: false`
- `PRECISION / CONTINUE`

Internal residual families:

- public type surface and resolver result DTOs;
- callable classification helper functions;
- resolver orchestration and AST-to-HIR lowering;
- type inference and binding insertion;
- expression semantic inference;
- manual indicator formula inference;
- callable registry and helper classification tables;
- resolver-local test harness.

## Decision

Select `root.contracts.quantscript.typed_resolution.public_type_surface` as the next child.

This child should freeze and then isolate the public typed-resolution DTO and enum surface before behavior-heavy resolver movement. It is the safest first split because the public type surface is referenced by analysis, lowering, and crate-root re-exports, while the implementation logic can continue to live in the parent until later children are selected.

## Candidate Physical Surface

Initial candidate block in `quantscript/src/resolve/mod.rs`:

- `ResolveResult`
- `ResolvedFunction`
- `ResolvedCallableKind`
- `ChangeHelperKind`
- `KnownIndicatorHelperKind`
- `KnownUniverseHelperKind`
- `MovingAverageHelperKind`
- `RsiHelperKind`
- `ResolvedChangeSmoothingKind`
- `ResolvedFetchSourceKind`
- `ResolvedSeriesCapabilityKind`
- `ResolvedSeriesBoundaryKind`
- `ResolvedWindowAggregateKind`
- `ResolvedBuiltinMathKind`
- `ResolvedMemberMutationKind`
- `ResolvedExprSemantic`
- `ResolvedSeriesViewKind`
- `ResolvedWindowAggregateView`
- `ResolvedManualIndicatorFormula`
- `ResolvedCallable`

`ResolveResult::has_errors` belongs with this public type surface unless the baseline proves it must stay parent-owned.

## Boundary

The next baseline must not include:

- `lower_script_to_typed_hir`
- `expr_semantic_key`
- `classify_series_capability_name`
- `classify_builtin_math_name`
- `classify_member_mutation_name`
- `Resolver`
- resolver lowering methods
- semantic inference functions
- callable registry builders
- tests, except as protected evidence

## Why This Child First

| Criterion | Judgment |
| --- | --- |
| Public API risk | High, so freeze first before behavior movement. |
| Cohesion | Strong: DTOs and enums define the external typed-resolution contract. |
| Split value | High: later resolver behavior splits can depend on a compact public type module. |
| Governance cost | Moderate and justified because the parent is oversized and public-facing. |

## Next Step

BE-002GD-01 `root.contracts.quantscript.typed_resolution.public_type_surface` baseline_plan.
