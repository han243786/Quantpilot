# v4.16.0 root.contracts.quantscript.analysis_diagnostics.strategy_presence_gate baseline plan

> Batch: BE-002HK-01
> Node: `root.contracts.quantscript.analysis_diagnostics.strategy_presence_gate`
> Parent: `root.contracts.quantscript.analysis_diagnostics`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Scope

Freeze the QuantScript strategy presence diagnostics before movement.

Current owner:

- `quantscript/src/analysis_diagnostics/analysis.rs`

Planned child:

- `quantscript/src/analysis_diagnostics/strategy_presence_gate.rs`

## Items Frozen For Movement

The next extraction may move only:

- `check_strategy_has_fetch`
- `contains_fetch_like_call_in_stmts`
- `contains_fetch_like_call_in_expr`
- `check_strategy_has_emit`

## Diagnostic Boundary

The child owns:

- `QS0610` strategy function must include at least one `fetch()` / `get_data()` call;
- recursive AST traversal for fetch-like calls;
- `QS0611` strategy function must include at least one `emit Intent()` call.

## Parent Wiring To Preserve

The analysis parent must keep:

- `ScriptAnalysis` and `analyze_script_module` public surface unchanged;
- diagnostic family invocation order unchanged;
- crate-root public exports and `crate::analysis` / `crate::diagnostics` compatibility unchanged;
- `contains_emit_in_stmts` as parent-owned shared traversal for this child and the already closed `dead_code_emit_gate`.

The child may expose:

- `check_strategy_has_fetch`
- `check_strategy_has_emit`

## Shared Helper Rule

`contains_emit_in_stmts` remains parent-owned in this baseline. The next extraction may call it only through the analysis_diagnostics parent boundary, not by importing another child.

## Forbidden Movement

The next extraction must not move or change:

- `contains_emit_in_stmts`;
- dead-code emit diagnostics already closed;
- diagnostic DTO/span types;
- unsupported construct, lookahead-window, warmup/fetch, indirect-recursion, symbol-whitelist, fetch-lookback-warning, and index-bounds children already closed;
- evaluator normalization, runtime lowering, v4 static audit, tests, authoring samples, backend, executor, frontend, plugin metadata, or release transition.

## Expected Validation

The extraction must pass:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002HK-02 `root.contracts.quantscript.analysis_diagnostics.strategy_presence_gate` actual_extraction.
