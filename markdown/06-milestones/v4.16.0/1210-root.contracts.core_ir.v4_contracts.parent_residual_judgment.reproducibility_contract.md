# v4.16.0 root.contracts.core_ir.v4_contracts parent residual judgment selects reproducibility_contract

> Batch: BE-001VP-01
> Node: `root.contracts.core_ir.v4_contracts`
> Parent: `root.contracts.core_ir`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts` selects
`root.contracts.core_ir.v4_contracts.reproducibility_contract` as the next child.

The selected child owns the v4 reproducibility evidence contract currently in `qrpc_core_ir/src/v4.rs`:

- `ReproducibilityContract`
- `RunEvidenceKind`
- `EventEnvelopeField`
- `impl Default for ReproducibilityContract`
- `ReproducibilityContract::validate_static_contract`
- `default_reproducibility_contract_version`
- `default_reproducibility_evidence`
- `default_event_envelope_fields`

The selected child does not own plugin governance, complexity budget, developer learning pipeline, static bundles,
compile-time capability reports, compatibility bridge, bridge builders, tests, machine contract, machine graph contract,
QS contracts, runtime mode, venue capability matrix, or version manifest.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `reproducibility_contract` is a named white-box child for replay evidence and event envelope requirements. |
| parent_child_communication_kept | pass | The child only needs v4 schema constants/default helpers and remains re-exported by the parent. |
| equivalence_baseline_freezable | pass | Required evidence, event envelope fields, and validation branches are finite. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | Reproducibility DTOs remain public through the v4 facade. |
| state_machine_phase | no | Reproducibility is replay evidence policy, not graph state topology. |
| strategy_branch | guarded | Replay evidence and event envelope requirements guard strategy/run reproducibility. |
| independent_failure_mode | pass | Missing evidence, missing event envelope fields, replay requirement, and reserved full-tick failures are independent from complexity/plugin rules. |
| reuse_pressure | guarded | Static bundles, compile-time reports, and tests reuse reproducibility validation. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The child owns DTOs, enums, defaults, and static validation as one evidence policy family. |
| communication_cost_rises | no | A single child keeps replay evidence policy together while parent re-exports it. |
| local_proof_missing | no | Existing qrpc-core-ir tests cover required risk decision evidence through the public v4 surface. |
| line_count_only | no | Selection is based on reproducibility ownership. |

leaf_split_decision_result

`continue_split: true`

Selected next child:

`root.contracts.core_ir.v4_contracts.reproducibility_contract`

next_recursive_step

BE-001VQ-01 root.contracts.core_ir.v4_contracts.reproducibility_contract baseline_plan
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs`

**Markers**:
- `BE-001VP-01`

**Next step**:
BE-001VQ-01 root.contracts.core_ir.v4_contracts.reproducibility_contract baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
