# v4.16.0 runtime_facade_coordinator.coordinator_test_harness baseline plan

> Batch: BE-001YV-01
> Node: `root.contracts.runtime_support.runtime_facade_coordinator.coordinator_test_harness`
> Parent: `root.contracts.runtime_support.runtime_facade_coordinator`
> Stage: `baseline_plan`
> Movement: No code movement.

## Boundary

This child owns the `runtime_facade_coordinator` test-only harness.

Planned extracted file:

- `qrpc_runtime/src/runtime_facade_coordinator/coordinator_test_harness.rs`

Planned moved content:

- noop data provider fixture
- noop intent provider fixture
- noop agent provider fixture
- noop execution module fixture
- reject-all risk checker fixture
- coordinator integration tests
- `sample_config` runtime protocol fixture

## White-Box Node

Inputs:

- compiled runtime protocol config
- injected provider fixtures
- runtime cycle/session calls

Outputs:

- assertions over slow/fast cycles
- assertions over provider override behavior
- assertions over risk clamp/reject behavior
- assertions over execution plan suppression
- assertions over fill traceability

Internal behavior to preserve:

- `session_runs_both_slow_and_fast_cycles`
- `risk_clamps_when_leverage_limit_is_tight`
- `custom_data_module_provider_can_override_runtime_inputs`
- `custom_intent_module_provider_can_override_runtime_signals`
- `custom_agent_module_provider_can_override_runtime_decisions`
- `custom_risk_checker_provider_can_override_runtime_decisions`
- `custom_execution_module_provider_can_override_runtime_plans`
- `fills_are_traceable_back_to_runtime_chain`

## Exclusions

Do not move in this batch:

- production coordinator struct fields
- `ConfigGenerationEntry`
- debug implementation
- child module declarations
- any production child module implementation

## Equivalence Plan

The parent module will declare `#[cfg(test)] mod coordinator_test_harness;`.

The test module must retain access to parent private fields through Rust child-module privacy.

Required gates:

- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-001YV-02 `root.contracts.runtime_support.runtime_facade_coordinator.coordinator_test_harness` actual_extraction.
