# v4.16.0 root.contracts.core_ir.v4_contracts.schema_identity_constants single leaf closeout

> Batch: BE-001TV-03
> Node: `root.contracts.core_ir.v4_contracts.schema_identity_constants`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`schema_identity_constants` is now a closed compact leaf.

The extraction in BE-001TV-02 created a physical owner for v4 schema/version constants, compat machine/event identifiers, and guard constants while preserving the v4 parent facade.

## Closed Surface

| Surface | Result | Evidence |
| --- | --- | --- |
| Physical owner exists | pass | `qrpc_core_ir/src/v4/schema_identity_constants.rs` owns the selected constants. |
| v4 parent facade preserved | pass | `qrpc_core_ir/src/v4.rs` re-exports `schema_identity_constants::*`. |
| Rust equivalence verified | pass | BE-001TV-02 passed `cargo check -p qrpc-core-ir`, `cargo test -p qrpc-core-ir`, and smart pre-commit. |
| Full workspace check preserved | pass | Smart pre-commit passed `cargo check -p quantpilot`. |

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | Child owns only v4 schema identity constants. |
| parent_child_communication_kept | pass | Constants remain reachable through the v4 parent facade. |
| equivalence_baseline_freezable | pass | BE-001TV-01 froze names, values, and non-owned public arrays. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | pass | Constants are public v4 API. |
| state_machine_phase | guarded | Compat machine/event IDs support the bridge contract. |
| strategy_branch | guarded | Compat identifiers are used by v1-to-v4 bridge output. |
| independent_failure_mode | pass | Schema identity drift is now isolated from DTO/validator families. |
| reuse_pressure | pass | Defaults, validators, bridge helpers, and tests reference these constants. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | pass | Splitting individual constants would create ownerless micro leaves. |
| communication_cost_rises | pass | Further split would add imports/re-exports without better proof. |
| local_proof_missing | no | `cargo test -p qrpc-core-ir` covers post-extraction references. |
| line_count_only | pass | Further split would be line-count driven only. |

leaf_split_decision_result

`stop_split: true`; `schema_identity_constants` should remain one compact static-constant leaf.

## Residual Boundary

Still outside this closed leaf:

- `contracts.core_ir.v4_contracts.backtest_artifact_contract`;
- `contracts.core_ir.v4_contracts.machine_contract`;
- `contracts.core_ir.v4_contracts.machine_graph_contract`;
- remaining v4 contract families, default helpers, and tests;
- `contracts.core_ir.crate_facade`;
- release transition.

## Next Step

BE-001TW-01 `root.contracts.core_ir.v4_contracts` parent_residual_judgment selects `backtest_artifact_contract`.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
