# v4.16.0 root.contracts.quantscript.v4_static_audit parent residual judgment selects machine_block_parser

> Batch: BE-002KD-01
> Node: `root.contracts.quantscript.v4_static_audit`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select next child:

`root.contracts.quantscript.v4_static_audit.machine_block_parser`

## Parent Residual Scan

Closed children:

- `runtime_handoff_builder`;
- `audit_entrypoint`;
- `capability_type_parser`;
- `event_catalog_derivation`;
- `parser_utilities_diagnostics`;
- `static_document_parser`.

Remaining parent-owned clusters:

- machine block orchestration and `V4MachineContract` assembly;
- state, state block, state group, memory, transition, edge, and risk-plane
  parsers;
- machine template parser and thin capability/type wrappers;
- public DTOs and local static-audit tests.

## Why This Child

`machine_block_parser` is the next highest-value extraction because it is the
entrypoint for parsing a machine envelope and assembling the machine contract.
It has a clear parent-facing call site from the document parser, but it still
delegates state, memory, transition, template, and nested machine details through
parent-owned helpers.

Selecting this boundary keeps the next extraction small enough for equivalence
checking while making the parent less entangled.

## Proposed Ownership

| Field | Value |
| --- | --- |
| Candidate child | `machine_block_parser` |
| Planned file | `quantscript/src/v4_static_audit/machine_block_parser.rs` |
| Likely owned type | `ParsedMachine` |
| Likely owned function | `parse_machine_block` implementation body |
| Parent wrapper | `parse_machine_block` |
| Lower-level dependencies | remain parent-controlled for the next baseline |

## Exclusions For Next Baseline

- Do not move `parse_state`.
- Do not move `parse_state_block`.
- Do not move `parse_memory`.
- Do not move `parse_transition`.
- Do not move `parse_edge`.
- Do not move `parse_risk_plane`.
- Do not move `parse_machine_template` unless the baseline proves it is required
  as a parent wrapper.
- Do not move public DTOs or local tests.
- Do not introduce release-transition links.

## Next Step

BE-002KE-01 `root.contracts.quantscript.v4_static_audit.machine_block_parser`
baseline_plan.
