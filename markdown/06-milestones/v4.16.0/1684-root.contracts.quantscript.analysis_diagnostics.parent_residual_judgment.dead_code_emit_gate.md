# v4.16.0 root.contracts.quantscript.analysis_diagnostics parent residual judgment: dead_code_emit_gate

> Batch: BE-002HH-01
> Node: `root.contracts.quantscript.analysis_diagnostics`
> Selected child: `root.contracts.quantscript.analysis_diagnostics.dead_code_emit_gate`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `root.contracts.quantscript.analysis_diagnostics.dead_code_emit_gate` for the next recursive child.

## Parent Residual Scan

Current parent:

- `quantscript/src/analysis_diagnostics/mod.rs`
- `quantscript/src/analysis_diagnostics/analysis.rs`
- `quantscript/src/analysis_diagnostics/diagnostics.rs`

Closed children:

- `root.contracts.quantscript.analysis_diagnostics.unsupported_construct_gate`
- `root.contracts.quantscript.analysis_diagnostics.lookahead_window_gate`
- `root.contracts.quantscript.analysis_diagnostics.warmup_fetch_gate`
- `root.contracts.quantscript.analysis_diagnostics.indirect_recursion_gate`
- `root.contracts.quantscript.analysis_diagnostics.symbol_whitelist_gate`
- `root.contracts.quantscript.analysis_diagnostics.fetch_lookback_warning_gate`
- `root.contracts.quantscript.analysis_diagnostics.index_bounds_gate`

Remaining major diagnostic families in `analysis.rs`:

| Candidate | Boundary | Decision |
| --- | --- | --- |
| `strategy_presence_gate` | strategy fetch/emit existence diagnostics; shares `contains_emit_in_stmts` with dead-code emit. | Queue |
| `dead_code_emit_gate` | `QS0612` constant-false branch emit warning; can use parent-mediated emit traversal. | Select |

## Selected Boundary

The next baseline may freeze only:

- `check_dead_code_emit`
- `check_dead_code_emit_from_stmts`
- `is_constant_false`

Expected child:

- planned `dead_code_emit_gate` child file.

## Shared Helper Rule

`contains_emit_in_stmts` must remain parent-owned for the next extraction. It is shared by strategy presence and dead-code emit, and moving it into either child would create sibling pressure later.

The next extraction may expose the helper to `dead_code_emit_gate` only through a parent-mediated `pub(in crate::analysis_diagnostics)` bridge or equivalent parent-owned function boundary.

## Why This Child

`dead_code_emit_gate` is the next best child because:

- it owns one isolated warning family (`QS0612`);
- it is smaller than the strategy presence family and can close quickly;
- it avoids mixing fetch-presence traversal with dead-code branch traversal;
- it preserves the shared emit traversal in the parent rather than creating child-to-child coupling.

## Exclusions

Do not move in the next baseline:

- strategy fetch/emit presence checks;
- `contains_emit_in_stmts`;
- diagnostic DTO/span types;
- unsupported construct, lookahead-window, warmup/fetch, indirect-recursion, symbol-whitelist, fetch-lookback-warning, and index-bounds children already closed;
- evaluator normalization, runtime lowering, v4 static audit, tests, authoring samples, backend, executor, frontend, plugin metadata, or release transition.

## Next Step

BE-002HI-01 `root.contracts.quantscript.analysis_diagnostics.dead_code_emit_gate` baseline_plan.
