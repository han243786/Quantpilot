# v4.16.0 root.contracts.qrpc_core.protocol_primitives single leaf closeout

> Batch: BE-001RO-03
> Node: `root.contracts.qrpc_core.protocol_primitives`
> Parent: `root.contracts.qrpc_core`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core.protocol_primitives` has been evaluated after BE-001RO-02 extraction.

Decision:

`stop_split: true`

The node is now a compact foundational child module with one responsibility: qrpc-core protocol primitive constants, primitive enums, symbol parsing/serde behavior, display behavior, and primitive defaults.

## Split Rule Evaluation

| Rule | Result |
| --- | --- |
| Physical owner | Stop. The selected surface is isolated in `qrpc_core/src/protocol_primitives.rs`. |
| Public method count | Stop. The child exposes only primitive methods/impls: `Symbol::parse`, `Symbol::as_str`, `Symbol` serde, `OrderSide` display, and two defaults. |
| Mixed responsibility | Stop. The enums are all foundational protocol primitives consumed by later qrpc-core schemas. |
| State ownership | Stop. The child owns no runtime state. |
| Side effects | Stop. No IO, persistence, locks, runtime lifecycle, external calls, or public API mutation are involved. |
| Parent-mediated dependency | Covered. Later qrpc-core residuals consume primitives through the crate-root facade. |
| Future reopen rule | Allowed only when a concrete version, primitive enum, symbol parsing, serde/display/default, or public facade proposal is made. |

## White-Box Boundary

| Input | Processing owner | Output |
| --- | --- | --- |
| Primitive protocol value proposals and serde/display/default requests | `contracts.qrpc_core.protocol_primitives` | Stable primitive constants, enums, parse/serde/display behavior, and defaults |

The leaf may describe and guard:

- qrpc-core version constants;
- primitive enum variants and serde attributes;
- `Symbol` parsing and serialization/deserialization;
- `OrderSide` display;
- primitive defaults.

## Rust Local Fields

| Field | Result |
| --- | --- |
| Crate | `qrpc-core` |
| Child visibility | Private module; public items are re-exported by the crate root. |
| Public exports | Preserved through `pub use protocol_primitives::*;`. |
| Parent facade | `qrpc_core/src/lib.rs` remains the crate-root coordinator. |
| Production dependency check | Passed. Later residuals still use primitive names through the crate-root facade. |

## Non-Claims

This closeout does not claim:

- runtime protocol config, artifact specs, runtime IO contracts, RFC execution contracts, or tests changed;
- qrpc_core parent residual judgment is complete;
- closed error/proto/plugin/strategy IR contracts changed;
- `qrpc_core_ir`, `qrpc_compiler`, `qrpc_runtime`, `quantscript`, backend, executor, frontend, physical `plugins/*`, or release transition changed.

## Proof

- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-001RP-01 `root.contracts.qrpc_core` parent_residual_judgment selects `contracts.qrpc_core.runtime_protocol_config`.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
