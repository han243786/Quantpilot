# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline parent residual judgment - replay_builder

> Batch: BE-002CR-01
> Node: `root.contracts.runtime_support.sandbox_replay_timeline`
> Selected child: `root.contracts.runtime_support.sandbox_replay_timeline.replay_builder`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.

---

## Decision

Select `replay_builder` as the next sandbox/replay/timeline child.

## Evidence

`timeline_data_providers` and `unified_timeline` are closed. The next sandbox baseline child owns replay construction in `qrpc_runtime/src/sandbox/replay.rs`.

The existing replay file is already physically separated and owns:

- deterministic v4 replay bar construction;
- deterministic replay tick sorting;
- Core IR to kline provider construction;
- quote provider fallback construction;
- resampled provider construction;
- real and mock unified timeline builders;
- replay-local tests.

## Candidate Boundary

The next baseline should determine whether `qrpc_runtime/src/sandbox/replay.rs` is already a terminal child or whether it needs further internal splitting.

Out of scope:

- timeline provider child, already closed;
- unified timeline child, already closed;
- sandbox mode/control surface;
- realtime and fast-backtest sandbox implementations;
- release-transition optimization.

## Dependency Rule

Replay builder code must continue using provider and unified timeline behavior through the parent-owned `timeline` surface. No sibling shortcut may be introduced.

## Next Step

BE-002CS-01 `root.contracts.runtime_support.sandbox_replay_timeline.replay_builder` baseline_plan.
