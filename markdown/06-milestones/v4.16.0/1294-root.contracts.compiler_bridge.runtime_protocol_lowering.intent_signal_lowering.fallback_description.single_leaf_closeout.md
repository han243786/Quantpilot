# v4.16.0 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.fallback_description single leaf closeout stops split

> Batch: BE-001XR-03
> Node: `root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.fallback_description`
> Parent: `root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering`
> Stage: `single_leaf_closeout`
> Movement: no Rust code movement; post-extraction closeout only.

---

## Summary

`root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.fallback_description` is closed as a stable child leaf.

The leaf owns one cohesive raw-text fallback description function. Further splitting by intent kind would create tiny helper-only fragments and would increase plumbing around shared `IntentConfig` parameter access, spread detection, and decoded spread formatting.

Decision:

`stop_split: true`

Next step:

Return to `root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering` parent residual judgment.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | Leaf is named `fallback_description` and owns raw fallback condition strings. |
| parent_child_communication_kept | pass | Parent calls one child entrypoint and receives one `String`. |
| equivalence_baseline_freezable | pass | BE-001XR-01 froze every fallback string format and BE-001XR-02 moved only that function. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | false | The leaf is private and not a public facade. |
| state_machine_phase | true | It owns the fallback phase after structured condition lowering fails. |
| strategy_branch | true | It is separate from structured condition lowering and intent kind mapping. |
| independent_failure_mode | true | Fallback text is the safety path for unsupported structured condition metadata. |
| reuse_pressure | false | The function is called from one parent signal-rule path. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | true | Per-intent children would be private string fragments without independent owner. |
| communication_cost_rises | true | Further split would spread shared `IntentConfig` parameter/default handling across tiny children. |
| local_proof_missing | false | Baseline documents all string formats and current compiler tests keep runtime output shape stable. |
| line_count_only | false | Stop decision is based on ownership and communication cost, not line count. |

leaf_split_decision_result

`stop_split_true`

Add `contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.fallback_description` to `closed_children`.

next_recursive_step

BE-001XS-01 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering parent_residual_judgment

## Boundary

**Real files**:
- `qrpc_compiler/src/runtime_protocol_lowering/intent_signal_lowering/fallback_description.rs`

**Markers**:
- `BE-001XR-03`
- `fallback_description stop_split: true`
- `raw fallback strings kept local`
- `return to intent_signal_lowering residual judgment`

**Next step**:
BE-001XS-01 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering parent_residual_judgment

---

## Gates

- `cargo test -p qrpc-compiler`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
