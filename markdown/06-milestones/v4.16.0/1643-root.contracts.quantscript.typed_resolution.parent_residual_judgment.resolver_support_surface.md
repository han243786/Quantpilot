# v4.16.0 root.contracts.quantscript.typed_resolution parent residual judgment: resolver_support_surface

> Batch: BE-002GM-01
> Node: `root.contracts.quantscript.typed_resolution`
> Selected child: `root.contracts.quantscript.typed_resolution.resolver_support_surface`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `root.contracts.quantscript.typed_resolution.resolver_support_surface` for the next recursive child.

## Current Parent State

Closed typed-resolution children:

- `root.contracts.quantscript.typed_resolution.public_type_surface`
- `root.contracts.quantscript.typed_resolution.callable_classification_surface`
- `root.contracts.quantscript.typed_resolution.semantic_inference_surface`
- `root.contracts.quantscript.typed_resolution.type_inference_binding_surface`
- `root.contracts.quantscript.typed_resolution.resolver_orchestration_surface`

Current parent file:

- `quantscript/src/resolve/mod.rs`

The parent now owns the public entrypoint, `Resolver` state/types, support helpers, scalar expression helpers, resolved function projection, return-list helpers, and resolver-local tests.

## Residual Candidate Review

| Candidate | Evidence | Decision |
|-----------|----------|----------|
| `resolver_support_surface` | Cohesive support block containing expression construction, labels, ID allocation, callable target lookup, call target return typing, scalar literal helpers, resolved function projection, and return-list target helpers. These are production helpers shared by closed orchestration/type/semantic surfaces. | Select |
| `resolver_test_harness` | Local resolver tests are still large, but should move only after production helpers are separated so the parent/test boundary is clear. | Queue |
| parent closeout | Too early; production helper residuals and local tests remain in the parent file. | Reject |

## Selected Child Scope

The next baseline may freeze only resolver support helpers:

- `make_expr`
- `expr_label`
- `alloc_def_id`
- `alloc_expr_id`
- `callable_target`
- `call_target_return_type`
- `expr_number`
- `expr_integer`
- `series_index_view_kind`
- `series_window_span`
- `build_resolved_functions`
- `first_return_expr`
- `returned_list_target`

Likely child file:

- `quantscript/src/resolve/resolver_support_surface.rs`

## Forbidden Movement

The next baseline and extraction must not move:

- public resolver DTOs/enums already closed in `public_type_surface`;
- callable/helper classifiers and registry seeding already closed in `callable_classification_surface`;
- semantic/manual indicator inference already closed in `semantic_inference_surface`;
- type inference, binding insertion, condition validation, and type annotation resolution already closed in `type_inference_binding_surface`;
- module/function/block/statement/expression lowering already closed in `resolver_orchestration_surface`;
- `lower_script_to_typed_hir`, `expr_semantic_key`, `FunctionSignature`, `CallableTarget`, `BindingInfo`, `Resolver`, `MemberCapabilityUse`, and resolver-local tests;
- analysis, lowering, evaluator, runtime, backend, executor, frontend, plugin metadata, or release transition.

## Next Step

BE-002GN-01 `root.contracts.quantscript.typed_resolution.resolver_support_surface` baseline_plan.
