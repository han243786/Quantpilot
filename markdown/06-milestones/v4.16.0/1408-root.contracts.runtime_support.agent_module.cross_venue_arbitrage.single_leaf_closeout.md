# v4.16.0 root.contracts.runtime_support.agent_module.cross_venue_arbitrage single leaf closeout

> Batch: BE-002AB-03
> Node: `root.contracts.runtime_support.agent_module.cross_venue_arbitrage`
> Parent: `root.contracts.runtime_support.agent_module`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Decision

`stop_split: true`

`root.contracts.runtime_support.agent_module.cross_venue_arbitrage` is closed as a terminal child.

## Evidence

Current owner file:

- `qrpc_runtime/src/agent_module/cross_venue_arbitrage.rs`

Terminal leaf control:

```text
Leaf: root.contracts.runtime_support.agent_module.cross_venue_arbitrage
Decision: STOP
Score: 27
Metrics: loc=175, functions=3, public_surface=1, branches=6, domains=2
Terminal leaf control: size_bucket=terminal_target_150_600, governance_mode=stop_split
```

## Why No Deeper Split

No deeper child is opened because:

- the child is within the terminal target size range;
- the three functions form one cohesive arbitrage policy owner;
- splitting explicit spread-signal path, quote-pair path, and cost-buffer helper would create algorithm micro-leaves;
- trading-semantics risk is high enough that extra movement would add governance cost without stronger module ownership;
- shared availability helper ownership remains parent-mediated for later shared-helper decisions.

## Equivalence Evidence

The extraction batch already passed:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`: 227 passed, 2 ignored
- `cargo check -p quantpilot`
- `git diff --check`
- `tools/check-utf8.ps1`
- `tools/check-full-feature-tree.ps1`
- `tools/check-matrix-governance.ps1`

## Next Step

BE-002AC-01 `root.contracts.runtime_support.agent_module` parent_residual_judgment selects `shared_scoring_portfolio_helpers`.
