# v4.16.0 root.contracts.qrpc_core.runtime_io_contract parent closeout

> Batch: BE-001SO-01
> Node: `root.contracts.qrpc_core.runtime_io_contract`
> Parent: `root.contracts.qrpc_core`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core.runtime_io_contract` closes as a compact parent facade.

Final parent file:

- `qrpc_core/src/runtime_io_contract.rs`

Closed runtime IO children:

- `market_data_io`
- `decision_flow`
- `execution_io`
- `portfolio_state`
- `runtime_output`
- `backtest_output`

## Parent Closeout Evidence

| Check | Result | Evidence |
| --- | --- | --- |
| Parent facade shape | pass | `qrpc_core/src/runtime_io_contract.rs` now contains only child `mod` declarations and `pub use` exports. |
| Public surface preserved | pass | `qrpc_core/src/lib.rs` continues re-exporting `runtime_io_contract::*`. |
| Child boundaries closed | pass | All six runtime IO children have single leaf closeout records with `stop_split: true`. |
| Parent-child communication | pass | Child dependencies are mediated through the runtime IO parent facade. |
| Open runtime IO residuals | pass | None remain under `root.contracts.qrpc_core.runtime_io_contract`. |

## Runtime IO Module Tree

```text
root.contracts.qrpc_core.runtime_io_contract
  -> market_data_io
  -> decision_flow
  -> execution_io
  -> portfolio_state
  -> runtime_output
  -> backtest_output
```

## Hard Boundaries

This closeout does not:

- change any Rust source code;
- change qrpc-core public exports;
- reopen closed runtime IO children;
- move RFC execution contracts, Core IR contracts, compiler bridge, runtime support, QuantScript, plugin metadata, or executor residuals;
- introduce release-transition optimization.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | Runtime IO parent and six children are named in the module tree. |
| parent_child_communication_kept | pass | Parent facade is the public aggregation point. |
| equivalence_baseline_freezable | pass | Each child has baseline, extraction closeout, and single leaf closeout evidence. |

leaf_split_decision_result

`parent_closeout: true`

`runtime_io_contract` no longer needs internal child selection. The cursor returns to `root.contracts.qrpc_core`.

## Next Step

BE-001SP-01 `root.contracts.qrpc_core` parent_residual_judgment selects the next qrpc-core residual candidate, expected `rfc_execution_contracts`.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
