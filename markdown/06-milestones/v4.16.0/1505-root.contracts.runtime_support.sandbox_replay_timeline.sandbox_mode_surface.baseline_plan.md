# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline.sandbox_mode_surface baseline plan

> Batch: BE-002CU-01
> Node: `root.contracts.runtime_support.sandbox_replay_timeline.sandbox_mode_surface`
> Parent: `root.contracts.runtime_support.sandbox_replay_timeline`
> Stage: `baseline_plan`
> Movement: No Rust code movement.

---

## Scope

Freeze the baseline for extracting the public sandbox mode/control surface from `qrpc_runtime/src/sandbox/mod.rs`.

Allowed movement:

- add `qrpc_runtime/src/sandbox/mode_surface.rs`;
- move only the public supported-key constants, runtime support boundary DTO/function, deterministic test mode DTO/enums, `SandboxMode`, `SandboxSnapshot`, and `Sandbox` trait;
- keep concrete sandbox implementations in `mod.rs`;
- keep helper functions and tests in `mod.rs`.

## In-Scope Surface

- `SUPPORTED_RUNTIME_MODE_KEYS`
- `SUPPORTED_RUNTIME_EXECUTION_MODULE_KEYS`
- `RuntimeSupportBoundary`
- `runtime_support_boundary`
- `DeterministicClockMode`
- `DeterministicEventOrdering`
- `DeterministicParallelismPolicy`
- `DeterministicTestMode`
- `SandboxMode`
- `SandboxSnapshot`
- `Sandbox`

## Out of Scope

- `RealTimeSandbox`
- `FastBacktestSandbox`
- `compute_benchmark_equity`
- `snapshot_from`
- `ensure_running`
- `trace_id`
- local sandbox tests
- replay/timeline children

## Equivalence Baseline

The next actual extraction must preserve:

- crate-root exports from `qrpc_runtime/src/lib.rs`;
- `Sandbox` trait method signatures;
- `SandboxSnapshot` serialization shape;
- deterministic test mode defaults and constructors;
- `runtime_support_boundary` key values;
- concrete sandbox implementations of the trait.

## Dependency Rule

Concrete sandbox implementations must consume the mode surface through parent-owned imports/re-exports. No sibling shortcut or release-transition optimization is allowed.

## Gates

Required gates for actual extraction:

- `cargo fmt`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime sandbox -- --nocapture`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 / full feature tree / matrix governance

## Next Step

BE-002CV-01 `root.contracts.runtime_support.sandbox_replay_timeline.sandbox_mode_surface` actual_extraction.
