# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline.fast_backtest_sandbox extract closeout

> Batch: BE-002DB-01
> Node: `root.contracts.runtime_support.sandbox_replay_timeline.fast_backtest_sandbox`
> Parent: `root.contracts.runtime_support.sandbox_replay_timeline`
> Stage: `actual_extraction`
> Movement: Rust code movement completed.

---

## Extraction Result

`FastBacktestSandbox` has been moved from `qrpc_runtime/src/sandbox/mod.rs` into:

- `qrpc_runtime/src/sandbox/fast_backtest_sandbox.rs`

The sandbox parent now declares `mod fast_backtest_sandbox;` and re-exports:

- `pub use self::fast_backtest_sandbox::FastBacktestSandbox;`

## Moved Surface

- `FastBacktestSandbox`
- fast-backtest constructors and replay compatibility constructors
- execution assumption and risk monitor helpers
- `FastBacktestSandbox::replay_timestamps`
- `FastBacktestSandbox::run_backtest`
- `impl Sandbox for FastBacktestSandbox`
- `compute_benchmark_equity`

## Preserved Parent Surface

The following remain parent-owned:

- `snapshot_from`
- `ensure_running`
- `trace_id`
- sandbox tests
- parent re-exports for concrete sandbox implementations

## Equivalence Notes

No public API has been removed. Existing callers still access `FastBacktestSandbox` through the sandbox parent re-export. The fast backtest child uses explicit imports for replay/timeline/type dependencies and parent-owned shared helpers.

## Gates

- `cargo fmt --check` PASS
- `cargo check -p qrpc-runtime` PASS
- `cargo test -p qrpc-runtime sandbox -- --nocapture` PASS: 21 passed, 2 ignored
- `git diff --check` PASS

## Next Step

BE-002DB-02 `root.contracts.runtime_support.sandbox_replay_timeline.fast_backtest_sandbox` single_leaf_closeout.
