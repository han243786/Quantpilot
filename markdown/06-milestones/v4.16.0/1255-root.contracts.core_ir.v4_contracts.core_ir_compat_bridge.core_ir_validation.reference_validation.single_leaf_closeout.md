# v4.16.0 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation.reference_validation single leaf closeout stops split

> Batch: BE-001WM-03
> Node: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation.reference_validation`
> Parent: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation`
> Stage: `single_leaf_closeout`
> Movement: No code movement. This closeout marks the reference-validation child terminal.

---

## Summary

root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation.reference_validation single leaf closeout stops split

`reference_validation` is now a terminal validation leaf. It has one parent-facing entrypoint and three tightly coupled expression traversal helpers under one diagnostic owner.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | PASS | The leaf owns Core IR object/reference validation and `V4BRIDGE040`-`046` diagnostic routing. |
| parent_child_communication_kept | PASS | Parent validation calls only `validate_core_ir_references_for_v4_bridge`; no sibling horizontal communication is needed. |
| equivalence_baseline_freezable | PASS | BE-001WM-02 passed `cargo fmt -p qrpc-core-ir`, `cargo test -p qrpc-core-ir`, and `cargo check -p quantpilot`. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | PARTIAL | The leaf is private, but it protects the public bridge acceptance path from invalid references. |
| state_machine_phase | FAIL | The leaf does not build or transition v4 machines. |
| strategy_branch | PASS | It branches across indicators, signals, agent policies, risk policies, scalar expressions, series expressions, and custom value expressions. |
| independent_failure_mode | PASS | It owns reference diagnostics separately from node/edge validation diagnostics. |
| reuse_pressure | PARTIAL | The leaf may be reused by future bridge variants, but current reuse remains internal. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | PASS | Further splitting scalar/series/custom helpers would create helper-only leaves without independent ownership. |
| communication_cost_rises | PASS | Splitting expression helpers would require extra shared id-set and diagnostic plumbing across tiny child modules. |
| local_proof_missing | PASS | Existing bridge tests remain the correct equivalence proof for this private validation leaf. |
| line_count_only | PASS | Stop decision is based on cohesion and communication cost, not just the 265-line size. |

leaf_split_decision_result

`stop_split: true`

The next recursive step returns to the `core_ir_validation` parent residual judgment.

next_recursive_step

BE-001WN-01 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation parent_residual_judgment
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/core_ir_compat_bridge/core_ir_validation/reference_validation.rs`

**Markers**:
- `BE-001WM-03`

**Next step**:
BE-001WN-01 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation parent_residual_judgment

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
