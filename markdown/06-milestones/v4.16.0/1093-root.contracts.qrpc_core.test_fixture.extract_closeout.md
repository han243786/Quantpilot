# v4.16.0 root.contracts.qrpc_core.test_fixture extract closeout

> Batch: BE-001TD-02
> Node: `root.contracts.qrpc_core.test_fixture`
> Parent: `root.contracts.qrpc_core`
> Stage: `extract_closeout`
> Movement: Rust cfg-test code moved under the same crate-root parent.

---

## Summary

`test_fixture` now owns the qrpc-core crate-root cfg-test helper and two regression tests that were previously embedded in `qrpc_core/src/lib.rs`.

Created:

- `qrpc_core/src/tests.rs`

Updated:

- `qrpc_core/src/lib.rs`

## Moved Surface

| Surface | Result |
| --- | --- |
| `sample_runtime_protocol` | Moved into `tests.rs` unchanged. |
| `canonical_digest_is_stable_for_equivalent_payloads` | Moved into `tests.rs` unchanged. |
| `run_and_backtest_specs_capture_protocol_boundary` | Moved into `tests.rs` unchanged. |
| `BTreeMap` cfg-test import | Moved into `tests.rs`. |
| Crate root facade | Replaced inline cfg-test module block with `#[cfg(test)] mod tests;`. |

## Equivalence Proof

| Check | Result | Evidence |
| --- | --- | --- |
| Helper fixture preserved | pass | `sample_runtime_protocol` values match BE-001TD-01. |
| Digest regression preserved | pass | Equivalent JSON digest assertions remain unchanged. |
| Run/backtest regression preserved | pass | Run spec, snapshot spec, and backtest spec assertions remain unchanged. |
| Production facade preserved | pass | Production module declarations and public re-exports in `lib.rs` remain unchanged. |
| Test parent view preserved | pass | `tests.rs` uses `super::*`, preserving the previous inline test module view. |

## Parent-Child Communication

The crate root remains the only local communication gateway:

- tests keep using `super::*`;
- production child modules remain private or public exactly as before;
- no direct runtime/backend/executor/frontend shortcut was introduced.

## Hard Boundary Result

No helper data, assertion, public export, production module declaration, serde shape, digest behavior, spec behavior, runtime behavior, backend behavior, executor behavior, frontend behavior, physical `plugins/*`, or release-transition pathway was changed.

## Next Step

BE-001TD-03 `root.contracts.qrpc_core.test_fixture` single_leaf_closeout.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
