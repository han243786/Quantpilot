# v4.16.0 root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness single leaf closeout

> Batch: BE-002IA-03
> Node: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness`
> Parent: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`integration_test_harness` is extracted but not closed.

The mandatory leaf granularity judge returned `PRECISION`, so this child must continue with precision single-leaf governance instead of being marked terminal.

## Leaf Granularity Evidence

Command:

- `tools/evaluate-leaf-granularity.ps1 -LeafId root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness -ParentId root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface -Path quantscript/src/lowering/orchestrator/integration_test_harness.rs -Depth 5`

Result:

- `Decision: PRECISION`
- `Split decision: CONTINUE`
- `Governance packaging: precision_single_leaf`
- `Score: 65`
- `loc=853`
- `functions=76`
- `public_surface=0`
- `branches=55`
- `domains=5`
- `risk_tags=cross_crate,trading_semantics`
- `size_bucket=oversized_over_800`
- `governance_mode=precision_single_leaf`

## Rationale

The harness is cohesive as a regression owner, but it is now too large to treat as a final terminal leaf under the current governance rules. It mixes several independent scenario groups:

- basic runtime config lowering smoke;
- runtime protocol compiler and coordinator integration;
- semantic rejection cases;
- manual indicator/formula recognition;
- spread formula admission and rejection;
- portfolio rebalance lowering and rejection.

Because these groups are test-only but cross crate and trading-semantics heavy, further splitting must be precision-scoped. The next step should choose one internal scenario group, freeze its current tests, and move only that group into a child test module.

## Parent Residual

Return to `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness` internal residual judgment.

Initial candidate queue:

- `basic_runtime_smoke_tests`
- `manual_formula_tests`
- `spread_lowering_tests`
- `rebalance_lowering_tests`
- `semantic_rejection_tests`

## Next Step

BE-002IB-01 `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness` parent_residual_judgment.
