# v4.16.0 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering single leaf closeout keeps split open

> Batch: BE-001XL-01
> Node: `root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering`
> Parent: `root.contracts.compiler_bridge.runtime_protocol_lowering`
> Stage: `single_leaf_closeout`
> Movement: no Rust code movement; post-extraction split decision only.

---

## Summary

`root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering` completed its first extraction, but this leaf must remain open.

The child still contains separable behavior pockets: parent-facing intent indicator/signal facade, structured condition lowering, fallback condition description, runtime comparison metadata decoding, spread-threshold condition lowering, and intent kind mapping.

Decision:

`stop_split: false`

Next step:

Return to parent residual judgment and select a smaller internal child before further Rust movement. The likely first residual candidate is `condition_lowering`, because it owns the branch-heavy MA/RSI/Momentum/ZScore/QuoteObserve structured expression construction and has strong local regression coverage.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | Residual children can be named as `condition_lowering`, `fallback_description`, `comparison_decode`, and facade/kind mapping. |
| parent_child_communication_kept | pass | Future children can remain under `intent_signal_lowering` and be called by that parent only. |
| equivalence_baseline_freezable | pass | Existing qrpc-compiler tests freeze structured condition output for MA, RSI, Momentum, ZScore, and spread branches. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | true | Parent-facing functions `lower_runtime_intent_to_indicator` and `lower_runtime_intent_to_signal_rule` coordinate several internal phases. |
| state_machine_phase | true | Structured condition lowering is a distinct phase inside runtime intent signal conversion. |
| strategy_branch | true | Condition lowering is separate from fallback text and intent kind mapping. |
| independent_failure_mode | true | Comparison metadata decoding can reject unsupported codes independently of indicator construction. |
| reuse_pressure | true | Comparison decoding is reused by MA, RSI, Momentum, ZScore, and spread condition branches. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | false | The next residual candidate owns multiple structured condition branches, not a one-line helper. |
| communication_cost_rises | false | Parent-mediated private calls can keep facade and branch helpers separate without sibling links. |
| local_proof_missing | false | Runtime protocol tests already assert representative structured condition outputs. |
| line_count_only | false | Continue-split is based on branch ownership and failure domain, not only 394-line size. |

leaf_split_decision_result

`continue_split`

`intent_signal_lowering` remains open. Do not add it to `closed_children` yet.

next_recursive_step

BE-001XM-01 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering parent_residual_judgment
## Boundary

**Real files**:
- `qrpc_compiler/src/runtime_protocol_lowering/intent_signal_lowering.rs`

**Markers**:
- `BE-001XL-01`
- `intent_signal_lowering stop_split: false`
- `condition lowering residual remains`
- `comparison decode residual remains`

**Next step**:
BE-001XM-01 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering parent_residual_judgment

---

## Gates

- `cargo test -p qrpc-compiler`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
