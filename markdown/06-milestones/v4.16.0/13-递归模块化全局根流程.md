# v4.16.0 递归模块化全局根流程

> 版本类型: MINOR architecture / governance。
> 基准: `07-顶层大模块统计.md`、`12-system十叶模块等价基线.md`。
> 执行档位: 重型。
> 判定: 将“顶层模块 -> 叶子抽离 -> 叶子整理 -> 是否继续细分 -> 递归处理 -> 全局根收束”固化为后续全量模块树的通用流程。

---

## 目标

本文件把后续模块化工作固定为递归流程:

```text
全局根 root
  -> 分顶层模块
  -> 选择一个顶层模块
  -> 做叶子抽离
  -> 整理这些叶子
  -> 判断叶子是否值得拆得更细
      -> 值得: 把该叶子当成新的局部顶层模块，重复同一流程
      -> 不值得: 标记该叶子完成
  -> 全部叶子完成后，标记该顶层模块完成
  -> 按同一规范处理其他顶层模块
  -> 六大顶层模块都完成后，收束到全局根和全量模块树
```

这不是一次性全量抽离，也不是无限细分。它是一个可停止、可验证、可递归的模块化算法。

这里的“整理这些叶子”指白箱整理和等价 closeout: 登记 owner、真实文件、public 入口、关键内部实现、未迁移边界、回归证据和是否继续细分。它不等同于代码目录整理、命名重排、旧实现删除或重构；这些仍属于后续整理/重构方案。

---

## 三矩阵影响声明

| 矩阵 | 影响节点 | 变更类型 |
| --- | --- | --- |
| 流程矩阵 | 递归模块化状态机、顶层完成判定、叶子细分判定 | 扩展 |
| 规范矩阵 | 父子通信、owner 归属、单叶 closeout、停止条件 | 扩展 |
| 引导矩阵 | 全局根、六大顶层模块、全量模块树、模块树白箱节点 | 扩展 |
| 模块树 | `root.system`、`root.backend`、`root.frontend`、`root.executor`、`root.contracts`、`root.docs` | 建立递归处理口径 |

---

## 引导坐标声明

| 坐标 | 指向 |
| --- | --- |
| 全量树节点 | `markdown/10-overview/overview-full-feature-tree.md` 根1-根7 |
| 模块树节点 | `root.system`、`root.backend`、`root.frontend`、`root.executor`、`root.contracts`、`root.docs`、递归产生的子叶节点 |
| 真实文件 | `markdown/06-milestones/v4.16.0/07-顶层大模块统计.md`、`08-system大模块分层统计.md`、`11-system抽离经验回填.md`、`12-system十叶模块等价基线.md`、`13-递归模块化全局根流程.md`、`markdown/00-matrix-governance/module-tree.md`、`markdown/10-overview/overview-full-feature-tree.md` |
| public 方法 | 每层递归都必须登记 public 入口、兼容 public 入口和关键内部实现；不得把内部 helper 冒充 public API |
| 测试/门禁 | `tools/check-matrix-governance.ps1`、`tools/check-full-feature-tree.ps1`、`tools/check-utf8.ps1`、受影响顶层模块的单测/契约测试/人工等价检查 |

---

## 六大顶层模块

| # | 顶层模块 | 全局根路径 | 当前处理状态 | 递归入口 |
| --- | --- | --- | --- | --- |
| 1 | `system` | `root.system` | 已完成顶层阶段性 closeout；S1-S10 均已完成 closeout 或静态 closeout | `12-system十叶模块等价基线.md`、`24-system顶层阶段性closeout.md`、`25-system.build_delivery.S6-S9恢复提案与适配性校验.md` |
| 2 | `backend` | `root.backend` | 已进入 R5: 9 个 L2 叶子已完成逐叶 closeout，9 叶均已有子 facade 坐标；其余 8 叶已补逐叶完成记录；`backend.runtime.routes` 已完成 route aggregate 抽离，`backend.runtime.routes.run` 已完成 run route group 抽离和单叶 closeout，`runtime.run.v4_handoff` 已完成抽离与单叶 closeout，当前停止内部细分；`runtime.run.session_start` 已完成抽离与单叶 closeout，当前停止内部细分；`runtime.run.record_store` 已完成抽离与单叶 closeout，当前停止内部细分；`runtime.run.replay_status` 已完成抽离与单叶 closeout，当前停止内部细分；`runtime.event_stream` 已完成抽离与单叶 closeout，当前停止内部细分；`runtime.backtest.record_store` 已完成单叶 closeout 并设置 `stop_split: true`；`runtime.backtest.replay` 已完成单叶 closeout 并设置 `stop_split: true`；`runtime.backtest.experiment_sweep.parameter_grid` 已完成单叶 closeout 并设置 `stop_split: true`；BE-001X-01 `runtime.backtest.experiment_sweep` 父叶残余判断已完成，父叶仍 `stop_split: false`，下一候选为 `runtime.backtest.experiment_sweep.start_orchestration` / BE-001Y-01 | `03-后端抽离登记.md`、`06-后端接口边界首批抽离方案.md`、`28-backend大模块分层统计.md`、`29-backend.interface_boundary等价基线.md`、`30-backend九叶模块壳抽离记录.md`、`31-39 backend 单叶 closeout`、`40-backend.strategy_config_L3模块壳抽离记录.md`、`41-backend其余八叶模块壳抽离记录.md`、`42-49 backend 子叶抽离完成记录`、`50-backend.runtime.routes单子叶等价基线.md`、`51-backend.runtime.routes抽离记录.md`、`52-backend.runtime.routes.run单子叶等价基线.md`、`53-backend.runtime.routes.run抽离记录.md`、`54-backend.runtime.routes.run单叶closeout.md`、`55-runtime.run.v4_handoff单子叶等价基线.md`、`56-runtime.run.v4_handoff抽离记录.md`、`57-runtime.run.v4_handoff单叶closeout.md`、`58-runtime.run.session_start单子叶等价基线.md`、`59-runtime.run.session_start抽离记录.md`、`60-runtime.run.session_start单叶closeout.md`、`61-runtime.run.record_store单子叶等价基线.md`、`62-runtime.run.record_store真实边界梳理.md`、`63-runtime.run.record_store抽离方案.md`、`64-runtime.run.record_store抽离记录.md`、`65-runtime.run.record_store单叶closeout.md`、`66-runtime.run.replay_status单子叶等价基线.md`、`67-runtime.run.replay_status抽离方案.md`、`68-runtime.run.replay_status抽离记录.md`、`69-runtime.run.replay_status单叶closeout.md`、`70-runtime.event_stream单子叶等价基线.md`、`71-runtime.event_stream抽离方案.md`、`72-runtime.event_stream抽离记录.md`、`73-runtime.event_stream单叶closeout.md`、`74-runtime.backtest单子叶等价基线.md`、`75-runtime.backtest抽离方案.md`、`99-runtime.backtest.record_store单子叶等价基线.md`、`100-runtime.backtest.record_store抽离方案.md`、`101-runtime.backtest.record_store抽离记录.md`、`102-runtime.backtest.record_store单叶closeout.md`、`103-runtime.backtest.replay单子叶等价基线.md`、`104-runtime.backtest.replay抽离方案.md`、`105-runtime.backtest.replay抽离记录.md`、`106-runtime.backtest.replay单叶closeout.md`、`107-runtime.backtest.experiment_sweep单子叶等价基线.md`、`108-runtime.backtest.experiment_sweep抽离方案.md`、`109-runtime.backtest.experiment_sweep抽离记录.md`、`110-runtime.backtest.experiment_sweep单叶closeout.md`、`111-runtime.backtest.experiment_sweep.parameter_grid单子叶等价基线.md`、`112-runtime.backtest.experiment_sweep.parameter_grid抽离方案.md`、`113-runtime.backtest.experiment_sweep.parameter_grid抽离记录.md`、`114-runtime.backtest.experiment_sweep.parameter_grid单叶closeout.md`、`115-runtime.backtest.experiment_sweep父叶残余判断.md` |
| 3 | `frontend` | `root.frontend` | 延后，等待后端接口边界稳定 | `04-前端抽离登记.md` |
| 4 | `executor` | `root.executor` | 延后，不迁移执行端状态所有权 | 后续 executor 抽离登记 |
| 5 | `contracts` | `root.contracts` | 白箱覆盖缺口，需先补 `contracts.*` 节点 | 后续 contracts 抽离登记 |
| 6 | `docs` | `root.docs` | 治理控制面已维护 | `docs.matrix_governance`、`docs.feature_tree` |

---

## 递归状态机

| 状态 | 输入 | 输出 | 通过条件 |
| --- | --- | --- | --- |
| R0 顶层模块确认 | 全局根、顶层候选 | 顶层模块清单 | 顶层 owner 和真实范围清楚 |
| R1 叶子划分 | 单个顶层模块 | L2 叶子清单 | 每个叶子都有 owner、真实文件和边界说明 |
| R2 叶子抽离 | 单个叶子 | 候选模块、兼容桥、回退点 | 单叶单批，不跨父域 |
| R3 叶子整理 | 已抽离或已登记叶子 | 白箱 closeout、等价基线、public/内部实现分类、未迁移边界 | 能证明功能与之前一致，且不借代码整理或重构解释完成 |
| R4 细分价值判断 | 已整理叶子 | 继续细分 / 停止细分 | 有明确 owner 和独立等价证据才继续拆 |
| R5 局部递归 | 值得细分的叶子 | 新的局部顶层模块 | 仿照 R0-R4 重复处理 |
| R6 顶层完成 | 顶层所有叶子 | 顶层模块完成记录 | 所有叶子完成或明确停止细分 |
| R7 全局根收束 | 六大顶层完成记录 | 全量模块树完成 | 全局根能解释所有 active 模块和边界 |

---

## 细分价值判定

叶子只有满足下列条件之一，才值得继续拆得更细:

| 值得细分的信号 | 说明 |
| --- | --- |
| 独立 owner | 叶子内部存在两个以上稳定 owner，且父子通信可以清楚表达 |
| 独立 public 入口 | 子叶有独立 public 入口或明确兼容入口 |
| 独立状态/锁/持久化 | 子叶拥有不同状态、锁顺序、事务或持久化责任 |
| 独立验证证据 | 子叶能单独用测试、契约、artifact 或人工清单证明等价 |
| 独立发布/构建边界 | 子叶对应单独构建、CI、交付、schema 或配置边界 |
| 长期演进频率不同 | 子叶变化频率明显不同，放在一起会制造冲突 |

满足下列情况时，不值得继续拆:

| 不值得细分的信号 | 说明 |
| --- | --- |
| 只是文件夹好看 | 没有独立 owner，只是目录美化 |
| 只是 helper 分组 | 内部 helper 没有 public/兼容入口和独立验证 |
| 只是 generated asset | 生成资产没有独立行为 owner |
| 会增加横向通信 | 拆开后需要绕过父模块互相调用 |
| 需要整理或重构才能解释完成 | 当前仍不进入整理/重构 |

---

## 顶层模块完成判定

一个顶层模块只有同时满足以下条件，才能视为处理完成:

1. 顶层模块的叶子清单已固定或明确暂缓。
2. 每个叶子都有 public 入口、关键内部实现、保留外部边界和等价证据。
3. 每个叶子都完成抽离、完成登记，或明确不值得继续拆。
4. 值得细分的叶子已经按同一递归流程处理完毕。
5. 没有未解释的横向连接、owner 冲突、新旧入口冲突或测试缺口扩大。
6. closeout 写明“抽离完成”还是“整理/重构仍未启动”。

---

## 全局根完成判定

六大顶层模块全部完成后，才能宣告全局根完成:

| 全局根条件 | 要求 |
| --- | --- |
| 六大顶层齐全 | `system`、`backend`、`frontend`、`executor`、`contracts`、`docs` 都有完成记录 |
| 全量模块树齐全 | 模块树能定位每个 active 模块、叶子、真实文件和 public 入口 |
| 全量树齐全 | 全量树能覆盖所有 active 文件和治理索引 |
| 父子通信闭环 | 叶子只能经父模块或明确 adapter 通信 |
| 停止条件可解释 | 每个叶子为什么停止细分都能被审查 |
| 无发布态偷渡 | 未经开发者明确声明，不引入发布态横向连接 |

---

## 当前应用到 system

`system` 现在处于 R4 与 R6 之间:

1. R0 顶层模块确认已完成: `root.system` 是六大顶层之一。
2. R1 叶子划分已完成: 3 层、10 个叶子。
3. R2 叶子抽离已部分完成: S2 `system.entry.backend_process` 已完成正式单叶 closeout。
4. R3 叶子整理已推进: `12-system十叶模块等价基线.md` 已铺好，S1/S2/S3/S4/S5/S7/S8/S10 都有 closeout 或静态 closeout。
5. R4 细分价值判断已完成当前抽离阶段判断: S1-S10 均已停止细分或完成静态 closeout。

因此 system 新分出来的叶子已经完成本阶段能做的 R3/R4 整理: S1-S10 都已收束。`24-system顶层阶段性closeout.md` 已刷新为 R6 阶段性收束。若继续 system，必须另起整理、重构、发布验收或具体变更方案；否则应转向下一个顶层模块或后端接口边界后续批次。

---

## 当前应用到 backend

`backend` 现在处于 R5:

1. R0 顶层模块确认已完成: `root.backend` 是六大顶层之一。
2. R1 叶子划分已启动: `28-backend大模块分层统计.md` 登记 9 个 L2 叶子候选。
3. R2 首批叶子已落壳: `30-backend九叶模块壳抽离记录.md` 建立 `src/backend/` 与 9 个叶子 facade。
4. R3 叶子整理已完成当前范围: `31-39 backend 单叶 closeout` 已为 9 个叶子登记 owner、真实文件、public/接口入口、兼容桥、回退点和等价证据。
5. R4 细分价值判断已完成当前判断: `backend.interface_boundary`、`backend.capability`、`backend.app_state_wiring`、`backend.test_support` 停止细分；`backend.strategy_config`、`backend.runtime`、`backend.graph_compile`、`backend.storage_security`、`backend.ops_governance` 值得进入下一轮 L3 等价基线。
6. R5 局部递归已启动: `40-backend.strategy_config_L3模块壳抽离记录.md` 已建立 artifact、preflight、diff、AI proposal binding 四个 L3 子叶 facade；`41-backend其余八叶模块壳抽离记录.md` 已为其余 8 叶建立薄壳子 facade，`42-49` 已分别补齐其余 8 叶的逐叶完成记录。
7. R5 单子叶基线已启动: `50-backend.runtime.routes单子叶等价基线.md` 选择 `backend.runtime.routes` 作为首个 BE-001F 子叶，固定 route aggregate facade、真实 runtime owner 和等价证据。
8. R5 单子叶抽离已完成: `51-backend.runtime.routes抽离记录.md` 将 runtime route aggregate 列表迁入 `src/backend/runtime/routes.rs`，handler、state owner、event stream、artifact schema 和 persistence 保留原位。
9. R5 子叶递归已继续: `52-backend.runtime.routes.run单子叶等价基线.md` 选择 `backend.runtime.routes.run`，`53-backend.runtime.routes.run抽离记录.md` 将 run route group 迁入 `src/backend/runtime/routes/run.rs`，handler、state owner、event stream 和 persistence 仍保留原位。
10. R5 单叶 closeout 已完成: `54-backend.runtime.routes.run单叶closeout.md` 确认 route facade 本身停止细分，但 `src/runtime/run.rs` handler 层值得另起基线，默认下一候选为 `runtime.run.v4_handoff`。
11. R5 handler 层单子叶基线已启动: `55-runtime.run.v4_handoff单子叶等价基线.md` 固定 `/api/runtime/v4/run` 的 source/graph handoff、initial event、paper simulated runtime 和 `api_run` 证据。
12. R5 handler 层单子叶抽离已完成: `56-runtime.run.v4_handoff抽离记录.md` 将 v4 handoff handler/type/helper 迁入 `src/runtime/run/v4_handoff.rs`，父级 `runtime` 保留受控 re-export，`src/runtime/run.rs` 仍保留 legacy run/session/record/replay/status sibling。
13. R5 handler 层单叶 closeout 已完成: `57-runtime.run.v4_handoff单叶closeout.md` 判定本叶停止内部细分；下一步若继续 run handler，必须回到 `runtime.run` sibling 队列重新建基线。
14. R5 handler sibling 基线已建立: `58-runtime.run.session_start单子叶等价基线.md` 固定 legacy `/api/runtime/test-run`、`start_test_run`、run lock、compile path、sandbox session、event envelope 与 in-memory run record 等价边界；当前不宣告抽离完成。
15. R5 handler sibling 抽离已完成: `59-runtime.run.session_start抽离记录.md` 将 `start_test_run` 迁入 `src/runtime/run/session_start.rs`，父级 `runtime` 保留兼容 re-export，`src/runtime/run.rs` 继续保留 record/replay/status/SSE sibling。
16. R5 handler sibling 单叶 closeout 已完成: `60-runtime.run.session_start单叶closeout.md` 判定本叶停止内部细分；下一步若继续 run handler，应回到 `runtime.run` sibling 队列，默认候选为 `runtime.run.record_store`。
17. R5 handler sibling 基线已建立: `61-runtime.run.record_store单子叶等价基线.md` 固定 run record list/detail/save/discard、persistence、audit、state owner 和 `api_run` 证据；当前不宣告抽离完成。
18. R5 handler sibling 真实边界已梳理: `62-runtime.run.record_store真实边界梳理.md` 校正 discard 真实 route 为 `DELETE /api/runtime/runs/:run_id`，并明确第一轮抽离只迁移四个 handler，不迁移 shared helper owner。
19. R5 handler sibling 抽离方案已建立: `63-runtime.run.record_store抽离方案.md` 锁定下一轮只移动 `list_runs`、`get_run_detail`、`save_run_record`、`discard_run_record`，通过父级 `runtime` re-export 保持 route facade 不变。
20. R5 handler sibling 抽离已完成: `64-runtime.run.record_store抽离记录.md` 将四个 record_store handler 迁入 `src/runtime/run/record_store.rs`，父级 `runtime` 保留兼容 re-export，`src/runtime/run.rs` 继续保留 replay/status/SSE sibling。
21. R5 handler sibling 单叶 closeout 已完成: `65-runtime.run.record_store单叶closeout.md` 判定本叶停止内部细分；下一步若继续 run handler，应回到 `runtime.run` sibling 队列，默认候选为 `runtime.run.replay_status`。
22. R5 handler sibling 基线已建立: `66-runtime.run.replay_status单子叶等价基线.md` 固定 run replay/status 两个 handler、response mapping owner、metrics owner 和 SSE 排除边界；当前不宣告代码迁移完成。
23. R5 handler sibling 抽离方案已建立: `67-runtime.run.replay_status抽离方案.md` 锁定下一轮只移动 `get_run_replay` 与 `get_run_status`，通过父级 `runtime` re-export 保持 route facade 不变。
24. R5 handler sibling 实际抽离已完成: `68-runtime.run.replay_status抽离记录.md` 已将 `get_run_replay` 与 `get_run_status` 迁入 `src/runtime/run/replay_status.rs`；当前不宣告 SSE、response mapping、schema、metrics、state/persistence owner 或本叶 closeout 已完成。
25. R5 handler sibling 单叶 closeout 已完成: `69-runtime.run.replay_status单叶closeout.md` 判定本叶停止内部细分；下一步若继续 runtime route，应回到父级 `backend.runtime.routes` sibling 队列，默认候选为 `runtime.event_stream`。
26. R5 父级 sibling 基线已建立: `70-runtime.event_stream单子叶等价基线.md` 固定 `/api/runtime/runs/:run_id/events`、`stream_run_events`、SSE frame order、keep-alive 和父级 route owner；当前不宣告代码迁移完成。
27. R5 父级 sibling 抽离方案已建立: `71-runtime.event_stream抽离方案.md` 锁定下一批只移动 `stream_run_events`，保留 route facade、shared helper、state/persistence/frontend 和 SSE 生命周期；当前不移动代码。
28. R5 父级 sibling 抽离已完成: `72-runtime.event_stream抽离记录.md` 将 `stream_run_events` 迁入 `src/runtime/event_stream.rs`，父级 `runtime` 保留兼容 re-export，route facade、shared helper、state/persistence/frontend 和 SSE 生命周期均不迁移。
29. R5 父级 sibling 单叶 closeout 已完成: `73-runtime.event_stream单叶closeout.md` 判定本叶停止内部细分；下一步若继续 runtime route，应回到父级 `backend.runtime.routes` sibling 队列，默认候选为 `runtime.backtest`。
30. R5 父级 sibling 基线已建立: `74-runtime.backtest单子叶等价基线.md` 固定 backtest route group、handler、artifact/compare/replay/persistence/shared owner 和禁止迁移边界；当前不移动代码。
31. R5 父级 sibling 抽离方案已建立: `75-runtime.backtest抽离方案.md` 锁定下一批只抽离 backtest route facade 到计划目标文件，handler、artifact、compare、persistence、schema、state 和 frontend owner 均不迁移。
32. R5 父级 sibling route facade 抽离已完成: `76-runtime.backtest抽离记录.md` 将 backtest route group 迁入 `src/backend/runtime/routes/backtest.rs`；handler、artifact、compare、persistence、schema、state、frontend owner 和发布过渡均不迁移。
33. R5 父级 sibling 单叶 closeout 已完成: `77-runtime.backtest单叶closeout.md` 判定 route facade 本身停止细分；`src/runtime/backtest.rs` handler 域仍值得继续递归，默认下一候选为 `runtime.backtest.execution_start`。
34. R5 handler 域单子叶基线已建立: `78-runtime.backtest.execution_start单子叶等价基线.md` 冻结 backtest 创建路径、legacy/v4 execution helper、artifact generation、governance event 和 transient spill 边界；当前不移动代码。
35. R5 handler 域抽离方案已建立: `79-runtime.backtest.execution_start抽离方案.md` 锁定下一批只移动 backtest 创建路径 handler/helper，并通过父级兼容出口保留 `start_backtest_run` 与 `execute_backtest_request` 复用桥；当前不移动代码。
36. R5 handler 域第一轮物理抽离已完成: `80-runtime.backtest.execution_start抽离记录.md` 将 backtest 创建路径 handler/helper 迁入 `src/runtime/backtest/execution_start.rs`；父级 `runtime` 保留 `start_backtest_run` re-export 和 `execute_backtest_request` experiment 复用桥。
37. R5 handler 域单叶 closeout 已完成: `81-runtime.backtest.execution_start单叶closeout.md` 确认 `runtime.backtest.execution_start` 等价，但不设置 `stop_split: true`；下一轮递归候选为 `runtime.backtest.execution_start.v4_projection`。
38. R5 handler 域下一轮单子叶基线已建立: `82-runtime.backtest.execution_start.v4_projection单子叶等价基线.md` 冻结 v4 artifact projection helper、单元测试和 API 回归证据；当前不移动代码。
39. R5 handler 域下一轮抽离方案已建立: `83-runtime.backtest.execution_start.v4_projection抽离方案.md` 锁定下一批只移动 projection helper 与现有两个单元测试；当前不移动代码。
40. R5 handler 域下一轮第一轮物理抽离已完成: `84-runtime.backtest.execution_start.v4_projection抽离记录.md` 将 projection helper 与现有两个单元测试迁入 `src/runtime/backtest/v4_projection.rs`；request resolution、record write、schema、state、persistence、frontend 和发布过渡均不迁移。
41. R5 handler 域下一轮单叶 closeout 已完成: `85-runtime.backtest.execution_start.v4_projection单叶closeout.md` 确认 projection 子模块等价并设置 `stop_split: true`；下一候选回到父叶 `runtime.backtest.execution_start.v4_request_resolution`。
42. R5 handler 域下一轮单子叶基线已建立: `86-runtime.backtest.execution_start.v4_request_resolution单子叶等价基线.md` 冻结 v4 请求识别、graph/symbol/event resolution、错误 code 和兼容桥；当前不移动代码。
43. R5 handler 域下一轮抽离方案已建立: `87-runtime.backtest.execution_start.v4_request_resolution抽离方案.md` 锁定下一批只移动四个 request resolution helper；当前不移动代码。
44. R5 handler 域下一轮物理抽离已完成: `88-runtime.backtest.execution_start.v4_request_resolution抽离记录.md` 将四个 request resolution helper 迁入 `src/runtime/backtest/v4_request_resolution.rs`；replay/runtime execution、projection、record write、schema、state、persistence、frontend 和发布过渡均不迁移。
45. R5 handler 域下一轮单叶 closeout 已完成: `89-runtime.backtest.execution_start.v4_request_resolution单叶closeout.md` 确认 request resolution 子模块等价并设置 `stop_split: true`；下一候选回到父叶 `runtime.backtest.execution_start.v4_runtime_execution`。
46. R5 handler 域下一轮单子叶基线已建立: `90-runtime.backtest.execution_start.v4_runtime_execution单子叶等价基线.md` 冻结 deterministic bars/ticks、`V4PaperSimulatedRuntime`、`spawn_blocking` 和 `V4BacktestArtifact` 输出；当前不移动代码。
47. R5 handler 域下一轮抽离方案已建立: `91-runtime.backtest.execution_start.v4_runtime_execution抽离方案.md` 锁定下一批只移动 deterministic runtime execution 最小 helper；当前不移动代码。
48. R5 handler 域下一轮物理抽离已完成: `92-runtime.backtest.execution_start.v4_runtime_execution抽离记录.md` 将 deterministic bars/ticks、blocking runtime replay 和 `run_v4_backtest_runtime_execution` 迁入 `src/runtime/backtest/v4_runtime_execution.rs`；expanded graph、request resolution、projection、record write、schema、state、persistence、frontend 和发布过渡均不迁移。
49. R5 handler 域下一轮单叶 closeout 已完成: `93-runtime.backtest.execution_start.v4_runtime_execution单叶closeout.md` 确认 runtime execution 子模块等价并设置 `stop_split: true`；下一候选回到父叶 `runtime.backtest.execution_start`。

50. R5 handler 域父叶候选基线已建立: `94-runtime.backtest.execution_start.legacy_dispatch单子叶等价基线.md` 冻结 legacy non-v4 path 的 QS compile、execution assumption override、compile artifact bundle、blocking FastBacktestSandbox replay 和排除边界；当前 `no code movement`，下一步只能先做 BE-001R-02 抽离方案。
51. R5 handler 域父叶候选抽离方案已建立: `95-runtime.backtest.execution_start.legacy_dispatch抽离方案.md` 限定下一批只迁移 legacy compile/sandbox dispatch 最小 helper；当前 `no code movement`，下一步若继续只能进入 BE-001R-03 实际抽离。
52. R5 handler 域父叶候选物理抽离已完成: `96-runtime.backtest.execution_start.legacy_dispatch抽离记录.md` 将 legacy compile/assumption/artifact/sandbox replay 两段式 helper 迁入 `src/runtime/backtest/legacy_dispatch.rs`；record assembly、artifact views、transient spill、state write、audit log、schema、persistence、frontend 和发布过渡均不迁移。
53. R5 handler 域父叶候选单叶 closeout 已完成: `97-runtime.backtest.execution_start.legacy_dispatch单叶closeout.md` 确认 legacy dispatch helper 等价并设置 `stop_split: true`；下一步回到 `runtime.backtest.execution_start` 父叶残余判断或 `runtime.backtest` 上层队列。
54. R5 handler 域父叶残余判断已完成: `98-runtime.backtest.execution_start父叶残余判断.md` 确认 `execution_start` 当前不再私拆 record/state/persistence 边界，下一步回到 `runtime.backtest` 上层队列，默认候选为 `runtime.backtest.record_store` / BE-001T-01。
55. R5 backtest handler 域上层候选基线已建立: `99-runtime.backtest.record_store单子叶等价基线.md` 冻结 backtest list/detail/save/discard、transient/persistent record、artifact view、audit 和排除边界；当前 `no code movement`，下一步只能先做 BE-001T-02 抽离方案。
56. R5 backtest handler 域 record_store 抽离方案已建立: `100-runtime.backtest.record_store抽离方案.md` 限定下一批只迁移 `list_backtests`、`get_backtest_detail`、`save_backtest_record`、`discard_backtest_record` 四个 handler；当前 `no code movement`，下一步只能进入 BE-001T-03 实际抽离记录。
57. R5 backtest handler 域 record_store 第一轮物理抽离已完成: `101-runtime.backtest.record_store抽离记录.md` 将四个 handler 迁入 `src/runtime/backtest/record_store.rs`；route facade、shared helper owner、state owner、persistence owner、artifact/transient owner、frontend route 和发布过渡均不迁移。
58. R5 backtest handler 域 record_store 单叶 closeout 已完成: `102-runtime.backtest.record_store单叶closeout.md` 确认四个 handler 等价并设置 `stop_split: true`；下一步回到 `runtime.backtest` sibling 队列，默认先为 `runtime.backtest.replay` 建立等价基线。
59. R5 backtest handler 域 replay 单子叶基线已建立: `103-runtime.backtest.replay单子叶等价基线.md` 冻结 replay route、record lookup、query normalization、response mapping、metrics 和排除边界；当前 `no code movement`，下一步只能先做 BE-001U-02 抽离方案。
60. R5 backtest handler 域 replay 抽离方案已建立: `104-runtime.backtest.replay抽离方案.md` 限定下一批只迁移 `get_backtest_replay`，并保持 route facade、record lookup、query normalization、response mapping、schema、metrics、state/persistence、artifact schema、frontend caller 和发布过渡边界不变。
61. R5 backtest handler 域 replay 第一轮物理抽离已完成: `105-runtime.backtest.replay抽离记录.md` 将 `get_backtest_replay` 迁入 `src/runtime/backtest/replay.rs`；父级 re-export、route facade、record lookup、query normalization、response mapping、schema、metrics、state/persistence、artifact schema、frontend caller 和发布过渡均不迁移。
62. R5 backtest handler 域 replay 单叶 closeout 已完成: `106-runtime.backtest.replay单叶closeout.md` 确认 `get_backtest_replay` 等价并设置 `stop_split: true`；下一步回到 `runtime.backtest` sibling 队列，默认先为 `runtime.backtest.experiment_sweep` 建立等价基线。
63. R5 backtest handler 域 experiment_sweep 单子叶基线已建立: `107-runtime.backtest.experiment_sweep单子叶等价基线.md` 冻结 experiment routes、参数网格、`execute_backtest_request` 复用桥、experiment persistence、save/discard lifecycle、audit、response mapping 和排除边界；当前 `no code movement`，下一步只能先做 BE-001V-02 抽离方案。
64. R5 backtest handler 域 experiment_sweep 抽离方案已建立: `108-runtime.backtest.experiment_sweep抽离方案.md` 限定下一批只迁移 5 个 experiment handler 和 3 个参数网格 helper；当前 `no code movement`，下一步只能进入 BE-001V-03 实际抽离记录。
65. R5 backtest handler 域 experiment_sweep 第一轮物理抽离已完成: `109-runtime.backtest.experiment_sweep抽离记录.md` 将 5 个 experiment handler 和 3 个参数网格 helper 迁入 `src/runtime/backtest/experiment_sweep.rs`；下一步只能进入 BE-001V-04 单叶 closeout。
66. R5 backtest handler 域 experiment_sweep 单叶 closeout 已完成: `110-runtime.backtest.experiment_sweep单叶closeout.md` 确认本叶等价但 `stop_split: false`；下一步只能为 `runtime.backtest.experiment_sweep.parameter_grid` 建立 BE-001W-01 单子叶等价基线。
67. R5 backtest handler 域 experiment_sweep.parameter_grid 单子叶等价基线已建立: `111-runtime.backtest.experiment_sweep.parameter_grid单子叶等价基线.md` 冻结参数网格校验、轴归一化、base fallback、去重、variant count 和展开顺序；当前 `no code movement`，下一步只能进入 BE-001W-02 抽离方案。
68. R5 backtest handler 域 experiment_sweep.parameter_grid 抽离方案已建立: `112-runtime.backtest.experiment_sweep.parameter_grid抽离方案.md` 限定下一批只迁移 3 个 helper 到父级私有子模块；当前 `no code movement`，下一步只能进入 BE-001W-03 实际抽离记录。
69. R5 backtest handler 域 experiment_sweep.parameter_grid 第一轮物理抽离已完成: `113-runtime.backtest.experiment_sweep.parameter_grid抽离记录.md` 将 3 个参数网格 helper 迁入 `src/runtime/backtest/parameter_grid.rs`；下一步只能进入 BE-001W-04 单叶 closeout。
70. R5 backtest handler 域 experiment_sweep.parameter_grid 单叶 closeout 已完成: `114-runtime.backtest.experiment_sweep.parameter_grid单叶closeout.md` 确认本叶等价并设置 `stop_split: true`；下一步回到 `runtime.backtest.experiment_sweep` 父叶残余判断，默认进入 BE-001X-01。
71. R5 backtest handler 域 experiment_sweep 父叶残余判断已完成: `115-runtime.backtest.experiment_sweep父叶残余判断.md` 确认 `parameter_grid` 已关闭但父叶仍 `stop_split: false`；下一步只能进入 BE-001Y-01 `runtime.backtest.experiment_sweep.start_orchestration` 单子叶等价基线。

因此 backend 当前不能宣称顶层完成。下一步若继续 runtime handler，只能进入 BE-001Y-01 `runtime.backtest.experiment_sweep.start_orchestration` 单子叶等价基线；不应混入 request resolution、projection、record_store、event stream、run replay/status、replay、report/mutation、artifact schema owner、compare owner、response mapping owner、schema owner、state/persistence owner 或 frontend route 迁移。任何 handler、schema、state、auth/storage 安全实现或测试资产迁移动作，都必须另起提案并回到适配性校验。

---

## 禁止事项

- 不一次性推进一个顶层模块的全部叶子抽离。
- 不在叶子等价基线未完成前继续拆 L3。
- 不把整理、重构、旧实现退役混入抽离批次。
- 不把配置样例、generated schema、构建脚本误判为 runtime 行为真源。
- 不让 AI 主动提出发布版本过渡或横向连接。

---

## 验收标准

1. 递归模块化流程进入 v4.16 里程碑索引。
2. 顶层模块、叶子抽离、叶子整理、细分价值判断、顶层完成、全局根收束都有明确状态。
3. `system` 的下一步能自然落到单叶 closeout。
4. 后续 `backend`、`frontend`、`executor`、`contracts`、`docs` 能复用同一流程。
5. 治理门禁能发现本流程文档缺失。
6. `runtime.run.session_start` 已完成 closeout 并停止内部细分；后续 run handler 递归必须回到 sibling 队列，不能把 record/replay/SSE/state owner 混入本叶。
7. `runtime.run.record_store` 已建立单子叶等价基线；后续若继续只能先做 record store 抽离方案，不能把 replay/status、SSE、state owner 或 persistence owner 迁移混入基线批次。
8. `runtime.run.record_store` 真实边界已校正；后续抽离不得引入 `/discard` route，不得把 `load_run_record_from_state`、`persist_run_record`、`sanitize_storage_path_segment` 等 shared helper 私有化。
9. `runtime.run.record_store` 抽离方案已建立；下一步实际抽离只允许移动四个 handler，并必须保持父级 re-export、route facade、shared helper owner、state owner 和 frontend route 不变。
10. `runtime.run.record_store` 第一轮物理抽离已完成；后续必须先做单叶 closeout，不能直接细拆 persistence/audit/response projection helper。
11. `runtime.run.record_store` 单叶 closeout 已完成；本叶当前停止内部细分，下一步默认进入 `runtime.run.replay_status` 等价基线，不能把 `runtime.event_stream` 混入 run route group。
12. `runtime.run.replay_status` 等价基线已建立；后续若继续只能先做抽离方案，不能直接迁移代码，不能把 SSE、response mapping owner、schema owner、metrics owner、state owner 或 persistence owner 混入。
13. `runtime.run.replay_status` 抽离方案已建立；下一步实际抽离只允许移动 `get_run_replay` 与 `get_run_status`，并必须保持父级 re-export、route facade、SSE、response mapping owner、schema owner、metrics owner、state owner 和 persistence owner 不变。
14. `runtime.run.replay_status` 实际抽离已完成；下一步只能做本叶 closeout，确认两个 handler 等价并判断是否继续细拆，不能顺手迁移 SSE 或 shared owner。
15. `runtime.run.replay_status` 单叶 closeout 已完成；本叶停止内部细分，下一步默认回到父级 `runtime.event_stream` 候选，不能把 SSE 当作 replay_status 的内部子叶。
16. `runtime.event_stream` 等价基线已建立；后续若继续只能先做抽离方案，不能直接迁移代码，不能把 replay/status、record store、state owner、persistence owner 或 frontend caller 混入。
17. `runtime.event_stream` 抽离方案已建立；后续若继续只能做最小物理抽离，不能借机迁移 shared helper、route facade、state owner、persistence owner、frontend caller 或发布过渡连接。
18. `runtime.event_stream` 第一轮抽离已完成；后续若继续只能先做单叶 closeout，不能直接继续拆 shared helper、SSE lifecycle、state owner、persistence owner 或 frontend caller。
19. `runtime.event_stream` 单叶 closeout 已完成；本叶停止内部细分，下一步默认回到父级 `runtime.backtest` 候选，不能把 backtest 当作 event stream 的内部子叶。
20. `runtime.backtest` 等价基线已建立；后续若继续只能先做抽离方案，不能直接迁移代码，不能把 experiment/report/mutation、artifact schema、compare owner、replay helper、state owner、persistence owner 或 frontend caller 混入。
21. `runtime.backtest` 抽离方案已建立；后续若继续只能做 route facade 最小抽离，不能直接切 handler、artifact、compare、persistence、schema、state 或 frontend owner。
22. `runtime.backtest` route facade 抽离已完成；后续只能先做单叶 closeout，不能直接继续拆 handler、artifact、compare、persistence、schema、state 或 frontend owner。
23. `runtime.backtest` 单叶 closeout 已完成；后续默认进入 `runtime.backtest.execution_start` 等价基线，不能直接迁移代码或混入 record store、replay、experiment、artifact schema、compare、persistence、schema、state 或 frontend owner。
24. `runtime.backtest.execution_start` 等价基线已建立；后续若继续只能先做抽离方案，不能直接迁移代码，也不能混入 record store、replay、experiment、artifact schema、compare、persistence、schema、state 或 frontend owner。
25. `runtime.backtest.execution_start` 抽离方案已建立；后续若继续只能进入抽离记录并移动允许清单中的 backtest 创建路径 handler/helper，不能破坏 experiment 复用桥，也不能混入 record store、replay、artifact schema、compare、persistence、schema、state、frontend 或发布过渡。
26. `runtime.backtest.execution_start` 第一轮物理抽离已完成；后续若继续只能做单叶 closeout，不能直接继续细拆 v4 helper、artifact projection、event projection 或迁移 record/replay/experiment/shared owner。
27. `runtime.backtest.execution_start` 单叶 closeout 已完成；后续若继续只能先为 `runtime.backtest.execution_start.v4_projection` 建立等价基线，不能直接移动代码，也不能把 record store、replay、experiment 或共享 owner 混入该子叶。
28. `runtime.backtest.execution_start.v4_projection` 等价基线已建立；后续若继续只能先做抽离方案，不能直接移动代码，也不能把 request resolution、record write、artifact schema、response schema、state owner、persistence owner 或 frontend caller 混入该子叶。
29. `runtime.backtest.execution_start.v4_projection` 抽离方案已建立；后续若继续只能进入抽离记录并移动 projection helper 与现有两个单元测试，不能把 request resolution、record write、artifact schema、response schema、state owner、persistence owner、frontend caller 或发布过渡混入。
30. `runtime.backtest.execution_start.v4_projection` 第一轮物理抽离已完成；后续若继续只能先做单叶 closeout，不能直接继续拆 request resolution、record write、artifact schema、response schema、state owner、persistence owner、frontend caller 或发布过渡。
31. `runtime.backtest.execution_start.v4_projection` 单叶 closeout 已完成并设置 `stop_split: true`；后续若继续只能回到父叶，为 `runtime.backtest.execution_start.v4_request_resolution` 建立等价基线，不能从 projection 子叶继续外扩。
32. `runtime.backtest.execution_start.v4_request_resolution` 等价基线已建立；后续若继续只能先做抽离方案，不能直接移动代码，也不能把 projection、record write、artifact schema、response schema、state owner、persistence owner 或 frontend caller 混入该子叶。
33. `runtime.backtest.execution_start.v4_request_resolution` 抽离方案已建立；后续若继续只能进入抽离记录并移动四个 request resolution helper，不能把 replay/runtime execution、projection、record write、artifact schema、response schema、state owner、persistence owner、frontend caller 或发布过渡混入。
34. `runtime.backtest.execution_start.v4_request_resolution` 第一轮物理抽离已完成；后续若继续只能进入单叶 closeout，不能把 replay/runtime execution、projection、record write、artifact schema、response schema、state owner、persistence owner、frontend caller 或发布过渡混入。
35. `runtime.backtest.execution_start.v4_request_resolution` 单叶 closeout 已完成并设置 `stop_split: true`；后续若继续只能回到父叶，为 `runtime.backtest.execution_start.v4_runtime_execution` 建立等价基线，不能从 request resolution 子叶继续外扩。
36. `runtime.backtest.execution_start.v4_runtime_execution` 等价基线已建立；后续若继续只能先做抽离方案，不能直接移动代码，也不能把 request resolution、projection、record write、artifact schema、response schema、state owner、persistence owner 或 frontend caller 混入该子叶。
37. `runtime.backtest.execution_start.v4_runtime_execution` 抽离方案已建立；后续若继续只能进入抽离记录，移动 deterministic runtime execution 最小 helper，不能把 request resolution、projection、record write、artifact schema、response schema、state owner、persistence owner 或 frontend caller 混入该子叶。
38. `runtime.backtest.execution_start.v4_runtime_execution` 第一轮物理抽离已完成；后续若继续只能进入单叶 closeout，判断该 helper 等价和是否值得继续细拆，不能把 expanded graph、request resolution、projection、record write、artifact schema、response schema、state owner、persistence owner 或 frontend caller 混入该子叶。
39. `runtime.backtest.execution_start.v4_runtime_execution` 单叶 closeout 已完成并设置 `stop_split: true`；后续若继续只能回到父叶另起候选基线，不能从 runtime execution 子叶继续细拆或外扩。
40. `runtime.backtest.execution_start.legacy_dispatch` 等价基线已建立；后续若继续只能先做 BE-001R-02 抽离方案，不能直接移动 legacy compile/sandbox dispatch 代码，也不能混入 record write、persistence、state owner 或 frontend caller。
41. `runtime.backtest.execution_start.legacy_dispatch` 抽离方案已建立；后续若继续只能进入 BE-001R-03 最小物理抽离，不能顺手迁移 record assembly、artifact views、transient spill、state write、audit log 或发布过渡连接。
42. `runtime.backtest.execution_start.legacy_dispatch` 第一轮物理抽离已完成；后续若继续只能进入 BE-001R-04 单叶 closeout，不能把 record assembly、artifact views、transient spill、state write、audit log、schema、persistence、frontend caller 或发布过渡混入该子叶。
43. `runtime.backtest.execution_start.legacy_dispatch` 单叶 closeout 已完成并设置 `stop_split: true`；后续若继续只能回到父叶残余判断或上层 `runtime.backtest` 队列，不能从 legacy dispatch 子叶继续细拆或外扩。
44. `runtime.backtest.execution_start` 父叶残余判断已完成；后续若继续只能回到上层 `runtime.backtest` 队列，为 `runtime.backtest.record_store` 建立等价基线，不能在 `execution_start` 内私拆 record finalize、state write、persistence、schema 或 frontend caller。
45. `runtime.backtest.record_store` 等价基线已建立；后续若继续只能先做 BE-001T-02 抽离方案，不能直接移动代码，也不能混入 replay、experiment、compare、artifact schema owner、state owner、persistence owner 或 frontend caller。
46. `runtime.backtest.record_store` 抽离方案已建立；后续若继续只能进入 BE-001T-03 最小物理抽离，移动四个 handler 并保持父级 re-export、route facade、shared helper owner、state owner、persistence owner、artifact/transient owner、frontend route 和发布过渡边界不变。
47. `runtime.backtest.record_store` 第一轮物理抽离已完成；后续若继续只能进入单叶 closeout，确认四个 handler 等价并判断是否继续细拆，不能顺手迁移 shared helper、persistence、audit、artifact/transient、response projection、frontend route 或发布过渡连接。
48. `runtime.backtest.record_store` 单叶 closeout 已完成并设置 `stop_split: true`；后续若继续只能回到 `runtime.backtest` sibling 队列，为 `runtime.backtest.replay` 建立单子叶等价基线，不能继续细拆 record_store 或迁移 shared helper、persistence、audit、artifact/transient、response mapping、frontend caller 或发布过渡连接。
49. `runtime.backtest.replay` 单子叶等价基线已建立；后续若继续只能先做 BE-001U-02 抽离方案，不能直接移动 `get_backtest_replay`，也不能迁移 record_store、execution_start、experiment_sweep、compare、response mapping、schema、state/persistence、artifact schema、frontend caller 或发布过渡连接。
50. `runtime.backtest.replay` 抽离方案已建立；后续若继续只能进入 BE-001U-03 实际抽离记录，第一轮只允许迁移 `get_backtest_replay`，不能迁移 query normalization、response mapping、schema、metrics、record lookup、state/persistence、artifact schema、frontend caller 或发布过渡连接。
51. `runtime.backtest.replay` 第一轮物理抽离已完成；后续若继续只能进入单叶 closeout，确认 `get_backtest_replay` 等价并判断是否继续细拆，不能顺手迁移 query normalization、response mapping、schema、metrics、record lookup、state/persistence、artifact schema、frontend caller 或发布过渡连接。
52. `runtime.backtest.replay` 单叶 closeout 已完成并设置 `stop_split: true`；后续若继续只能回到 `runtime.backtest` sibling 队列，为 `runtime.backtest.experiment_sweep` 建立单子叶等价基线，不能继续细拆 replay 或迁移 query normalization、response mapping、schema、metrics、record lookup、state/persistence、artifact schema、frontend caller 或发布过渡连接。
53. `runtime.backtest.experiment_sweep` 单子叶等价基线已建立；后续若继续只能先做 BE-001V-02 抽离方案，不能直接移动 experiment handler/helper，也不能迁移 route facade、execution_start、record_store、replay、compare、schema、state/persistence、response mapping、frontend caller 或发布过渡连接。
54. `runtime.backtest.experiment_sweep` 抽离方案已建立；后续若继续只能进入 BE-001V-03 实际抽离记录，第一轮只允许迁移 5 个 experiment handler 和 3 个参数网格 helper，不能迁移 route aggregate、execution_start、persistence、response mapping、schema、state、audit、frontend caller 或发布过渡连接。
55. `runtime.backtest.experiment_sweep` 第一轮物理抽离已完成；后续若继续只能进入 BE-001V-04 单叶 closeout，不能删除 drained parent include、迁移 route aggregate、execution_start、persistence、response mapping、schema、state、audit、frontend caller 或发布过渡连接。
56. `runtime.backtest.experiment_sweep` 单叶 closeout 已完成；本叶 `stop_split: false`，后续若继续只能进入 BE-001W-01 `runtime.backtest.experiment_sweep.parameter_grid` 单子叶等价基线，不能直接移动 helper、删除 drained parent include 或迁移 shared owner。
57. `runtime.backtest.experiment_sweep.parameter_grid` 单子叶等价基线已建立；当前 `no code movement`，后续若继续只能进入 BE-001W-02 抽离方案，不能直接移动 helper、新增子文件、修改 schema、改 `MAX_EXPERIMENT_VARIANTS` 或宣称 closeout。
58. `runtime.backtest.experiment_sweep.parameter_grid` 抽离方案已建立；当前 `no code movement`，后续若继续只能进入 BE-001W-03 实际抽离记录，第一轮只允许迁移 3 个 helper 到父级私有子模块，不能迁移 route、handler orchestration、execution_start、persistence、mapping、schema、state、audit、frontend caller 或发布过渡。
59. `runtime.backtest.experiment_sweep.parameter_grid` 第一轮物理抽离已完成；后续若继续只能进入 BE-001W-04 单叶 closeout，不能继续细拆 axis normalization、variant expansion、schema、route、state/persistence、frontend caller 或发布过渡连接。
60. `runtime.backtest.experiment_sweep.parameter_grid` 单叶 closeout 已完成；本叶 `stop_split: true`，后续若继续只能进入 BE-001X-01 `runtime.backtest.experiment_sweep` 父叶残余判断，不能从 parameter_grid 继续细拆或宣称父叶最终完成。
61. `runtime.backtest.experiment_sweep` 父叶残余判断已完成；父叶仍 `stop_split: false`，后续若继续只能进入 BE-001Y-01 `runtime.backtest.experiment_sweep.start_orchestration` 单子叶等价基线，不能直接移动 start handler、record lifecycle、route、schema、state/persistence、frontend caller 或发布过渡连接。
