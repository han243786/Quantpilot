# v4.16.0 递归模块化全局根流程

> 版本类型: MINOR architecture / governance。
> 基准: `07-顶层大模块统计.md`、`12-system十叶模块等价基线.md`。
> 执行档位: 重型。
> 判定: 将“顶层模块 -> 叶子抽离 -> 叶子整理 -> 是否继续细分 -> 递归处理 -> 全局根收束”固化为后续全量模块树的通用流程。

---

## 目标

本文件把后续模块化工作固定为递归流程:

```text
全局根 root
  -> 分顶层模块
  -> 选择一个顶层模块
  -> 做叶子抽离
  -> 整理这些叶子
  -> 判断叶子是否值得拆得更细
      -> 值得: 把该叶子当成新的局部顶层模块，重复同一流程
      -> 不值得: 标记该叶子完成
  -> 全部叶子完成后，标记该顶层模块完成
  -> 按同一规范处理其他顶层模块
  -> 六大顶层模块都完成后，收束到全局根和全量模块树
```

这不是一次性全量抽离，也不是无限细分。它是一个可停止、可验证、可递归的模块化算法。

这里的“整理这些叶子”指白箱整理和等价 closeout: 登记 owner、真实文件、public 入口、关键内部实现、未迁移边界、回归证据和是否继续细分。它不等同于代码目录整理、命名重排、旧实现删除或重构；这些仍属于后续整理/重构方案。

---

## 三矩阵影响声明

| 矩阵 | 影响节点 | 变更类型 |
| --- | --- | --- |
| 流程矩阵 | 递归模块化状态机、顶层完成判定、叶子细分判定 | 扩展 |
| 规范矩阵 | 父子通信、owner 归属、单叶 closeout、停止条件 | 扩展 |
| 引导矩阵 | 全局根、六大顶层模块、全量模块树、模块树白箱节点 | 扩展 |
| 模块树 | `root.system`、`root.backend`、`root.frontend`、`root.executor`、`root.contracts`、`root.docs` | 建立递归处理口径 |

---

## 引导坐标声明

| 坐标 | 指向 |
| --- | --- |
| 全量树节点 | `markdown/10-overview/overview-full-feature-tree.md` 根1-根7 |
| 模块树节点 | `root.system`、`root.backend`、`root.frontend`、`root.executor`、`root.contracts`、`root.docs`、递归产生的子叶节点 |
| 真实文件 | `markdown/06-milestones/v4.16.0/07-顶层大模块统计.md`、`08-system大模块分层统计.md`、`11-system抽离经验回填.md`、`12-system十叶模块等价基线.md`、`13-递归模块化全局根流程.md`、`markdown/00-matrix-governance/module-tree.md`、`markdown/10-overview/overview-full-feature-tree.md` |
| public 方法 | 每层递归都必须登记 public 入口、兼容 public 入口和关键内部实现；不得把内部 helper 冒充 public API |
| 测试/门禁 | `tools/check-matrix-governance.ps1`、`tools/check-full-feature-tree.ps1`、`tools/check-utf8.ps1`、受影响顶层模块的单测/契约测试/人工等价检查 |

---

## 六大顶层模块

| # | 顶层模块 | 全局根路径 | 当前处理状态 | 递归入口 |
| --- | --- | --- | --- | --- |
| 1 | `system` | `root.system` | 已完成顶层阶段性 closeout；S1-S10 均已完成 closeout 或静态 closeout | `12-system十叶模块等价基线.md`、`24-system顶层阶段性closeout.md`、`25-system.build_delivery.S6-S9恢复提案与适配性校验.md` |
| 2 | `backend` | `root.backend` | 已进入 R5: 9 个 L2 叶子已完成逐叶 closeout，9 叶均已有子 facade 坐标；其余 8 叶已补逐叶完成记录；`backend.runtime.routes` 已完成 route aggregate 抽离，`backend.runtime.routes.run` 已完成 run route group 抽离和单叶 closeout，`runtime.run.v4_handoff` 已完成抽离与单叶 closeout，当前停止内部细分；`runtime.run.session_start` 已完成抽离与单叶 closeout，当前停止内部细分；`runtime.run.record_store` 已完成抽离与单叶 closeout，当前停止内部细分；`runtime.run.replay_status` 已完成抽离与单叶 closeout，当前停止内部细分；`runtime.event_stream` 已完成抽离与单叶 closeout，当前停止内部细分；`runtime.backtest.record_store` 已完成单叶 closeout 并设置 `stop_split: true`；`runtime.backtest.replay` 已完成单叶 closeout 并设置 `stop_split: true`；`runtime.backtest.experiment_sweep.parameter_grid` 已完成单叶 closeout 并设置 `stop_split: true`；BE-001Y-04 `runtime.backtest.experiment_sweep.start_orchestration` 单叶 closeout 已完成并设置 `stop_split: true`；BE-001Z-01 已完成 `runtime.backtest.experiment_sweep` 第二轮父叶残余判断；BE-001AA-01 已建立 `record_lifecycle` 单子叶等价基线；BE-001AA-02 已建立抽离方案；BE-001AA-03 已完成实际抽离；BE-001AA-04 已完成单叶 closeout 并设置 `stop_split: true`；BE-001AB-01 已完成第三轮父叶残余判断并设置 `runtime.backtest.experiment_sweep` 父叶 `stop_split: true`；BE-001AC-01 已完成 `runtime.backtest` 父叶残余判断并设置父叶 `stop_split: true`；BE-001AD-01 已完成 `backend.runtime.routes` 父叶残余判断，父叶保持 `stop_split: false`；BE-001AE-04 已完成 `backend.runtime.routes.mutation` route facade 单叶 closeout 并设置 `stop_split: true`；BE-001AF-01 已建立 `runtime.mutation.parameter_mutation` 单子叶等价基线；下一步只能进入 BE-001AF-02 抽离方案 | `03-后端抽离登记.md`、`06-后端接口边界首批抽离方案.md`、`28-backend大模块分层统计.md`、`29-backend.interface_boundary等价基线.md`、`30-backend九叶模块壳抽离记录.md`、`31-39 backend 单叶 closeout`、`40-backend.strategy_config_L3模块壳抽离记录.md`、`41-backend其余八叶模块壳抽离记录.md`、`42-49 backend 子叶抽离完成记录`、`50-backend.runtime.routes单子叶等价基线.md`、`51-backend.runtime.routes抽离记录.md`、`52-backend.runtime.routes.run单子叶等价基线.md`、`53-backend.runtime.routes.run抽离记录.md`、`54-backend.runtime.routes.run单叶closeout.md`、`55-runtime.run.v4_handoff单子叶等价基线.md`、`56-runtime.run.v4_handoff抽离记录.md`、`57-runtime.run.v4_handoff单叶closeout.md`、`58-runtime.run.session_start单子叶等价基线.md`、`59-runtime.run.session_start抽离记录.md`、`60-runtime.run.session_start单叶closeout.md`、`61-runtime.run.record_store单子叶等价基线.md`、`62-runtime.run.record_store真实边界梳理.md`、`63-runtime.run.record_store抽离方案.md`、`64-runtime.run.record_store抽离记录.md`、`65-runtime.run.record_store单叶closeout.md`、`66-runtime.run.replay_status单子叶等价基线.md`、`67-runtime.run.replay_status抽离方案.md`、`68-runtime.run.replay_status抽离记录.md`、`69-runtime.run.replay_status单叶closeout.md`、`70-runtime.event_stream单子叶等价基线.md`、`71-runtime.event_stream抽离方案.md`、`72-runtime.event_stream抽离记录.md`、`73-runtime.event_stream单叶closeout.md`、`74-runtime.backtest单子叶等价基线.md`、`75-runtime.backtest抽离方案.md`、`99-runtime.backtest.record_store单子叶等价基线.md`、`100-runtime.backtest.record_store抽离方案.md`、`101-runtime.backtest.record_store抽离记录.md`、`102-runtime.backtest.record_store单叶closeout.md`、`103-runtime.backtest.replay单子叶等价基线.md`、`104-runtime.backtest.replay抽离方案.md`、`105-runtime.backtest.replay抽离记录.md`、`106-runtime.backtest.replay单叶closeout.md`、`107-runtime.backtest.experiment_sweep单子叶等价基线.md`、`108-runtime.backtest.experiment_sweep抽离方案.md`、`109-runtime.backtest.experiment_sweep抽离记录.md`、`110-runtime.backtest.experiment_sweep单叶closeout.md`、`111-runtime.backtest.experiment_sweep.parameter_grid单子叶等价基线.md`、`112-runtime.backtest.experiment_sweep.parameter_grid抽离方案.md`、`113-runtime.backtest.experiment_sweep.parameter_grid抽离记录.md`、`114-runtime.backtest.experiment_sweep.parameter_grid单叶closeout.md`、`115-runtime.backtest.experiment_sweep父叶残余判断.md`、`116-runtime.backtest.experiment_sweep.start_orchestration单子叶等价基线.md`、`117-runtime.backtest.experiment_sweep.start_orchestration抽离方案.md`、`118-runtime.backtest.experiment_sweep.start_orchestration抽离记录.md`、`119-runtime.backtest.experiment_sweep.start_orchestration单叶closeout.md`、`120-runtime.backtest.experiment_sweep第二轮父叶残余判断.md`、`121-runtime.backtest.experiment_sweep.record_lifecycle单子叶等价基线.md`、`122-runtime.backtest.experiment_sweep.record_lifecycle抽离方案.md`、`123-runtime.backtest.experiment_sweep.record_lifecycle抽离记录.md`、`124-runtime.backtest.experiment_sweep.record_lifecycle单叶closeout.md`、`125-runtime.backtest.experiment_sweep第三轮父叶残余判断.md`、`126-runtime.backtest父叶残余判断.md`、`127-backend.runtime.routes父叶残余判断.md`、`128-backend.runtime.routes.mutation单子叶等价基线.md`、`129-backend.runtime.routes.mutation抽离方案.md`、`130-backend.runtime.routes.mutation抽离记录.md`、`131-backend.runtime.routes.mutation单叶closeout.md`、`132-runtime.mutation.parameter_mutation单子叶等价基线.md` |
| 3 | `frontend` | `root.frontend` | 延后，等待后端接口边界稳定 | `04-前端抽离登记.md` |
| 4 | `executor` | `root.executor` | 延后，不迁移执行端状态所有权 | 后续 executor 抽离登记 |
| 5 | `contracts` | `root.contracts` | 白箱覆盖缺口，需先补 `contracts.*` 节点 | 后续 contracts 抽离登记 |
| 6 | `docs` | `root.docs` | 治理控制面已维护 | `docs.matrix_governance`、`docs.feature_tree` |

最新状态补充: `backend` 已完成 BE-001AN-04 `runtime.mutation.parameter_mutation.transition_lifecycle.activation_snapshot_side_effect` 单叶 closeout，并设置 `stop_split: true`；下一步只能进入 BE-001AO-01 父叶残余判断。
最新状态补充: `backend` 已完成 BE-001AO-01 `runtime.mutation.parameter_mutation.transition_lifecycle` 第四轮父叶残余判断，父叶保持 `stop_split: false`；下一步只能进入 BE-001AP-01 `runtime.mutation.parameter_mutation.transition_lifecycle.transition_record_persistence` 单子叶等价基线。
最新状态补充: `backend` 已完成 BE-001AP-01 `runtime.mutation.parameter_mutation.transition_lifecycle.transition_record_persistence` 单子叶等价基线；下一步只能进入 BE-001AP-02 抽离方案。
最新状态补充: `backend` 已完成 BE-001AP-02 `runtime.mutation.parameter_mutation.transition_lifecycle.transition_record_persistence` 抽离方案；下一步只能进入 BE-001AP-03 实际抽离。
最新状态补充: `backend` 已完成 BE-001AP-03 `runtime.mutation.parameter_mutation.transition_lifecycle.transition_record_persistence` 实际抽离；下一步只能进入 BE-001AP-04 单叶 closeout。
最新状态补充: `backend` 已完成 BE-001AP-04 `runtime.mutation.parameter_mutation.transition_lifecycle.transition_record_persistence` 单叶 closeout 并设置 `stop_split: true`；下一步只能进入 BE-001AQ-01 父叶残余判断。
最新状态补充: `backend` 已完成 BE-001AQ-01 `runtime.mutation.parameter_mutation.transition_lifecycle` 第五轮父叶残余判断，父叶保持 `stop_split: false`；下一步只能进入 BE-001AR-01 `rollback_record_identity` 单子叶等价基线。
最新状态补充: `backend` 已完成 BE-001AR-01 `runtime.mutation.parameter_mutation.transition_lifecycle.rollback_record_identity` 单子叶等价基线；下一步只能进入 BE-001AR-02 抽离方案。
最新状态补充: `backend` 已完成 BE-001AR-02 `runtime.mutation.parameter_mutation.transition_lifecycle.rollback_record_identity` 抽离方案；下一步只能进入 BE-001AR-03 实际抽离。
最新状态补充: `backend` 已完成 BE-001AR-03 `runtime.mutation.parameter_mutation.transition_lifecycle.rollback_record_identity` 实际抽离；下一步只能进入 BE-001AR-04 单叶 closeout。
最新状态补充: `backend` 已完成 BE-001AR-04 `runtime.mutation.parameter_mutation.transition_lifecycle.rollback_record_identity` 单叶 closeout；下一步只能进入 BE-001AS-01 父叶残余判断。
最新状态补充: `backend` 已完成 BE-001AS-01 `runtime.mutation.parameter_mutation.transition_lifecycle` 第六轮父叶残余判断并设置父叶 `stop_split: true`；下一步只能进入 BE-001AT-01 `runtime.mutation.parameter_mutation` 父叶残余判断。
最新状态补充: `backend` 已完成 BE-001AT-01 `runtime.mutation.parameter_mutation` 父叶残余判断，父叶保持 `stop_split: false`；下一步只能进入 BE-001AU-01 `proposal_creation` 单子叶等价基线。
最新状态补充: `backend` 已完成 BE-001AU-01 `runtime.mutation.parameter_mutation.proposal_creation` 单子叶等价基线；下一步只能进入 BE-001AU-02 抽离方案。
最新状态补充: `backend` 已完成 BE-001BS-04 `backend.runtime.routes.experiment` 单叶 closeout，并设置 `stop_split: true`；下一步只能进入 BE-001BT-01 `backend.runtime.routes` 父叶残余判断。
最新状态补充: `backend` 已完成 BE-001BT-01 `backend.runtime.routes` 第三轮父叶残余判断；父叶保持 `stop_split: false`，下一步只能进入 BE-001BU-01 `backend.runtime.routes.evidence` 单子叶等价基线。
最新状态补充: `backend` 已完成 BE-001BU-01 `backend.runtime.routes.evidence` 单子叶等价基线；下一步只能进入 BE-001BU-02 抽离方案。
补充递归入口: `132-runtime.mutation.parameter_mutation单子叶等价基线.md`、`133-runtime.mutation.parameter_mutation抽离方案.md`、`134-runtime.mutation.parameter_mutation抽离记录.md`、`135-runtime.mutation.parameter_mutation单叶closeout.md`、`136-runtime.mutation.parameter_mutation.transition_lifecycle单子叶等价基线.md`、`137-runtime.mutation.parameter_mutation.transition_lifecycle抽离方案.md`、`138-runtime.mutation.parameter_mutation.transition_lifecycle抽离记录.md`、`139-runtime.mutation.parameter_mutation.transition_lifecycle单叶closeout.md`、`140-runtime.mutation.parameter_mutation.transition_lifecycle.boundary_safety单子叶等价基线.md`、`141-runtime.mutation.parameter_mutation.transition_lifecycle.boundary_safety抽离方案.md`、`142-runtime.mutation.parameter_mutation.transition_lifecycle.boundary_safety抽离记录.md`、`143-runtime.mutation.parameter_mutation.transition_lifecycle.boundary_safety单叶closeout.md`、`144-runtime.mutation.parameter_mutation.transition_lifecycle父叶残余判断.md`、`145-runtime.mutation.parameter_mutation.transition_lifecycle.activation_flow单子叶等价基线.md`、`146-runtime.mutation.parameter_mutation.transition_lifecycle.activation_flow抽离方案.md`、`147-runtime.mutation.parameter_mutation.transition_lifecycle.activation_flow抽离记录.md`、`148-runtime.mutation.parameter_mutation.transition_lifecycle.activation_flow单叶closeout.md`、`149-runtime.mutation.parameter_mutation.transition_lifecycle第二轮父叶残余判断.md`、`150-runtime.mutation.parameter_mutation.transition_lifecycle.rollback_flow单子叶等价基线.md`、`151-runtime.mutation.parameter_mutation.transition_lifecycle.rollback_flow抽离方案.md`、`152-runtime.mutation.parameter_mutation.transition_lifecycle.rollback_flow抽离记录.md`、`153-runtime.mutation.parameter_mutation.transition_lifecycle.rollback_flow单叶closeout.md`、`154-runtime.mutation.parameter_mutation.transition_lifecycle第三轮父叶残余判断.md`、`155-runtime.mutation.parameter_mutation.transition_lifecycle.activation_snapshot_side_effect单子叶等价基线.md`、`156-runtime.mutation.parameter_mutation.transition_lifecycle.activation_snapshot_side_effect抽离方案.md`、`157-runtime.mutation.parameter_mutation.transition_lifecycle.activation_snapshot_side_effect抽离记录.md`、`158-runtime.mutation.parameter_mutation.transition_lifecycle.activation_snapshot_side_effect单叶closeout.md`、`159-runtime.mutation.parameter_mutation.transition_lifecycle第四轮父叶残余判断.md`、`160-runtime.mutation.parameter_mutation.transition_lifecycle.transition_record_persistence单子叶等价基线.md`、`161-runtime.mutation.parameter_mutation.transition_lifecycle.transition_record_persistence抽离方案.md`、`162-runtime.mutation.parameter_mutation.transition_lifecycle.transition_record_persistence抽离记录.md`、`163-runtime.mutation.parameter_mutation.transition_lifecycle.transition_record_persistence单叶closeout.md`、`164-runtime.mutation.parameter_mutation.transition_lifecycle第五轮父叶残余判断.md`、`165-runtime.mutation.parameter_mutation.transition_lifecycle.rollback_record_identity单子叶等价基线.md`、`166-runtime.mutation.parameter_mutation.transition_lifecycle.rollback_record_identity抽离方案.md`、`167-runtime.mutation.parameter_mutation.transition_lifecycle.rollback_record_identity抽离记录.md`、`168-runtime.mutation.parameter_mutation.transition_lifecycle.rollback_record_identity单叶closeout.md`、`169-runtime.mutation.parameter_mutation.transition_lifecycle第六轮父叶残余判断.md`、`170-runtime.mutation.parameter_mutation父叶残余判断.md`、`171-runtime.mutation.parameter_mutation.proposal_creation单子叶等价基线.md`。

---

## 递归状态机

| 状态 | 输入 | 输出 | 通过条件 |
| --- | --- | --- | --- |
| R0 顶层模块确认 | 全局根、顶层候选 | 顶层模块清单 | 顶层 owner 和真实范围清楚 |
| R1 叶子划分 | 单个顶层模块 | L2 叶子清单 | 每个叶子都有 owner、真实文件和边界说明 |
| R2 叶子抽离 | 单个叶子 | 候选模块、兼容桥、回退点 | 单叶单批，不跨父域 |
| R3 叶子整理 | 已抽离或已登记叶子 | 白箱 closeout、等价基线、public/内部实现分类、未迁移边界 | 能证明功能与之前一致，且不借代码整理或重构解释完成 |
| R4 细分价值判断 | 已整理叶子 | 继续细分 / 停止细分 | 有明确 owner 和独立等价证据才继续拆 |
| R5 局部递归 | 值得细分的叶子 | 新的局部顶层模块 | 仿照 R0-R4 重复处理 |
| R6 顶层完成 | 顶层所有叶子 | 顶层模块完成记录 | 所有叶子完成或明确停止细分 |
| R7 全局根收束 | 六大顶层完成记录 | 全量模块树完成 | 全局根能解释所有 active 模块和边界 |

---

## 细分价值判定

叶子只有满足下列条件之一，才值得继续拆得更细:

| 值得细分的信号 | 说明 |
| --- | --- |
| 独立 owner | 叶子内部存在两个以上稳定 owner，且父子通信可以清楚表达 |
| 独立 public 入口 | 子叶有独立 public 入口或明确兼容入口 |
| 独立状态/锁/持久化 | 子叶拥有不同状态、锁顺序、事务或持久化责任 |
| 独立验证证据 | 子叶能单独用测试、契约、artifact 或人工清单证明等价 |
| 独立发布/构建边界 | 子叶对应单独构建、CI、交付、schema 或配置边界 |
| 长期演进频率不同 | 子叶变化频率明显不同，放在一起会制造冲突 |

满足下列情况时，不值得继续拆:

| 不值得细分的信号 | 说明 |
| --- | --- |
| 只是文件夹好看 | 没有独立 owner，只是目录美化 |
| 只是 helper 分组 | 内部 helper 没有 public/兼容入口和独立验证 |
| 只是 generated asset | 生成资产没有独立行为 owner |
| 会增加横向通信 | 拆开后需要绕过父模块互相调用 |
| 需要整理或重构才能解释完成 | 当前仍不进入整理/重构 |

---

## 顶层模块完成判定

一个顶层模块只有同时满足以下条件，才能视为处理完成:

1. 顶层模块的叶子清单已固定或明确暂缓。
2. 每个叶子都有 public 入口、关键内部实现、保留外部边界和等价证据。
3. 每个叶子都完成抽离、完成登记，或明确不值得继续拆。
4. 值得细分的叶子已经按同一递归流程处理完毕。
5. 没有未解释的横向连接、owner 冲突、新旧入口冲突或测试缺口扩大。
6. closeout 写明“抽离完成”还是“整理/重构仍未启动”。

---

## 全局根完成判定

六大顶层模块全部完成后，才能宣告全局根完成:

| 全局根条件 | 要求 |
| --- | --- |
| 六大顶层齐全 | `system`、`backend`、`frontend`、`executor`、`contracts`、`docs` 都有完成记录 |
| 全量模块树齐全 | 模块树能定位每个 active 模块、叶子、真实文件和 public 入口 |
| 全量树齐全 | 全量树能覆盖所有 active 文件和治理索引 |
| 父子通信闭环 | 叶子只能经父模块或明确 adapter 通信 |
| 停止条件可解释 | 每个叶子为什么停止细分都能被审查 |
| 无发布态偷渡 | 未经开发者明确声明，不引入发布态横向连接 |

---

## 当前应用到 system

`system` 现在处于 R4 与 R6 之间:

1. R0 顶层模块确认已完成: `root.system` 是六大顶层之一。
2. R1 叶子划分已完成: 3 层、10 个叶子。
3. R2 叶子抽离已部分完成: S2 `system.entry.backend_process` 已完成正式单叶 closeout。
4. R3 叶子整理已推进: `12-system十叶模块等价基线.md` 已铺好，S1/S2/S3/S4/S5/S7/S8/S10 都有 closeout 或静态 closeout。
5. R4 细分价值判断已完成当前抽离阶段判断: S1-S10 均已停止细分或完成静态 closeout。

因此 system 新分出来的叶子已经完成本阶段能做的 R3/R4 整理: S1-S10 都已收束。`24-system顶层阶段性closeout.md` 已刷新为 R6 阶段性收束。若继续 system，必须另起整理、重构、发布验收或具体变更方案；否则应转向下一个顶层模块或后端接口边界后续批次。

---

## 当前应用到 backend

`backend` 现在处于 R5:

1. R0 顶层模块确认已完成: `root.backend` 是六大顶层之一。
2. R1 叶子划分已启动: `28-backend大模块分层统计.md` 登记 9 个 L2 叶子候选。
3. R2 首批叶子已落壳: `30-backend九叶模块壳抽离记录.md` 建立 `src/backend/` 与 9 个叶子 facade。
4. R3 叶子整理已完成当前范围: `31-39 backend 单叶 closeout` 已为 9 个叶子登记 owner、真实文件、public/接口入口、兼容桥、回退点和等价证据。
5. R4 细分价值判断已完成当前判断: `backend.interface_boundary`、`backend.capability`、`backend.app_state_wiring`、`backend.test_support` 停止细分；`backend.strategy_config`、`backend.runtime`、`backend.graph_compile`、`backend.storage_security`、`backend.ops_governance` 值得进入下一轮 L3 等价基线。
6. R5 局部递归已启动: `40-backend.strategy_config_L3模块壳抽离记录.md` 已建立 artifact、preflight、diff、AI proposal binding 四个 L3 子叶 facade；`41-backend其余八叶模块壳抽离记录.md` 已为其余 8 叶建立薄壳子 facade，`42-49` 已分别补齐其余 8 叶的逐叶完成记录。
7. R5 单子叶基线已启动: `50-backend.runtime.routes单子叶等价基线.md` 选择 `backend.runtime.routes` 作为首个 BE-001F 子叶，固定 route aggregate facade、真实 runtime owner 和等价证据。
8. R5 单子叶抽离已完成: `51-backend.runtime.routes抽离记录.md` 将 runtime route aggregate 列表迁入 `src/backend/runtime/routes.rs`，handler、state owner、event stream、artifact schema 和 persistence 保留原位。
9. R5 子叶递归已继续: `52-backend.runtime.routes.run单子叶等价基线.md` 选择 `backend.runtime.routes.run`，`53-backend.runtime.routes.run抽离记录.md` 将 run route group 迁入 `src/backend/runtime/routes/run.rs`，handler、state owner、event stream 和 persistence 仍保留原位。
10. R5 单叶 closeout 已完成: `54-backend.runtime.routes.run单叶closeout.md` 确认 route facade 本身停止细分，但 `src/runtime/run.rs` handler 层值得另起基线，默认下一候选为 `runtime.run.v4_handoff`。
11. R5 handler 层单子叶基线已启动: `55-runtime.run.v4_handoff单子叶等价基线.md` 固定 `/api/runtime/v4/run` 的 source/graph handoff、initial event、paper simulated runtime 和 `api_run` 证据。
12. R5 handler 层单子叶抽离已完成: `56-runtime.run.v4_handoff抽离记录.md` 将 v4 handoff handler/type/helper 迁入 `src/runtime/run/v4_handoff.rs`，父级 `runtime` 保留受控 re-export，`src/runtime/run.rs` 仍保留 legacy run/session/record/replay/status sibling。
13. R5 handler 层单叶 closeout 已完成: `57-runtime.run.v4_handoff单叶closeout.md` 判定本叶停止内部细分；下一步若继续 run handler，必须回到 `runtime.run` sibling 队列重新建基线。
14. R5 handler sibling 基线已建立: `58-runtime.run.session_start单子叶等价基线.md` 固定 legacy `/api/runtime/test-run`、`start_test_run`、run lock、compile path、sandbox session、event envelope 与 in-memory run record 等价边界；当前不宣告抽离完成。
15. R5 handler sibling 抽离已完成: `59-runtime.run.session_start抽离记录.md` 将 `start_test_run` 迁入 `src/runtime/run/session_start.rs`，父级 `runtime` 保留兼容 re-export，`src/runtime/run.rs` 继续保留 record/replay/status/SSE sibling。
16. R5 handler sibling 单叶 closeout 已完成: `60-runtime.run.session_start单叶closeout.md` 判定本叶停止内部细分；下一步若继续 run handler，应回到 `runtime.run` sibling 队列，默认候选为 `runtime.run.record_store`。
17. R5 handler sibling 基线已建立: `61-runtime.run.record_store单子叶等价基线.md` 固定 run record list/detail/save/discard、persistence、audit、state owner 和 `api_run` 证据；当前不宣告抽离完成。
18. R5 handler sibling 真实边界已梳理: `62-runtime.run.record_store真实边界梳理.md` 校正 discard 真实 route 为 `DELETE /api/runtime/runs/:run_id`，并明确第一轮抽离只迁移四个 handler，不迁移 shared helper owner。
19. R5 handler sibling 抽离方案已建立: `63-runtime.run.record_store抽离方案.md` 锁定下一轮只移动 `list_runs`、`get_run_detail`、`save_run_record`、`discard_run_record`，通过父级 `runtime` re-export 保持 route facade 不变。
20. R5 handler sibling 抽离已完成: `64-runtime.run.record_store抽离记录.md` 将四个 record_store handler 迁入 `src/runtime/run/record_store.rs`，父级 `runtime` 保留兼容 re-export，`src/runtime/run.rs` 继续保留 replay/status/SSE sibling。
21. R5 handler sibling 单叶 closeout 已完成: `65-runtime.run.record_store单叶closeout.md` 判定本叶停止内部细分；下一步若继续 run handler，应回到 `runtime.run` sibling 队列，默认候选为 `runtime.run.replay_status`。
22. R5 handler sibling 基线已建立: `66-runtime.run.replay_status单子叶等价基线.md` 固定 run replay/status 两个 handler、response mapping owner、metrics owner 和 SSE 排除边界；当前不宣告代码迁移完成。
23. R5 handler sibling 抽离方案已建立: `67-runtime.run.replay_status抽离方案.md` 锁定下一轮只移动 `get_run_replay` 与 `get_run_status`，通过父级 `runtime` re-export 保持 route facade 不变。
24. R5 handler sibling 实际抽离已完成: `68-runtime.run.replay_status抽离记录.md` 已将 `get_run_replay` 与 `get_run_status` 迁入 `src/runtime/run/replay_status.rs`；当前不宣告 SSE、response mapping、schema、metrics、state/persistence owner 或本叶 closeout 已完成。
25. R5 handler sibling 单叶 closeout 已完成: `69-runtime.run.replay_status单叶closeout.md` 判定本叶停止内部细分；下一步若继续 runtime route，应回到父级 `backend.runtime.routes` sibling 队列，默认候选为 `runtime.event_stream`。
26. R5 父级 sibling 基线已建立: `70-runtime.event_stream单子叶等价基线.md` 固定 `/api/runtime/runs/:run_id/events`、`stream_run_events`、SSE frame order、keep-alive 和父级 route owner；当前不宣告代码迁移完成。
27. R5 父级 sibling 抽离方案已建立: `71-runtime.event_stream抽离方案.md` 锁定下一批只移动 `stream_run_events`，保留 route facade、shared helper、state/persistence/frontend 和 SSE 生命周期；当前不移动代码。
28. R5 父级 sibling 抽离已完成: `72-runtime.event_stream抽离记录.md` 将 `stream_run_events` 迁入 `src/runtime/event_stream.rs`，父级 `runtime` 保留兼容 re-export，route facade、shared helper、state/persistence/frontend 和 SSE 生命周期均不迁移。
29. R5 父级 sibling 单叶 closeout 已完成: `73-runtime.event_stream单叶closeout.md` 判定本叶停止内部细分；下一步若继续 runtime route，应回到父级 `backend.runtime.routes` sibling 队列，默认候选为 `runtime.backtest`。
30. R5 父级 sibling 基线已建立: `74-runtime.backtest单子叶等价基线.md` 固定 backtest route group、handler、artifact/compare/replay/persistence/shared owner 和禁止迁移边界；当前不移动代码。
31. R5 父级 sibling 抽离方案已建立: `75-runtime.backtest抽离方案.md` 锁定下一批只抽离 backtest route facade 到计划目标文件，handler、artifact、compare、persistence、schema、state 和 frontend owner 均不迁移。
32. R5 父级 sibling route facade 抽离已完成: `76-runtime.backtest抽离记录.md` 将 backtest route group 迁入 `src/backend/runtime/routes/backtest.rs`；handler、artifact、compare、persistence、schema、state、frontend owner 和发布过渡均不迁移。
33. R5 父级 sibling 单叶 closeout 已完成: `77-runtime.backtest单叶closeout.md` 判定 route facade 本身停止细分；`src/runtime/backtest.rs` handler 域仍值得继续递归，默认下一候选为 `runtime.backtest.execution_start`。
34. R5 handler 域单子叶基线已建立: `78-runtime.backtest.execution_start单子叶等价基线.md` 冻结 backtest 创建路径、legacy/v4 execution helper、artifact generation、governance event 和 transient spill 边界；当前不移动代码。
35. R5 handler 域抽离方案已建立: `79-runtime.backtest.execution_start抽离方案.md` 锁定下一批只移动 backtest 创建路径 handler/helper，并通过父级兼容出口保留 `start_backtest_run` 与 `execute_backtest_request` 复用桥；当前不移动代码。
36. R5 handler 域第一轮物理抽离已完成: `80-runtime.backtest.execution_start抽离记录.md` 将 backtest 创建路径 handler/helper 迁入 `src/runtime/backtest/execution_start.rs`；父级 `runtime` 保留 `start_backtest_run` re-export 和 `execute_backtest_request` experiment 复用桥。
37. R5 handler 域单叶 closeout 已完成: `81-runtime.backtest.execution_start单叶closeout.md` 确认 `runtime.backtest.execution_start` 等价，但不设置 `stop_split: true`；下一轮递归候选为 `runtime.backtest.execution_start.v4_projection`。
38. R5 handler 域下一轮单子叶基线已建立: `82-runtime.backtest.execution_start.v4_projection单子叶等价基线.md` 冻结 v4 artifact projection helper、单元测试和 API 回归证据；当前不移动代码。
39. R5 handler 域下一轮抽离方案已建立: `83-runtime.backtest.execution_start.v4_projection抽离方案.md` 锁定下一批只移动 projection helper 与现有两个单元测试；当前不移动代码。
40. R5 handler 域下一轮第一轮物理抽离已完成: `84-runtime.backtest.execution_start.v4_projection抽离记录.md` 将 projection helper 与现有两个单元测试迁入 `src/runtime/backtest/v4_projection.rs`；request resolution、record write、schema、state、persistence、frontend 和发布过渡均不迁移。
41. R5 handler 域下一轮单叶 closeout 已完成: `85-runtime.backtest.execution_start.v4_projection单叶closeout.md` 确认 projection 子模块等价并设置 `stop_split: true`；下一候选回到父叶 `runtime.backtest.execution_start.v4_request_resolution`。
42. R5 handler 域下一轮单子叶基线已建立: `86-runtime.backtest.execution_start.v4_request_resolution单子叶等价基线.md` 冻结 v4 请求识别、graph/symbol/event resolution、错误 code 和兼容桥；当前不移动代码。
43. R5 handler 域下一轮抽离方案已建立: `87-runtime.backtest.execution_start.v4_request_resolution抽离方案.md` 锁定下一批只移动四个 request resolution helper；当前不移动代码。
44. R5 handler 域下一轮物理抽离已完成: `88-runtime.backtest.execution_start.v4_request_resolution抽离记录.md` 将四个 request resolution helper 迁入 `src/runtime/backtest/v4_request_resolution.rs`；replay/runtime execution、projection、record write、schema、state、persistence、frontend 和发布过渡均不迁移。
45. R5 handler 域下一轮单叶 closeout 已完成: `89-runtime.backtest.execution_start.v4_request_resolution单叶closeout.md` 确认 request resolution 子模块等价并设置 `stop_split: true`；下一候选回到父叶 `runtime.backtest.execution_start.v4_runtime_execution`。
46. R5 handler 域下一轮单子叶基线已建立: `90-runtime.backtest.execution_start.v4_runtime_execution单子叶等价基线.md` 冻结 deterministic bars/ticks、`V4PaperSimulatedRuntime`、`spawn_blocking` 和 `V4BacktestArtifact` 输出；当前不移动代码。
47. R5 handler 域下一轮抽离方案已建立: `91-runtime.backtest.execution_start.v4_runtime_execution抽离方案.md` 锁定下一批只移动 deterministic runtime execution 最小 helper；当前不移动代码。
48. R5 handler 域下一轮物理抽离已完成: `92-runtime.backtest.execution_start.v4_runtime_execution抽离记录.md` 将 deterministic bars/ticks、blocking runtime replay 和 `run_v4_backtest_runtime_execution` 迁入 `src/runtime/backtest/v4_runtime_execution.rs`；expanded graph、request resolution、projection、record write、schema、state、persistence、frontend 和发布过渡均不迁移。
49. R5 handler 域下一轮单叶 closeout 已完成: `93-runtime.backtest.execution_start.v4_runtime_execution单叶closeout.md` 确认 runtime execution 子模块等价并设置 `stop_split: true`；下一候选回到父叶 `runtime.backtest.execution_start`。

50. R5 handler 域父叶候选基线已建立: `94-runtime.backtest.execution_start.legacy_dispatch单子叶等价基线.md` 冻结 legacy non-v4 path 的 QS compile、execution assumption override、compile artifact bundle、blocking FastBacktestSandbox replay 和排除边界；当前 `no code movement`，下一步只能先做 BE-001R-02 抽离方案。
51. R5 handler 域父叶候选抽离方案已建立: `95-runtime.backtest.execution_start.legacy_dispatch抽离方案.md` 限定下一批只迁移 legacy compile/sandbox dispatch 最小 helper；当前 `no code movement`，下一步若继续只能进入 BE-001R-03 实际抽离。
52. R5 handler 域父叶候选物理抽离已完成: `96-runtime.backtest.execution_start.legacy_dispatch抽离记录.md` 将 legacy compile/assumption/artifact/sandbox replay 两段式 helper 迁入 `src/runtime/backtest/legacy_dispatch.rs`；record assembly、artifact views、transient spill、state write、audit log、schema、persistence、frontend 和发布过渡均不迁移。
53. R5 handler 域父叶候选单叶 closeout 已完成: `97-runtime.backtest.execution_start.legacy_dispatch单叶closeout.md` 确认 legacy dispatch helper 等价并设置 `stop_split: true`；下一步回到 `runtime.backtest.execution_start` 父叶残余判断或 `runtime.backtest` 上层队列。
54. R5 handler 域父叶残余判断已完成: `98-runtime.backtest.execution_start父叶残余判断.md` 确认 `execution_start` 当前不再私拆 record/state/persistence 边界，下一步回到 `runtime.backtest` 上层队列，默认候选为 `runtime.backtest.record_store` / BE-001T-01。
55. R5 backtest handler 域上层候选基线已建立: `99-runtime.backtest.record_store单子叶等价基线.md` 冻结 backtest list/detail/save/discard、transient/persistent record、artifact view、audit 和排除边界；当前 `no code movement`，下一步只能先做 BE-001T-02 抽离方案。
56. R5 backtest handler 域 record_store 抽离方案已建立: `100-runtime.backtest.record_store抽离方案.md` 限定下一批只迁移 `list_backtests`、`get_backtest_detail`、`save_backtest_record`、`discard_backtest_record` 四个 handler；当前 `no code movement`，下一步只能进入 BE-001T-03 实际抽离记录。
57. R5 backtest handler 域 record_store 第一轮物理抽离已完成: `101-runtime.backtest.record_store抽离记录.md` 将四个 handler 迁入 `src/runtime/backtest/record_store.rs`；route facade、shared helper owner、state owner、persistence owner、artifact/transient owner、frontend route 和发布过渡均不迁移。
58. R5 backtest handler 域 record_store 单叶 closeout 已完成: `102-runtime.backtest.record_store单叶closeout.md` 确认四个 handler 等价并设置 `stop_split: true`；下一步回到 `runtime.backtest` sibling 队列，默认先为 `runtime.backtest.replay` 建立等价基线。
59. R5 backtest handler 域 replay 单子叶基线已建立: `103-runtime.backtest.replay单子叶等价基线.md` 冻结 replay route、record lookup、query normalization、response mapping、metrics 和排除边界；当前 `no code movement`，下一步只能先做 BE-001U-02 抽离方案。
60. R5 backtest handler 域 replay 抽离方案已建立: `104-runtime.backtest.replay抽离方案.md` 限定下一批只迁移 `get_backtest_replay`，并保持 route facade、record lookup、query normalization、response mapping、schema、metrics、state/persistence、artifact schema、frontend caller 和发布过渡边界不变。
61. R5 backtest handler 域 replay 第一轮物理抽离已完成: `105-runtime.backtest.replay抽离记录.md` 将 `get_backtest_replay` 迁入 `src/runtime/backtest/replay.rs`；父级 re-export、route facade、record lookup、query normalization、response mapping、schema、metrics、state/persistence、artifact schema、frontend caller 和发布过渡均不迁移。
62. R5 backtest handler 域 replay 单叶 closeout 已完成: `106-runtime.backtest.replay单叶closeout.md` 确认 `get_backtest_replay` 等价并设置 `stop_split: true`；下一步回到 `runtime.backtest` sibling 队列，默认先为 `runtime.backtest.experiment_sweep` 建立等价基线。
63. R5 backtest handler 域 experiment_sweep 单子叶基线已建立: `107-runtime.backtest.experiment_sweep单子叶等价基线.md` 冻结 experiment routes、参数网格、`execute_backtest_request` 复用桥、experiment persistence、save/discard lifecycle、audit、response mapping 和排除边界；当前 `no code movement`，下一步只能先做 BE-001V-02 抽离方案。
64. R5 backtest handler 域 experiment_sweep 抽离方案已建立: `108-runtime.backtest.experiment_sweep抽离方案.md` 限定下一批只迁移 5 个 experiment handler 和 3 个参数网格 helper；当前 `no code movement`，下一步只能进入 BE-001V-03 实际抽离记录。
65. R5 backtest handler 域 experiment_sweep 第一轮物理抽离已完成: `109-runtime.backtest.experiment_sweep抽离记录.md` 将 5 个 experiment handler 和 3 个参数网格 helper 迁入 `src/runtime/backtest/experiment_sweep.rs`；下一步只能进入 BE-001V-04 单叶 closeout。
66. R5 backtest handler 域 experiment_sweep 单叶 closeout 已完成: `110-runtime.backtest.experiment_sweep单叶closeout.md` 确认本叶等价但 `stop_split: false`；下一步只能为 `runtime.backtest.experiment_sweep.parameter_grid` 建立 BE-001W-01 单子叶等价基线。
67. R5 backtest handler 域 experiment_sweep.parameter_grid 单子叶等价基线已建立: `111-runtime.backtest.experiment_sweep.parameter_grid单子叶等价基线.md` 冻结参数网格校验、轴归一化、base fallback、去重、variant count 和展开顺序；当前 `no code movement`，下一步只能进入 BE-001W-02 抽离方案。
68. R5 backtest handler 域 experiment_sweep.parameter_grid 抽离方案已建立: `112-runtime.backtest.experiment_sweep.parameter_grid抽离方案.md` 限定下一批只迁移 3 个 helper 到父级私有子模块；当前 `no code movement`，下一步只能进入 BE-001W-03 实际抽离记录。
69. R5 backtest handler 域 experiment_sweep.parameter_grid 第一轮物理抽离已完成: `113-runtime.backtest.experiment_sweep.parameter_grid抽离记录.md` 将 3 个参数网格 helper 迁入 `src/runtime/backtest/parameter_grid.rs`；下一步只能进入 BE-001W-04 单叶 closeout。
70. R5 backtest handler 域 experiment_sweep.parameter_grid 单叶 closeout 已完成: `114-runtime.backtest.experiment_sweep.parameter_grid单叶closeout.md` 确认本叶等价并设置 `stop_split: true`；下一步回到 `runtime.backtest.experiment_sweep` 父叶残余判断，默认进入 BE-001X-01。
71. R5 backtest handler 域 experiment_sweep 父叶残余判断已完成: `115-runtime.backtest.experiment_sweep父叶残余判断.md` 确认 `parameter_grid` 已关闭但父叶仍 `stop_split: false`；下一步只能进入 BE-001Y-01 `runtime.backtest.experiment_sweep.start_orchestration` 单子叶等价基线。
72. R5 backtest handler 域 experiment_sweep.start_orchestration 单子叶等价基线已建立: `116-runtime.backtest.experiment_sweep.start_orchestration单子叶等价基线.md` 冻结 `start_backtest_experiment` 创建编排、guard、QS compile、variant request、`execute_backtest_request` 复用桥和 preview persistence；当前 `no code movement`，下一步只能进入 BE-001Y-02 抽离方案。
73. R5 backtest handler 域 experiment_sweep.start_orchestration 抽离方案已建立: `117-runtime.backtest.experiment_sweep.start_orchestration抽离方案.md` 限定下一批只迁移 `start_backtest_experiment` 到计划子文件；当前 `no code movement`，下一步只能进入 BE-001Y-03 实际抽离记录。
74. R5 backtest handler 域 experiment_sweep.start_orchestration 实际抽离已完成: `118-runtime.backtest.experiment_sweep.start_orchestration抽离记录.md` 将 `start_backtest_experiment` 迁入 `src/runtime/backtest/start_orchestration.rs`；下一步只能进入 BE-001Y-04 单叶 closeout。
75. R5 backtest handler 域 experiment_sweep.start_orchestration 单叶 closeout 已完成: `119-runtime.backtest.experiment_sweep.start_orchestration单叶closeout.md` 确认本叶等价并设置 `stop_split: true`；该回流已由 BE-001Z-01 `runtime.backtest.experiment_sweep` 父叶残余判断承接，并重新判断 `record_lifecycle` 是否值得另起基线。
76. R5 backtest handler 域 experiment_sweep 第二轮父叶残余判断已完成: `120-runtime.backtest.experiment_sweep第二轮父叶残余判断.md` 确认 `parameter_grid` 与 `start_orchestration` 均已关闭并设置 `stop_split: true`，父叶仍 `stop_split: false`；下一步只能进入 BE-001AA-01 `runtime.backtest.experiment_sweep.record_lifecycle` 单子叶等价基线。
77. R5 backtest handler 域 experiment_sweep.record_lifecycle 单子叶等价基线已建立: `121-runtime.backtest.experiment_sweep.record_lifecycle单子叶等价基线.md` 冻结 list/detail/save/discard 的输入输出、排序分页、scoped lookup、saved conflict、variant backtest persistence、transient cleanup、state cache、audit 和 response mapping 排除边界；当前 `no code movement`，下一步只能进入 BE-001AA-02 抽离方案。
78. R5 backtest handler 域 experiment_sweep.record_lifecycle 抽离方案已建立: `122-runtime.backtest.experiment_sweep.record_lifecycle抽离方案.md` 限定下一批只迁移四个 lifecycle handler 到计划目标文件 `src/runtime/backtest/record_lifecycle.rs`；当前 `no code movement`，下一步只能进入 BE-001AA-03 实际抽离记录。
79. R5 backtest handler 域 experiment_sweep.record_lifecycle 实际抽离已完成: `123-runtime.backtest.experiment_sweep.record_lifecycle抽离记录.md` 将四个 lifecycle handler 迁入 `src/runtime/backtest/record_lifecycle.rs`；下一步只能进入 BE-001AA-04 单叶 closeout。
80. R5 backtest handler 域 experiment_sweep.record_lifecycle 单叶 closeout 已完成: `124-runtime.backtest.experiment_sweep.record_lifecycle单叶closeout.md` 确认四个 lifecycle handler 等价并设置 `stop_split: true`；下一步只能进入 BE-001AB-01 第三轮父叶残余判断。
81. R5 backtest handler 域 experiment_sweep 第三轮父叶残余判断已完成: `125-runtime.backtest.experiment_sweep第三轮父叶残余判断.md` 确认 `parameter_grid`、`start_orchestration`、`record_lifecycle` 三个子叶均已 closeout 并设置 `stop_split: true`，父叶自身也设置 `stop_split: true`；下一步只能进入 BE-001AC-01 `runtime.backtest` 父叶残余判断。
82. R5 backtest handler 域 runtime.backtest 父叶残余判断已完成: `126-runtime.backtest父叶残余判断.md` 确认 `execution_start`、`record_store`、`replay`、`experiment_sweep` 均已完成当前递归范围内 closeout，父叶自身设置 `stop_split: true`；下一步只能进入 BE-001AD-01 `backend.runtime.routes` 父叶残余判断。
83. R5 route aggregate 父叶残余判断已完成: `127-backend.runtime.routes父叶残余判断.md` 确认 `backend.runtime.routes` 仍有 mutation / AI proposal / approval / evidence / report / experiment / ops 等残余候选，父叶保持 `stop_split: false`；BE-001AE-01 已建立 `backend.runtime.routes.mutation` 单子叶等价基线，下一步只能进入 BE-001AE-02 抽离方案。
84. R5 route aggregate mutation 单子叶等价基线已建立: `128-backend.runtime.routes.mutation单子叶等价基线.md` 冻结 mutation / AI proposal / approval route group 的 path、method、handler owner、输入输出、`approval_records -> ai_proposals` 锁顺序、测试证据和禁止迁移边界；当前 `no code movement`，下一步只能进入 BE-001AE-02 抽离方案。
85. R5 route aggregate mutation 抽离方案已建立: `129-backend.runtime.routes.mutation抽离方案.md` 只规划 route facade 最小迁移；下一步只能进入 BE-001AE-03 实际抽离记录，新建 planned `src/backend/runtime/routes/mutation.rs` 并由父级委托，不得移动 handler、state、schema、frontend caller 或发布过渡连接。
86. R5 route aggregate mutation 实际抽离已完成: `130-backend.runtime.routes.mutation抽离记录.md` 已将 mutation / AI proposal / approval route group 迁入 `src/backend/runtime/routes/mutation.rs`，父级 `src/backend/runtime/routes.rs` 只保留 `pub mod mutation` 与 `mutation::register_routes(router)` 委托；下一步只能进入 BE-001AE-04 单叶 closeout，不得移动 handler、state、schema、frontend caller 或发布过渡连接。
87. R5 route aggregate mutation 单叶 closeout 已完成: `131-backend.runtime.routes.mutation单叶closeout.md` 确认 route facade 等价并设置 `stop_split: true`；`src/runtime/mutation.rs` handler 域值得进入 BE-001AF-01 `runtime.mutation.parameter_mutation` 单子叶等价基线，下一步不得移动 handler、AppState、锁顺序、schema、frontend caller 或发布过渡连接。
88. R5 mutation handler 域 parameter_mutation 单子叶等价基线已建立: `132-runtime.mutation.parameter_mutation单子叶等价基线.md` 冻结 create/list/detail/activate/rollback、safe window、activation/rollback event、parameter version、run record append、persistence bridge、状态 owner、schema owner 和 `api_mutation` 证据；当前 `no code movement`，下一步只能进入 BE-001AF-02 抽离方案。
89. R5 mutation handler 域 parameter_mutation 抽离方案已建立: `133-runtime.mutation.parameter_mutation抽离方案.md` 固定目标子模块、父级 re-export、五个 public handler 迁移清单、本叶私有 helper 迁移清单和 shared helper 保留边界；当前仍是 `no code movement`，下一步只能进入 BE-001AF-03 实际抽离。
90. R5 mutation handler 域 parameter_mutation 实际抽离已完成: `134-runtime.mutation.parameter_mutation抽离记录.md` 将 create/list/detail/activate/rollback 和本叶私有 helper 迁入 `src/runtime/mutation/parameter_mutation.rs`，父级 `src/runtime/mod.rs` 通过 `pub(crate) use mutation_parameter_mutation` 保持兼容出口；下一步只能进入 BE-001AF-04 单叶整理 / closeout，不得移动 AI proposal、approval review、AppState、schema、frontend caller 或发布过渡连接。
91. R5 mutation handler 域 parameter_mutation 单叶 closeout 已完成: `135-runtime.mutation.parameter_mutation单叶closeout.md` 确认第一轮抽离等价成立，但本叶设置 `stop_split: false`；下一步只能进入 BE-001AG-01 `runtime.mutation.parameter_mutation.transition_lifecycle` 单子叶等价基线，不得直接移动代码。
92. R5 mutation handler 域 transition_lifecycle 单子叶等价基线已建立: `136-runtime.mutation.parameter_mutation.transition_lifecycle单子叶等价基线.md` 冻结 activation / rollback lifecycle、safe window、boundary resolution、transition persistence、auto snapshot side effect 和 run event append；当前 `no code movement`，下一步只能进入 BE-001AG-02 抽离方案。
93. R5 mutation handler 域 transition_lifecycle 抽离方案已建立: `137-runtime.mutation.parameter_mutation.transition_lifecycle抽离方案.md` 固定目标文件 `src/runtime/mutation/parameter_mutation/transition_lifecycle.rs`、父级 `#[path = "parameter_mutation/transition_lifecycle.rs"] mod transition_lifecycle;`、handler re-export 和 boundary validation 的 `pub(super)` 父级复用；当前 `no code movement`，下一步只能进入 BE-001AG-03 实际抽离。
94. R5 mutation handler 域 transition_lifecycle 实际抽离已完成: `138-runtime.mutation.parameter_mutation.transition_lifecycle抽离记录.md` 记录 `src/runtime/mutation/parameter_mutation/transition_lifecycle.rs` 已承接 activation / rollback handler、boundary/safe window、transition persistence 和 auto snapshot helper；父级以 `#[path = "parameter_mutation/transition_lifecycle.rs"] mod transition_lifecycle;` 注册 child，下一步只能进入 BE-001AG-04 单叶 closeout。
95. R5 mutation handler 域 transition_lifecycle 单叶 closeout 已完成: `139-runtime.mutation.parameter_mutation.transition_lifecycle单叶closeout.md` 判定实际抽离等价成立，但本叶设置 `stop_split: false`；下一步只能进入 BE-001AH-01 `runtime.mutation.parameter_mutation.transition_lifecycle.boundary_safety` 单子叶等价基线。
96. R5 mutation handler 域 boundary_safety 单子叶等价基线已建立: `140-runtime.mutation.parameter_mutation.transition_lifecycle.boundary_safety单子叶等价基线.md` 冻结 boundary validation、boundary resolution、safe window evaluation、reason code 优先级和父级复用方式；当前 `no code movement`，下一步只能进入 BE-001AH-02 抽离方案。
97. R5 mutation handler 域 boundary_safety 抽离方案已建立: `141-runtime.mutation.parameter_mutation.transition_lifecycle.boundary_safety抽离方案.md` 固定目标文件、父级 `mod boundary_safety`、delegating validation wrapper、helper visibility 和回退点；当前 `no code movement`，下一步只能进入 BE-001AH-03 实际抽离。
98. R5 mutation handler 域 boundary_safety 实际抽离已完成: `142-runtime.mutation.parameter_mutation.transition_lifecycle.boundary_safety抽离记录.md` 迁移 boundary validation、boundary resolution 和 safe-window evaluation 三个 helper，父级保留 delegating validation wrapper；下一步只能进入 BE-001AH-04 单叶 closeout。
99. R5 mutation handler 域 boundary_safety 单叶 closeout 已完成: `143-runtime.mutation.parameter_mutation.transition_lifecycle.boundary_safety单叶closeout.md` 设置 `stop_split: true`；下一步只能进入 BE-001AI-01 `runtime.mutation.parameter_mutation.transition_lifecycle` 父叶残余判断。
100. R5 mutation handler 域 transition_lifecycle 父叶残余判断已完成: `144-runtime.mutation.parameter_mutation.transition_lifecycle父叶残余判断.md` 确认 `boundary_safety` 已停止细拆，但父叶仍保持 `stop_split: false`；下一步只能进入 BE-001AJ-01 `runtime.mutation.parameter_mutation.transition_lifecycle.activation_flow` 单子叶等价基线。
101. R5 mutation handler 域 activation_flow 单子叶等价基线已建立: `145-runtime.mutation.parameter_mutation.transition_lifecycle.activation_flow单子叶等价基线.md` 冻结 activation handler 状态机、event append、metrics、transition persistence 和 snapshot trigger；当前 `no code movement`，下一步只能进入 BE-001AJ-02 抽离方案。
102. R5 mutation handler 域 activation_flow 抽离方案已建立: `146-runtime.mutation.parameter_mutation.transition_lifecycle.activation_flow抽离方案.md` 固定目标文件、path-attributed child、父级 handler re-export 和 helper 保留边界；当前 `no code movement`，下一步只能进入 BE-001AJ-03 实际抽离。
103. R5 mutation handler 域 activation_flow 实际抽离已完成: `147-runtime.mutation.parameter_mutation.transition_lifecycle.activation_flow抽离记录.md` 迁移 `activate_runtime_parameter_mutation` 到 `src/runtime/mutation/parameter_mutation/transition_lifecycle/activation_flow.rs`，父级保留 path-attributed child 与 handler re-export；下一步只能进入 BE-001AJ-04 单叶 closeout。
104. R5 mutation handler 域 activation_flow 单叶 closeout 已完成: `148-runtime.mutation.parameter_mutation.transition_lifecycle.activation_flow单叶closeout.md` 设置 `stop_split: true`；下一步只能进入 BE-001AK-01 `runtime.mutation.parameter_mutation.transition_lifecycle` 父叶残余判断。
105. R5 mutation handler 域 transition_lifecycle 第二轮父叶残余判断已完成: `149-runtime.mutation.parameter_mutation.transition_lifecycle第二轮父叶残余判断.md` 确认 `boundary_safety` 与 `activation_flow` 已停止细拆，但父叶仍保持 `stop_split: false`；下一步只能进入 BE-001AL-01 `runtime.mutation.parameter_mutation.transition_lifecycle.rollback_flow` 单子叶等价基线。
106. R5 mutation handler 域 rollback_flow 单子叶等价基线已建立: `150-runtime.mutation.parameter_mutation.transition_lifecycle.rollback_flow单子叶等价基线.md` 冻结 `rollback_runtime_parameter_mutation` 的 activated-only gate、ledger lookup、safe-window、RollbackScheduled / RolledBack / RollbackFailed 状态机、run event append、metrics 和 transition persistence；当前 `no code movement`，下一步只能进入 BE-001AL-02 抽离方案。
107. R5 mutation handler 域 rollback_flow 抽离方案已建立: `151-runtime.mutation.parameter_mutation.transition_lifecycle.rollback_flow抽离方案.md` 固定目标文件、path-attributed child、父级 handler re-export 和 helper 保留边界；当前 `no code movement`，下一步只能进入 BE-001AL-03 实际抽离。
108. R5 mutation handler 域 rollback_flow 实际抽离已完成: `152-runtime.mutation.parameter_mutation.transition_lifecycle.rollback_flow抽离记录.md` 迁移 `rollback_runtime_parameter_mutation` 到 `src/runtime/mutation/parameter_mutation/transition_lifecycle/rollback_flow.rs`，父级保留 path-attributed child 与 handler re-export；下一步只能进入 BE-001AL-04 单叶 closeout。
109. R5 mutation handler 域 rollback_flow 单叶 closeout 已完成: `153-runtime.mutation.parameter_mutation.transition_lifecycle.rollback_flow单叶closeout.md` 设置 `stop_split: true`；下一步只能进入 BE-001AM-01 `runtime.mutation.parameter_mutation.transition_lifecycle` 父叶残余判断。
110. R5 mutation handler 域 transition_lifecycle 第三轮父叶残余判断已完成: `154-runtime.mutation.parameter_mutation.transition_lifecycle第三轮父叶残余判断.md` 确认 `boundary_safety`、`activation_flow` 与 `rollback_flow` 已停止细拆，但父叶仍保持 `stop_split: false`；下一步只能进入 BE-001AN-01 `runtime.mutation.parameter_mutation.transition_lifecycle.activation_snapshot_side_effect` 单子叶等价基线。
111. R5 mutation handler 域 activation_snapshot_side_effect 单子叶等价基线已建立: `155-runtime.mutation.parameter_mutation.transition_lifecycle.activation_snapshot_side_effect单子叶等价基线.md` 冻结 `auto_snapshot_on_activation` 的 config generation、history truncation、snapshot id/payload/signature、atomic write 和 in-memory insert；当前 `no code movement`，下一步只能进入 BE-001AN-02 抽离方案。
112. R5 mutation handler 域 activation_snapshot_side_effect 抽离方案已建立: `156-runtime.mutation.parameter_mutation.transition_lifecycle.activation_snapshot_side_effect抽离方案.md` 固定目标 child、父级 path attribute、helper import、`pub(super)` 可见性和回退点；当前 `no code movement`，下一步只能进入 BE-001AN-03 实际抽离。
113. R5 mutation handler 域 activation_snapshot_side_effect 实际抽离已完成: `157-runtime.mutation.parameter_mutation.transition_lifecycle.activation_snapshot_side_effect抽离记录.md` 记录 `auto_snapshot_on_activation` 已迁入 child，父级通过 path-attributed child 和 helper import 保持受控调用面；下一步只能进入 BE-001AN-04 单叶 closeout。
114. R5 mutation handler 域 activation_snapshot_side_effect 单叶 closeout 已完成: `158-runtime.mutation.parameter_mutation.transition_lifecycle.activation_snapshot_side_effect单叶closeout.md` 设置 `stop_split: true`；下一步只能回到 BE-001AO-01 `transition_lifecycle` 父叶残余判断。
115. R5 mutation handler 域 transition_lifecycle 第四轮父叶残余判断已完成: `159-runtime.mutation.parameter_mutation.transition_lifecycle第四轮父叶残余判断.md` 确认四个已抽子叶均已 closeout 并设置 `stop_split: true`，但父叶仍保留 `mutation_lifecycle_entry`、`persist_runtime_parameter_mutation_transition` 与 `runtime_parameter_mutation_rollback_record_id`，因此父叶保持 `stop_split: false`；下一步只能进入 BE-001AP-01 `runtime.mutation.parameter_mutation.transition_lifecycle.transition_record_persistence` 单子叶等价基线。
116. R5 mutation handler 域 transition_record_persistence 单子叶等价基线已建立: `160-runtime.mutation.parameter_mutation.transition_lifecycle.transition_record_persistence单子叶等价基线.md` 冻结 lifecycle entry 构造、transition record 持久化、`state.parameter_mutations` in-memory index 和 activation / rollback 调用点；当前 `no code movement`，下一步只能进入 BE-001AP-02 抽离方案。
117. R5 mutation handler 域 transition_record_persistence 抽离方案已建立: `161-runtime.mutation.parameter_mutation.transition_lifecycle.transition_record_persistence抽离方案.md` 固定目标文件、父级 path attribute、helper import、`pub(super)` visibility 和回退点；当前 `no code movement`，下一步只能进入 BE-001AP-03 实际抽离。
118. R5 mutation handler 域 transition_record_persistence 实际抽离已完成: `162-runtime.mutation.parameter_mutation.transition_lifecycle.transition_record_persistence抽离记录.md` 创建 `src/runtime/mutation/parameter_mutation/transition_lifecycle/transition_record_persistence.rs` 并迁移 `mutation_lifecycle_entry` 与 `persist_runtime_parameter_mutation_transition`；下一步只能进入 BE-001AP-04 单叶 closeout。
119. R5 mutation handler 域 transition_record_persistence 单叶 closeout 已完成: `163-runtime.mutation.parameter_mutation.transition_lifecycle.transition_record_persistence单叶closeout.md` 设置 `stop_split: true`；下一步只能回到 BE-001AQ-01 `transition_lifecycle` 第五轮父叶残余判断。
120. R5 mutation handler 域 transition_lifecycle 第五轮父叶残余判断已完成: `164-runtime.mutation.parameter_mutation.transition_lifecycle第五轮父叶残余判断.md` 确认五个已抽子叶均已 closeout 并设置 `stop_split: true`，但父叶仍保留 `runtime_parameter_mutation_rollback_record_id`，因此父叶保持 `stop_split: false`；下一步只能进入 BE-001AR-01 `runtime.mutation.parameter_mutation.transition_lifecycle.rollback_record_identity` 单子叶等价基线。
121. R5 mutation handler 域 rollback_record_identity 单子叶等价基线已建立: `165-runtime.mutation.parameter_mutation.transition_lifecycle.rollback_record_identity单子叶等价基线.md` 冻结 rollback id digest input、prefix、slice length 和 error mapping；当前 `no code movement`，下一步只能进入 BE-001AR-02 抽离方案。
122. R5 mutation handler 域 rollback_record_identity 抽离方案已建立: `166-runtime.mutation.parameter_mutation.transition_lifecycle.rollback_record_identity抽离方案.md` 固定目标文件、父级 path attribute、helper import、`pub(super)` visibility 和回退点；当前 `no code movement`，下一步只能进入 BE-001AR-03 实际抽离。
123. R5 mutation handler 域 rollback_record_identity 实际抽离已完成: `167-runtime.mutation.parameter_mutation.transition_lifecycle.rollback_record_identity抽离记录.md` 创建 `src/runtime/mutation/parameter_mutation/transition_lifecycle/rollback_record_identity.rs` 并迁移 `runtime_parameter_mutation_rollback_record_id`；下一步只能进入 BE-001AR-04 单叶 closeout。
124. R5 mutation handler 域 rollback_record_identity 单叶 closeout 已完成: `168-runtime.mutation.parameter_mutation.transition_lifecycle.rollback_record_identity单叶closeout.md` 设置 `stop_split: true`；下一步只能回到 BE-001AS-01 `transition_lifecycle` 第六轮父叶残余判断。
125. R5 mutation handler 域 transition_lifecycle 第六轮父叶残余判断已完成: `169-runtime.mutation.parameter_mutation.transition_lifecycle第六轮父叶残余判断.md` 确认六个子叶均已 closeout，父叶只剩 facade / re-export / wrapper / child imports，设置 `stop_split: true`；下一步只能进入 BE-001AT-01 `runtime.mutation.parameter_mutation` 父叶残余判断。
126. R5 mutation handler 域 parameter_mutation 父叶残余判断已完成: `170-runtime.mutation.parameter_mutation父叶残余判断.md` 确认 proposal creation handler、record id helper、list/detail handler 仍为 parent-owned implementation residual，父叶保持 `stop_split: false`；下一步只能进入 BE-001AU-01 `proposal_creation` 单子叶等价基线。
127. R5 mutation handler 域 proposal_creation 单子叶等价基线已建立: `171-runtime.mutation.parameter_mutation.proposal_creation单子叶等价基线.md` 冻结 create handler、record id helper、record 字段与调用顺序；当前 `no code movement`，下一步只能进入 BE-001AU-02 抽离方案。
128. R5 mutation handler 域 proposal_creation 抽离方案已建立: `172-runtime.mutation.parameter_mutation.proposal_creation抽离方案.md` 固定目标文件、父级 path attribute、handler re-export、`use super::*`、迁移清单和回退点；当前 `no code movement`，下一步只能进入 BE-001AU-03 实际抽离。
129. R5 mutation handler 域 proposal_creation 实际抽离已完成: `173-runtime.mutation.parameter_mutation.proposal_creation抽离记录.md` 创建 `src/runtime/mutation/parameter_mutation/proposal_creation.rs` 并迁移 create handler 与 record id helper；下一步只能进入 BE-001AU-04 单叶 closeout。
130. R5 mutation handler 域 proposal_creation 单叶 closeout 已完成: `174-runtime.mutation.parameter_mutation.proposal_creation单叶closeout.md` 确认本叶等价并设置 `stop_split: true`；下一步只能进入 BE-001AV-01 `runtime.mutation.parameter_mutation` 父叶残余判断。
131. R5 mutation handler 域 parameter_mutation 第二轮父叶残余判断已完成: `175-runtime.mutation.parameter_mutation第二轮父叶残余判断.md` 确认 `transition_lifecycle` 与 `proposal_creation` 均已 closeout，但 list/detail 查询流仍为父叶残余，父叶保持 `stop_split: false`；下一步只能进入 BE-001AW-01 `record_query` 单子叶等价基线。
132. R5 mutation handler 域 record_query 单子叶等价基线已建立: `176-runtime.mutation.parameter_mutation.record_query单子叶等价基线.md` 冻结 list/detail 查询流的排序、filtering、scoped lookup、in-memory 优先级、persistence fallback 和 pagination 语义；当前 `no code movement`，下一步只能进入 BE-001AW-02 抽离方案。
133. R5 mutation handler 域 record_query 抽离方案已建立: `177-runtime.mutation.parameter_mutation.record_query抽离方案.md` 固定 BE-001AW-03 目标文件、父级 path attribute、双 handler re-export、`use super::*`、迁移清单和回退点；当前 `no code movement`，下一步只能进入 BE-001AW-03 实际抽离。
134. R5 mutation handler 域 record_query 实际抽离已完成: `178-runtime.mutation.parameter_mutation.record_query抽离记录.md` 创建 `src/runtime/mutation/parameter_mutation/record_query.rs` 并迁移 list/detail handler；下一步只能进入 BE-001AW-04 单叶 closeout。
135. R5 mutation handler 域 record_query 单叶 closeout 已完成: `179-runtime.mutation.parameter_mutation.record_query单叶closeout.md` 确认 read model 等价并设置 `stop_split: true`；下一步只能进入 BE-001AX-01 `runtime.mutation.parameter_mutation` 父叶残余判断。
136. R5 mutation handler 域 parameter_mutation 第三轮父叶残余判断已完成: `180-runtime.mutation.parameter_mutation第三轮父叶残余判断.md` 确认三个 child 均已 closeout，父叶只剩 facade / child declaration / re-export / controlled import，并设置 `stop_split: true`；下一步只能进入 BE-001AY-01 `runtime.mutation.ai_proposal` 单子叶等价基线。
137. R5 mutation handler 域 AI proposal 单子叶等价基线已建立: `181-runtime.mutation.ai_proposal单子叶等价基线.md` 冻结 AI proposal candidate、static check、proposal list/detail、approval list/detail、approve/reject/claim、sandbox gate、approval persistence、状态迁移和测试证据；当前 `no code movement`，下一步只能进入 BE-001AY-02 抽离方案。
138. R5 mutation handler 域 AI proposal 抽离方案已建立: `182-runtime.mutation.ai_proposal抽离方案.md` 固定 BE-001AY-03 目标文件 `src/runtime/mutation/ai_proposal.rs`、父级 `mutation_ai_proposal` 声明、public handler re-export、child import、迁移清单和 shared helper 保留清单；当前 `no code movement`，下一步只能进入 BE-001AY-03 实际抽离。
139. R5 mutation handler 域 AI proposal 实际抽离已完成: `183-runtime.mutation.ai_proposal抽离记录.md` 创建 `src/runtime/mutation/ai_proposal.rs` 并迁移 AI proposal / approval public handlers 与专属 helper；下一步只能进入 BE-001AY-04 单叶 closeout。
140. R5 mutation handler 域 AI proposal 单叶 closeout 已完成: `184-runtime.mutation.ai_proposal单叶closeout.md` 确认第一轮抽离等价成立，但本叶设置 `stop_split: false`；下一步只能进入 BE-001AZ-01 `runtime.mutation.ai_proposal.static_check` 单子叶等价基线。
141. R5 mutation handler 域 AI proposal static_check 单子叶等价基线已建立: `185-runtime.mutation.ai_proposal.static_check单子叶等价基线.md` 冻结 validation / analysis helper 与测试证据；当前 `no code movement`，下一步只能进入 BE-001AZ-02 抽离方案。
142. R5 mutation handler 域 AI proposal static_check 抽离方案已建立: `186-runtime.mutation.ai_proposal.static_check抽离方案.md` 固定 BE-001AZ-03 目标文件、父级 child 声明、helper import、`pub(super)` visibility、迁移清单和回退点；当前 `no code movement`，下一步只能进入 BE-001AZ-03 实际抽离。
143. R5 mutation handler 域 AI proposal static_check 实际抽离已完成: `187-runtime.mutation.ai_proposal.static_check抽离记录.md` 确认 helper 与静态检查单测已迁入 `src/runtime/mutation/ai_proposal/static_check.rs`，父级只保留受控 child import；下一步只能进入 BE-001AZ-04 单叶 closeout。
144. R5 mutation handler 域 AI proposal static_check 单叶 closeout 已完成: `188-runtime.mutation.ai_proposal.static_check单叶closeout.md` 确认本叶设置 `stop_split: true`，后续回到父级残余判断。
145. R5 mutation handler 域 AI proposal 父叶残余判断已完成: `189-runtime.mutation.ai_proposal父叶残余判断.md` 确认父叶保持 `stop_split: false`，下一候选为 BE-001BB-01 `runtime.mutation.ai_proposal.source_governance_identity`。
146. R5 mutation handler 域 AI proposal source_governance_identity 单子叶等价基线已建立: `190-runtime.mutation.ai_proposal.source_governance_identity单子叶等价基线.md` 冻结 source context、governance projection、record identity 与非目标边界；当前 `no code movement`，下一步只能进入 BE-001BB-02 抽离方案。
147. R5 mutation handler 域 AI proposal source_governance_identity 抽离方案已建立: `191-runtime.mutation.ai_proposal.source_governance_identity抽离方案.md` 固定 BE-001BB-03 目标文件、父级 child 声明、helper import、`pub(super)` struct / field visibility、迁移清单和回退点；当前 `no code movement`，下一步只能进入 BE-001BB-03 实际抽离。
148. R5 mutation handler 域 AI proposal source_governance_identity 实际抽离已完成: `192-runtime.mutation.ai_proposal.source_governance_identity抽离记录.md` 确认 source context、governance projection 与 record identity helper 已迁入 child，父级只保留受控 import；下一步只能进入 BE-001BB-04 单叶 closeout。
149. R5 mutation handler 域 AI proposal source_governance_identity 单叶 closeout 已完成: `193-runtime.mutation.ai_proposal.source_governance_identity单叶closeout.md` 确认本叶设置 `stop_split: true`，后续回到父级残余判断。
150. R5 mutation handler 域 AI proposal 第二轮父叶残余判断已完成: `194-runtime.mutation.ai_proposal第二轮父叶残余判断.md` 确认父叶保持 `stop_split: false`，下一候选为 BE-001BD-01 `runtime.mutation.ai_proposal.event_lifecycle`。
151. R5 mutation handler 域 AI proposal event_lifecycle 单子叶等价基线已建立: `195-runtime.mutation.ai_proposal.event_lifecycle单子叶等价基线.md` 冻结 event contract、event payload、lifecycle entry、transition persistence 与非目标边界；当前 `no code movement`，下一步只能进入 BE-001BD-02 抽离方案。
152. R5 mutation handler 域 AI proposal event_lifecycle 抽离方案已建立: `196-runtime.mutation.ai_proposal.event_lifecycle抽离方案.md` 固定 BE-001BD-03 目标文件、父级 child 声明、helper import、`pub(super)` visibility、迁移清单和回退点；当前 `no code movement`，下一步只能进入 BE-001BD-03 实际抽离。
153. R5 mutation handler 域 AI proposal event_lifecycle 实际抽离已完成: `197-runtime.mutation.ai_proposal.event_lifecycle抽离记录.md` 将 event contract、event builder、lifecycle entry 与 transition persistence helper 迁入 `src/runtime/mutation/ai_proposal/event_lifecycle.rs`；下一步只能进入 BE-001BD-04 单叶 closeout。
154. R5 mutation handler 域 AI proposal event_lifecycle 单叶 closeout 已完成: `198-runtime.mutation.ai_proposal.event_lifecycle单叶closeout.md` 设置 `stop_split: true`；下一步只能进入 BE-001BE-01 `runtime.mutation.ai_proposal` 父叶残余判断。
155. R5 mutation handler 域 AI proposal 第三轮父叶残余判断已完成: `199-runtime.mutation.ai_proposal第三轮父叶残余判断.md` 确认父叶保持 `stop_split: false`，下一候选为 BE-001BF-01 `runtime.mutation.ai_proposal.record_query`。
156. R5 mutation handler 域 AI proposal record_query 单子叶等价基线已建立: `200-runtime.mutation.ai_proposal.record_query单子叶等价基线.md` 冻结 proposal list/detail/read-through loader 的 filtering、sorting、memory-first lookup 与 disk fallback；当前 `no code movement`，下一步只能进入 BE-001BF-02 抽离方案。
157. R5 mutation handler 域 AI proposal record_query 抽离方案已建立: `201-runtime.mutation.ai_proposal.record_query抽离方案.md` 固定目标文件、父级 child 声明、双 handler re-export、loader helper import、迁移清单、非目标和回退点；当前 `no code movement`，下一步只能进入 BE-001BF-03 实际抽离。
158. R5 mutation handler 域 AI proposal record_query 实际抽离已完成: `202-runtime.mutation.ai_proposal.record_query抽离记录.md` 将 proposal list/detail/read-through loader 迁入 child 文件；下一步只能进入 BE-001BF-04 单叶 closeout。
159. R5 mutation handler 域 AI proposal record_query 单叶 closeout 已完成: `203-runtime.mutation.ai_proposal.record_query单叶closeout.md` 设置 `stop_split: true`；下一步只能进入 BE-001BG-01 `runtime.mutation.ai_proposal` 父叶残余判断。
160. R5 mutation handler 域 AI proposal 第四轮父叶残余判断已完成: `204-runtime.mutation.ai_proposal第四轮父叶残余判断.md` 确认父叶保持 `stop_split: false`，下一候选为 BE-001BH-01 `runtime.mutation.ai_proposal.approval_review`。
161. R5 mutation handler 域 AI proposal approval_review 单子叶等价基线已建立: `205-runtime.mutation.ai_proposal.approval_review单子叶等价基线.md` 冻结 approval list/detail/approve/reject/claim、锁顺序、review_state guard、reviewer lifecycle 与 status side effect；当前 `no code movement`，下一步只能进入 BE-001BH-02 抽离方案。
162. R5 mutation handler 域 AI proposal approval_review 抽离方案已建立: `206-runtime.mutation.ai_proposal.approval_review抽离方案.md` 固定目标文件、父级 child 声明、五 handler re-export、helper 保留清单和回退点；当前 `no code movement`，下一步只能进入 BE-001BH-03 实际抽离。
163. R5 mutation handler 域 AI proposal approval_review 实际抽离已完成: `207-runtime.mutation.ai_proposal.approval_review抽离记录.md` 将 approval list/detail/approve/reject/claim 五个 handler 迁入 `src/runtime/mutation/ai_proposal/approval_review.rs`；下一步只能进入 BE-001BH-04 单叶 closeout。
164. R5 mutation handler 域 AI proposal approval_review 单叶 closeout 已完成: `208-runtime.mutation.ai_proposal.approval_review单叶closeout.md` 设置 `stop_split: true`；下一步只能进入 BE-001BI-01 `runtime.mutation.ai_proposal` 第五轮父叶残余判断。
165. R5 mutation handler 域 AI proposal 第五轮父叶残余判断已完成: `209-runtime.mutation.ai_proposal第五轮父叶残余判断.md` 确认父叶保持 `stop_split: false`，下一候选为 BE-001BJ-01 `runtime.mutation.ai_proposal.approval_persistence`。
166. R5 mutation handler 域 AI proposal approval_persistence 单子叶等价基线已建立: `210-runtime.mutation.ai_proposal.approval_persistence单子叶等价基线.md` 冻结 approval record persistence helper、disk fallback、not_found 与 serde decode 边界；当前 `no code movement`，下一步只能进入 BE-001BJ-02 抽离方案。
167. R5 mutation handler 域 AI proposal approval_persistence 抽离方案已建立: `211-runtime.mutation.ai_proposal.approval_persistence抽离方案.md` 固定目标文件、父级 child 声明、helper import、`pub(super)` visibility 与回退点；当前 `no code movement`，下一步只能进入 BE-001BJ-03 实际抽离。
168. R5 mutation handler 域 AI proposal approval_persistence 实际抽离已完成: `212-runtime.mutation.ai_proposal.approval_persistence抽离记录.md` 将 `persist_approval` 与 `load_approval_from_disk` 迁入 child，父级保留受控 helper import；下一步只能进入 BE-001BJ-04 单叶 closeout。
169. R5 mutation handler 域 AI proposal approval_persistence 单叶 closeout 已完成: `213-runtime.mutation.ai_proposal.approval_persistence单叶closeout.md` 设置 `stop_split: true`；下一步只能进入 BE-001BK-01 `runtime.mutation.ai_proposal` 第六轮父叶残余判断。
170. R5 mutation handler 域 AI proposal 第六轮父叶残余判断已完成: `214-runtime.mutation.ai_proposal第六轮父叶残余判断.md` 确认父叶保持 `stop_split: false`，下一候选为 BE-001BL-01 `runtime.mutation.ai_proposal.sandbox_trigger`。
171. R5 mutation handler 域 AI proposal sandbox_trigger 单子叶等价基线已建立: `215-runtime.mutation.ai_proposal.sandbox_trigger单子叶等价基线.md` 冻结 sandbox gate、background task、retry / panic guard、report URL 回写与失败 lifecycle；当前 `no code movement`，下一步只能进入 BE-001BL-02 抽离方案。
172. R5 mutation handler 域 AI proposal sandbox_trigger 抽离方案已建立: `216-runtime.mutation.ai_proposal.sandbox_trigger抽离方案.md` 固定目标文件、父级 child 声明、helper import、`FutureExt` 迁移和 background task helper 提取方式；当前 `no code movement`，下一步只能进入 BE-001BL-03 实际抽离。
173. R5 mutation handler 域 AI proposal sandbox_trigger 实际抽离已完成: `217-runtime.mutation.ai_proposal.sandbox_trigger抽离记录.md` 将 sandbox approve gate 与 background sandbox verification helper 迁入 `src/runtime/mutation/ai_proposal/sandbox_trigger.rs`；下一步只能进入 BE-001BL-04 单叶 closeout。
174. R5 mutation handler 域 AI proposal sandbox_trigger 单叶 closeout 已完成: `218-runtime.mutation.ai_proposal.sandbox_trigger单叶closeout.md` 设置 `stop_split: true`；下一步只能进入 BE-001BM-01 `runtime.mutation.ai_proposal` 第七轮父叶残余判断。
175. R5 mutation handler 域 AI proposal 第七轮父叶残余判断已完成: `219-runtime.mutation.ai_proposal第七轮父叶残余判断.md` 确认父叶保持 `stop_split: false`，下一候选为 BE-001BN-01 `runtime.mutation.ai_proposal.status_transition`。
176. R5 mutation handler 域 AI proposal status_transition 单子叶等价基线已建立: `220-runtime.mutation.ai_proposal.status_transition单子叶等价基线.md` 冻结 approved projection、状态迁移矩阵、状态写入副作用和父子通信规则；当前 `no code movement`，下一步只能进入 BE-001BN-02 抽离方案。
177. R5 mutation handler 域 AI proposal status_transition 抽离方案已建立: `221-runtime.mutation.ai_proposal.status_transition抽离方案.md` 固定目标文件、父级 child 声明、helper import 和 child-private transition guard；当前 `no code movement`，下一步只能进入 BE-001BN-03 实际抽离。
178. R5 mutation handler 域 AI proposal status_transition 实际抽离已完成: `222-runtime.mutation.ai_proposal.status_transition抽离记录.md` 创建 `src/runtime/mutation/ai_proposal/status_transition.rs`，迁移 approved projection、状态迁移矩阵和 scoped status side effect；下一步只能进入 BE-001BN-04 单叶 closeout。
179. R5 mutation handler 域 AI proposal status_transition 单叶 closeout 已完成: `223-runtime.mutation.ai_proposal.status_transition单叶closeout.md` 设置 `stop_split: true`；下一步只能进入 BE-001BO-01 `runtime.mutation.ai_proposal` 第八轮父叶残余判断。
180. R5 mutation handler 域 AI proposal 第八轮父叶残余判断已完成: `224-runtime.mutation.ai_proposal第八轮父叶残余判断.md` 确认父叶仍保留 proposal create orchestration，父叶保持 `stop_split: false`；下一候选为 BE-001BP-01 `runtime.mutation.ai_proposal.proposal_creation`。
181. R5 mutation handler 域 AI proposal proposal_creation 单子叶等价基线已建立: `225-runtime.mutation.ai_proposal.proposal_creation单子叶等价基线.md` 冻结 `create_runtime_ai_proposal` 输入输出、调用顺序、状态副作用、`approval_records -> ai_proposals` 锁顺序和非目标边界；当前 `no code movement`，下一步只能进入 BE-001BP-02 抽离方案。
182. R5 mutation handler 域 AI proposal proposal_creation 抽离方案已建立: `226-runtime.mutation.ai_proposal.proposal_creation抽离方案.md` 固定目标文件、父级 child 声明、handler re-export、`use super::*`、迁移清单、非目标和回退点；当前 `no code movement`，下一步只能进入 BE-001BP-03 实际抽离。
183. R5 mutation handler 域 AI proposal proposal_creation 实际抽离已完成: `227-runtime.mutation.ai_proposal.proposal_creation抽离记录.md` 创建 `src/runtime/mutation/ai_proposal/proposal_creation.rs` 并迁移 `create_runtime_ai_proposal`；下一步只能进入 BE-001BP-04 单叶 closeout。
184. R5 mutation handler 域 AI proposal proposal_creation 单叶 closeout 已完成: `228-runtime.mutation.ai_proposal.proposal_creation单叶closeout.md` 确认本叶等价并设置 `stop_split: true`；下一步只能进入 BE-001BQ-01 `runtime.mutation.ai_proposal` 父叶残余判断。
185. R5 mutation handler 域 AI proposal 父叶残余判断已完成: `229-runtime.mutation.ai_proposal第九轮父叶残余判断.md` 确认九个子叶均已 closeout，并设置父叶 `stop_split: true`；下一步只能进入 BE-001BR-01 `backend.runtime.routes` 父叶残余判断。
186. R5 route aggregate 第二轮父叶残余判断已完成: `230-backend.runtime.routes第二轮父叶残余判断.md` 确认 run/backtest/mutation route child 与当前 handler 回流均已关闭，但父 aggregate 仍直接持有 experiment / evidence / report_ops / event_stream route，因此 `backend.runtime.routes` 保持 `stop_split: false`；下一步只能进入 BE-001BS-01 `backend.runtime.routes.experiment` 单子叶等价基线。
187. R5 route aggregate experiment 单子叶等价基线已建立: `231-backend.runtime.routes.experiment单子叶等价基线.md` 冻结 experiment route group 的 path/method、handler owner、父级委托、状态/persistence 边界、测试证据和非目标；当前 `no code movement`，下一步只能进入 BE-001BS-02 抽离方案。
188. R5 route aggregate experiment 抽离方案已建立: `232-backend.runtime.routes.experiment抽离方案.md` 限定下一批只创建 experiment route child 并迁移五个 route registration，父级委托保持 reports -> experiment -> ops 相对顺序；当前 `no code movement`，下一步只能进入 BE-001BS-03 实际抽离。
189. R5 route aggregate experiment 实际抽离已完成: `233-backend.runtime.routes.experiment抽离记录.md` 创建 `src/backend/runtime/routes/experiment.rs` 并迁移五个 experiment route registration；父级保留 `experiment::register_routes(router)` 委托，下一步只能进入 BE-001BS-04 单叶 closeout。
190. R5 route aggregate experiment 单叶 closeout 已完成: `234-backend.runtime.routes.experiment单叶closeout.md` 确认本 route child 等价并设置 `stop_split: true`；下一步只能进入 BE-001BT-01 `backend.runtime.routes` 父叶残余判断。
191. R5 route aggregate 第三轮父叶残余判断已完成: `235-backend.runtime.routes第三轮父叶残余判断.md` 确认 evidence / report_ops / event_stream 仍为 direct residual，父叶保持 `stop_split: false`；下一步只能进入 BE-001BU-01 `backend.runtime.routes.evidence` 单子叶等价基线。
192. R5 route aggregate evidence 单子叶等价基线已建立: `236-backend.runtime.routes.evidence单子叶等价基线.md` 冻结 evidence health / cleanup route 的 path/method、handler owner、父级委托、状态/持久化读取边界和回归证据；当前 `no code movement`，下一步只能进入 BE-001BU-02 抽离方案。
193. R5 route aggregate evidence 抽离方案已建立: `237-backend.runtime.routes.evidence抽离方案.md` 固定目标 route child、父级委托顺序和允许迁移清单；当前 `no code movement`，下一步只能进入 BE-001BU-03 实际抽离。
194. R5 route aggregate evidence 实际抽离已完成: `238-backend.runtime.routes.evidence抽离记录.md` 创建 `src/backend/runtime/routes/evidence.rs` 并迁移 health/cleanup 两条 route registration；下一步只能进入 BE-001BU-04 单叶 closeout。
195. R5 route aggregate evidence 单叶 closeout 已完成: `239-backend.runtime.routes.evidence单叶closeout.md` 设置 `stop_split: true`；下一步只能进入 BE-001BV-01 `backend.runtime.routes` 父叶残余判断。
196. R5 route aggregate 第四轮父叶残余判断已完成: `240-backend.runtime.routes第四轮父叶残余判断.md` 确认 event_stream / report_ops 仍为 direct residual，父叶保持 `stop_split: false`；下一步只能进入 BE-001BW-01 `backend.runtime.routes.event_stream` 单子叶等价基线。
197. R5 route aggregate event_stream 单子叶等价基线已建立: `241-backend.runtime.routes.event_stream单子叶等价基线.md` 冻结 `/api/runtime/runs/:run_id/events` GET route、`runtime_handlers::stream_run_events` handler owner、SSE frame contract、keepalive contract 与回归证据；当前 `no code movement`，下一步只能进入 BE-001BW-02 抽离方案。
198. R5 route aggregate event_stream 抽离方案已建立: `242-backend.runtime.routes.event_stream抽离方案.md` 固定目标 route child、父级委托顺序、允许迁移清单和回退点；当前 `no code movement`，下一步只能进入 BE-001BW-03 实际抽离。
199. R5 route aggregate event_stream 实际抽离已完成: `243-backend.runtime.routes.event_stream抽离记录.md` 创建 `src/backend/runtime/routes/event_stream.rs` 并迁移一条 event stream route registration；父级保留 `event_stream::register_routes(router)` 委托，下一步只能进入 BE-001BW-04 单叶 closeout。
200. R5 route aggregate event_stream 单叶 closeout 已完成: `244-backend.runtime.routes.event_stream单叶closeout.md` 设置 `stop_split: true`；下一步只能进入 BE-001BX-01 `backend.runtime.routes` 第五轮父叶残余判断。
201. R5 route aggregate 第五轮父叶残余判断已完成: `245-backend.runtime.routes第五轮父叶残余判断.md` 确认 report_ops 为唯一 direct residual，父叶保持 `stop_split: false`；下一步只能进入 BE-001BY-01 `backend.runtime.routes.report_ops` 单子叶等价基线。
202. R5 route aggregate report_ops 单子叶等价基线已建立: `246-backend.runtime.routes.report_ops单子叶等价基线.md` 冻结 runtime reports、merge records、config generations、storage health、ops/audit/research reports 的 path/method、handler owner、状态/持久化边界和回归证据；当前 `no code movement`，下一步只能进入 BE-001BY-02 抽离方案。
203. R5 route aggregate report_ops 抽离方案已建立: `247-backend.runtime.routes.report_ops抽离方案.md` 固定目标 route child、两个父级委托入口、route order、允许迁移清单和回退点；当前 `no code movement`，下一步只能进入 BE-001BY-03 实际抽离。
204. R5 route aggregate report_ops 实际抽离已完成: `248-backend.runtime.routes.report_ops抽离记录.md` 创建 `src/backend/runtime/routes/report_ops.rs` 并迁移十条 report_ops route registration；父级保留两段委托以保持 route order，下一步只能进入 BE-001BY-04 单叶 closeout。
205. R5 route aggregate report_ops 单叶 closeout 已完成: `249-backend.runtime.routes.report_ops单叶closeout.md` 设置 `stop_split: true`；下一步只能进入 BE-001BZ-01 `backend.runtime.routes` 第六轮父叶残余判断。
206. R5 route aggregate 第六轮父叶残余判断已完成: `250-backend.runtime.routes第六轮父叶残余判断.md` 确认七个 route child 均已 closeout，父叶不再直接持有 route registration，并设置 `backend.runtime.routes stop_split: true`；下一步只能进入 BE-001CA-01 `backend.runtime` 父叶残余判断。
207. R5 backend.runtime 父叶残余判断已完成: `251-backend.runtime父叶残余判断.md` 确认 `backend.runtime.routes` 已收口，但 `src/runtime/mod.rs` 仍持有 report/evidence/ops handler 残余，因此 `backend.runtime stop_split: false`；下一步只能进入 BE-001CB-01 `runtime.report_ops` 单子叶等价基线。
208. R5 runtime.report_ops 单子叶等价基线已建立: `252-runtime.report_ops单子叶等价基线.md` 冻结 runtime report / v1 ops report handler 边界、允许迁移清单、排除项和 v1 endpoint 测试缺口；下一步只能进入 BE-001CB-02 抽离方案。
209. R5 runtime.report_ops 抽离方案已建立: `253-runtime.report_ops抽离方案.md` 固定 BE-001CB-03 只创建 `src/runtime/report_ops.rs`，通过 `src/runtime/mod.rs` 受控 re-export 迁移允许清单内 handler；`runtime.evidence_health`、schema、frontend、persistence、storage lifecycle、`AppState` 与 release transition guard 均不得迁移。
210. R5 runtime.report_ops 实际抽离已完成: `254-runtime.report_ops抽离记录.md` 创建 `src/runtime/report_ops.rs` 并迁移十个 public handler 与四个 private helper；`runtime.evidence_health`、schema、frontend、persistence、storage lifecycle、`AppState` 与 release transition guard 均未迁移；下一步只能进入 BE-001CB-04 单叶 closeout。
211. R5 runtime.report_ops 单叶 closeout 已完成: `255-runtime.report_ops单叶closeout.md` 确认第一轮抽离等价成立，但 `runtime.report_ops stop_split: false`；下一步只能进入 BE-001CC-01 `runtime.report_ops.runtime_report` 单子叶等价基线，不能直接创建目标文件、迁移 handler 或处理 v1 ops/report endpoints。
212. R5 runtime.report_ops.runtime_report 单子叶等价基线已建立: `256-runtime.report_ops.runtime_report单子叶等价基线.md` 冻结四个 runtime report public handler 与四个 private helper 边界；当前 `no code movement`，`src/runtime/report_ops/runtime_report.rs` 尚未创建，下一步只能进入 BE-001CC-02 抽离方案。
213. R5 runtime.report_ops.runtime_report 抽离方案已建立: `257-runtime.report_ops.runtime_report抽离方案.md` 固定 BE-001CC-03 只创建 `src/runtime/report_ops/runtime_report.rs`，并通过 `src/runtime/report_ops.rs` 受控 `mod runtime_report` 与 `pub(crate) use runtime_report::{...}` 迁移允许清单内 handler/helper。
214. R5 runtime.report_ops.runtime_report 实际抽离已完成: `258-runtime.report_ops.runtime_report抽离记录.md` 创建 `src/runtime/report_ops/runtime_report.rs` 并迁移四个 public handler 与四个 private helper；`src/runtime/mod.rs`、route facade、v1 ops/report endpoints、`runtime.evidence_health` 与 release transition guard 均未迁移；下一步只能进入 BE-001CC-04 单叶 closeout。
215. R5 runtime.report_ops.runtime_report 单叶 closeout 已完成: `259-runtime.report_ops.runtime_report单叶closeout.md` 确认 runtime report child 等价并设置 `runtime.report_ops.runtime_report stop_split: true`；父级 `runtime.report_ops stop_split: false`，下一步只能进入 BE-001CD-01 父叶残余判断。
216. R5 runtime.report_ops 父叶残余判断已完成: `260-runtime.report_ops父叶残余判断.md` 确认父级仍保留 v1 report endpoints 与 merge/generation/storage health endpoints，因此 `runtime.report_ops stop_split: false`；下一步只能进入 BE-001CE-01 `runtime.report_ops.v1_report_endpoints` 单子叶等价基线。
217. R5 runtime.report_ops.v1_report_endpoints 单子叶等价基线已建立: `261-runtime.report_ops.v1_report_endpoints单子叶等价基线.md` 冻结三个 `/api/v1/reports/*` handler、状态读取面、父级出口和专门测试缺口；当前 `no code movement`，`src/runtime/report_ops/v1_report_endpoints.rs` 尚未创建，下一步只能进入 BE-001CE-02 抽离方案。
218. R5 runtime.report_ops.v1_report_endpoints 抽离方案已建立: `262-runtime.report_ops.v1_report_endpoints抽离方案.md` 选择 test-first；BE-001CE-03 只补 endpoint smoke，不迁移 handler，BE-001CE-04 才允许实际抽离。
219. R5 runtime.report_ops.v1_report_endpoints endpoint smoke 补测已完成: `263-runtime.report_ops.v1_report_endpoints补测记录.md` 新增 `tests/api_v1_reports.rs` 并覆盖三条 `/api/v1/reports/*` 基础 JSON contract；下一步只能进入 BE-001CE-04 实际抽离。
220. R5 runtime.report_ops.v1_report_endpoints 实际抽离已完成: `264-runtime.report_ops.v1_report_endpoints抽离记录.md` 创建 `src/runtime/report_ops/v1_report_endpoints.rs` 并迁移三个 v1 report handler；`src/runtime/mod.rs`、route facade、merge/generation/storage health、`runtime.evidence_health` 与 release transition guard 均未迁移；下一步只能进入 BE-001CE-05 单叶 closeout。
221. R5 runtime.report_ops.v1_report_endpoints 单叶 closeout 已完成: `265-runtime.report_ops.v1_report_endpoints单叶closeout.md` 确认本叶作为 v1 report projection surface 已稳定，设置 `runtime.report_ops.v1_report_endpoints stop_split: true`；父级 `runtime.report_ops stop_split: false`，下一步只能进入 BE-001CF-01 父叶残余判断。
222. R5 runtime.report_ops 父叶残余判断已完成: `266-runtime.report_ops父叶残余判断.md` 确认 `runtime_report` 与 `v1_report_endpoints` 两个 child 均已 closeout，但父级仍保留 `list_merge_records`、`list_config_generations`、`get_storage_health`，因此 `runtime.report_ops stop_split: false`；下一步只能进入 BE-001CG-01 `runtime.report_ops.merge_generation_health` 单子叶等价基线。
223. R5 runtime.report_ops.merge_generation_health 单子叶等价基线已建立: `267-runtime.report_ops.merge_generation_health单子叶等价基线.md` 冻结 `/api/v1/merge/records`、`/api/v1/runtime/generations`、`/api/v1/storage/health` 与 `list_merge_records`、`list_config_generations`、`get_storage_health` 的白箱边界；当前 `no code movement`，planned child 文件尚未创建。
224. R5 runtime.report_ops.merge_generation_health 抽离方案已建立: `268-runtime.report_ops.merge_generation_health抽离方案.md` 选择 test-first；下一步只能进入 BE-001CG-03 endpoint smoke 补测，BE-001CG-04 才允许创建 planned child 并迁移三个 handler。
225. R5 runtime.report_ops.merge_generation_health endpoint smoke 补测已完成: `269-runtime.report_ops.merge_generation_health补测记录.md` 新增 `tests/api_v1_ops_health.rs` 并覆盖 `/api/v1/merge/records`、`/api/v1/runtime/generations`、`/api/v1/storage/health` 最小 JSON contract；下一步只能进入 BE-001CG-04 实际抽离。
226. R5 runtime.report_ops.merge_generation_health 实际抽离已完成: `270-runtime.report_ops.merge_generation_health抽离记录.md` 创建 `src/runtime/report_ops/merge_generation_health.rs` 并迁移 `list_merge_records`、`list_config_generations`、`get_storage_health`；`src/runtime/mod.rs`、route facade、`runtime.evidence_health` 与 release transition guard 均未迁移；下一步只能进入 BE-001CG-05 单叶 closeout。
227. R5 runtime.report_ops.merge_generation_health 单叶 closeout 已完成: `271-runtime.report_ops.merge_generation_health单叶closeout.md` 确认本叶等价并设置 `runtime.report_ops.merge_generation_health stop_split: true`；下一步只能进入 BE-001CH-01 `runtime.report_ops` 父叶残余判断。
228. R5 runtime.report_ops 第二轮父叶残余判断已完成: `272-runtime.report_ops第二轮父叶残余判断.md` 确认三个 report_ops child 均已 closeout，父级仅保留受控 re-export，因此设置 `runtime.report_ops stop_split: true`；下一步只能进入 BE-001CI-01 `backend.runtime` 父叶残余判断。
229. R5 backend.runtime 第二轮父叶残余判断已完成: `273-backend.runtime第二轮父叶残余判断.md` 确认 `backend.runtime.routes` 与 `runtime.report_ops` 已关闭，但 `src/runtime/mod.rs` 仍直接持有 evidence health handler 残余，因此 `backend.runtime stop_split: false`；下一步只能进入 BE-001CJ-01 `runtime.evidence_health` 单子叶等价基线。

因此 backend 当前不能宣称顶层完成。下一步只能进入 BE-001CJ-01 `runtime.evidence_health` 单子叶等价基线；不应混入 request resolution、projection、record_store、run replay/status、replay、artifact schema owner、compare owner、response mapping owner、schema owner、state/persistence owner 或 frontend route 迁移。任何 handler、schema、state、auth/storage 安全实现或测试资产迁移动作，都必须另起提案并回到适配性校验。

---

## 禁止事项

- 不一次性推进一个顶层模块的全部叶子抽离。
- 不在叶子等价基线未完成前继续拆 L3。
- 不把整理、重构、旧实现退役混入抽离批次。
- 不把配置样例、generated schema、构建脚本误判为 runtime 行为真源。
- 不让 AI 主动提出发布版本过渡或横向连接。

---

## 验收标准

1. 递归模块化流程进入 v4.16 里程碑索引。
2. 顶层模块、叶子抽离、叶子整理、细分价值判断、顶层完成、全局根收束都有明确状态。
3. `system` 的下一步能自然落到单叶 closeout。
4. 后续 `backend`、`frontend`、`executor`、`contracts`、`docs` 能复用同一流程。
5. 治理门禁能发现本流程文档缺失。
6. `runtime.run.session_start` 已完成 closeout 并停止内部细分；后续 run handler 递归必须回到 sibling 队列，不能把 record/replay/SSE/state owner 混入本叶。
7. `runtime.run.record_store` 已建立单子叶等价基线；后续若继续只能先做 record store 抽离方案，不能把 replay/status、SSE、state owner 或 persistence owner 迁移混入基线批次。
8. `runtime.run.record_store` 真实边界已校正；后续抽离不得引入 `/discard` route，不得把 `load_run_record_from_state`、`persist_run_record`、`sanitize_storage_path_segment` 等 shared helper 私有化。
9. `runtime.run.record_store` 抽离方案已建立；下一步实际抽离只允许移动四个 handler，并必须保持父级 re-export、route facade、shared helper owner、state owner 和 frontend route 不变。
10. `runtime.run.record_store` 第一轮物理抽离已完成；后续必须先做单叶 closeout，不能直接细拆 persistence/audit/response projection helper。
11. `runtime.run.record_store` 单叶 closeout 已完成；本叶当前停止内部细分，下一步默认进入 `runtime.run.replay_status` 等价基线，不能把 `runtime.event_stream` 混入 run route group。
12. `runtime.run.replay_status` 等价基线已建立；后续若继续只能先做抽离方案，不能直接迁移代码，不能把 SSE、response mapping owner、schema owner、metrics owner、state owner 或 persistence owner 混入。
13. `runtime.run.replay_status` 抽离方案已建立；下一步实际抽离只允许移动 `get_run_replay` 与 `get_run_status`，并必须保持父级 re-export、route facade、SSE、response mapping owner、schema owner、metrics owner、state owner 和 persistence owner 不变。
14. `runtime.run.replay_status` 实际抽离已完成；下一步只能做本叶 closeout，确认两个 handler 等价并判断是否继续细拆，不能顺手迁移 SSE 或 shared owner。
15. `runtime.run.replay_status` 单叶 closeout 已完成；本叶停止内部细分，下一步默认回到父级 `runtime.event_stream` 候选，不能把 SSE 当作 replay_status 的内部子叶。
16. `runtime.event_stream` 等价基线已建立；后续若继续只能先做抽离方案，不能直接迁移代码，不能把 replay/status、record store、state owner、persistence owner 或 frontend caller 混入。
17. `runtime.event_stream` 抽离方案已建立；后续若继续只能做最小物理抽离，不能借机迁移 shared helper、route facade、state owner、persistence owner、frontend caller 或发布过渡连接。
18. `runtime.event_stream` 第一轮抽离已完成；后续若继续只能先做单叶 closeout，不能直接继续拆 shared helper、SSE lifecycle、state owner、persistence owner 或 frontend caller。
19. `runtime.event_stream` 单叶 closeout 已完成；本叶停止内部细分，下一步默认回到父级 `runtime.backtest` 候选，不能把 backtest 当作 event stream 的内部子叶。
20. `runtime.backtest` 等价基线已建立；后续若继续只能先做抽离方案，不能直接迁移代码，不能把 experiment/report/mutation、artifact schema、compare owner、replay helper、state owner、persistence owner 或 frontend caller 混入。
21. `runtime.backtest` 抽离方案已建立；后续若继续只能做 route facade 最小抽离，不能直接切 handler、artifact、compare、persistence、schema、state 或 frontend owner。
22. `runtime.backtest` route facade 抽离已完成；后续只能先做单叶 closeout，不能直接继续拆 handler、artifact、compare、persistence、schema、state 或 frontend owner。
23. `runtime.backtest` 单叶 closeout 已完成；后续默认进入 `runtime.backtest.execution_start` 等价基线，不能直接迁移代码或混入 record store、replay、experiment、artifact schema、compare、persistence、schema、state 或 frontend owner。
24. `runtime.backtest.execution_start` 等价基线已建立；后续若继续只能先做抽离方案，不能直接迁移代码，也不能混入 record store、replay、experiment、artifact schema、compare、persistence、schema、state 或 frontend owner。
25. `runtime.backtest.execution_start` 抽离方案已建立；后续若继续只能进入抽离记录并移动允许清单中的 backtest 创建路径 handler/helper，不能破坏 experiment 复用桥，也不能混入 record store、replay、artifact schema、compare、persistence、schema、state、frontend 或发布过渡。
26. `runtime.backtest.execution_start` 第一轮物理抽离已完成；后续若继续只能做单叶 closeout，不能直接继续细拆 v4 helper、artifact projection、event projection 或迁移 record/replay/experiment/shared owner。
27. `runtime.backtest.execution_start` 单叶 closeout 已完成；后续若继续只能先为 `runtime.backtest.execution_start.v4_projection` 建立等价基线，不能直接移动代码，也不能把 record store、replay、experiment 或共享 owner 混入该子叶。
28. `runtime.backtest.execution_start.v4_projection` 等价基线已建立；后续若继续只能先做抽离方案，不能直接移动代码，也不能把 request resolution、record write、artifact schema、response schema、state owner、persistence owner 或 frontend caller 混入该子叶。
29. `runtime.backtest.execution_start.v4_projection` 抽离方案已建立；后续若继续只能进入抽离记录并移动 projection helper 与现有两个单元测试，不能把 request resolution、record write、artifact schema、response schema、state owner、persistence owner、frontend caller 或发布过渡混入。
30. `runtime.backtest.execution_start.v4_projection` 第一轮物理抽离已完成；后续若继续只能先做单叶 closeout，不能直接继续拆 request resolution、record write、artifact schema、response schema、state owner、persistence owner、frontend caller 或发布过渡。
31. `runtime.backtest.execution_start.v4_projection` 单叶 closeout 已完成并设置 `stop_split: true`；后续若继续只能回到父叶，为 `runtime.backtest.execution_start.v4_request_resolution` 建立等价基线，不能从 projection 子叶继续外扩。
32. `runtime.backtest.execution_start.v4_request_resolution` 等价基线已建立；后续若继续只能先做抽离方案，不能直接移动代码，也不能把 projection、record write、artifact schema、response schema、state owner、persistence owner 或 frontend caller 混入该子叶。
33. `runtime.backtest.execution_start.v4_request_resolution` 抽离方案已建立；后续若继续只能进入抽离记录并移动四个 request resolution helper，不能把 replay/runtime execution、projection、record write、artifact schema、response schema、state owner、persistence owner、frontend caller 或发布过渡混入。
34. `runtime.backtest.execution_start.v4_request_resolution` 第一轮物理抽离已完成；后续若继续只能进入单叶 closeout，不能把 replay/runtime execution、projection、record write、artifact schema、response schema、state owner、persistence owner、frontend caller 或发布过渡混入。
35. `runtime.backtest.execution_start.v4_request_resolution` 单叶 closeout 已完成并设置 `stop_split: true`；后续若继续只能回到父叶，为 `runtime.backtest.execution_start.v4_runtime_execution` 建立等价基线，不能从 request resolution 子叶继续外扩。
36. `runtime.backtest.execution_start.v4_runtime_execution` 等价基线已建立；后续若继续只能先做抽离方案，不能直接移动代码，也不能把 request resolution、projection、record write、artifact schema、response schema、state owner、persistence owner 或 frontend caller 混入该子叶。
37. `runtime.backtest.execution_start.v4_runtime_execution` 抽离方案已建立；后续若继续只能进入抽离记录，移动 deterministic runtime execution 最小 helper，不能把 request resolution、projection、record write、artifact schema、response schema、state owner、persistence owner 或 frontend caller 混入该子叶。
38. `runtime.backtest.execution_start.v4_runtime_execution` 第一轮物理抽离已完成；后续若继续只能进入单叶 closeout，判断该 helper 等价和是否值得继续细拆，不能把 expanded graph、request resolution、projection、record write、artifact schema、response schema、state owner、persistence owner 或 frontend caller 混入该子叶。
39. `runtime.backtest.execution_start.v4_runtime_execution` 单叶 closeout 已完成并设置 `stop_split: true`；后续若继续只能回到父叶另起候选基线，不能从 runtime execution 子叶继续细拆或外扩。
40. `runtime.backtest.execution_start.legacy_dispatch` 等价基线已建立；后续若继续只能先做 BE-001R-02 抽离方案，不能直接移动 legacy compile/sandbox dispatch 代码，也不能混入 record write、persistence、state owner 或 frontend caller。
41. `runtime.backtest.execution_start.legacy_dispatch` 抽离方案已建立；后续若继续只能进入 BE-001R-03 最小物理抽离，不能顺手迁移 record assembly、artifact views、transient spill、state write、audit log 或发布过渡连接。
42. `runtime.backtest.execution_start.legacy_dispatch` 第一轮物理抽离已完成；后续若继续只能进入 BE-001R-04 单叶 closeout，不能把 record assembly、artifact views、transient spill、state write、audit log、schema、persistence、frontend caller 或发布过渡混入该子叶。
43. `runtime.backtest.execution_start.legacy_dispatch` 单叶 closeout 已完成并设置 `stop_split: true`；后续若继续只能回到父叶残余判断或上层 `runtime.backtest` 队列，不能从 legacy dispatch 子叶继续细拆或外扩。
44. `runtime.backtest.execution_start` 父叶残余判断已完成；后续若继续只能回到上层 `runtime.backtest` 队列，为 `runtime.backtest.record_store` 建立等价基线，不能在 `execution_start` 内私拆 record finalize、state write、persistence、schema 或 frontend caller。
45. `runtime.backtest.record_store` 等价基线已建立；后续若继续只能先做 BE-001T-02 抽离方案，不能直接移动代码，也不能混入 replay、experiment、compare、artifact schema owner、state owner、persistence owner 或 frontend caller。
46. `runtime.backtest.record_store` 抽离方案已建立；后续若继续只能进入 BE-001T-03 最小物理抽离，移动四个 handler 并保持父级 re-export、route facade、shared helper owner、state owner、persistence owner、artifact/transient owner、frontend route 和发布过渡边界不变。
47. `runtime.backtest.record_store` 第一轮物理抽离已完成；后续若继续只能进入单叶 closeout，确认四个 handler 等价并判断是否继续细拆，不能顺手迁移 shared helper、persistence、audit、artifact/transient、response projection、frontend route 或发布过渡连接。
48. `runtime.backtest.record_store` 单叶 closeout 已完成并设置 `stop_split: true`；后续若继续只能回到 `runtime.backtest` sibling 队列，为 `runtime.backtest.replay` 建立单子叶等价基线，不能继续细拆 record_store 或迁移 shared helper、persistence、audit、artifact/transient、response mapping、frontend caller 或发布过渡连接。
49. `runtime.backtest.replay` 单子叶等价基线已建立；后续若继续只能先做 BE-001U-02 抽离方案，不能直接移动 `get_backtest_replay`，也不能迁移 record_store、execution_start、experiment_sweep、compare、response mapping、schema、state/persistence、artifact schema、frontend caller 或发布过渡连接。
50. `runtime.backtest.replay` 抽离方案已建立；后续若继续只能进入 BE-001U-03 实际抽离记录，第一轮只允许迁移 `get_backtest_replay`，不能迁移 query normalization、response mapping、schema、metrics、record lookup、state/persistence、artifact schema、frontend caller 或发布过渡连接。
51. `runtime.backtest.replay` 第一轮物理抽离已完成；后续若继续只能进入单叶 closeout，确认 `get_backtest_replay` 等价并判断是否继续细拆，不能顺手迁移 query normalization、response mapping、schema、metrics、record lookup、state/persistence、artifact schema、frontend caller 或发布过渡连接。
52. `runtime.backtest.replay` 单叶 closeout 已完成并设置 `stop_split: true`；后续若继续只能回到 `runtime.backtest` sibling 队列，为 `runtime.backtest.experiment_sweep` 建立单子叶等价基线，不能继续细拆 replay 或迁移 query normalization、response mapping、schema、metrics、record lookup、state/persistence、artifact schema、frontend caller 或发布过渡连接。
53. `runtime.backtest.experiment_sweep` 单子叶等价基线已建立；后续若继续只能先做 BE-001V-02 抽离方案，不能直接移动 experiment handler/helper，也不能迁移 route facade、execution_start、record_store、replay、compare、schema、state/persistence、response mapping、frontend caller 或发布过渡连接。
54. `runtime.backtest.experiment_sweep` 抽离方案已建立；后续若继续只能进入 BE-001V-03 实际抽离记录，第一轮只允许迁移 5 个 experiment handler 和 3 个参数网格 helper，不能迁移 route aggregate、execution_start、persistence、response mapping、schema、state、audit、frontend caller 或发布过渡连接。
55. `runtime.backtest.experiment_sweep` 第一轮物理抽离已完成；后续若继续只能进入 BE-001V-04 单叶 closeout，不能删除 drained parent include、迁移 route aggregate、execution_start、persistence、response mapping、schema、state、audit、frontend caller 或发布过渡连接。
56. `runtime.backtest.experiment_sweep` 单叶 closeout 已完成；本叶 `stop_split: false`，后续若继续只能进入 BE-001W-01 `runtime.backtest.experiment_sweep.parameter_grid` 单子叶等价基线，不能直接移动 helper、删除 drained parent include 或迁移 shared owner。
57. `runtime.backtest.experiment_sweep.parameter_grid` 单子叶等价基线已建立；当前 `no code movement`，后续若继续只能进入 BE-001W-02 抽离方案，不能直接移动 helper、新增子文件、修改 schema、改 `MAX_EXPERIMENT_VARIANTS` 或宣称 closeout。
58. `runtime.backtest.experiment_sweep.parameter_grid` 抽离方案已建立；当前 `no code movement`，后续若继续只能进入 BE-001W-03 实际抽离记录，第一轮只允许迁移 3 个 helper 到父级私有子模块，不能迁移 route、handler orchestration、execution_start、persistence、mapping、schema、state、audit、frontend caller 或发布过渡。
59. `runtime.backtest.experiment_sweep.parameter_grid` 第一轮物理抽离已完成；后续若继续只能进入 BE-001W-04 单叶 closeout，不能继续细拆 axis normalization、variant expansion、schema、route、state/persistence、frontend caller 或发布过渡连接。
60. `runtime.backtest.experiment_sweep.parameter_grid` 单叶 closeout 已完成；本叶 `stop_split: true`，后续若继续只能进入 BE-001X-01 `runtime.backtest.experiment_sweep` 父叶残余判断，不能从 parameter_grid 继续细拆或宣称父叶最终完成。
61. `runtime.backtest.experiment_sweep` 父叶残余判断已完成；父叶仍 `stop_split: false`，后续若继续只能进入 BE-001Y-01 `runtime.backtest.experiment_sweep.start_orchestration` 单子叶等价基线，不能直接移动 start handler、record lifecycle、route、schema、state/persistence、frontend caller 或发布过渡连接。
62. `runtime.backtest.experiment_sweep.start_orchestration` 单子叶等价基线已建立；当前 `no code movement`，后续若继续只能进入 BE-001Y-02 抽离方案，不能直接移动 start handler、混入 record lifecycle、迁移 route/schema/state/persistence/frontend caller 或启动发布过渡连接。
63. `runtime.backtest.experiment_sweep.start_orchestration` 抽离方案已建立；当前 `no code movement`，后续若继续只能进入 BE-001Y-03 实际抽离记录，第一轮只允许迁移 `start_backtest_experiment`，不能混入 record lifecycle、route/schema/state/persistence/response mapping/audit/frontend caller 或发布过渡连接。
64. `runtime.backtest.experiment_sweep.start_orchestration` 实际抽离已完成；`start_backtest_experiment` 已迁入 `src/runtime/backtest/start_orchestration.rs`，后续若继续只能进入 BE-001Y-04 单叶 closeout，不能混入 record lifecycle、route/schema/state/persistence/response mapping/audit/frontend caller 或发布过渡连接。
65. `runtime.backtest.experiment_sweep.start_orchestration` 单叶 closeout 已完成；本叶 `stop_split: true`，该回流已由 BE-001Z-01 父叶残余判断承接，不能从 start_orchestration 继续细拆或直接移动 record lifecycle、route/schema/state/persistence/response mapping/audit/frontend caller 或发布过渡连接。
66. `runtime.backtest.experiment_sweep` 第二轮父叶残余判断已完成；父叶仍 `stop_split: false`，后续若继续只能进入 BE-001AA-01 `runtime.backtest.experiment_sweep.record_lifecycle` 单子叶等价基线，不能直接移动 list/detail/save/discard、route/schema/state/persistence/response mapping/audit/frontend caller 或发布过渡连接。
67. `runtime.backtest.experiment_sweep.record_lifecycle` 单子叶等价基线已建立；当前 `no code movement`，后续若继续只能进入 BE-001AA-02 抽离方案，不能直接移动代码、混入 parameter_grid/start_orchestration、迁移 route/schema/state/persistence/response mapping/audit/frontend caller 或发布过渡连接。
68. `runtime.backtest.experiment_sweep.record_lifecycle` 抽离方案已建立；当前 `no code movement`，后续若继续只能进入 BE-001AA-03 实际抽离记录，不能混入 route/schema/state/persistence/response mapping/audit/frontend caller 或发布过渡连接。
69. `runtime.backtest.experiment_sweep.record_lifecycle` 实际抽离已完成；后续若继续只能进入 BE-001AA-04 单叶 closeout，不能继续细拆 save/discard、迁移 route/schema/state/persistence/response mapping/audit/frontend caller 或发布过渡连接。
70. `runtime.backtest.experiment_sweep.record_lifecycle` 单叶 closeout 已完成并设置 `stop_split: true`；后续若继续只能回到父级进入 BE-001AB-01 第三轮父叶残余判断，不能从 record_lifecycle 继续细拆或迁移 route/schema/state/persistence/response mapping/audit/frontend caller 或发布过渡连接。
71. `runtime.backtest.experiment_sweep` 第三轮父叶残余判断已完成并设置父叶 `stop_split: true`；后续若继续只能回到 `runtime.backtest` 上层队列进入 BE-001AC-01，不能宣称 `runtime.backtest` 顶层已完成，也不能迁移 route/schema/state/persistence/response mapping/frontend caller 或发布过渡连接。
72. `runtime.backtest` 父叶残余判断已完成并设置父叶 `stop_split: true`；后续若继续只能回到 `backend.runtime.routes` 上层队列进入 BE-001AD-01，不能删除 drained parent include、迁移 compare/artifact schema/persistence/response mapping/frontend caller 或发布过渡连接。
73. `backend.runtime.routes` 父叶残余判断已完成但父叶保持 `stop_split: false`；BE-001AE-01 已建立 `backend.runtime.routes.mutation` 单子叶等价基线，不能宣称父叶完成、mutation route 已抽离、handler/AppState/锁顺序/schema/frontend caller 已迁移或发布过渡已启动。
74. `backend.runtime.routes.mutation` 单子叶等价基线已建立；BE-001AE-02 已建立抽离方案，不能宣称 route facade 已创建、handler/AppState/锁顺序/schema/frontend caller 已迁移或发布过渡已启动。
75. `backend.runtime.routes.mutation` 抽离方案已建立；后续若继续只能进入 BE-001AE-03 route facade 最小物理抽离，不能直接移动 handler、AppState、锁顺序、schema、frontend caller、report/evidence/experiment/ops 或发布过渡连接。
76. `backend.runtime.routes.mutation` route facade 最小物理抽离已完成；后续若继续只能进入 BE-001AE-04 单叶 closeout，不能宣称 mutation handler、AppState、锁顺序、schema、frontend caller、report/evidence/experiment/ops、发布过渡连接或整理/重构已经完成。
77. `backend.runtime.routes.mutation` 单叶 closeout 已完成并设置 route facade `stop_split: true`；后续若继续只能进入 BE-001AF-01 `runtime.mutation.parameter_mutation` 单子叶等价基线，不能直接移动 parameter mutation handler、AI proposal、approval review、shared persistence/governance helper、AppState、锁顺序、schema、frontend caller 或发布过渡连接。
78. `runtime.mutation.parameter_mutation` 单子叶等价基线已建立；后续若继续只能进入 BE-001AF-02 抽离方案，不能直接移动 create/list/detail/activate/rollback handler、auto snapshot side effect、run record append、AI proposal、approval review、shared persistence/governance helper、AppState、schema、frontend caller 或发布过渡连接。
79. `runtime.mutation.parameter_mutation` 抽离方案已建立；后续若继续只能进入 BE-001AF-03 实际抽离，并且只能迁移方案列明的五个 public handler 与本叶私有 helper，不能移动 AI proposal、approval review、shared event append/governance helper、AppState、schema、frontend caller 或发布过渡连接。
80. `runtime.mutation.parameter_mutation` 实际抽离已完成；后续若继续只能进入 BE-001AF-04 单叶整理 / closeout，不能直接继续拆 activate/rollback、safe window、auto snapshot 或 shared helper，除非 closeout 判定值得细拆并重新建立单子叶等价基线。
81. `runtime.mutation.parameter_mutation` 单叶 closeout 已完成且 `stop_split: false`；后续若继续只能进入 BE-001AG-01 `runtime.mutation.parameter_mutation.transition_lifecycle` 单子叶等价基线，不能直接移动 activation / rollback lifecycle、safe window、auto snapshot 或 shared helper。
82. `runtime.mutation.parameter_mutation.transition_lifecycle` 单子叶等价基线已建立；后续若继续只能进入 BE-001AG-02 抽离方案，不能直接创建目标文件或移动代码。
83. `runtime.mutation.parameter_mutation.transition_lifecycle` 抽离方案已建立；后续若继续只能进入 BE-001AG-03 实际抽离，不能混入 proposal create/list/detail、AI proposal、approval review、AppState、schema、frontend caller 或发布过渡连接。
84. `runtime.mutation.parameter_mutation.transition_lifecycle` 实际抽离已完成；后续若继续只能进入 BE-001AG-04 单叶 closeout，不能直接宣称本叶停止细拆或父叶完成。
85. `runtime.mutation.parameter_mutation.transition_lifecycle` 单叶 closeout 已完成并设置 `stop_split: false`；后续若继续只能进入 BE-001AH-01 `boundary_safety` 单子叶等价基线，不能直接创建 `boundary_safety.rs` 或迁移 activation/rollback。
86. `runtime.mutation.parameter_mutation.transition_lifecycle.boundary_safety` 单子叶等价基线已建立；后续若继续只能进入 BE-001AH-02 抽离方案，不能直接创建 `boundary_safety.rs` 或改变 reason code 优先级。
87. `runtime.mutation.parameter_mutation.transition_lifecycle.boundary_safety` 抽离方案已建立；后续若继续只能进入 BE-001AH-03 实际抽离，不能混入 activation/rollback handler 或发布过渡连接。
88. `runtime.mutation.parameter_mutation.transition_lifecycle.boundary_safety` 实际抽离已完成；后续若继续只能进入 BE-001AH-04 单叶 closeout，不能混入 activation/rollback handler、AI proposal/approval、schema/frontend caller 或发布过渡连接。
89. `runtime.mutation.parameter_mutation.transition_lifecycle.boundary_safety` 单叶 closeout 已完成并设置 `stop_split: true`；后续只能回到 BE-001AI-01 父叶残余判断，不能继续拆 boundary_safety 或直接迁移 activation/rollback。
90. `runtime.mutation.parameter_mutation.transition_lifecycle` 父叶残余判断已完成；父叶仍为 `stop_split: false`，后续只能进入 BE-001AJ-01 `activation_flow` 单子叶等价基线，不能直接创建 `activation_flow.rs`、迁移 activation/rollback handler 或启动发布过渡。
91. `runtime.mutation.parameter_mutation.transition_lifecycle.activation_flow` 单子叶等价基线已建立；后续只能进入 BE-001AJ-02 抽离方案，不能直接创建目标文件、迁移 activation handler、迁移 rollback flow、迁移 snapshot helper body 或启动发布过渡。
92. `runtime.mutation.parameter_mutation.transition_lifecycle.activation_flow` 抽离方案已建立；后续只能进入 BE-001AJ-03 实际抽离，且只允许迁移 `activate_runtime_parameter_mutation`，不能迁移 rollback flow、snapshot helper body、route facade、schema/frontend caller 或启动发布过渡。
93. `runtime.mutation.parameter_mutation.transition_lifecycle.activation_flow` 实际抽离已完成；后续只能进入 BE-001AJ-04 单叶 closeout，不能迁移 rollback flow、snapshot helper body、route facade、schema/frontend caller、AI proposal、approval review、AppState 或启动发布过渡。
94. `runtime.mutation.parameter_mutation.transition_lifecycle.activation_flow` 单叶 closeout 已完成并设置 `stop_split: true`；后续只能回到 BE-001AK-01 父叶残余判断，不能继续细拆 activation_flow 或直接迁移 rollback flow/snapshot helper body。
95. `runtime.mutation.parameter_mutation.transition_lifecycle` 第二轮父叶残余判断已完成；父叶仍为 `stop_split: false`，后续只能进入 BE-001AL-01 `rollback_flow` 单子叶等价基线，不能直接创建 `rollback_flow.rs` 或迁移 rollback handler。
96. `runtime.mutation.parameter_mutation.transition_lifecycle.rollback_flow` 单子叶等价基线已建立；后续只能进入 BE-001AL-02 抽离方案，不能直接创建 `rollback_flow.rs`、迁移 rollback handler、迁移 snapshot helper body 或启动发布过渡。
97. `runtime.mutation.parameter_mutation.transition_lifecycle.rollback_flow` 抽离方案已建立；后续只能进入 BE-001AL-03 实际抽离，且只允许迁移 `rollback_runtime_parameter_mutation`，不能迁移 activation flow、snapshot helper body、route facade、schema/frontend caller 或启动发布过渡。
98. `runtime.mutation.parameter_mutation.transition_lifecycle.rollback_flow` 实际抽离已完成；后续只能进入 BE-001AL-04 单叶 closeout，不能迁移 rollback id helper、transition persistence、snapshot helper body、route facade、schema/frontend caller 或启动发布过渡。
99. `runtime.mutation.parameter_mutation.transition_lifecycle.rollback_flow` 单叶 closeout 已完成；后续只能进入 BE-001AM-01 父叶残余判断，不能继续拆 rollback_flow、迁移 rollback helper、snapshot helper body、route facade、schema/frontend caller 或启动发布过渡。
100. `runtime.mutation.parameter_mutation.transition_lifecycle` 第三轮父叶残余判断已完成；后续只能进入 BE-001AN-01 `activation_snapshot_side_effect` 单子叶等价基线，不能直接创建 side effect 文件、迁移 snapshot helper body、迁移 shared helper 或启动发布过渡。
101. `runtime.mutation.parameter_mutation.transition_lifecycle.activation_snapshot_side_effect` 单子叶等价基线已建立；后续只能进入 BE-001AN-02 抽离方案，不能直接创建目标文件、移动 snapshot helper body、迁移 shared helper 或启动发布过渡。
102. `runtime.mutation.parameter_mutation.transition_lifecycle.activation_snapshot_side_effect` 抽离方案已建立；后续只能进入 BE-001AN-03 实际抽离，并且只能迁移 `auto_snapshot_on_activation`，不能迁移 activation/rollback、shared lifecycle/persistence helper、AppState、schema、frontend caller 或启动发布过渡。
103. `runtime.mutation.parameter_mutation.transition_lifecycle.activation_snapshot_side_effect` 实际抽离已完成；后续只能进入 BE-001AN-04 单叶 closeout，不能迁移 shared lifecycle/persistence helper、AppState、schema、frontend caller 或启动发布过渡。
104. `runtime.mutation.parameter_mutation.transition_lifecycle.activation_snapshot_side_effect` 单叶 closeout 已完成并设置 `stop_split: true`；后续只能回到 BE-001AO-01 父叶残余判断，不能继续拆 activation_snapshot_side_effect 或迁移 shared helper。
105. `runtime.mutation.parameter_mutation.transition_lifecycle` 第四轮父叶残余判断已完成；后续只能进入 BE-001AP-01 `transition_record_persistence` 单子叶等价基线，不能直接移动 shared lifecycle/persistence helper、rollback id、AppState、schema、frontend caller 或启动发布过渡。
106. `runtime.mutation.parameter_mutation.transition_lifecycle.transition_record_persistence` 单子叶等价基线已建立；后续只能进入 BE-001AP-02 抽离方案，不能直接创建目标文件、迁移 rollback id、改变 AppState/schema/frontend caller 或启动发布过渡。
107. `runtime.mutation.parameter_mutation.transition_lifecycle.transition_record_persistence` 抽离方案已建立；后续只能进入 BE-001AP-03 实际抽离，不能迁移 rollback id、activation/rollback handler、boundary/snapshot helper、AppState、schema、frontend caller 或启动发布过渡。
108. `runtime.mutation.parameter_mutation.transition_lifecycle.transition_record_persistence` 实际抽离已完成；后续只能进入 BE-001AP-04 单叶 closeout，不能迁移 rollback id、activation/rollback handler、boundary/snapshot helper、AppState、schema、frontend caller 或启动发布过渡。
109. `runtime.mutation.parameter_mutation.transition_lifecycle.transition_record_persistence` 单叶 closeout 已完成并设置 `stop_split: true`；后续只能进入 BE-001AQ-01 `transition_lifecycle` 第五轮父叶残余判断，不能继续拆 transition_record_persistence、迁移 rollback id、改变 AppState/schema/frontend caller 或启动发布过渡。
110. `runtime.mutation.parameter_mutation.transition_lifecycle` 第五轮父叶残余判断已完成；后续只能进入 BE-001AR-01 `rollback_record_identity` 单子叶等价基线，不能直接创建目标文件、回改 closed child、改变 AppState/schema/frontend caller 或启动发布过渡。
111. `runtime.mutation.parameter_mutation.transition_lifecycle.rollback_record_identity` 单子叶等价基线已建立；后续只能进入 BE-001AR-02 抽离方案，不能直接创建目标文件、移动 rollback id helper、回改 rollback_flow、改变 AppState/schema/frontend caller 或启动发布过渡。
112. `runtime.mutation.parameter_mutation.transition_lifecycle.rollback_record_identity` 抽离方案已建立；后续只能进入 BE-001AR-03 实际抽离，不能迁移其他 helper、回改 rollback_flow、改变 AppState/schema/frontend caller 或启动发布过渡。
113. `runtime.mutation.parameter_mutation.transition_lifecycle.rollback_record_identity` 实际抽离已完成；后续只能进入 BE-001AR-04 单叶 closeout，不能回改 rollback_flow、继续拆其它 sibling、改变 AppState/schema/frontend caller 或启动发布过渡。
114. `runtime.mutation.parameter_mutation.transition_lifecycle.rollback_record_identity` 单叶 closeout 已完成；后续只能进入 BE-001AS-01 父叶残余判断，不能继续拆本叶、移动代码、改变 AppState/schema/frontend caller 或启动发布过渡。
115. `runtime.mutation.parameter_mutation.transition_lifecycle` 第六轮父叶残余判断已完成；后续只能进入 BE-001AT-01 `runtime.mutation.parameter_mutation` 父叶残余判断，不能继续拆 closed child、移动代码、改变 AppState/schema/frontend caller 或启动发布过渡。
116. `runtime.mutation.parameter_mutation` 父叶残余判断已完成；后续只能进入 BE-001AU-01 `proposal_creation` 单子叶等价基线，不能直接创建目标文件、移动 create handler、改变 AppState/schema/frontend caller 或启动发布过渡。
117. `runtime.mutation.parameter_mutation.proposal_creation` 单子叶等价基线已建立；后续只能进入 BE-001AU-02 抽离方案，不能直接创建目标文件、移动 create handler、迁移 list/detail、改变 AppState/schema/frontend caller 或启动发布过渡。
118. `runtime.mutation.parameter_mutation.proposal_creation` 抽离方案已建立；后续只能进入 BE-001AU-03 实际抽离，不能迁移 list/detail、回改 transition_lifecycle、改变 AppState/schema/frontend caller 或启动发布过渡。
119. `runtime.mutation.parameter_mutation.proposal_creation` 实际抽离已完成；后续只能进入 BE-001AU-04 单叶 closeout，不能迁移 list/detail、继续细拆未判定分支、改变 AppState/schema/frontend caller 或启动发布过渡。
120. `runtime.mutation.parameter_mutation.proposal_creation` 单叶 closeout 已完成；后续只能进入 BE-001AV-01 父叶残余判断，不能继续细拆 proposal_creation、迁移 list/detail、改变 AppState/schema/frontend caller 或启动发布过渡。
121. `runtime.mutation.parameter_mutation` 第二轮父叶残余判断已完成；后续只能进入 BE-001AW-01 `record_query` 单子叶等价基线，不能直接创建目标文件、移动 list/detail、改变 AppState/schema/frontend caller 或启动发布过渡。
122. `runtime.mutation.parameter_mutation.record_query` 单子叶等价基线已建立；后续只能进入 BE-001AW-02 抽离方案，不能直接创建目标文件、移动 list/detail、改变 AppState/schema/frontend caller 或启动发布过渡。
123. `runtime.mutation.parameter_mutation.record_query` 抽离方案已建立；后续只能进入 BE-001AW-03 实际抽离，且只允许创建计划 child 并迁移 list/detail handler，不能混入 create/activation/rollback、AI proposal、approval、AppState/schema/frontend caller 或发布过渡连接。
124. `runtime.mutation.parameter_mutation.record_query` 实际抽离已完成；后续只能进入 BE-001AW-04 单叶 closeout，不能继续细拆或混入 create/activation/rollback、AI proposal、approval、AppState/schema/frontend caller 或发布过渡连接。
125. `runtime.mutation.parameter_mutation.record_query` 单叶 closeout 已完成并设置 `stop_split: true`；后续只能进入 BE-001AX-01 父叶残余判断，不能从 record_query 继续细拆或启动发布过渡。
126. `runtime.mutation.parameter_mutation` 第三轮父叶残余判断已完成并设置 `stop_split: true`；后续只能回到 mutation sibling 队列进入 BE-001AY-01 `runtime.mutation.ai_proposal` 单子叶等价基线，不能继续拆 parameter_mutation 或启动发布过渡。
127. `runtime.mutation.ai_proposal` 单子叶等价基线已建立；后续只能进入 BE-001AY-02 抽离方案，不能直接创建目标文件、移动 handler/helper、重排 `approval_records -> ai_proposals` 锁顺序、迁移 AppState/schema/frontend caller 或启动发布过渡。
128. `runtime.mutation.ai_proposal` 抽离方案已建立；后续只能进入 BE-001AY-03 实际抽离，且只允许创建计划 child、迁移清单内 handler/helper 和 `FutureExt` import，不能迁移 shared helper、回改 parameter mutation、改变 AppState/schema/frontend caller 或启动发布过渡。
129. `runtime.mutation.ai_proposal` 实际抽离已完成；后续只能进入 BE-001AY-04 单叶 closeout，不能继续细拆 static_check、record_query、approval_review、status_transition、approval_persistence 或 sandbox_trigger，不能改变 AppState/schema/frontend caller 或启动发布过渡。
130. `runtime.mutation.ai_proposal` 单叶 closeout 已完成并设置 `stop_split: false`；后续只能进入 BE-001AZ-01 `runtime.mutation.ai_proposal.static_check` 单子叶等价基线，不能直接创建 static_check 目标文件、移动 helper、改变 approval review/AppState/schema/frontend caller 或启动发布过渡。
131. `runtime.mutation.ai_proposal.static_check` 单子叶等价基线已建立；后续只能进入 BE-001AZ-02 抽离方案，不能直接创建目标文件、移动 helper、改变 approval review/AppState/schema/frontend caller 或启动发布过渡。
132. `runtime.mutation.ai_proposal.static_check` 抽离方案已建立；后续只能进入 BE-001AZ-03 实际抽离，且只允许创建计划 child、迁移清单内 helper 和静态检查单测，不能迁移 approval gate、create handler、AppState/schema/frontend caller 或启动发布过渡。
133. `runtime.mutation.ai_proposal.static_check` 实际抽离已完成；后续只能进入 BE-001AZ-04 单叶 closeout，确认本 child 是否继续细拆，不能迁移 approval gate、record query、AppState/schema/frontend caller 或启动发布过渡。
134. `runtime.mutation.ai_proposal.static_check` 单叶 closeout 已完成并设置 `stop_split: true`；后续只能进入 BE-001BA-01 `runtime.mutation.ai_proposal` 父叶残余判断，不能继续细拆 static_check 或启动发布过渡。
135. `runtime.mutation.ai_proposal` 父叶残余判断已完成；后续只能进入 BE-001BB-01 `runtime.mutation.ai_proposal.source_governance_identity` 单子叶等价基线，不能直接创建 child 文件、移动 helper、迁移 approval review 或启动发布过渡。
136. `runtime.mutation.ai_proposal.source_governance_identity` 单子叶等价基线已建立；后续只能进入 BE-001BB-02 抽离方案，不能直接创建目标文件、移动 helper、迁移 event lifecycle / approval review 或启动发布过渡。
137. `runtime.mutation.ai_proposal.source_governance_identity` 抽离方案已建立；后续只能进入 BE-001BB-03 实际抽离，且只允许创建计划 child 并迁移 source context、governance projection 与 record identity helper，不能迁移 create handler、event lifecycle、approval review、record query、AppState/schema/frontend caller 或启动发布过渡。
138. `runtime.mutation.ai_proposal.source_governance_identity` 实际抽离已完成；后续只能进入 BE-001BB-04 单叶 closeout，不能直接继续细拆 source loader、governance projection、record id digest，也不能迁移 create handler、event lifecycle、approval review、record query、AppState/schema/frontend caller 或启动发布过渡。
139. `runtime.mutation.ai_proposal.source_governance_identity` 单叶 closeout 已完成并设置 `stop_split: true`；后续只能进入 BE-001BC-01 `runtime.mutation.ai_proposal` 父叶残余判断，不能继续细拆 source_governance_identity 或启动发布过渡。
140. `runtime.mutation.ai_proposal` 第二轮父叶残余判断已完成；后续只能进入 BE-001BD-01 `runtime.mutation.ai_proposal.event_lifecycle` 单子叶等价基线，不能直接创建 event_lifecycle 文件、移动 helper、迁移 approval review / record query 或启动发布过渡。
141. `runtime.mutation.ai_proposal.event_lifecycle` 单子叶等价基线已建立；后续只能进入 BE-001BD-02 抽离方案，不能直接创建目标文件、移动 helper、迁移 approval review / record query 或启动发布过渡。
142. `runtime.mutation.ai_proposal.event_lifecycle` 抽离方案已建立；后续只能进入 BE-001BD-03 实际抽离，且只允许创建计划 child 并迁移 event/lifecycle helper，不能迁移 record_query、approval_review、approval_persistence、sandbox_trigger、AppState/schema/frontend caller 或启动发布过渡。
143. `runtime.mutation.ai_proposal.event_lifecycle` 实际抽离已完成；后续只能进入 BE-001BD-04 单叶 closeout，不能继续迁移 record_query、approval_review、approval_persistence、sandbox_trigger、status_transition、AppState/schema/frontend caller 或启动发布过渡。
144. `runtime.mutation.ai_proposal.event_lifecycle` 单叶 closeout 已完成并设置 `stop_split: true`；后续只能进入 BE-001BE-01 父叶残余判断，不能继续细拆 event_lifecycle 或启动发布过渡。
145. `runtime.mutation.ai_proposal` 第三轮父叶残余判断已完成；后续只能进入 BE-001BF-01 `runtime.mutation.ai_proposal.record_query` 单子叶等价基线，不能直接创建 record_query 文件、迁移 handler 或启动发布过渡。
146. `runtime.mutation.ai_proposal.record_query` 单子叶等价基线已建立；后续只能进入 BE-001BF-02 抽离方案，不能直接创建 record_query 文件、迁移 handler、迁移 approval review 或启动发布过渡。
147. `runtime.mutation.ai_proposal.record_query` 抽离方案已建立；后续只能进入 BE-001BF-03 实际抽离，且只能创建 record_query 目标文件并迁移 list/detail/read-through loader，不能迁移 approval review、status transition、sandbox trigger、AppState/schema/frontend caller 或启动发布过渡。
148. `runtime.mutation.ai_proposal.record_query` 实际抽离已完成；后续只能进入 BE-001BF-04 单叶 closeout，不能继续迁移 approval review、status transition、sandbox trigger、AppState/schema/frontend caller 或启动发布过渡。
149. `runtime.mutation.ai_proposal.record_query` 单叶 closeout 已完成并设置 `stop_split: true`；后续只能进入 BE-001BG-01 `runtime.mutation.ai_proposal` 父叶残余判断，不能继续细拆 record_query 或启动发布过渡。
150. `runtime.mutation.ai_proposal` 第四轮父叶残余判断已完成；后续只能进入 BE-001BH-01 `runtime.mutation.ai_proposal.approval_review` 单子叶等价基线，不能直接创建 approval_review 文件、迁移 approval handler、迁移 approval persistence、迁移 sandbox trigger、改变 AppState/schema/frontend caller 或启动发布过渡。
151. `runtime.mutation.ai_proposal.approval_review` 单子叶等价基线已建立；后续只能进入 BE-001BH-02 抽离方案，不能直接创建 approval_review 文件、迁移 handler、迁移 approval persistence/sandbox trigger/status transition、改变 AppState/schema/frontend caller 或启动发布过渡。
152. `runtime.mutation.ai_proposal.approval_review` 抽离方案已建立；后续只能进入 BE-001BH-03 实际抽离，且只能创建计划 child 并迁移五个 approval handler，不能迁移 approval persistence、sandbox trigger、status transition、AppState/schema/frontend caller 或启动发布过渡。
153. `runtime.mutation.ai_proposal.approval_review` 实际抽离已完成；后续只能进入 BE-001BH-04 单叶 closeout，不能继续迁移 approval persistence、sandbox trigger、status transition、AppState/schema/frontend caller 或启动发布过渡。
154. `runtime.mutation.ai_proposal.approval_review` 单叶 closeout 已完成并设置 `stop_split: true`；后续只能进入 BE-001BI-01 父叶残余判断，不能继续细拆 approval_review 或启动发布过渡。
155. `runtime.mutation.ai_proposal` 第五轮父叶残余判断已完成；后续只能进入 BE-001BJ-01 `approval_persistence` 单子叶等价基线，不能直接创建 approval_persistence 文件或启动发布过渡。
156. `runtime.mutation.ai_proposal.approval_persistence` 单子叶等价基线已建立；后续只能进入 BE-001BJ-02 抽离方案，不能直接创建 approval_persistence 文件、迁移 helper、横向连接 approval_review、迁移 sandbox/status helper 或启动发布过渡。
157. `runtime.mutation.ai_proposal.approval_persistence` 抽离方案已建立；后续只能进入 BE-001BJ-03 实际抽离，且只能创建计划 child 并迁移 `persist_approval` 与 `load_approval_from_disk`，不能迁移 approval_review、sandbox/status helper、AppState/schema/frontend caller 或启动发布过渡。
158. `runtime.mutation.ai_proposal.approval_persistence` 实际抽离已完成；后续只能进入 BE-001BJ-04 单叶 closeout，不能继续迁移 sandbox/status helper、AppState/schema/frontend caller 或启动发布过渡。
159. `runtime.mutation.ai_proposal.approval_persistence` 单叶 closeout 已完成并设置 `stop_split: true`；后续只能进入 BE-001BK-01 父叶残余判断，不能继续细拆 approval_persistence 或启动发布过渡。
160. `runtime.mutation.ai_proposal` 第六轮父叶残余判断已完成；后续只能进入 BE-001BL-01 `runtime.mutation.ai_proposal.sandbox_trigger` 单子叶等价基线，不能直接创建 sandbox_trigger 文件、迁移 sandbox task、迁移 status_transition、改变 AppState/schema/frontend caller 或启动发布过渡。
161. `runtime.mutation.ai_proposal.sandbox_trigger` 单子叶等价基线已建立；后续只能进入 BE-001BL-02 抽离方案，不能直接创建目标文件、移动 sandbox task、迁移 status_transition、横向连接 approval_persistence 或启动发布过渡。
162. `runtime.mutation.ai_proposal.sandbox_trigger` 抽离方案已建立；后续只能进入 BE-001BL-03 实际抽离，且只能迁移两个 sandbox helper 并提取 background task helper，不能迁移 status_transition、整个 create orchestration、AppState/schema/frontend caller 或启动发布过渡。
163. `runtime.mutation.ai_proposal.sandbox_trigger` 实际抽离已完成；后续只能进入 BE-001BL-04 单叶 closeout，不能继续迁移 status_transition、proposal create orchestration、AppState/schema/frontend caller、route facade、runtime persistence owner 或启动发布过渡。
164. `runtime.mutation.ai_proposal.sandbox_trigger` 单叶 closeout 已完成；后续只能进入 BE-001BM-01 父叶残余判断，不能继续细拆 sandbox_trigger、回改已 closeout 子叶、迁移 status_transition 代码或启动发布过渡。
165. `runtime.mutation.ai_proposal` 第七轮父叶残余判断已完成；后续只能进入 BE-001BN-01 `runtime.mutation.ai_proposal.status_transition` 单子叶等价基线，不能直接创建 status_transition 文件、迁移状态 helper、回改已 closeout 子叶或启动发布过渡。
166. `runtime.mutation.ai_proposal.status_transition` 单子叶等价基线已建立；后续只能进入 BE-001BN-02 抽离方案，不能直接创建目标文件、迁移状态 helper、回改已 closeout 子叶或启动发布过渡。
167. `runtime.mutation.ai_proposal.status_transition` 抽离方案已建立；后续只能进入 BE-001BN-03 实际抽离，且只能迁移三个状态 helper，不能迁移 proposal create orchestration、AppState/schema/frontend caller 或启动发布过渡。
168. `runtime.mutation.ai_proposal.status_transition` 实际抽离已完成；后续只能进入 BE-001BN-04 单叶 closeout，不能继续迁移 proposal create orchestration、回改 closed child、改变 AppState/schema/frontend caller 或启动发布过渡。
169. `runtime.mutation.ai_proposal.status_transition` 单叶 closeout 已完成；后续只能进入 BE-001BO-01 父叶残余判断，不能继续细拆 status_transition、回改 closed child、改变 AppState/schema/frontend caller 或启动发布过渡。
170. `runtime.mutation.ai_proposal` 第八轮父叶残余判断已完成；后续只能进入 BE-001BP-01 `proposal_creation` 单子叶等价基线，不能直接创建目标文件、迁移 `create_runtime_ai_proposal`、回改 closed child、改变 AppState/schema/frontend caller 或启动发布过渡。
171. `runtime.mutation.ai_proposal.proposal_creation` 单子叶等价基线已建立；后续只能进入 BE-001BP-02 抽离方案，不能直接创建目标文件、迁移 `create_runtime_ai_proposal`、拆分 approval record construction、回改 closed child、改变 AppState/schema/frontend caller 或启动发布过渡。
172. `runtime.mutation.ai_proposal.proposal_creation` 抽离方案已建立；后续只能进入 BE-001BP-03 实际抽离，且只能创建计划 child 并迁移 `create_runtime_ai_proposal`，不能迁移 record_query、approval_review、approval_persistence、status_transition、sandbox_trigger、改变 AppState/schema/frontend caller 或启动发布过渡。
173. `runtime.mutation.ai_proposal.proposal_creation` 实际抽离已完成；后续只能进入 BE-001BP-04 单叶 closeout，不能继续拆 approval record construction、lifecycle append、sandbox trigger call、transition persistence、改变 AppState/schema/frontend caller 或启动发布过渡。
174. `runtime.mutation.ai_proposal.proposal_creation` 单叶 closeout 已完成；后续只能进入 BE-001BQ-01 父叶残余判断，不能继续细拆本叶、迁移 AppState/schema/frontend caller、route facade、runtime persistence owner 或启动发布过渡。
175. `runtime.mutation.ai_proposal` 父叶残余判断已完成并设置 `stop_split: true`；后续只能进入 BE-001BR-01 `backend.runtime.routes` 父叶残余判断，不能继续细拆 AI proposal、回改 closed child、迁移 AppState/schema/frontend caller 或启动发布过渡。
176. `backend.runtime.routes` 第二轮父叶残余判断已完成；后续只能进入 BE-001BS-01 `backend.runtime.routes.experiment` 单子叶等价基线，不能直接创建 experiment route 子文件、迁移 handler、迁移 AppState/schema/frontend caller/runtime persistence owner 或启动发布过渡。
177. `backend.runtime.routes.experiment` 单子叶等价基线已建立；后续只能进入 BE-001BS-02 抽离方案，不能直接创建 experiment route 子文件、迁移 handler、迁移 AppState/schema/frontend caller/runtime persistence owner 或启动发布过渡。
178. `backend.runtime.routes.experiment` 抽离方案已建立；后续只能进入 BE-001BS-03 实际抽离，不能迁移 handler、改变 route order、迁移 AppState/schema/frontend caller/runtime persistence owner 或启动发布过渡。
179. `backend.runtime.routes.experiment` 实际抽离已完成；后续只能进入 BE-001BS-04 单叶 closeout，不能继续细拆 experiment route child、迁移 handler、迁移 AppState/schema/frontend caller/runtime persistence owner 或启动发布过渡。
180. `backend.runtime.routes.experiment` 单叶 closeout 已完成并设置 `stop_split: true`；后续只能进入 BE-001BT-01 `backend.runtime.routes` 父叶残余判断，不能从 experiment route child 继续细拆、直接迁移 evidence/report_ops/event_stream 或启动发布过渡。
181. `backend.runtime.routes` 第三轮父叶残余判断已完成；后续只能进入 BE-001BU-01 `backend.runtime.routes.evidence` 单子叶等价基线，不能直接创建 evidence route 子文件、迁移 evidence handler、迁移 AppState/schema/frontend caller/runtime persistence owner 或启动发布过渡。
182. `backend.runtime.routes.evidence` 单子叶等价基线已建立；后续只能进入 BE-001BU-02 抽离方案，不能直接创建 `src/backend/runtime/routes/evidence.rs`、迁移 evidence handler、迁移 AppState/schema/frontend caller/runtime persistence owner 或启动发布过渡。
183. `backend.runtime.routes.evidence` 抽离方案已建立；后续只能进入 BE-001BU-03 实际抽离，且只能创建 route child facade 并迁移 health/cleanup 两条 route registration，必须保持 event_stream -> evidence -> mutation 顺序，不能迁移 handler、schema、AppState、frontend caller、runtime persistence owner 或启动发布过渡。
184. `backend.runtime.routes.evidence` 实际抽离已完成；后续只能进入 BE-001BU-04 单叶 closeout，不能继续细拆或处理 report_ops/event_stream，不能迁移 handler、schema、AppState、frontend caller、runtime persistence owner 或启动发布过渡。
185. `backend.runtime.routes.evidence` 单叶 closeout 已完成并设置 `stop_split: true`；后续只能进入 BE-001BV-01 `backend.runtime.routes` 父叶残余判断，不能从 evidence route child 继续细拆或启动发布过渡。
186. `backend.runtime.routes` 第四轮父叶残余判断已完成；后续只能进入 BE-001BW-01 `backend.runtime.routes.event_stream` 单子叶等价基线，不能直接创建 event_stream route 子文件、迁移 SSE handler、迁移 AppState/schema/frontend caller/runtime persistence owner 或启动发布过渡。
187. `backend.runtime.routes.event_stream` 单子叶等价基线已建立；后续只能进入 BE-001BW-02 抽离方案，不能直接创建 event_stream route 子文件、迁移 SSE handler、改变 SSE frame/keepalive、迁移 AppState/schema/frontend caller/runtime persistence owner 或启动发布过渡。
188. `backend.runtime.routes.event_stream` 抽离方案已建立；后续只能进入 BE-001BW-03 实际抽离，且只能创建 route child facade 并迁移一条 route registration，必须保持 run -> event_stream -> evidence -> mutation 顺序，不能迁移 handler、改变 SSE frame/keepalive、迁移 AppState/schema/frontend caller/runtime persistence owner 或启动发布过渡。
189. `backend.runtime.routes.event_stream` 实际抽离已完成；后续只能进入 BE-001BW-04 单叶 closeout，不能继续细拆或处理 report_ops，不能迁移 handler、改变 SSE frame/keepalive、迁移 AppState/schema/frontend caller/runtime persistence owner 或启动发布过渡。
190. `backend.runtime.routes.event_stream` 单叶 closeout 已完成并设置 `stop_split: true`；后续只能进入 BE-001BX-01 `backend.runtime.routes` 第五轮父叶残余判断，不能从 event_stream route child 继续细拆、直接处理 report_ops 或启动发布过渡。
191. `backend.runtime.routes` 第五轮父叶残余判断已完成；后续只能进入 BE-001BY-01 `backend.runtime.routes.report_ops` 单子叶等价基线，不能直接创建 report_ops route 子文件、迁移 handler、迁移 AppState/schema/frontend caller/runtime persistence owner 或启动发布过渡。
192. `backend.runtime.routes.report_ops` 单子叶等价基线已建立；后续只能进入 BE-001BY-02 抽离方案，不能直接创建 report_ops route 子文件、迁移 handler、迁移 AppState/schema/frontend caller/runtime persistence owner 或启动发布过渡。
193. `backend.runtime.routes.report_ops` 抽离方案已建立；后续只能进入 BE-001BY-03 实际抽离，不能跳过创建 route facade 做 handler/schema/state/frontend/runtime persistence/storage lifecycle 迁移，也不能启动发布过渡。
194. `backend.runtime.routes.report_ops` 实际抽离已完成；后续只能进入 BE-001BY-04 单叶 closeout，不能跳过 closeout 继续拆 handler/schema/state/frontend/runtime persistence/storage lifecycle 或启动发布过渡。
195. `backend.runtime.routes.report_ops` 单叶 closeout 已完成并设置 `stop_split: true`；后续只能进入 BE-001BZ-01 父叶残余判断，不能从 report_ops route child 继续细拆或启动发布过渡。
196. `backend.runtime.routes` 第六轮父叶残余判断已完成并设置 `stop_split: true`；后续只能进入 BE-001CA-01 `backend.runtime` 父叶残余判断，不能从 route aggregate 继续细拆或启动发布过渡。
197. `backend.runtime` 父叶残余判断已完成并保持 `stop_split: false`；后续只能进入 BE-001CB-01 `runtime.report_ops` 单子叶等价基线，不能直接创建 child 文件、迁移 handler 或启动发布过渡。
198. `runtime.report_ops` 单子叶等价基线已建立；后续只能进入 BE-001CB-02 抽离方案，不能直接创建 `src/runtime/report_ops.rs`、迁移 handler、扩大 v1 ops/report endpoint 测试缺口或启动发布过渡。
199. `runtime.report_ops` 抽离方案已建立；后续只能进入 BE-001CB-03 实际抽离，迁移清单仅限 report helper 与十个 public handler，不能处理 `runtime.evidence_health`、schema owner、frontend caller、persistence owner、storage lifecycle owner、`AppState` 或 release transition guard。
200. `runtime.report_ops` 实际抽离已完成；后续只能进入 BE-001CB-04 单叶 closeout，不能跳过 closeout 处理 `runtime.evidence_health`、schema owner、frontend caller、persistence owner、storage lifecycle owner、`AppState` 或 release transition guard。
201. `runtime.report_ops` 单叶 closeout 已完成并设置 `stop_split: false`；后续只能进入 BE-001CC-01 `runtime.report_ops.runtime_report` 单子叶等价基线，不能直接创建 child 文件、迁移 handler、处理 v1 ops/report endpoints 或启动 release transition。
202. `runtime.report_ops.runtime_report` 单子叶等价基线已建立；后续只能进入 BE-001CC-02 抽离方案，不能创建 `src/runtime/report_ops/runtime_report.rs`、迁移 handler、处理 v1 ops/report endpoints、`runtime.evidence_health` 或启动 release transition。
203. `runtime.report_ops.runtime_report` 抽离方案已建立；后续只能进入 BE-001CC-03 实际抽离，迁移清单仅限四个 runtime report public handler 与四个 private helper，不能处理 v1 ops/report endpoints、`runtime.evidence_health`、schema owner、frontend caller、state/persistence owner、storage lifecycle owner、`AppState` 或 release transition。
204. `runtime.report_ops.runtime_report` 实际抽离已完成；后续只能进入 BE-001CC-04 单叶 closeout，不能跳过 closeout 处理 v1 ops/report endpoints、`runtime.evidence_health`、schema owner、frontend caller、state/persistence owner、storage lifecycle owner、`AppState` 或 release transition。
205. `runtime.report_ops.runtime_report` 单叶 closeout 已完成并设置 `stop_split: true`；后续只能进入 BE-001CD-01 `runtime.report_ops` 父叶残余判断，不能回改 closed child、处理 `runtime.evidence_health`、schema owner、frontend caller、state/persistence owner、storage lifecycle owner、`AppState` 或启动 release transition。
206. `runtime.report_ops` 父叶残余判断已完成并保持 `stop_split: false`；后续只能进入 BE-001CE-01 `runtime.report_ops.v1_report_endpoints` 单子叶等价基线，不能创建 child 文件、迁移 handler、处理 merge/generation/storage health endpoints、`runtime.evidence_health`、schema owner、frontend caller、state/persistence owner、storage lifecycle owner、`AppState` 或启动 release transition。
207. `runtime.report_ops.v1_report_endpoints` 单子叶等价基线已建立；后续只能进入 BE-001CE-02 抽离方案，不能创建 `src/runtime/report_ops/v1_report_endpoints.rs`、迁移 handler、处理 merge/generation/storage health endpoints、`runtime.evidence_health`、schema owner、frontend caller、state/persistence owner、storage lifecycle owner、`AppState` 或启动 release transition。
208. `runtime.report_ops.v1_report_endpoints` 抽离方案已建立；后续只能进入 BE-001CE-03 endpoint smoke 补测，不能创建 child module、迁移 handler、处理 merge/generation/storage health endpoints、`runtime.evidence_health`、schema owner、frontend caller、state/persistence owner、storage lifecycle owner、`AppState` 或启动 release transition。
209. `runtime.report_ops.v1_report_endpoints` endpoint smoke 补测已完成；后续只能进入 BE-001CE-04 实际抽离，迁移清单仅限三个 v1 report handler，不能处理 merge/generation/storage health endpoints、`runtime.evidence_health`、schema owner、frontend caller、state/persistence owner、storage lifecycle owner、`AppState` 或启动 release transition。
210. `runtime.report_ops.v1_report_endpoints` 实际抽离已完成；后续只能进入 BE-001CE-05 单叶 closeout，不能继续迁移 merge/generation/storage health endpoints、`runtime.evidence_health`、schema owner、frontend caller、state/persistence owner、storage lifecycle owner、`AppState` 或启动 release transition。
211. `runtime.report_ops.v1_report_endpoints` 单叶 closeout 已完成并设置 `stop_split: true`；后续只能进入 BE-001CF-01 `runtime.report_ops` 父叶残余判断，不能从本叶继续细拆 ops/audit/research 微叶，不能启动 release transition。
212. `runtime.report_ops` 父叶残余判断已完成并保持 `stop_split: false`；后续只能进入 BE-001CG-01 `runtime.report_ops.merge_generation_health` 单子叶等价基线，不能直接创建 child 文件、迁移 handler、处理 `runtime.evidence_health` 或启动 release transition。
213. `runtime.report_ops.merge_generation_health` 单子叶等价基线已建立；后续只能进入 BE-001CG-02 抽离方案，不能直接创建 child 文件、迁移 handler、跳过 endpoint smoke 缺口判定、处理 `runtime.evidence_health` 或启动 release transition。
214. `runtime.report_ops.merge_generation_health` 抽离方案已建立并选择 test-first；后续只能进入 BE-001CG-03 endpoint smoke 补测，不能创建 child 文件、迁移 handler、处理 `runtime.evidence_health` 或启动 release transition。
215. `runtime.report_ops.merge_generation_health` endpoint smoke 补测已完成；后续只能进入 BE-001CG-04 实际抽离，迁移清单仅限 `list_merge_records`、`list_config_generations`、`get_storage_health`，不能处理 `runtime.evidence_health`、schema owner、frontend caller、runtime persistence owner、storage lifecycle owner、`AppState` 或启动 release transition。
216. `runtime.report_ops.merge_generation_health` 实际抽离已完成；后续只能进入 BE-001CG-05 单叶 closeout，不能继续迁移 `runtime.evidence_health`、schema owner、frontend caller、runtime persistence owner、storage lifecycle owner、`AppState` 或启动 release transition。
217. `runtime.report_ops.merge_generation_health` 单叶 closeout 已完成并设置 `stop_split: true`；后续只能进入 BE-001CH-01 `runtime.report_ops` 父叶残余判断，不能从本叶继续细拆 merge_records/config_generations/storage_health 微叶，不能启动 release transition。
218. `runtime.report_ops` 第二轮父叶残余判断已完成并设置 `stop_split: true`；后续只能进入 BE-001CI-01 `backend.runtime` 父叶残余判断，不能从 `runtime.report_ops` 继续细拆或启动 release transition。
219. `backend.runtime` 第二轮父叶残余判断已完成并保持 `stop_split: false`；后续只能进入 BE-001CJ-01 `runtime.evidence_health` 单子叶等价基线，不能直接创建 child 文件、迁移 handler、处理 shared helpers 或启动 release transition。
220. `runtime.evidence_health` 单子叶等价基线已建立；后续只能进入 BE-001CJ-02 抽离方案，不能直接创建 `src/runtime/evidence_health.rs`、迁移 handler/helper、迁移 schema/runtime persistence/metrics owner、改变 `AppState` 或启动 release transition。
221. `runtime.evidence_health` 抽离方案已建立；后续只能进入 BE-001CJ-03 实际抽离，迁移清单仅限 `runtime_report_status_counts`、`get_runtime_evidence_health`、`cleanup_runtime_evidence`，不能迁移 route/schema/runtime persistence/metrics owner、`AppState` 或启动 release transition。
222. `runtime.evidence_health` 实际抽离已完成；后续只能进入 BE-001CJ-04 单叶 closeout，不能跳过 closeout 回到 `backend.runtime` 父叶，不能迁移 schema/runtime persistence/metrics owner、`AppState` 或启动 release transition。
223. `runtime.evidence_health` 单叶 closeout 已完成并设置 `stop_split: true`；后续只能进入 BE-001CK-01 `backend.runtime` 第三轮父叶残余判断，不能继续细拆 health / cleanup 微叶，不能启动 release transition。
224. `backend.runtime` 第三轮父叶残余判断已完成并保持 `stop_split: false`；后续只能进入 BE-001CL-01 `runtime.mutation.shared_governance` 单子叶等价基线，不能直接创建 child 文件、迁移 helper、处理 query DTO/run guard 或启动 release transition。
225. `runtime.mutation.shared_governance` 单子叶等价基线已建立；后续只能进入 BE-001CL-02 抽离方案，不能直接创建 child 文件、迁移 9 个 shared governance helper、处理 query DTO/run guard 或启动 release transition。
226. `runtime.mutation.shared_governance` 抽离方案已建立；后续只能进入 BE-001CL-03 实际抽离，迁移清单仅限 9 个 shared governance helper，不能处理 query DTO/run guard、`AppState`、schema/frontend/persistence/storage owner、lock order 或 release transition。
227. `runtime.mutation.shared_governance` 实际抽离已完成；后续只能进入 BE-001CL-04 单叶 closeout，不能直接回到 `backend.runtime` 父叶，不能处理 query/guard/response support 或 release transition。
228. `runtime.mutation.shared_governance` 单叶 closeout 已完成并设置 `stop_split: true`；后续只能进入 BE-001CM-01 `backend.runtime` 第四轮父叶残余判断，不能继续细拆 validation / event contract / governance projection 微叶，也不能启动 release transition。
229. `backend.runtime` 第四轮父叶残余判断已完成并保持 `stop_split: false`；后续只能进入 BE-001CN-01 `runtime.query_support` 单子叶等价基线，不能直接创建 `src/runtime/query_support.rs`、迁移 query DTO/helper、处理 response support、run guard 或启动 release transition。
230. `runtime.query_support` 单子叶等价基线已建立；后续只能进入 BE-001CN-02 抽离方案，不能直接创建 `src/runtime/query_support.rs`、迁移 query DTO/helper、删除 parent include 或启动 release transition。
231. `runtime.query_support` 抽离方案已建立；后续只能进入 BE-001CN-03 实际抽离，迁移清单仅限 7 个 query DTO、`clean_optional_filter` 与 `normalized_replay_options`，不能处理 response support、run guard、experiment limit、parent include 删除或启动 release transition。
232. `runtime.query_support` 实际抽离已完成；后续只能进入 BE-001CN-04 单叶 closeout，不能跳过 closeout 去处理 response support、run guard、experiment limit、parent include 删除或启动 release transition。
233. `runtime.query_support` 单叶 closeout 已完成并设置 `stop_split: true`；后续只能进入 BE-001CO-01 `backend.runtime` 第五轮父叶残余判断，不能从本叶继续细拆 replay/mutation/report query 或 normalization 微叶。
234. `backend.runtime` 第五轮父叶残余判断已完成并保持 `stop_split: false`；后续只能进入 BE-001CP-01 `runtime.response_support` 单子叶等价基线，不能直接创建 `src/runtime/response_support.rs`、迁移 response DTO、处理 run guard、experiment limit、parent include 删除或启动 release transition。
235. `runtime.response_support` 单子叶等价基线已建立；后续只能进入 BE-001CP-02 抽离方案，不能直接创建 child 文件、迁移 response DTO、处理 run guard、experiment limit、parent include 删除或启动 release transition。
236. `runtime.response_support` 抽离方案已建立；后续只能进入 BE-001CP-03 实际抽离，迁移清单仅限 3 个 response DTO，不能处理 run guard、experiment limit、parent include 删除或启动 release transition。
237. `runtime.response_support` 实际抽离已完成；后续只能进入 BE-001CP-04 单叶 closeout，不能跳过 closeout 处理 run guard、experiment limit、parent include 删除或启动 release transition。
238. `runtime.response_support` 单叶 closeout 已完成并设置 `stop_split: true`；后续只能进入 BE-001CQ-01 `backend.runtime` 第六轮父叶残余判断，不能从 response_support 继续细拆或启动 release transition。
239. `backend.runtime` 第六轮父叶残余判断已完成并保持 `stop_split: false`；后续只能进入 BE-001CR-01 `runtime.run_guard` 单子叶等价基线，不能直接创建 guard child、处理 experiment limit、删除 parent include 或启动 release transition。
240. `runtime.run_guard` 单子叶等价基线已建立；后续只能进入 BE-001CR-02 抽离方案，不能直接创建 `src/runtime/run_guard.rs`、迁移 guard、处理 experiment limit、删除 parent include 或启动 release transition。
241. `runtime.run_guard` 抽离方案已建立；后续只能进入 BE-001CR-03 实际抽离，迁移清单仅限 `RunInProgressGuard` 与 `Drop for RunInProgressGuard`，不能处理 experiment limit、删除 parent include、改变 `AppState` 或启动 release transition。
242. `runtime.run_guard` 实际抽离已完成；后续只能进入 BE-001CR-04 单叶 closeout，不能跳过 closeout 处理 experiment limit、parent include cleanup、`AppState` 或 release transition。
243. `runtime.run_guard` 单叶 closeout 已完成并设置 `stop_split: true`；后续只能进入 BE-001CS-01 `backend.runtime` 第七轮父叶残余判断，不能从 run_guard 继续细拆或启动 release transition。
244. `backend.runtime` 第七轮父叶残余判断已完成并保持 `stop_split: false`；后续只能进入 BE-001CT-01 `runtime.experiment_limit` 单子叶等价基线，不能直接迁移 `MAX_EXPERIMENT_VARIANTS`、删除 parent include 或启动 release transition。
245. `runtime.experiment_limit` 单子叶等价基线已建立；后续只能进入 BE-001CT-02 抽离方案，不能直接创建 `src/runtime/experiment_limit.rs`、迁移 `MAX_EXPERIMENT_VARIANTS`、删除 parent include 或启动 release transition。
246. `runtime.experiment_limit` test-first 抽离方案已建立；后续只能进入 BE-001CT-03 endpoint smoke 补测，不能直接创建 planned child、迁移 `MAX_EXPERIMENT_VARIANTS`、删除 parent include 或启动 release transition。
247. `runtime.experiment_limit` endpoint smoke 补测已完成；后续只能进入 BE-001CT-04 实际抽离，不能跳过测试证据、处理 parent include cleanup 或启动 release transition。
248. `runtime.experiment_limit` 实际抽离已完成；后续只能进入 BE-001CT-05 单叶 closeout，不能跳过 closeout 处理 parent include cleanup 或启动 release transition。
249. `runtime.experiment_limit` 单叶 closeout 已完成并设置 `stop_split: true`；后续只能进入 BE-001CU-01 `backend.runtime` 第八轮父叶残余判断，不能从 experiment_limit 继续细拆或启动 release transition。
250. `backend.runtime` 第八轮父叶残余判断已完成并保持 `stop_split: false`；后续只能进入 BE-001CV-01 `runtime.parent_include_cleanup` 单子叶等价基线，不能直接删除 drained include、删除 drained 文件或启动 release transition。
251. `runtime.parent_include_cleanup` 单子叶等价基线已建立；后续只能进入 BE-001CV-02 抽离方案，不能跳过方案直接删除 `include!(...)`、删除 drained 文件或启动 release transition。
252. `runtime.parent_include_cleanup` 抽离方案已建立；后续只能进入 BE-001CV-03 实际 cleanup，且只能删除三条 drained `include!(...)` 与三个 drained 文件，不能顺手处理 `backend.runtime` 父叶 closeout 或 release transition。
253. `runtime.parent_include_cleanup` 实际 cleanup 已完成；三条 drained `include!(...)` 与三个 drained 文件已删除，后续只能进入 BE-001CW-01 `backend.runtime` 第九轮父叶残余判断，不能跳过判断宣称 `backend.runtime stop_split: true` 或启动 release transition。
254. `backend.runtime` 第九轮父叶残余判断已完成并保持 `stop_split: false`；父级真实残余从 drained include 转为 parent import bridge，后续只能进入 BE-001CX-01 `runtime.parent_import_bridge` 单子叶等价基线，不能直接批量替换 `use super::*` 或启动 release transition。
255. `runtime.parent_import_bridge` 单子叶等价基线已建立；当前冻结 46 个 runtime 文件仍依赖 parent import bridge 的事实，后续只能进入 BE-001CX-02 抽离方案，不能直接批量改写 Rust import、不能新增 sibling horizontal link、不能启动 release transition。
256. `runtime.parent_import_bridge` 抽离方案已建立；后续采用 staged explicit import pass，下一步只能进入 BE-001CX-03 `runtime.root_support_import_pilot` 实际抽离，且首批只处理 `query_support` 与 `response_support` 两个文件。
257. `runtime.root_support_import_pilot` 实际抽离已完成；`query_support` 与 `response_support` 已移除 parent wildcard import，runtime parent bridge 依赖文件数从 46 降为 44。下一步只能进入 BE-001CX-04 单叶 closeout，不能直接扩大战线处理 run/backtest/mutation/report_ops。
258. `runtime.root_support_import_pilot` 单叶 closeout 已完成并设置 `stop_split: true`；下一步只能进入 BE-001CY-01 `runtime.root_entry_import_pass` 单子叶等价基线，不能跳过基线直接改写 root entry 文件。
259. `runtime.root_entry_import_pass` 单子叶等价基线已建立；root entry 候选为 `event_stream`、`evidence_health`、`report_ops`、`run_guard` 与 `mod.rs`，其中 `run_guard` 是 test-only super import。下一步只能进入 BE-001CY-02 抽离方案。
260. `runtime.root_entry_import_pass` 抽离方案已建立；BE-001CY-03 只允许实际改写 `src/runtime/event_stream.rs` 与 `src/runtime/evidence_health.rs` 的 parent wildcard import，不得混入 `src/runtime/report_ops.rs`、test-only `src/runtime/run_guard.rs`、`src/runtime/mod.rs`、sibling horizontal link 或 release transition。
261. `runtime.root_entry_import_pass` 实际抽离已完成；`src/runtime/event_stream.rs` 与 `src/runtime/evidence_health.rs` 已移除 parent wildcard import，runtime parent bridge 依赖文件数从 44 降为 42。下一步只能进入 BE-001CY-04 单叶 closeout，不能直接处理 `src/runtime/report_ops.rs` 或 `src/runtime/mod.rs`。
262. `runtime.root_entry_import_pass` 单叶 closeout 已完成并设置 `stop_split: true`；下一步只能进入 BE-001CZ-01 `runtime.report_ops_import_pass` 单子叶等价基线，不能跳过 report_ops 直接处理 `src/runtime/mod.rs`。
263. `runtime.report_ops_import_pass` 单子叶等价基线已建立；候选范围为 `src/runtime/report_ops.rs` 与 3 个 report_ops child，且 parent facade 的 `use super::*` 是 child 依赖父级白箱输入的转运层。下一步只能进入 BE-001CZ-02 抽离方案，不能直接改写 Rust import。
264. `runtime.report_ops_import_pass` 抽离方案已建立；BE-001CZ-03 只允许同批处理 report_ops four-file pocket，不能混入 `src/runtime/mod.rs`、test-only `src/runtime/run_guard.rs` 或 run/backtest/mutation 子树。
265. `runtime.report_ops_import_pass` 实际抽离已完成；report_ops four-file pocket 已移除 parent wildcard import，runtime parent bridge 依赖文件数从 42 降为 38。下一步只能进入 BE-001CZ-04 单叶 closeout。
266. `runtime.report_ops_import_pass` 单叶 closeout 已完成并设置 `stop_split: true`；该叶不继续拆微叶，下一步回到 BE-001DA-01 `runtime.parent_import_bridge` 父叶残余判断，重新选择后续 staged explicit import pass 候选。
267. `runtime.parent_import_bridge` 父叶残余判断已完成并保持 `stop_split: false`；当前剩余 38 个 parent bridge 依赖文件，下一步选择 BE-001DB-01 `runtime.run_import_pass` 单子叶等价基线。
268. `runtime.run_import_pass` 单子叶等价基线已建立；候选范围只包含 4 个 `src/runtime/run/**` child，下一步只能进入 BE-001DB-02 抽离方案，不能直接改写 Rust import。
269. `runtime.run_import_pass` 抽离方案已建立；BE-001DB-03 只允许改写 4 个 run child 的 import，不得混入 root parent bridge、backtest/mutation 子树或 release transition。
270. `runtime.run_import_pass` 实际抽离已完成；4 个 run child 已移除 parent wildcard import，runtime parent bridge 依赖文件数从 38 降为 34。下一步只能进入 BE-001DB-04 单叶 closeout。
271. `runtime.run_import_pass` 单叶 closeout 已完成并设置 `stop_split: true`；该叶不继续拆微叶，下一步回到 BE-001DC-01 `runtime.parent_import_bridge` 父叶残余判断。
272. `runtime.parent_import_bridge` 父叶残余判断已完成并保持 `stop_split: false`；三叶暂停目标取消，后续按每个递归步验收后提交的干净节奏推进。当前剩余 34 个 parent bridge 依赖文件，下一步选择 BE-001DD-01 `runtime.backtest_import_pass` 单子叶等价基线。
273. `runtime.backtest_import_pass` 单子叶等价基线已建立；冻结 11 个 backtest 残余文件和 public/internal 方法边界。下一步只能进入 BE-001DD-02 抽离方案，不能直接改写 Rust import。
274. `runtime.backtest_import_pass` 抽离方案已建立；拒绝 11 文件整批 import rewrite，按 pocket 递归推进。下一步只能进入 BE-001DE-01 `runtime.backtest.record_store_import_pass` 单子叶等价基线。
275. `runtime.backtest.record_store_import_pass` 单子叶等价基线已建立；冻结 `record_store.rs` 的 4 个 public 方法和预期显式输入面。下一步只能进入 BE-001DE-02 抽离方案，不能直接改写 Rust import。
276. `runtime.backtest.record_store_import_pass` 抽离方案已建立；BE-001DE-03 只允许改写 `src/runtime/backtest/record_store.rs` 顶部 import，不得混入 replay、experiment、execution_start、root bridge 或 release transition。
277. `runtime.backtest.record_store_import_pass` 实际抽离已完成；`src/runtime/backtest/record_store.rs` 已从 parent wildcard import 收敛为显式输入，runtime parent bridge 依赖文件数从 34 降为 33。下一步只能进入 BE-001DE-04 单叶 closeout。
278. `runtime.backtest.record_store_import_pass` 单叶 closeout 已完成并设置 `stop_split: true`；该 import pocket 不继续拆微叶。下一步回到 BE-001DF-01 `runtime.backtest_import_pass` 父叶残余判断，再由父叶选择下一个 staged explicit import pass 候选。
279. `runtime.backtest_import_pass` 父叶残余判断已完成并保持 `stop_split: false`；当前剩余分布为 root 1 / run 0 / backtest 10 / mutation 21 / test-only 1 / total 33。下一步只能进入 BE-001DG-01 `runtime.backtest.replay_import_pass` 单子叶等价基线。
280. `runtime.backtest.replay_import_pass` 单子叶等价基线已建立；冻结 `src/runtime/backtest/replay.rs` 的 replay handler import 输入面。下一步只能进入 BE-001DG-02 抽离方案，不能直接改写 Rust import。
281. `runtime.backtest.replay_import_pass` 抽离方案已建立；BE-001DG-03 只允许改写 `src/runtime/backtest/replay.rs` 顶部 import，不得混入 experiment、execution_start、root bridge、mutation 或 release transition。
282. `runtime.backtest.replay_import_pass` 实际抽离已完成；`src/runtime/backtest/replay.rs` 已从 parent wildcard import 收敛为显式输入，runtime parent bridge 依赖文件数从 33 降为 32。下一步只能进入 BE-001DG-04 单叶 closeout。
283. `runtime.backtest.replay_import_pass` 单叶 closeout 已完成并设置 `stop_split: true`；本叶不继续拆 query / lookup / response / metric 微叶。下一步只能回到 BE-001DH-01 `runtime.backtest_import_pass` 父叶残余判断，重新选择 staged explicit import pass 候选。
284. `runtime.backtest_import_pass` 第二轮父叶残余判断已完成并保持 `stop_split: false`；当前剩余分布为 root 1 / run 0 / backtest 9 / mutation 21 / test-only 1 / total 32。下一步只能进入 BE-001DI-01 `runtime.backtest.experiment_sweep_import_pass` 单子叶等价基线。
285. `runtime.backtest.experiment_sweep_import_pass` 单子叶等价基线已建立；本基线冻结 experiment sweep 四文件 import pocket。下一步只能进入 BE-001DI-02 抽离方案，不能直接改写 Rust import。
286. `runtime.backtest.experiment_sweep_import_pass` 抽离方案已建立；BE-001DI-03 只允许改写四文件 import 和必要 visibility，不能混入 execution_start、root bridge、mutation、test-only run_guard 或 release transition。
287. `runtime.backtest.experiment_sweep_import_pass` 实际抽离已完成；四文件 parent import 已收敛，runtime parent bridge 依赖文件数从 32 降为 28。下一步只能进入 BE-001DI-04 单叶 closeout。
288. `runtime.backtest.experiment_sweep_import_pass` 单叶 closeout 已完成并设置 `stop_split: true`；该 import pocket 不继续拆 grid / lifecycle / orchestration 微叶。旧的三叶暂停目标取消，下一步只能回到 BE-001DJ-01 `runtime.backtest_import_pass` 父叶残余判断。
289. `runtime.backtest_import_pass` 第三轮父叶残余判断已完成并保持 `stop_split: false`；当前剩余分布为 root 1 / run 0 / backtest 5 / mutation 21 / test-only 1 / total 28。下一步只能进入 BE-001DK-01 `runtime.backtest.execution_start_import_pass` 单子叶等价基线。
290. `runtime.backtest.execution_start_import_pass` 单子叶等价基线已建立；冻结 execution_start 五文件 import pocket。下一步只能进入 BE-001DK-02 抽离方案，不能直接改写 Rust import。
291. `runtime.backtest.execution_start_import_pass` 抽离方案已建立；BE-001DK-03 只允许五文件 explicit import rewrite 和必要 visibility，不能混入 root bridge、mutation、test-only run_guard 或 release transition。
292. `runtime.backtest.execution_start_import_pass` 实际抽离已完成；五文件 parent wildcard / super import 已清除，runtime parent bridge 依赖文件数从 28 降为 23，backtest residual 为 0。下一步只能进入 BE-001DK-04 单叶 closeout。
293. `runtime.backtest.execution_start_import_pass` 单叶 closeout 已完成并设置 `stop_split: true`；该 import pocket 不继续拆 legacy dispatch / v4 projection / request resolution / runtime execution 微叶。旧的三叶暂停目标保持取消，下一步只能回到 BE-001DL-01 `runtime.backtest_import_pass` 父叶残余判断。
294. `runtime.backtest_import_pass` 第四轮父叶残余判断已完成并设置 `stop_split: true`；backtest residual 为 0。下一步只能回到 BE-001DM-01 `runtime.parent_import_bridge` 父叶残余判断，不能直接跳入 mutation 或 root bridge rewrite。
295. `runtime.parent_import_bridge` 父叶残余判断已完成并保持 `stop_split: false`；当前剩余 root 1 / run 0 / backtest 0 / mutation 21 / test-only 1 / total 23。下一步只能进入 BE-001DN-01 `runtime.mutation_import_pass` 单子叶等价基线。
296. `runtime.mutation_import_pass` 单子叶等价基线已建立；冻结 21 个 mutation parent bridge 文件和 AI proposal / parameter mutation / shared governance pocket。下一步只能进入 BE-001DN-02 抽离方案，不能直接批量改写 Rust import。
297. `runtime.mutation_import_pass` 抽离方案已建立并保持 `stop_split: false`；拒绝 21 文件整批改写。下一步只能进入 BE-001DO-01 `runtime.mutation.shared_governance_import_pass` 单子叶等价基线。
298. `runtime.mutation.shared_governance_import_pass` 单子叶等价基线已建立；冻结 `src/runtime/mutation/shared_governance.rs` 的 9 个 helper、当前 parent wildcard import 和显式 import 输入面。下一步只能进入 BE-001DO-02 抽离方案，不能跳过方案直接改写 Rust。
299. `runtime.mutation.shared_governance_import_pass` 抽离方案已建立；BE-001DO-03 只允许单文件替换 `use super::*` 为显式 import。下一步只能进入 BE-001DO-03 实际抽离记录，不能混入 parameter_mutation / ai_proposal / root bridge。
300. `runtime.mutation.shared_governance_import_pass` 实际抽离已完成；`src/runtime/mutation/shared_governance.rs` parent wildcard import 已清零。当前 parent bridge 剩余 root 1 / run 0 / backtest 0 / mutation 20 / test-only 1 / total 22；下一步只能进入 BE-001DO-04 单叶 closeout。
301. `runtime.mutation.shared_governance_import_pass` 单叶 closeout 已完成并设置 `stop_split: true`；该 import pocket 不继续拆微叶。下一步只能进入 BE-001DP-01 `runtime.mutation_import_pass` 父叶残余判断。
302. `runtime.mutation_import_pass` 父叶残余判断已完成并保持 `stop_split: false`；当前 mutation residual 为 20。下一步只能进入 BE-001DQ-01 `runtime.mutation.parameter_mutation_import_pass` 单子叶等价基线，不能直接整批改写 20 文件。
303. `runtime.mutation.parameter_mutation_import_pass` 单子叶等价基线已建立；本批冻结 10 个 `parameter_mutation` residual 文件、public handler、内部 helper 和隐式输入面。旧的三叶暂停目标继续取消；下一步只能进入 BE-001DQ-02 抽离方案，并先判断是否继续拆小 pocket。
304. `runtime.mutation.parameter_mutation_import_pass` 抽离方案已建立并保持 `stop_split: false`；拒绝 10 文件整批 rewrite。下一步只能进入 BE-001DR-01 `runtime.mutation.parameter_mutation.record_query_import_pass` 单子叶等价基线。
305. `runtime.mutation.parameter_mutation.record_query_import_pass` 单子叶等价基线已建立；冻结 `record_query.rs` 的 list/detail 读路径、当前 parent wildcard import 和预期显式输入面。下一步只能进入 BE-001DR-02 抽离方案。
306. `runtime.mutation.parameter_mutation.record_query_import_pass` 抽离方案已建立；BE-001DR-03 只允许改写 `src/runtime/mutation/parameter_mutation/record_query.rs` 顶部 import。下一步只能进入实际抽离记录。
307. `runtime.mutation.parameter_mutation.record_query_import_pass` 实际抽离已完成；`src/runtime/mutation/parameter_mutation/record_query.rs` parent wildcard import 已清零。当前 parent bridge 剩余 total 21 / mutation 19；下一步只能进入 BE-001DR-04 单叶 closeout。
308. `runtime.mutation.parameter_mutation.record_query_import_pass` 单叶 closeout 已完成并设置 `stop_split: true`；该 read-only import pocket 不继续拆 list/detail 微叶。旧三叶暂停目标保持取消，下一步只能进入 BE-001DS-01 `runtime.mutation.parameter_mutation_import_pass` 父叶残余判断。
309. `runtime.mutation.parameter_mutation_import_pass` 父叶残余判断已完成并保持 `stop_split: false`；当前 `parameter_mutation` residual 为 9 文件，拒绝整批 rewrite。下一步只能进入 BE-001DT-01 `runtime.mutation.parameter_mutation.proposal_creation_import_pass` 单子叶等价基线。
310. `runtime.mutation.parameter_mutation.proposal_creation_import_pass` 单子叶等价基线已建立；冻结 `proposal_creation.rs` 的 proposal create handler、record id helper 和隐式输入面。下一步只能进入 BE-001DT-02 抽离方案。
311. `runtime.mutation.parameter_mutation.proposal_creation_import_pass` 抽离方案已建立；BE-001DT-03 只允许改写 `src/runtime/mutation/parameter_mutation/proposal_creation.rs` 顶部 import。不得改函数体、lifecycle sibling、parent facade 或 release transition。
312. `runtime.mutation.parameter_mutation.proposal_creation_import_pass` 实际抽离已完成；`src/runtime/mutation/parameter_mutation/proposal_creation.rs` parent wildcard import 已清零。当前 parent bridge 剩余 total 20 / mutation 18；下一步只能进入 BE-001DT-04 单叶 closeout。
313. `runtime.mutation.parameter_mutation.proposal_creation_import_pass` 单叶 closeout 已完成并设置 `stop_split: true`；该 write-path import pocket 不继续拆 create handler / record-id helper 微叶。旧三叶暂停目标保持取消，下一步只能进入 BE-001DU-01 `runtime.mutation.parameter_mutation_import_pass` 父叶残余判断。
314. `runtime.mutation.parameter_mutation_import_pass` 第二轮父叶残余判断已完成并保持 `stop_split: false`；当前 `parameter_mutation` residual 为 8 文件。下一步只能进入 BE-001DV-01 `runtime.mutation.parameter_mutation.transition_lifecycle_import_pass` 单子叶等价基线，不能直接改写 Rust。
315. `runtime.mutation.parameter_mutation.transition_lifecycle_import_pass` 单子叶等价基线已建立；冻结 7 文件 lifecycle 子树、activation / rollback public handler、boundary 父级白箱 helper 与内部 helper 输入面。下一步只能进入 BE-001DV-02 抽离方案。
316. `runtime.mutation.parameter_mutation.transition_lifecycle_import_pass` 抽离方案已建立并保持 `stop_split: false`；拒绝 7 文件同批 rewrite，下一步只能进入 BE-001DW-01 `runtime.mutation.parameter_mutation.transition_lifecycle.boundary_safety_import_pass` 单子叶等价基线。
317. `runtime.mutation.parameter_mutation.transition_lifecycle.boundary_safety_import_pass` 单子叶等价基线已建立；冻结 `boundary_safety.rs` 的 boundary validation、boundary resolution 与 safe-window evaluation 输入面。下一步只能进入 BE-001DW-02 抽离方案。
318. `runtime.mutation.parameter_mutation.transition_lifecycle.boundary_safety_import_pass` 抽离方案已建立；下一步只允许 BE-001DW-03 单文件改写 `src/runtime/mutation/parameter_mutation/transition_lifecycle/boundary_safety.rs` 顶部 import，旧三叶暂停目标继续取消，递归队列保持干净。
319. `runtime.mutation.parameter_mutation.transition_lifecycle.boundary_safety_import_pass` 实际抽离已完成；`boundary_safety.rs` 已移除 `use super::*`，residual 降为 total 19 / mutation 17 / parameter_mutation 7 / transition_lifecycle 6。下一步只能进入 BE-001DW-04 单叶 closeout。
320. `runtime.mutation.parameter_mutation.transition_lifecycle.boundary_safety_import_pass` 单叶 closeout 已完成并设置 `stop_split: true`；本 import pocket 不继续拆微叶，下一步只能进入 BE-001DX-01 `runtime.mutation.parameter_mutation.transition_lifecycle_import_pass` 父叶残余判断。
321. `runtime.mutation.parameter_mutation.transition_lifecycle_import_pass` 父叶残余判断已完成并保持 `stop_split: false`；下一步选择 BE-001DY-01 `runtime.mutation.parameter_mutation.transition_lifecycle.rollback_record_identity_import_pass` 单子叶等价基线。
322. `runtime.mutation.parameter_mutation.transition_lifecycle.rollback_record_identity_import_pass` 单子叶等价基线已建立；当前 `no code movement`，冻结 rollback id helper 的 digest/id 输出语义、`use super::*` residual 和预期显式 import 输入面。下一步只能进入 BE-001DY-02 抽离方案；旧三叶暂停目标继续取消，递归队列保持干净。
