# v4.16.0 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.fallback_description equivalence baseline and extraction plan

> Batch: BE-001XR-01
> Node: `root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.fallback_description`
> Parent: `root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering`
> Stage: `baseline_plan`
> Movement: no Rust code movement.

---

## Summary

`root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.fallback_description` is frozen before physical Rust movement.

This child owns the raw-text fallback condition descriptions used when structured runtime condition lowering returns `None`.

Decision:

`baseline_frozen: true`

Next movement:

`BE-001XR-02` may create `qrpc_compiler/src/runtime_protocol_lowering/intent_signal_lowering/fallback_description.rs` or equivalent private child module and move only the fallback description function listed below.

## Allowed Movement Set

The extraction step may move this symbol:

| Symbol | Responsibility |
| --- | --- |
| `describe_runtime_intent_condition` | Build raw-text fallback condition descriptions for MA/SMA, LongTermSell, RSI, MACD, Momentum, ZScore, and QuoteObserve intents. |

## Parent Calls Frozen

After extraction:

- `lower_runtime_intent_to_signal_rule` may call the child for raw fallback description when `condition_lowering::lower_runtime_intent_condition(intent)` returns `None`.
- The fallback must still produce `ScalarExpr::RawText { source }` through the parent-facing signal rule function.
- `lower_runtime_intent_to_indicator`, `lower_runtime_intent_kind`, and `condition_lowering` ownership remain outside this child.
- `runtime_intent_is_spread`, `decode_series_field`, `decode_align_direction`, and `decode_spread_output` remain parent/shared helpers and may only be used through parent-mediated private module access.

## Equivalence Baseline

BE-001XR-02 must preserve:

- raw fallback text for `LongTermBuy` and `SmaCrossover`: `ma_cross(fast=..., slow=..., entry_ratio=...)`;
- raw fallback text for `LongTermSell`: `ma_deviation(lookback=..., baseline_period=..., threshold_ratio=...)`;
- raw fallback text for `Rsi`: `rsi(period=..., oversold=..., overbought=...)`;
- raw fallback text for `Macd`: `macd(fast=..., slow=..., signal=...)`;
- raw fallback text for `Momentum`: `momentum(lookback=..., threshold_ratio=...)`;
- raw fallback text for `ZScore`: `zscore(window=..., entry_z=...)`;
- raw fallback text for non-spread `QuoteObserve`: `quote_observe(mid_price_delta)`;
- raw fallback text for spread `QuoteObserve`, including input count, decoded field, align, resample period, window size, output, and max time diff default;
- structured condition lowering behavior and public runtime Core IR output shape.

## Hard Exclusions

This baseline must not:

- move `lower_runtime_intent_to_indicator`;
- move `lower_runtime_intent_to_signal_rule`;
- move `lower_runtime_intent_kind`;
- move or edit `condition_lowering`;
- move runtime orchestration, data binding, agent policy, risk policy, Strategy IR lowering, canonical hash, public compiler facades, tests, or release transition;
- change fallback strings, parameter defaults, rounding behavior, or spread-observe field order.

## Boundary

**Real files**:
- `qrpc_compiler/src/runtime_protocol_lowering/intent_signal_lowering.rs`

**Markers**:
- `BE-001XR-01`
- `fallback_description baseline_frozen: true`
- `only describe_runtime_intent_condition may move`
- `condition_lowering remains closed`

**Next step**:
BE-001XR-02 root.contracts.compiler_bridge.runtime_protocol_lowering.intent_signal_lowering.fallback_description extract_closeout

---

## Gates

- `cargo test -p qrpc-compiler`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
