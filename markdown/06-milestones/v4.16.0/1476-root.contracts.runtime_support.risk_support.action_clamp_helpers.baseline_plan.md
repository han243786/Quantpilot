# v4.16.0 root.contracts.runtime_support.risk_support.action_clamp_helpers baseline plan

> Batch: BE-002BX-01
> Node: `root.contracts.runtime_support.risk_support.action_clamp_helpers`
> Parent: `root.contracts.runtime_support.risk_support`
> Stage: `baseline_plan`
> Movement: No Rust code movement.

---

## Scope

Freeze the baseline for extracting action-list clamp helpers.

Allowed movement:

- add `qrpc_runtime/src/risk_checker/action_clamp_helpers.rs`;
- add a parent child declaration in `qrpc_runtime/src/risk_checker.rs`;
- move only the selected action-list clamp helpers;
- keep `clamp_actions` as the parent-facing `pub(super)` entrypoint.

## In-Scope Helpers

- `clamp_actions`
- `clamp_sell_actions_to_inventory`
- `clamp_buy_actions_to_total_headroom`
- `clamp_buy_actions_to_exchange_headroom`
- `clamp_buy_actions_to_symbol_net_exposure`
- `clamp_buy_actions_to_portfolio_net_exposure`

## Out of Scope

- provider trait and public evaluate surface;
- `evaluate_risk_decision`;
- `enforce_risk_mode`;
- closed event payload projection child;
- closed direction/cross-symbol constraints child;
- portfolio target clamp helpers;
- `push_reason_code`;
- exposure math helpers such as `available_sell_ratio`, `symbol_net_exposure_ratio`, and `portfolio_net_exposure_ratio`;
- tests.

## Dependency Rule

The child may use `super::*` to access parent-owned types, `push_reason_code`, and exposure math helpers. It must not import sibling children directly and must not introduce a release-transition shortcut.

## Equivalence Baseline

The next actual extraction must preserve:

- action-list clamp ordering;
- sell inventory clamp semantics;
- total, exchange, symbol, and portfolio buy headroom scaling;
- reason-code mutation;
- reason text mutation;
- provider/evaluate public surface.

## Gates

Required gates for actual extraction:

- `cargo fmt`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime risk -- --nocapture`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 / full feature tree / matrix governance

## Next Step

BE-002BY-01 `root.contracts.runtime_support.risk_support.action_clamp_helpers` actual_extraction.
