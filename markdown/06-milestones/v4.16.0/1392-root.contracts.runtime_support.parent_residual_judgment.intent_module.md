# v4.16.0 root.contracts.runtime_support parent residual judgment selects intent_module

> Batch: BE-001ZS-01
> Node: `root.contracts.runtime_support`
> Stage: `parent_residual_judgment`
> Selected child: `root.contracts.runtime_support.intent_module`
> Movement: No Rust code movement.

---

## Parent State

Closed runtime-support children:

- `contracts.runtime_support.runtime_facade_coordinator`
- `contracts.runtime_support.data_module`

Remaining runtime-support queue from the frozen baseline:

- `contracts.runtime_support.intent_module`
- `contracts.runtime_support.agent_module`
- `contracts.runtime_support.core_ir_evaluator`
- `contracts.runtime_support.execution_module`
- `contracts.runtime_support.fill_engine`
- `contracts.runtime_support.risk_support`
- `contracts.runtime_support.sandbox_replay_timeline`
- `contracts.runtime_support.v4_runtime_support`
- `contracts.runtime_support.live_execution`
- `contracts.runtime_support.plugin_runtime_support`
- `contracts.runtime_support.ops_compat_support`
- `contracts.runtime_support.backtest_metrics`

## Selected Child Boundary

`root.contracts.runtime_support.intent_module` owns runtime intent generation from normalized market data and Core IR signal semantics.

Candidate physical surface:

- `qrpc_runtime/src/intent_module.rs`

Candidate public/parent-facing surface:

- intent provider trait and built-in provider behavior;
- quote observe intent generation;
- indicator/Core IR evaluator backed intent generation;
- MACD/RSI/custom indicator intent paths;
- intent module tests.

## Why This Child Next

| Criterion | Judgment |
| --- | --- |
| Queue order | It is the next child after the now-closed `data_module` in `root.contracts.runtime_support` baseline. |
| Cohesion | Strong: it converts runtime data/evaluator outputs into intent signals. |
| Inputs | Core IR signal rules, normalized market data, evaluator results, runtime context |
| Outputs | runtime intent signals/events consumed by agent/risk/execution stages through parent/provider boundaries |
| Risk | Medium: signal semantics and built-in strategy behavior must remain equivalent. |
| Exclusions | agent decision logic, risk checks, execution plans, fill simulation, sandbox, v4 runtime, live execution, plugins, and backtest metrics stay outside this child. |

## Leaf Split Decision Gate

`intent_module` should continue to baseline because:

- it is still an unclosed runtime-support child from the frozen queue;
- the file has independent signal/intent behavior from agent, risk, execution, and data modules;
- it is large enough to justify a dedicated baseline before any extraction;
- it has test coverage that should be preserved before deeper split decisions.

## Equivalence Baseline For Next Step

Next baseline must preserve:

- intent provider trait behavior;
- built-in quote observe signal behavior;
- MACD/RSI/custom indicator intent paths;
- Core IR evaluator preferred path behavior;
- parent/provider communication boundaries;
- existing intent module tests.

Required gates for the next movement:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-001ZT-01 `root.contracts.runtime_support.intent_module` baseline_plan.
