# v4.16.0 root.contracts.runtime_support.v4_runtime_support.type_surface extract closeout

> Batch: BE-002DK-01
> Node: `root.contracts.runtime_support.v4_runtime_support.type_surface`
> Parent: `root.contracts.runtime_support.v4_runtime_support`
> Stage: `actual_extraction`
> Movement: Rust code movement.

---

## Result

`root.contracts.runtime_support.v4_runtime_support.type_surface` actual extraction is complete.

New child:

- `qrpc_runtime/src/v4_runtime/type_surface.rs`

Parent updates:

- `qrpc_runtime/src/v4_runtime.rs` declares `mod type_surface;`
- `qrpc_runtime/src/v4_runtime.rs` re-exports the child with `pub use self::type_surface::*;`
- `qrpc_runtime/src/v4_runtime_types.rs` no longer owns public v4 DTOs, public v4 execution event constants, `V4PaperSimulatedRuntime`, or `V4Runtime`.

## Preserved Boundary

Moved into the child:

- v4 public DTOs and enums;
- public v4 execution event constants;
- `V4PaperSimulatedRuntime`;
- `V4Runtime`.

Kept parent-owned:

- private runtime state structs;
- graph symbol expansion;
- machine family initialization helpers;
- runtime constructors and mode gates;
- event replay orchestration;
- machine transition engine;
- risk/execution gates;
- simulated execution behavior;
- v4 tests.

## Equivalence

The crate-root `qrpc_runtime::*` v4 public surface remains mediated by `qrpc_runtime/src/lib.rs` and `qrpc_runtime::v4_runtime` re-exports. `V4PaperSimulatedRuntime` fields remain non-public externally and are `pub(super)` only for the v4 runtime parent.

## Verified Gates

- `cargo fmt`: PASS
- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime v4 -- --nocapture`: PASS, 32 passed
- `cargo check -p quantpilot`: PASS
- `git diff --check`: PASS
- UTF-8 check: PASS
- full feature tree check: PASS
- matrix governance check: PASS

## Next Step

BE-002DK-02 `root.contracts.runtime_support.v4_runtime_support.type_surface` single_leaf_closeout.
