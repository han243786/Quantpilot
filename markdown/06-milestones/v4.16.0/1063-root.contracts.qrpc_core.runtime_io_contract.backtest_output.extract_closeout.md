# v4.16.0 root.contracts.qrpc_core.runtime_io_contract.backtest_output extract closeout

> Batch: BE-001SN-02
> Node: `root.contracts.qrpc_core.runtime_io_contract.backtest_output`
> Parent: `root.contracts.qrpc_core.runtime_io_contract`
> Stage: `extract_closeout`
> Movement: Physical Rust extraction.

---

## Summary

`backtest_output` is now a private runtime IO child module.

Moved from `qrpc_core/src/runtime_io_contract.rs` into `qrpc_core/src/runtime_io_contract/backtest_output.rs`:

- `BacktestEquityPoint`
- `BacktestRiskAdjusted`
- `BacktestTradeAnalysis`
- `BacktestDrawdownAnalysis`
- `BacktestBenchmarkComparison`
- `BacktestSummary`
- `PeriodReturn`
- `BacktestOutput`

The runtime IO parent is now a pure facade over child modules.

## Equivalence Proof

| Surface | Result |
| --- | --- |
| Public facade | Preserved through `pub use backtest_output::*;` in `qrpc_core/src/runtime_io_contract.rs` and existing crate-root re-export. |
| DTO fields | Unchanged for all moved DTOs. |
| serde derives/defaults | Preserved, including defaulted metric fields. |
| skip rules | Preserved for benchmark equity curve, period returns, and optional v4 artifact. |
| Parent-owned dependencies | `SessionOutput` and `PortfolioState` are imported through the runtime IO parent. |
| Final parent shape | `qrpc_core/src/runtime_io_contract.rs` is now only `mod` declarations plus `pub use` exports. |

## Files

| File | Role |
| --- | --- |
| `qrpc_core/src/runtime_io_contract.rs` | Runtime IO parent facade. |
| `qrpc_core/src/runtime_io_contract/backtest_output.rs` | Final backtest output DTO child owner. |

## Boundary Notes

`backtest_output` imports `SessionOutput` and `PortfolioState` through `super::{...}` so the parent facade remains the bridge. It does not import `runtime_output` or `portfolio_state` child modules directly.

No RFC execution contracts, crate-root tests, compiler behavior, runtime behavior, backend behavior, or release-transition behavior were moved.

## Next Step

BE-001SN-03 `root.contracts.qrpc_core.runtime_io_contract.backtest_output` single_leaf_closeout.

## Gates

- `git diff --check`
- `cargo fmt`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
