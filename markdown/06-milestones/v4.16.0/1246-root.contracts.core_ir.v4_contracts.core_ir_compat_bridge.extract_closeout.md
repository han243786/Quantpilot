# v4.16.0 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge actual extraction complete

> Batch: BE-001WI-02
> Node: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `extract_closeout`
> Movement: Extracted Core IR v4 compatibility bridge code from the v4 parent facade into a dedicated child module.

---

## Summary

root.contracts.core_ir.v4_contracts.core_ir_compat_bridge actual extraction complete

BE-001WI-02 created `qrpc_core_ir/src/v4/core_ir_compat_bridge.rs` and moved the Core IR v4 compatibility report DTOs, bridge verdict/diagnostic DTOs, phase4 report validation, bridge entrypoint, Core IR input/reference validation helpers, compatibility graph/machine/event builders, bridge metadata helpers, bridge id sanitization, and bridge diagnostic push helper out of `qrpc_core_ir/src/v4.rs`.

The parent facade now declares `mod core_ir_compat_bridge;` and re-exports the child through `pub use core_ir_compat_bridge::*;`, preserving the public `qrpc_core_ir::v4::*` API surface.

Shared default helpers used by existing sibling modules stayed in `qrpc_core_ir/src/v4.rs`; only the bridge-specific `default_core_ir_compat_bridge_version` moved with the bridge DTO that references it.

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs`
- `qrpc_core_ir/src/v4/core_ir_compat_bridge.rs`

**Markers**:
- `BE-001WI-02`

**Next step**:
BE-001WI-03 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge single_leaf_closeout

---

## Equivalence Evidence

- `cargo fmt -p qrpc-core-ir`: passed.
- `cargo test -p qrpc-core-ir`: passed, 54 tests.
- `cargo check -p quantpilot`: passed.

## Residual Judgment Input

BE-001WI-03 must decide whether this extracted bridge should remain one leaf or split into smaller children. The main split candidates are:

- bridge DTO/report validation
- Core IR input/reference validation
- compatibility graph/machine/event construction

The decision should apply the current recursive leaf split rules and avoid micro-splitting helper-only fragments unless they have independent ownership, independent failure modes, or clear future change pressure.

---

## Gates

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `cargo check -p quantpilot`
