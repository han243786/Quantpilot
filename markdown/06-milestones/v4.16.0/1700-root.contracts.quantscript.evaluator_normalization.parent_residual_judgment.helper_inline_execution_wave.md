# v4.16.0 root.contracts.quantscript.evaluator_normalization parent residual judgment - helper_inline_execution_wave

> Batch: BE-002HQ-01
> Node: `root.contracts.quantscript.evaluator_normalization`
> Selected child: `root.contracts.quantscript.evaluator_normalization.helper_inline_execution_wave`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `root.contracts.quantscript.evaluator_normalization.helper_inline_execution_wave` as the next child.

The terminal leaf tool returned a conservative parent-level `STOP`, but the parent still has stronger ownership evidence for one coarse wave:

- helper function inline execution;
- symbolic helper body execution;
- helper-local control-flow execution for `if`, `for`, `while`, and `match`;
- environment snapshot and branch convergence handling.

This is not a micro split. It separates helper execution semantics from the public normalization orchestration while keeping all helper execution forms together.

## Tool Evidence

Command:

- `tools/evaluate-leaf-granularity.ps1 -Path quantscript/src/evaluator/mod.rs`

Result:

- `Decision: STOP`
- `Score: 52`
- `loc=790`
- `functions=39`
- `domains=8`
- `size_bucket=split_pressure_601_800`

Override rationale:

- the tool reason says to stop unless stronger ownership evidence exists;
- the helper inline execution block has an independent owner, state model, recursion/iteration budget, branch convergence logic, and failure mode;
- extracting it as one wave avoids splitting by individual control-flow statements and preserves the newer anti-over-splitting rule.

## Selected Child Boundary

`helper_inline_execution_wave` owns:

- `maybe_inline_function`
- `should_skip_inline`
- `MAX_RECURSION_DEPTH`
- `MAX_FOR_ITERATIONS`
- `evaluate_function`
- `execute_function_body`
- `execute_stmt`
- `IfStmtParts`
- `execute_if_stmt`
- `execute_for_stmt`
- `execute_while_stmt`
- `execute_match_stmt`
- `execute_block_in_place`
- `BlockSnapshot`
- `execute_block_snapshot`
- `execute_match_arm_snapshot`
- `execute_match_arm_body`
- `pattern_matches`

## Parent Residual After Planned Extraction

The parent should retain:

- public `normalize_script_module`;
- `EvalContext`;
- `normalize_function`, `normalize_block`, `normalize_stmt`, `normalize_expr`;
- the closed `folding_value_wave` child;
- evaluator tests until helper inline extraction is stable.

## Excluded From This Child

Do not move:

- public normalization entrypoint;
- AST walk normalization functions;
- folding/value helpers already owned by `folding_value_wave`;
- evaluator tests;
- parser, lowering, analysis diagnostics, typed resolution, static audit, test plan, legacy config, or release transition code.

## Next Step

BE-002HR-01 `root.contracts.quantscript.evaluator_normalization.helper_inline_execution_wave` baseline_plan.
