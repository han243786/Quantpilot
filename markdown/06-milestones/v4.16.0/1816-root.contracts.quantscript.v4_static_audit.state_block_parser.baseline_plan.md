# v4.16.0 root.contracts.quantscript.v4_static_audit.state_block_parser baseline plan

> Batch: BE-002KG-01
> Node: `root.contracts.quantscript.v4_static_audit.state_block_parser`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Goal

Freeze the state line and state block parser boundary before Rust movement.

This child owns parsing a state declaration or state block, including nested
child-machine handoff and depth-limit handling. State-group, memory,
transition, edge, and risk-plane parsing remain separate parent-controlled
candidates.

## Frozen Boundary

| Field | Value |
| --- | --- |
| Approved functions | `parse_state`, `parse_state_block`, `skip_block`, `empty_child_machine` |
| Planned file | `quantscript/src/v4_static_audit/state_block_parser.rs` |
| Inputs | prepared lines, state line text, line number, machine depth, parent template |
| Outputs | `MachineState`, next index, or diagnostics |
| Parent callers | `machine_block_parser` through parent wrappers |
| Visibility | helper functions remain private to the child unless wrapper access requires `pub(super)` |

## Equivalent Behavior

The extracted child must preserve:

- flat state missing-id diagnostic `QSV4110`;
- block state syntax diagnostic `QSV4110`;
- nested depth limit diagnostic `QSV4118`;
- duplicate child-machine diagnostic `QSV4120`;
- unsupported state block statement diagnostic `QSV4121`;
- missing state close diagnostic `QSV4122`;
- simple state `initial` and `terminal` flag parsing;
- state block child-machine parsing through the parent `parse_machine_block`
  wrapper;
- invalid fallback child machine construction;
- block skipping behavior for rejected nested states.

## Exclusions

- Do not move `parse_state_group`.
- Do not move `parse_memory`.
- Do not move `parse_transition`.
- Do not move `parse_edge`.
- Do not move `parse_risk_plane`.
- Do not move `parse_machine_template`.
- Do not move public report DTOs or local tests.
- Do not introduce release-transition cross-child links.

## Allowed Rust Movement For Next Step

BE-002KG-02 may:

1. add `mod state_block_parser;`;
2. create `quantscript/src/v4_static_audit/state_block_parser.rs`;
3. move `parse_state`;
4. move `parse_state_block`;
5. move private helpers `skip_block` and `empty_child_machine`;
6. keep parent private wrappers named `parse_state` and `parse_state_block`;
7. adjust imports and `pub(super)` visibility only where required by this
   extraction.

## Gates For Actual Extraction

| Gate | Command |
| --- | --- |
| format | `cargo fmt --all --check` |
| crate tests | `cargo test -p quantscript` |
| workspace compile | `cargo check -p quantpilot` |
| diff hygiene | `git diff --check` |
| UTF-8 | `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1` |
| full tree | `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1` |
| matrix governance | `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1` |

## Next Step

BE-002KG-02 `root.contracts.quantscript.v4_static_audit.state_block_parser`
actual_extraction.
