# v4.16.0 root.contracts.quantscript.v4_static_audit.runtime_handoff_builder baseline plan

> Batch: BE-002JS-01
> Node: `root.contracts.quantscript.v4_static_audit.runtime_handoff_builder`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Baseline

Freeze `runtime_handoff_builder` before Rust movement.

The approved movement is limited to the implementation body of
`build_v4_qs_runtime_handoff`. The public function remains in
`quantscript/src/v4_static_audit.rs` as a stable wrapper that delegates to a new
child module.

## Equivalent Behavior To Preserve

| Behavior | Baseline |
| --- | --- |
| Rejected audit report | Adds `v4 QS static audit must be accepted before runtime handoff` |
| Missing parsed graph | Adds `runtime handoff requires a parsed v4 machine graph` |
| Missing/rejected capability report | Adds `runtime handoff requires an accepted compile-time capability report` |
| Execution submission attached | Adds `runtime handoff must not carry execution submission attachment` |
| Non-`PaperSimulated` mode | Adds `current v4 runtime handoff only allows PaperSimulated start` |
| Accepted handoff | Diagnostics empty, `accepted_for_runtime_handoff` and `paper_simulated_start_allowed` true |
| Provider submission | Always false in this child |
| Runtime/lowering attachment | Always false in this child |

## Planned Rust Movement

| File | Action |
| --- | --- |
| `quantscript/src/v4_static_audit.rs` | Add `mod runtime_handoff_builder`; keep public wrapper |
| `quantscript/src/v4_static_audit/runtime_handoff_builder.rs` | New child with handoff construction implementation |

## Exclusions

- Do not move report DTO definitions.
- Do not move `audit_v4_quant_script_static`.
- Do not move parser helpers or static document parsing.
- Do not move tests in this batch.

## Verification Plan

- `cargo fmt --all --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- governance gates for diff whitespace, UTF-8, full feature tree, and matrix governance

## Next Step

BE-002JS-02 `root.contracts.quantscript.v4_static_audit.runtime_handoff_builder` actual_extraction.
