# v4.16.0 root.contracts.core_ir.v1_contract.root_graph_contract single leaf closeout

> Batch: BE-001TK-03
> Node: `root.contracts.core_ir.v1_contract.root_graph_contract`
> Parent: `root.contracts.core_ir.v1_contract`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root_graph_contract` is now a closed compact leaf.

The extraction in BE-001TK-02 created a physical owner for v1 root schema identity, metadata/source DTOs, constructor behavior, and DAG validation while preserving v1 and crate-root facades.

## Closed Surface

| Surface | Result |
| --- | --- |
| Physical owner exists | pass | `qrpc_core_ir/src/v1/root_graph_contract.rs` owns the selected root graph contract. |
| v1 parent facade preserved | pass | `qrpc_core_ir/src/v1.rs` re-exports `root_graph_contract::*`. |
| Crate facade preserved | pass | `qrpc_core_ir/src/lib.rs` still re-exports `v1::*`. |
| Rust equivalence verified | pass | `cargo check -p qrpc-core-ir`, `cargo test -p qrpc-core-ir`, and smart pre-commit passed in BE-001TK-02. |
| Full workspace check preserved | pass | Smart pre-commit passed `cargo check -p quantpilot`. |

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | Child owns constants, root DTOs, constructor, metadata/source DTOs, and DAG validation. |
| parent_child_communication_kept | pass | Sibling DTO references are imported through `super::...`; consumers remain facade-mediated. |
| equivalence_baseline_freezable | pass | BE-001TK-01 froze constants, fields, constructor, validation behavior, and diagnostics. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | pass | Root Core IR names are public qrpc-core-ir API. |
| state_machine_phase | guarded | DAG validation is pure graph validation. |
| strategy_branch | pass | Root Core IR models strategy graph identity and topology. |
| independent_failure_mode | pass | Root graph drift is isolated from expression/policy/test drift. |
| reuse_pressure | pass | Compiler/runtime paths depend on root contract names. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | pass | Splitting constants, root DTOs, constructor, and DAG validation would create tiny coupled leaves. |
| communication_cost_rises | pass | Extra boundaries would add re-export churn without independent proof. |
| local_proof_missing | no | qrpc-core-ir tests prove the move. |
| line_count_only | pass | Further split would be line-count driven only. |

leaf_split_decision_result

`stop_split: true`; `root_graph_contract` should remain one compact leaf.

## Residual Boundary

Still outside this closed leaf:

- `contracts.core_ir.v1_contract.data_indicator_expression_contract`;
- `contracts.core_ir.v1_contract.policy_execution_contract`;
- `contracts.core_ir.v1_contract.test_fixture`;
- v1 parent closeout;
- v4 contracts and core_ir parent closeout;
- release transition.

## Next Step

BE-001TL-01 `root.contracts.core_ir.v1_contract` parent_residual_judgment selects `data_indicator_expression_contract`.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
