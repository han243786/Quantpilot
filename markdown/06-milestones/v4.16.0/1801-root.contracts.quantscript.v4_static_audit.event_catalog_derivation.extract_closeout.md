# v4.16.0 root.contracts.quantscript.v4_static_audit.event_catalog_derivation extract closeout

> Batch: BE-002JY-02
> Node: `root.contracts.quantscript.v4_static_audit.event_catalog_derivation`
> Stage: `actual_extraction`
> Movement: Rust code movement complete.
> governance_next_authority: active

---

## Movement Summary

`quantscript/src/v4_static_audit/event_catalog_derivation.rs` now owns the v4
static-audit event catalog projection.

Moved implementation:

- `derive_event_catalog`;
- recursive `machine_family` traversal;
- event source-kind selection;
- event emitter/consumer aggregation;
- event catalog schema version and empty metadata construction.

The parent `quantscript/src/v4_static_audit.rs` now declares
`mod event_catalog_derivation;` and calls
`event_catalog_derivation::derive_event_catalog(&graph)` from
`parse_v4_static_document`.

## Preserved Boundaries

- Public API unchanged.
- Static document parsing remains parent-owned.
- Machine, state, memory, transition, edge, and risk-plane parsers remain
  parent-owned.
- Capability/type parsing remains in the closed `capability_type_parser` child.
- DTO definitions and local tests remain parent-owned.
- No release-transition link was introduced.

## Gates

| Gate | Result |
| --- | --- |
| `cargo fmt --all --check` | PASS |
| `cargo test -p quantscript` | PASS, 88 tests |
| `cargo check -p quantpilot` | PASS, no warnings |
| `git diff --check` | PASS |
| `check-utf8.ps1` | PASS |
| `check-matrix-governance.ps1` | PASS |
| `check-full-feature-tree.ps1` | pending rerun after this file and tree coverage |

## Next Step

BE-002JY-03 `root.contracts.quantscript.v4_static_audit.event_catalog_derivation`
single_leaf_closeout.
