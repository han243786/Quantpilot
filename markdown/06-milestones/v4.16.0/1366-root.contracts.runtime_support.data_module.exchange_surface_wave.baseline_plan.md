# v4.16.0 root.contracts.runtime_support.data_module.exchange_surface_wave baseline plan

> Batch: BE-001ZG-01
> Node: `root.contracts.runtime_support.data_module.exchange_surface_wave`
> Stage: `baseline_plan`
> Movement: No code movement.
> Current file: `qrpc_runtime/src/data_module.rs`
> Planned child file: `qrpc_runtime/src/data_module/exchange_surface.rs`

---

## Summary

The `exchange_surface_wave` baseline is frozen.

This same-parent wave batches two child boundaries under `root.contracts.runtime_support.data_module`:

- `exchange_endpoints`
- `exchange_payload_parsing`

The purpose is to avoid a standalone endpoint micro leaf while still extracting the exchange-specific surface from the data-module parent.

## Three Matrix Impact

| Matrix | Impact |
| --- | --- |
| Process matrix | Uses `same_parent_wave` under `recursive-high-speed-v2` to batch endpoint and parser evidence. |
| Standard matrix | Preserves parent-child communication through `data_module`; no sibling horizontal link or release transition. |
| Guidance matrix | Adds one exchange surface white-box child while keeping child-level evidence rows for endpoint and parser concerns. |

## Wave Child Rows

| child | baseline boundary | write set | movement | equivalence gate | closeout decision | residuals |
| --- | --- | --- | --- | --- | --- | --- |
| `exchange_endpoints` | Provider key, data endpoint, ping endpoint, OKX/Binance endpoint construction, OKX instrument id/bar helpers. | `qrpc_runtime/src/data_module.rs`, planned child file, governance docs. | Move endpoint helpers to exchange surface child. | qrpc-runtime tests plus quantpilot check. | Evaluate after extraction; expected `stop_split: true`. | Fetch/transport and cache stay parent-owned. |
| `exchange_payload_parsing` | OKX candle/ticker parsing, Binance kline parsing, OKX status guard, numeric field coercion. | `qrpc_runtime/src/data_module.rs`, planned child file, governance docs. | Move payload parser helpers to same exchange surface child. | Existing parser unit tests plus qrpc-runtime tests. | Evaluate after extraction; expected `stop_split: true`. | Normalization and mock data stay parent-owned. |

## Leaf Granularity Smart Judge

| Child | Score Result |
| --- | --- |
| `exchange_endpoints` | `WAVE`; the helper pocket is too small for standalone governance but provides value when batched with parsers. |
| `exchange_payload_parsing` | `WAVE`; parser helpers are cohesive with endpoint exchange contracts and share the same evidence surface. |

The wave resolves to `SPLIT` at the parent level because moving exchange-specific helpers out of the parent reduces `data_module.rs` residual size and gives later normalization/cache/mock leaves cleaner boundaries.

## Frozen Scope

Allowed in the next movement:

- add an exchange surface child module;
- move `OKX_BASE_URL`;
- move `BINANCE_BASE_URL`;
- move `provider_key_for_source`;
- move `endpoint_for_source`;
- move `ping_endpoint_for_source`;
- move `binance_endpoint_for_source`;
- move `okx_endpoint_for_source`;
- move `okx_inst_id`;
- move `okx_spot_symbol`;
- move `okx_bar`;
- move `parse_okx_candles`;
- move `parse_okx_ticker`;
- move `parse_binance_klines`;
- move `ensure_okx_success`;
- move numeric parser helpers used only by payload parsing.

Explicitly excluded:

- `fetch_and_normalize`;
- `probe_ping`;
- `fetch_okx`;
- `fetch_json`;
- `fetch_json_via_powershell`;
- `fetch_historical_raw_klines`;
- `load_*cache`;
- `persist_*cache`;
- `normalize_kline_series`;
- `normalize_quote`;
- `mock_raw_*`;
- `mock_data`;
- `DataModuleProvider` and DTOs;
- provider collection orchestration;
- tests except imports needed to keep existing tests compiling.

## Equivalence Surface

The extraction must preserve:

- provider key mapping;
- OKX and Binance endpoint strings;
- ping endpoint strings;
- OKX instrument id formatting;
- OKX bar interval mapping;
- OKX success/error validation;
- candle/ticker raw payload parsing;
- Binance kline raw payload parsing;
- parser unit test behavior.

## Gates

The next movement must pass:

- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-001ZG-02 `root.contracts.runtime_support.data_module.exchange_surface_wave` actual extraction.
