# v4.16.0 root.contracts.quantscript.typed_resolution parent residual judgment: resolver_orchestration_surface

> Batch: BE-002GK-01
> Node: `root.contracts.quantscript.typed_resolution`
> Selected child: `root.contracts.quantscript.typed_resolution.resolver_orchestration_surface`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `root.contracts.quantscript.typed_resolution.resolver_orchestration_surface` for the next recursive child.

## Current Parent State

Closed typed-resolution children:

- `root.contracts.quantscript.typed_resolution.public_type_surface`
- `root.contracts.quantscript.typed_resolution.callable_classification_surface`
- `root.contracts.quantscript.typed_resolution.semantic_inference_surface`
- `root.contracts.quantscript.typed_resolution.type_inference_binding_surface`

Current parent file:

- `quantscript/src/resolve/mod.rs`

The parent now mainly owns resolver orchestration, AST-to-HIR lowering, scalar support helpers, resolved function projection, return-list helpers, and resolver-local tests.

## Residual Candidate Review

| Candidate | Evidence | Decision |
|-----------|----------|----------|
| `resolver_orchestration_surface` | Cohesive orchestration block from `resolve_module` through `lower_callee_expr`; owns module import/function/test-block lowering, function body lowering, statement lowering, match arm lowering, expression lowering, call callee lowering, and typed-HIR assembly handoff. | Select |
| `resolver_support_surface` | `make_expr`, `expr_label`, ID allocation, callable target helpers, scalar expression helpers, resolved function projection, and return-list helpers are support residuals; keep queued until orchestration is thinner. | Queue |
| `resolver_test_harness` | Tests should move only after the production resolver parent pockets are reduced and the support surface is clearer. | Queue |

## Selected Child Scope

The next baseline may freeze only resolver orchestration and AST-to-HIR lowering:

- `resolve_module`
- `seed_imported_callables`
- `seed_function_signatures`
- `resolve_function`
- `lower_block`
- `lower_stmt`
- `lower_match_arm`
- `lower_expr`
- `lower_callee_expr`

Likely child file:

- `quantscript/src/resolve/resolver_orchestration_surface.rs`

## Forbidden Movement

The next baseline and extraction must not move:

- public resolver DTOs/enums already closed in `public_type_surface`;
- callable/helper classifiers and registry seeding already closed in `callable_classification_surface`;
- semantic/manual indicator inference already closed in `semantic_inference_surface`;
- type inference, binding insertion, condition validation, and type annotation resolution already closed in `type_inference_binding_surface`;
- `make_expr`, `expr_label`, ID allocation, callable target helpers, scalar expression helpers, resolved function builder, return-list helpers, and resolver-local tests;
- analysis, lowering, evaluator, runtime, backend, executor, frontend, plugin metadata, or release transition.

## Split Judgment

This is not a terminal split decision. The selected child is expected to need a baseline first because it is behavior-bearing and still interacts with parent-owned support helpers and closed helper children.

## Next Step

BE-002GL-01 `root.contracts.quantscript.typed_resolution.resolver_orchestration_surface` baseline_plan.
