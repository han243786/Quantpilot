# v4.16.0 root.contracts.core_ir.v4_contracts.version_manifest actual extraction complete

> Batch: BE-001VM-02
> Node: `root.contracts.core_ir.v4_contracts.version_manifest`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `extract_closeout`
> Movement: Code movement completed.

---

## Summary

`root.contracts.core_ir.v4_contracts.version_manifest` actual extraction is complete.

Implemented movement:

- `qrpc_core_ir/src/v4.rs` now declares `mod version_manifest;`
- `qrpc_core_ir/src/v4.rs` re-exports the child through `pub use version_manifest::*;`
- `qrpc_core_ir/src/v4/version_manifest.rs` owns `V4VersionManifest`, its default construction, and
  `V4VersionManifest::validate_static_contract`
- `default_version_manifest_version` and `default_qs_language_version` moved into the child
- the child imports parent/sibling version default helpers without moving machine, QS type-system, or venue
  implementations

Equivalence notes:

- public version manifest surface remains unchanged through the parent re-export
- version defaults, compatibility flags, and validation error strings remain frozen from BE-001VM-01
- schema identity constants, QS type-system implementation, machine contract implementation, venue capability matrix
  implementation, plugin governance, reproducibility, complexity budget, developer learning pipeline, static bundles,
  compile-time capability reports, compatibility bridge, bridge builders, tests, machine graph contract, QS profile, and
  runtime mode contract remain outside this child

Verification:

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir` (54 passed)
- `cargo check -p quantpilot`

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs,qrpc_core_ir/src/v4/version_manifest.rs`

**Markers**:
- `BE-001VM-02`

**Next step**:
BE-001VM-03 root.contracts.core_ir.v4_contracts.version_manifest single_leaf_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
