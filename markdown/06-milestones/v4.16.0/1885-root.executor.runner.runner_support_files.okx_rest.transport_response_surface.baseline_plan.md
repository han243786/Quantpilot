# v4.16.0 root.executor.runner.runner_support_files.okx_rest.transport_response_surface baseline plan

> Batch: BE-002LV-01
> Node: `root.executor.runner.runner_support_files.okx_rest.transport_response_surface`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Goal

Freeze the OKX REST transport and response-handling boundary before Rust
movement.

The first movement may extract transport/response helpers from
`src-executor/okx_rest.rs` into:

`src-executor/okx_rest/transport_response_surface.rs`

## Frozen Boundary

| Field | Value |
| --- | --- |
| Planned module | `src-executor/okx_rest/transport_response_surface.rs` |
| Approved helper surface | proxy URL selection, `ureq::Agent` timeout/proxy construction, raw signed request dispatch, signed request dispatch with OKX success check, OKX top-level/per-order success validation |
| Parent callers | order/query/cancel/balance wrappers and ignored smoke tests |
| Parent retained | public types/constants facade, signing request child, order action wrappers, lookup validation, smoke test orchestration |

## Equivalent Behavior

The extracted child must preserve:

- REST proxy environment precedence;
- connect/read/write timeout values;
- status responses being parsed as OKX JSON payloads;
- transport errors remaining errors;
- top-level `code != "0"` rejection behavior;
- first-item `sCode != "0"` rejection behavior.

## Exclusions

- Do not move order/query/cancel/balance action wrappers.
- Do not move lookup path validation helpers.
- Do not move signing request helpers.
- Do not introduce release-transition links.

## Gates For Actual Extraction

| Gate | Command |
| --- | --- |
| format | `cargo fmt --all --check` |
| executor compile | `cargo check -p quantpilot --bin executor` |
| focused tests | `cargo test -p quantpilot --bin executor okx_` |
| diff hygiene | `git diff --check` |

## Next Step

BE-002LV-02
`root.executor.runner.runner_support_files.okx_rest.transport_response_surface`
actual_extraction.
