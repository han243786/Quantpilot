# v4.16.0 root.contracts.runtime_support.data_module.normalization extract closeout

> Batch: BE-001ZI-02
> Node: `root.contracts.runtime_support.data_module.normalization`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> Parent: `root.contracts.runtime_support.data_module`
> Baseline: `1371-root.contracts.runtime_support.data_module.normalization.baseline_plan.md`

---

## Summary

The `normalization` actual extraction is complete.

`qrpc_runtime/src/data_module/normalization.rs` now owns raw-to-normalized market data conversion:

- `normalize_kline_series`;
- `normalize_quote`;
- `quote_snapshot_from_price`.

The parent `qrpc_runtime/src/data_module.rs` keeps orchestration, fetch/cache, transport, exchange surface, mock data generation, historical cache, quality diagnostics, source mapping, and tests.

## Three Matrix Impact

| Matrix | Impact |
| --- | --- |
| Process matrix | Executes the frozen normalization baseline as one verifiable Rust step. |
| Standard matrix | Keeps parent-child communication through `data_module`; no sibling horizontal link or release transition. |
| Guidance matrix | Adds the concrete normalization child file to the full tree and module tree. |

## Extracted Surface

New child file:

- `qrpc_runtime/src/data_module/normalization.rs`

Parent facade updates:

- `mod normalization;`
- parent imports `normalize_kline_series` and `normalize_quote`;
- parent re-exports `quote_snapshot_from_price` as `pub(crate)` to preserve the old crate-visible path;
- no public trait, DTO, route, schema, persistence, or lock owner change.

## Preserved Boundaries

Still excluded:

- raw OKX/Binance parser helpers in `exchange_surface`;
- mock raw Kline/Quote generation;
- `mock_kline_bars_for_backtest`;
- historical cache load/persist/fetch helpers;
- HTTP transport helpers;
- in-memory cache helpers;
- `BuiltinDataModule::fetch_and_normalize`;
- tests.

## Equivalence Surface

The extraction preserves:

- OHLC validity filtering;
- clamped high/low warning behavior;
- `NormalizedKline` field mapping;
- `KlineSeriesSnapshot` metadata and `DataQualitySnapshot::default`;
- `QuoteSnapshot` bid/ask/mid/size/latency/status mapping;
- `quote_snapshot_from_price` bid/ask spread and healthy source status;
- parent call-site behavior.

## Gates

Required before commit:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-001ZI-03 `root.contracts.runtime_support.data_module.normalization` single_leaf_closeout.
