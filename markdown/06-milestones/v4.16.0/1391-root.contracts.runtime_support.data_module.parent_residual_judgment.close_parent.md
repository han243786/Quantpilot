# v4.16.0 root.contracts.runtime_support.data_module parent residual judgment close parent

> Batch: BE-001ZR-01
> Node: `root.contracts.runtime_support.data_module`
> Stage: `parent_residual_judgment`
> Decision: `parent_closeout: true`
> Movement: No Rust code movement.

---

## Parent Closeout Decision

`root.contracts.runtime_support.data_module` is closed.

All planned data-module children are closed:

- `contracts.runtime_support.data_module.source_mapping`
- `contracts.runtime_support.data_module.quality_diagnostics`
- `contracts.runtime_support.data_module.collection_orchestration`
- `contracts.runtime_support.data_module.exchange_surface_wave`
- `contracts.runtime_support.data_module.exchange_endpoints`
- `contracts.runtime_support.data_module.exchange_payload_parsing`
- `contracts.runtime_support.data_module.normalization`
- `contracts.runtime_support.data_module.mock_data_generation`
- `contracts.runtime_support.data_module.historical_cache`
- `contracts.runtime_support.data_module.http_transport`
- `contracts.runtime_support.data_module.test_harness`

## Parent Residual Shape

The remaining parent file `qrpc_runtime/src/data_module.rs` is a compact facade and orchestration owner.

It keeps:

- `DataCollectionRequest` and `DataCollectionOutput`;
- `DataModuleProvider`;
- `FetchDiagnostics`;
- `BuiltinDataModule` state;
- parent-owned `reqwest::Client`, in-memory cache, and circuit breaker;
- `fetch_and_normalize` orchestration;
- request-interval cache and stale-cache fallback orchestration;
- parent-mediated imports/re-exports for child communication;
- constants shared by children.

## Why No New Child Is Opened

| Candidate | Decision |
| --- | --- |
| request cache | Keep in parent: it depends on parent-owned `BuiltinDataModule.cache`, diagnostic composition, and fallback orchestration. |
| breaker decision logic | Keep in parent: it is part of provider orchestration around transport/cache/mock fallback. |
| provider facade | Keep in parent: this is the parent node's purpose after child extraction. |
| constants | Keep in parent: children use these through the parent boundary. |

Opening new leaves here would produce small, high-governance-cost fragments and weaken the parent facade.

## Equivalence Evidence

The immediately preceding data-module extraction steps passed:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Cursor Movement

Add `contracts.runtime_support.data_module` to `closed_children`.

Return to `root.contracts.runtime_support` parent residual judgment. Next candidate: `contracts.runtime_support.parent_closeout`.
