# v4.16.0 root.contracts.core_ir.v1_contract.test_fixture baseline plan

> Batch: BE-001TQ-01
> Node: `root.contracts.core_ir.v1_contract.test_fixture`
> Parent: `root.contracts.core_ir.v1_contract`
> Stage: `baseline_plan`
> Movement: No code movement.

---

## Summary

This baseline freezes the local cfg-test round-trip fixture currently embedded in `qrpc_core_ir/src/v1.rs`.

The next implementation step may move only the cfg-test module and its test-only `BTreeMap` import into `qrpc_core_ir/src/v1/tests.rs`.

## Frozen Surface

| Surface | Frozen owner for next step |
| --- | --- |
| `#[cfg(test)] use std::collections::BTreeMap` | `test_fixture` |
| `core_ir_round_trips` | `test_fixture` |

## Frozen Test Behavior

| Assertion path | Frozen behavior |
| --- | --- |
| sample root graph | Builds `CoreStrategyIr::new` with sample metadata and execution rule. |
| sample children | Pushes data binding, indicator, signal rule, agent policy, and risk policy into the root graph. |
| serialization proof | Serializes and deserializes the sample graph with `serde_json`. |
| identity proof | Asserts decoded `ir_version` equals `CORE_IR_V1_VERSION` and decoded graph equals the original graph. |

## Allowed Movement

BE-001TQ-02 may:

- create `qrpc_core_ir/src/v1/tests.rs`;
- replace the inline test module in `qrpc_core_ir/src/v1.rs` with `#[cfg(test)] mod tests;`;
- move the test-only `BTreeMap` import into the test child;
- keep test access through `use super::*`.

## Forbidden Movement

BE-001TQ-02 must not:

- move production DTOs or helper code;
- change sample values, assertions, serde behavior, or facade exports;
- alter root graph, expression, or policy child files;
- change v4 contracts, qrpc-core contracts, compiler/runtime/backend/executor/frontend behavior;
- introduce release-transition wiring.

## Parent-Child Communication

The v1 parent remains the test gateway. The cfg-test child must use `super::*` and must not import sibling children directly.

## Equivalence Baseline

| Check | Baseline |
| --- | --- |
| Current file | `qrpc_core_ir/src/v1.rs` owns the cfg-test residual. |
| Current proof | `cargo test -p qrpc-core-ir` passes before extraction. |
| Required post-extraction proof | `cargo test -p qrpc-core-ir` must pass after extraction. |

## Next Step

BE-001TQ-02 `root.contracts.core_ir.v1_contract.test_fixture` extract_closeout.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
