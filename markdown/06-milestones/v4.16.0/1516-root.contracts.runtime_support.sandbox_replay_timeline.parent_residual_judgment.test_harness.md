# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline parent residual judgment: test_harness

> Batch: BE-002DC-01
> Node: `root.contracts.runtime_support.sandbox_replay_timeline`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.

---

## Decision

Select next child:

- `root.contracts.runtime_support.sandbox_replay_timeline.test_harness`

## Closed Children

- `timeline_data_providers`
- `unified_timeline`
- `replay_builder`
- `sandbox_mode_surface`
- `realtime_sandbox`
- `fast_backtest_sandbox`

## Residual Surface

The sandbox parent still owns local sandbox tests:

- realtime sandbox session/snapshot tests
- fast backtest pre-start rejection test
- market update event test
- ignored long-form fast backtest replay/latency tests
- runtime support boundary test
- sample runtime protocol fixture

## Parent-Owned Helpers

The following helpers stay parent-owned and are not selected as child leaves:

- `snapshot_from`
- `ensure_running`
- `trace_id`

They are shared by concrete sandbox children and remain a compact parent coordination surface.

## Next Step

BE-002DD-01 `root.contracts.runtime_support.sandbox_replay_timeline.test_harness` baseline_plan.
