# v4.16.0 root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.entrypoint_runtime_config_assembly extract closeout

> Batch: BE-002IL-02
> Node: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.entrypoint_runtime_config_assembly`
> Parent: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface`
> Stage: `actual_extraction`
> Movement: Production Rust movement.
> governance_next_authority: active

---

## Extraction Summary

`entrypoint_runtime_config_assembly` has been extracted into:

- `quantscript/src/lowering/orchestrator/entrypoint_runtime_config_assembly.rs`

The parent `quantscript/src/lowering/orchestrator.rs` now acts as a public facade for:

- `lower_script_to_runtime_config`
- `lower_script_to_runtime_config_with_context`

## Symbols Moved

Moved into the child:

- private runtime config assembly behind `lower_script_to_runtime_config_with_context`
- `ERR_MISSING_STRATEGY_FN`
- `ERR_NO_FETCH_CALLS`
- assembly-only imports for analysis, normalization, typed HIR lowering, data source inference, bindings, intents, profile detection, universe expansion, diagnostics, `AgentConfig`, `RiskConfig`, and runtime defaults

The child function is parent-visible only through `pub(super)`.

## Parent Boundary Kept

The parent remains owner for:

- public lowering entrypoint signatures
- public facade delegation
- `integration_test_harness` test module declaration

Public re-exports in `quantscript/src/lowering/mod.rs` are unchanged.

## Validation

Passed after extraction:

- `cargo fmt --check`
- `cargo test -p quantscript` (88 tests)
- `cargo check -p quantpilot`
- `git diff --check`

## Next Step

BE-002IL-03 single_leaf_closeout.
