# v4.16.0 root.contracts.runtime_support.risk_support.event_payload_projection single leaf closeout

> Batch: BE-002BS-02
> Node: `root.contracts.runtime_support.risk_support.event_payload_projection`
> Parent: `root.contracts.runtime_support.risk_support`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Decision

`root.contracts.runtime_support.risk_support.event_payload_projection` is closed with `stop_split: true`.

## Leaf Granularity Result

| Metric | Value |
| --- | --- |
| LOC | 150 |
| Functions | 5 |
| Public surface | 1 `pub(super)` helper |
| Branches | 5 |
| Domains | 4 |
| Score | 34 |
| Decision | STOP |
| Governance mode | `stop_split` |

## Rationale

The child is a compact projection helper group. Splitting reason-code naming, explanation summaries, action stats, target-weight stats, and payload construction would create tiny leaves with higher governance cost and no clearer parent interface.

The single `pub(super)` entrypoint keeps parent-mediated access while preserving event payload shape.

## Preserved Boundaries

- provider/evaluate surface remains parent-owned;
- risk decision mutation remains parent-owned;
- direction and cross-symbol constraints remain outside this child;
- action and portfolio target clamps remain outside this child;
- exposure math helpers remain parent-owned;
- tests remain outside this child;
- no sibling shortcut exists.

## Next Step

BE-002BT-01 `root.contracts.runtime_support.risk_support` parent_residual_judgment selects `direction_cross_constraints`.
