# v4.16.0 root.contracts.runtime_support.v4_runtime_support parent residual judgment - type_surface

> Batch: BE-002DI-01
> Node: `root.contracts.runtime_support.v4_runtime_support`
> Selected child: `root.contracts.runtime_support.v4_runtime_support.type_surface`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.

---

## Decision

Select `root.contracts.runtime_support.v4_runtime_support.type_surface` as the first v4 runtime support child.

## Evidence

The BE-002DH-01 baseline child queue starts with `type_surface`. This is the correct first child because `qrpc_runtime/src/v4_runtime_types.rs` currently supplies the public DTO/constant surface through `include!("v4_runtime_types.rs")` and crate-root re-exports from `qrpc_runtime/src/lib.rs`.

The candidate owns:

- v4 runtime input and backtest input DTOs;
- runtime event envelope/origin DTOs;
- runtime output and memory snapshot DTOs;
- machine/risk/execution snapshot and decision DTOs;
- simulated execution config, request, order, fill, position, asset point, and snapshot DTOs;
- simulated order/action/status enums;
- venue adapter runtime boundary DTO;
- public v4 execution event constants;
- `V4Runtime` type alias and `V4PaperSimulatedRuntime` type definition.

## Boundary

The next baseline must freeze only the v4 type/constant surface before movement. It must not move:

- graph symbol expansion helpers;
- machine family initialization helpers;
- runtime constructors and mode gates;
- event replay orchestration;
- machine transition engine;
- risk/execution capability gates;
- simulated execution engine behavior;
- v4 tests;
- live execution, plugin runtime, ops compatibility, metrics, QuantScript, plugin metadata, executor, backend, frontend, or release-transition optimization.

## Dependency Rule

The type surface child may expose DTOs through the v4 runtime support parent and crate-root re-export surface. It must not directly own runtime behavior or call sibling runtime-support modules.

## Next Step

BE-002DJ-01 `root.contracts.runtime_support.v4_runtime_support.type_surface` baseline_plan.
