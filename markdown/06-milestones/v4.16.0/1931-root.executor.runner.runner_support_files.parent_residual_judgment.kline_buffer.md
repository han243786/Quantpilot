# v4.16.0 root.executor.runner.runner_support_files parent residual judgment selects kline_buffer

> Batch: BE-002MT-01
> Parent: `root.executor.runner.runner_support_files`
> Stage: `parent_residual_judgment`
> Selected child: `root.executor.runner.runner_support_files.kline_buffer`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `kline_buffer` as the next child under `runner_support_files`.

## Evidence

Closed children:

- `okx_rest`
- `live_runner`
- `ws_client`

Remaining cohesive groups:

- `kline_buffer`
- `migration_api`

`kline_buffer` is already physically isolated in `src-executor/kline_buffer.rs`
and owns `KlinePool`, symbol buffer creation, ticker-to-bar projection, OHLCV
merge behavior, recent-bar projection, and latest-price lookup.

## Boundary

The next baseline may freeze movement for `src-executor/kline_buffer.rs` only.
It must not move `migration_api` or runner modules in the same step.

## Next Step

BE-002MU-01
`root.executor.runner.runner_support_files.kline_buffer`
baseline_plan.
