# v4.16.0 root.contracts.quantscript.runtime_lowering.intent_inference.spread_intent_inference single leaf closeout

> Batch: BE-002JB-03
> Node: `root.contracts.quantscript.runtime_lowering.intent_inference.spread_intent_inference`
> Parent: `root.contracts.quantscript.runtime_lowering.intent_inference`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Closeout Decision

`spread_intent_inference` is closed as a terminal child and sets `stop_split: true`.

The extracted child is inside the terminal target size range and has one parent-facing entry for spread intent construction. Its internal helpers are cohesive around one output owner: formal spread matching, operand decoding, params construction, and resample/align parsing. Splitting those helpers again would add extra parent-child forwarding and documentation cost without creating a stronger independent owner.

## Leaf Split Decision Result

Command:

- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -LeafId root.contracts.quantscript.runtime_lowering.intent_inference.spread_intent_inference -ParentId root.contracts.quantscript.runtime_lowering.intent_inference -Path quantscript/src/lowering/intents/spread_intent_inference.rs -Depth 5`

Result:

| Field | Value |
| --- | --- |
| `split_decision` | `STOP` |
| `governance_packaging` | `stop_split` |
| `final_decision` | `STOP` |
| `normalized_split_score` | 43 |
| `loc` | 464 |
| `function_count` | 16 |
| `public_surface` | 1 |
| `branches` | 38 |
| `risk_tags` | `cross_crate`, `public_api`, `trading_semantics` |
| `reason` | medium score without same-parent wave candidate; avoid standalone governance |

## Boundary Kept

The parent `quantscript/src/lowering/intents.rs` remains the mediator for:

- public(crate) intent inference facades;
- condition-to-intent dispatch;
- MA ratio and legacy emit builders;
- data-source canonicalization;
- inferred agent param projection;
- comparison, relation, and id helper functions.

The child does not introduce sibling horizontal links and only calls parent-owned helpers through the parent module boundary.

## Validation

No Rust movement in this closeout step. The preceding extraction was validated by:

- `cargo fmt --check`
- `cargo test -p quantscript` (88 tests)
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8, full feature tree, and matrix governance checks

## Next Step

Return to `root.contracts.quantscript.runtime_lowering.intent_inference` parent residual judgment.
