# v4.16.0 root.contracts.quantscript.typed_resolution.resolver_orchestration_surface baseline plan

> Batch: BE-002GL-01
> Node: `root.contracts.quantscript.typed_resolution.resolver_orchestration_surface`
> Parent: `root.contracts.quantscript.typed_resolution`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Scope

Freeze resolver orchestration and AST-to-HIR lowering before movement.

Current owner:

- `quantscript/src/resolve/mod.rs`

Planned child:

- `quantscript/src/resolve/resolver_orchestration_surface.rs`

## Items Frozen For Movement

The next extraction may move only:

- `resolve_module`
- `seed_imported_callables`
- `seed_function_signatures`
- `resolve_function`
- `lower_block`
- `lower_stmt`
- `lower_match_arm`
- `lower_expr`
- `lower_callee_expr`

## Parent Wiring To Preserve

The parent resolver module must keep:

- `lower_script_to_typed_hir` public entrypoint unchanged;
- `Resolver` state ownership unchanged;
- public DTO and enum re-exports unchanged;
- closed child module calls to callable classification, semantic inference, and type inference unchanged;
- support helpers available to the moved orchestration methods through the parent/child boundary;
- diagnostics, type arena mutation, expression semantic recording, and typed-HIR shape unchanged;
- downstream `crate::resolve::*` compatibility unchanged.

## Forbidden Movement

The next extraction must not move or change:

- public resolver DTOs/enums already closed in `public_type_surface`;
- callable/helper classifiers and registry seeding already closed in `callable_classification_surface`;
- semantic/manual indicator inference already closed in `semantic_inference_surface`;
- type inference, binding insertion, condition validation, and type annotation resolution already closed in `type_inference_binding_surface`;
- `make_expr`, `expr_label`, `alloc_def_id`, `alloc_expr_id`, `callable_target`, and `call_target_return_type`;
- `expr_number`, `expr_integer`, `series_index_view_kind`, `series_window_span`, `build_resolved_functions`, `first_return_expr`, and `returned_list_target`;
- resolver-local tests;
- analysis, lowering, evaluator, runtime, backend, executor, frontend, plugin metadata, or release transition.

## Expected Validation

The extraction must pass:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002GL-02 `root.contracts.quantscript.typed_resolution.resolver_orchestration_surface` actual_extraction.
