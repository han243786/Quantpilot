# v4.16.0 root.contracts.runtime_support.v4_runtime_support.test_harness.simulated_execution_scenarios single leaf closeout

> Batch: BE-002FK-02
> Node: `root.contracts.runtime_support.v4_runtime_support.test_harness.simulated_execution_scenarios`
> Parent: `root.contracts.runtime_support.v4_runtime_support.test_harness`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Set `stop_split: true` for `simulated_execution_scenarios`.

The child is inside the target terminal size band and has one clear owner: paper-simulated execution scenario coverage. Further splitting would separate order lifecycle variants and replay metrics into smaller children that still share the same runtime setup, simulated execution state, event expectations, and fixture access.

## Leaf Granularity Evidence

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -Path qrpc_runtime\src\v4_runtime\test_harness\simulated_execution_scenarios.rs
```

Result:

| Field | Value |
| --- | --- |
| `split_decision` | `STOP` |
| `governance_packaging` | `stop_split` |
| `final_decision` | `STOP` |
| Score | `34` |
| LOC | `444` |
| Functions | `9` |
| Public/internal-visible surface | `0` |
| Branches | `2` |
| Domains | `8` |
| Risk tags | `lock`, `persistence`, `route_schema`, `state_machine`, `trading_semantics` |
| Terminal size bucket | `terminal_target_150_600` |

## Stop-Split Rationale

Rejected finer splits:

| Candidate | Decision | Reason |
| --- | --- | --- |
| happy path / fill accounting tests | Reject | These tests establish the same paper-simulated execution baseline used by order lifecycle variants. |
| conditional order lifecycle tests | Reject | Stop, OCO, trailing, GTD, and amend variants share request construction and simulated execution state ownership. |
| tick replay metrics test | Reject | The test still exercises the same simulated execution replay surface and is not large enough to justify standalone governance. |

## Evidence

Inherited from BE-002FK-01 extraction:

- `cargo fmt --check`: PASS
- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime v4 -- --nocapture`: PASS, 32 tests
- `cargo check -p quantpilot`: PASS

This closeout is docs-only and updates the recursive cursor plus governance indexes.

## Next Step

BE-002FL-01 `root.contracts.runtime_support.v4_runtime_support.test_harness` parent_residual_judgment.
