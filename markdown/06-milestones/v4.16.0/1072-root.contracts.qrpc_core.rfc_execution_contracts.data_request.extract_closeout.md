# v4.16.0 root.contracts.qrpc_core.rfc_execution_contracts.data_request extract closeout

> Batch: BE-001SS-02
> Node: `root.contracts.qrpc_core.rfc_execution_contracts.data_request`
> Parent: `root.contracts.qrpc_core.rfc_execution_contracts`
> Stage: `extract_closeout`
> Movement: Rust physical extraction complete.

---

## Summary

`data_request` now owns the RFC-001 data request taxonomy and DTO family that was previously embedded in `qrpc_core/src/rfc_execution_contracts.rs`.

Moved file:

- `qrpc_core/src/rfc_execution_contracts/data_request.rs`

Updated parent facade:

- `qrpc_core/src/rfc_execution_contracts.rs`

## Actual Extraction

| Baseline item | Result |
| --- | --- |
| `MarketScope` | Moved into `data_request.rs`. |
| `PrimaryDataType` | Moved into `data_request.rs`. |
| `SourceType` | Moved into `data_request.rs`. |
| `Timeframe` | Moved into `data_request.rs`. |
| `TimeRange` | Moved into `data_request.rs`. |
| `RoundingMode` | Moved into `data_request.rs`. |
| `PrecisionPolicy` | Moved into `data_request.rs`. |
| `UsageTag` | Moved into `data_request.rs`. |
| `DataRequest` | Moved into `data_request.rs`. |
| Parent facade | Added `mod data_request;` and `pub use data_request::*;`. |

## Equivalence Evidence

| Check | Result | Evidence |
| --- | --- | --- |
| Public facade preserved | pass | `qrpc_core/src/rfc_execution_contracts.rs` re-exports `data_request::*`, and `qrpc_core/src/lib.rs` still re-exports RFC execution contracts. |
| Closed sibling boundaries preserved | pass | Allocation, order, execution feedback, and handoff code stayed in the RFC execution parent. |
| Data request surface preserved | pass | The moved file contains the frozen taxonomy enums, range/precision DTOs, usage tag, and `DataRequest`. |
| Parent-child communication | pass | The child imports only serde and the public `Symbol` type through the crate facade. |

## Validation Results

- `cargo fmt -p qrpc-core` passed.
- `cargo check -p qrpc-core` passed.
- `cargo test -p qrpc-core` passed: 12 unit tests passed; doc tests passed with 0 tests.

## Hard Boundaries

This extraction did not:

- change enum variants, struct fields, derives, serde shape, public exports, or tests;
- move allocation, order, feedback, or handoff contracts;
- reopen closed qrpc-core child modules;
- change `qrpc_core_ir`, `qrpc_compiler`, `qrpc_runtime`, `quantscript`, backend, executor, frontend, physical `plugins/*`, or release transition;
- introduce direct sibling shortcuts.

## Next Step

BE-001SS-03 `root.contracts.qrpc_core.rfc_execution_contracts.data_request` single_leaf_closeout.

The closeout must decide whether the RFC-001 taxonomy/DTO family should stop as one compact schema leaf or split further into taxonomy subleaves.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
