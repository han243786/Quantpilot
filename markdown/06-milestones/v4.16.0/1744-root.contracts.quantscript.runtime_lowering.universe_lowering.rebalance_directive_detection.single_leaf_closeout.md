# v4.16.0 root.contracts.quantscript.runtime_lowering.universe_lowering.rebalance_directive_detection single leaf closeout

> Batch: BE-002IQ-03
> Node: `root.contracts.quantscript.runtime_lowering.universe_lowering.rebalance_directive_detection`
> Parent: `root.contracts.quantscript.runtime_lowering.universe_lowering`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`rebalance_directive_detection` is closed with `stop_split: true`.

## Leaf Granularity Evidence

Command:

- `tools/evaluate-leaf-granularity.ps1 -LeafId root.contracts.quantscript.runtime_lowering.universe_lowering.rebalance_directive_detection -ParentId root.contracts.quantscript.runtime_lowering.universe_lowering -Path quantscript/src/lowering/universe/rebalance_directive_detection.rs -Depth 5`

Result:

- `Decision: STOP`
- `Split decision: STOP`
- `Governance packaging: stop_split`
- `Score: 38`
- `loc=373`
- `functions=8`
- `public_surface=2`
- `branches=40`
- `domains=4`
- `risk_tags=cross_crate,public_api,state_machine,trading_semantics`
- `size_bucket=terminal_target_150_600`
- `governance_mode=stop_split`

## Rationale

The child owns one cohesive rebalance directive detection surface: strategy traversal, rebalance call recognition, schedule parsing, allocation parser variants, fixed-weight validation, and `PortfolioRebalanceDirective` assembly. It is inside the preferred terminal leaf range and communicates with the parent through a small facade plus parent-owned universe expression evaluation.

Further splitting allocation variants or schedule parsing would create narrow semantic leaves while increasing parent-child calls and governance cost.

## Parent Residual

Return to `root.contracts.quantscript.runtime_lowering.universe_lowering` parent residual judgment.

Remaining candidate groups:

- `universe_construct_expansion`
- `universe_expression_evaluation`
- `universe_metadata_selection`
- `universe_test_harness`

## Next Step

BE-002IR-01 `root.contracts.quantscript.runtime_lowering.universe_lowering` parent_residual_judgment.
