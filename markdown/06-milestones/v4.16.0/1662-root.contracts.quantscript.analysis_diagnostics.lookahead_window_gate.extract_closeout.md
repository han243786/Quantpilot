# v4.16.0 root.contracts.quantscript.analysis_diagnostics.lookahead_window_gate extract closeout

> Batch: BE-002GW-02
> Node: `root.contracts.quantscript.analysis_diagnostics.lookahead_window_gate`
> Parent: `root.contracts.quantscript.analysis_diagnostics`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> governance_next_authority: active

---

## Result

`root.contracts.quantscript.analysis_diagnostics.lookahead_window_gate` actual extraction is complete.

New child:

- `quantscript/src/analysis_diagnostics/lookahead_window_gate.rs`

Parent wiring:

- `quantscript/src/analysis_diagnostics/mod.rs` declares `mod lookahead_window_gate;`.
- `quantscript/src/analysis_diagnostics/analysis.rs` imports `collect_series_index_diagnostics` and `collect_centered_window_diagnostics` from the child.
- `analyze_script_module` keeps the same diagnostic invocation order.

## Moved Surface

Moved into the child:

- `collect_series_index_diagnostics`
- `collect_series_index_from_stmts`
- `collect_series_index_from_expr`
- `collect_centered_window_diagnostics`
- `collect_centered_window_from_stmts`
- `collect_centered_window_from_expr`
- `centered_window_uses_future_bars`
- `arg_bool_named`
- `expr_integer`

## Preserved Boundary

Preserved:

- diagnostic codes `QS0401`, `QS0402`, and `QS0403`;
- negative index, trailing-window, and centered-window semantics;
- `ScriptAnalysis` and `analyze_script_module` behavior;
- crate-root public exports and internal compatibility aliases;
- warmup, fetch lookback, strategy presence, indirect recursion, index bounds, dead-code emit, symbol whitelist, diagnostic DTOs, evaluator normalization, runtime lowering, static audit, tests, samples, backend, executor, frontend, plugin metadata, and release transition outside this child.

## Validation

Executed for this extraction:

- `cargo fmt --check` PASS
- `cargo test -p quantscript` PASS, 88 tests

Remaining closeout gates must pass before commit:

- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002GW-03 `root.contracts.quantscript.analysis_diagnostics.lookahead_window_gate` single_leaf_closeout.
