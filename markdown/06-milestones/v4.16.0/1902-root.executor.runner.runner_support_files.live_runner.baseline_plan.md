# v4.16.0 root.executor.runner.runner_support_files.live_runner baseline plan

> Batch: BE-002ME-01
> Node: `root.executor.runner.runner_support_files.live_runner`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Goal

Freeze `src-executor/live_runner.rs` as the next recursive parent before its
internal child queue is selected.

## Frozen Parent Boundary

| Field | Value |
| --- | --- |
| Parent file | `src-executor/live_runner.rs` |
| Parent role | live strategy runner orchestration for V3 and V4 execution paths |
| Public surface | `LiveRunner`, `RunnerStatus`, `RunnerPool`, `RunnerInstance`, `V4Runner`, `V4ExecutorEvidenceEvent` |
| External inputs | `ActiveStrategy`, `ExecutionMode`, `RuntimeKind`, `TriggerEvent`, `WsEvent`, `KlinePool`, runtime coordinator outputs |
| External outputs | runner status updates, trigger broadcasts, V4 evidence broadcasts, websocket event dispatch |

## Initial Child Queue

- `v3_live_runner`
- `runner_instance_dispatch`
- `v4_runner`
- `runner_pool_orchestration`
- `v4_market_metadata_helpers`
- `test_harness`

## Equivalent Behavior

Future extraction under this parent must preserve:

- V3 fast/slow cycle rate limit and daily order count behavior;
- V3 trigger detection semantics;
- V4 tick/kline handling and evidence broadcast behavior;
- runner pool registration/removal/broadcast semantics;
- V4 venue/default-symbol/market-matrix helper behavior;
- local tests and ignored behavior.

## Exclusions

- Do not move `ws_client`, `kline_buffer`, or `migration_api` in this parent.
- Do not alter `ExecutorState` or route modules.
- Do not introduce release-transition links.

## Next Step

BE-002ME-02
`root.executor.runner.runner_support_files.live_runner`
parent_residual_judgment.
