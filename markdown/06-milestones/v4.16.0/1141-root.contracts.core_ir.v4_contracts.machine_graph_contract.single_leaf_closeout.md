# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract single leaf closeout continues split

> Batch: BE-001UE-03
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract` remains open for deeper recursive split.

The extracted module is a parent-sized graph contract collection: it owns graph schema, event catalog schema, event catalog validation, graph validation, risk-plane validation, and traversal helpers. The next child should isolate `event_catalog`, which has its own schema and validation failure modes.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `event_catalog` is a clear schema/validation owner under graph contracts. |
| parent_child_communication_kept | pass | Graph validation can consume event catalog types through the machine graph parent. |
| equivalence_baseline_freezable | pass | Event catalog fields, validation branches, and error strings can be frozen before movement. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | pass | `MachineEventCatalog` and event spec DTOs are public v4 schema API. |
| state_machine_phase | pass | Event catalog is the graph event typing/permission layer. |
| strategy_branch | guarded | Graph/bridge consumers depend on event catalog correctness, but bridge behavior remains outside. |
| independent_failure_mode | pass | Event catalog has independent duplicate event, payload, and required/nullable failures. |
| reuse_pressure | pass | Graph validation and tests consume the event catalog schema/validation. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | Event catalog is a real sub-contract, not a tiny helper. |
| communication_cost_rises | no | Moving catalog schema/validation together keeps graph validation dependency simple. |
| local_proof_missing | no | `cargo test -p qrpc-core-ir` passed after graph extraction and includes event catalog tests. |
| line_count_only | no | Split is by event catalog ownership. |

leaf_split_decision_result

`continue_split: true`

Next selected child candidate:

`root.contracts.core_ir.v4_contracts.machine_graph_contract.event_catalog`

## Boundary

**Real files**:

- `qrpc_core_ir/src/v4/machine_graph_contract.rs`

**Markers**:

- `BE-001UE-03`

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-001UF-01 `root.contracts.core_ir.v4_contracts.machine_graph_contract` parent_residual_judgment selects `event_catalog`.
