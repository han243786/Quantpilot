# v4.16.0 root.contracts.core_ir.v1_contract.test_fixture extract closeout

> Batch: BE-001TQ-02
> Node: `root.contracts.core_ir.v1_contract.test_fixture`
> Parent: `root.contracts.core_ir.v1_contract`
> Stage: `extract_closeout`
> Movement: Rust cfg-test code moved under the same v1 parent facade.

---

## Summary

`test_fixture` now owns the v1 local round-trip regression test that was previously embedded in `qrpc_core_ir/src/v1.rs`.

Created:

- `qrpc_core_ir/src/v1/tests.rs`

Updated:

- `qrpc_core_ir/src/v1.rs`

## Moved Surface

| Surface | Result |
| --- | --- |
| `core_ir_round_trips` | Moved into `tests.rs` unchanged. |
| test-only `BTreeMap` import | Moved into `tests.rs`. |
| v1 parent cfg-test declaration | Replaced inline module with `#[cfg(test)] mod tests;`. |

## Equivalence Proof

| Check | Result | Evidence |
| --- | --- | --- |
| Test assertion preserved | pass | Sample construction, serde round trip, version assertion, and equality assertion are unchanged. |
| Parent-mediated access preserved | pass | Test child uses `use super::*` through the v1 parent facade. |
| Production facade reduced | pass | `qrpc_core_ir/src/v1.rs` now only declares/re-exports child modules plus cfg-test child declaration. |
| Release transition avoided | pass | No production wiring or performance shortcut was introduced. |

## Hard Boundary Result

No production DTO, helper behavior, test assertion, v4 contract, qrpc-core behavior, compiler behavior, runtime behavior, backend behavior, executor behavior, frontend behavior, physical `plugins/*`, or release-transition pathway was intentionally changed.

## Next Step

BE-001TQ-03 `root.contracts.core_ir.v1_contract.test_fixture` single_leaf_closeout.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
