# v4.16.0 root.contracts.quantscript.analysis_diagnostics parent residual judgment: strategy_presence_gate

> Batch: BE-002HJ-01
> Node: `root.contracts.quantscript.analysis_diagnostics`
> Selected child: `root.contracts.quantscript.analysis_diagnostics.strategy_presence_gate`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `root.contracts.quantscript.analysis_diagnostics.strategy_presence_gate` for the next recursive child.

## Parent Residual Scan

Current parent:

- `quantscript/src/analysis_diagnostics/mod.rs`
- `quantscript/src/analysis_diagnostics/analysis.rs`
- `quantscript/src/analysis_diagnostics/diagnostics.rs`

Closed children:

- `root.contracts.quantscript.analysis_diagnostics.unsupported_construct_gate`
- `root.contracts.quantscript.analysis_diagnostics.lookahead_window_gate`
- `root.contracts.quantscript.analysis_diagnostics.warmup_fetch_gate`
- `root.contracts.quantscript.analysis_diagnostics.indirect_recursion_gate`
- `root.contracts.quantscript.analysis_diagnostics.symbol_whitelist_gate`
- `root.contracts.quantscript.analysis_diagnostics.fetch_lookback_warning_gate`
- `root.contracts.quantscript.analysis_diagnostics.index_bounds_gate`
- `root.contracts.quantscript.analysis_diagnostics.dead_code_emit_gate`

Remaining major diagnostic families in `analysis.rs`:

| Candidate | Boundary | Decision |
| --- | --- | --- |
| `strategy_presence_gate` | strategy fetch/emit existence diagnostics (`QS0610`, `QS0611`). | Select |

## Selected Boundary

The next baseline may freeze only:

- `check_strategy_has_fetch`
- `contains_fetch_like_call_in_stmts`
- `contains_fetch_like_call_in_expr`
- `check_strategy_has_emit`

Expected child:

- planned `strategy_presence_gate` child file.

## Shared Helper Rule

`contains_emit_in_stmts` must remain parent-owned for the next extraction. It already serves the closed `dead_code_emit_gate` leaf and should not be moved into `strategy_presence_gate`, otherwise the closed child would depend on a sibling.

The next extraction may call it only through the analysis_diagnostics parent boundary.

## Why This Child

`strategy_presence_gate` is now the only remaining diagnostic family in `analysis.rs` that has a coherent child owner:

- it owns `QS0610` strategy fetch presence diagnostics;
- it owns `QS0611` strategy emit presence diagnostics;
- it can use the parent-mediated emit traversal helper without changing public API or diagnostic ordering.

## Exclusions

Do not move in the next baseline:

- `contains_emit_in_stmts`;
- dead-code emit diagnostics already closed;
- diagnostic DTO/span types;
- unsupported construct, lookahead-window, warmup/fetch, indirect-recursion, symbol-whitelist, fetch-lookback-warning, and index-bounds children already closed;
- evaluator normalization, runtime lowering, v4 static audit, tests, authoring samples, backend, executor, frontend, plugin metadata, or release transition.

## Next Step

BE-002HK-01 `root.contracts.quantscript.analysis_diagnostics.strategy_presence_gate` baseline_plan.
