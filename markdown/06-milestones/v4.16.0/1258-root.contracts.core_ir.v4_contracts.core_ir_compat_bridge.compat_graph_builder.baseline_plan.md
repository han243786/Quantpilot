# v4.16.0 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder equivalence baseline and extraction plan

> Batch: BE-001WP-01
> Node: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder`
> Parent: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge`
> Stage: `baseline_plan`
> Movement: No code movement. This document freezes the graph-builder child boundary before extraction.

---

## Summary

root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder equivalence baseline and extraction plan

This child owns v4 compatibility graph construction after Core IR validation succeeds.

## Frozen White-Box Surface

BE-001WP-02 may move only these functions into `qrpc_core_ir/src/v4/core_ir_compat_bridge/compat_graph_builder.rs`:

- `build_core_ir_compat_machine_graph`
- `build_core_ir_observation_machine`
- `build_core_ir_decision_machine`
- `build_core_ir_execution_machine`
- `compat_machine`
- `build_core_ir_compat_event_catalog`
- `compat_machine_metadata`
- `compat_edge_metadata`
- `count_memory_field`
- `core_ir_edge_labels`
- `string_value_array`
- `sanitize_core_ir_compat_id`

## Forbidden Movement

BE-001WP-02 must not move:

- bridge report DTOs and verdict/diagnostic DTOs
- `CoreIrV4CompatibilityReport::validate_for_phase4`
- `bridge_core_ir_to_v4_machine_graph`
- `core_ir_validation` child modules
- `default_core_ir_compat_bridge_version`
- `push_core_ir_v4_bridge_diagnostic`

## Allowed Extraction

BE-001WP-02 may create `qrpc_core_ir/src/v4/core_ir_compat_bridge/compat_graph_builder.rs`, add a private nested module declaration in `core_ir_compat_bridge.rs`, and call `build_core_ir_compat_machine_graph` through the parent bridge entrypoint. The parent bridge remains the only public API.

## Equivalence Baseline

The extraction is equivalent only if these gates pass:

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `cargo check -p quantpilot`

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/core_ir_compat_bridge.rs`

**Markers**:
- `BE-001WP-01`

**Next step**:
BE-001WP-02 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder extract_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
