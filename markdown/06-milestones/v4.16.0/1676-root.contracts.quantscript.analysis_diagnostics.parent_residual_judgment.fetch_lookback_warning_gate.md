# v4.16.0 root.contracts.quantscript.analysis_diagnostics parent residual judgment: fetch_lookback_warning_gate

> Batch: BE-002HD-01
> Node: `root.contracts.quantscript.analysis_diagnostics`
> Selected child: `root.contracts.quantscript.analysis_diagnostics.fetch_lookback_warning_gate`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `root.contracts.quantscript.analysis_diagnostics.fetch_lookback_warning_gate` for the next recursive child.

## Parent Residual Scan

Current parent:

- `quantscript/src/analysis_diagnostics/mod.rs`
- `quantscript/src/analysis_diagnostics/analysis.rs`
- `quantscript/src/analysis_diagnostics/diagnostics.rs`

Closed children:

- `root.contracts.quantscript.analysis_diagnostics.unsupported_construct_gate`
- `root.contracts.quantscript.analysis_diagnostics.lookahead_window_gate`
- `root.contracts.quantscript.analysis_diagnostics.warmup_fetch_gate`
- `root.contracts.quantscript.analysis_diagnostics.indirect_recursion_gate`
- `root.contracts.quantscript.analysis_diagnostics.symbol_whitelist_gate`

Remaining major diagnostic families in `analysis.rs`:

| Candidate | Boundary | Decision |
| --- | --- | --- |
| `strategy_presence_gate` | strategy fetch/emit existence diagnostics; shares emit traversal with dead-code emit. | Queue |
| `fetch_lookback_warning_gate` | `lookback < 1` warning over `fetch()` / `get_data()` calls; uses parent-mediated `arg_number_named`. | Select |
| `index_bounds_gate` | data source lookback map and array/index bounds warning; depends on parent-mediated `fetch_lookback`. | Queue |
| `dead_code_emit_gate` | constant-false emit warning; shares emit traversal logic with strategy presence. | Queue |

## Selected Boundary

The next baseline may freeze only:

- `collect_fetch_lookback_warnings`
- `collect_fetch_lookback_warnings_from_stmts`
- `collect_fetch_lookback_warnings_from_expr`

Expected child:

- planned `fetch_lookback_warning_gate` child file.

## Why This Child

`fetch_lookback_warning_gate` is the next best child because:

- it owns one isolated warning family (`QS0503`);
- it can keep `arg_number_named` as a parent-mediated helper import from the already closed warmup/fetch child;
- it does not share emit traversal logic with strategy presence or dead-code diagnostics;
- it can expose one parent-callable diagnostic entrypoint while preserving current diagnostic order.

## Exclusions

Do not move in the next baseline:

- strategy fetch/emit presence checks;
- data source map and index bounds checks;
- dead-code emit checks;
- diagnostic DTO/span types;
- unsupported construct, lookahead-window, warmup/fetch, indirect-recursion, and symbol-whitelist children already closed;
- evaluator normalization, runtime lowering, v4 static audit, tests, authoring samples, backend, executor, frontend, plugin metadata, or release transition.

## Next Step

BE-002HE-01 `root.contracts.quantscript.analysis_diagnostics.fetch_lookback_warning_gate` baseline_plan.
