# v4.16.0 root.contracts.core_ir.v4_contracts parent residual judgment selects developer_learning_pipeline_contract

> Batch: BE-001VT-01
> Node: `root.contracts.core_ir.v4_contracts`
> Parent: `root.contracts.core_ir`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts` selects
`root.contracts.core_ir.v4_contracts.developer_learning_pipeline_contract` as the next child.

The selected child owns the v4 developer learning pipeline governance contract currently in `qrpc_core_ir/src/v4.rs`:

- `DeveloperLearningPipelineContract`
- `impl Default for DeveloperLearningPipelineContract`
- `DeveloperLearningPipelineContract::validate_static_contract`
- `default_learning_pipeline_contract_version`
- `default_learning_dir`

The selected child does not own static bundles, compile-time capability reports, compatibility bridge, complexity
budget, reproducibility, plugin governance, tests, machine/graph contracts, QS contracts, runtime mode, venue capability
matrix, or version manifest.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `developer_learning_pipeline_contract` is a named white-box child for learning pipeline governance policy. |
| parent_child_communication_kept | pass | The child only needs v4 schema/default helpers and remains re-exported by the parent. |
| equivalence_baseline_freezable | pass | Learning flags, local directory default, and validation branches are finite. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | The learning pipeline contract remains public through the v4 facade. |
| state_machine_phase | no | Learning governance is documentation/record policy, not graph state topology. |
| strategy_branch | no | The child is governance policy, not a strategy branch. |
| independent_failure_mode | pass | Learning directory, gitignored, explicit write, mandatory gate, MAJOR closeout, and owner-first failures are independent from complexity/reproducibility rules. |
| reuse_pressure | guarded | Static bundles and tests reuse the learning pipeline validation. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The child owns a public DTO, defaults, and validation as one governance contract. |
| communication_cost_rises | no | One child keeps learning governance policy together while parent re-exports it. |
| local_proof_missing | no | Existing qrpc-core-ir tests cover local learning records staying out of git. |
| line_count_only | no | Selection is based on learning governance ownership. |

leaf_split_decision_result

`continue_split: true`

Selected next child:

`root.contracts.core_ir.v4_contracts.developer_learning_pipeline_contract`

next_recursive_step

BE-001VU-01 root.contracts.core_ir.v4_contracts.developer_learning_pipeline_contract baseline_plan
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs`

**Markers**:
- `BE-001VT-01`

**Next step**:
BE-001VU-01 root.contracts.core_ir.v4_contracts.developer_learning_pipeline_contract baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
