# v4.16.0 runtime_facade_coordinator parent residual judgment: portfolio_projection

> Batch: BE-001YS-01
> Node: `root.contracts.runtime_support.runtime_facade_coordinator`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

## Parent Status

`runtime_facade_coordinator` remains open.

Closed children:

- `contracts.runtime_support.runtime_facade_coordinator.constructor_provider_wiring`
- `contracts.runtime_support.runtime_facade_coordinator.session_cycle_orchestration`
- `contracts.runtime_support.runtime_facade_coordinator.execution_market_entrypoints`
- `contracts.runtime_support.runtime_facade_coordinator.state_config_accessors`
- `contracts.runtime_support.runtime_facade_coordinator.config_generation`
- `contracts.runtime_support.runtime_facade_coordinator.provider_delegation_helpers`

## Selected Child

Next child:

`root.contracts.runtime_support.runtime_facade_coordinator.portfolio_projection`

## Selection Reason

The parent still owns a cohesive portfolio projection cluster:

- `RuntimeCoordinator::portfolio_update_event`
- `RuntimeCoordinator::refresh_portfolio_state`
- `portfolio_equity_estimate`
- `quote_price_map`

These methods compute portfolio event payloads, quote lookup maps, mark-price refreshes, exchange exposures, total notional, leverage, and updated timestamps. They are distinct from provider delegation and from the coordinator test harness.

## Split Decision Gate

The selected child is worth extracting because:

- it is the remaining runtime portfolio calculation surface under the coordinator
- it has a clear input/output boundary around normalized market data and portfolio state
- it removes projection/math behavior from the parent facade
- it leaves the parent with the coordinator struct, debug implementation, module declarations, and tests only

## Remaining Parent Residuals

After this child, the remaining queue is:

1. `contracts.runtime_support.runtime_facade_coordinator.coordinator_test_harness`

## Next Step

BE-001YT-01 `root.contracts.runtime_support.runtime_facade_coordinator.portfolio_projection` baseline_plan.
