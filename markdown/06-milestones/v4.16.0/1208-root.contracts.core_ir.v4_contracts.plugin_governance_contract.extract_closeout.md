# v4.16.0 root.contracts.core_ir.v4_contracts.plugin_governance_contract actual extraction complete

> Batch: BE-001VO-02
> Node: `root.contracts.core_ir.v4_contracts.plugin_governance_contract`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `extract_closeout`
> Movement: Code movement completed.

---

## Summary

`root.contracts.core_ir.v4_contracts.plugin_governance_contract` actual extraction is complete.

Implemented movement:

- `qrpc_core_ir/src/v4.rs` now declares `mod plugin_governance_contract;`
- `qrpc_core_ir/src/v4.rs` re-exports the child through `pub use plugin_governance_contract::*;`
- `qrpc_core_ir/src/v4/plugin_governance_contract.rs` owns plugin governance DTOs, plugin manifest DTO/enums,
  default plugin kinds/fields, static plugin governance validation, and plugin manifest validation
- the child imports QS type-system, runtime mode, venue capability matrix, and sibling helper surfaces as inputs
  without moving those implementations

Equivalence notes:

- public plugin governance and manifest surface remains unchanged through the parent re-export
- plugin kind/default field order, governance flags, manifest validation branches, and validation error strings remain
  frozen from BE-001VO-01
- static bundles, compile-time capability reports, compatibility bridge, reproducibility, complexity budget, developer
  learning pipeline, bridge builders, tests, machine contract, machine graph contract, QS profile, and version manifest
  remain outside this child

Verification:

- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir` (54 passed)
- `cargo check -p quantpilot`

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs,qrpc_core_ir/src/v4/plugin_governance_contract.rs`

**Markers**:
- `BE-001VO-02`

**Next step**:
BE-001VO-03 root.contracts.core_ir.v4_contracts.plugin_governance_contract single_leaf_closeout

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
