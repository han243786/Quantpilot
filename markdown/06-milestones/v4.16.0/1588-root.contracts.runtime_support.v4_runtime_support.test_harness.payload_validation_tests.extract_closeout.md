# v4.16.0 root.contracts.runtime_support.v4_runtime_support.test_harness.payload_validation_tests extract closeout

> Batch: BE-002FE-01
> Node: `root.contracts.runtime_support.v4_runtime_support.test_harness.payload_validation_tests`
> Parent: `root.contracts.runtime_support.v4_runtime_support.test_harness`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> governance_next_authority: active

---

## Decision

Extract `payload_validation_tests` as a test-only child.

The extraction moved five malformed payload/config rejection tests into a dedicated child module while preserving all assertions, payload literals, and production behavior.

## Code Movement

| Path | Change |
| --- | --- |
| `qrpc_runtime/src/v4_runtime/test_harness/payload_validation_tests.rs` | New test-only child owning malformed payload/config rejection tests. |
| `qrpc_runtime/src/v4_runtime/test_harness.rs` | Adds `mod payload_validation_tests;` and removes the moved test bodies. |

Moved tests:

- `v4_runtime_rejects_event_payload_missing_required_catalog_field`
- `v4_runtime_rejects_event_payload_with_wrong_catalog_type`
- `v4_runtime_rejects_unknown_external_input_payload_field`
- `v4_runtime_input_event_denies_unknown_top_level_fields`
- `v4_runtime_rejects_non_finite_default_execution_costs`

## Equivalence

The migration preserves:

- all 32 v4 runtime tests;
- all malformed payload/config assertion bodies;
- parent-mediated test access through `use super::*`;
- no production runtime code movement.

Observed namespace change:

- from `v4_runtime::test_harness::*`
- to `v4_runtime::test_harness::payload_validation_tests::*` for the moved tests

## Evidence

Verified after extraction:

| Gate | Result |
| --- | --- |
| `cargo fmt --check` | PASS |
| `cargo check -p qrpc-runtime` | PASS |
| `cargo test -p qrpc-runtime v4 -- --nocapture` | PASS, 32 tests |
| `cargo check -p quantpilot` | PASS |

Governance gates are run at commit time for this batch:

- `git diff --check`;
- `tools/check-utf8.ps1`;
- `tools/check-full-feature-tree.ps1`;
- `tools/check-matrix-governance.ps1`.

## Next Step

BE-002FE-02 `root.contracts.runtime_support.v4_runtime_support.test_harness.payload_validation_tests` single_leaf_closeout.
