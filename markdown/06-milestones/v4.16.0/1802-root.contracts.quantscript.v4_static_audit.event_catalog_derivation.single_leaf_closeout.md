# v4.16.0 root.contracts.quantscript.v4_static_audit.event_catalog_derivation single leaf closeout

> Batch: BE-002JY-03
> Node: `root.contracts.quantscript.v4_static_audit.event_catalog_derivation`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`event_catalog_derivation stop_split: true`.

The child is a cohesive event catalog projection leaf. It has a small stable
surface, owns one graph-to-catalog transformation, and its only private helper
is the recursive child-machine traversal required by that transformation.
Further splitting would separate one projection decision into micro helpers and
increase parent-child forwarding cost.

## Leaf Granularity Evidence

`tools/evaluate-leaf-granularity.ps1 -Path quantscript/src/v4_static_audit/event_catalog_derivation.rs`
result:

| Metric | Result |
| --- | --- |
| Decision | `STOP` |
| Split decision | `STOP` |
| Governance packaging | `stop_split` |
| Score | 27 |
| LOC | 101 |
| Functions | 2 |
| Public surface | 1 |
| Branches | 10 |
| Size bucket | `small_100_149` |
| Governance mode | `stop_split` |

The risk tags are inherited from the v4 static-audit domain, but the extracted
leaf itself is terminal-sized and behaviorally cohesive.

## Equivalence Evidence

| Gate | Result |
| --- | --- |
| `cargo fmt --all --check` | PASS |
| `cargo test -p quantscript` | PASS, 88 tests |
| `cargo check -p quantpilot` | PASS, no warnings |
| `git diff --check` | PASS |
| `check-utf8.ps1` | PASS |
| `check-full-feature-tree.ps1` | PASS |
| `check-matrix-governance.ps1` | PASS |

## Residuals

Remain in `root.contracts.quantscript.v4_static_audit`:

- static document parser;
- machine/state/memory/transition/edge/risk-plane parsers;
- machine template parser;
- parser utilities and diagnostics helper;
- DTO definitions;
- local static-audit tests.

Closed siblings:

- `runtime_handoff_builder`;
- `audit_entrypoint`;
- `capability_type_parser`;
- `event_catalog_derivation`.

## Next Step

BE-002JZ-01 `root.contracts.quantscript.v4_static_audit`
parent_residual_judgment.
