# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.traversal_helpers single leaf closeout stops split

> Batch: BE-001VB-03
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.traversal_helpers`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract.traversal_helpers` stops splitting after extracting the
two machine traversal primitives into one reusable helper child.

The child owns:

- `collect_machine_family`
- `machine_nested_depth`

Both helpers are recursive traversal primitives over the same nested machine structure, and splitting them further
would add module hops without creating a stronger owner.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `traversal_helpers` names the reusable graph machine traversal boundary. |
| parent_child_communication_kept | pass | The parent re-exports both helpers while the child owns implementation. |
| equivalence_baseline_freezable | pass | BE-001VB-01 froze traversal order and depth semantics. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | no | Helpers remain internal to the v4 module. |
| state_machine_phase | guarded | Helpers traverse nested machine graphs but do not own phase behavior. |
| strategy_branch | no | No strategy-specific branch exists. |
| independent_failure_mode | no | Helpers do not emit errors; callers own validation failures. |
| reuse_pressure | pass | The extracted child now serves v4 complexity and validation callers. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | yes | Splitting each helper into a separate file would create micro-leaves with no stronger owner. |
| communication_cost_rises | yes | Further split would add indirection across two recursive helpers over the same data shape. |
| local_proof_missing | no | `cargo test -p qrpc-core-ir` and commit-time `cargo check -p quantpilot` passed after extraction. |
| line_count_only | no | The stop decision is based on cohesive traversal ownership. |

leaf_split_decision_result

`stop_split: true`

This leaf is complete. Return to `root.contracts.core_ir.v4_contracts.machine_graph_contract`
for parent residual judgment.

next_recursive_step

BE-001VC-01 root.contracts.core_ir.v4_contracts.machine_graph_contract parent_residual_judgment closes parent
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/traversal_helpers.rs`

**Markers**:
- `BE-001VB-03`

**Next step**:
BE-001VC-01 root.contracts.core_ir.v4_contracts.machine_graph_contract parent_residual_judgment closes parent

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
