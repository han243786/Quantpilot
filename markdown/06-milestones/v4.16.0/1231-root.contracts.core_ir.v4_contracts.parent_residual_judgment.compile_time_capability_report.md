# v4.16.0 root.contracts.core_ir.v4_contracts parent residual judgment selects compile_time_capability_report

> Batch: BE-001WA-01
> Node: `root.contracts.core_ir.v4_contracts`
> Parent: `root.contracts.core_ir`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts` selects
`root.contracts.core_ir.v4_contracts.compile_time_capability_report` as the next child.

The selected child owns the compile-time capability report surface currently in `qrpc_core_ir/src/v4.rs`:

- `V4CompileTimeCapabilityRequest`
- `V4CompileTimeCapabilityReport`
- `V4CapabilityReportVerdict`
- `V4CapabilityReportDiagnosticSeverity`
- `V4CapabilityReportDiagnostic`
- `V4TypeCapabilityStatus`
- `V4TypeCapabilityReportEntry`
- `V4ExecutionCapabilityStatus`
- `V4ExecutionCapabilityReportEntry`
- `V4PluginCapabilityStatus`
- `V4PluginCapabilityReportEntry`
- `V4StaticContractBundle::build_compile_time_capability_report`
- private type/execution/plugin capability entry builders
- `V4CompileTimeCapabilityReport::validate_for_compile`
- `push_capability_diagnostic`
- compile-time capability request/report version default helpers

The selected child does not own static bundle DTO/default/validation, compatibility bridge DTOs/builders/validators,
tests, sample builders, schema identity constants, or already extracted sibling contract implementations.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `compile_time_capability_report` is a named white-box child for capability request/report DTOs and report construction. |
| parent_child_communication_kept | guarded | The child can implement report builders on the parent bundle type while keeping parent facade re-export behavior. |
| equivalence_baseline_freezable | pass | Request/report DTOs, status enums, diagnostics, and report construction branches are finite. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | Capability request/report DTOs and validators remain public through `qrpc_core_ir::v4::*`. |
| state_machine_phase | no | This is compile-time capability validation, not state-machine topology. |
| strategy_branch | no | This is contract/report behavior, not strategy branch behavior. |
| independent_failure_mode | pass | Capability report diagnostics and verdict construction fail independently from static bundle validation and Core IR bridge compatibility. |
| reuse_pressure | pass | Tests and downstream compile paths reuse request/report DTOs and report validation. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The child owns public DTOs plus report construction and validation behavior. |
| communication_cost_rises | no | Keeping report DTOs and builders together avoids scattering diagnostic construction. |
| local_proof_missing | no | Existing qrpc-core-ir tests cover accepted and rejected capability report paths. |
| line_count_only | no | Selection is based on compile-time capability report ownership. |

leaf_split_decision_result

`continue_split: true`

Selected next child:

`root.contracts.core_ir.v4_contracts.compile_time_capability_report`

next_recursive_step

BE-001WB-01 root.contracts.core_ir.v4_contracts.compile_time_capability_report baseline_plan
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4.rs`

**Markers**:
- `BE-001WA-01`

**Next step**:
BE-001WB-01 root.contracts.core_ir.v4_contracts.compile_time_capability_report baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
