# v4.16.0 root.contracts.runtime_support.v4_runtime_support.test_harness extract closeout

> Batch: BE-002EY-01
> Node: `root.contracts.runtime_support.v4_runtime_support.test_harness`
> Parent: `root.contracts.runtime_support.v4_runtime_support`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> governance_next_authority: active

---

## Decision

Extract `test_harness` as a dedicated v4 runtime support test-only child.

The extraction moved the existing v4 runtime local test module into the v4 runtime support module directory while preserving all test bodies, fixtures, assertions, and production behavior.

## Code Movement

| Path | Change |
| --- | --- |
| `qrpc_runtime/src/v4_runtime/test_harness.rs` | New test-only child file containing the former v4 runtime local tests. |
| `qrpc_runtime/src/v4_runtime.rs` | Replaces the legacy `#[path = "v4_runtime_tests.rs"] mod tests;` entry with `#[cfg(test)] mod test_harness;`. |
| `qrpc_runtime/src/v4_runtime_tests.rs` | Removed after its contents were moved. |

## Equivalence

The migration preserves:

- all 32 v4 runtime tests;
- all local fixtures and sample graph builders;
- all event payload expectations and assertions;
- parent-mediated test access through `use super::*`;
- production v4 runtime, type, replay, transition, risk/execution, and simulated execution behavior.

Observed test namespace change:

- from `v4_runtime::tests::*`
- to `v4_runtime::test_harness::*`

This is expected from the child module name and does not change behavior.

## Out of Scope

- production runtime code changes beyond the test module declaration;
- public v4 DTOs or constants;
- constructor, replay, transition, risk/execution gate, or simulated execution logic;
- live execution, plugin runtime, ops compatibility, metrics, QuantScript, plugin metadata, executor, backend, frontend, or release-transition optimization.

## Evidence

Verified after extraction:

| Gate | Result |
| --- | --- |
| `cargo fmt --check` | PASS |
| `cargo check -p qrpc-runtime` | PASS |
| `cargo test -p qrpc-runtime v4 -- --nocapture` | PASS, 32 tests under `v4_runtime::test_harness::*` |
| `cargo check -p quantpilot` | PASS |

Governance gates are run at commit time for this batch:

- `git diff --check`;
- `tools/check-utf8.ps1`;
- `tools/check-full-feature-tree.ps1`;
- `tools/check-matrix-governance.ps1`.

## Next Step

BE-002EY-02 `root.contracts.runtime_support.v4_runtime_support.test_harness` single_leaf_closeout.
