# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation single leaf closeout stops split

> Batch: BE-001UZ-01
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation` stops splitting after its
major validation leaves have been extracted and closed.

Closed child leaves:

- `risk_plane_validation`
- `event_usage_validation`
- `graph_acyclic_validation`
- `machine_identity_validation`
- `edge_identity_validation`

Remaining parent ownership:

- public `V4MachineGraphContract::validate_static_contract` orchestration
- schema version, graph ID, and empty machine-list root guards
- vertical dispatch into machine, edge, DAG, event, and risk validation leaves
- `machine_event_party_allowed`, the compact private predicate used by event party validation

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | All major graph validation children are named white-box leaves under `graph_static_validation`. |
| parent_child_communication_kept | pass | The parent orchestrates leaf calls and passes only compact maps/results into children. |
| equivalence_baseline_freezable | pass | Child extractions preserved public `validate_static_contract` behavior through qrpc-core-ir tests. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | The public validation method intentionally remains in the parent. |
| state_machine_phase | guarded | State-machine-related graph phases are already isolated into child leaves. |
| strategy_branch | no | No remaining strategy-family branch is left inside the parent. |
| independent_failure_mode | no | Remaining root guards are lightweight parent preconditions, not a new owned child. |
| reuse_pressure | no | No additional caller or public surface requires extracting the residual parent guard/helper code. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | yes | Splitting schema/graph/empty-machine root guards would create a tiny helper without stable child ownership. |
| communication_cost_rises | yes | Moving the remaining predicate/helper out would add extra module hops for no behavior gain. |
| local_proof_missing | no | `cargo test -p qrpc-core-ir` and commit-time `cargo check -p quantpilot` passed after the last extraction. |
| line_count_only | no | The stop decision is based on parent orchestration ownership. |

leaf_split_decision_result

`stop_split: true`

The `graph_static_validation` child is complete. Return to
`root.contracts.core_ir.v4_contracts.machine_graph_contract` for parent residual judgment.

next_recursive_step

BE-001VA-01 root.contracts.core_ir.v4_contracts.machine_graph_contract parent_residual_judgment closes parent
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation.rs`

**Markers**:
- `BE-001UZ-01`

**Next step**:
BE-001VA-01 root.contracts.core_ir.v4_contracts.machine_graph_contract parent_residual_judgment closes parent

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
