# v4.16.0 root.contracts.runtime_support.v4_runtime_support.test_harness parent residual judgment - payload_validation_tests

> Batch: BE-002FC-01
> Node: `root.contracts.runtime_support.v4_runtime_support.test_harness`
> Selected child: `root.contracts.runtime_support.v4_runtime_support.test_harness.payload_validation_tests`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `root.contracts.runtime_support.v4_runtime_support.test_harness.payload_validation_tests` as the next internal test-harness child.

## Evidence

After closing `fixture_builders`, the remaining test harness still has 32 tests and stays above the terminal target size. The payload validation cluster is the next cleanest same-parent child because it owns only rejection behavior for malformed runtime input and simulated config values.

The candidate owns:

- `v4_runtime_rejects_event_payload_missing_required_catalog_field`
- `v4_runtime_rejects_event_payload_with_wrong_catalog_type`
- `v4_runtime_rejects_unknown_external_input_payload_field`
- `v4_runtime_input_event_denies_unknown_top_level_fields`
- `v4_runtime_rejects_non_finite_default_execution_costs`

## Boundary

The next baseline must freeze only payload/config validation test movement. It must not move:

- replay, graph, or multi-symbol tests;
- simulated execution fill/order scenario tests;
- capability/risk/live guard tests;
- fixture builders;
- production v4 runtime behavior;
- release-transition optimization.

## Expected Movement Shape

The next movement should be test-only:

- add `qrpc_runtime/src/v4_runtime/test_harness/payload_validation_tests.rs`;
- add `mod payload_validation_tests;` in `qrpc_runtime/src/v4_runtime/test_harness.rs`;
- move only the five selected `#[test]` functions;
- preserve parent-mediated test access with `use super::*` inside the child.

## Next Step

BE-002FD-01 `root.contracts.runtime_support.v4_runtime_support.test_harness.payload_validation_tests` baseline_plan.
