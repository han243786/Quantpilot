# v4.16.0 root.contracts.quantscript.runtime_lowering parent residual judgment: universe_lowering

> Batch: BE-002IN-01
> Node: `root.contracts.quantscript.runtime_lowering`
> Selected child: `root.contracts.quantscript.runtime_lowering.universe_lowering`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `universe_lowering` as the next child under `runtime_lowering`.

`lowering_orchestrator_surface` is closed. The remaining runtime lowering subtree still contains several production owners. `quantscript/src/lowering/universe.rs` is the next frozen queue item and is materially oversized, cohesive, and high risk enough to require precision single-leaf governance before movement.

## White-Box Boundary

| Field | Boundary |
| --- | --- |
| Input | QuantScript statements, expressions, runtime lowering context, and instrument pool data |
| Output | `PortfolioRebalanceDirective`, expanded statements, `InstrumentPoolSpec`, universe symbols, and validation diagnostics |
| Current owner | `quantscript/src/lowering/universe.rs` |
| Planned owner | `root.contracts.quantscript.runtime_lowering.universe_lowering` |
| Parent caller | `runtime_lowering` module facade through `mod universe` and `extract_instrument_pool_spec` re-export |
| Exclusions | orchestrator facade, intent inference, binding/source decoding, indicator binding resolution, manual formula fallback, source recovery, semantic bridge, helper env, context surface, shared arg helpers, and diagnostic formatting |

## Leaf Granularity Evidence

Command:

- `tools/evaluate-leaf-granularity.ps1 -LeafId root.contracts.quantscript.runtime_lowering.universe_lowering -ParentId root.contracts.quantscript.runtime_lowering -Path quantscript/src/lowering/universe.rs -Depth 4`

Result:

- `Decision: PRECISION`
- `Split decision: CONTINUE`
- `Governance packaging: precision_single_leaf`
- `Score: 56`
- `loc=1560`
- `functions=32`
- `public_surface=3`
- `branches=141`
- `domains=6`
- `risk_tags=cross_crate,persistence,public_api,state_machine,trading_semantics`
- `size_bucket=oversized_over_800`
- `governance_mode=precision_single_leaf`

## Split Decision Gate

| Gate | Result | Evidence |
| --- | --- | --- |
| `oversized_leaf` | PASS | `universe.rs` is above the 800 LOC split-pressure threshold. |
| `strong_owner_boundary` | PASS | Universe expansion, rebalance directive decoding, and instrument pool selection form a distinct runtime lowering owner. |
| `batch_downgrade_forbidden` | PASS | The leaf is high risk and must use precision single-leaf governance before movement. |
| `parent_child_boundary` | PASS | Parent remains the `runtime_lowering` module facade; no sibling horizontal connection is proposed. |

## Risk Classification

| Risk | Result |
| --- | --- |
| Public API / route / schema | Public `extract_instrument_pool_spec` re-export must remain stable |
| Persistence / lock owner | Reads context universe data; no persistence ownership movement planned |
| Trading semantics evidence | High; rebalance, allocation, universe sorting, and symbol expansion affect strategy behavior |
| Cross-crate behavior | Present through `qrpc_core` runtime config and instrument data contracts |

## Next Step

BE-002IO-01 `root.contracts.quantscript.runtime_lowering.universe_lowering` baseline_plan.
