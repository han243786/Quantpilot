# v4.16.0 root.contracts.runtime_support.risk_support.action_clamp_helpers single leaf closeout

> Batch: BE-002BY-02
> Node: `root.contracts.runtime_support.risk_support.action_clamp_helpers`
> Parent: `root.contracts.runtime_support.risk_support`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Decision

`root.contracts.runtime_support.risk_support.action_clamp_helpers` is closed with `stop_split: true`.

## Leaf Granularity Result

| Metric | Value |
| --- | --- |
| LOC | 240 |
| Functions | 6 |
| Public surface | 1 `pub(super)` helper |
| Branches | 29 |
| Domains | 2 |
| Score | 38 |
| Decision | STOP |
| Governance mode | `stop_split` |

## Rationale

The child is a cohesive action-list clamp helper group. Splitting sell inventory, total headroom, exchange headroom, symbol exposure, and portfolio exposure into separate leaves would create tiny coupled helpers with higher governance cost and no clearer parent boundary.

`clamp_actions` remains the single parent-facing entrypoint.

## Preserved Boundaries

- provider/evaluate surface remains parent-owned;
- closed projection and direction-constraint children remain outside this child;
- portfolio target clamp helpers remain outside this child;
- exposure math helpers remain parent-owned;
- tests remain outside this child;
- no sibling shortcut exists.

## Next Step

BE-002BZ-01 `root.contracts.runtime_support.risk_support` parent_residual_judgment selects `portfolio_target_clamp_helpers`.
