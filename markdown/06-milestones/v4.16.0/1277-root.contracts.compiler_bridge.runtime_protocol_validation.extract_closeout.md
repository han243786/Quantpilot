# v4.16.0 root.contracts.compiler_bridge.runtime_protocol_validation actual extraction complete

> Batch: BE-001XC-01
> Node: `root.contracts.compiler_bridge.runtime_protocol_validation`
> Parent: `root.contracts.compiler_bridge`
> Stage: `extract_closeout`
> Movement: Rust validation helpers moved into a private child module.

---

## Summary

`root.contracts.compiler_bridge.runtime_protocol_validation` has been physically extracted.

`qrpc_compiler/src/runtime_protocol_validation.rs` now owns runtime protocol validation-only behavior. The parent `qrpc_compiler/src/lib.rs` keeps the public `validate_runtime_protocol_config` facade and delegates to the child.

Moved into child:

- runtime cost finite/positive checks;
- `ensure_unique_ids`;
- `validate_data_sources`;
- `validate_intents`;
- `validate_intent`;
- `validate_agents`;
- `validate_agent`;
- `validate_risks`;
- `validate_risk`.

Preserved in parent:

- public validation function name/signature;
- runtime protocol compile/lowering;
- Strategy IR lowering;
- `runtime_intent_is_spread` shared helper;
- spread/custom expression helpers;
- canonical config hash generation;
- test fixture module.

No release-transition link was introduced.

## Boundary

**Real files**:
- `qrpc_compiler/src/lib.rs`
- `qrpc_compiler/src/runtime_protocol_validation.rs`

**Markers**:
- `BE-001XC-01`
- `runtime_protocol_validation extracted: true`
- `parent validation facade preserved`
- `lowering helpers not moved`

**Next step**:
BE-001XD-01 root.contracts.compiler_bridge.runtime_protocol_validation single_leaf_closeout

---

## Gates

- `cargo test -p qrpc-compiler`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
