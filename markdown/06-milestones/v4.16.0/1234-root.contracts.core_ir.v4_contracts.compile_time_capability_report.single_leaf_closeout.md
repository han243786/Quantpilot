# v4.16.0 root.contracts.core_ir.v4_contracts.compile_time_capability_report single leaf closeout continues split

> Batch: BE-001WB-03
> Node: `root.contracts.core_ir.v4_contracts.compile_time_capability_report`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`compile_time_capability_report` is extracted, but it should continue recursively as a parent module rather than close as one final leaf.

Current ownership:

- request/report DTOs
- capability verdict/diagnostic/status/entry DTOs
- report construction on `V4StaticContractBundle`
- type/execution/plugin entry builders
- report validation
- compile-time capability default helpers
- capability diagnostic push helper

The DTO surface, report construction, and report validation have separate change reasons and failure modes. The next child should isolate `report_builder` so diagnostic construction and entry assembly can be governed separately from DTO schema and final report validation.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `compile_time_capability_report` is named and extracted, but contains DTOs, builder behavior, and validator behavior. |
| parent_child_communication_kept | pass | A nested report-builder child can implement methods on `V4StaticContractBundle` while preserving the public v4 facade. |
| equivalence_baseline_freezable | pass | Report builder diagnostics, entry assembly, and validation behavior are covered by current qrpc-core-ir tests. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | satisfied | The module owns public request/report DTOs and public report validation behavior. |
| state_machine_phase | no | This is compile-time capability reporting, not state-machine topology. |
| strategy_branch | no | This is not strategy branch behavior. |
| independent_failure_mode | pass | Report construction failure/diagnostics differ from DTO schema and final report validation. |
| reuse_pressure | pass | Downstream compile paths and tests reuse report construction and validation. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | `report_builder` owns a coherent public method plus private entry-builder helpers. |
| communication_cost_rises | no | One nested builder child keeps diagnostic assembly together. |
| local_proof_missing | no | `cargo test -p qrpc-core-ir` and `cargo check -p quantpilot` passed after extraction. |
| line_count_only | no | Continue decision is based on DTO-vs-builder-vs-validator ownership separation. |

leaf_split_decision_result

`continue_split: true`

Selected next child:

`root.contracts.core_ir.v4_contracts.compile_time_capability_report.report_builder`

next_recursive_step

BE-001WC-01 root.contracts.core_ir.v4_contracts.compile_time_capability_report parent_residual_judgment selects report_builder
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/compile_time_capability_report.rs`

**Markers**:
- `BE-001WB-03`

**Next step**:
BE-001WC-01 root.contracts.core_ir.v4_contracts.compile_time_capability_report parent_residual_judgment selects report_builder

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
