# v4.16.0 root.contracts.quantscript.typed_resolution baseline plan

> Batch: BE-002GB-01
> Node: `root.contracts.quantscript.typed_resolution`
> Parent: `root.contracts.quantscript`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Scope

Freeze the typed-HIR resolution boundary currently owned by:

- `quantscript/src/resolve.rs`
- crate-root public re-exports in `quantscript/src/lib.rs`

Current observed size:

- `quantscript/src/resolve.rs`: 2752 LOC
- public surface: 24 public functions, structs, or enums
- local test blocks: 10 `#[test]` blocks

## Public Surface Frozen

The next movement or closeout must preserve these crate-root exports and their behavior:

- `ResolveResult`
- `lower_script_to_typed_hir`
- `expr_semantic_key`
- `classify_series_capability_name`
- `classify_builtin_math_name`
- `classify_member_mutation_name`
- `ChangeHelperKind`
- `KnownIndicatorHelperKind`
- `KnownUniverseHelperKind`
- `MovingAverageHelperKind`
- `ResolveResult`
- `ResolvedBuiltinMathKind`
- `ResolvedCallable`
- `ResolvedCallableKind`
- `ResolvedChangeSmoothingKind`
- `ResolvedExprSemantic`
- `ResolvedFetchSourceKind`
- `ResolvedFunction`
- `ResolvedManualIndicatorFormula`
- `ResolvedMemberMutationKind`
- `ResolvedSeriesBoundaryKind`
- `ResolvedSeriesCapabilityKind`
- `ResolvedSeriesViewKind`
- `ResolvedWindowAggregateKind`
- `ResolvedWindowAggregateView`
- `RsiHelperKind`

## Behavioral Boundary Frozen

The selected child owns:

- lowering parsed `ScriptModule` into `TypedHirModule`;
- `ResolveResult::has_errors`;
- function signature resolution and user function registration;
- import/helper/callable classification;
- expression semantic keys;
- series view, window aggregate, boundary, smoothing, indicator, and manual formula semantic inference;
- type arena usage during resolution;
- typed-HIR diagnostic emission for resolver-level errors;
- resolver-local tests validating typed-HIR and semantic inference behavior.

## Parent And Sibling Boundaries

The parent `root.contracts.quantscript` keeps the crate facade and top-level convenience functions:

- `parse_formal_quant_script_typed_hir`
- `analyze_formal_quant_script`
- `parse_formal_quant_script_config`
- `extract_formal_instrument_pool_spec`

The next movement must not change:

- `syntax_ast_surface` parser, type, AST, and HIR DTO ownership;
- `legacy_config_compat` deprecated config compatibility;
- `analysis_diagnostics` consumers in `analysis.rs`;
- evaluator normalization in `evaluator.rs`;
- runtime lowering under `quantscript/src/lowering/*`;
- `v4_static_audit.rs`, `test_plan.rs`, authoring samples, boundary samples, backend, runtime support, compiler bridge, executor, frontend, plugin metadata, or release-transition behavior.

## Downstream Callers To Protect

Typed resolution currently feeds:

- `quantscript/src/analysis.rs`
- `quantscript/src/lowering/binding_sources.rs`
- `quantscript/src/lowering/bindings.rs`
- `quantscript/src/lowering/helper_env.rs`
- `quantscript/src/lowering/fallback.rs`
- `quantscript/src/lowering/orchestrator.rs`
- `quantscript/src/lowering/semantic.rs`
- root crate compatibility exports consumed through `src/lib.rs`

## Next Allowed Step

BE-002GB-02 `root.contracts.quantscript.typed_resolution` actual_extraction_or_structural_closeout.

Because `typed_resolution` already has a physical owner file, the next step must first decide whether to:

- keep `resolve.rs` as the child owner and move directly into single-leaf closeout / split judgment; or
- introduce a parent folder/facade only if it improves later subleaf extraction without changing public imports.

No code movement is authorized by this baseline alone.
