# v4.16.0 root.contracts.core_ir.v4_contracts.machine_contract.static_validation single leaf closeout stops split

> Batch: BE-001UB-03
> Node: `root.contracts.core_ir.v4_contracts.machine_contract.static_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_contract`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_contract.static_validation` closes as one behavior leaf.

The file owns one public method, `V4MachineContract::validate_static_contract`. Its internal checks are tightly ordered around shared `BTreeSet` state, accumulated error order, and recursive child-machine validation. Further splitting would create private helper fragments and increase the risk of changing error order or context strings.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | The leaf owns one named public validation method. |
| parent_child_communication_kept | pass | Dependencies stay limited to machine schema/policy enums and `V4_MACHINE_CONTRACT_VERSION`. |
| equivalence_baseline_freezable | pass | BE-001UB-01 froze branch order, error text, and recursion behavior. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | The public method is already isolated; no smaller public boundary remains. |
| state_machine_phase | guarded | The validation phases are meaningful internally, but they share accumulated state and error ordering. |
| strategy_branch | no | No strategy branch behavior lives inside the validation leaf. |
| independent_failure_mode | guarded | Individual failures are independent test cases, but not independent module owners. |
| reuse_pressure | no | Reuse is through the single public validation method. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | pass | Splitting state/group/transition/memory checks would create private helper-only leaves. |
| communication_cost_rises | pass | Smaller helpers would need shared sets and error aggregation plumbing. |
| local_proof_missing | no | `cargo test -p qrpc-core-ir` passed after extraction. |
| line_count_only | pass | Further split would be driven by line count, not ownership. |

leaf_split_decision_result

`stop_split: true`

This child is closed. The recursion returns to `root.contracts.core_ir.v4_contracts.machine_contract` for parent closeout.

## Boundary

**Real files**:

- `qrpc_core_ir/src/v4/machine_contract/static_validation.rs`

**Markers**:

- `BE-001UB-03`

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-001UC-01 `root.contracts.core_ir.v4_contracts.machine_contract` parent_residual_judgment closes parent.
