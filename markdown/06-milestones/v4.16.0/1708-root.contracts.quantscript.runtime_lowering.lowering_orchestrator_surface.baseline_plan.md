# v4.16.0 root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface baseline plan

> Batch: BE-002HW-01
> Node: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface`
> Parent: `root.contracts.quantscript.runtime_lowering`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Baseline Decision

`root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface` is frozen as a precision child.

The current owner is `quantscript/src/lowering/orchestrator.rs`. Because this file combines public entrypoints, runtime config assembly, risk/execution profile parsing, and integration tests, the next movement must choose a smaller internal child through parent residual judgment before moving code.

## Frozen Public Entry Surface

| Symbol | Visibility | Invariant |
| --- | --- | --- |
| `lower_script_to_runtime_config` | `pub` | Remains the default-context wrapper over `lower_script_to_runtime_config_with_context`. |
| `lower_script_to_runtime_config_with_context` | `pub` | Keeps the public lowering sequence and returns equivalent `RuntimeProtocolCoreConfig`. |

The parent `quantscript/src/lowering/mod.rs` must continue re-exporting both symbols.

## Frozen Internal Owners

| Internal owner | Symbols / behavior |
| --- | --- |
| `entrypoint_runtime_config_assembly` | normalization, universe expansion, typed HIR, analysis diagnostics, strategy lookup, data source dedupe, binding collection, intent inference, agent/risk/runtime config construction |
| `risk_profile_detection` | `GlobalRiskProfileSpec`, `detect_global_risk_profile`, `risk_profile_call_args`, `parse_global_risk_profile_args`, `risk_profile_number_field`, `nested_risk_profile_call`, `expr_contains_risk_profile` |
| `execution_profile_detection` | `PaperExecutionProfileSpec`, `detect_paper_execution_profile`, `execution_profile_call_args`, `parse_paper_execution_profile_args`, `execution_profile_number_field`, `nested_execution_profile_call`, `expr_contains_execution_profile` |
| `integration_test_harness` | all current `#[cfg(test)]` integration tests in `orchestrator.rs` |

## Equivalence Invariants

- missing strategy and missing fetch errors remain outwardly equivalent;
- semantic diagnostics still fail before runtime lowering;
- risk and execution profile declarations must remain top-level-only;
- default risk and execution profile values are unchanged;
- rebalance directive projection remains parent-mediated through `universe`;
- data source dedupe key and agent/risk id construction remain equivalent;
- integration tests remain colocated with the public surface until a dedicated test harness child is selected.

## Risk Gate

Command:

- `tools/evaluate-leaf-granularity.ps1 -LeafId root.contracts.quantscript.runtime_lowering -ParentId root.contracts.quantscript -Path quantscript/src/lowering/orchestrator.rs -Depth 3`

Result:

- `Decision: PRECISION`
- `Split decision: CONTINUE`
- `Governance packaging: precision_single_leaf`
- `Score: 56`
- `loc=1376`
- `functions=90`
- `risk_tags=cross_crate,persistence,public_api,state_machine,trading_semantics`

## Candidate Internal Queue

| Candidate child | Default next action |
| --- | --- |
| `profile_detection_surface` | Prefer first movement candidate; it is cohesive and can reduce public orchestrator size without changing runtime config assembly. |
| `integration_test_harness` | Candidate after production behavior is stable; may move tests without public API changes. |
| `entrypoint_runtime_config_assembly` | Keep parent-owned unless later residual judgment proves a smaller child can move without obscuring public flow. |

## Gates For Actual Movement

Any Rust extraction from this baseline must pass:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8, full-feature-tree, and matrix governance checks

## Next Step

BE-002HX-01 `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface` parent_residual_judgment.
