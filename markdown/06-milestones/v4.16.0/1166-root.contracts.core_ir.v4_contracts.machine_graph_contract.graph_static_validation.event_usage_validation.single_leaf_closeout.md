# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation single leaf closeout stops split

> Batch: BE-001UR-01
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation`
stops splitting.

The parent is now a compact event usage orchestrator. Its two substantial private phases are
closed children:

- `event_reference_resolution`
- `event_party_validation`

The remaining parent code owns catalog presence checks, local catalog validation delegation,
event spec map construction, undeclared event checks, and child dispatch. Further splitting would
turn the parent into small orchestration fragments.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | The node is a named white-box parent for graph event usage validation. |
| parent_child_communication_kept | pass | Child phases return data to the parent and remain parent-mediated. |
| equivalence_baseline_freezable | pass | `cargo test -p qrpc-core-ir` passed after child extractions with 54 tests passed and 0 failed. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | The public graph validator remains one level above this parent. |
| state_machine_phase | pass | Event usage validation remains a complete graph static-validation phase. |
| strategy_branch | guarded | It protects transition/action/edge event contract semantics. |
| independent_failure_mode | guarded | Major independent failure modes are already delegated to closed child leaves. |
| reuse_pressure | no | No additional caller requires smaller parent fragments. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | yes | Remaining splits would be catalog-presence/spec-map orchestration fragments. |
| communication_cost_rises | yes | More children would pass catalog/spec maps and error vectors for little ownership gain. |
| local_proof_missing | no | Existing event catalog tests prove the parent through the public validator. |
| line_count_only | no | Stop decision is based on completed child boundaries and communication cost. |

leaf_split_decision_result

`stop_split: true`

Next parent residual candidate:

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.graph_acyclic_validation`

next_recursive_step

BE-001US-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation parent_residual_judgment selects graph_acyclic_validation
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/event_usage_validation.rs`

**Markers**:
- `BE-001UR-01`

**Next step**:
BE-001US-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation parent_residual_judgment selects graph_acyclic_validation

---

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
