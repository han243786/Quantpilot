# v4.16.0 root.executor.runner.runner_support_files.live_runner parent residual judgment selects test_harness

> Batch: BE-002MO-01
> Parent: `root.executor.runner.runner_support_files.live_runner`
> Stage: `parent_residual_judgment`
> Selected child: `root.executor.runner.runner_support_files.live_runner.test_harness`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `test_harness` as the next child under `live_runner`.

## Evidence

Closed children:

- `v3_live_runner`
- `runner_instance_dispatch`
- `v4_runner`
- `runner_pool_orchestration`
- `v4_market_metadata_helpers`

Remaining cohesive group:

- local `#[cfg(test)]` test harness.

The test harness owns V3 trigger detection tests and the V4 realtime
paper-simulated runner loop smoke test. This is now the only remaining local
owner inside `src-executor/live_runner.rs`.

## Boundary

The next baseline may freeze movement for the local `#[cfg(test)] mod tests`
only. It must not move production modules or helper ownership in the same step.

## Next Step

BE-002MP-01
`root.executor.runner.runner_support_files.live_runner.test_harness`
baseline_plan.
