# v4.16.0 root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_zscore_formula extract closeout

> Batch: BE-002JM-02
> Node: `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_zscore_formula`
> Stage: `actual_extraction`
> Movement: Rust code movement complete.
> governance_next_authority: active

---

## Extraction Result

`quantscript/src/lowering/fallback/manual_zscore_formula.rs` now owns manual ZScore fallback recognition:

- direct `ResolvedManualIndicatorFormula::ZScore` handling;
- target expression source recovery for direct ZScore formulas;
- numerator subtract-shape validation;
- ZScore operand matching through resolve-first semantic evidence;
- legacy current/mean/stddev source and window recovery.

The parent `quantscript/src/lowering/fallback.rs` keeps the stable public(crate) surface:

- `manual_zscore_from_expr` delegates to the child implementation;
- `match_zscore_operands` delegates to the child implementation;
- divide-branch recognizer order remains ZScore, momentum ratio, moving-average.

## Boundary Preservation

| Contract | Result |
| --- | --- |
| Recognizer order | Preserved in `resolve_manual_formula_binding` |
| Public(crate) wrappers | Preserved in the parent module |
| Non-ZScore fallback owners | Not moved |
| Moving-average/window fallback | Still parent-owned |
| Boundary lookback helper | Still parent-owned |
| Trading semantics | Preserved by existing ZScore fallback tests and full `quantscript` test suite |
| Parent/child communication | Parent delegates through `mod manual_zscore_formula`; child legacy helper remains private |

## Verification

| Gate | Result |
| --- | --- |
| `cargo fmt --all --check` | PASS |
| `cargo test -p quantscript` | PASS, 88 tests |
| `cargo check -p quantpilot` | PASS, no warnings |

Additional governance gates are run before commit for diff whitespace, UTF-8, full feature tree, and matrix governance.

## Next Step

BE-002JM-03 `root.contracts.quantscript.runtime_lowering.manual_formula_fallback.manual_zscore_formula` single_leaf_closeout.
