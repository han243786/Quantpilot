# v4.16.0 root.executor.runner.runner_support_files.live_runner.test_harness baseline plan

> Batch: BE-002MP-01
> Node: `root.executor.runner.runner_support_files.live_runner.test_harness`
> Stage: `baseline_plan`
> governance_next_authority: active

---

## Frozen Boundary

The next movement may extract only:

- local `#[cfg(test)] mod tests`

Target file:

`src-executor/live_runner/test_harness.rs`

The parent `src-executor/live_runner.rs` must keep only the `#[cfg(test)] mod
test_harness;` module wiring for tests.

## Explicit Non-Movement

Do not move in BE-002MP-02:

- production runner modules
- V4 market metadata helpers
- runner pool orchestration
- runner instance dispatch
- V3 or V4 runner production surfaces

## Compatibility Notes

- The test names and assertions must remain unchanged.
- The `cargo test -p quantpilot --bin executor live_runner` target must keep
  discovering the same tests through the parent module.

## Required Gates

| Gate | Required |
| --- | --- |
| `cargo fmt --all --check` | Yes |
| `cargo check -p quantpilot --bin executor` | Yes |
| `cargo test -p quantpilot --bin executor live_runner` | Yes |
| `git diff --check` | Yes |

## Next Step

BE-002MP-02
`root.executor.runner.runner_support_files.live_runner.test_harness`
actual_extraction.
