# v4.16.0 root.contracts.compiler_bridge parent residual judgment selects runtime_protocol_lowering

> Batch: BE-001XE-01
> Node: `root.contracts.compiler_bridge`
> Parent: `root.contracts`
> Stage: `parent_residual_judgment`
> Movement: no Rust code movement; parent queue selection only.

---

## Summary

`root.contracts.compiler_bridge` selects `runtime_protocol_lowering` as the next child after `runtime_protocol_validation` closeout.

This child owns runtime protocol config to Core IR lowering and compiled runtime protocol facade behavior. It remains separate from Strategy IR lowering, spread/custom expression helpers, validation, tests, and release transition.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | Selected child is `root.contracts.compiler_bridge.runtime_protocol_lowering`. |
| parent_child_communication_kept | pass | Child remains under compiler bridge parent; validation child is closed and called through parent facade. |
| equivalence_baseline_freezable | pass | Source is unchanged in this selection step; next step freezes lowering boundaries before movement. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | true | Public functions include compile/lower runtime protocol entrypoints. |
| state_machine_phase | true | Lowering maps data, intent, agent, risk, and execution phases into Core IR. |
| strategy_branch | true | Runtime protocol lowering is separate from Strategy IR lowering. |
| independent_failure_mode | true | Runtime lowering can fail after validation through intent/indicator conversions and DAG validation. |
| reuse_pressure | true | Runtime lowering is used by compile and direct lowering entrypoints. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | false | Runtime protocol lowering has clear public entrypoints and conversion ownership. |
| communication_cost_rises | false | Baseline first can preserve facade and avoid sibling coupling. |
| local_proof_missing | false | Existing qrpc-compiler tests cover runtime lowering and compile outputs. |
| line_count_only | false | Selection is based on public lowering semantics, not only line count. |

leaf_split_decision_result

`selected: runtime_protocol_lowering`.

Next recursive step: BE-001XF-01 freezes the runtime protocol lowering baseline. No Rust movement is allowed before that baseline.

next_recursive_step

BE-001XF-01 root.contracts.compiler_bridge.runtime_protocol_lowering baseline_plan
## Boundary

**Real files**:
- `qrpc_compiler/src/lib.rs`
- `qrpc_compiler/src/runtime_protocol_validation.rs`

**Markers**:
- `BE-001XE-01`
- `root.contracts.compiler_bridge selected runtime_protocol_lowering`
- `validation child closed`
- `strategy ir lowering remains queued`

**Next step**:
BE-001XF-01 root.contracts.compiler_bridge.runtime_protocol_lowering baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
