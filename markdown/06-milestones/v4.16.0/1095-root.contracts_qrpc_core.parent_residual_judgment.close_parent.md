# v4.16.0 root.contracts.qrpc_core parent closeout

> Batch: BE-001TE-01
> Node: `root.contracts.qrpc_core`
> Parent: `root.contracts`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core` is now a compact parent facade.

All selected qrpc-core production child owners and the final cfg-test residual are physically extracted, individually closed, and routed through the crate root where appropriate.

## Parent Facade Evidence

| Surface | Result |
| --- | --- |
| Crate root | `qrpc_core/src/lib.rs` contains production module declarations, public facade exports, and one cfg-test module declaration. |
| Error contract | Closed. |
| Event envelope proto | Closed. |
| Plugin contract | Closed as parent after child splits. |
| Strategy IR | Closed as parent after child splits. |
| Protocol primitives | Closed. |
| Runtime protocol config | Closed. |
| Artifact specs | Closed as parent after child splits. |
| Runtime IO contract | Closed as parent after child splits. |
| RFC execution contracts | Closed as parent after child splits. |
| Test fixture | Closed as cfg-test leaf. |

## Communication Rule Result

The qrpc-core parent preserves parent-mediated communication:

- production consumers continue through qrpc-core crate exports;
- internal child modules remain mediated by their local parents;
- crate-root tests live in `qrpc_core/src/tests.rs` and use `super::*`;
- no direct runtime/backend/executor/frontend shortcut or release-transition link was introduced.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | The parent now owns crate-level module declarations and public re-export routing. |
| parent_child_communication_kept | pass | Child access remains mediated by qrpc-core and local parent facades. |
| equivalence_baseline_freezable | pass | Child baselines and extraction closeouts froze public names and behavior. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | pass | qrpc-core remains a public contract crate facade. |
| state_machine_phase | guarded | State-related contracts are isolated in runtime IO and RFC execution children. |
| strategy_branch | no | No strategy branch is present. |
| independent_failure_mode | pass | Major qrpc-core contract families are separated by physical child owners. |
| reuse_pressure | pass | Downstream crates consume stable qrpc-core facade exports. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | pass | Remaining parent has no residual behavior to split. |
| communication_cost_rises | pass | Further parent splitting would only add facade churn. |
| local_proof_missing | no | `cargo check -p qrpc-core`, `cargo test -p qrpc-core`, and `cargo check -p quantpilot` passed during final child extraction. |
| line_count_only | pass | Parent closeout is based on completed child ownership, not line count. |

leaf_split_decision_result

`parent_closeout: true`; `root.contracts.qrpc_core` is closed as a compact parent facade.

## Next Step

BE-001TF-01 `root.contracts` parent_residual_judgment selects `core_ir`.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
