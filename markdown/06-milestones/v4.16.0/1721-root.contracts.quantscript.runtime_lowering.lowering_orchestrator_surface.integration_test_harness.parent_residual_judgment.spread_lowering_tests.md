# v4.16.0 root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness parent residual judgment: spread_lowering_tests

> Batch: BE-002ID-01
> Node: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness`
> Selected child: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.spread_lowering_tests`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `spread_lowering_tests` as the next child under `integration_test_harness`.

`manual_formula_tests` is now closed. Among the remaining groups, spread lowering has the clearest high-value boundary because it owns cross-source quote/spread admission rules, explicit helper lowering, non-admitted rejection cases, and Core IR spread spec assertions.

## White-Box Boundary

| Field | Boundary |
| --- | --- |
| Input | Test-only QuantScript fixtures with multi-exchange source fetches, spread helpers, field/resample/align helpers, and non-admitted spread formulas |
| Output | Assertions over `IntentKind::QuoteObserve`, spread params, Core IR spread spec, and rejection diagnostics |
| Current owner | `quantscript/src/lowering/orchestrator/integration_test_harness.rs` |
| Planned owner | `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.spread_lowering_tests` |
| Parent caller | `integration_test_harness` test module only |
| Exclusions | Basic smoke, runtime coordinator smoke, semantic missing-helper rejection, exchange-only decoding, rebalance lowering, manual formula tests |

## Candidate Tests

The selected child should freeze and move only these existing tests:

- `rejects_non_admitted_cross_source_spread_formula_for_formal_lowering`
- `rejects_non_admitted_asymmetric_window_spread_formula_for_formal_lowering`
- `lowers_admitted_explicit_spread_helper_into_quote_observe`
- `rejects_non_admitted_helper_annotated_formula_spread`

## Split Decision Gate

| Gate | Result | Evidence |
| --- | --- | --- |
| `micro_leaf_without_owner` | PASS | Four tests form one cohesive spread admission/rejection and Core IR projection surface. |
| `communication_cost_rises` | PASS | Movement is test-only and remains under the parent test harness. |
| `local_proof_missing` | PASS | `cargo test -p quantscript` directly executes the moved tests. |
| `line_count_only` | PASS | Selection is based on spread scenario ownership, not raw size. |

## Risk Classification

| Risk | Result |
| --- | --- |
| Public API / route / schema | No change planned |
| Persistence / lock owner | No change planned |
| Trading semantics evidence | High, test-only; spread admission/rejection must stay equivalent |
| Cross-crate behavior | Present through `qrpc_core_ir` spread assertions |

## Residual Queue

After this child, remaining candidate groups are:

- `basic_runtime_smoke_tests`
- `rebalance_lowering_tests`
- `semantic_rejection_tests`

## Next Step

BE-002IE-01 `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.integration_test_harness.spread_lowering_tests` baseline_plan.
