# v4.16.0 root.contracts.core_ir.v1_contract extract closeout

> Batch: BE-001TI-02
> Node: `root.contracts.core_ir.v1_contract`
> Parent: `root.contracts.core_ir`
> Stage: `extract_closeout`
> Movement: Rust code moved under the same crate-root facade.

---

## Summary

`v1_contract` now owns the v1 Core IR schema, helper builders, DAG validation, and local round-trip test that were previously embedded in `qrpc_core_ir/src/lib.rs`.

Created:

- `qrpc_core_ir/src/v1.rs`

Updated:

- `qrpc_core_ir/src/lib.rs`

## Moved Surface

| Surface | Result |
| --- | --- |
| v1 constants | Moved into `v1.rs` unchanged. |
| v1 DTOs/enums | Moved into `v1.rs` unchanged. |
| v1 helper builders | Moved into `v1.rs` unchanged. |
| `CoreStrategyIr::new` | Moved into `v1.rs` unchanged. |
| `CoreStrategyIr::validate_dag` | Moved into `v1.rs` unchanged. |
| v1 round-trip test | Moved into `v1.rs` unchanged. |
| Crate root facade | `lib.rs` now declares `mod v1;`, `pub mod v4;`, and `pub use v1::*;`. |

## Equivalence Proof

| Check | Result | Evidence |
| --- | --- | --- |
| Public v1 names preserved | pass | `qrpc_core_ir/src/lib.rs` re-exports `v1::*`. |
| v4 module preserved | pass | `pub mod v4;` remains in the crate root. |
| Constants and serde shape preserved | pass | v1 declarations were mechanically moved without semantic edits. |
| Helper behavior preserved | pass | Helper function bodies were mechanically moved unchanged. |
| DAG validation preserved | pass | `validate_dag` implementation was mechanically moved unchanged. |
| Test coverage preserved | pending validation | BE-001TI-02 must pass `cargo test -p qrpc-core-ir`. |

## Parent-Child Communication

The crate root remains the public gateway:

- external consumers continue using `qrpc_core_ir::*` for v1 names;
- v4 consumers continue using `qrpc_core_ir::v4`;
- no qrpc-core, compiler, runtime, backend, executor, frontend, or release-transition shortcut was introduced.

## Hard Boundary Result

No constant value, struct field, enum variant, serde shape, helper behavior, validation behavior, test assertion, v4 contract, qrpc-core behavior, compiler behavior, runtime behavior, backend behavior, executor behavior, frontend behavior, physical `plugins/*`, or release-transition pathway was intentionally changed.

## Next Step

BE-001TI-03 `root.contracts.core_ir.v1_contract` single_leaf_closeout.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
