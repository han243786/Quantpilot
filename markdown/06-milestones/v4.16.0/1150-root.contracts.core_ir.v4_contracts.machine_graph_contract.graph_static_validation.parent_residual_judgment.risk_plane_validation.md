# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation parent residual judgment selects risk_plane_validation

> Batch: BE-001UJ-01
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation` selects
`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.risk_plane_validation`
as the next child.

The selected child owns only `V4MachineGraphContract::validate_risk_plane`.
It does not own `validate_static_contract`, `validate_graph_acyclic`,
`validate_event_catalog`, `machine_event_party_allowed`, graph schema, traversal helpers,
or event catalog schema and validation.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `risk_plane_validation` is a named white-box child for graph execution-safety invariants. |
| parent_child_communication_kept | pass | The child can read graph schema, machine contracts, template taxonomy, and risk-plane constants through the validation parent/crate boundary. |
| equivalence_baseline_freezable | pass | The helper body has finite branch outcomes: required risk plane, min priority, decision template, inbound edge origin, and missing inbound edge failures. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | The public validation entry remains in the parent, while this child owns a critical private validation phase. |
| state_machine_phase | pass | Risk-plane validation is the execution-safety phase inside graph static validation. |
| strategy_branch | guarded | It protects graph semantics used by downstream bridge/static-bundle paths. |
| independent_failure_mode | pass | Its error paths are independent from DAG and event-catalog usage validation. |
| reuse_pressure | guarded | Reuse stays through the parent entry, but the phase is independently inspectable and testable. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The helper owns a real safety invariant set, not formatting or incidental lines. |
| communication_cost_rises | no | Moving one helper leaves root graph validation orchestration intact. |
| local_proof_missing | no | Existing qrpc-core-ir tests cover graph/risk-plane behavior through the public contract validator. |
| line_count_only | no | Selection is based on behavior ownership, not file size. |

leaf_split_decision_result

`continue_split: true`

Selected next child:

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.risk_plane_validation`

next_recursive_step

BE-001UK-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.risk_plane_validation baseline_plan
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation.rs`

**Markers**:
- `BE-001UJ-01`

**Next step**:
BE-001UK-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.risk_plane_validation baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
