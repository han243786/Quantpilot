# v4.16.0 root.contracts.quantscript.runtime_lowering.intent_inference.single_indicator_intent_inference single leaf closeout

> Batch: BE-002IZ-03
> Node: `root.contracts.quantscript.runtime_lowering.intent_inference.single_indicator_intent_inference`
> Parent: `root.contracts.quantscript.runtime_lowering.intent_inference`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Closeout Decision

`single_indicator_intent_inference` is closed as a terminal child and sets `stop_split: true`.

The extracted child is a cohesive single-entry owner for RSI, MACD, Momentum, and ZScore condition-to-`IntentConfig` construction. It is inside the terminal target size range, has one parent-facing entry, and continuing to split by indicator family would add extra module forwarding without a stronger owner boundary.

## Leaf Split Decision Result

Command:

- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -LeafId root.contracts.quantscript.runtime_lowering.intent_inference.single_indicator_intent_inference -ParentId root.contracts.quantscript.runtime_lowering.intent_inference -Path quantscript/src/lowering/intents/single_indicator_intent_inference.rs -Depth 5`

Result:

| Field | Value |
| --- | --- |
| `split_decision` | `STOP` |
| `governance_packaging` | `stop_split` |
| `final_decision` | `STOP` |
| `normalized_split_score` | 41 |
| `loc` | 260 |
| `function_count` | 1 |
| `public_surface` | 1 |
| `branches` | 28 |
| `risk_tags` | `cross_crate`, `public_api` |
| `reason` | medium score without same-parent wave candidate; avoid standalone governance |

## Boundary Kept

The parent `quantscript/src/lowering/intents.rs` remains the mediator for:

- public(crate) intent inference facades;
- condition-to-intent dispatch;
- spread, MA ratio, and legacy emit builders;
- data-source canonicalization;
- inferred agent param projection;
- comparison, relation, and id helper functions.

The child does not introduce sibling horizontal links and only calls parent-owned helpers through the parent module boundary.

## Validation

No Rust movement in this closeout step. The preceding extraction was validated by:

- `cargo fmt --check`
- `cargo test -p quantscript` (88 tests)
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8, full feature tree, and matrix governance checks

## Next Step

Return to `root.contracts.quantscript.runtime_lowering.intent_inference` parent residual judgment.
