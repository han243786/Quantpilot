# v4.16.0 root.contracts.compiler_bridge.runtime_protocol_lowering.risk_policy_lowering single leaf closeout stops split

> Batch: BE-001XW-03
> Node: `root.contracts.compiler_bridge.runtime_protocol_lowering.risk_policy_lowering`
> Parent: `root.contracts.compiler_bridge.runtime_protocol_lowering`
> Stage: `single_leaf_closeout`
> Movement: no Rust code movement; post-extraction closeout only.

---

## Summary

`root.contracts.compiler_bridge.runtime_protocol_lowering.risk_policy_lowering` is closed as a stable child leaf.

The leaf owns one cohesive runtime risk policy conversion function. Further splitting by field family would create helper-only micro leaves without independent public owner and would increase communication around `RiskConfig` field projection.

Decision:

`stop_split: true`

Next step:

Return to `root.contracts.compiler_bridge.runtime_protocol_lowering` parent residual judgment.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | Leaf is named `risk_policy_lowering` and owns runtime risk policy conversion. |
| parent_child_communication_kept | pass | Parent calls one child entrypoint with `&RiskConfig`, receiving `RiskPolicy`. |
| equivalence_baseline_freezable | pass | BE-001XW-01 froze every projected field and default. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | false | The leaf is private and not a public facade. |
| state_machine_phase | true | It owns the runtime risk-to-policy lowering phase. |
| strategy_branch | true | It is separate from agent policy lowering and runtime orchestration. |
| independent_failure_mode | false | The conversion is deterministic and does not own a separate failure path. |
| reuse_pressure | false | The function is called from one parent runtime lowering path. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | true | Splitting field projection families would create private helper-only fragments. |
| communication_cost_rises | true | Further split would spread simple `RiskConfig` projection across tiny modules. |
| local_proof_missing | false | Baseline documents all fields and compiler tests keep runtime output shape stable. |
| line_count_only | false | Stop decision is based on owner cohesion and communication cost, not line count. |

leaf_split_decision_result

`stop_split_true`

Add `contracts.compiler_bridge.runtime_protocol_lowering.risk_policy_lowering` to `closed_children`.

next_recursive_step

BE-001XX-01 root.contracts.compiler_bridge.runtime_protocol_lowering parent_residual_judgment

## Boundary

**Real files**:
- `qrpc_compiler/src/runtime_protocol_lowering/risk_policy_lowering.rs`

**Markers**:
- `BE-001XW-03`
- `risk_policy_lowering stop_split: true`
- `RiskPolicy projection kept local`
- `return to runtime_protocol_lowering residual judgment`

**Next step**:
BE-001XX-01 root.contracts.compiler_bridge.runtime_protocol_lowering parent_residual_judgment

---

## Gates

- `cargo test -p qrpc-compiler`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
