# v4.16.0 root.contracts.quantscript.runtime_lowering.universe_lowering.rebalance_directive_detection extract closeout

> Batch: BE-002IQ-02
> Node: `root.contracts.quantscript.runtime_lowering.universe_lowering.rebalance_directive_detection`
> Parent: `root.contracts.quantscript.runtime_lowering.universe_lowering`
> Stage: `actual_extraction`
> Movement: Production Rust movement.
> governance_next_authority: active

---

## Extraction Summary

`rebalance_directive_detection` has been extracted into:

- `quantscript/src/lowering/universe/rebalance_directive_detection.rs`

The parent `quantscript/src/lowering/universe.rs` now keeps the public(crate) facade functions and delegates rebalance detection / instrument pool extraction into the child module.

## Symbols Moved

Moved into the child:

- strategy function lookup for rebalance directives
- rebalance statement traversal
- `rebalance(...)` call detection
- schedule parsing
- allocation parser for `equal_weight`, `fixed_weights`, `rank_weight`, and `score_weight`
- `PortfolioRebalanceDirective` assembly
- rebalance-only error constants

The child calls parent-owned `evaluate_universe_expr` through the parent-child boundary. Universe expression evaluation, construct expansion, metadata selection, and tests remain parent-owned.

## Parent Boundary Kept

The parent remains owner for:

- `detect_portfolio_rebalance_directive` facade
- `extract_instrument_pool_spec` facade
- `expand_universe_constructs`
- `UniverseValue`
- universe expression evaluation
- metadata selection and sorting helpers
- module-local tests

## Validation

Passed after extraction:

- `cargo fmt --check`
- `cargo test -p quantscript` (88 tests)
- `cargo check -p quantpilot`

## Next Step

BE-002IQ-03 single_leaf_closeout.
