# v4.16.0 root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.profile_detection_surface extract closeout

> Batch: BE-002HY-02
> Node: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.profile_detection_surface`
> Parent: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface`
> Stage: `actual_extraction`
> Movement: Rust code moved.
> governance_next_authority: active

---

## Extraction Result

`profile_detection_surface` is extracted.

Created:

- `quantscript/src/lowering/profile_detection_surface.rs`

Updated:

- `quantscript/src/lowering/mod.rs`
- `quantscript/src/lowering/orchestrator.rs`

## Moved Owner

The new child owns:

- `GlobalRiskProfileSpec`
- `PaperExecutionProfileSpec`
- `detect_global_risk_profile`
- `detect_paper_execution_profile`
- risk/execution profile call argument recognition;
- risk/execution profile argument parsing;
- nested profile call placement validation;
- recursive expression scanning for risk/execution profile calls.

## Parent Residual

`orchestrator.rs` remains the parent owner for:

- public lowering entrypoints;
- normalization, universe expansion, typed HIR, and semantic analysis sequence;
- runtime config assembly;
- data-source dedupe;
- binding collection;
- intent inference;
- agent/risk/runtime config construction;
- integration tests.

The parent imports only `detect_global_risk_profile` and `detect_paper_execution_profile` from the child. No sibling horizontal call was introduced.

## Validation

Passed:

- `cargo fmt`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`

Remaining closeout gates:

- `git diff --check`
- UTF-8
- full-feature-tree
- matrix governance

## Next Step

BE-002HY-03 single_leaf_closeout.
