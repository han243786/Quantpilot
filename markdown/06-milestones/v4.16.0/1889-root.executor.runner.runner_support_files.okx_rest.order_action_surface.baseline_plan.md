# v4.16.0 root.executor.runner.runner_support_files.okx_rest.order_action_surface baseline plan

> Batch: BE-002LX-01
> Node: `root.executor.runner.runner_support_files.okx_rest.order_action_surface`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Goal

Freeze the OKX REST public action wrapper boundary before Rust movement.

The first movement may extract action wrappers from `src-executor/okx_rest.rs`
into:

`src-executor/okx_rest/order_action_surface.rs`

## Frozen Boundary

| Field | Value |
| --- | --- |
| Planned module | `src-executor/okx_rest/order_action_surface.rs` |
| Approved public surface | `place_order`, `place_order_with_profile`, `query_order`, `query_order_with_profile`, `cancel_order`, `cancel_order_with_profile`, `fetch_balance`, `fetch_balance_with_profile` |
| Parent dependencies | `OkxCredentials`, `OkxOrderRequest`, `OkxCancelOrderRequest`, `OkxTradingProfile`, REST path constants |
| Child dependencies | signing request facade, transport dispatch, parent lookup validation/path helper |
| Parent retained | public types/constants facade, lookup validation helpers, local tests and smoke test orchestration |

## Equivalent Behavior

The extracted child must preserve:

- demo profile defaulting for non-profile wrappers;
- request body serialization for place/cancel order;
- query path construction through `okx_order_lookup_path`;
- cancel lookup validation before signed request construction;
- balance endpoint action behavior;
- public function names and signatures.

## Exclusions

- Do not move lookup validation helpers in this step.
- Do not move local serialization/profile/path tests.
- Do not alter signing or transport children.
- Do not introduce release-transition links.

## Gates For Actual Extraction

| Gate | Command |
| --- | --- |
| format | `cargo fmt --all --check` |
| executor compile | `cargo check -p quantpilot --bin executor` |
| focused tests | `cargo test -p quantpilot --bin executor okx_` |
| diff hygiene | `git diff --check` |

## Next Step

BE-002LX-02
`root.executor.runner.runner_support_files.okx_rest.order_action_surface`
actual_extraction.
