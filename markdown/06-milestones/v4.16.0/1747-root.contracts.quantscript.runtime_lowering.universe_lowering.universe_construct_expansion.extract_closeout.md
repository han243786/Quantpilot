# v4.16.0 root.contracts.quantscript.runtime_lowering.universe_lowering.universe_construct_expansion extract closeout

> Batch: BE-002IS-02
> Node: `root.contracts.quantscript.runtime_lowering.universe_lowering.universe_construct_expansion`
> Parent: `root.contracts.quantscript.runtime_lowering.universe_lowering`
> Stage: `actual_extraction`
> Movement: Production Rust movement.
> governance_next_authority: active

---

## Extraction Summary

`universe_construct_expansion` has been extracted into:

- `quantscript/src/lowering/universe/universe_construct_expansion.rs`

The parent `quantscript/src/lowering/universe.rs` now keeps the public(crate) `expand_universe_constructs` facade and delegates AST expansion to the child module.

## Symbols Moved

Moved into the child:

- `expand_universe_constructs` implementation body
- `expand_stmts`
- `uniquify_local_bindings`
- `collect_binding_names`
- `rename_stmt_bindings`
- `rename_expr_bindings`
- `renamed_identifier`
- `substitute_universe_stmt`
- `substitute_universe_expr`
- `substitute_symbol_in_stmt`
- `substitute_symbol_in_expr`

The child calls parent-owned `evaluate_universe_expr` and `UniverseValue` through the parent-child boundary. Rebalance detection, universe expression evaluation, metadata selection, and module-local tests remain parent-owned or in their existing child owners.

## Parent Boundary Kept

The parent remains owner for:

- `detect_portfolio_rebalance_directive` facade
- `extract_instrument_pool_spec` facade
- `expand_universe_constructs` public(crate) facade
- `UniverseValue`
- universe expression evaluation
- instrument pool selection key mapping
- metadata lookup, metric projection, and sorting helpers
- module-local tests

## Validation

Passed after extraction:

- `cargo fmt`
- `cargo fmt --check`
- `cargo test -p quantscript` (88 tests)
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8, full feature tree, and matrix governance checks

## Next Step

BE-002IS-03 single_leaf_closeout.
