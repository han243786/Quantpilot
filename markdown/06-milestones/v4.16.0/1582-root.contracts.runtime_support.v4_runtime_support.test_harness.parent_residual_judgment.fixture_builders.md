# v4.16.0 root.contracts.runtime_support.v4_runtime_support.test_harness parent residual judgment - fixture_builders

> Batch: BE-002EZ-01
> Node: `root.contracts.runtime_support.v4_runtime_support.test_harness`
> Selected child: `root.contracts.runtime_support.v4_runtime_support.test_harness.fixture_builders`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `root.contracts.runtime_support.v4_runtime_support.test_harness.fixture_builders` as the first internal test-harness child.

## Evidence

The current test harness is oversized and has a clear shared fixture cluster at the top of `qrpc_runtime/src/v4_runtime/test_harness.rs`.

The candidate owns:

- `sample_core_ir_for_v4_runtime`;
- `sample_compat_graph`;
- `runtime_simulated_market_matrix`;
- `provider_native_market_matrix_for_paper`;
- `provider_native_market_matrix_for_live_actual`;
- `runtime_simulated_market_matrix_for_live_actual`;
- `sample_runtime`;
- `nested_observation_graph`.

These helpers are shared by replay, payload validation, simulated execution, and capability/risk guard tests. Extracting them first reduces coupling for later test scenario children without changing any test assertion.

## Boundary

The next baseline must freeze only shared fixture movement. It must not move:

- actual `#[test]` functions;
- test assertions or expected event payloads;
- production v4 runtime behavior;
- runtime constructor, replay, transition, risk/execution gate, or simulated execution code;
- release-transition optimization.

## Expected Movement Shape

The next movement should be test-only:

- add `qrpc_runtime/src/v4_runtime/test_harness/fixture_builders.rs`;
- add `mod fixture_builders;` and `use fixture_builders::*;` in `qrpc_runtime/src/v4_runtime/test_harness.rs`;
- move only the shared fixture helper functions into the new child;
- preserve parent-mediated access with `use super::*` inside the child.

## Next Step

BE-002FA-01 `root.contracts.runtime_support.v4_runtime_support.test_harness.fixture_builders` baseline_plan.
