# v4.16.0 root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface parent residual judgment: profile_detection_surface

> Batch: BE-002HX-01
> Node: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface`
> Selected child: `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.profile_detection_surface`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.profile_detection_surface` is selected as the next child.

Reason:

- it owns a cohesive risk/execution profile parsing and top-level placement validation pocket;
- it can be extracted without changing the public lowering entrypoints;
- it does not own runtime config assembly, data-source inference, binding collection, intent inference, or integration test harness behavior;
- moving it first reduces `orchestrator.rs` size while preserving the public parent flow.

## White-Box Boundary

| Field | Boundary |
| --- | --- |
| Input | `FunctionDecl` strategy body and profile declaration `CallArg` / `Expr` nodes |
| Output | Optional risk profile spec and optional execution profile spec |
| Owner | currently `quantscript/src/lowering/orchestrator.rs`; planned child file `quantscript/src/lowering/profile_detection_surface.rs` |
| Parent caller | `lower_script_to_runtime_config_with_context` in `orchestrator.rs` |
| Exclusions | runtime config assembly, diagnostics formatting, universe expansion, data-source inference, binding collection, intent inference, integration tests |

## Selected Symbols

| Group | Symbols |
| --- | --- |
| Risk profile | `GlobalRiskProfileSpec`, `detect_global_risk_profile`, `risk_profile_call_args`, `parse_global_risk_profile_args`, `risk_profile_number_field`, `nested_risk_profile_call`, `expr_contains_risk_profile` |
| Execution profile | `PaperExecutionProfileSpec`, `detect_paper_execution_profile`, `execution_profile_call_args`, `parse_paper_execution_profile_args`, `execution_profile_number_field`, `nested_execution_profile_call`, `expr_contains_execution_profile` |

## Split Gate

| Gate | Result | Evidence |
| --- | --- | --- |
| `micro_leaf_without_owner` | NO | The child owns two profile families and their placement validation, not a single helper. |
| `communication_cost_rises` | NO | The parent will keep two direct calls and receive simple spec values. |
| `local_proof_missing` | NO | Existing orchestrator tests cover profile behavior through the public lowering entrypoint. |
| `line_count_only` | NO | Selection is based on a cohesive parsing/validation owner. |

## Residuals

Still open under `lowering_orchestrator_surface`:

- `profile_detection_surface`
- `integration_test_harness`
- parent-owned `entrypoint_runtime_config_assembly`

## Next Step

BE-002HY-01 `root.contracts.quantscript.runtime_lowering.lowering_orchestrator_surface.profile_detection_surface` baseline_plan.
