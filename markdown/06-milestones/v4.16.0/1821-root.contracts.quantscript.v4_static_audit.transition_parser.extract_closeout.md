# v4.16.0 root.contracts.quantscript.v4_static_audit.transition_parser extract closeout

> Batch: BE-002KI-02
> Node: `root.contracts.quantscript.v4_static_audit.transition_parser`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> governance_next_authority: active

---

## Movement

`parse_transition` has been extracted from
`quantscript/src/v4_static_audit.rs` into:

`quantscript/src/v4_static_audit/transition_parser.rs`

The parent keeps a private `parse_transition` wrapper so
`machine_block_parser` continues to call through the parent boundary.

## Ownership Now

| Field | Value |
| --- | --- |
| Child file | `quantscript/src/v4_static_audit/transition_parser.rs` |
| Owned function | `parse_transition` |
| Parent wrapper | `parse_transition` |
| Child imports | `diag`, `split_words`, `split_csv_words` |
| Owned outputs | `MachineTransition`, `MachineEventSelector`, `MachineActionSpec` |

## Preserved Behavior

- transition syntax validation stays on `QSV4115`;
- unsupported modifier validation stays on `QSV4116`;
- transition id format remains `{machine_id}.t{transition_index}`;
- emit/write CSV token handling is unchanged;
- guard/source/freshness/default diagnostics fields are unchanged;
- state-group, memory, edge, risk-plane, machine-template, DTO, and test owners
  remain outside.

## Gates

Minimum extraction gates were run before commit.

## Next Step

BE-002KI-03 `root.contracts.quantscript.v4_static_audit.transition_parser`
single_leaf_closeout.
