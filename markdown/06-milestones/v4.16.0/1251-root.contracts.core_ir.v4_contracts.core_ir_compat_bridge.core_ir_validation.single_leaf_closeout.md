# v4.16.0 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation single leaf closeout continues split

> Batch: BE-001WK-03
> Node: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation`
> Parent: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge`
> Stage: `single_leaf_closeout`
> Movement: No code movement. This closeout decides whether the validation child should continue splitting.

---

## Summary

root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation single leaf closeout continues split

`core_ir_validation` is a coherent validation child, but it is still too broad to be terminal. It currently owns version/required-field validation, node identity collection, edge endpoint validation, object reference validation, and scalar/series/custom expression reference validation.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | PASS | `core_ir_validation` has a named validation pipeline and a clear next child candidate, `reference_validation`. |
| parent_child_communication_kept | PASS | The parent bridge calls `validate_core_ir_for_v4_bridge`; nested validation children can remain private and parent-mediated. |
| equivalence_baseline_freezable | PASS | BE-001WK-02 passed `cargo fmt -p qrpc-core-ir`, `cargo test -p qrpc-core-ir`, and `cargo check -p quantpilot`. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | PARTIAL | The child is private, but it controls the public bridge entrypoint acceptance path. |
| state_machine_phase | FAIL | This child validates Core IR before v4 machine construction; no state-machine construction occurs here. |
| strategy_branch | PASS | Reference validation branches across indicators, signals, agents, risks, and expression kinds. |
| independent_failure_mode | PASS | Node/edge validation (`V4BRIDGE010`-`033`) and reference/expression validation (`V4BRIDGE040`-`046`) fail independently. |
| reuse_pressure | PARTIAL | Future bridge variants may reuse reference validation separately from node/edge validation. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | PASS | `reference_validation` has concrete ownership: object reference checks plus scalar/series/custom expression reference checks. |
| communication_cost_rises | PASS | The child can accept derived id sets and diagnostics; no horizontal sibling calls are needed. |
| local_proof_missing | PASS | Existing bridge rejection tests cover unknown Core IR endpoints and reference-sensitive acceptance/rejection flows. |
| line_count_only | PASS | Continue split is justified by independent failure modes and ownership, not only by 478 lines. |

leaf_split_decision_result

`continue_split: true`

The next child should be `reference_validation`, owning `validate_core_ir_references_for_v4_bridge`, `validate_core_ir_scalar_expr_refs`, `validate_core_ir_series_expr_refs`, and `validate_core_ir_custom_value_refs`.

next_recursive_step

BE-001WL-01 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation parent_residual_judgment selects reference_validation
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/core_ir_compat_bridge/core_ir_validation.rs`

**Markers**:
- `BE-001WK-03`

**Next step**:
BE-001WL-01 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.core_ir_validation parent_residual_judgment selects reference_validation

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
