# v4.16.0 root.contracts.runtime_support parent residual judgment selects risk_support

> Batch: BE-002BO-01
> Node: `root.contracts.runtime_support`
> Candidate: `root.contracts.runtime_support.risk_support`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.

---

## Decision

Select `root.contracts.runtime_support.risk_support` as the next child.

## Closed Runtime Support Children

- `root.contracts.runtime_support.runtime_facade_coordinator`
- `root.contracts.runtime_support.data_module`
- `root.contracts.runtime_support.intent_module`
- `root.contracts.runtime_support.agent_module`
- `root.contracts.runtime_support.core_ir_evaluator`
- `root.contracts.runtime_support.execution_module`
- `root.contracts.runtime_support.fill_engine`

## Candidate Scope

`root.contracts.runtime_support.risk_support` owns the runtime risk-checking boundary currently concentrated in:

- `qrpc_runtime/src/risk_checker.rs`

This file owns risk-check request/output DTOs, the `RiskCheckerProvider` trait, default `RiskChecker`, risk decision evaluation, direction conflict and cross-symbol checks, action clamping, portfolio target clamps, exposure helpers, event payload projection, reason-code helpers, and local tests.

## Boundary Rules

- runtime facade and provider injection remain outside this child;
- agent decision production remains outside this child;
- execution/fill handling remains outside this child;
- risk monitor internals remain a separate sibling until the baseline proves a shared owner;
- release-transition optimization remains out of scope.

## Baseline Watchpoints

The next baseline must explicitly handle historical comment/encoding damage in `qrpc_runtime/src/risk_checker.rs` without broad semantic drift. Any code unmasking or comment normalization must be scoped to formatter/compiler clarity and equivalence evidence.

## Next Step

BE-002BP-01 `root.contracts.runtime_support.risk_support` baseline_plan.
