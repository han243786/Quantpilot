# v4.16.0 root.contracts.runtime_support.v4_runtime_support.test_harness.graph_replay_scenarios single leaf closeout

> Batch: BE-002FH-02
> Node: `root.contracts.runtime_support.v4_runtime_support.test_harness.graph_replay_scenarios`
> Parent: `root.contracts.runtime_support.v4_runtime_support.test_harness`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Set `stop_split: true` for `graph_replay_scenarios`.

The child is a small cohesive test-only owner for graph expansion, nested state-machine, deterministic bar replay, and market tick snapshot behavior. Further splitting would produce micro modules around individual scenario families without reducing meaningful coupling.

## Leaf Granularity Evidence

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -Path qrpc_runtime\src\v4_runtime\test_harness\graph_replay_scenarios.rs
```

Result:

| Field | Value |
| --- | --- |
| `split_decision` | `STOP` |
| `governance_packaging` | `stop_split` |
| `final_decision` | `STOP` |
| Score | `31` |
| LOC | `129` |
| Functions | `5` |
| Public/internal-visible surface | `0` |
| Branches | `2` |
| Domains | `6` |
| Risk tags | `persistence`, `state_machine`, `trading_semantics` |
| Terminal size bucket | `small_100_149` |

## Stop-Split Rationale

Rejected finer splits:

| Candidate | Decision | Reason |
| --- | --- | --- |
| market tick snapshot test | Reject | One-test child; no separate owner beyond graph/replay scenario evidence. |
| nested machine tests | Reject | Two tests share the same nested graph fixture and should remain together. |
| bar replay / multi-symbol tests | Reject | Splitting would create small scenario children with no stronger boundary than the current test-only owner. |

## Evidence

Inherited from BE-002FH-01 extraction:

- `cargo fmt --check`: PASS
- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime v4 -- --nocapture`: PASS, 32 tests
- `cargo check -p quantpilot`: PASS

This closeout is docs-only and updates the recursive cursor plus governance indexes.

## Next Step

BE-002FI-01 `root.contracts.runtime_support.v4_runtime_support.test_harness` parent_residual_judgment.
