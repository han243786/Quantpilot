# v4.16.0 root.contracts.runtime_support.v4_runtime_support.test_harness.graph_replay_scenarios baseline plan

> Batch: BE-002FG-01
> Node: `root.contracts.runtime_support.v4_runtime_support.test_harness.graph_replay_scenarios`
> Parent: `root.contracts.runtime_support.v4_runtime_support.test_harness`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Freeze the `graph_replay_scenarios` equivalence baseline.

The next movement may create a test-only child module for graph/replay scenario tests and must preserve all assertions, input literals, event expectations, and parent-mediated fixture access.

## Frozen Test Set

Only these tests may move in the next extraction:

- `market_price_tick_helper_records_market_snapshot`
- `v4_nested_machine_parent_transition_wins_over_child_transition`
- `v4_nested_machine_memory_is_isolated_and_visible_in_snapshot`
- `v4_backtest_bar_replay_is_deterministic`
- `v4_multi_symbol_expansion_creates_independent_machine_instances`

## Boundary

The extraction must not move:

- `v4_backtest_tick_replay_is_deterministic_and_reports_micro_metrics`;
- simulated execution fill, portfolio, stop/OCO/trailing/GTD/amend tests;
- runtime recovery/cache snapshot tests;
- live/capability/risk guard tests;
- `fixture_builders`;
- `payload_validation_tests`;
- production v4 runtime code;
- any release-transition optimization.

## Expected Code Shape

| Path | Expected change |
| --- | --- |
| `qrpc_runtime/src/v4_runtime/test_harness/graph_replay_scenarios.rs` | New test-only child with the five frozen tests and `use super::*;`. |
| `qrpc_runtime/src/v4_runtime/test_harness.rs` | Add `mod graph_replay_scenarios;` and remove only the moved test bodies. |

No public API, runtime state machine behavior, or production module boundary changes are allowed.

## Equivalence Checks

Run after extraction:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime v4 -- --nocapture`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-002FH-01 `root.contracts.runtime_support.v4_runtime_support.test_harness.graph_replay_scenarios` actual_extraction.
