# v4.16.0 root.contracts.quantscript.runtime_lowering.universe_lowering parent residual judgment: rebalance_directive_detection

> Batch: BE-002IP-01
> Node: `root.contracts.quantscript.runtime_lowering.universe_lowering`
> Selected child: `root.contracts.quantscript.runtime_lowering.universe_lowering.rebalance_directive_detection`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `rebalance_directive_detection` as the first internal child under `universe_lowering`.

The baseline queue starts here because rebalance directive detection is the parent-facing runtime lowering entry for portfolio rebalance semantics. It owns the strategy function scan, rebalance statement traversal, allocation expression parsing, schedule parsing, weighting rule projection, and final `PortfolioRebalanceDirective` assembly.

## White-Box Boundary

| Field | Boundary |
| --- | --- |
| Input | `ScriptModule`, `LoweringContext`, and best-effort pool extraction mode |
| Output | `Option<PortfolioRebalanceDirective>` and extracted `InstrumentPoolSpec` path |
| Current owner | `quantscript/src/lowering/universe.rs` |
| Planned owner | `root.contracts.quantscript.runtime_lowering.universe_lowering.rebalance_directive_detection` |
| Parent caller | `detect_portfolio_rebalance_directive` and `extract_instrument_pool_spec` facade functions |
| Exclusions | universe construct expansion, expression evaluation internals, symbol substitution, metadata sorting, and module-local test harness |

## Symbols Approved For Next Baseline

The next baseline may freeze movement for:

- `detect_portfolio_rebalance_directive`
- `detect_portfolio_rebalance_directive_with_mode`
- `extract_instrument_pool_spec`
- `collect_portfolio_rebalance_directive_from_stmts`
- `portfolio_rebalance_directive_from_expr`
- `parse_rebalance_schedule`
- `parse_rebalance_allocation_expr`
- `evaluate_number_list_expr`
- rebalance allocation error constants used only by this child

## Split Decision Gate

| Gate | Result | Evidence |
| --- | --- | --- |
| `oversized_parent_requires_child` | PASS | `universe_lowering` is `PRECISION` and cannot close as a single terminal leaf. |
| `strong_owner_boundary` | PASS | Rebalance directive detection has a distinct input/output contract and produces `PortfolioRebalanceDirective`. |
| `parent_facade_preserved` | PASS | Parent can keep public(crate) entrypoint signatures while delegating implementation to a child. |
| `sibling_exclusion_clear` | PASS | Expression evaluation, expansion, metadata selection, and tests remain outside this child. |

## Risk Classification

| Risk | Result |
| --- | --- |
| Public API / route / schema | Parent-visible Rust signatures must remain stable |
| Persistence / lock owner | Reads `LoweringContext`; no persistence movement |
| Trading semantics evidence | High; allocation, schedule, and weighting semantics affect runtime portfolios |
| Cross-crate behavior | Present through `PortfolioRebalanceDirective`, `InstrumentPoolSpec`, and `qrpc_core::RebalanceSchedule` |

## Next Step

BE-002IQ-01 `root.contracts.quantscript.runtime_lowering.universe_lowering.rebalance_directive_detection` baseline_plan.
