# v4.16.0 root.executor.runner.runner_support_files.okx_rest.signing_request_surface extract closeout

> Batch: BE-002LT-02
> Node: `root.executor.runner.runner_support_files.okx_rest.signing_request_surface`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> governance_next_authority: active

---

## Movement

The OKX REST signing and signed request construction surface has been extracted
from `src-executor/okx_rest.rs` into:

`src-executor/okx_rest/signing_request_surface.rs`

The parent keeps public types/constants, transport/proxy/raw send behavior,
response success checks, order/query/cancel/balance action wrappers, lookup
validation, and smoke probes.

## Ownership Now

| Field | Value |
| --- | --- |
| Child file | `src-executor/okx_rest/signing_request_surface.rs` |
| Owned helpers | credential validation, HMAC signing, OKX timestamp formatting, REST base URL resolution for signed requests, public `build_signed_request`, timestamp-injected signed request builder |
| Moved tests | signing/base64 test, timestamp format test, demo header/flag test, public-entry credential rejection test |
| Parent facade | `okx_rest.rs` re-exports `build_signed_request` |
| Parent retained | transport/proxy/raw send, OKX success checks, order/query/cancel/balance wrappers, lookup validation, smoke probes |

## Preserved Behavior

- HMAC signing input shape is unchanged.
- OKX timestamp format is unchanged.
- Public `build_signed_request` credential/path/method validation is unchanged.
- Timestamp-injected private builder keeps its original no-validation behavior.
- Demo simulated-trading header and SDK/audit markers are unchanged.

## Gates

| Gate | Result |
| --- | --- |
| `cargo fmt --all --check` | PASS |
| `cargo check -p quantpilot --bin executor` | PASS |
| `cargo test -p quantpilot --bin executor okx_` | PASS, 12 passed / 2 ignored |
| `git diff --check` | PASS |

## Next Step

BE-002LT-03
`root.executor.runner.runner_support_files.okx_rest.signing_request_surface`
single_leaf_closeout.
