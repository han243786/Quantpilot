# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation.event_party_validation actual extraction complete

> Batch: BE-001UO-02
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation.event_party_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation`
> Stage: `extract_closeout`
> Movement: Code movement completed.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation.event_party_validation`
actual extraction is complete.

The child now owns only transition/action/edge emitter and consumer permission checks.
The parent event usage validation still owns referenced event discovery, catalog presence checks,
local catalog validation, event spec map construction, and undeclared event checks.

## Movement

Moved:

- transition consumer allowed-party checks;
- transition source allowed-emitter checks;
- action emitted-event allowed-emitter checks;
- edge source allowed-emitter checks;
- edge target allowed-consumer checks.

Kept in parent:

- referenced event discovery;
- event catalog presence requirement;
- `MachineEventCatalog::validate_static_contract` delegation;
- event spec map construction;
- undeclared event rejection;
- `validate_event_catalog` orchestration.

## Equivalence Result

- `cargo fmt -p qrpc-core-ir` passed.
- `cargo test -p qrpc-core-ir` passed: 54 tests passed, 0 failed.

## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/event_usage_validation.rs`
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation/event_usage_validation/event_party_validation.rs`

**Markers**:
- `BE-001UO-02`

**Next step**:
BE-001UO-03 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.event_usage_validation.event_party_validation single_leaf_closeout

---

## Gates

- `git diff --check`
- `cargo fmt -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
