# v4.16.0 root.executor.runner.v4_deploy_support baseline plan

> Batch: BE-002LN-01
> Node: `root.executor.runner.v4_deploy_support`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Goal

Freeze the v4 deploy support boundary before Rust movement.

The first movement may extract the v4 deploy/preflight/static bundle helper
surface from `src-executor/main.rs` into `src-executor/v4_deploy_support.rs`.

## Frozen Boundary

| Field | Value |
| --- | --- |
| Planned file | `src-executor/v4_deploy_support.rs` |
| Approved helper surface | `V4StrategyDeployRequest`, `is_v4_deploy_request`, `deploy_v4_strategy`, `parse_execution_mode`, `resolve_v4_deploy_graph`, `extract_v4_subscribed_symbols`, `empty_core_ir`, `ensure_strategy_config_preflight_allows_start`, `strategy_config_preflight_error`, JSON path helpers, v4 static bundle helpers |
| Parent callers | `strategy_lifecycle_routes` and existing tests |
| Parent retained | route registration, route modules, runner setup, provider helpers, audit helper, runner support files |

## Equivalent Behavior

The extracted child must preserve:

- v4 deployment request detection behavior;
- v4 graph/source resolution behavior;
- execution mode parsing and live-mode rejection behavior;
- v4 strategy registration payload behavior;
- strategy config preflight start gate behavior;
- JSON path helper semantics;
- executor v4 static bundle construction behavior.

## Exclusions

- Do not move strategy lifecycle route handlers already closed.
- Do not move market stream route handlers already closed.
- Do not move OKX provider route handlers already closed.
- Do not move runner setup/support files.
- Do not introduce release-transition links.

## Gates For Actual Extraction

| Gate | Command |
| --- | --- |
| format | `cargo fmt --all --check` |
| executor compile | `cargo check -p quantpilot --bin executor` |
| diff hygiene | `git diff --check` |

## Next Step

BE-002LN-02 `root.executor.runner.v4_deploy_support`
actual_extraction.
