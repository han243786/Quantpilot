# v4.16.0 root.executor.runner.runner_support_files.okx_rest.lookup_validation_surface baseline plan

> Batch: BE-002LZ-01
> Node: `root.executor.runner.runner_support_files.okx_rest.lookup_validation_surface`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Goal

Freeze the OKX order lookup and validation helper boundary before Rust movement.

The next movement may extract lookup helpers from `src-executor/okx_rest.rs`
into:

`src-executor/okx_rest/lookup_validation_surface.rs`

## Frozen Boundary

| Field | Value |
| --- | --- |
| Planned module | `src-executor/okx_rest/lookup_validation_surface.rs` |
| Approved surface | `okx_order_lookup_path`, `validate_order_lookup`, `clean_optional_token`, `valid_okx_inst_char`, `valid_okx_id_char` |
| Parent dependencies | `OKX_ORDER_PATH` |
| Child dependencies | `anyhow::bail`, `anyhow::Result` |
| Parent retained | public types/constants, re-exports, signing/transport/order action children, and local tests |

## Equivalent Behavior

The extracted child must preserve:

- trim behavior for `inst_id`, `ord_id`, and `cl_ord_id`;
- query path construction order;
- validation error behavior before query/cancel request creation;
- accepted character rules for OKX instrument IDs and order IDs.

## Exclusions

- Do not move action wrappers in this step.
- Do not move tests in this step.
- Do not alter public type definitions or constants.
- Do not introduce release-transition links.

## Gates For Actual Extraction

| Gate | Command |
| --- | --- |
| format | `cargo fmt --all --check` |
| executor compile | `cargo check -p quantpilot --bin executor` |
| focused tests | `cargo test -p quantpilot --bin executor okx_` |
| diff hygiene | `git diff --check` |

## Next Step

BE-002LZ-02
`root.executor.runner.runner_support_files.okx_rest.lookup_validation_surface`
actual_extraction.
