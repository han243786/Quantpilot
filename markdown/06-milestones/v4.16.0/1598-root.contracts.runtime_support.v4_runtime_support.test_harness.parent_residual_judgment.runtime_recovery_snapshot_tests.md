# v4.16.0 root.contracts.runtime_support.v4_runtime_support.test_harness parent residual judgment - runtime_recovery_snapshot_tests

> Batch: BE-002FL-01
> Node: `root.contracts.runtime_support.v4_runtime_support.test_harness`
> Selected child: `root.contracts.runtime_support.v4_runtime_support.test_harness.runtime_recovery_snapshot_tests`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `root.contracts.runtime_support.v4_runtime_support.test_harness.runtime_recovery_snapshot_tests` as the next internal test-harness child.

## Evidence

After closing simulated execution scenarios, the remaining test harness is mostly live/capability/risk guard coverage plus two runtime recovery/cache snapshot tests. The recovery/cache tests have a separate owner because they verify soft-silent recovery lifecycle, cache return, machine memory snapshot projection, and runtime snapshot metadata rather than execution capability admission.

The candidate owns:

- `v4_runtime_returns_last_cache_and_recovers_soft_silent_machine`
- `v4_runtime_memory_snapshot_records_machine_memory_and_cache`

## Boundary

The next baseline must freeze only runtime recovery/cache snapshot test movement. It must not move:

- live actual mode boundary tests;
- capability policy rejection tests;
- risk-plane source rejection tests;
- simulated order capability rejection tests;
- fixture builders;
- malformed payload/config rejection tests;
- graph/replay scenario tests;
- simulated execution scenario tests;
- production v4 runtime behavior;
- release-transition optimization.

## Expected Movement Shape

The next movement should be test-only:

- add `qrpc_runtime/src/v4_runtime/test_harness/runtime_recovery_snapshot_tests.rs`;
- add `mod runtime_recovery_snapshot_tests;` in `qrpc_runtime/src/v4_runtime/test_harness.rs`;
- move only the two selected `#[test]` functions;
- preserve parent-mediated test access with `use super::*` inside the child.

## Next Step

BE-002FM-01 `root.contracts.runtime_support.v4_runtime_support.test_harness.runtime_recovery_snapshot_tests` baseline_plan.
