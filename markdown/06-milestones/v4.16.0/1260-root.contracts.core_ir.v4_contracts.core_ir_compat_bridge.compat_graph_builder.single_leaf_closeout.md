# v4.16.0 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder single leaf closeout continues split

> Batch: BE-001WP-03
> Node: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder`
> Parent: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge`
> Stage: `single_leaf_closeout`
> Movement: No code movement. This closeout decides whether the graph-builder child should continue splitting.

---

## Summary

root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder single leaf closeout continues split

`compat_graph_builder` is a valid builder child, but it is not terminal. It currently owns graph assembly, three compatibility machine builders, shared machine construction, event catalog construction, graph metadata, and helper functions.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | PASS | `compat_graph_builder` has clear graph/machine/event construction ownership and a clear event-catalog child candidate. |
| parent_child_communication_kept | PASS | The bridge parent calls the builder child; builder children can remain private and parent-mediated. |
| equivalence_baseline_freezable | PASS | BE-001WP-02 passed `cargo fmt -p qrpc-core-ir`, `cargo test -p qrpc-core-ir`, and `cargo check -p quantpilot`. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | PARTIAL | The child is private, but its graph output is returned through the public bridge report. |
| state_machine_phase | PASS | It builds machine graph, machines, graph edges, event catalog, and risk plane binding. |
| strategy_branch | PASS | Machine builders branch over data/indicator, signal/agent/risk, and execution Core IR groups. |
| independent_failure_mode | PASS | Event catalog construction has independent event source/scope/payload/consumer ownership from machine construction. |
| reuse_pressure | PARTIAL | Event catalog construction can evolve separately as bridge event semantics expand. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | PASS | `event_catalog_builder` owns a complete event catalog construction phase, not a helper-only fragment. |
| communication_cost_rises | PASS | The event catalog child can accept `CoreStrategyIr` and return `MachineEventCatalog`; no horizontal sibling calls are needed. |
| local_proof_missing | PASS | Existing bridge mapping and machine graph validation tests exercise event catalog construction through the public bridge entrypoint. |
| line_count_only | PASS | Continue split is based on state-machine/event ownership, not only on the 407-line size. |

leaf_split_decision_result

`continue_split: true`

The next child should be `event_catalog_builder`, owning `build_core_ir_compat_event_catalog`.

next_recursive_step

BE-001WQ-01 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder parent_residual_judgment selects event_catalog_builder
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/core_ir_compat_bridge/compat_graph_builder.rs`

**Markers**:
- `BE-001WP-03`

**Next step**:
BE-001WQ-01 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder parent_residual_judgment selects event_catalog_builder

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
