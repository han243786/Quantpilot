# v4.16.0 root.contracts.quantscript.analysis_diagnostics.warmup_fetch_gate baseline plan

> Batch: BE-002GY-01
> Node: `root.contracts.quantscript.analysis_diagnostics.warmup_fetch_gate`
> Parent: `root.contracts.quantscript.analysis_diagnostics`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Scope

Freeze the required warmup and fetch lookback diagnostic gate before movement.

Current owner:

- `quantscript/src/analysis_diagnostics/analysis.rs`

Planned child:

- `quantscript/src/analysis_diagnostics/warmup_fetch_gate.rs`

## Items Frozen For Movement

The next extraction may move only:

- `infer_required_warmup_bars`
- `required_warmup_for_semantic`
- `collect_warmup_diagnostics`
- `collect_warmup_from_function`
- `collect_fetch_lookbacks_from_stmts`
- `collect_fetch_lookbacks_from_expr`
- `fetch_lookback`
- `arg_number_named`

## Diagnostic Boundary

The child owns:

- required warmup inference from resolver semantics;
- fetch/get_data lookback collection from script AST;
- `QS0501` insufficient warmup diagnostics;
- numeric `lookback` argument parsing and clamping helper.

## Parent Wiring To Preserve

The analysis parent must keep:

- `ScriptAnalysis.required_warmup_bars` output unchanged;
- `ScriptAnalysis` and `analyze_script_module` public surface unchanged;
- diagnostic family invocation order unchanged;
- crate-root public exports and `crate::analysis` / `crate::diagnostics` compatibility unchanged.

The child may expose:

- `infer_required_warmup_bars` to calculate the parent `required_warmup_bars`;
- `collect_warmup_diagnostics` for `QS0501`;
- `fetch_lookback` only if needed by the parent-owned index-bounds family.

## Shared Helper Rule

`fetch_lookback` is temporarily allowed to be parent-visible because the later `index_bounds_gate` still reads data-source lookback information from `analysis.rs`.

This does not permit sibling horizontal calls:

- `index_bounds_gate` is not allowed to import `warmup_fetch_gate` directly.
- Until `index_bounds_gate` is extracted, `analysis.rs` remains the parent coordinator and may call the helper.
- If a later extraction wants to move `index_bounds_gate`, it must either keep the helper parent-mediated or freeze a new shared-parent helper boundary.

## Forbidden Movement

The next extraction must not move or change:

- strategy fetch/emit presence checks;
- fetch lookback warnings;
- indirect recursion checks;
- data source map and index bounds checks;
- dead-code emit checks;
- symbol whitelist checks;
- diagnostic DTO/span types;
- lookahead-window diagnostics already closed;
- unsupported construct diagnostics already closed;
- evaluator normalization, runtime lowering, v4 static audit, tests, authoring samples, backend, executor, frontend, plugin metadata, or release transition.

## Expected Validation

The extraction must pass:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002GY-02 `root.contracts.quantscript.analysis_diagnostics.warmup_fetch_gate` actual_extraction.
