# v4.16.0 root.contracts.runtime_support.data_module parent residual judgment selects test_harness

> Batch: BE-001ZP-01
> Node: `root.contracts.runtime_support.data_module`
> Stage: `parent_residual_judgment`
> Selected child: `root.contracts.runtime_support.data_module.test_harness`
> Movement: No Rust code movement.

---

## Parent State

Closed data-module children:

- `contracts.runtime_support.data_module.source_mapping`
- `contracts.runtime_support.data_module.quality_diagnostics`
- `contracts.runtime_support.data_module.collection_orchestration`
- `contracts.runtime_support.data_module.exchange_surface_wave`
- `contracts.runtime_support.data_module.exchange_endpoints`
- `contracts.runtime_support.data_module.exchange_payload_parsing`
- `contracts.runtime_support.data_module.normalization`
- `contracts.runtime_support.data_module.mock_data_generation`
- `contracts.runtime_support.data_module.historical_cache`
- `contracts.runtime_support.data_module.http_transport`

Remaining data-module residuals:

- `contracts.runtime_support.data_module.test_harness`
- parent facade closeout

## Selected Child Boundary

`root.contracts.runtime_support.data_module.test_harness` owns the local unit-test harness currently embedded in `qrpc_runtime/src/data_module.rs`.

Candidate implementation surface:

- `sample_core_ir_with_quote_binding`
- `sample_core_ir_with_quote_and_kline_bindings`
- OKX candle parser test
- OKX ticker parser test
- built-in data module mock/non-OKX behavior test
- mixed-source fast-cycle collection test
- source hint request-control restoration test
- request-interval cache test
- pseudo-random deterministic/range/difference tests

## Why This Child Next

| Criterion | Judgment |
| --- | --- |
| Cohesion | Strong: these items are test fixtures and assertions for the data-module facade and its extracted children. |
| Inputs | parent private/test-visible helpers, parser helpers, mock generator, `BuiltinDataModule`, sample Core IR fixtures |
| Outputs | unit-test assertions only |
| Risk | Low: no production movement is needed; extraction should preserve test behavior and parent-private access through a child `#[cfg(test)]` module. |
| Exclusions | production parent facade, request cache, breaker logic, transport, historical cache, mock generation, normalization, source mapping, and quality diagnostics remain outside this child. |
| Parent communication | Test module remains a child of `data_module`, so it may use parent-private items through normal Rust child-module visibility. |

## Leaf Split Decision Gate

`test_harness` should continue to baseline/extraction because:

- it is the last queued data-module child before parent facade closeout;
- it is a low-risk movement that reduces parent file size and keeps production facade easier to scan;
- the tests still serve as local equivalence guards for the extracted children, even if larger test programs may later be discarded;
- moving it to a child module does not introduce runtime coupling or public API changes.

Further split inside `test_harness` is not approved. If the extracted test module is small enough, terminal leaf control should stop it rather than creating parser-test/mock-test micro leaves.

## Equivalence Baseline For Next Step

Next baseline must preserve:

- all existing `data_module::tests::*` test names and assertions;
- sample Core IR fixture construction;
- direct access to parent-private helpers through a `#[cfg(test)]` child module;
- parser helper test coverage;
- mock/non-OKX behavior coverage;
- mixed-source collection coverage;
- request-control source hint restoration coverage;
- request-interval cache coverage;
- pseudo-random deterministic/range/difference coverage.

Required gates for the next movement:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-001ZQ-01 `root.contracts.runtime_support.data_module.test_harness` baseline_plan.
