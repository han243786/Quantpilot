# v4.16.0 root.contracts.runtime_support.risk_support parent residual judgment selects portfolio_target_clamp_helpers

> Batch: BE-002BZ-01
> Node: `root.contracts.runtime_support.risk_support`
> Candidate: `root.contracts.runtime_support.risk_support.portfolio_target_clamp_helpers`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.

---

## Decision

Select `root.contracts.runtime_support.risk_support.portfolio_target_clamp_helpers` as the next child.

## Current Closed Children

- `root.contracts.runtime_support.risk_support.event_payload_projection`
- `root.contracts.runtime_support.risk_support.direction_cross_constraints`
- `root.contracts.runtime_support.risk_support.action_clamp_helpers`

## Candidate Scope

This child covers portfolio-target clamp orchestration and target-weight mutation helpers:

- `clamp_portfolio_target_limits`
- `refresh_portfolio_target_current_weights`
- `clamp_portfolio_target_new_positions`
- `clamp_portfolio_target_single_weight`
- `clamp_portfolio_target_turnover`
- `clamp_portfolio_target_total_buy_headroom`
- `clamp_portfolio_target_exchange_headroom`
- `clamp_portfolio_target_min_trade_weight`
- `clamp_portfolio_target_portfolio_net_exposure`

## Rationale

The candidate is cohesive because these helpers mutate portfolio target weights after risk policy evaluation. They share target-weight state, current-weight refresh, single-weight, turnover, buy headroom, exchange headroom, minimum trade, and portfolio net exposure constraints.

## Preserved Boundaries

- provider/evaluate surface remains parent-owned;
- closed projection, direction-constraint, and action-clamp children remain outside this child;
- `push_reason_code` remains parent-owned;
- exposure math helpers remain parent-owned;
- tests remain outside this child;
- no sibling shortcut may be introduced.

## Next Step

BE-002CA-01 `root.contracts.runtime_support.risk_support.portfolio_target_clamp_helpers` baseline_plan.
