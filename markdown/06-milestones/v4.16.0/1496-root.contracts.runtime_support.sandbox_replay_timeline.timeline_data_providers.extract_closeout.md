# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline.timeline_data_providers extract closeout

> Batch: BE-002CN-01
> Node: `root.contracts.runtime_support.sandbox_replay_timeline.timeline_data_providers`
> Parent: `root.contracts.runtime_support.sandbox_replay_timeline`
> Stage: `actual_extraction`
> Movement: Rust code movement.

---

## Scope

Extract timeline data provider implementations from `qrpc_runtime/src/sandbox/timeline.rs`.

Moved file:

- `qrpc_runtime/src/sandbox/timeline_data_providers.rs`

Moved surface:

- `TimelineDataProvider`
- `KlineProvider`
- `QuoteProvider`
- `ResampleKlineProvider`
- `bar_interval_ms`

## Parent Boundary

`qrpc_runtime/src/sandbox/timeline.rs` now keeps `UnifiedTimeline` and re-exports the provider child through the timeline parent. Existing replay imports continue to use the parent-mediated `super::timeline::{...}` surface.

`UnifiedTimeline`, replay builder functions, sandbox modes, sandbox implementations, and tests remain outside this movement.

## Equivalence

The extraction preserves:

- Kline asof snapshot behavior and rolling window cap;
- Quote asof behavior and kline fallback construction;
- resampled kline interval aggregation behavior;
- provider timestamps and data IDs;
- replay builder access to provider types through the timeline parent.

The provider child carries `#![allow(dead_code)]` to preserve the pre-existing reserved provider API surface without introducing warning noise after moving it into a private child module.

## Gates

Verified for this batch:

- `cargo fmt --check`: pass
- `cargo check -p qrpc-runtime`: pass
- `cargo test -p qrpc-runtime sandbox -- --nocapture`: pass, 21 passed / 2 ignored

Remaining full gates are required before commit:

- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 / full feature tree / matrix governance

## Next Step

BE-002CN-02 `root.contracts.runtime_support.sandbox_replay_timeline.timeline_data_providers` single_leaf_closeout.
