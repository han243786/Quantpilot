# v4.16.0 root.contracts.runtime_support.sandbox_replay_timeline.realtime_sandbox single leaf closeout

> Batch: BE-002CY-02
> Node: `root.contracts.runtime_support.sandbox_replay_timeline.realtime_sandbox`
> Parent: `root.contracts.runtime_support.sandbox_replay_timeline`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Decision

`stop_split: true`

`realtime_sandbox` is a terminal leaf. It owns one coherent realtime sandbox implementation surface and should not be split into constructor, trait implementation, handoff, restore, or risk-monitor helper micro-leaves.

## Leaf Granularity Evidence

`tools/evaluate-leaf-granularity.ps1` result:

- Decision: STOP
- Score: 35
- LOC: 165 reported by tool
- Functions: 18
- Public surface: 8
- Governance mode: `stop_split`
- Reason: low split score or terminal-sized cohesive leaf

## Stop Rationale

- The leaf is inside the target terminal size band.
- Splitting would create micro-owner fragments around one concrete implementation.
- Handoff/restore and risk-monitor helpers depend on the same `RuntimeCoordinator` state owner.
- Further split would raise parent-child communication cost without improving white-box clarity.

## Preserved Boundary

`qrpc_runtime/src/sandbox/realtime_sandbox.rs` remains the owner for:

- `RealTimeSandbox`
- realtime constructors/accessors
- `impl Sandbox for RealTimeSandbox`
- realtime handoff/restore behavior
- realtime risk-monitor helpers

`qrpc_runtime/src/sandbox/mod.rs` remains the parent owner for:

- `FastBacktestSandbox`
- shared snapshot/running/trace helpers
- sandbox tests
- parent re-export of `RealTimeSandbox`

## Next Step

BE-002CZ-01 `root.contracts.runtime_support.sandbox_replay_timeline` parent_residual_judgment, expected next candidate: `fast_backtest_sandbox`.
