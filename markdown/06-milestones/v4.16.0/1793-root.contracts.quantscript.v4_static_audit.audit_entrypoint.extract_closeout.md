# v4.16.0 root.contracts.quantscript.v4_static_audit.audit_entrypoint extract closeout

> Batch: BE-002JU-02
> Node: `root.contracts.quantscript.v4_static_audit.audit_entrypoint`
> Stage: `actual_extraction`
> Movement: Rust code movement complete.
> governance_next_authority: active

---

## Extraction Result

`quantscript/src/v4_static_audit/audit_entrypoint.rs` now owns the
static-audit orchestration implementation from source input and
`V4StaticContractBundle` to `V4QsStaticAuditReport`.

The parent `quantscript/src/v4_static_audit.rs` keeps the public
`audit_v4_quant_script_static` function as a stable wrapper and delegates
directly to the child module.

## Boundary Preservation

| Boundary | Result |
| --- | --- |
| Public API | Unchanged; callers still use `audit_v4_quant_script_static` from the parent module |
| Parent-child communication | Direct parent wrapper to private child function |
| Parser ownership | `parse_v4_static_document` and all parser helpers remain parent-owned |
| DTO ownership | Static audit report, runtime handoff report, and verdict DTOs remain parent-owned |
| Capability report integration | Preserved inside the moved orchestration body |
| Runtime handoff | Closed `runtime_handoff_builder` child unchanged |
| Tests | Local static-audit tests remain parent-owned |

## Verification

Completed before governance packaging:

- `cargo fmt --all --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`

Required before commit:

- `git diff --check`
- `tools/check-utf8.ps1`
- `tools/check-full-feature-tree.ps1`
- `tools/check-matrix-governance.ps1`
- `recursive-state.json` JSON parse

## Next Step

BE-002JU-03 `root.contracts.quantscript.v4_static_audit.audit_entrypoint`
single_leaf_closeout.
