# v4.16.0 data_module parent residual judgment selects collection_orchestration

> Batch: BE-001ZD-01
> Node: `root.contracts.runtime_support.data_module`
> Stage: `parent_residual_judgment`
> Selected child: `root.contracts.runtime_support.data_module.collection_orchestration`
> Movement: No code movement.

## Context

`source_mapping` and `quality_diagnostics` are closed. The next child in the data-module queue is `collection_orchestration`.

## Selected Child

`root.contracts.runtime_support.data_module.collection_orchestration`.

This child owns the top-level data collection flow:

- `impl DataModuleProvider for BuiltinDataModule`
- `collect`
- normalized data accumulation
- duplicate source suppression
- runtime data event assembly
- runtime data quality warning/error event assembly

The next baseline must decide whether `fetch_and_normalize` joins this child or remains parent-owned until cache, exchange, transport, and mock leaves are separated.

## Selection Reason

`collection_orchestration` is the next cohesive data-module behavior family.

Reasons:

- it is the public provider entrypoint that the runtime coordinator calls
- it composes source mapping, fetch/normalize, quality diagnostics, and runtime event output
- it owns the ordering and deduplication of source collection
- separating it gives later exchange/cache/mock leaves a clear parent orchestration boundary
- it is independently covered by mixed-source and mock fallback data module tests

## Split Decision Gate

The child should proceed through baseline before movement.

Expected outcome:

- add a collection orchestration child module if the baseline confirms movement is low-risk
- keep `DataModuleProvider`, `DataCollectionRequest`, and `DataCollectionOutput` parent-visible
- preserve parent-child communication through `data_module`
- leave source mapping, quality diagnostics, exchange endpoints, exchange payload parsing, normalization, mock data, historical cache, transport, and tests outside this movement unless the baseline identifies a required dependency

## Equivalence Baseline

Required gates for the next movement:

- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-001ZE-01 `root.contracts.runtime_support.data_module.collection_orchestration` baseline_plan.
