# v4.16.0 root.contracts.quantscript.runtime_lowering.intent_inference parent residual judgment: single_indicator_intent_inference

> Batch: BE-002IY-01
> Node: `root.contracts.quantscript.runtime_lowering.intent_inference`
> Selected child: `root.contracts.quantscript.runtime_lowering.intent_inference.single_indicator_intent_inference`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `single_indicator_intent_inference` as the next child under `intent_inference`.

`intent_collection_orchestration` is closed. The next remaining cohesive owner is the single-indicator condition builder that converts resolved RSI, MACD, Momentum, and ZScore indicator bindings into `IntentConfig` outputs. This owner is large enough to isolate, has stable inputs/outputs, and does not require moving spread matching, MA ratio recognition, or legacy emit fallback.

## White-Box Boundary

| Field | Boundary |
| --- | --- |
| Input | `IndicatorBinding`, relation direction, threshold, action string, `BindingEnv`, and optional runtime id hint |
| Output | zero or one `IntentConfig` in a `Vec`, or `anyhow::Error` |
| Current owner | `quantscript/src/lowering/intents.rs` |
| Planned owner | `root.contracts.quantscript.runtime_lowering.intent_inference.single_indicator_intent_inference` |
| Parent caller | `intents_from_condition` |
| Exclusions | statement traversal, spread inference, MA ratio inference, legacy emit fallback, data-source canonicalization implementation, relation/id helpers, and agent param projection |

## Symbols Approved For Next Baseline

The next baseline may freeze movement for:

- `single_indicator_intent`

The moved child may call parent-owned helpers for canonicalization, comparison op conversion, comparison shape coding, and shared structured comparison keys through the parent module boundary.

## Split Decision Gate

| Gate | Result | Evidence |
| --- | --- | --- |
| `parent_still_oversized` | PASS | `intent_inference` remains an oversized precision parent after orchestration extraction. |
| `strong_owner_boundary` | PASS | RSI/MACD/Momentum/ZScore intent construction forms one cohesive single-indicator builder. |
| `parent_facade_preserved` | PASS | Parent can keep `intents_from_condition` as dispatcher and call the child. |
| `sibling_exclusion_clear` | PASS | Spread, MA ratio, legacy emit, and relation/id helper bodies remain outside. |

## Risk Classification

| Risk | Result |
| --- | --- |
| Public API / route / schema | Parent-visible intent inference signatures must remain stable |
| Persistence / lock owner | No persistence ownership movement |
| Trading semantics evidence | High; emitted intent ids, kinds, params, and comparison codes affect strategy behavior |
| Cross-crate behavior | Present through `IntentConfig`, `IntentKind`, and `IndicatorBinding` contracts |

## Next Step

BE-002IZ-01 `root.contracts.quantscript.runtime_lowering.intent_inference.single_indicator_intent_inference` baseline_plan.
