# v4.16.0 root.contracts.quantscript.typed_resolution.resolver_orchestration_surface extract closeout

> Batch: BE-002GL-02
> Node: `root.contracts.quantscript.typed_resolution.resolver_orchestration_surface`
> Parent: `root.contracts.quantscript.typed_resolution`
> Stage: `actual_extraction`
> Movement: Rust code movement.
> governance_next_authority: active

---

## Result

`root.contracts.quantscript.typed_resolution.resolver_orchestration_surface` actual extraction is complete.

New child:

- `quantscript/src/resolve/resolver_orchestration_surface.rs`

Parent wiring:

- `quantscript/src/resolve/mod.rs` now declares `mod resolver_orchestration_surface;`.
- `lower_script_to_typed_hir` still calls `Resolver::default().resolve_module(module)`.
- `Resolver` state and support helpers remain parent-owned.

## Moved Methods

Moved into the child as `pub(super)` resolver methods:

- `resolve_module`
- `seed_imported_callables`
- `seed_function_signatures`
- `resolve_function`
- `lower_block`
- `lower_stmt`
- `lower_match_arm`
- `lower_expr`
- `lower_callee_expr`

## Preserved Boundary

Preserved in parent or existing children:

- public resolver DTOs/enums in `public_type_surface`;
- callable/helper classifiers and registry seeding in `callable_classification_surface`;
- semantic/manual indicator inference in `semantic_inference_surface`;
- type inference, binding insertion, condition validation, and type annotation resolution in `type_inference_binding_surface`;
- `make_expr`, `expr_label`, ID allocation, callable target helpers, scalar expression helpers, resolved function builder, return-list helpers, and resolver-local tests.

## Validation

Executed during this batch:

- `cargo fmt`
- `cargo test -p quantscript` PASS, 88 tests

Remaining closeout gates must run before the next commit:

- `cargo fmt --check`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002GL-03 `root.contracts.quantscript.typed_resolution.resolver_orchestration_surface` single_leaf_closeout.
