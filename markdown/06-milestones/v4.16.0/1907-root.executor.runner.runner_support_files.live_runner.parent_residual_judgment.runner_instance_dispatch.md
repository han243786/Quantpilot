# v4.16.0 root.executor.runner.runner_support_files.live_runner parent residual judgment selects runner_instance_dispatch

> Batch: BE-002MG-01
> Parent: `root.executor.runner.runner_support_files.live_runner`
> Stage: `parent_residual_judgment`
> Selected child: `root.executor.runner.runner_support_files.live_runner.runner_instance_dispatch`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `runner_instance_dispatch` as the next child under `live_runner`.

## Evidence

Closed child:

- `v3_live_runner`

Remaining cohesive groups:

- `RunnerInstance` dispatch between V3 and V4 runner variants;
- `V4Runner` and V4 evidence events;
- `RunnerPool` orchestration;
- V4 market metadata helpers;
- local test harness.

`RunnerInstance` is a compact but explicit adapter that owns construction,
event dispatch, subscribed-symbol projection, K-line pool projection, V4 memory
snapshot projection, and stopped-state projection across V3/V4 runner variants.

## Boundary

The next baseline may freeze movement for `RunnerInstance` and its impl only.
It must not move `RunnerPool`, `V4Runner`, V4 helper functions, or tests in the
same extraction step.

## Next Step

BE-002MH-01
`root.executor.runner.runner_support_files.live_runner.runner_instance_dispatch`
baseline_plan.
