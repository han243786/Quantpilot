# v4.16.0 runtime_facade_coordinator.constructor_provider_wiring extract closeout

> Batch: BE-001YH-02
> Node: `root.contracts.runtime_support.runtime_facade_coordinator.constructor_provider_wiring`
> Parent: `root.contracts.runtime_support.runtime_facade_coordinator`
> Stage: `actual_extraction`
> Movement: Code movement complete.

## Extracted Files

- `qrpc_runtime/src/runtime_facade_coordinator/constructor_provider_wiring.rs`

## Code Movement

The constructor/provider wiring methods moved out of `qrpc_runtime/src/runtime_facade_coordinator.rs` into a child module:

- `RuntimeCoordinator::from_core_ir`
- `RuntimeCoordinator::new`
- `RuntimeCoordinator::with_modules_from_core_ir`
- `RuntimeCoordinator::with_modules`
- `RuntimeCoordinator::with_data_module_from_core_ir`
- `RuntimeCoordinator::with_data_module`
- `RuntimeCoordinator::with_intent_module_from_core_ir`
- `RuntimeCoordinator::with_intent_module`
- `RuntimeCoordinator::with_agent_module_from_core_ir`
- `RuntimeCoordinator::with_agent_module`
- `RuntimeCoordinator::with_risk_checker_from_core_ir`
- `RuntimeCoordinator::with_risk_checker`
- `RuntimeCoordinator::with_execution_module_from_core_ir`
- `RuntimeCoordinator::with_execution_module`
- `RuntimeCoordinator::with_module_providers_from_core_ir`
- `RuntimeCoordinator::with_module_providers`

`qrpc_runtime/src/runtime_facade_coordinator.rs` now declares `mod constructor_provider_wiring;` and keeps the `RuntimeCoordinator` type, debug implementation, session/cycle behavior, state/config accessors, portfolio projection, and tests.

## Public API Equivalence

Public constructor calls remain available as `RuntimeCoordinator::*`.

The crate-root facade remains unchanged:

- `qrpc_runtime/src/lib.rs` still exposes `RuntimeCoordinator`
- no public constructor names changed
- no sibling runtime-support module gained a shortcut dependency

## Boundary Result

The child owns constructor and provider injection only.

The parent still owns:

- coordinator type definition
- runtime session/cycle orchestration
- execution and market-data entry points
- state/config accessors
- config generation after construction
- portfolio projection
- coordinator tests

## Equivalence Gates

Required gates for this batch:

- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-001YH-03 `root.contracts.runtime_support.runtime_facade_coordinator.constructor_provider_wiring` single_leaf_closeout.
