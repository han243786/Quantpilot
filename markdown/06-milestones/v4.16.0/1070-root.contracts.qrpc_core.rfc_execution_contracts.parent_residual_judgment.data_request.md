# v4.16.0 root.contracts.qrpc_core.rfc_execution_contracts parent residual judgment selects data_request

> Batch: BE-001SR-01
> Node: `root.contracts.qrpc_core.rfc_execution_contracts`
> Parent: `root.contracts.qrpc_core`
> Selected child: `root.contracts.qrpc_core.rfc_execution_contracts.data_request`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core.rfc_execution_contracts` returns to its child queue after the parent file was physically extracted and judged worth continuing.

Decision:

`next_child: root.contracts.qrpc_core.rfc_execution_contracts.data_request`

## Closed RFC Execution Children

| Child | Result |
| --- | --- |
| none | This is the first child under the newly extracted parent. |

## Open RFC Execution Residuals

| Residual | Status |
| --- | --- |
| `contracts.qrpc_core.rfc_execution_contracts.data_request` | Selected next. Owns RFC-001 data request taxonomy and DTOs. |
| `contracts.qrpc_core.rfc_execution_contracts.allocation` | Queued. Owns allocation DTOs and budget projection helper. |
| `contracts.qrpc_core.rfc_execution_contracts.order_contract` | Queued. Owns order DTOs and transition matrix helper. |
| `contracts.qrpc_core.rfc_execution_contracts.execution_feedback` | Queued. Owns execution feedback event DTOs. |
| `contracts.qrpc_core.rfc_execution_contracts.handoff_snapshot` | Queued. Owns handoff snapshot DTO and completeness validation. |

## Selection Rationale

`data_request` is selected because it is the first contiguous schema family in `qrpc_core/src/rfc_execution_contracts.rs` and has no dependency on later allocation/order/feedback/handoff contracts:

- `MarketScope`;
- `PrimaryDataType`;
- `SourceType`;
- `Timeframe`;
- `TimeRange`;
- `RoundingMode`;
- `PrecisionPolicy`;
- `UsageTag`;
- `DataRequest`.

This child can move before the other RFC execution residuals because the later families only need primitive qrpc-core public names and remain parent-owned until their own baselines.

## White Box Boundary

| Item | Owner | Input | Output | Side effects |
| --- | --- | --- | --- | --- |
| Market scope taxonomy | `data_request` | exchange/market request context | scope enum schema | none |
| Data source taxonomy | `data_request` | primary/source/timeframe request context | data source and timeframe schema | none |
| Precision and time range DTOs | `data_request` | time bounds and rounding policy | request parameter schema | none |
| Data request DTO | `data_request` | instrument, taxonomy enums, optional range/lookback, precision, usage, priority, timestamp | RFC-001 `DataRequest` schema | none |

## Same-Parent Parallel Judgment

`same_parent_parallel_children` remains inactive for this selection.

Reason: this is the first child under the new RFC execution parent. Serial extraction establishes the parent facade/child import pattern before any later homogeneous DTO siblings are considered for a parallel wave.

## Hard Boundaries

The next `data_request` baseline must not:

- edit Rust source code;
- move allocation, order, feedback, or handoff contracts;
- change enum variants, struct fields, derives, serde shape, public exports, or tests;
- reopen closed qrpc-core children;
- change `qrpc_core_ir`, `qrpc_compiler`, `qrpc_runtime`, `quantscript`, backend, executor, frontend, physical `plugins/*`, or release transition;
- introduce direct sibling shortcuts.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `data_request` owns RFC-001 data request taxonomy and `DataRequest`. |
| parent_child_communication_kept | pass | Future child exports are mediated through the `rfc_execution_contracts` parent facade and crate root. |
| equivalence_baseline_freezable | pass | Enum variants, DTO fields, derives, and serde shape can be frozen before movement. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | pass | Data request schema is public qrpc-core contract API. |
| state_machine_phase | no | No runtime state machine or transition matrix is selected. |
| strategy_branch | no | No strategy branch is selected. |
| independent_failure_mode | pass | Data request schema drift is independent from allocation math, order transition, feedback, and handoff validation. |
| reuse_pressure | pass | Runtime/backend request flows consume these names through `qrpc_core::*`. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The selected child is a coherent RFC-001 schema family. |
| communication_cost_rises | no | Parent facade re-export preserves current consumers. |
| local_proof_missing | no | qrpc-core compile/test gates cover the move. |
| line_count_only | no | Split is by schema family ownership, not raw line count. |

leaf_split_decision_result

`continue_baseline: true`; `data_request` is worth selecting as the first RFC execution child.

## Next Step

BE-001SS-01 `root.contracts.qrpc_core.rfc_execution_contracts.data_request` baseline_plan.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
