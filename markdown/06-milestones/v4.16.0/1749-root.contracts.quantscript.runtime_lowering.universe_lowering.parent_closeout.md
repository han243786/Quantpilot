# v4.16.0 root.contracts.quantscript.runtime_lowering.universe_lowering parent closeout

> Batch: BE-002IT-01
> Node: `root.contracts.quantscript.runtime_lowering.universe_lowering`
> Parent: `root.contracts.quantscript.runtime_lowering`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

`universe_lowering` is closed with `parent_closeout: true`.

The closed child set now covers:

- `rebalance_directive_detection`
- `universe_construct_expansion`

## Parent Residual Judgment

The residual parent `quantscript/src/lowering/universe.rs` remains a cohesive terminal owner for universe expression evaluation, metadata selection, instrument pool assembly, and module-local tests.

Further splitting was considered and rejected for this cycle. The remaining functions are tightly coupled to `UniverseValue`, `UniverseSnapshot`, metadata lookup, metric projection, and the three public(crate) universe lowering facades. Splitting them into `universe_expression_evaluation`, `universe_metadata_selection`, or `universe_test_harness` would increase parent-child forwarding and governance overhead without creating a stronger stable owner under the current terminal leaf rules.

## Leaf Granularity Evidence

Command:

- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\evaluate-leaf-granularity.ps1 -LeafId root.contracts.quantscript.runtime_lowering.universe_lowering -ParentId root.contracts.quantscript.runtime_lowering -Path quantscript/src/lowering/universe.rs -Depth 4`

Result:

| Field | Value |
| --- | --- |
| `split_decision` | `STOP` |
| `governance_packaging` | `stop_split` |
| `final_decision` | `STOP` |
| `normalized_split_score` | 48 |
| `loc` | 610 |
| `function_count` | 16 |
| `public_surface` | 3 |
| `branches` | 46 |
| `risk_tags` | `cross_crate`, `persistence`, `public_api`, `trading_semantics` |
| `reason` | medium score without same-parent wave candidate; avoid standalone governance |

## White-Box Boundary

| Field | Result |
| --- | --- |
| Parent input | `ScriptModule`, `LoweringContext`, universe helper expressions, and `UniverseSnapshot` metadata |
| Parent output | `PortfolioRebalanceDirective`, `InstrumentPoolSpec`, expanded `ScriptModule`, or `anyhow::Error` |
| Closed child owners | `rebalance_directive_detection`, `universe_construct_expansion` |
| Residual parent owner | universe expression evaluation, metadata selection, pool assembly, and tests |
| Public API impact | None; public(crate) facades stay stable |

## Closeout Evidence

- `rebalance_directive_detection` is closed with `stop_split: true`.
- `universe_construct_expansion` is closed with `stop_split: true`.
- The residual parent scored `STOP` under terminal leaf control.
- Latest extraction validation passed `cargo test -p quantscript` with 88 tests and `cargo check -p quantpilot`.

## Next Step

Return to `root.contracts.quantscript.runtime_lowering` parent residual judgment.
