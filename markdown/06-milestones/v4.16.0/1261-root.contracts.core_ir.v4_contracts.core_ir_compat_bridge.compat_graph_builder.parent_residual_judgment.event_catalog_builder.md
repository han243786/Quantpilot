# v4.16.0 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder parent residual judgment selects event_catalog_builder

> Batch: BE-001WQ-01
> Node: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder`
> Parent: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge`
> Stage: `parent_residual_judgment`
> Movement: No code movement. This document selects the event-catalog child for extraction.

---

## Summary

root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder parent residual judgment selects event_catalog_builder

The next child is `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder.event_catalog_builder`.

It owns `build_core_ir_compat_event_catalog`, including compat event type specs, event source/scope assignment, payload field definitions, allowed emitters, allowed consumers, replayability, and event catalog metadata.

It does not own graph id construction, machine construction, graph edges, risk-plane binding, machine metadata helpers, memory-count fields, Core IR edge labels, string-array helper, or compat id sanitization.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | PASS | `event_catalog_builder` has a single event catalog construction surface. |
| parent_child_communication_kept | PASS | The graph builder parent can call the event catalog child while graph assembly remains parent-mediated. |
| equivalence_baseline_freezable | PASS | Existing bridge mapping and machine graph validation tests exercise event catalog construction through the public bridge entrypoint. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | PARTIAL | The child is private, but its event catalog is included in the graph returned by the public bridge report. |
| state_machine_phase | PASS | The child defines runtime, machine, and risk-plane event semantics for the compatibility state-machine graph. |
| strategy_branch | PARTIAL | It uses strategy metadata but mainly builds fixed event semantics. |
| independent_failure_mode | PASS | Event catalog source/scope/payload/consumer semantics can fail independently from machine and edge construction. |
| reuse_pressure | PARTIAL | Future bridge event semantics can evolve without modifying machine construction. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | PASS | The selected child owns a complete event catalog construction phase. |
| communication_cost_rises | PASS | The child can accept `CoreStrategyIr` and return `MachineEventCatalog`; no horizontal communication is needed. |
| local_proof_missing | PASS | Existing graph validation tests cover event catalog presence and event references. |
| line_count_only | PASS | Selection is based on event semantic ownership, not line count. |

leaf_split_decision_result

`selected_child: event_catalog_builder`

The graph builder parent remains open. Remaining parent surfaces include graph assembly, compatibility machine builders, graph edges, risk-plane binding, metadata helpers, memory field helper, Core IR edge labels, string array helper, and compat id sanitization.

next_recursive_step

BE-001WR-01 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder.event_catalog_builder baseline_plan
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/core_ir_compat_bridge/compat_graph_builder.rs`

**Markers**:
- `BE-001WQ-01`

**Next step**:
BE-001WR-01 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge.compat_graph_builder.event_catalog_builder baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
