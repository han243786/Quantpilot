# v4.16.0 root.contracts.core_ir.v4_contracts.reproducibility_contract single leaf closeout stops split

> Batch: BE-001VQ-03
> Node: `root.contracts.core_ir.v4_contracts.reproducibility_contract`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.reproducibility_contract` stops splitting after extracting the full
reproducibility evidence contract family.

The child owns:

- reproducibility policy DTO
- run evidence catalog
- event envelope field catalog
- default evidence and envelope field lists
- static reproducibility validation

These pieces form one replay proof: required evidence, event envelope fields, key decision replay, and full-tick replay
reservation must stay together to keep reproducibility policy readable.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `reproducibility_contract` names the complete replay evidence policy boundary. |
| parent_child_communication_kept | pass | The v4 parent re-exports public reproducibility types and only provides schema/default helpers. |
| equivalence_baseline_freezable | pass | BE-001VQ-01 froze required evidence, event envelope fields, replay flags, and validation behavior. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | Reproducibility DTOs remain available through `qrpc_core_ir::v4`. |
| state_machine_phase | no | Reproducibility is replay evidence policy, not state topology. |
| strategy_branch | guarded | Replay evidence requirements guard strategy/run reproducibility. |
| independent_failure_mode | guarded | Missing evidence, missing envelope fields, disabled key replay, and reserved full-tick replay failures belong to one contract. |
| reuse_pressure | guarded | Static bundles, compile-time reports, compatibility reports, and tests reuse reproducibility validation. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | yes | Splitting enums/defaults/validation would create small leaves without independent owners. |
| communication_cost_rises | yes | Further split would route one replay evidence proof across multiple private modules. |
| local_proof_missing | no | `cargo test -p qrpc-core-ir`, `cargo check -p quantpilot`, and commit-time gates passed after extraction. |
| line_count_only | no | The stop decision is based on cohesive reproducibility ownership. |

leaf_split_decision_result

`stop_split: true`

This leaf is complete. Return to `root.contracts.core_ir.v4_contracts` for parent residual judgment.

next_recursive_step

BE-001VR-01 root.contracts.core_ir.v4_contracts parent_residual_judgment
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/reproducibility_contract.rs`

**Markers**:
- `BE-001VQ-03`

**Next step**:
BE-001VR-01 root.contracts.core_ir.v4_contracts parent_residual_judgment

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
