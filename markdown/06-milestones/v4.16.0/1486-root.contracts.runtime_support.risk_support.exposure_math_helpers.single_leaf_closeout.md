# v4.16.0 root.contracts.runtime_support.risk_support.exposure_math_helpers single leaf closeout

> Batch: BE-002CE-02
> Node: `root.contracts.runtime_support.risk_support.exposure_math_helpers`
> Parent: `root.contracts.runtime_support.risk_support`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Decision

`stop_split: true`

`exposure_math_helpers` is a terminal leaf. It is a cohesive shared math pocket with four helper functions, one domain, and no independent public owner beyond the risk-support parent.

## Leaf Granularity Evidence

`tools/evaluate-leaf-granularity.ps1` result:

- decision: `STOP`
- score: `17`
- loc: `39`
- functions: `4`
- domains: `1`
- terminal leaf control bucket: `micro_under_100`
- governance mode: `stop_split`

The script recommends stopping unless stronger ownership evidence exists. No such evidence is present: splitting this leaf further would create wrapper churn and increase governance overhead without improving the module tree.

## Boundary

The leaf owns:

- `available_sell_ratio`
- `portfolio_equity`
- `symbol_net_exposure_ratio`
- `portfolio_net_exposure_ratio`

The parent still owns public risk checking behavior and composes the closed sibling children through parent-mediated imports.

## Gates

Evidence inherited from BE-002CE-01:

- `cargo fmt --check`: pass
- `cargo check -p qrpc-runtime`: pass
- `cargo test -p qrpc-runtime risk -- --nocapture`: pass, 39 tests
- `cargo check -p quantpilot`: pass
- `git diff --check`: pass
- UTF-8 / full feature tree / matrix governance: pass

## Next Step

BE-002CF-01 `root.contracts.runtime_support.risk_support` parent_residual_judgment.
