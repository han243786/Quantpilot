# v4.16.0 root.contracts.quantscript.typed_resolution.public_type_surface baseline plan

> Batch: BE-002GD-01
> Node: `root.contracts.quantscript.typed_resolution.public_type_surface`
> Parent: `root.contracts.quantscript.typed_resolution`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Scope

Freeze the public typed-resolution DTO and enum surface before moving it out of the resolver parent.

Current owner:

- `quantscript/src/resolve/mod.rs`

Planned child:

- `quantscript/src/resolve/public_type_surface.rs`

## Items Frozen For Movement

The next extraction may move only:

- `ResolveResult`
- `impl ResolveResult`
- `ResolvedFunction`
- `ResolvedCallableKind`
- `ChangeHelperKind`
- `KnownIndicatorHelperKind`
- `KnownUniverseHelperKind`
- `MovingAverageHelperKind`
- `RsiHelperKind`
- `ResolvedChangeSmoothingKind`
- `ResolvedFetchSourceKind`
- `ResolvedSeriesCapabilityKind`
- `ResolvedSeriesBoundaryKind`
- `ResolvedWindowAggregateKind`
- `ResolvedBuiltinMathKind`
- `ResolvedMemberMutationKind`
- `ResolvedExprSemantic`
- `ResolvedSeriesViewKind`
- `ResolvedWindowAggregateView`
- `ResolvedManualIndicatorFormula`
- `ResolvedCallable`

## Parent Wiring To Preserve

The parent resolver module must keep:

- `mod public_type_surface;`
- `pub use public_type_surface::{...};`
- existing `crate::resolve::*` downstream import compatibility;
- crate-root `pub use resolve::{...};` compatibility in `quantscript/src/lib.rs`.

## Forbidden Movement

The next extraction must not move or change:

- `lower_script_to_typed_hir`
- `expr_semantic_key`
- `classify_series_capability_name`
- `classify_builtin_math_name`
- `classify_member_mutation_name`
- `FunctionSignature`, `CallableTarget`, `BindingInfo`, `Resolver`
- resolver lowering, type inference, semantic inference, callable registry, helper classification, or tests
- analysis, lowering, evaluator, static audit, test plan, backend, runtime support, compiler bridge, executor, frontend, plugin metadata, or release transition.

## Expected Validation

The extraction must pass:

- `cargo fmt --check`
- `cargo test -p quantscript`
- `cargo check -p quantpilot`
- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002GD-02 `root.contracts.quantscript.typed_resolution.public_type_surface` actual_extraction.
