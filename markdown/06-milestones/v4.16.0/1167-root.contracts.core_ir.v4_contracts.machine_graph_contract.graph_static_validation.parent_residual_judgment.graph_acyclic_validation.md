# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation parent residual judgment selects graph_acyclic_validation

> Batch: BE-001US-01
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation` selects
`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.graph_acyclic_validation`
as the next child.

The selected child owns only `V4MachineGraphContract::validate_graph_acyclic` and its local
DFS/cycle path detection. It does not own root graph validation orchestration, graph schema,
edge identity/endpoint checks, event usage validation, risk-plane validation, event party helper,
or traversal helpers.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `graph_acyclic_validation` is a named white-box child for DAG/cycle validation. |
| parent_child_communication_kept | pass | It reads only `self.edges` and returns a local `Result<(), Vec<String>>`. |
| equivalence_baseline_freezable | pass | The helper has finite adjacency, DFS, and cycle path formatting behavior. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | The public graph validator remains in the parent, while acyclicity is a stable private validation phase. |
| state_machine_phase | pass | DAG validation is the graph topology phase of static validation. |
| strategy_branch | guarded | It protects machine graph topology consumed by bridge/static contract users. |
| independent_failure_mode | pass | Cycle detection and cycle path formatting fail independently from event/risk validation. |
| reuse_pressure | guarded | Reuse remains through parent validation, but the phase is independent and compact. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The child owns a real topology invariant. |
| communication_cost_rises | no | Moving the helper keeps all topology data inside `self.edges`; no extra cross-child communication is needed. |
| local_proof_missing | no | Existing qrpc-core-ir tests cover cycle rejection through the public graph validator. |
| line_count_only | no | Selection is based on graph topology ownership. |

leaf_split_decision_result

`continue_split: true`

Selected next child:

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.graph_acyclic_validation`

next_recursive_step

BE-001UT-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.graph_acyclic_validation baseline_plan
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation.rs`

**Markers**:
- `BE-001US-01`

**Next step**:
BE-001UT-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.graph_acyclic_validation baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
