# v4.16.0 root.contracts.runtime_support.agent_module.shared_scoring_portfolio_helpers single leaf closeout

> Batch: BE-002AD-02
> Node: `root.contracts.runtime_support.agent_module.shared_scoring_portfolio_helpers`
> Parent: `root.contracts.runtime_support.agent_module`
> Stage: `single_leaf_closeout`
> Movement: No Rust code movement.

---

## Decision

`stop_split: true`

`root.contracts.runtime_support.agent_module.shared_scoring_portfolio_helpers` is closed as a parent-owned terminal helper pocket.

## Evidence

Current owner file:

- `qrpc_runtime/src/agent_module.rs`

Closed policy children continue to access the helper pocket through the parent boundary:

- `weighted_signal_decisions`
- `portfolio_rebalance`
- `cross_venue_arbitrage`

## Why No Physical Extraction

No helper child file is opened because:

- the helper pocket is shared by multiple sibling policy children;
- direct helper-module access from those children would create sibling coupling;
- parent wrapper/re-export plumbing would add indirection without stronger ownership clarity;
- current `super::` access keeps communication parent-mediated;
- this closeout matches terminal-leaf control goals by avoiding helper micro-module churn.

## Preserved Helper Ownership

The parent remains owner of:

- threshold and spread constants;
- signal-kind indexing;
- signal score calculation;
- available/current position ratio helpers;
- portfolio equity helper.

## Equivalence Evidence

No Rust code movement happened in this closeout. The preceding arbitrage extraction already passed:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`: 227 passed, 2 ignored
- `cargo check -p quantpilot`
- `git diff --check`
- `tools/check-utf8.ps1`
- `tools/check-full-feature-tree.ps1`
- `tools/check-matrix-governance.ps1`

## Next Step

BE-002AE-01 `root.contracts.runtime_support.agent_module` parent_residual_judgment selects `test_harness`.
