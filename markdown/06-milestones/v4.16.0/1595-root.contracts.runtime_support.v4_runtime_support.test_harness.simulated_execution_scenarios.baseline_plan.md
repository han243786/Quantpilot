# v4.16.0 root.contracts.runtime_support.v4_runtime_support.test_harness.simulated_execution_scenarios baseline plan

> Batch: BE-002FJ-01
> Node: `root.contracts.runtime_support.v4_runtime_support.test_harness.simulated_execution_scenarios`
> Parent: `root.contracts.runtime_support.v4_runtime_support.test_harness`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Freeze the `simulated_execution_scenarios` equivalence baseline.

The next movement may create a test-only child module for simulated execution scenario tests and must preserve all assertions, input literals, order request fields, event expectations, and parent-mediated fixture access.

## Frozen Test Set

Only these tests may move in the next extraction:

- `v4_paper_simulated_runtime_runs_compat_bridge_graph_until_execution_ready`
- `v4_runtime_simulated_execution_config_controls_fill_fee_and_asset_curve`
- `v4_runtime_updates_simulated_portfolio_from_market_price`
- `v4_runtime_triggers_stop_market_order_on_market_price_update`
- `v4_runtime_oco_bracket_fills_one_leg_and_cancels_the_other`
- `v4_runtime_trailing_stop_updates_trigger_then_fills`
- `v4_runtime_gtd_order_expires_on_advance_time`
- `v4_runtime_amends_open_order_without_replacing_order_id`
- `v4_backtest_tick_replay_is_deterministic_and_reports_micro_metrics`

## Boundary

The extraction must not move:

- `v4_runtime_rejects_order_when_request_uses_undeclared_capability`;
- runtime recovery/cache snapshot tests;
- live actual mode boundary tests;
- capability policy rejection tests;
- risk-plane source rejection tests;
- fixture builders;
- malformed payload/config rejection tests;
- graph/replay scenario tests already extracted;
- production v4 runtime code;
- any release-transition optimization.

## Expected Code Shape

| Path | Expected change |
| --- | --- |
| `qrpc_runtime/src/v4_runtime/test_harness/simulated_execution_scenarios.rs` | New test-only child with the nine frozen tests and `use super::*;`. |
| `qrpc_runtime/src/v4_runtime/test_harness.rs` | Add `mod simulated_execution_scenarios;` and remove only the moved test bodies. |

No public API, runtime state machine behavior, or production module boundary changes are allowed.

## Equivalence Checks

Run after extraction:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime v4 -- --nocapture`
- `cargo check -p quantpilot`
- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-002FK-01 `root.contracts.runtime_support.v4_runtime_support.test_harness.simulated_execution_scenarios` actual_extraction.
