# v4.16.0 root.contracts.runtime_support.agent_module.test_harness extract closeout

> Batch: BE-002AF-02
> Node: `root.contracts.runtime_support.agent_module.test_harness`
> Parent: `root.contracts.runtime_support.agent_module`
> Stage: `actual_extraction`
> Movement: Rust test code movement.

---

## Result

`root.contracts.runtime_support.agent_module.test_harness` has been physically extracted as a test-only child module.

New owner file:

- `qrpc_runtime/src/agent_module/test_harness.rs`

Parent file:

- `qrpc_runtime/src/agent_module.rs`

## Moved Surface

Moved into the child:

- local `agent_module` `#[cfg(test)] mod tests` contents;
- sample execution-rule fixture;
- sample Core IR and portfolio fixtures;
- weighted signal decision tests;
- cross-venue arbitrage tests;
- portfolio rebalance tests;
- long-term symbol and exchange inheritance tests.

Parent changes:

- added `#[cfg(test)] mod test_harness;`
- removed the inline test module from the parent;
- restored the shared `MIN_QUANTITY_RATIO` constant as a standalone parent-owned const after the previous mojibake comment had swallowed it.

## Preserved Parent Ownership

The parent still owns:

- production dispatch and event assembly;
- public request/output/provider/builtin types;
- policy child declarations;
- shared helper ownership and constants.

The test child uses parent-owned private surfaces through `use super::*`; no production visibility was widened.

## Equivalence Notes

This extraction does not change production code behavior. Test names now resolve under `agent_module::test_harness::*`, while fixture construction, assertions, and helper access remain equivalent.

## Gates

Passed:

- `cargo fmt --check`
- `cargo check -p qrpc-runtime`
- `cargo test -p qrpc-runtime`: 227 passed, 2 ignored
- `cargo check -p quantpilot`

Remaining for commit gate:

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-matrix-governance.ps1`

## Next Step

BE-002AF-03 `root.contracts.runtime_support.agent_module.test_harness` single_leaf_closeout.
