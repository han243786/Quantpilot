# v4.16.0 root.contracts.core_ir.v4_contracts.schema_identity_constants baseline plan

> Batch: BE-001TV-01
> Node: `root.contracts.core_ir.v4_contracts.schema_identity_constants`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `baseline_plan`
> Movement: No code movement.

---

## Summary

This baseline freezes the v4 schema identity constants currently embedded near the top of `qrpc_core_ir/src/v4.rs`.

The next implementation step may move only the frozen constants into `qrpc_core_ir/src/v4/schema_identity_constants.rs`. Public names must remain available through the v4 parent module.

## Frozen Public Surface

| Constant | Frozen value |
| --- | --- |
| `V4_MACHINE_CONTRACT_VERSION` | `quantpilot/machine-contract/v1` |
| `V4_VENUE_CAPABILITY_MATRIX_VERSION` | `quantpilot/venue-capability-matrix/v1` |
| `V4_QS_STATE_MACHINE_PROFILE_VERSION` | `quantpilot/qs-state-machine-profile/v1` |
| `V4_MACHINE_GRAPH_CONTRACT_VERSION` | `quantpilot/machine-graph-contract/v1` |
| `V4_MACHINE_EVENT_CATALOG_VERSION` | `quantpilot/machine-event-catalog/v1` |
| `V4_RUNTIME_MODE_CONTRACT_VERSION` | `quantpilot/runtime-mode-contract/v1` |
| `V4_QS_TYPE_SYSTEM_VERSION` | `quantpilot/qs-type-system/v1` |
| `V4_STATIC_CONTRACT_BUNDLE_VERSION` | `quantpilot/static-contract-bundle/v1` |
| `V4_VERSION_MANIFEST_VERSION` | `quantpilot/version-manifest/v1` |
| `V4_PLUGIN_GOVERNANCE_VERSION` | `quantpilot/plugin-governance/v1` |
| `V4_REPRODUCIBILITY_CONTRACT_VERSION` | `quantpilot/reproducibility-contract/v1` |
| `V4_COMPLEXITY_BUDGET_CONTRACT_VERSION` | `quantpilot/complexity-budget/v1` |
| `V4_LEARNING_PIPELINE_CONTRACT_VERSION` | `quantpilot/learning-pipeline/v1` |
| `V4_COMPILE_TIME_CAPABILITY_REQUEST_VERSION` | `quantpilot/compile-time-capability-request/v1` |
| `V4_COMPILE_TIME_CAPABILITY_REPORT_VERSION` | `quantpilot/compile-time-capability-report/v1` |
| `V4_CORE_IR_COMPAT_BRIDGE_VERSION` | `quantpilot/core-ir-compat-bridge/v1` |
| `V4_BACKTEST_ARTIFACT_VERSION` | `quantpilot/v4-backtest-artifact/v1` |
| `V4_COMPAT_OBSERVATION_MACHINE_ID` | `compat.observation` |
| `V4_COMPAT_DECISION_MACHINE_ID` | `compat.decision` |
| `V4_COMPAT_EXECUTION_MACHINE_ID` | `compat.execution` |
| `V4_COMPAT_CORE_IR_LOADED_EVENT` | `compat.core_ir_loaded` |
| `V4_COMPAT_OBSERVATION_READY_EVENT` | `compat.observation_ready` |
| `V4_COMPAT_RISK_APPROVED_EVENT` | `compat.risk_approved` |
| `V4_RUNTIME_EVENT_REJECTED_EVENT` | `v4.runtime.event_rejected` |
| `V4_RISK_PLANE_MIN_PRIORITY` | `9_000` |
| `V4_QS_TYPE_MAX_NESTING_DEPTH` | `8` |

## Explicit Non-Owners

These later public arrays remain outside this child:

- `V4_RUNTIME_EXECUTION_EVENTS`;
- `V4_FIRST_WAVE_QS_SCALAR_TYPES`;
- `V4_FIRST_WAVE_QS_COMPOSITE_TYPES`;
- `V4_FIRST_WAVE_EXECUTION_CAPABILITIES`.

They belong to runtime mode, QS type-system, and venue capability families respectively.

## Allowed Movement

BE-001TV-02 may:

- create `qrpc_core_ir/src/v4/schema_identity_constants.rs`;
- add `mod schema_identity_constants;` and `pub use schema_identity_constants::*;` to `qrpc_core_ir/src/v4.rs`;
- move only the frozen constants listed above.

## Forbidden Movement

BE-001TV-02 must not:

- move DTOs, enums, impls, validators, default helpers, bridge helpers, public arrays from other families, or tests;
- change constant names, values, types, visibility, or ordering;
- change v1 contracts, qrpc-core, compiler, runtime, backend, executor, frontend, physical `plugins/*`, or release transition;
- introduce direct sibling shortcuts.

## Parent-Child Communication

The v4 parent remains the local gateway:

- constants move into a private v4 child module;
- `qrpc_core_ir::v4::*` remains stable through parent re-export;
- all existing v4 references in the parent continue resolving through the parent namespace.

## Equivalence Baseline

| Check | Baseline |
| --- | --- |
| Current file | `qrpc_core_ir/src/v4.rs` owns the frozen constants. |
| Current proof | `cargo test -p qrpc-core-ir` passes before extraction. |
| Required post-extraction proof | `cargo check -p qrpc-core-ir`, `cargo test -p qrpc-core-ir`, and governance checks must pass. |

## Next Step

BE-001TV-02 `root.contracts.core_ir.v4_contracts.schema_identity_constants` extract_closeout.

## Gates

- `git diff --check`
- `cargo fmt --check -p qrpc-core-ir`
- `cargo check -p qrpc-core-ir`
- `cargo test -p qrpc-core-ir`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
