# v4.16.0 root.contracts parent residual judgment selects quantscript

> Batch: BE-002FU-01
> Node: `root.contracts`
> Selected child: `root.contracts.quantscript`
> Stage: `parent_residual_judgment`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Parent State

Closed `root.contracts` children:

- `contracts.api_surface`
- `contracts.qrpc_core`
- `contracts.core_ir`
- `contracts.compiler_bridge`
- `contracts.runtime_support`

Remaining root/contracts queue:

- `contracts.quantscript`
- `contracts.plugin_metadata`

The separate root-level Rust residual `executor` remains queued outside the contracts parent.

## Decision

Select `root.contracts.quantscript` as the next contracts child.

This is a parent-selection step only. The QuantScript crate is an independent language-contract subsystem rather than a terminal leaf. It owns formal QS parsing, typed HIR, resolve semantics, lowering, diagnostics, static audit, compatibility config parsing, authoring samples, and boundary samples.

## Evidence

Current physical surface:

- `quantscript/src/lib.rs`
- `quantscript/src/script.rs`
- `quantscript/src/resolve.rs`
- `quantscript/src/analysis.rs`
- `quantscript/src/evaluator.rs`
- `quantscript/src/v4_static_audit.rs`
- `quantscript/src/lowering/*`
- `quantscript/src/hir.rs`
- `quantscript/src/types.rs`
- `quantscript/src/test_plan.rs`
- `quantscript/*.md`
- `quantscript/*.qs`
- `quantscript/authoring_samples/*`
- `quantscript/boundary_samples/*`

Observed size and responsibility signals:

- `resolve.rs`, `analysis.rs`, `v4_static_audit.rs`, `script.rs`, `evaluator.rs`, and `lowering/*` are all non-trivial responsibility clusters.
- `quantscript/src/lib.rs` exposes public parser/analyzer/lowering/audit functions and compatibility config entrypoints consumed by backend, compiler, runtime, and tests.
- The `lowering` sub-tree already has multiple physical child modules, so the next baseline should preserve that parent/child shape instead of flattening it.

## Boundary

The next baseline must freeze only the `root.contracts.quantscript` contract. It must not change:

- QRPC Core, Core IR, compiler, runtime support, plugin metadata, backend, executor, frontend, or E2E behavior;
- public serialized schema or runtime protocol structures;
- formal QS diagnostics, error text, HIR semantics, static audit result shape, or lowering output;
- release-transition shortcuts.

Child-to-child communication must stay parent-mediated through the QuantScript crate parent and its existing module exports.

## Next Step

BE-002FV-01 `root.contracts.quantscript` baseline_plan.
