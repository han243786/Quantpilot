# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.event_catalog single leaf closeout stops split

> Batch: BE-001UG-03
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.event_catalog`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract.event_catalog` closes as one compact schema/validation leaf.

The file owns the event catalog schema family and one local catalog validation method. Further splitting would create DTO-only or helper-only fragments and would separate catalog schema from its local invariant gate.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | The leaf owns event catalog schema and local catalog validation. |
| parent_child_communication_kept | pass | The graph parent consumes catalog types through the child re-export. |
| equivalence_baseline_freezable | pass | BE-001UG-01 froze fields, serde defaults, validation branches, and error strings. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | Public catalog DTOs are already grouped; no smaller public behavior boundary remains. |
| state_machine_phase | guarded | Catalog event typing is one graph phase, but inner DTOs are not independent modules. |
| strategy_branch | no | No strategy branch behavior lives in the leaf. |
| independent_failure_mode | guarded | Local validation failures exist but belong to the same catalog invariant gate. |
| reuse_pressure | no | Reuse is through the catalog schema as a whole. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | pass | Splitting payload fields/source/scope would create tiny DTO-only owners. |
| communication_cost_rises | pass | Smaller leaves would increase imports into graph validation and tests. |
| local_proof_missing | no | `cargo test -p qrpc-core-ir` passed after extraction. |
| line_count_only | pass | Further split would be line-count driven. |

leaf_split_decision_result

`stop_split: true`

This child is closed. The recursion returns to `root.contracts.core_ir.v4_contracts.machine_graph_contract` and selects graph static validation next.

## Boundary

**Real files**:

- `qrpc_core_ir/src/v4/machine_graph_contract/event_catalog.rs`

**Markers**:

- `BE-001UG-03`

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-001UH-01 `root.contracts.core_ir.v4_contracts.machine_graph_contract` parent_residual_judgment selects `graph_static_validation`.
