# v4.16.0 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge single leaf closeout continues split

> Batch: BE-001WI-03
> Node: `root.contracts.core_ir.v4_contracts.core_ir_compat_bridge`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `single_leaf_closeout`
> Movement: No code movement. This closeout decides whether the extracted bridge leaf should continue splitting.

---

## Summary

root.contracts.core_ir.v4_contracts.core_ir_compat_bridge single leaf closeout continues split

The extracted bridge is a valid facade leaf, but it is not yet a terminal leaf. `qrpc_core_ir/src/v4/core_ir_compat_bridge.rs` is about 1045 lines and contains three independently changing behavior groups: report/phase4 validation, Core IR validation, and v4 compatibility graph construction.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | PASS | `core_ir_compat_bridge` owns the compatibility report DTOs, bridge entrypoint, Core IR validation helpers, graph/event builders, and bridge diagnostics. |
| parent_child_communication_kept | PASS | The parent facade communicates through `mod core_ir_compat_bridge;` and `pub use core_ir_compat_bridge::*;`; public callers still use `qrpc_core_ir::v4::*`. |
| equivalence_baseline_freezable | PASS | BE-001WI-02 passed `cargo fmt -p qrpc-core-ir`, `cargo test -p qrpc-core-ir`, and `cargo check -p quantpilot`. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | PASS | Public bridge entrypoint and public report DTOs are distinct from private validators/builders. |
| state_machine_phase | PASS | The builder section constructs Observation, Decision, and Execution v4 compatibility machines plus event catalog and risk-plane binding. |
| strategy_branch | PASS | Validation branches over data bindings, indicators, signals, agents, risks, execution, and graph edges. |
| independent_failure_mode | PASS | Core IR reference errors (`V4BRIDGE040`-`046`) and v4 static graph errors (`V4BRIDGE900`) fail for different reasons and should be isolated. |
| reuse_pressure | PARTIAL | Current reuse is internal, but validation and builder groups are likely future bridge extension points. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | PASS | Candidate children have concrete owners: `core_ir_validation`, `compat_graph_builder`, and report validation/DTO surface. |
| communication_cost_rises | PASS | Child boundaries can remain parent-mediated through private modules without sibling horizontal calls. |
| local_proof_missing | PASS | Existing bridge tests exercise accepted graph mapping and rejection paths for missing data/risk, cycles, and unknown edge endpoints. |
| line_count_only | PASS | The split is justified by independent responsibilities and failure modes, not only by the 1045-line size. |

leaf_split_decision_result

`continue_split: true`

The next child should be `core_ir_validation`, because it owns the largest independent failure surface: Core IR node identity collection, edge endpoint validation, indicator/data/signal/agent/risk reference checks, and scalar/series/custom expression reference validation.

next_recursive_step

BE-001WJ-01 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge parent_residual_judgment selects core_ir_validation
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/core_ir_compat_bridge.rs`

**Markers**:
- `BE-001WI-03`

**Next step**:
BE-001WJ-01 root.contracts.core_ir.v4_contracts.core_ir_compat_bridge parent_residual_judgment selects core_ir_validation

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
