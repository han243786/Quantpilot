# v4.16.0 root.executor.runner.runner_support_files.live_runner parent residual judgment selects v4_market_metadata_helpers

> Batch: BE-002MM-01
> Parent: `root.executor.runner.runner_support_files.live_runner`
> Stage: `parent_residual_judgment`
> Selected child: `root.executor.runner.runner_support_files.live_runner.v4_market_metadata_helpers`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Decision

Select `v4_market_metadata_helpers` as the next child under `live_runner`.

## Evidence

Closed children:

- `v3_live_runner`
- `runner_instance_dispatch`
- `v4_runner`
- `runner_pool_orchestration`

Remaining cohesive groups:

- V4 market metadata helpers;
- local test harness.

The helper group owns V4 execution capability matrix construction plus graph
metadata extraction for default venue and symbol resolution. It serves the V4
runner constructor and the local V4 test fixture.

## Boundary

The next baseline may freeze movement for:

- `executor_v4_market_matrix`
- `resolve_v4_runner_venue_id`
- `resolve_v4_runner_default_symbol`
- `graph_metadata_string`

It must not move the local test harness in the same extraction step.

## Next Step

BE-002MN-01
`root.executor.runner.runner_support_files.live_runner.v4_market_metadata_helpers`
baseline_plan.
