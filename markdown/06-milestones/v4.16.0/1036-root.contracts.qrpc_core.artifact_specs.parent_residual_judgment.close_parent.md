# v4.16.0 root.contracts.qrpc_core.artifact_specs parent residual judgment closes parent

> Batch: BE-001RZ-01
> Node: `root.contracts.qrpc_core.artifact_specs`
> Parent: `root.contracts.qrpc_core`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.qrpc_core.artifact_specs` returns from `artifact_bundle_contract` closeout with all planned artifact specs children closed.

Decision:

`parent_closeout: true`

The parent is now a compact artifact specs facade. It owns child module declarations and public re-exports, while child modules own their digest, run/backtest spec, and artifact bundle contract surfaces.

## Closed Artifact Specs Children

| Child | Result |
| --- | --- |
| `contracts.qrpc_core.artifact_specs.canonical_digest` | Closed with `stop_split: true`. |
| `contracts.qrpc_core.artifact_specs.run_backtest_specs` | Closed with `stop_split: true`. |
| `contracts.qrpc_core.artifact_specs.artifact_bundle_contract` | Closed with `stop_split: true`. |

## Parent Residual Evaluation

| Residual | Decision |
| --- | --- |
| Child `mod` declarations | Keep parent-owned. They define the artifact specs module tree. |
| Public `pub use` facade | Keep parent-owned. It preserves `qrpc_core::artifact_specs::*` and `qrpc_core::*` public surfaces. |
| Direct child-to-child imports | None introduced. Bundle contracts reference digest through the parent facade. |
| Further artifact specs splitting | Stop. Every planned artifact specs child owner has closed. |

## Parent Facade Proof

`qrpc_core/src/artifact_specs.rs` now contains only:

- `mod artifact_bundle_contract;`
- `mod canonical_digest;`
- `mod run_backtest_specs;`
- `pub use artifact_bundle_contract::*;`
- `pub use canonical_digest::*;`
- `pub use run_backtest_specs::*;`

## White-Box Boundary

| Input | Processing owner | Output |
| --- | --- | --- |
| Artifact spec child module proposals and public surface requests | `contracts.qrpc_core.artifact_specs` | Stable artifact specs parent facade, module declarations, and public re-exports |

The parent may describe and guard:

- artifact specs child module declarations;
- artifact specs public re-export surface;
- parent-mediated child ownership boundaries;
- no-release-transition sibling dependency rules.

## Hard Boundaries Held

BE-001RZ-01 does not:

- move Rust code;
- change digest algorithms, serde shapes, run/backtest spec fields, artifact bundle fields, public exports, or tests;
- create direct sibling-to-sibling imports that bypass the artifact specs parent;
- change runtime IO contracts, RFC execution contracts, qrpc-core parent residuals, compiler/runtime/backend/executor/frontend behavior, or release transition.

## Gates

- `git diff --check`
- `cargo fmt --check`
- `cargo check -p qrpc-core`
- `cargo test -p qrpc-core`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`

## Next Step

BE-001SA-01 `root.contracts.qrpc_core` parent_residual_judgment selects `contracts.qrpc_core.runtime_io_contract`.
