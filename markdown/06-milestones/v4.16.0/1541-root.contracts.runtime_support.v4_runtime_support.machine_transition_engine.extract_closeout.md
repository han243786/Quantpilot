# v4.16.0 root.contracts.runtime_support.v4_runtime_support.machine_transition_engine extract closeout

> Batch: BE-002DW-01
> Node: `root.contracts.runtime_support.v4_runtime_support.machine_transition_engine`
> Parent: `root.contracts.runtime_support.v4_runtime_support`
> Stage: `actual_extraction`
> Movement: Rust code movement.

---

## Extracted Child

`qrpc_runtime/src/v4_runtime/machine_transition_engine.rs` now owns the v4 machine transition engine surface:

- `V4PaperSimulatedRuntime::process_event`
- `V4PaperSimulatedRuntime::transition_candidate_for_machine`
- `V4PaperSimulatedRuntime::machine_transition_preflight`
- `V4PaperSimulatedRuntime::payload_for_emitted_event`
- `MachineTransitionPreflight`
- `matching_transition`

The v4 runtime parent now declares `mod machine_transition_engine;`.

## Boundary Preserved

The extraction deliberately keeps these parent-owned:

- event payload schema validation;
- shared event construction helpers;
- `machine_spec` and `find_machine_spec`;
- risk-plane decision evaluation and recording;
- execution-capability decision evaluation and recording;
- simulated execution order/fill application;
- v4 tests.

`process_event` is `pub(super)` because the closed `event_replay_orchestration` child drains the queue and calls it.

## Equivalence Notes

- Candidate selection still checks parent and child machine transition matches.
- Candidate sorting still prefers higher priority and then stable sort id.
- Unsupported guards and undeclared memory writes still fail closed through rejected runtime events.
- State id, machine status, last-event time, memory writes, cached output, transition-applied event, and emitted machine events remain equivalent.
- Risk/execution gate hooks and simulated execution hooks are still called from the transition path but remain parent-owned.

## Verified Gates

- `cargo fmt --check`: PASS
- `cargo check -p qrpc-runtime`: PASS
- `cargo test -p qrpc-runtime v4 -- --nocapture`: PASS, 32 passed
- `cargo check -p quantpilot`: PASS

Pending governance gates are expected to run before commit:

- `git diff --check`
- UTF-8 check
- full feature tree check
- matrix governance check

## Next Step

BE-002DW-02 `root.contracts.runtime_support.v4_runtime_support.machine_transition_engine` single_leaf_closeout.
