# v4.16.0 root.contracts.core_ir.v4_contracts.complexity_budget_contract single leaf closeout stops split

> Batch: BE-001VS-03
> Node: `root.contracts.core_ir.v4_contracts.complexity_budget_contract`
> Parent: `root.contracts.core_ir.v4_contracts`
> Stage: `single_leaf_closeout`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.complexity_budget_contract` stops splitting after extracting the full complexity
budget and metrics contract family.

The child owns:

- complexity budget DTO and defaults
- complexity metrics DTO
- static budget validation
- metric over-budget validation
- graph-derived complexity metric calculation

These pieces form one complexity proof: budget limits, computed metrics, graph topology-derived counts, and over-budget
diagnostics must stay together to keep complexity policy readable.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `complexity_budget_contract` names the complete complexity budget and metrics boundary. |
| parent_child_communication_kept | pass | The v4 parent re-exports public complexity types; graph traversal remains a sibling input. |
| equivalence_baseline_freezable | pass | BE-001VS-01 froze budget defaults, metrics fields, validation behavior, and graph metric calculation. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | Complexity budget and metric DTOs remain available through `qrpc_core_ir::v4`. |
| state_machine_phase | guarded | Metrics inspect graph topology but do not own graph state schema or graph validation. |
| strategy_branch | guarded | Complexity budgets guard strategy graph expansion and compile-time capability checks. |
| independent_failure_mode | guarded | Zero-budget and over-budget failures belong to one complexity policy contract. |
| reuse_pressure | pass | Static bundles, compile-time reports, compatibility bridge, and tests reuse complexity metrics and budget validation. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | yes | Splitting budget DTOs, metrics DTOs, defaults, and graph metric calculation would create small leaves without independent owners. |
| communication_cost_rises | yes | Further split would route one complexity proof across multiple private modules and report call sites. |
| local_proof_missing | no | `cargo test -p qrpc-core-ir`, `cargo check -p quantpilot`, and commit-time gates passed after extraction. |
| line_count_only | no | The stop decision is based on cohesive complexity policy ownership. |

leaf_split_decision_result

`stop_split: true`

This leaf is complete. Return to `root.contracts.core_ir.v4_contracts` for parent residual judgment.

next_recursive_step

BE-001VT-01 root.contracts.core_ir.v4_contracts parent_residual_judgment
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/complexity_budget_contract.rs`

**Markers**:
- `BE-001VS-03`

**Next step**:
BE-001VT-01 root.contracts.core_ir.v4_contracts parent_residual_judgment

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
