# v4.16.0 root.executor.runner.market_stream_routes baseline plan

> Batch: BE-002LL-01
> Node: `root.executor.runner.market_stream_routes`
> Stage: `baseline_plan`
> Movement: No Rust code movement.
> governance_next_authority: active

---

## Goal

Freeze the market stream route boundary before Rust movement.

The first movement may extract only the route handlers from
`src-executor/main.rs` into `src-executor/market_stream_routes.rs`.

## Frozen Boundary

| Field | Value |
| --- | --- |
| Planned file | `src-executor/market_stream_routes.rs` |
| Approved handlers | `get_klines`, `strategy_events_sse` |
| Parent callers | route registration in `src-executor/main.rs` |
| Parent retained | runner pool setup, shared state types, strategy lifecycle routes, provider order routes, v4 deploy helpers |

## Equivalent Behavior

The extracted child must preserve:

- recent kline projection response shape;
- runner kline pool lookup behavior;
- fallback executor state kline buffer behavior;
- strategy event SSE connection behavior;
- initial v4 runtime memory snapshot emission behavior;
- trigger and audit event stream behavior;
- SSE keepalive behavior.

## Exclusions

- Do not move strategy lifecycle handlers already closed.
- Do not move OKX provider route handlers already closed.
- Do not move v4 deploy helper bodies.
- Do not move runner setup or support files.
- Do not introduce release-transition links.

## Gates For Actual Extraction

| Gate | Command |
| --- | --- |
| format | `cargo fmt --all --check` |
| executor compile | `cargo check -p quantpilot --bin executor` |
| diff hygiene | `git diff --check` |

## Next Step

BE-002LL-02 `root.executor.runner.market_stream_routes`
actual_extraction.
