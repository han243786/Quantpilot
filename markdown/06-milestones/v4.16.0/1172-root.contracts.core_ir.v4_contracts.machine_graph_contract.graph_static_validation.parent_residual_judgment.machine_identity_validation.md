# v4.16.0 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation parent residual judgment selects machine_identity_validation

> Batch: BE-001UV-01
> Node: `root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation`
> Parent: `root.contracts.core_ir.v4_contracts.machine_graph_contract`
> Stage: `parent_residual_judgment`
> Movement: No code movement.

---

## Summary

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation` selects
`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.machine_identity_validation`
as the next child.

The selected child owns machine inventory validation: required machine IDs, duplicate top-level
machine IDs, duplicate IDs across nested machines, `machines_by_id`/`all_machines_by_id` map
construction, and per-machine static contract delegation. It does not own schema version checks,
graph ID checks, edge identity/endpoint/self-loop checks, DAG validation, event usage validation,
risk-plane validation, event party helper, graph schema, or traversal helpers beyond parent-mediated
machine family collection.

## leaf_split_decision_gate

leaf_split_base_gate

| Rule | Result | Evidence |
| --- | --- | --- |
| white_box_boundary_named | pass | `machine_identity_validation` is a named white-box child for graph machine inventory validation. |
| parent_child_communication_kept | pass | It can return top-level/all-machine maps and errors to the parent without sibling calls. |
| equivalence_baseline_freezable | pass | Required ID, duplicate ID, nested duplicate ID, and delegated machine validation branches are finite. |

leaf_split_positive_trigger

| Trigger | Result | Evidence |
| --- | --- | --- |
| public_or_handler_boundary | guarded | The public graph validator remains in the parent, while machine identity is a private graph input phase. |
| state_machine_phase | pass | Machine inventory validation occurs before edge, event, and risk phases. |
| strategy_branch | guarded | It protects machine IDs and nested machine validation consumed by later graph phases. |
| independent_failure_mode | pass | Machine identity/delegation failures are independent from edge, DAG, event, and risk validation. |
| reuse_pressure | guarded | The maps produced by this phase are reused by later parent-dispatched validation phases. |

leaf_split_stop_condition

| Stop condition | Result | Evidence |
| --- | --- | --- |
| micro_leaf_without_owner | no | The child owns a real graph input invariant and produces later-phase maps. |
| communication_cost_rises | no | A result struct can carry maps/errors back to the parent cleanly. |
| local_proof_missing | no | Existing qrpc-core-ir tests cover machine/graph validation through the public validator. |
| line_count_only | no | Selection is based on behavior ownership and data production. |

leaf_split_decision_result

`continue_split: true`

Selected next child:

`root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.machine_identity_validation`

next_recursive_step

BE-001UW-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.machine_identity_validation baseline_plan
## Boundary

**Real files**:
- `qrpc_core_ir/src/v4/machine_graph_contract/static_validation.rs`

**Markers**:
- `BE-001UV-01`

**Next step**:
BE-001UW-01 root.contracts.core_ir.v4_contracts.machine_graph_contract.graph_static_validation.machine_identity_validation baseline_plan

---

## Gates

- `git diff --check`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-full-feature-tree.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-matrix-governance.ps1`
