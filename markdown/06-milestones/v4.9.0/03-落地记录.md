# v4.9.0 落地记录 — 功能完整度与插件执行安全

> 日期: 2026-05-26
> 范围: PaperActual 安全启动、插件执行安全、策略包、设置页、API 治理、沙箱重试、执行端图表
> 裁剪: 账户相关内容不纳入本轮

---

## 已落地

| 项 | 结果 | 代表文件 |
|----|------|----------|
| PaperActual 启动门禁 | 移除 `NOT_IMPLEMENTED` 启动阻断, 改为启动前验证 OKX demo 凭证 | `src-executor/main.rs` |
| 插件执行安全 | 新增 `PluginSandbox::execute_checked`, 执行前接入 registry security check | `qrpc_runtime/src/plugin_sandbox.rs` |
| 插件安全测试 | 覆盖 network allow/deny 与 credential deny | `qrpc_runtime/src/plugin_runtime_registry.rs` |
| 策略包导入/导出 | 顶部工具栏支持 `.quantpilot-strategy.json` 导入导出 | `frontend/src/components/TopToolbar.jsx`, `graphStorePersistenceActions.js` |
| 设置页 | 统一管理语言、主题、QS 草稿策略和 profile 摘要入口 | `frontend/src/pages/SettingsPage.jsx` |
| API 版本治理 | 新增 route diff 脚本并补齐 OpenAPI executor 路径 | `tools/check-openapi-route-diff.ps1`, `contracts/openapi/root.yaml` |
| 沙箱失败处理 | 审批页显示沙箱待处理状态并提供手动重新请求 | `frontend/src/components/ApprovalPanel.jsx` |
| 执行端 K 线控制 | 增加周期、bars preset 和数据源说明 | `frontend-executor/src/components/KlineChart.jsx` |

## 验证

```powershell
cargo check --workspace
cargo test --workspace --no-run
cargo test -p qrpc-runtime plugin_runtime_registry
cargo test -p quantpilot --bin executor okx_demo_order_request_validation_keeps_provider_specific_shape
powershell -NoProfile -ExecutionPolicy Bypass -File tools\check-openapi-route-diff.ps1 -FailOnDiff
```

## 边界

- PaperActual 当前完成的是“安全启动门禁 + runner 启动解阻”。provider 下单仍保持显式 submit/query/cancel 审计路由, 没有把策略运行事件自动转换为真实 provider 订单。
- 真实资金 `LiveActual` 自动交易继续延后, 本轮只允许 OKX demo/testnet 边界。
