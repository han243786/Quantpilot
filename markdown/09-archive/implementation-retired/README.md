# Retired Implementation Notes

> Scope: inactive implementation notes and stale generated implementation snapshots.
> Batch: document deadwood cleanup, 2026-06-13.

This archive keeps historical implementation materials that are no longer authoritative for current development.

Move criteria:

1. The document used an old implementation path or described a superseded implementation surface.
2. Active implementation authority already exists under `markdown/03-implementation/`, `governance-next/`, or current milestone records.
3. Remaining references are historical, archive-local, or compatibility notes rather than current ownership links.

Do not use files in this directory as current implementation instructions. Treat them as historical context only.
