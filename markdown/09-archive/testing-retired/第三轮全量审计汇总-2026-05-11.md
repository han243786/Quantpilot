# 第三轮全量审计汇总

> 2026-05-11 | 5 Agent: 集成链路 / 状态机 / 并发竞态 / 资源清理 / 序列化兼容

---

## 阻断级 (S0)

| # | 来源 | 发现 |
|---|------|------|
| 1 | 集成链路+状态机 | **`validate_dag()` 在编译管道中从未被调用** — DAG 环检测是死代码，含分支/环的图编译通过无警告 |

## 高优先级 (P1)

| # | 来源 | 发现 |
|---|------|------|
| 2 | 序列化 | 全库 0 处 `#[serde(alias)]` — 字段重命名后旧持久化数据直接反序列化失败 |
| 3 | 资源清理 | `main.rs:709` tokio::spawn 后台任务 JoinHandle 未保存 — 无法 abort |
| 4 | 资源清理 | SSE EventSource 在组件卸载时未 close (非 stopRuntime 路径) |
| 5 | 状态机 | PluginLifecycleState 无状态转移警卫 — 允许 Registered→Stopped、Faulted→Stopped |
| 6 | 状态机 | OrderStatus 是被动数据字段 — 无状态机强制，非法跳转不被阻止 |
| 7 | 序列化 | `credential_vault.rs` `unwrap_or_default()` 静默退化空保险库 |

## 中优先级 (P2)

| # | 来源 | 发现 |
|---|------|------|
| 8 | 并发 | `main.rs:841,870` 嵌套写锁 (approval_records + ai_proposals) — 未来重构死锁风险 |
| 9 | 并发 | `runtime_api.rs:2932` 长时间持有 runs 读锁 — 高负载饿死写入者 |
| 10 | 序列化 | 前端 localStorage 无 schema_version — 升级后缓存不兼容 |
| 11 | 集成 | `runtime_api.rs:294` expect 有前置逻辑保障但无文档说明 |
| 12 | 状态机 | 前端 RuntimeState 无 "reconnecting" 状态 |
| 13 | 状态机 | `actix`/`deactivate` 无条件覆盖当前状态而非校验合法转移 |
| 14 | 资源清理 | `credential_vault get_service` 返回未包装 Zeroizing 的克隆 String |

## 低优先级 (P2-)

| # | 来源 | 发现 |
|---|------|------|
| 15 | 并发 | Atomic ordering 部分使用 SeqCst 过度保守 |
| 16 | 序列化 | `normalizeGraphShape` 无写入升级机制 |

---

## 三回合审计汇总

| | Round 1 (角色) | Round 2 (深度) | Round 3 (系统) | 合计 | 已修 |
|---|:--:|:--:|:--:|:--:|:--:|
| S0 | 5 | 3 | 1 | 9 | 8 ✅ |
| P1 | 9 | 8 | 6 | 23 | 19 ✅ |
| P2 | 14 | 14 | 7 | 35 | 22 ✅ |
| P2- | — | — | 2 | 2 | — |
| **合计** | **28** | **25** | **16** | **69** | **49** |
