# 第二轮全量审计汇总

> 2026-05-11 | 5 Agent: 文档一致性 / 性能内存 / 错误处理链 / i18n文本 / 代码重复结构

---

## 阻断级 (S0)

| # | 来源 | 发现 |
|---|------|------|
| 1 | 文档 | 系统架构手册 struct 字段大面积与实际代码不一致 (KlineSeriesSnapshot/QuoteSnapshot/AgentDecision/PortfolioState/SignalSide) |
| 2 | 错误处理 | SSE 断开无自动重连 — 前端 `onerror` 仅 close + 设 failure, 不重试 |
| 3 | i18n | en-US locale 完全为空, 英文语言切换后仍显示中文 |

## 高优先级 (P1)

| # | 来源 | 发现 |
|---|------|------|
| 4 | 性能 | `alert_engine.rs:116,123,157` — `clone()` 整个规则向量, 仅需引用 |
| 5 | 性能 | `runtime_event_projection.rs:51-52` — payload `clone()` 两次冗余 |
| 6 | 错误处理 | `data_module.rs:567,704` — Mutex poison 静默吞, `.ok()?` 无日志 |
| 7 | 错误处理 | `plugin_runtime_registry.rs:253` — 原始 `eprintln!` 绕过 `safe_eprintln!` 脱敏 |
| 8 | 代码结构 | `compute_dir_size_sync` 在 `alert_engine.rs` 和 `runtime_api.rs` 中完全重复 |
| 9 | 代码结构 | `frontend_runtime_mapping.rs:7/368/376` — dead_code 标记的废弃代码应删除 |
| 10 | 文档 | README 写 `/api/compile` 但实际路由是 `/api/runtime/compile` |
| 11 | 文档 | README 遗漏 8 组已注册的 API 路由 |

## 中优先级 (P2)

| # | 来源 | 发现 |
|---|------|------|
| 12 | 性能 | 10 处 `Vec::new() + push()` 循环缺少 `with_capacity()` |
| 13 | 性能 | `runtime_event_projection.rs` — 每个事件 `format!()` 构建显示字符串 |
| 14 | 错误处理 | `credential_vault.rs` 7 处 `|_|` 丢弃加密错误详情 |
| 15 | 错误处理 | `graph_api.rs:465-468` — `latest.json` 损坏时静默跳过无告警 |
| 16 | 错误处理 | SSE 后端无连接超时/健康检测端点 |
| 17 | i18n | 30+ JS/JSX 文件硬编码中文未用 `t()` 包裹 |
| 18 | 代码结构 | `runtime_validation.rs` 5 次重复 `unsupported_module` 校验循环 |
| 19 | 代码结构 | 测试辅助函数 `assert_complete_event_envelopes` / `field_names` 重复定义 |
| 20 | 文档 | CLI 文档写 `strategy-ir compile` 实际命令是 `strategy-ir validate` |

## 低优先级 (P2-)

| # | 来源 | 发现 |
|---|------|------|
| 21 | 性能 | `quantscript/script.rs` 多处 `.trim().to_string()` 不必要分配 |
| 22 | 性能 | BTreeMap vs HashMap 取舍 (现状可接受, 后续可优化) |
| 23 | 代码结构 | workspace `sha2` 依赖独立解析, 应统一为 workspace 级别 |
| 24 | 代码结构 | `FrontendRuntimeConfig` 等接口类型应下沉到子 crate |
| 25 | i18n | `safe_log.rs` 硬编码 6 个脱敏 pattern, 自定义字段可能泄露 |

---

## 两轮审计对比

| | 第一轮 (8 Agent) | 第二轮 (5 Agent) |
|---|:--:|:--:|
| 角度 | 角色诱错 (功能边界) | 质量深度 (代码+文档) |
| S0 | 5 | 3 |
| P1 | 9 | 8 |
| P2 | 14 | 14 |
| 合计 | 28 | 25 |

## 累计

| 严重度 | 第一轮 | 第二轮 | 总计 | 已修复 |
|--------|:--:|:--:|:--:|:--:|
| S0 | 5 | 3 | 8 | 5 ✅ |
| P1 | 9 | 8 | 17 | 9 ✅ |
| P2 | 14 | 14 | 28 | 14 ✅ |
| **合计** | **28** | **25** | **53** | **28** |

**剩余**: 第二轮 25 项 (3 S0 + 8 P1 + 14 P2)
