# 多 Agent 全量诱错审计汇总

> 日期: 2026-05-11 | 8 个 Agent 并行审计 | 覆盖: API/存储/编译链/前端/插件/沙盒/配置/组件

---

## 阻断级 (S0) — 必须立即修复

| # | 来源 | 文件:行 | 问题 | 影响 |
|---|------|------|------|------|
| 1 | Plugin | `plugins/builtin/data/kline/plugin.json:12` | 能力合约 ID `"qrpc.data_module_provider"` 与 `PluginCapabilityContract::parse()` 期望的 `"quantpilot.capability.data_module_provider"` 不匹配 — manifest 加载必然失败 | 内置插件无法注册 |
| 2 | API | `runtime_api.rs:294` | `.expect("backtest artifact views should exist")` — 路径不经过填充逻辑时直接 panic | 回测响应 panic |
| 3 | Compiler | `quantscript/src/evaluator.rs:272-278` | `should_skip_inline` 用英文子串匹配，但错误消息已汉化为中文 — 内联容错机制完全失效 | QS 编译误报硬错误 |
| 4 | Config | `Cargo.toml` | 无 `dotenvy`/`dotenv` 依赖 — `.env` 和 `.env.example` 文件永不加载 | 所有环境变量配置无效 |
| 5 | Frontend | `TopToolbar.jsx:168-169` | `graph.metadata.graph_id` 无可选链 — metadata 为 undefined 时抛出 TypeError | 页面白屏崩溃 |

## 高优先级 (P1) — 发布前修复

| # | 来源 | 文件:行 | 问题 | 影响 |
|---|------|------|------|------|
| 6 | Security | `sandbox_verification.rs:270` | URL 参数 `proposal_id` 直接拼路径，未净化 `../../` | 路径遍历任意读写 |
| 7 | API | `rate_limiter.rs` | TokenBucket BTreeMap 永不过期无 eviction | 伪造 IP DoS 内存耗尽 |
| 8 | Plugin | `plugin_market.rs:39,53` | `reqwest::get()` 无超时，`text()` 无长度限制 | 恶意服务 OOM |
| 9 | Plugin | `plugin_runtime_registry.rs:221` | `scan_atoms()` 单文件校验失败 return Err 破坏整体原子性 | 一个坏文件阻止全部注册 |
| 10 | Config | `main.rs:639-651` | 13 个 `create_dir_all` 任一失败崩溃服务器 | 启动失败无恢复 |
| 11 | Config | `main.rs:716-719` | `QUANTPILOT_PORT` 非法值静默回退 3000 无警告 | 用户误以为端口生效 |
| 12 | Frontend | `backtestDetailPage.jsx:69` | `void loadBacktestDetail(id)` 不捕获 Promise 拒绝 | 未捕获异常 |
| 13 | Frontend | `graphStorePersistenceHelpers.js:54` | `localStorage.setItem` 无 try-catch 包裹 QuotaExceededError | 存储满时崩溃 |
| 14 | API | `credential_api.rs:80` | `Path(service)` 直接传入无校验 | 越权删除凭证 |

## 中优先级 (P2) — 下个里程碑

| # | 来源 | 文件:行 | 问题 |
|---|------|------|------|
| 15 | Security | `credential_vault.rs:178,199,209,217,270` | 5 处 `Mutex::lock().unwrap()` — 中毒即 panic |
| 16 | Security | `safe_log.rs:17` | `EXTRA_PATTERNS.lock().unwrap()` 同上 |
| 17 | Runtime | `data_module.rs:111,1359` | `reqwest::Client::builder().build().expect(...)` — TLS 失败 panic |
| 18 | Runtime | `data_module.rs:727` | Mutex poison 时缓存写入静默丢弃 |
| 19 | Storage | `runtime_persistence.rs:27,285,307` | 多处 `run_id`/`backtest_id` 未净化拼路径 |
| 20 | Config | `.env.example:31` vs `alert_engine.rs:297` | 存储水印文档默认 90MB，代码默认 400MB |
| 21 | Frontend | `BacktestDetailPage.jsx:131` | `NaN` 未过滤 → `"NaN%"` 显示 |
| 22 | Frontend | `App.jsx` | `ErrorBoundary.jsx` 已存在但未在 App 中使用 |
| 23 | Plugin | `plugin_runtime_registry.rs:273` | `validate_suite()` 未校验 atom 版本兼容性 |
| 24 | Plugin | `plugins/atoms/` 目录不存在 | `scan_atoms` 硬编码路径与实际目录结构不一致 |
| 25 | API | `rate_limiter.rs:87-91` | 错误消息为英文 `"Too Many Requests"` |
| 26 | Compiler | `evaluator.rs` | AST 递归无 depth limit — 恶意深层嵌套栈溢出 |
| 27 | Compiler | `evaluator.rs:443-463` | for 循环 Range `0..N` 无迭代上限 — 百万级 OOM |
| 28 | Frontend | `CredentialInput.jsx:86-90` | `handleSave` try/finally 无 catch — 保存失败静默 |

---

## 统计

| 严重度 | 数量 |
|--------|:--:|
| 阻断 (S0) | 5 |
| 高 (P1) | 9 |
| 中 (P2) | 14 |
| **合计** | **28** |

## Agent 覆盖

| Agent | 审计区域 | 发现数 |
|-------|---------|:--:|
| API Fuzzer | compile/runtime/credential/graph API, auth, rate_limiter | 8 |
| Storage & Security | storage_lifecycle, credential_vault, sandbox_verification, persistence | 5 |
| Compiler/QS Fuzzer | QS parser/tokenizer/evaluator/lowering, compiler, diagnostics | 4 |
| Frontend UI | CredentialInput, StrategyWorkspacePage, graphStore, TopToolbar, api.js | 5 |
| Plugin System | PluginManifest, registry, market, plugin.json | 6 |
| Sandbox Runtime | sandbox, evaluator, fill_engine, execution, risk, data_module, hotswap | 3 |
| Config & CLI | cli_support, main.rs, app_router, .env, Cargo.toml, auth_middleware | 5 |
| Frontend Components | EventStreamPanel, BacktestDetailPage, StrategyCanvas, i18n, router | 4 |
