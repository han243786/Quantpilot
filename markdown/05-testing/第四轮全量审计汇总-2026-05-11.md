# 第四轮全量审计汇总

> 2026-05-11 | 5 Agent: 数据完整性 / 边界极限 / 启动恢复 / 网络降级 / 多标签隔离

---

## S0 阻断

| # | 来源 | 发现 |
|---|------|------|
| 1 | 边界 | **end_ms < start_ms 不拒绝** — `parse_date_range_ms` 只返回 end_ms，时间倒流静默通过 |
| 2 | 边界 | **全库 0 处 NaN/Inf/Infinity 输入校验** — f64 比较 `> 0.0` 对 NaN 永远 false |
| 3 | 边界 | **100 并发编译无 semaphore** — 前 100 个请求全部放行，CPU 密集操作可撑爆线程池 |

## P1 高优

| # | 来源 | 发现 |
|---|------|------|
| 4 | 多标签 | 双标签可同时启动 Paper 运行 — 无 run_id 互斥锁 |
| 5 | 多标签 | 标签 B 打开已被标签 A 删除的策略 — 无友好降级 |
| 6 | 启动 | 端口冲突报原生 OS 错误 (Address already in use) — 无中文 |
| 7 | 网络 | data_module 429 无退避重试 — 直接降级 mock 数据 |
| 8 | 网络 | 插件市场 404 误报为 "JSON 解析失败" — 未检查 HTTP 状态码 |
| 9 | 数据 | 全库 0 处 `#[serde(deny_unknown_fields)]` — 未知字段静默丢弃 |

## P2 中优

| # | 来源 | 发现 |
|---|------|------|
| 10 | 多标签 | 0 处 `storage` 事件监听 — 跨标签完全盲区 |
| 11 | 边界 | 配额阈值用 `>` 非 `>=` — 450MB 恰好不触发清理 |
| 12 | 边界 | QUANTPILOT_PORT=0 不拒绝 — 保留端口 |
| 13 | 边界 | 回测无 K 线数量上限 — 100 万条 bar 可并发触发 OOM |
| 14 | 网络 | 5xx/4xx 不分临时/永久 — 一律缓存回退 |
| 15 | 启动 | Tauri `wait_for_backend` 端口检查不验证进程归属 |

---

## 四回合累计

| | R1 | R2 | R3 | R4 | 合计 | 已修 |
|---|:--:|:--:|:--:|:--:|:--:|:--:|
| S0 | 5 | 3 | 1 | 3 | 12 | 9 |
| P1 | 9 | 8 | 6 | 6 | 29 | 25 |
| P2 | 14 | 14 | 7 | 6 | 41 | 28 |
| P2- | — | — | 2 | — | 2 | — |
| **合计** | **28** | **25** | **16** | **15** | **84** | **62** |
