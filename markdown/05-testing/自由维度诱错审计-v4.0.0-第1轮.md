# 自由维度诱错审计 — v4.0.0 第 1 轮

> 执行日期: 2026-05-24  
> 执行顺序: `04-Codex执行规范.md` V9  
> 审计方式: Codex 主线程 + 5 个只读 Explorer Agent 并行审计  
> 审计基准 HEAD: `88306bf`  
> 结论: **无 S0**。允许继续 V10 closeout 定稿；P1/P2/P3 必须进入后续版本修复队列。

## 一、执行范围

| 维度 | Agent | 审计主题 | 状态 |
|---|---|---|---|
| A | Volta | 逻辑 / 契约 / GP | 完成，无 S0 |
| B | Hypatia | 并发 / 竞态 | 完成，无 S0 |
| C | Herschel | 边界 / 数值 | 完成，无 S0 |
| D | Copernicus | 持久化 / 回放 | 完成，无 S0 |
| E | James | API / 错误 | 完成，无 S0 |
| 主线程 | Codex | 门禁证据、版本暴露、E2E 吞错交叉验证 | 完成，无 S0 |

本轮 Agent 全部为只读审计，未修改代码。按超级规范化 Agent 规则，未触发“Agent 修改代码后必须运行 cargo check”的附加验证。

## 二、严重度汇总

| 严重度 | 数量 | 本轮处理 |
|---|---:|---|
| S0 | 0 | 无阻断修复项 |
| P1 | 12 | 记录为 v4.0.1 优先修复 |
| P2 | 10 | 记录为 v4.1.0 或门禁补强项 |
| P3 | 3 | 进入持续回归清单 |

## 三、S0 结论

无 S0。

本轮未发现需要在 v4.0.0 当前 closeout 前阻断修复的问题。V8 已在 V9 前通过 23 项全量 closeout 门禁；V9 发现均为 P1/P2/P3，不触发“修复后重新验证全部门禁”的 S0 分支。

## 四、P1 Findings

| ID | 来源 | 文件:行号 | 触发条件 | 修复建议 | GP 条款 |
|---|---|---|---|---|---|
| P1-1 | A/D | `qrpc_runtime/src/v4_runtime.rs:32`, `qrpc_runtime/src/v4_runtime.rs:41`, `qrpc_runtime/src/v4_runtime.rs:1537` | v4 runtime 事件 envelope 只有 `sequence/event_type/source/ts_ms/payload/replayable`，缺少 GP 要求的 `event_id`、显式事件时间和 `freshness`，外部输入也无法携带这些字段 | 在 `V4RuntimeInputEvent` / `V4RuntimeEventEnvelope` 增加 `event_id`、`event_time`、`freshness`；内部事件生成确定性 ID，补 replay/evidence 测试 | GP §1.8 |
| P1-2 | A/C/D | `qrpc_runtime/src/v4_runtime.rs:2035` | transition 声明 `FreshOnly` 时，`transition_freshness_matches` 对所有 freshness requirement 都返回 true，stale/recovering 数据可触发迁移 | 基于事件 freshness/status/age 精确匹配；缺失 freshness 证据时 fail-closed | GP §1.8, §1.9 |
| P1-3 | C | `qrpc_runtime/src/v4_runtime.rs:920` | 构造带 `guard: Some(...)` 的 `MachineTransition` 后，runtime 只按 event type/source/freshness 匹配，不评估 guard，迁移会无条件执行 | 在候选筛选或应用前加入 guard evaluator；不支持的 guard 应拒绝 runtime 启动或拒绝该 transition | GP §1.8 |
| P1-4 | C | `qrpc_runtime/src/v4_runtime.rs:1511`, `qrpc_runtime/src/v4_runtime.rs:1104` | Risk Plane machine emit 任意 `source_kind == RiskPlane` 事件时会自动注入 `risk_plane_approved: true`；若 `risk.rejected` 被接到 Execution，仍可能被当作批准 | approval 必须来自风险机 action/typed payload 明确字段；静态校验禁止非 approval 语义事件接入 Execution | GP §1.9 |
| P1-5 | C | `src/runtime_validation.rs:367`, `src/runtime_validation.rs:433`, `src/runtime_validation.rs:437`, `src/runtime/backtest.rs:215` | `fee_bps = NaN` 或 `slippage_bps = Infinity` 只经过 `< 0.0` 判断，可进入 runtime protocol / sweep grid | 统一要求 `value.is_finite() && value >= 0.0`，并设置合理上限 | GP §10.3, 数值边界 |
| P1-6 | D | `src/runtime/run.rs:32` | `/api/runtime/test-run` 仍走 `RealTimeSandbox::new(RuntimeCoordinator::new(compiled))`，`RunRecord` 不保存 v4 `memory_snapshot` / v4 runtime output，v4 evidence 无法通过运行记录 replay 追溯 | 增加 v4 PaperSimulated API/handoff，或在 `RunRecord` 增加版本化 `v4_runtime` / `memory_snapshot` / `v4_events` 字段并在 detail/replay 返回 | GP §1.8, §10.5 |
| P1-7 | B | `src/runtime/mutation.rs:1053`, `src/runtime/mutation.rs:1061` | 创建 AI proposal 后的后台 sandbox verification 在 `tokio::spawn` 内调用 `Handle::current().block_on(...)` 重入当前 runtime，可能 panic 后被吞掉并重试失败 | async task 内直接 `.await` sandbox verification；需要捕获 panic 时用 `JoinError` 或 future catch unwind，不要重入当前 runtime | GP §8.5 |
| P1-8 | E | `release/release-manifest.yaml:4` | v4.0.0 打包/发布 manifest 仍暴露 `version: 2.0.0` | 同步为 `4.0.0`，并纳入 `tools/check-version-consistency.ps1` | 超级规范化 §8.1 |
| P1-9 | E | `contracts/openapi/root.yaml:4` | OpenAPI `info.version` 和 summary 仍为 `2.3.3`，API 文档消费者会认为 HTTP 契约不是 v4.0.0 | 同步 OpenAPI 活版本声明到 v4.0.0，并纳入版本一致性脚本 | 超级规范化 §8.1, GP §4.4 |
| P1-10 | E | `contracts/openapi/root.yaml:1472` | `/api/credentials` 与 `/api/test/scenario/run` 位于 `components:` 缩进层级内，不在顶层 `paths:` 下，OpenAPI 工具可能忽略真实路由 | 将端点移回顶层 `paths:`，补 OpenAPI lint/解析门禁 | GP §4.4 |
| P1-11 | E | `src/api_errors.rs:62` | `json_bad_request*` 将 JSON 序列化为 `String` 返回，Axum 可能按文本响应处理；部分错误 helper 直接返回纯中文字符串 | 统一返回 `Json<ApiErrorResponse>` 或显式 JSON response；补 content-type/status 回归测试 | GP §4.2, §2.1 |
| P1-12 | 主线程 | `frontend/tests/e2e/scenario-test-v2.spec.js:9`, `frontend/tests/e2e/scenario-test-v2.spec.js:16`, `frontend/tests/e2e/scenario-test-v2.spec.js:18` | `step()` catch 后只打印 `[FAIL]` 不 rethrow；V8 日志中出现 Delete 键步骤失败但 Playwright 进程仍通过 | catch 后 rethrow，或聚合失败并在测试结束显式 fail；截图保留但不能吞掉断言/交互失败 | 超级规范化 §8.5, GP §10.3 |

## 五、P2 Findings

| ID | 来源 | 文件:行号 | 触发条件 | 修复建议 | GP 条款 |
|---|---|---|---|---|---|
| P2-1 | A | `qrpc_core_ir/src/v4.rs:246`, `qrpc_runtime/src/v4_runtime.rs:982` | `MachineActionSpec.memory_writes` 未校验是否属于本 machine 声明 memory；runtime 只要 payload 有同名 key 就写入 | 静态校验 `memory_writes ⊆ machine.memory.name`，runtime 写入前再次按声明字段拒绝未知字段 | GP §1.7, §5.2 |
| P2-2 | A | `quantscript/src/v4_static_audit.rs:542`, `qrpc_core_ir/src/v4.rs:283` | QS `memory <name>: <type>` 的 type 作为字符串透传，Core IR 只检查非空，`made_up` 类型可通过 | 将 memory type 解析为 `QsTypeRef`，或在 audit/Core IR 中调用类型系统契约校验 | GP §1.7, §1.2 |
| P2-3 | C | `qrpc_runtime/src/backtest_metrics.rs:23` | `equity_curve` 时间戳非单调时，`end_ms - start_ms` debug 可能 panic，release 可能 wrap 生成错误 annualized metrics | 使用 `saturating_sub`，并在输入层要求 equity curve 单调递增；非单调返回诊断 | GP §10.3 |
| P2-4 | C | `qrpc_runtime/src/v4_runtime.rs:914` | runtime transition 只看 `event_type/source/freshness`，不按 `MachineEventCatalog.payload_fields` 校验 required/nullable/type | 在 `submit_event` / `process_event` 按 event catalog 校验 payload，失败记录 rejected event 且不推进状态 | GP §1.8 |
| P2-5 | D | `markdown/06-milestones/v4.0.0/03-closeout.md:229` | closeout 版本记录仍是旧 HEAD，与当前审计 HEAD `88306bf` 不一致 | V10 定稿时更新版本记录、审计报告路径和门禁结果到同一 HEAD | 超级规范化 §8.5 |
| P2-6 | B | `src-executor/main.rs:312`, `src-executor/main.rs:326`, `src-executor/main.rs:341`, `src-executor/main.rs:355`, `src-executor/live_runner.rs:203`, `src-executor/live_runner.rs:206`, `src-executor/live_runner.rs:210`, `src-executor/live_runner.rs:229` | 对同一 strategy 并发调用 start，两个请求都可能看到非 Running；`RunnerPool::register` 覆盖 runner 但 `symbol_index` 追加重复 sid，后续事件可能重复投递 | start 状态检查、置 Running、注册 runner 做原子 compare-and-set；`symbol_index` 使用 set 或注册前去重 | GP §8.5 |
| P2-7 | E | `src/api_errors.rs:51` | 默认错误 helper 将 `error_code` 固定为 `None`，大量 `json_bad_request(...)` 缺少稳定诊断码 | 默认 helper 派生或强制传入 `error_code`，中文 message 保持用户展示 | GP §4.2, §2.1 |
| P2-8 | E | `src/alert_engine.rs:169` | acknowledge 不存在告警时返回 `json_bad_request("not_found", ...)`，实际 HTTP 状态为 400 而非 404 | 增加统一 `json_not_found` helper，返回 404 + 结构化错误体 | GP §4.2, §4.4 |
| P2-9 | E | `start.ps1:1` | 启动脚本标题和控制台输出仍显示 `QuantPilot Desktop v2.0.0` | 同步为 v4.0.0，或从单一版本源生成启动横幅 | 超级规范化 §8.1 |
| P2-10 | 主线程 | `tools/check-version-consistency.ps1:37`, `tools/check-version-consistency.ps1:50`, `tools/check-version-consistency.ps1:62` | 版本一致性脚本覆盖 Cargo/Tauri/package/关键文档，但未覆盖 `release/release-manifest.yaml`、`contracts/openapi/root.yaml`、`start.ps1`、executor 启动 banner；V8 可在这些入口漂移时通过 | 扩展脚本检查活版本文件和启动/契约 banner，避免下一次 MAJOR closeout 漏检 | 超级规范化 §8.1 |

## 六、P3 Findings

| ID | 来源 | 文件:行号 | 触发条件 | 修复建议 | GP 条款 |
|---|---|---|---|---|---|
| P3-1 | B | `src/runtime/mutation.rs:1088`, `src/runtime/mutation.rs:1095` | sandbox verification 成功后持有 `approval_records.write().await`，并在锁内 await `persist_approval(...)` 文件 I/O | 锁内只修改并 clone 需持久化对象，释放锁后再 await I/O | GP §8.5 |
| P3-2 | B | `src/alert_engine.rs:152`, `src/alert_engine.rs:163` | acknowledge alert 时持有 `alert_firings.write().await`，并在锁内 await `persist_alert_firing(...)` | 锁内更新并 clone，离开锁作用域后持久化 | GP §8.5 |
| P3-3 | E | `src-executor/main.rs:43` | 执行端启动日志仍输出 `v3.7.0`，发布排障时容易误判执行端版本 | 从 crate/package 版本注入，或在 release manifest 中声明组件版本矩阵 | 超级规范化 §8.1, GP §4.4 |

## 七、处理决定

- S0: 无，当前不需要阻断修复。
- P1: 全部进入 v4.0.1 优先修复队列。若 v4.0.0 要作为对外 release 包发布，应优先修复 P1-8、P1-9、P1-10、P1-11、P1-12。
- P2: P2-5 在 V10 closeout 定稿中当前修复；其余进入 v4.1.0 或门禁补强。
- P3: 进入持续回归与异步锁治理清单。

## 八、证据锚点

| 证据 | 状态 |
|---|---|
| V1 `cargo test --workspace` | 通过 |
| V2 clippy / executor warning budget | 通过 |
| V3 frontend build / vitest / E2E / audit | 通过 |
| V4 executor build / cargo check / test | 通过 |
| V5 scenario smoke | 通过 |
| V6 GP §10.3 11 项回归 | 通过 |
| V7 learning closeout | 通过 |
| V8 23 项 closeout gates | 通过 |
| V9 5 维度并行诱错审计 | 完成，无 S0 |

