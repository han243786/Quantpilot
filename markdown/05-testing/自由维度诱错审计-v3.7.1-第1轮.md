# 自由维度诱错审计 — v3.7.1 第 1 轮

> 审计日期: 2026-05-23 | 维度: A(逻辑/契约/GP) / B(并发/竞态) / C(边界/数值) / D(持久化/迁移) / E(API/错误路径)
>
> 状态: 审计完成；S0/P1/P2/P3 与后续门禁暴露项已按超级规范化闭环，完整 closeout 已通过

---

## 总览

本轮按超级规范化 §3 执行 5 维度并行诱错。所有 Agent 均为只读审计，未修改文件。

| 严重度 | A | B | C | D | E | 去重合计 |
|--------|:--:|:--:|:--:|:--:|:--:|:--:|
| S0 阻断 | 0 | 0 | 0 | 3 | 0 | 3 |
| P1 高 | 2 | 4 | 4 | 3 | 4 | 17 |
| P2 中 | 2 | 2 | 1 | 2 | 2 | 9 |
| P3 低 | 1 | 1 | 0 | 0 | 0 | 2 |
| 合计 | 5 | 7 | 5 | 8 | 6 | 31 |

本地门禁烟测已执行：`tools/check-gates-smoke.ps1` 确认 UTF-8、用户可见文本、正向能力宣称和能力治理快照门禁能够拒绝注入的坏样本。

---

## S0 — 阻断级

| ID | 维度 | 文件:行 | 触发条件 | 影响 | 修复建议 |
|----|------|---------|----------|------|----------|
| S0-1 | D | `tools/cleanup-artifacts.ps1:74` | 执行 `tools/cleanup-artifacts.ps1 -Mode execute -IncludeRuntimeArtifacts` | 会删除 runs/backtests/experiments、graphs JSON/QS 和 versions，违反清理脚本不触碰产品真实数据的治理边界 | 移除或重命名高危开关；只允许清理显式临时前缀；给脚本加静态门禁，断言不会枚举 `graphs/latest.json`、`graphs/versions`、`backtests`、`runs` |
| S0-2 | D | `src/graph_api.rs:425` | 图保存时主图 rename 成功，但 latest、QS 或版本文件 rename 失败 | API 仍返回保存成功并写审计，但版本历史、latest 指针和 QS 工件可能不一致 | 改成事务式保存：所有 staging 文件写入并 fsync 后再 rename；任一步失败回滚；成功后 fsync 父目录再写审计 |
| S0-3 | D | `src/credential_vault.rs:73`, `src-executor/credential_vault_v2.rs:172`, `src/auth/mod.rs:119` | 首次生成机器密钥、执行端机器密钥或 JWT secret 后崩溃 | 内存中使用的新密钥可能未持久化，后续凭证或 token 不可恢复 | 抽统一 `atomic_write_secret_file`，执行 tmp 写入、fsync、rename、父目录 fsync；密钥持久化失败应阻断初始化 |

关联条款：GP §2.6、§2.8、§7.4、§7.5、§10.4；超级规范化 §8.4、§8.8。

---

## S0 修复闭环 — 2026-05-23

| ID | 状态 | 修复证据 | 回归保护 |
|----|:--:|----------|----------|
| S0-1 | ✅ | `tools/cleanup-artifacts.ps1` 废弃并拒绝 `-IncludeRuntimeArtifacts`，仅保留旧 `test-*` 和日志清理 | 新增 `tools/check-cleanup-boundary.ps1`，并接入 CI 与 `tools/run-closeout-gates.bat` |
| S0-2 | ✅ | `src/graph_api.rs` 保存流程改为 staging 文件逐一 fsync、提交阶段逐个替换、任一步失败回滚已替换工件，成功后同步父目录 | 新增 `rollback_restores_replaced_graph_artifact` 聚焦测试 |
| S0-3 | ✅ | `src/storage_lifecycle.rs` 新增 `atomic_write_secret_file`；`src/credential_vault.rs`、`src-executor/credential_vault_v2.rs`、`src/auth/mod.rs` 均改为先原子落盘再进入内存缓存 | 新增 `atomic_secret_write_persists_data_without_stale_tmp`，复验 JWT secret 与 vault 首次创建测试 |

本轮优化已执行的验证：

- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-cleanup-boundary.ps1`
- `cargo fmt --check`
- `cargo check --workspace`
- `cargo test -p quantpilot atomic_secret_write_persists_data_without_stale_tmp`
- `cargo test -p quantpilot rollback_restores_replaced_graph_artifact`
- `cargo test -p quantpilot test_jwt_secret_consistent_across_calls`
- `cargo test -p quantpilot test_load_fresh_vault_creates_file`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-user-facing-text.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-gates-smoke.ps1`
- `git diff --check`

完整 `tools/run-closeout-gates.bat` 已在 2026-05-23 复跑通过，当前报告允许宣称 v3.7.1 closeout 通过。

---

## P1/P2/P3 修复闭环 — 2026-05-23

| ID | 状态 | 修复证据 | 回归保护 |
|----|:--:|----------|----------|
| P1-1 | ✅ | 新增 `tools/check-pre-commit-hook.ps1`，并将 `.git/hooks/pre-commit` 与 `scripts/pre-commit` 同步 | pre-commit 同步门禁已接入 CI 与 `tools/run-closeout-gates.bat` |
| P1-2 | ✅ | 能力治理真源统一到 `markdown/03-implementation/governance/implementation-capability-governance-registry.generated.md`，OpenAPI 与前端支持矩阵纳入渲染校验 | `tools/check-capability-governance.ps1` |
| P1-3 | ✅ | `new_app_state` 从显式 store dir 派生 storage/auth/vault root，测试不再回落真实 `storage` | `cargo check --workspace` 与相关集成测试路径复验 |
| P1-4 | ✅ | 编译缓存 key 纳入 `runtime_config`，避免跨请求串用 compile metadata | `compile_cache_key_includes_runtime_config` |
| P1-5 | ✅ | 回测 volatility 改为 guard 设置并恢复，避免全局状态残留 | `cargo check --workspace` |
| P1-6 | ✅ | live/testnet 限流改为互斥预约，避免并发穿透 | `live_execution` 与 `global_rate_limiter_is_keyed_by_runtime_config` |
| P1-7 | ✅ | FillEngine 保存订单级 slippage/fee，并在成交价和费用中应用 | `order_level_slippage_changes_market_fill_price` |
| P1-8 | ✅ | auth limiter 调整为 6/minute 语义 | `global_rate_limiter_is_keyed_by_runtime_config` |
| P1-9 | ✅ | live sell 按可用持仓校验，buy 按现金+手续费校验 | `place_order_rejects_sell_without_available_position` |
| P1-10 | ✅ | IOC/GTC/FOK 买单统一做 cash cap，允许 partial 时裁剪可成交数量 | `gtc_buy_order_is_capped_by_available_cash` |
| P1-11 | ✅ | 已保存 backtest artifacts 加载改为强校验 manifest 引用、文件存在和 digest | `cargo check --workspace` |
| P1-12 | ✅ | `backtest_id` 增加路径段白名单校验，拒绝 `..`、分隔符和 NUL | `validate_backtest_id_segment_rejects_path_traversal` |
| P1-13 | ✅ | 备份源补齐 auth/executor 关键状态，清理配额改为递归计量 | `permanent_sources_include_auth_and_executor_state` |
| P1-14 | ✅ | 后端 migration sender 与 executor dev proxy 改用本地 session key 签名 | `npm.cmd run build` 与 executor 签名测试 |
| P1-15 | ✅ | executor HMAC 绑定 method/path/query/timestamp/body，并加入短窗 replay cache | `request_signature_requires_timestamp`、`request_signature_binds_path_and_rejects_replay` |
| P1-16 | ✅ | JSON rejection middleware 仅处理 extractor rejection，保留 handler 结构化 JSON 错误 | `preserves_structured_handler_bad_request_json` |
| P1-17 | ✅ | `/api/capabilities` 加入只读公开白名单，避免前端首屏退回 cache/safe_fallback | `test_capabilities_path_is_whitelisted` |

| ID | 状态 | 修复证据 | 回归保护 |
|----|:--:|----------|----------|
| P2-1 | ✅ | 功能演进豁免只允许出现在 `## 非目标` 范围内 | `tools/check-feature-evolution.ps1` |
| P2-2 | ✅ | GP、README、overview、closeout 文档同步到 v3.7.1 与 21 项 closeout gates | `tools/check-version-consistency.ps1` |
| P2-3 | ✅ | qrpc session key 默认落到 storage root，load/init 对同 key 幂等并加初始化锁 | `cargo test -p qrpc-session` |
| P2-4 | ✅ | 全局 limiter 按运行配置分桶，避免 OnceLock 配置漂移 | `global_rate_limiter_is_keyed_by_runtime_config` |
| P2-5 | ✅ | executor 热调参后端校验参数存在性、类型、有限数值、范围与字符串长度 | `cargo check --workspace` |
| P2-6 | ✅ | snapshot restore 审计改为持久化 audit JSON 后再返回响应 | `cargo check --workspace` |
| P2-7 | ✅ | executor strategy register 持久化错误不再吞掉，状态写入改为临时文件、fsync、bak 回滚 | `persist_to_path_writes_state_without_tmp_or_bak_leftovers` |
| P2-8 | ✅ | SPA fallback 前增加 `/api/*path` JSON 404 | `unknown_api_route_returns_json_404_instead_of_spa_fallback` |
| P2-9 | ✅ | executor strategy-scoped read handler 入口校验 strategy_id 存在 | `cargo check --workspace` |

| ID | 状态 | 修复证据 | 回归保护 |
|----|:--:|----------|----------|
| P3-1 | ✅ | closeout 文档拆分“脚本已收口”和“完整 closeout 未通过”的状态口径 | 文档一致性复验 |
| P3-2 | ✅ | env helper 通过 `catch_unwind`/guard 在 panic 时恢复环境变量 | `cargo check --workspace` |

---

## 追加诱错收口 — 2026-05-23

完整 closeout 复跑继续暴露了 E2E 与运行时状态机问题，本轮已追加收口：

| 问题 | 状态 | 修复证据 | 回归保护 |
|------|:--:|----------|----------|
| Playwright 只启动 Vite，E2E 通过代理访问后端时 3000 无服务 | ✅ | `frontend/playwright.config.js` 同时启动 `quantpilot-api` 与 Vite | `npm.cmd run test:e2e` |
| E2E API harness 把 `/src/api/client.js` 误判为 `/api/*` 后端请求 | ✅ | `frontend/tests/e2e/support/apiHarness.js` 改为按 URL pathname 识别真实 `/api/` 请求 | `editor-capabilities-smoke.spec.js` |
| 后台任务在 Tokio runtime 内再次 `block_on`，E2E 启动后端时 panic | ✅ | `src/lib.rs` 后台循环直接 await async 任务，仅同步清理保留 `catch_unwind` | `tools/run-closeout-gates.bat` |
| runtime/backtest 启动先持有 `runtime` 锁再内部编译，`compileCurrentGraph` 被锁短路 | ✅ | `frontend/src/store/graphStoreRuntimeSessionActions.js` 调整为先编译、后进入 runtime 锁 | `graphStore.runtimeErrors.test.js` 新增锁顺序回归 |
| SSE `run_completed` 与 50ms 事件批处理竞态，完成态会被后续批处理覆盖回 running | ✅ | completion 前主动 `flushBatch()`，并显式保留 runId / transient artifact 状态 | `keeps completion after run_completed arrives before the event batch timer` |
| workspace E2E mock 缺少 mutations/reports，详情页与研究页出现未声明 API 请求 | ✅ | `workspaceBootstrapMocks.js` 补齐 `/api/runtime/mutations` 与 `/api/runtime/reports` | run/backtest smoke |
| 工作区 tab 与 toolbar 入口漂移，E2E 仍按旧 `overview/diagnostics/code toolbar` 语义断言 | ✅ | editor/run/backtest/scenario v2 均改为当前 `dashboard/code/research/source` 与研究页 toolbar 语义；test bridge 兼容 `ad-tab--active` | `npm.cmd run test:e2e` |
| 旧 `scenario-test.spec.js` 为人工探索脚本，内部 catch 失败且会超时污染自动门禁 | ✅ | 文件显式 `test.skip`，确定性覆盖由 smoke specs 与 `scenario-test-v2` 承担 | `npm.cmd run test:e2e` 显式 skipped |
| 视觉回归读取 live 运维状态，`/api/v1/snapshots` 等本地数据会导致基线漂移 | ✅ | `visual-regression.spec.js` 改为固定 capabilities、workspace bootstrap、alerts、snapshots、runbook API fixture，并启用未声明 API 守卫；同步刷新 MS Edge Windows 基线 | `npx.cmd playwright test tests/e2e/visual-regression.spec.js --update-snapshots` 与 `npm.cmd run test:e2e` |

---

## P1 — 高优先级

| ID | 维度 | 文件:行 | 触发条件 | 影响 | 修复建议 |
|----|------|---------|----------|------|----------|
| P1-1 | A | `.git/hooks/pre-commit:8`, `scripts/pre-commit:8`, `README.md:118` | 本地执行 `git commit` | 实际安装 hook 与版本化脚本漂移，缺少 UTF-8、`cargo fmt --check` 和测试包装入口，rustfmt drift 仍可进入仓库 | 重新安装或同步 `.git/hooks/pre-commit`；增加只读门禁检查实际 hook 与 `scripts/pre-commit` 一致 |
| P1-2 | A/E | `tools/check-capability-governance.ps1:2`, `markdown/03-implementation/...generated.md:20`, `contracts/openapi/root.yaml:1168`, `frontend/src/capabilities/supportMatrix.js:23` | 修改能力治理、生成客户端或按支持矩阵判断能力 | 活跃 `03-implementation` 注册表、旧 `markdown/implementation` 注册表、OpenAPI、前端支持矩阵存在 split-brain | 统一能力快照路径为 `markdown/03-implementation/...`；把 OpenAPI 和支持矩阵 drift 纳入能力治理门禁 |
| P1-3 | B | `src/app_runtime_helpers.rs:51`, `tests/common/mod.rs:47` | 并行集成测试创建临时目录 | `new_app_state` 仍从硬编码 `storage` 加载凭证库和 auth.db，测试会污染真实 storage 或互相串扰 | AppState 接收显式 storage root；测试所有子系统从临时 root 派生 |
| P1-4 | B | `src/compile_api.rs:21`, `src/compile_api.rs:67`, `src/compile_api.rs:252` | 相同 graph_json、不同 compile_id/runtime_config 请求命中缓存 | 第二个请求拿到第一个请求的 compile_id、runtime_config 或 artifact metadata | 缓存只存纯编译结果，或把响应相关字段纳入 cache key |
| P1-5 | B | `src/test_runner.rs:351`, `src/test_runner.rs:378`, `qrpc_runtime/src/data_module.rs:1285` | 场景配置 `@backtest { volatility: ... }` | 回测先执行再写全局 volatility，后续场景读到残留值，结果依赖顺序 | volatility 作为回测请求参数传入；短期用 RAII guard 设置并恢复全局值 |
| P1-6 | B | `qrpc_runtime/src/live_execution.rs:181`, `src/test_runner.rs:771` | 多线程同时发 OKX/live/testnet 请求 | 限流不是原子预约，并发请求可穿透间隔限制 | 使用 `Mutex<Instant>` 串行化，或 `compare_exchange` 原子预约下一次允许时间 |
| P1-7 | C | `qrpc_runtime/src/slippage.rs:373`, `qrpc_runtime/src/fill_engine.rs:823`, `src/runtime/backtest.rs:102` | 设置 `slippage_bps` 或回测滑点覆盖 | 工件宣称滑点变化，但成交价仍使用默认假设，指标和 A/B 比较误导 | FillEngine 使用订单级或 resolved execution assumptions；补不同滑点产生不同成交价/权益的测试 |
| P1-8 | C | `src/rate_limiter.rs:158`, `markdown/General_Policy.md:108` | 认证端点连续请求 | GP 要求 6 次/分钟/IP，当前约 60 次/分钟，暴力尝试阈值扩大 10 倍 | 实现分钟级 auth bucket，容量 6、补充 0.1 token/s |
| P1-9 | C | `qrpc_runtime/src/live_execution.rs:312`, `qrpc_runtime/src/live_execution.rs:759` | 现货卖出持仓但现金不足，或无持仓但现金充足 | 卖单错误按现金拒绝；无持仓卖出缺少提交前库存校验 | 按 side 分支：买单校验现金，卖单校验可用持仓和冻结数量 |
| P1-10 | C | `qrpc_runtime/src/fill_engine.rs:173`, `qrpc_runtime/src/fill_engine.rs:201`, `qrpc_runtime/src/fill_engine.rs:244` | IOC/GTC 买单用满现金且滑点/手续费抬高成交价 | 只有 FOK 检查现金，IOC/GTC 可能产生负现金 | 所有买单 TIF 做 affordability 校验；允许 partial 时按可支付数量裁剪 |
| P1-11 | D | `src/backtest_artifacts.rs:681` | 已保存回测目录中的 artifacts 缺失、损坏或被篡改 | 只要 manifest 可读就静默降级为空工件，审计证据可被削弱 | 加载已保存工件时强校验文件存在、schema_version 和 digest；legacy 降级必须显式标记 |
| P1-12 | D | `src/backtest_artifacts.rs:306`, `src/runtime_persistence.rs:334` | backtest_id 来自旧清单、导入或 URL 解码且含路径分隔符 | 持久化/加载路径边界不一致，未来迁移或导入可能越界 | 新增 `validate_backtest_id`，拒绝 `..`、`/`、`\\`；canonicalize 后必须位于 store dir 内 |
| P1-13 | D | `src/backup.rs:12`, `src/backup.rs:143` | 每日备份运行 | 备份遗漏 `.jwt_secret`、executor 状态；敏感密钥材料同根备份；配额只看目录 metadata | 建立可恢复性矩阵；敏感备份加密或分离；递归计算配额并按创建时间删除 |
| P1-14 | E | `src-executor/api_guard.rs:12`, `src/migration_sender.rs:153`, `frontend-executor/src/ExecutorApp.jsx:23` | 默认安全模式下部署到执行器、查询策略、start/stop 或 SSE | executor guard 启用，但主后端和 executor 前端没有签名通道，核心执行区默认 401 | 建立统一 executor API client 或后端代理；SSE 使用短期签名 query/cookie 或代理 |
| P1-15 | E/C | `src-executor/api_guard.rs:59`, `qrpc_session/src/lib.rs:124` | 复用有效签名请求或省略 `x-executor-timestamp` | timestamp 可选，签名只绑定 body，防重放和路径绑定不成立 | timestamp 必填并参与 HMAC；签名输入包含 method、path、timestamp、nonce、body digest |
| P1-16 | E | `src/lib.rs:969`, `src/runtime/run.rs:12`, `src/api_errors.rs:43` | handler 返回结构化 400 | JSON rejection middleware 按状态码重写 400，吞掉 details/partial_artifacts | 只处理 Axum extractor rejection；保留 handler 已生成的 JSON 错误响应 |
| P1-17 | E | `src/auth_middleware.rs:22`, `frontend/src/store/graphStore.js:99`, `frontend/src/api/client.js:67` | 非 DEV 启动时前端首屏调用 `/api/capabilities` | 能力真源 401，前端退到 cache/safe_fallback，用户看到能力失败而不是登录/API key 引导 | `/api/capabilities` 白名单只读公开，或前端统一注入 Authorization |

---

## P2 — 中优先级

| ID | 维度 | 文件:行 | 问题 | 修复建议 |
|----|------|---------|------|----------|
| P2-1 | A | `tools/check-feature-evolution.ps1:82` | 任意位置出现“不新增功能”即可跳过功能演进结构校验 | 只允许在 `## 非目标` 中识别该豁免，并检测扩展词与豁免并存 |
| P2-2 | A | `markdown/01-principles/README.md:6`, `markdown/10-overview/overview-current-status-and-roadmap.md:157` | 原则索引和 overview 仍以旧 GP 条款数/版本口径描述当前规则 | 同步 GP 版本、条款数和阻断数；纳入版本一致性检查 |
| P2-3 | B | `qrpc_session/src/lib.rs:16`, `qrpc_session/src/lib.rs:36` | 会话密钥使用同一系统临时文件，OnceLock 不允许多会话重置 | 文件路径加入 session id/port/pid；`load_session_key` 对同一 key 幂等 |
| P2-4 | B | `src/rate_limiter.rs:75`, `src/rate_limiter.rs:158` | limiter 用全局 OnceLock，运行期或测试修改配置不生效 | 将 limiter 放入 AppState 或 middleware layer，测试使用独立实例 |
| P2-5 | C | `src-executor/main.rs:512`, `frontend-executor/src/components/StrategyParamsPanel.jsx:29` | 热调参后端只校验 JSON 基本类型，不按 schema 校验有限数/范围 | 从策略参数 schema/core_ir 派生类型和 min/max，后端拒绝类型漂移和越界值 |
| P2-6 | D | `src/snapshot_service.rs:177` | snapshot restore 响应声称记录审计日志，但实现只有 `safe_eprintln!` | 新增持久化 restore audit，记录 actor、snapshot_id、signature 和恢复结果 |
| P2-7 | D | `src-executor/executor_state.rs:147` | executor 注册策略后吞掉 persist 错误，原子写入缺 fsync/bak | 注册返回持久化失败或写 audit；统一原子写入并保留 bak 恢复 |
| P2-8 | E | `src/app_router.rs:33`, `src/app_router.rs:42` | 未知 `/api/*` 路由可能落到 SPA fallback 返回 HTML | 在 SPA fallback 前增加 `/api/*path` JSON 404 |
| P2-9 | E | `src-executor/main.rs:381`, `src-executor/main.rs:397` | executor strategy-scoped 读接口不校验 strategy_id | handler 入口检查策略存在；未知 id 返回标准 JSON 404 |

---

## P3 — 低优先级

| ID | 维度 | 文件:行 | 问题 | 修复建议 |
|----|------|---------|------|----------|
| P3-1 | A | `markdown/06-milestones/v3.7.1/03-closeout.md:38`, `markdown/06-milestones/v3.7.1/03-closeout.md:63` | closeout 交付表把脚本收口标 ✅，验证表又标完整 closeout ❌，容易误读 | 状态位拆成“脚本已收口”和“执行未通过见验证表” |
| P3-2 | B | `src/auth_middleware.rs:131`, `qrpc_runtime/src/plugin_market.rs:257` | env helper 闭包 panic 时不会恢复环境变量 | 用 RAII env guard，在 Drop 中恢复旧值 |

---

## 门禁与验证

已执行：

- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-gates-smoke.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-pre-commit-hook.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-capability-governance.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-feature-evolution.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-cleanup-boundary.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-version-consistency.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-utf8.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-user-facing-text.ps1`
- `powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-executor-warning-budget.ps1 -MaxWarnings 47`
- `cargo fmt`
- `cargo fmt --check`
- `cargo check --workspace`
- `npm.cmd run build`（`frontend-executor`）
- `npx.cmd playwright test tests/e2e/visual-regression.spec.js --update-snapshots`
- `npm.cmd run test:e2e`
- `cargo test -p qrpc-session`
- `cargo test -p qrpc-runtime fill_engine`
- `cargo test -p qrpc-runtime live_execution`
- `cargo test -p quantpilot compile_cache_key_includes_runtime_config`
- `cargo test -p quantpilot capabilities`
- `cargo test -p quantpilot unknown_api_route_returns_json_404_instead_of_spa_fallback`
- `cargo test -p quantpilot preserves_structured_handler_bad_request_json`
- `cargo test -p quantpilot validate_backtest_id_segment_rejects_path_traversal`
- `cargo test -p quantpilot global_rate_limiter_is_keyed_by_runtime_config`
- `cargo test -p quantpilot permanent_sources_include_auth_and_executor_state`
- `cargo test -p quantpilot --bin executor request_signature`
- `cargo test -p quantpilot --bin executor persist_to_path_writes_state_without_tmp_or_bak_leftovers`
- `cargo test -p quantpilot atomic_secret_write_persists_data_without_stale_tmp`
- `cargo test -p quantpilot rollback_restores_replaced_graph_artifact`
- `cargo test -p quantpilot test_jwt_secret_consistent_across_calls`
- `cargo test -p quantpilot test_load_fresh_vault_creates_file`
- `git diff --check`

结果：通过。该烟测注入并确认以下坏样本被拒绝：

- UTF-8 BOM 文件
- 用户可见乱码片段
- 未白名单正向能力宣称
- 能力治理快照漂移

完整 `tools/run-closeout-gates.bat` 已执行并通过 21/21 项。该结果覆盖 UTF-8、用户可见文本、能力治理、功能演进、hook 同步、清理边界、Rust 格式、Rust workspace 测试、前端构建/单测/E2E、executor 与 QS 场景 smoke。

---

## 处理建议

1. 本报告发现的 31 项已完成代码、文档、门禁或测试闭环。
2. 后续版本推进时，不允许把新增功能、能力治理、pre-commit hook、清理边界、OpenAPI/front-end 支持矩阵从对应门禁中绕开。
3. 完整 closeout 仍以 `tools/run-closeout-gates.bat` 实际结果为准；当前 2026-05-23 本地结果为通过，后续改动必须重新执行。
