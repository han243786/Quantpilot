# RFC-012 Order Protocol

订单协议定义系统如何表示一个面向交易场地的具体订单对象。它需要覆盖订单身份、方向、价格、数量、订单类型、时效规则、状态和关联来源。协议既要能表达系统内部准备发送的订单，也要能表达交易所已经接受并返回状态的订单。

订单协议要求把订单语义与执行请求语义区分开。执行请求表达目标，订单表达具体动作。订单协议还应保留场地原始标识和系统内部标识，便于在回报、撤单、重试和审计中建立稳定关联。对于部分成交、改单和撤单，也应有清晰的状态表达方式。

本协议是执行链路中的基础对象。没有统一订单协议，交易所差异会直接渗入策略和风控层，系统的可维护性会迅速下降。